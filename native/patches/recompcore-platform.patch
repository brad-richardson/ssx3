diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9f837ec..733f9f8 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -661,8 +661,9 @@ if(ENABLE_VULKAN)
   endif()
 endif()
 
-if(NOT (WIN32 AND _M_ARM_64))
-  # OpenGL is available on all platforms except windows-arm64
+if(NOT (WIN32 AND _M_ARM_64) AND NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
+  # OpenGL is available on all platforms except windows-arm64 and Apple mobile
+  # (Metal-only there; the AGL/EGL interfaces are not built).
   add_definitions(-DHAS_OPENGL)
 endif()
 
@@ -715,7 +716,7 @@ if(ENABLE_CUBEB)
   add_definitions(-DHAVE_CUBEB)
 endif()
 
-if(NOT ANDROID)
+if(NOT ANDROID AND NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
   dolphin_find_optional_system_library(LibUSB Externals/libusb)
   add_definitions(-D__LIBUSB__)
 endif()
@@ -743,7 +744,7 @@ dolphin_find_optional_system_library(MBEDTLS Externals/mbedtls 2.28)
 
 dolphin_find_optional_system_library(CURL Externals/curl)
 
-if(NOT ANDROID)
+if(NOT ANDROID AND NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
   dolphin_find_optional_system_library(HIDAPI Externals/hidapi)
 endif()
 
diff --git a/GXRuntime/include/core/cpu_fast_fp.h b/GXRuntime/include/core/cpu_fast_fp.h
new file mode 100644
index 0000000..1a59985
--- /dev/null
+++ b/GXRuntime/include/core/cpu_fast_fp.h
@@ -0,0 +1,294 @@
+/* Inline floating-point fast paths for generated chunks.
+ *
+ * Forced-included (-include) before each generated chunk when the module is
+ * built with RECOMPCORE_FAST_FP. Function-like macros redirect the chunk's
+ * calls to the arithmetic helpers below; every other translation unit,
+ * including the interpreter and the helpers themselves, is unchanged.
+ *
+ * Value semantics for finite operands are exactly the helpers': the same
+ * double-precision operation, the same 25-bit rounding of the C operand for
+ * multiplies, the same single rounding, the same NI flush of tiny results.
+ * A NaN result, or any enabled FPSCR exception (VE/OE/UE/ZE/XE), falls back
+ * to the exact helper so payload propagation and exception state stay the
+ * same. What the fast path deliberately omits is FPSCR bookkeeping for
+ * non-exceptional results: FPRF classification, FI/FR and sticky inexact,
+ * zero-divide and invalid flags. That matches Dolphin's JIT defaults (FPRF
+ * off, AccurateNaNs off), which is the accuracy every Dolphin user runs.
+ */
+#pragma once
+#include "core/cpu.h"
+#include <math.h>
+#include <string.h>
+
+#define GX_FASTFP_INLINE static inline __attribute__((always_inline))
+#define GX_FASTFP_UNLIKELY(x) __builtin_expect(!!(x), 0)
+#define GX_FASTFP_ANY_E 0xF8u  /* VE OE UE ZE XE */
+#define GX_FASTFP_NI 0x04u
+
+GX_FASTFP_INLINE u64 gx_fastfp_bits(f64 v) { u64 b; memcpy(&b, &v, 8); return b; }
+GX_FASTFP_INLINE f64 gx_fastfp_f64(u64 b) { f64 v; memcpy(&v, &b, 8); return v; }
+GX_FASTFP_INLINE bool gx_fastfp_strict(const CPUState* cpu) { return (cpu->fpscr & GX_FASTFP_ANY_E) != 0; }
+
+/* force_single: NI flushes |value| < 2^-126 to a signed zero, then rounds. */
+GX_FASTFP_INLINE f32 gx_fastfp_force_single(const CPUState* cpu, f64 value) {
+    if (cpu->fpscr & GX_FASTFP_NI) {
+        const u64 bits = gx_fastfp_bits(value);
+        if ((bits & 0x7FFFFFFFFFFFFFFFull) < 0x3810000000000000ull) {
+            const u32 flushed = (u32)((bits & 0x8000000000000000ull) >> 32);
+            f32 r; memcpy(&r, &flushed, 4); return r;
+        }
+    }
+    return (f32)value;
+}
+
+/* force_25bit_c: C-operand rounding used by fmuls and the paired-single ops. */
+GX_FASTFP_INLINE f64 gx_fastfp_force_25bit_c(f64 d) {
+    u64 integral = gx_fastfp_bits(d);
+    const u64 exponent = integral & 0x7FF0000000000000ull;
+    const u64 fraction = integral & 0x000FFFFFFFFFFFFFull;
+    if (exponent == 0 && fraction != 0) {
+        s64 keep_mask = (s64)0xFFFFFFFFF8000000ll;
+        u64 round = 0x8000000u;
+        const unsigned shift = (unsigned)__builtin_clzll(fraction) - 11u;
+        keep_mask >>= shift;
+        round >>= shift;
+        integral = (integral & (u64)keep_mask) + (integral & round);
+    } else {
+        integral = (integral & 0xFFFFFFFFF8000000ull) + (integral & 0x8000000ull);
+    }
+    return gx_fastfp_f64(integral);
+}
+
+/* force_25_bit: C-operand rounding used by the scalar fused multiply-add. */
+GX_FASTFP_INLINE f64 gx_fastfp_force_25_bit(f64 value) {
+    u64 bits = gx_fastfp_bits(value);
+    const u64 fraction = bits & 0x000FFFFFFFFFFFFFull;
+    u64 keep_mask = 0xFFFFFFFFF8000000ull;
+    u64 round = 0x0000000008000000ull;
+    if ((bits & 0x7FF0000000000000ull) == 0 && fraction != 0) {
+        const unsigned shift = (unsigned)__builtin_clzll(fraction) - 11u;
+        if (shift < 28) {
+            keep_mask = ~((1ull << (27 - shift)) - 1);
+            round >>= shift;
+        } else {
+            keep_mask = ~0ull;
+            round = 0;
+        }
+    }
+    bits = (bits & keep_mask) + (bits & round);
+    return gx_fastfp_f64(bits);
+}
+
+/* Single-precision fused multiply-add as ni_madd_msub computes it (value only). */
+GX_FASTFP_INLINE f64 gx_fastfp_madd_single(f64 a, f64 c, f64 b, bool sub) {
+    const f64 c_round = gx_fastfp_force_25bit_c(c);
+    const f64 b_sign = sub ? -b : b;
+    f64 result = fma(a, c_round, b_sign);
+    const u64 result_bits = gx_fastfp_bits(result);
+    if ((result_bits & 0x000000001FFFFFFFull) == 0x0000000010000000ull) {
+        const f64 a_prime = b_sign - result;
+        const f64 b_prime = result + a_prime;
+        const f64 error = fma(a, c_round, a_prime) + (b_sign - b_prime);
+        if (error != 0.0)
+            result = gx_fastfp_f64(((error > 0.0) == (result > 0.0)) ? result_bits + 1 : result_bits - 1);
+    }
+    return result;
+}
+
+GX_FASTFP_INLINE void gx_fastfp_write_single(CPUState* cpu, u8 d, f32 v) {
+    cpu->fpr[d] = (f64)v;
+    cpu->ps1[d] = (f64)v;
+}
+GX_FASTFP_INLINE void gx_fastfp_write_both(CPUState* cpu, u8 d, f32 ps0, f32 ps1) {
+    cpu->fpr[d] = (f64)ps0;
+    cpu->ps1[d] = (f64)ps1;
+}
+
+/* ---- scalar single ---- */
+GX_FASTFP_INLINE void gx_fastfp_fadds(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fadds(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] + cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fadds(cpu, d, a, b); return; }
+    gx_fastfp_write_single(cpu, d, gx_fastfp_force_single(cpu, r));
+}
+GX_FASTFP_INLINE void gx_fastfp_fsubs(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fsubs(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] - cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fsubs(cpu, d, a, b); return; }
+    gx_fastfp_write_single(cpu, d, gx_fastfp_force_single(cpu, r));
+}
+GX_FASTFP_INLINE void gx_fastfp_fmuls(CPUState* cpu, u8 d, u8 a, u8 c) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fmuls(cpu, d, a, c); return; }
+    const f64 r = cpu->fpr[a] * gx_fastfp_force_25bit_c(cpu->fpr[c]);
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fmuls(cpu, d, a, c); return; }
+    gx_fastfp_write_single(cpu, d, gx_fastfp_force_single(cpu, r));
+}
+GX_FASTFP_INLINE void gx_fastfp_fdivs(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fdivs(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] / cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fdivs(cpu, d, a, b); return; }
+    gx_fastfp_write_single(cpu, d, gx_fastfp_force_single(cpu, r));
+}
+GX_FASTFP_INLINE void gx_fastfp_frsp(CPUState* cpu, u8 d, u8 b) {
+    const f64 value = cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu) || isnan(value))) { ppc_frsp(cpu, d, b); return; }
+    gx_fastfp_write_single(cpu, d, gx_fastfp_force_single(cpu, value));
+}
+
+/* ---- scalar double ---- */
+GX_FASTFP_INLINE void gx_fastfp_fadd(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fadd(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] + cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fadd(cpu, d, a, b); return; }
+    cpu->fpr[d] = r;
+}
+GX_FASTFP_INLINE void gx_fastfp_fsub(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fsub(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] - cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fsub(cpu, d, a, b); return; }
+    cpu->fpr[d] = r;
+}
+GX_FASTFP_INLINE void gx_fastfp_fmul(CPUState* cpu, u8 d, u8 a, u8 c) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fmul(cpu, d, a, c); return; }
+    const f64 r = cpu->fpr[a] * cpu->fpr[c];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fmul(cpu, d, a, c); return; }
+    cpu->fpr[d] = r;
+}
+GX_FASTFP_INLINE void gx_fastfp_fdiv(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_fdiv(cpu, d, a, b); return; }
+    const f64 r = cpu->fpr[a] / cpu->fpr[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r))) { ppc_fdiv(cpu, d, a, b); return; }
+    cpu->fpr[d] = r;
+}
+
+/* ---- fused multiply-add, generated form: returns whether output was written ---- */
+GX_FASTFP_INLINE bool gx_fastfp_fma(CPUState* cpu, f64 a, f64 c, f64 b, bool single,
+                                    bool subtract, bool negative, f64* output) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu)))
+        return ppc_fma(cpu, a, c, b, single, subtract, negative, output);
+    const f64 addend = subtract ? -b : b;
+    f64 result;
+    if (!single) {
+        result = fma(a, c, addend);
+    } else {
+        const f64 rounded_c = gx_fastfp_force_25_bit(c);
+        result = fma(a, rounded_c, addend);
+        u64 bits = gx_fastfp_bits(result);
+        if ((bits & 0x000000001FFFFFFFull) == 0x0000000010000000ull) {
+            const f64 a_prime = addend - result;
+            const f64 b_prime = result + a_prime;
+            const f64 error = fma(a, rounded_c, a_prime) + (addend - b_prime);
+            if (error != 0.0) {
+                if ((error > 0.0) == (result > 0.0)) bits++;
+                else bits--;
+                result = gx_fastfp_f64(bits);
+            }
+        }
+        result = (f64)(f32)result;
+    }
+    if (GX_FASTFP_UNLIKELY(isnan(result)))
+        return ppc_fma(cpu, a, c, b, single, subtract, negative, output);
+    *output = negative ? -result : result;
+    return true;
+}
+
+/* ---- paired single ---- */
+GX_FASTFP_INLINE void gx_fastfp_ps_add(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_add_op(cpu, d, a, b); return; }
+    const f64 r0 = cpu->fpr[a] + cpu->fpr[b], r1 = cpu->ps1[a] + cpu->ps1[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_add_op(cpu, d, a, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_sub(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_sub_op(cpu, d, a, b); return; }
+    const f64 r0 = cpu->fpr[a] - cpu->fpr[b], r1 = cpu->ps1[a] - cpu->ps1[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_sub_op(cpu, d, a, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_mul(CPUState* cpu, u8 d, u8 a, u8 c) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_mul_op(cpu, d, a, c); return; }
+    const f64 r0 = cpu->fpr[a] * gx_fastfp_force_25bit_c(cpu->fpr[c]);
+    const f64 r1 = cpu->ps1[a] * gx_fastfp_force_25bit_c(cpu->ps1[c]);
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_mul_op(cpu, d, a, c); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_div(CPUState* cpu, u8 d, u8 a, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_div_op(cpu, d, a, b); return; }
+    const f64 r0 = cpu->fpr[a] / cpu->fpr[b], r1 = cpu->ps1[a] / cpu->ps1[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_div_op(cpu, d, a, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_madd(CPUState* cpu, u8 d, u8 a, u8 c, u8 b, bool subtract, bool negative) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_madd_op(cpu, d, a, c, b, subtract, negative); return; }
+    const f64 t0 = gx_fastfp_madd_single(cpu->fpr[a], cpu->fpr[c], cpu->fpr[b], subtract);
+    const f64 t1 = gx_fastfp_madd_single(cpu->ps1[a], cpu->ps1[c], cpu->ps1[b], subtract);
+    if (GX_FASTFP_UNLIKELY(isnan(t0) || isnan(t1))) { ppc_ps_madd_op(cpu, d, a, c, b, subtract, negative); return; }
+    f32 ps0 = gx_fastfp_force_single(cpu, t0), ps1 = gx_fastfp_force_single(cpu, t1);
+    if (negative) { ps0 = -ps0; ps1 = -ps1; }
+    gx_fastfp_write_both(cpu, d, ps0, ps1);
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_madds0(CPUState* cpu, u8 d, u8 a, u8 c, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_madds0(cpu, d, a, c, b); return; }
+    const f64 t0 = gx_fastfp_madd_single(cpu->fpr[a], cpu->fpr[c], cpu->fpr[b], false);
+    const f64 t1 = gx_fastfp_madd_single(cpu->ps1[a], cpu->fpr[c], cpu->ps1[b], false);
+    if (GX_FASTFP_UNLIKELY(isnan(t0) || isnan(t1))) { ppc_ps_madds0(cpu, d, a, c, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, t0), gx_fastfp_force_single(cpu, t1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_madds1(CPUState* cpu, u8 d, u8 a, u8 c, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_madds1(cpu, d, a, c, b); return; }
+    const f64 t0 = gx_fastfp_madd_single(cpu->fpr[a], cpu->ps1[c], cpu->fpr[b], false);
+    const f64 t1 = gx_fastfp_madd_single(cpu->ps1[a], cpu->ps1[c], cpu->ps1[b], false);
+    if (GX_FASTFP_UNLIKELY(isnan(t0) || isnan(t1))) { ppc_ps_madds1(cpu, d, a, c, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, t0), gx_fastfp_force_single(cpu, t1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_muls0(CPUState* cpu, u8 d, u8 a, u8 c) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_muls0(cpu, d, a, c); return; }
+    const f64 c0 = gx_fastfp_force_25bit_c(cpu->fpr[c]);
+    const f64 r0 = cpu->fpr[a] * c0, r1 = cpu->ps1[a] * c0;
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_muls0(cpu, d, a, c); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_muls1(CPUState* cpu, u8 d, u8 a, u8 c) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_muls1(cpu, d, a, c); return; }
+    const f64 c1 = gx_fastfp_force_25bit_c(cpu->ps1[c]);
+    const f64 r0 = cpu->fpr[a] * c1, r1 = cpu->ps1[a] * c1;
+    if (GX_FASTFP_UNLIKELY(isnan(r0) || isnan(r1))) { ppc_ps_muls1(cpu, d, a, c); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, r1));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_sum0(CPUState* cpu, u8 d, u8 a, u8 c, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_sum0(cpu, d, a, c, b); return; }
+    const f64 r0 = cpu->fpr[a] + cpu->ps1[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r0))) { ppc_ps_sum0(cpu, d, a, c, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, r0), gx_fastfp_force_single(cpu, cpu->ps1[c]));
+}
+GX_FASTFP_INLINE void gx_fastfp_ps_sum1(CPUState* cpu, u8 d, u8 a, u8 c, u8 b) {
+    if (GX_FASTFP_UNLIKELY(gx_fastfp_strict(cpu))) { ppc_ps_sum1(cpu, d, a, c, b); return; }
+    const f64 r1 = cpu->fpr[a] + cpu->ps1[b];
+    if (GX_FASTFP_UNLIKELY(isnan(r1))) { ppc_ps_sum1(cpu, d, a, c, b); return; }
+    gx_fastfp_write_both(cpu, d, gx_fastfp_force_single(cpu, cpu->fpr[c]), gx_fastfp_force_single(cpu, r1));
+}
+
+/* Redirect the generated chunk's calls. Declarations in core/cpu.h were
+ * already processed above, so only call sites in the chunk expand. */
+#define ppc_fadds(ctx, d, a, b) gx_fastfp_fadds((ctx), (d), (a), (b))
+#define ppc_fsubs(ctx, d, a, b) gx_fastfp_fsubs((ctx), (d), (a), (b))
+#define ppc_fmuls(ctx, d, a, c) gx_fastfp_fmuls((ctx), (d), (a), (c))
+#define ppc_fdivs(ctx, d, a, b) gx_fastfp_fdivs((ctx), (d), (a), (b))
+#define ppc_frsp(ctx, d, b) gx_fastfp_frsp((ctx), (d), (b))
+#define ppc_fadd(ctx, d, a, b) gx_fastfp_fadd((ctx), (d), (a), (b))
+#define ppc_fsub(ctx, d, a, b) gx_fastfp_fsub((ctx), (d), (a), (b))
+#define ppc_fmul(ctx, d, a, c) gx_fastfp_fmul((ctx), (d), (a), (c))
+#define ppc_fdiv(ctx, d, a, b) gx_fastfp_fdiv((ctx), (d), (a), (b))
+#define ppc_fma(ctx, a, c, b, single, sub, neg, out) gx_fastfp_fma((ctx), (a), (c), (b), (single), (sub), (neg), (out))
+#define ppc_ps_add_op(ctx, d, a, b) gx_fastfp_ps_add((ctx), (d), (a), (b))
+#define ppc_ps_sub_op(ctx, d, a, b) gx_fastfp_ps_sub((ctx), (d), (a), (b))
+#define ppc_ps_mul_op(ctx, d, a, c) gx_fastfp_ps_mul((ctx), (d), (a), (c))
+#define ppc_ps_div_op(ctx, d, a, b) gx_fastfp_ps_div((ctx), (d), (a), (b))
+#define ppc_ps_madd_op(ctx, d, a, c, b, sub, neg) gx_fastfp_ps_madd((ctx), (d), (a), (c), (b), (sub), (neg))
+#define ppc_ps_madds0(ctx, d, a, c, b) gx_fastfp_ps_madds0((ctx), (d), (a), (c), (b))
+#define ppc_ps_madds1(ctx, d, a, c, b) gx_fastfp_ps_madds1((ctx), (d), (a), (c), (b))
+#define ppc_ps_muls0(ctx, d, a, c) gx_fastfp_ps_muls0((ctx), (d), (a), (c))
+#define ppc_ps_muls1(ctx, d, a, c) gx_fastfp_ps_muls1((ctx), (d), (a), (c))
+#define ppc_ps_sum0(ctx, d, a, c, b) gx_fastfp_ps_sum0((ctx), (d), (a), (c), (b))
+#define ppc_ps_sum1(ctx, d, a, c, b) gx_fastfp_ps_sum1((ctx), (d), (a), (c), (b))
+#define RECOMPCORE_FAST_FP_ACTIVE 1
diff --git a/Source/Core/AudioCommon/AudioCommon.cpp b/Source/Core/AudioCommon/AudioCommon.cpp
index 8ec653a..e050adf 100644
--- a/Source/Core/AudioCommon/AudioCommon.cpp
+++ b/Source/Core/AudioCommon/AudioCommon.cpp
@@ -7,6 +7,9 @@
 #include <fmt/format.h>
 
 #include "AudioCommon/AlsaSoundStream.h"
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+#include "AudioCommon/CoreAudioSoundStream.h"
+#endif
 #include "AudioCommon/CubebStream.h"
 #include "AudioCommon/Mixer.h"
 #include "AudioCommon/NullSoundStream.h"
@@ -30,6 +33,11 @@ constexpr int AUDIO_VOLUME_MAX = 100;
 
 static std::unique_ptr<SoundStream> CreateSoundStreamForBackend(std::string_view backend)
 {
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+  if (backend == BACKEND_COREAUDIO && CoreAudioSound::IsValid())
+    return std::make_unique<CoreAudioSound>();
+  else
+#endif
   if (backend == BACKEND_CUBEB && CubebStream::IsValid())
     return std::make_unique<CubebStream>();
   else if (backend == BACKEND_OPENAL && OpenALStream::IsValid())
@@ -98,6 +106,9 @@ void ShutdownSoundStream(Core::System& system)
 
 std::string GetDefaultSoundBackend()
 {
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+  return BACKEND_COREAUDIO;
+#endif
 #if defined(ANDROID)
   return BACKEND_OPENSLES;
 #else
@@ -123,6 +134,10 @@ std::vector<std::string> GetSoundBackends()
   std::vector<std::string> backends;
 
   backends.emplace_back(BACKEND_NULLSOUND);
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+  if (CoreAudioSound::IsValid())
+    backends.emplace_back(BACKEND_COREAUDIO);
+#endif
   if (CubebStream::IsValid())
     backends.emplace_back(BACKEND_CUBEB);
   if (AlsaSound::IsValid())
diff --git a/Source/Core/AudioCommon/CMakeLists.txt b/Source/Core/AudioCommon/CMakeLists.txt
index 2564448..f4e6ffd 100644
--- a/Source/Core/AudioCommon/CMakeLists.txt
+++ b/Source/Core/AudioCommon/CMakeLists.txt
@@ -13,6 +13,13 @@ add_library(audiocommon
   WaveFile.h
 )
 
+if(CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS")
+  target_sources(audiocommon PRIVATE
+    CoreAudioSoundStream.cpp
+    CoreAudioSoundStream.h
+  )
+endif()
+
 if(ENABLE_CUBEB)
   message(STATUS "Cubeb found, enabling Cubeb sound backend")
   target_sources(audiocommon PRIVATE
diff --git a/Source/Core/AudioCommon/Mixer.cpp b/Source/Core/AudioCommon/Mixer.cpp
index a57b602..e5ffb95 100644
--- a/Source/Core/AudioCommon/Mixer.cpp
+++ b/Source/Core/AudioCommon/Mixer.cpp
@@ -75,6 +75,45 @@ void Mixer::MixerFifo::Mix(s16* samples, std::size_t num_samples)
   if (!m_mixer->m_config_audio_preserve_pitch && 0 < emulation_speed && emulation_speed != 1.0)
     in_sample_rate *= emulation_speed;
 
+  // Calculate the ideal length of the granule queue before the iOS
+  // prebuffer check so the configured reserve is honored at startup.
+  const std::size_t buffer_size_ms = m_mixer->m_config_audio_buffer_ms;
+  const std::size_t buffer_size_samples = std::llround(buffer_size_ms * in_sample_rate / 1000.0);
+  const std::size_t buffer_size_granules =
+      std::clamp((buffer_size_samples) / (GRANULE_SIZE >> 1), static_cast<std::size_t>(4),
+                 static_cast<std::size_t>(MAX_GRANULE_QUEUE_SIZE));
+  m_granule_queue_size.store(buffer_size_granules, std::memory_order_relaxed);
+
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+  // Static-recompiled games can deliver DSP samples a fraction slower than
+  // wall clock during brief CPU/GPU contention. Keep the iOS DSP queue near
+  // half full by making a very small resampling correction instead of
+  // repeatedly draining it and fading whole parts of the mix away.
+  if (this == &m_mixer->m_dma_mixer)
+  {
+    const std::size_t queue_size = m_granule_queue_size.load(std::memory_order_relaxed);
+    const std::size_t head = m_queue_head.load(std::memory_order_acquire);
+    const std::size_t tail = m_queue_tail.load(std::memory_order_acquire);
+    const std::size_t queued = (head - tail) & GRANULE_QUEUE_MASK;
+    const double target = static_cast<double>(std::max<std::size_t>(2, queue_size / 2));
+
+    // Do not begin draining a one-granule queue. Waiting for half of the
+    // configured reserve costs roughly 60 ms at boot and gives the adaptive
+    // resampler enough headroom to absorb normal frame-time variation.
+    if (m_prebuffering)
+    {
+      if (queued < static_cast<std::size_t>(target))
+        return;
+      m_prebuffering = false;
+    }
+
+    const double error = (static_cast<double>(queued) - target) / target;
+    const double desired_rate = std::clamp(1.0 + 0.02 * error, 0.98, 1.02);
+    m_dynamic_rate += 0.1 * (desired_rate - m_dynamic_rate);
+    in_sample_rate *= m_dynamic_rate;
+  }
+#endif
+
   const double base = static_cast<double>(1 << GRANULE_FRAC_BITS);
   const u32 index_jump = std::lround(base * in_sample_rate / out_sample_rate);
 
@@ -85,19 +124,8 @@ void Mixer::MixerFifo::Mix(s16* samples, std::size_t num_samples)
 
   const StereoPair volume{m_LVolume.load() / 256.0f, m_RVolume.load() / 256.0f};
 
-  // Calculate the ideal length of the granule queue.
-  const std::size_t buffer_size_ms = m_mixer->m_config_audio_buffer_ms;
-  const std::size_t buffer_size_samples = std::llround(buffer_size_ms * in_sample_rate / 1000.0);
-
-  // Limit the possible queue sizes to any number between 4 and 64.
-  const std::size_t buffer_size_granules =
-      std::clamp((buffer_size_samples) / (GRANULE_SIZE >> 1), static_cast<std::size_t>(4),
-                 static_cast<std::size_t>(MAX_GRANULE_QUEUE_SIZE));
-
   bool fade_audio = m_queue_fading.load(std::memory_order_relaxed);
 
-  m_granule_queue_size.store(buffer_size_granules, std::memory_order_relaxed);
-
   while (num_samples-- > 0)
   {
     // The indexes for the front and back buffers are offset by 50% of the granule size.
@@ -568,6 +596,22 @@ bool Mixer::MixerFifo::Dequeue(Granule* granule)
   std::size_t next_tail = (tail + 1) & GRANULE_QUEUE_MASK;
   if (next_tail == head)
   {
+    if (this == &m_mixer->m_dma_mixer)
+      m_mixer->m_dma_empty_dequeues.fetch_add(1, std::memory_order_relaxed);
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+    // Preserve the true queue position on iOS so the adaptive rate above can
+    // recover. Rewinding into old granules makes music sound selectively
+    // muted as Dolphin's gap filler repeatedly fades the replayed data.
+    if (this == &m_mixer->m_dma_mixer)
+    {
+      std::fill(granule->begin(), granule->end(), StereoPair{0.0f, 0.0f});
+      m_queue_fading.store(false, std::memory_order_relaxed);
+      m_queue_looping.store(false, std::memory_order_relaxed);
+      m_prebuffering = true;
+      return false;
+    }
+#endif
+
     // Only fill gaps when running to prevent stutter on pause.
     const bool is_running = Core::GetState(Core::System::GetInstance()) == Core::State::Running;
     if (m_mixer->m_config_fill_audio_gaps && is_running)
diff --git a/Source/Core/AudioCommon/Mixer.h b/Source/Core/AudioCommon/Mixer.h
index aca334b..6f4ad1b 100644
--- a/Source/Core/AudioCommon/Mixer.h
+++ b/Source/Core/AudioCommon/Mixer.h
@@ -36,6 +36,9 @@ public:
   void PushGBASamples(std::size_t device_number, const s16* samples, std::size_t num_samples);
 
   u32 GetSampleRate() const { return m_output_sample_rate; }
+  // Empty DMA granule dequeues; a diagnostic of starvation, not a count of
+  // audible glitches. Startup, pause, and reload must be excluded by the host.
+  static u64 GetDMAEmptyDequeues() { return m_dma_empty_dequeues.load(std::memory_order_relaxed); }
   void SetSampleRate(u32 output_sample_rate) { m_output_sample_rate = output_sample_rate; }
 
   // Note: NullSoundStream sets the sample rate to 0.
@@ -60,6 +63,7 @@ public:
   static constexpr u64 FIXED_SAMPLE_RATE_DIVIDEND = 54000000 * 2;
 
 private:
+  inline static std::atomic<u64> m_dma_empty_dequeues{0};
   const std::size_t SURROUND_CHANNELS = 6;
 
   class MixerFifo final
@@ -152,6 +156,8 @@ private:
     std::atomic<bool> m_queue_fading{false};
     std::atomic<bool> m_queue_looping{false};
     float m_fade_volume = 1.0;
+    double m_dynamic_rate = 1.0;
+    bool m_prebuffering = true;
 
     void Enqueue();
     bool Dequeue(Granule* granule);
diff --git a/Source/Core/Common/CMakeLists.txt b/Source/Core/Common/CMakeLists.txt
index 807dca3..14f54ed 100644
--- a/Source/Core/Common/CMakeLists.txt
+++ b/Source/Core/Common/CMakeLists.txt
@@ -65,7 +65,6 @@ add_library(common
   FatFsUtil.h
   FileSearch.cpp
   FileSearch.h
-  FilesystemWatcher.cpp
   FilesystemWatcher.h
   FileUtil.cpp
   FileUtil.h
@@ -168,6 +167,14 @@ add_library(common
   WorkQueueThread.h
 )
 
+if(CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS")
+  # e-dant/watcher uses macOS-only FSEvents on Apple platforms, which is not
+  # available on the iOS device SDK; provide a linkable no-op.
+  target_sources(common PRIVATE FilesystemWatcher_iOS.cpp)
+else()
+  target_sources(common PRIVATE FilesystemWatcher.cpp)
+endif()
+
 add_dependencies(common dolphin_scmrev)
 
 if(MINGW)
@@ -327,7 +334,7 @@ if(WIN32)
   if(MSVC)
     target_sources(common PRIVATE CompatPatches.cpp)
   endif()
-elseif(APPLE)
+elseif(APPLE AND NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
   target_sources(common PRIVATE
     GL/GLInterface/AGL.h
     GL/GLInterface/AGL.mm
diff --git a/Source/Core/Common/FileUtil.cpp b/Source/Core/Common/FileUtil.cpp
index 6d2853b..f5ee5e4 100644
--- a/Source/Core/Common/FileUtil.cpp
+++ b/Source/Core/Common/FileUtil.cpp
@@ -676,6 +676,8 @@ std::string GetTempFilenameForAtomicWrite(std::string path)
 std::string GetBundleDirectory()
 {
   CFURLRef bundle_ref = CFBundleCopyBundleURL(CFBundleGetMainBundle());
+  if (!bundle_ref)
+    return {};
 
   // Starting in macOS Sierra, apps downloaded from the Internet may be
   // "translocated" to a read-only DMG and executed from there. This is
@@ -692,6 +694,7 @@ std::string GetBundleDirectory()
   //
   // The headers can be found under "Security" on opensource.apple.com:
   // Security/OSX/libsecurity_translocate/lib/SecTranslocate.h
+#if !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
   if (!s_security_framework.IsOpen())
   {
     s_security_framework.Open("/System/Library/Frameworks/Security.framework/Security");
@@ -708,6 +711,7 @@ std::string GetBundleDirectory()
     CFRelease(bundle_ref);
     bundle_ref = untranslocated_ref;
   }
+#endif
 
   char app_bundle_path[MAXPATHLEN];
   CFStringRef bundle_path = CFURLCopyFileSystemPath(bundle_ref, kCFURLPOSIXPathStyle);
@@ -762,6 +766,8 @@ static std::string CreateSysDirectoryPath()
 {
 #if defined(_WIN32) || defined(LINUX_LOCAL_DEV)
 #define SYSDATA_DIR "Sys"
+#elif defined(__APPLE__) && (defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) || defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__))
+#define SYSDATA_DIR "Sys"
 #elif defined __APPLE__
 #define SYSDATA_DIR "Contents/Resources/Sys"
 #else
diff --git a/Source/Core/Common/GL/GLContext.cpp b/Source/Core/Common/GL/GLContext.cpp
index eef6896..e275278 100644
--- a/Source/Core/Common/GL/GLContext.cpp
+++ b/Source/Core/Common/GL/GLContext.cpp
@@ -79,7 +79,7 @@ std::unique_ptr<GLContext> GLContext::Create(const WindowSystemInfo& wsi, bool s
                                              bool prefer_gles)
 {
   std::unique_ptr<GLContext> context;
-#if defined(__APPLE__)
+#if defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
   if (wsi.type == WindowSystemType::MacOS || wsi.type == WindowSystemType::Headless)
     context = std::make_unique<GLContextAGL>();
 #endif
diff --git a/Source/Core/Common/Logging/Log.h b/Source/Core/Common/Logging/Log.h
index 62119c8..e2a395f 100644
--- a/Source/Core/Common/Logging/Log.h
+++ b/Source/Core/Common/Logging/Log.h
@@ -85,6 +85,13 @@ enum class LogLevel : int
   LDEBUG = 5,    // Detailed debugging - might make things slow.
 };
 
+// Optional embedding hook for warnings and errors. It is independent of
+// Dolphin's user-configured log categories and listeners so a frontend can
+// retain failure evidence without enabling verbose logging.
+using EmbedderLogCallback = void (*)(LogLevel level, LogType type,
+                                     const char* message, void* user_data);
+void SetEmbedderLogCallback(EmbedderLogCallback callback, void* user_data);
+
 #if defined(_DEBUG) || defined(DEBUGFAST)
 constexpr auto MAX_EFFECTIVE_LOGLEVEL = Common::Log::LogLevel::LDEBUG;
 #else
diff --git a/Source/Core/Common/Logging/LogManager.cpp b/Source/Core/Common/Logging/LogManager.cpp
index 89ae089..5ff6332 100644
--- a/Source/Core/Common/Logging/LogManager.cpp
+++ b/Source/Core/Common/Logging/LogManager.cpp
@@ -23,6 +23,18 @@
 
 namespace Common::Log
 {
+namespace
+{
+std::atomic<EmbedderLogCallback> s_embedder_log_callback{nullptr};
+std::atomic<void*> s_embedder_log_user_data{nullptr};
+}
+
+void SetEmbedderLogCallback(EmbedderLogCallback callback, void* user_data)
+{
+  s_embedder_log_user_data.store(user_data, std::memory_order_relaxed);
+  s_embedder_log_callback.store(callback, std::memory_order_release);
+}
+
 const Config::Info<bool> LOGGER_WRITE_TO_FILE{{Config::System::Logger, "Options", "WriteToFile"},
                                               false};
 const Config::Info<bool> LOGGER_WRITE_TO_CONSOLE{
@@ -63,15 +75,23 @@ private:
 void GenericLogFmtImpl(LogLevel level, LogType type, const char* file, int line,
                        fmt::string_view format, const fmt::format_args& args)
 {
+  const EmbedderLogCallback embedder_callback =
+      s_embedder_log_callback.load(std::memory_order_acquire);
   auto* instance = LogManager::GetInstance();
-  if (instance == nullptr)
-    return;
-
-  if (!instance->IsEnabled(type, level))
+  const bool forward_to_embedder = embedder_callback != nullptr &&
+      (level == LogLevel::LERROR || level == LogLevel::LWARNING);
+  const bool forward_to_dolphin = instance != nullptr && instance->IsEnabled(type, level);
+  if (!forward_to_embedder && !forward_to_dolphin)
     return;
 
   const auto message = fmt::vformat(format, args);
-  instance->Log(level, type, file, line, message.c_str());
+  if (forward_to_embedder)
+  {
+    embedder_callback(level, type, message.c_str(),
+                      s_embedder_log_user_data.load(std::memory_order_relaxed));
+  }
+  if (forward_to_dolphin)
+    instance->Log(level, type, file, line, message.c_str());
 }
 
 static size_t DeterminePathCutOffPoint()
diff --git a/Source/Core/Common/MemoryUtil.cpp b/Source/Core/Common/MemoryUtil.cpp
index 0d66890..0ad61bd 100644
--- a/Source/Core/Common/MemoryUtil.cpp
+++ b/Source/Core/Common/MemoryUtil.cpp
@@ -5,6 +5,8 @@
 
 #include <cstddef>
 #include <cstdlib>
+#include <cstring>
+#include <cstdio>
 
 #include "Common/CommonFuncs.h"
 #include "Common/Logging/Log.h"
@@ -16,7 +18,7 @@
 #else
 #include <stdio.h>
 #include <sys/mman.h>
-#if defined(_M_ARM_64) && defined(__APPLE__)
+#if defined(_M_ARM_64) && defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
 #include <pthread.h>
 #endif
 #if defined __APPLE__ || defined __FreeBSD__ || defined __OpenBSD__ || defined __NetBSD__
@@ -30,11 +32,26 @@
 
 namespace Common
 {
+bool IsRuntimeCodeGenerationDisabled()
+{
+#if defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+  return true;
+#else
+  const char* value = std::getenv("SSX3_NO_EXECUTABLE_MEMORY");
+  return value && std::strcmp(value, "1") == 0;
+#endif
+}
+
 // This is purposely not a full wrapper for virtualalloc/mmap, but it
 // provides exactly the primitive operations that Dolphin needs.
 
 void* AllocateExecutableMemory(size_t size)
 {
+  if (IsRuntimeCodeGenerationDisabled())
+  {
+    std::fprintf(stderr, "[ssx3-nojit] forbidden executable allocation bytes=%zu\n", size);
+    std::abort();
+  }
 #if defined(_WIN32)
   void* ptr = VirtualAlloc(nullptr, size, MEM_COMMIT, PAGE_EXECUTE_READWRITE);
 #else
@@ -93,7 +110,7 @@ static int& JITPageWriteNestCounter()
 // Allows a thread to write to executable memory, but not execute the data.
 void JITPageWriteEnableExecuteDisable()
 {
-#if defined(_M_ARM_64) && defined(__APPLE__)
+#if defined(_M_ARM_64) && defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
   if (JITPageWriteNestCounter() == 0)
   {
     pthread_jit_write_protect_np(0);
@@ -112,7 +129,7 @@ void JITPageWriteDisableExecuteEnable()
   if (JITPageWriteNestCounter() < 0)
     PanicAlertFmt("JITPageWriteNestCounter() underflowed");
 
-#if defined(_M_ARM_64) && defined(__APPLE__)
+#if defined(_M_ARM_64) && defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
   if (JITPageWriteNestCounter() == 0)
   {
     pthread_jit_write_protect_np(1);
diff --git a/Source/Core/Common/MemoryUtil.h b/Source/Core/Common/MemoryUtil.h
index 11ee1c4..91f8c4c 100644
--- a/Source/Core/Common/MemoryUtil.h
+++ b/Source/Core/Common/MemoryUtil.h
@@ -8,6 +8,9 @@
 
 namespace Common
 {
+// Apple mobile always forbids runtime CPU code generation. Desktop can use
+// the same contract for an instrumented parity run.
+bool IsRuntimeCodeGenerationDisabled();
 void* AllocateExecutableMemory(size_t size);
 
 // These two functions control the executable/writable state of the W^X memory
diff --git a/Source/Core/Core/CMakeLists.txt b/Source/Core/Core/CMakeLists.txt
index f30654c..f32c253 100644
--- a/Source/Core/Core/CMakeLists.txt
+++ b/Source/Core/Core/CMakeLists.txt
@@ -532,8 +532,6 @@ add_library(core
   IOS/WFS/WFSI.h
   IOS/WFS/WFSSRV.cpp
   IOS/WFS/WFSSRV.h
-  USB/LibusbUtils.cpp
-  USB/LibusbUtils.h
   MemTools.cpp
   MemTools.h
   Movie.cpp
@@ -661,6 +659,18 @@ add_library(core
   Wii/WiiUtils.h
 )
 
+if(NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
+  target_sources(core PRIVATE
+    USB/LibusbUtils.cpp
+    USB/LibusbUtils.h
+  )
+else()
+  # No USB host access on iOS; provide linkable no-ops for the LibusbUtils API.
+  target_sources(core PRIVATE
+    USB/LibusbUtils_iOS.cpp
+  )
+endif()
+
 if(_M_X86_64)
   target_sources(core PRIVATE
     DSP/Jit/x64/DSPEmitter.cpp
diff --git a/Source/Core/Core/Config/MainSettings.h b/Source/Core/Core/Config/MainSettings.h
index 14689b4..9890835 100644
--- a/Source/Core/Core/Config/MainSettings.h
+++ b/Source/Core/Core/Config/MainSettings.h
@@ -21,6 +21,7 @@
 #define BACKEND_OPENAL "OpenAL"
 #define BACKEND_PULSEAUDIO "Pulse"
 #define BACKEND_OPENSLES "OpenSLES"
+#define BACKEND_COREAUDIO "CoreAudio"
 #define BACKEND_WASAPI _trans("WASAPI (Exclusive Mode)")
 
 namespace PowerPC
diff --git a/Source/Core/Core/MemTools.cpp b/Source/Core/Core/MemTools.cpp
index f66aca2..f8f4c84 100644
--- a/Source/Core/Core/MemTools.cpp
+++ b/Source/Core/Core/MemTools.cpp
@@ -22,7 +22,8 @@
 #ifdef _WIN32
 #include "Common/Assert.h"
 #endif
-#if defined(__APPLE__) && !defined(USE_SIGACTION_ON_APPLE)
+#if defined(__APPLE__) && !defined(USE_SIGACTION_ON_APPLE) && \
+    !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
 #include "Common/Thread.h"
 #endif
 
@@ -120,7 +121,8 @@ bool IsExceptionHandlerSupported()
   return true;
 }
 
-#elif defined(__APPLE__) && !defined(USE_SIGACTION_ON_APPLE)
+#elif defined(__APPLE__) && !defined(USE_SIGACTION_ON_APPLE) && \
+    !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
 
 static void CheckKR(const char* name, kern_return_t kr)
 {
@@ -256,6 +258,21 @@ bool IsExceptionHandlerSupported()
   return true;
 }
 
+#elif defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
+
+void InstallExceptionHandler()
+{
+}
+
+void UninstallExceptionHandler()
+{
+}
+
+bool IsExceptionHandlerSupported()
+{
+  return false;
+}
+
 #elif defined(_POSIX_VERSION) && !defined(_M_GENERIC)
 
 static struct sigaction old_sa_segv;
diff --git a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.cpp b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.cpp
index e3581cc..f65369e 100644
--- a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.cpp
+++ b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.cpp
@@ -13,6 +13,7 @@
 #include "Common/DynamicLibrary.h"
 #include "Common/FileUtil.h"
 #include "Common/Logging/Log.h"
+#include "Common/MemoryUtil.h"
 #include "Core/Config/MainSettings.h"
 #include "Core/Config/StaticRecompSettings.h"
 #include "Core/Config/ConfigManager.h"
@@ -148,6 +149,8 @@ void StaticRecompCore::Init()
   g_static_recomp_core = this;
   RefreshConfig();
   m_collect_dispatch_samples = std::getenv("STATICRECOMP_DISPATCH_SAMPLES") != nullptr;
+  m_return_hooks = m_module_source.return_hooks;
+  m_emulate_syscall_vector = m_module_source.emulate_syscall_vector;
   const char* fallback_override = std::getenv("STATICRECOMP_FALLBACK_RANGES");
   std::istringstream fallback_ranges(fallback_override ? fallback_override :
                                                          Config::Get(Config::MAIN_STATICRECOMP_FALLBACK_RANGES));
@@ -185,11 +188,18 @@ void StaticRecompCore::Init()
   m_lockstep_verifier = std::make_unique<StaticRecompLockstep::StaticRecompLockstepVerifier>(*this);
   m_lockstep_verifier->Init();
 
+  const char* no_jit = std::getenv("STATICRECOMP_NO_JIT");
+  if (!(no_jit && std::string_view(no_jit) == "1") &&
+      !Common::IsRuntimeCodeGenerationDisabled())
+  {
 #ifdef _M_ARM_64
-  m_fallback_jit = std::make_unique<JitArm64>(m_system);
+    m_fallback_jit = std::make_unique<JitArm64>(m_system);
 #elif defined(_M_X86_64)
-  m_fallback_jit = std::make_unique<Jit64>(m_system);
+    m_fallback_jit = std::make_unique<Jit64>(m_system);
 #endif
+  }
+  std::fprintf(stderr, "[staticrecomp] fallback mode: %s\n",
+               m_fallback_jit ? "jit" : "interpreter");
   if (m_fallback_jit)
   {
     m_fallback_jit->SetStaticRecompFallback(true);
@@ -201,14 +211,20 @@ void StaticRecompCore::Init()
 void StaticRecompCore::Shutdown()
 {
   g_static_recomp_core = nullptr;
+  std::fprintf(stderr,
+               "[staticrecomp] fallback_jit_runs=%llu\n",
+               static_cast<unsigned long long>(m_fallback_jit_runs));
   std::fprintf(stderr,
                "[staticrecomp] shutdown: native=%llu fallback=%llu native_exc=%llu hook_fb=%llu "
-               "smc_failed=%u verifications=%llu reverify_events=%llu bursts=%llu cycles=%llu\n",
+               "smc_failed=%u verifications=%llu reverify_events=%llu bursts=%llu cycles=%llu "
+               "hle_returns=%llu hle_vectors=%llu hle_rejects=%llu\n",
                (unsigned long long)m_native_dispatches, (unsigned long long)m_fallback_steps,
                (unsigned long long)m_native_exceptions,
                (unsigned long long)m_hook_fallback_instructions, m_failed_chunks,
                (unsigned long long)m_verifications, (unsigned long long)m_reverify_events,
-               (unsigned long long)m_bursts, (unsigned long long)m_charged_cycles);
+               (unsigned long long)m_bursts, (unsigned long long)m_charged_cycles,
+               (unsigned long long)m_hle_returns, (unsigned long long)m_hle_vectors,
+               (unsigned long long)m_hle_rejects);
   std::vector<std::pair<u32, u64>> dispatch_samples(m_dispatch_samples.begin(),
                                                     m_dispatch_samples.end());
   std::sort(dispatch_samples.begin(), dispatch_samples.end(),
@@ -219,6 +235,16 @@ void StaticRecompCore::Shutdown()
                  dispatch_samples[i].first,
                  static_cast<unsigned long long>(dispatch_samples[i].second));
   }
+  std::vector<std::pair<u32, u64>> fallback_samples(m_fallback_samples.begin(),
+                                                    m_fallback_samples.end());
+  std::sort(fallback_samples.begin(), fallback_samples.end(),
+            [](const auto& left, const auto& right) { return left.second > right.second; });
+  for (std::size_t i = 0; i < std::min<std::size_t>(fallback_samples.size(), 16); ++i)
+  {
+    std::fprintf(stderr, "[staticrecomp] fallback-site pc=%08x samples=%llu\n",
+                 fallback_samples[i].first,
+                 static_cast<unsigned long long>(fallback_samples[i].second));
+  }
   NOTICE_LOG_FMT(POWERPC,
                  "StaticRecomp: shutdown. native_dispatches={} fallback_steps={} "
                  "native_exceptions={} hook_fallback_instructions={} smc_failed_chunks={} "
diff --git a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.h b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.h
index 44d027b..4a1aee7 100644
--- a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.h
+++ b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore.h
@@ -113,6 +113,7 @@ private:
   void OnICacheInvalidate(u32 address, u32 length);
   int ChunkIndexOf(u32 address);
   bool IsForcedFallbackAddress(u32 address) const;
+  bool TryHostHle(PowerPC::PowerPCState& ppc);
   bool ChunkContainsHostCall(u32 index) const;
   void VerifyChunk(u32 index);
   bool ResolveNativeAddress(u32 runtime_address, u32* linked_address, u32* rel_section_index);
@@ -160,10 +161,15 @@ private:
 
   u64 m_native_dispatches = 0;
   u64 m_fallback_steps = 0;
+  u64 m_fallback_jit_runs = 0;
   u64 m_native_exceptions = 0;
   u64 m_hook_fallback_instructions = 0;
+  std::vector<StaticRecompModuleSource::HostReturnHook> m_return_hooks;
+  bool m_emulate_syscall_vector = false;
+  u64 m_hle_returns = 0, m_hle_vectors = 0, m_hle_rejects = 0;
   u64 m_timebase_cycle_remainder = 0;
   std::unordered_map<u32, u64> m_dispatch_samples;
+  std::unordered_map<u32, u64> m_fallback_samples;
   u64 m_bursts = 0;          // SyncIn..SyncOut native runs (diagnostic)
   u64 m_charged_cycles = 0;  // cycles flushed from module charges (diagnostic)
 
@@ -187,11 +193,41 @@ private:
   u64 m_verifications = 0;    // chunk hash checks performed
   u64 m_reverify_events = 0;  // invalidations that reset a chunk to Unverified
 
-  // Lookup table optimization for O(1) chunk searches
-  std::vector<int> m_chunk_lookup_table;
+  // Chunk lookup: one entry per 256-byte granule of RAM/EXRAM instead of one
+  // per word, so the table the dispatcher probes on every burst back-edge is
+  // 192 KiB rather than 24 MiB and no longer evicts the guest's working set.
+  //   >= 0  every word of the granule belongs to that chunk
+  //   -1    no chunk covers the granule
+  //   -2    mixed (chunk boundary or return hook inside): resolve per word
+  static constexpr u32 kLookupShift = 8;
+  static constexpr s16 kLookupNone = -1;
+  static constexpr s16 kLookupMixed = -2;
+  std::vector<s16> m_chunk_lookup_table;
   u32 m_lookup_ram_size = 0;
   u32 m_lookup_exram_size = 0;
-  int GetAddressLookupIndex(u32 address) const;
+  bool m_chunk_ranges_sorted = false;
+  int GetAddressLookupIndex(u32 address) const
+  {
+    if (address >= 0x80000000u && address < 0x80000000u + m_lookup_ram_size)
+      return static_cast<int>((address - 0x80000000u) >> kLookupShift);
+    if (address >= 0x90000000u && address < 0x90000000u + m_lookup_exram_size)
+      return static_cast<int>((m_lookup_ram_size >> kLookupShift) +
+                              ((address - 0x90000000u) >> kLookupShift));
+    return -1;
+  }
+  u32 GranuleStartAddress(int index) const;
+  int LookupChunkSlow(u32 linked_address) const;
+  // Chunk index for a linked (module) address, or -1. Hot path: one table load.
+  int LookupChunk(u32 linked_address) const
+  {
+    const int index = GetAddressLookupIndex(linked_address);
+    if (index < 0 || index >= static_cast<int>(m_chunk_lookup_table.size()))
+      return -1;
+    const s16 entry = m_chunk_lookup_table[static_cast<size_t>(index)];
+    if (entry >= kLookupNone)
+      return entry;
+    return LookupChunkSlow(linked_address);
+  }
   void InitLookupTable(u32 ram_size, u32 exram_size);
 
   // Dispatch locality: most control transfers stay inside one chunk, so the
diff --git a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_Run.cpp b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_Run.cpp
index 71a21c5..60ffe23 100644
--- a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_Run.cpp
+++ b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_Run.cpp
@@ -41,7 +41,9 @@ FilePtr OpenDispatchTrace()
   if (!path || !*path)
     return {};
 
-  FilePtr file(std::fopen(path, "w"));
+  // Append: the run loop is re-entered on pauses and host locks, and the trace
+  // must cover the whole session for execution-equivalence comparisons.
+  FilePtr file(std::fopen(path, "a"));
   if (file)
   {
     std::fprintf(file.get(), "dispatch,pc,lr,ctr,cr,timebase,ppc_downcount\n");
@@ -73,18 +75,7 @@ void StaticRecompCore::Run()
     if (!m_module_active || m_chunk_lookup_table.empty())
       return false;
 
-    int lookup_index = -1;
-    if (address >= 0x80000000u && address < 0x80000000u + m_lookup_ram_size)
-    {
-      lookup_index = static_cast<int>((address - 0x80000000u) >> 2);
-    }
-    else if (address >= 0x90000000u && address < 0x90000000u + m_lookup_exram_size)
-    {
-      lookup_index = static_cast<int>((m_lookup_ram_size >> 2) + ((address - 0x90000000u) >> 2));
-    }
-    if (lookup_index < 0 || lookup_index >= static_cast<int>(m_chunk_lookup_table.size()))
-      return false;
-    const int chunk = m_chunk_lookup_table[lookup_index];
+    const int chunk = LookupChunk(address);
     return chunk >= 0 && m_chunk_state[chunk] == CHUNK_VERIFIED;
   };
 
@@ -93,6 +84,7 @@ void StaticRecompCore::Run()
 
   if (!m_module_active && m_fallback_jit && !m_guest.host_call)
   {
+    ++m_fallback_jit_runs;
     m_fallback_jit->Run();
     return;
   }
@@ -100,9 +92,6 @@ void StaticRecompCore::Run()
   while (*state_ptr == CPU::State::Running)
   {
     core_timing.Advance();
-    const std::string current_game_id = SConfig::GetInstance().GetGameID();
-    m_module_active = m_module && (current_game_id.empty() || current_game_id == m_module->game_id);
-
     do
     {
       // MSR.FP needs no gate here: generated FPU instructions raise the
@@ -220,19 +209,29 @@ void StaticRecompCore::Run()
         }
         // SingleStepInner delivers synchronous exceptions itself; external
         // interrupts are delivered at slice start, as in Interpreter::Run.
-        if (m_module_active && IsForcedFallbackAddress(ppc.pc))
+        if (m_module_active && (m_emulate_syscall_vector || !m_return_hooks.empty()) &&
+            TryHostHle(ppc))
+        {
+          // Handled on the host; the next iteration dispatches at the new pc.
+        }
+        else if (m_module_active && IsForcedFallbackAddress(ppc.pc))
         {
+          if (m_collect_dispatch_samples && (m_fallback_steps & 4095u) == 0)
+            ++m_fallback_samples[ppc.pc];
           ppc.downcount -= interpreter.SingleStepInner();
           ++m_fallback_steps;
         }
         else if (m_fallback_jit)
         {
+          ++m_fallback_jit_runs;
           m_fallback_jit->Run();
         }
         else
         {
           do
           {
+            if (m_collect_dispatch_samples && (m_fallback_steps & 4095u) == 0)
+              ++m_fallback_samples[ppc.pc];
             ppc.downcount -= interpreter.SingleStepInner();
             ++m_fallback_steps;
           } while (!(m_module_active && DispatchableAt(ppc.pc)) &&
diff --git a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_SMC.cpp b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_SMC.cpp
index 491c57e..e466554 100644
--- a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_SMC.cpp
+++ b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompCore_SMC.cpp
@@ -120,10 +120,8 @@ bool StaticRecompCore::ResolveNativeAddress(u32 runtime_address, u32* linked_add
   };
   if (resolve_active())
     return true;
-  const int direct_index = GetAddressLookupIndex(runtime_address);
-  if (direct_index >= 0 && direct_index < static_cast<int>(m_chunk_lookup_table.size()))
   {
-    const int chunk = m_chunk_lookup_table[direct_index];
+    const int chunk = LookupChunk(runtime_address);
     if (chunk >= 0 && m_chunk_rel_sections[chunk] < 0)
     {
       *linked_address = runtime_address;
@@ -158,12 +156,41 @@ u32 StaticRecompCore::TranslateRelAddress(u32 linked_address)
   return runtime_address;
 }
 
-int StaticRecompCore::GetAddressLookupIndex(u32 address) const
+u32 StaticRecompCore::GranuleStartAddress(int index) const
 {
-  if (address >= 0x80000000u && address < 0x80000000u + m_lookup_ram_size)
-    return static_cast<int>((address - 0x80000000u) >> 2);
-  if (address >= 0x90000000u && address < 0x90000000u + m_lookup_exram_size)
-    return static_cast<int>((m_lookup_ram_size >> 2) + ((address - 0x90000000u) >> 2));
+  const u32 ram_granules = m_lookup_ram_size >> kLookupShift;
+  if (static_cast<u32>(index) < ram_granules)
+    return 0x80000000u + (static_cast<u32>(index) << kLookupShift);
+  return 0x90000000u + ((static_cast<u32>(index) - ram_granules) << kLookupShift);
+}
+
+int StaticRecompCore::LookupChunkSlow(u32 linked_address) const
+{
+  // Mixed granule: a return hook or a chunk boundary lives inside it.
+  for (const auto& hook : m_return_hooks)
+    if (hook.address == linked_address)
+      return -1;
+  if (!m_module)
+    return -1;
+  const StaticRecompRange* ranges = m_module->chunk_ranges;
+  if (m_chunk_ranges_sorted)
+  {
+    u32 lo = 0, hi = m_module->num_chunk_ranges;
+    while (lo < hi)
+    {
+      const u32 mid = lo + (hi - lo) / 2;
+      if (linked_address < ranges[mid].start)
+        hi = mid;
+      else if (linked_address >= ranges[mid].end)
+        lo = mid + 1;
+      else
+        return static_cast<int>(mid);
+    }
+    return -1;
+  }
+  for (u32 i = 0; i < m_module->num_chunk_ranges; ++i)
+    if (linked_address >= ranges[i].start && linked_address < ranges[i].end)
+      return static_cast<int>(i);
   return -1;
 }
 
@@ -181,23 +208,93 @@ void StaticRecompCore::InitLookupTable(u32 ram_size, u32 exram_size)
     return;
   }
 
-  u32 total_instructions = (ram_size + exram_size) >> 2;
-  m_chunk_lookup_table.assign(total_instructions, -1);
+  const u32 granule = 1u << kLookupShift;
+  const u32 total_granules = (ram_size >> kLookupShift) + (exram_size >> kLookupShift);
+  m_chunk_lookup_table.assign(total_granules, kLookupNone);
+  m_chunk_ranges_sorted = true;
 
   for (u32 i = 0; i < m_module->num_chunk_ranges; ++i)
   {
     const auto& chunk = m_module->chunk_ranges[i];
-    int start_idx = GetAddressLookupIndex(chunk.start);
-    int end_idx = GetAddressLookupIndex(chunk.end);
+    if (i > 0 && m_module->chunk_ranges[i - 1].end > chunk.start)
+      m_chunk_ranges_sorted = false;
+    if (chunk.end <= chunk.start || i > 0x7FFE)
+      continue;
+    const int first = GetAddressLookupIndex(chunk.start);
+    const int last = GetAddressLookupIndex(chunk.end - 1);
+    if (first < 0 || last < first)
+      continue;
+    for (int idx = first; idx <= last; ++idx)
+    {
+      const u32 granule_start = GranuleStartAddress(idx);
+      const bool whole = chunk.start <= granule_start && chunk.end >= granule_start + granule;
+      s16& entry = m_chunk_lookup_table[static_cast<size_t>(idx)];
+      entry = (whole && entry == kLookupNone) ? static_cast<s16>(i) : kLookupMixed;
+    }
+  }
+  // Host return hooks: bursts must stop at these entries so TryHostHle runs.
+  // Their granules resolve per word, and the slow path answers "no chunk"
+  // for the hook address itself.
+  for (const auto& hook : m_return_hooks)
+  {
+    const int idx = GetAddressLookupIndex(hook.address);
+    if (idx >= 0 && idx < static_cast<int>(m_chunk_lookup_table.size()))
+      m_chunk_lookup_table[static_cast<size_t>(idx)] = kLookupMixed;
+  }
+}
 
-    if (start_idx >= 0 && end_idx >= start_idx)
+bool StaticRecompCore::TryHostHle(PowerPC::PowerPCState& ppc)
+{
+  auto& memory = m_system.GetMemory();
+  const u32 ram_size = memory.GetRamSizeReal();
+  const auto word = [&](u32 address) -> u32 {
+    const u32 offset = address - 0x80000000u;
+    if (address < 0x80000000u || offset + 4 > ram_size)
+      return 0;
+    const u8* p = memory.GetRAM() + offset;
+    return (u32(p[0]) << 24) | (u32(p[1]) << 16) | (u32(p[2]) << 8) | p[3];
+  };
+  if (m_emulate_syscall_vector && ppc.pc == 0x00000C00u)
+  {
+    // The SDK's __OSSystemCallVector: HID0 toggled around a sync, then rfi.
+    // Without data-cache emulation only the rfi has an effect.
+    static constexpr u32 kStub[7] = {0x7d30faa6u, 0x612a0008u, 0x7d50fba6u, 0x4c00012cu,
+                                     0x7c0004acu, 0x7d30fba6u, 0x4c000064u};
+    for (u32 i = 0; i < 7; ++i)
     {
-      for (int idx = start_idx; idx < end_idx; ++idx)
+      if (word(0x80000C00u + 4 * i) != kStub[i])
       {
-        m_chunk_lookup_table[idx] = static_cast<int>(i);
+        ++m_hle_rejects;
+        return false;
       }
     }
+    const u32 mask = 0x87C0FFFFu;
+    ppc.msr.Hex = (ppc.msr.Hex & ~mask) | (ppc.spr[SPR_SRR1] & mask);
+    ppc.msr.Hex &= 0xFFFBFFFFu;
+    m_system.GetPowerPC().MSRUpdated();
+    ppc.pc = ppc.spr[SPR_SRR0] & ~3u;
+    ppc.npc = ppc.pc;
+    ppc.downcount -= 8;
+    ++m_hle_vectors;
+    return true;
   }
+  for (const auto& hook : m_return_hooks)
+  {
+    if (hook.address != ppc.pc)
+      continue;
+    if (word(hook.address) != hook.first_word ||
+        word(hook.address + hook.last_word_offset) != 0x4e800020u)
+    {
+      ++m_hle_rejects;
+      return false;
+    }
+    ppc.pc = ppc.spr[SPR_LR];
+    ppc.npc = ppc.pc;
+    ppc.downcount -= 8;
+    ++m_hle_returns;
+    return true;
+  }
+  return false;
 }
 
 int StaticRecompCore::ChunkIndexOf(u32 address)
@@ -208,11 +305,7 @@ int StaticRecompCore::ChunkIndexOf(u32 address)
   u32 linked_address = address;
   if (!ResolveNativeAddress(address, &linked_address, nullptr))
     return -1;
-  int idx = GetAddressLookupIndex(linked_address);
-  if (idx < 0 || idx >= static_cast<int>(m_chunk_lookup_table.size()))
-    return -1;
-
-  return m_chunk_lookup_table[idx];
+  return LookupChunk(linked_address);
 }
 
 bool StaticRecompCore::FastDispatchableAt(u32 address)
diff --git a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompModuleSource.h b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompModuleSource.h
index 07e1385..febf039 100644
--- a/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompModuleSource.h
+++ b/Source/Core/Core/PowerPC/StaticRecomp/StaticRecompModuleSource.h
@@ -5,6 +5,7 @@
 
 #include <string>
 #include <utility>
+#include <vector>
 
 #include "Core/PowerPC/StaticRecomp/StaticRecompABI.h"
 
@@ -40,6 +41,20 @@ struct StaticRecompModuleSource
   Kind kind = Kind::None;
   std::string path;
   const StaticRecompModuleDesc* descriptor = nullptr;
+  // Host-side HLE without per-dispatch cost. A return hook makes a verified
+  // function entry an immediate blr: the address is excluded from the dispatch
+  // lookup so bursts stop there, and the run loop performs the return. Only
+  // dispatcher entries (cross-chunk calls) are affected; intra-chunk callers
+  // keep executing the original body. The system-call vector option verifies
+  // the seven-word SDK stub at 0x80000C00 and performs its rfi directly.
+  struct HostReturnHook
+  {
+    u32 address;
+    u32 first_word;
+    u32 last_word_offset;  // offset of the terminating blr
+  };
+  std::vector<HostReturnHook> return_hooks;
+  bool emulate_syscall_vector = false;
   HostCall host_call = nullptr;
   HostCallContains host_call_contains = nullptr;
   HostCallRangeContains host_call_range_contains = nullptr;
diff --git a/Source/Core/DolphinNoGUI/Platform.h b/Source/Core/DolphinNoGUI/Platform.h
index 5b0729a..3c9b5fd 100644
--- a/Source/Core/DolphinNoGUI/Platform.h
+++ b/Source/Core/DolphinNoGUI/Platform.h
@@ -54,6 +54,7 @@ public:
 
 #ifdef __APPLE__
   static std::unique_ptr<Platform> CreateMacOSPlatform();
+  static std::unique_ptr<Platform> CreateIOSPlatform();
 #endif
 
 protected:
diff --git a/Source/Core/InputCommon/CMakeLists.txt b/Source/Core/InputCommon/CMakeLists.txt
index ddf8c30..60bda72 100644
--- a/Source/Core/InputCommon/CMakeLists.txt
+++ b/Source/Core/InputCommon/CMakeLists.txt
@@ -1,7 +1,6 @@
 add_library(inputcommon
   DynamicInputTextureManager.cpp
   DynamicInputTextureManager.h
-  GCAdapter.cpp
   GCAdapter.h
   ImageOperations.cpp
   ImageOperations.h
@@ -77,6 +76,12 @@ add_library(inputcommon
   DynamicInputTextures/DITSpecification.h
 )
 
+if(CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS")
+  target_sources(inputcommon PRIVATE GCAdapter_iOS.cpp)
+else()
+  target_sources(inputcommon PRIVATE GCAdapter.cpp)
+endif()
+
 target_link_libraries(inputcommon
 PUBLIC
   common
@@ -113,7 +118,7 @@ if(WIN32)
     )
   endif()
   target_link_libraries(inputcommon PRIVATE cfgmgr32.lib dinput8.lib dxguid.lib)
-elseif(APPLE)
+elseif(APPLE AND NOT (CMAKE_SYSTEM_NAME STREQUAL "iOS" OR CMAKE_SYSTEM_NAME STREQUAL "tvOS"))
   target_sources(inputcommon PRIVATE
     ControllerInterface/Quartz/Quartz.h
     ControllerInterface/Quartz/Quartz.mm
@@ -149,7 +154,7 @@ elseif(ANDROID)
   )
 endif()
 
-if(NOT ANDROID)
+if(TARGET LibUSB::LibUSB)
   target_link_libraries(inputcommon PUBLIC LibUSB::LibUSB)
 endif()
 
diff --git a/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h b/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h
index 8f7f4ba..475cc89 100644
--- a/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h
+++ b/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h
@@ -22,7 +22,7 @@
 #ifdef HAVE_X11
 #define CIFACE_USE_XLIB
 #endif
-#if defined(__APPLE__)
+#if defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
 #define CIFACE_USE_OSX
 #endif
 #if defined(HAVE_LIBEVDEV) && defined(HAVE_LIBUDEV)
diff --git a/Source/Core/UICommon/UICommon.cpp b/Source/Core/UICommon/UICommon.cpp
index 01dc87b..57326ab 100644
--- a/Source/Core/UICommon/UICommon.cpp
+++ b/Source/Core/UICommon/UICommon.cpp
@@ -55,7 +55,7 @@
 #include "UICommon/DBusUtils.h"
 #endif
 
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
 #include <IOKit/pwr_mgt/IOPMLib.h>
 #endif
 
@@ -517,7 +517,7 @@ void InhibitScreenSaver(bool inhibit)
                           (inhibit ? (ES_DISPLAY_REQUIRED | ES_SYSTEM_REQUIRED) : 0));
 #endif
 
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__) && !defined(__ENVIRONMENT_TV_OS_VERSION_MIN_REQUIRED__)
   static IOPMAssertionID s_power_assertion = kIOPMNullAssertionID;
   if (inhibit)
   {
diff --git a/Source/Core/VideoBackends/Metal/MTLGfx.mm b/Source/Core/VideoBackends/Metal/MTLGfx.mm
index 4b744fe..f1029fe 100644
--- a/Source/Core/VideoBackends/Metal/MTLGfx.mm
+++ b/Source/Core/VideoBackends/Metal/MTLGfx.mm
@@ -21,7 +21,9 @@
 Metal::Gfx::Gfx(MRCOwned<CAMetalLayer*> layer) : m_layer(std::move(layer))
 {
   UpdateActiveConfig();
+#if TARGET_OS_OSX
   [m_layer setDisplaySyncEnabled:g_ActiveConfig.bVSyncActive];
+#endif
 
   SetupSurface();
   g_state_tracker->FlushEncoders();
@@ -288,8 +290,11 @@ static MTLTextureType FromAbstract(AbstractTextureType type, bool multisample)
 {
   AbstractGfx::OnConfigChanged(bits);
 
-  if (bits & CONFIG_CHANGE_BIT_VSYNC)
+  if (bits & CONFIG_CHANGE_BIT_VSYNC) {
+#if TARGET_OS_OSX
     [m_layer setDisplaySyncEnabled:g_ActiveConfig.bVSyncActive];
+#endif
+  }
 
   if (bits & CONFIG_CHANGE_BIT_ANISOTROPY)
   {
@@ -525,6 +530,15 @@ TextureConfig cfg(info.width, info.height, 1, 1, 1, info.format, AbstractTexture
   CGSize size = [m_layer bounds].size;
   const float scale = [m_layer contentsScale];
 
+#if !TARGET_OS_OSX
+  // The UIKit host owns integer output dimensions. Reconstructing them from
+  // bounds and float contentsScale loses pixels for source-matched sizing.
+  const CGSize drawable_size = [m_layer drawableSize];
+  if (drawable_size.width >= 1 && drawable_size.height >= 1)
+    return {static_cast<u32>(drawable_size.width), static_cast<u32>(drawable_size.height), scale,
+            Util::ToAbstract([m_layer pixelFormat])};
+#endif
+
   return {static_cast<u32>(size.width * scale), static_cast<u32>(size.height * scale), scale,
           Util::ToAbstract([m_layer pixelFormat])};
 }
diff --git a/Source/Core/VideoBackends/Metal/MTLStateTracker.mm b/Source/Core/VideoBackends/Metal/MTLStateTracker.mm
index a9fb4e6..2b223d4 100644
--- a/Source/Core/VideoBackends/Metal/MTLStateTracker.mm
+++ b/Source/Core/VideoBackends/Metal/MTLStateTracker.mm
@@ -9,6 +9,7 @@
 
 #include "Common/Align.h"
 #include "Common/Assert.h"
+#include "Common/Logging/Log.h"
 
 #include "Core/System.h"
 
@@ -417,6 +418,13 @@ explicit Backref(StateTracker* tracker) : state_tracker(tracker) {}
   [m_current_render_cmdbuf
       addCompletedHandler:[backref = m_backref, draw = m_current_draw,
                            q = std::move(m_current_perf_query)](id<MTLCommandBuffer> buf) {
+        if ([buf status] == MTLCommandBufferStatusError)
+        {
+          NSError* error = [buf error];
+          ERROR_LOG_FMT(HOST_GPU, "Metal command buffer failed code={} description={}",
+                        error ? [error code] : 0,
+                        error ? [[error localizedDescription] UTF8String] : "unknown");
+        }
         std::lock_guard<std::mutex> guard(backref->mtx);
         if (StateTracker* tracker = backref->state_tracker)
         {
diff --git a/Source/Core/VideoBackends/Metal/MTLUtil.h b/Source/Core/VideoBackends/Metal/MTLUtil.h
index 1752c82..3070fc8 100644
--- a/Source/Core/VideoBackends/Metal/MTLUtil.h
+++ b/Source/Core/VideoBackends/Metal/MTLUtil.h
@@ -3,7 +3,9 @@
 
 #pragma once
 
+#if TARGET_OS_OSX
 #include <AppKit/NSScreen.h>
+#endif
 #include <Metal/Metal.h>
 
 #include <vector>
diff --git a/Source/Core/VideoBackends/Metal/MTLUtil.mm b/Source/Core/VideoBackends/Metal/MTLUtil.mm
index 9298c5b..d679514 100644
--- a/Source/Core/VideoBackends/Metal/MTLUtil.mm
+++ b/Source/Core/VideoBackends/Metal/MTLUtil.mm
@@ -78,8 +78,12 @@
   backend_info->bSupportsPartialMultisampleResolve = false;
   backend_info->bSupportsDynamicVertexLoader = true;
   backend_info->bSupportsVSLinePointExpand = true;
+#if TARGET_OS_OSX
   backend_info->bSupportsHDROutput =
       1.0 < [[NSScreen deepestScreen] maximumPotentialExtendedDynamicRangeColorComponentValue];
+#else
+  backend_info->bSupportsHDROutput = false;
+#endif
 }
 
 void Metal::Util::PopulateBackendInfoAdapters(BackendInfo* backend_info,
@@ -312,7 +316,13 @@ fragment float4 is_helper_test() {
     }
   }
 
+  // The simulator advertises the host's Apple GPU family, but its shader
+  // translator rejects render-target reads. Use the existing blend path there.
+#if TARGET_OS_SIMULATOR
+  backend_info->bSupportsFramebufferFetch = false;
+#else
   backend_info->bSupportsFramebufferFetch = [device supportsFamily:MTLGPUFamilyApple1];
+#endif
 #if TARGET_OS_OSX
   if (vendor == DriverDetails::VENDOR_INTEL)
     backend_info->bSupportsFramebufferFetch |= DetectIntelGPUFBFetch(device);
@@ -552,7 +562,7 @@ fragment float4 is_helper_test() {
   spirv_cross::CompilerMSL::Options options;
 #if TARGET_OS_OSX
   options.platform = spirv_cross::CompilerMSL::Options::macOS;
-#elif TARGET_OS_IOS
+#elif TARGET_OS_IOS || TARGET_OS_TV
   options.platform = spirv_cross::CompilerMSL::Options::iOS;
   // Otherwise SPIRV-Cross will try to compile subgroup ops to quad ops instead
   // (And crash because there's no quad_min or quad_max)
diff --git a/Source/Core/VideoCommon/Present.h b/Source/Core/VideoCommon/Present.h
index 91ba870..957ae77 100644
--- a/Source/Core/VideoCommon/Present.h
+++ b/Source/Core/VideoCommon/Present.h
@@ -55,6 +55,14 @@ public:
   u32 AutoIntegralScale() const;
   AbstractTextureFormat GetBackbufferFormat() const { return m_backbuffer_format; }
   void SetSuggestedWindowSize(int width, int height);
+  // Renderer-thread snapshot of the last aspect-correct, source-sized picture.
+  // Zero without a current XFB; includes the current internal scale.
+  std::tuple<int, int> GetSuggestedWindowSize() const
+  {
+    if (!m_xfb_entry)
+      return {0, 0};
+    return {m_last_window_request_width, m_last_window_request_height};
+  }
   void SetBackbuffer(int backbuffer_width, int backbuffer_height);
   void SetBackbuffer(SurfaceInfo info);
   void OnBackbufferSet(bool size_changed, bool is_first_set);
diff --git a/Source/Core/VideoCommon/VertexLoaderBase.cpp b/Source/Core/VideoCommon/VertexLoaderBase.cpp
index 98eb651..343b28a 100644
--- a/Source/Core/VideoCommon/VertexLoaderBase.cpp
+++ b/Source/Core/VideoCommon/VertexLoaderBase.cpp
@@ -14,6 +14,7 @@
 #include "Common/Assert.h"
 #include "Common/CommonTypes.h"
 #include "Common/MsgHandler.h"
+#include "Common/MemoryUtil.h"
 
 #include "VideoCommon/VertexLoader.h"
 #include "VideoCommon/VertexLoaderManager.h"
@@ -237,7 +238,7 @@ std::unique_ptr<VertexLoaderBase> VertexLoaderBase::CreateVertexLoader(const TVt
 {
   const VertexLoaderType loader_type = g_ActiveConfig.vertex_loader_type;
 
-  if (loader_type == VertexLoaderType::Software)
+  if (loader_type == VertexLoaderType::Software || Common::IsRuntimeCodeGenerationDisabled())
   {
     return std::make_unique<VertexLoader>(vtx_desc, vtx_attr);
   }
diff --git a/Source/Core/VideoCommon/XFMemory.h b/Source/Core/VideoCommon/XFMemory.h
index 4077e50..0ac9fa1 100644
--- a/Source/Core/VideoCommon/XFMemory.h
+++ b/Source/Core/VideoCommon/XFMemory.h
@@ -464,6 +464,14 @@ static_assert(sizeof(XFMemory) == sizeof(u32) * XFMEM_REGISTERS_END);
 
 extern XFMemory xfmem;
 
+// Research hook: when set, called after every write into XF matrix memory
+// with the XF word address and count, so a host-side frame replay can rewrite
+// position matrices as they are loaded. Null in normal operation.
+namespace XFReplay
+{
+extern void (*g_transform)(u16 address, u32 count);
+}
+
 void LoadXFReg(u16 base_address, u8 transfer_size, const u8* data);
 void LoadIndexedXF(CPArray array, u32 index, u16 address, u8 size);
 void PreprocessIndexedXF(CPArray array, u32 index, u16 address, u8 size);
diff --git a/Source/Core/VideoCommon/XFStructs.cpp b/Source/Core/VideoCommon/XFStructs.cpp
index 4ac11c8..c452c89 100644
--- a/Source/Core/VideoCommon/XFStructs.cpp
+++ b/Source/Core/VideoCommon/XFStructs.cpp
@@ -198,6 +198,11 @@ static void XFRegWritten(Core::System& system, XFStateManager& xf_state_manager,
   }
 }
 
+namespace XFReplay
+{
+void (*g_transform)(u16 address, u32 count) = nullptr;
+}
+
 void LoadXFReg(u16 base_address, u8 transfer_size, const u8* data)
 {
   if (base_address > XFMEM_REGISTERS_END)
@@ -238,6 +243,8 @@ void LoadXFReg(u16 base_address, u8 transfer_size, const u8* data)
       ((u32*)&xfmem)[xf_mem_base + i] = Common::swap32(data);
       data += 4;
     }
+    if (XFReplay::g_transform)
+      XFReplay::g_transform(static_cast<u16>(xf_mem_base), xf_mem_transfer_size);
   }
 
   // write to XF regs
@@ -278,20 +285,19 @@ void LoadIndexedXF(CPArray array, u32 index, u16 address, u8 size)
   }
 
   auto& xf_state_manager = system.GetXFStateManager();
-  bool changed = false;
-  for (u32 i = 0; i < size; ++i)
+  bool changed = XFReplay::g_transform != nullptr;
+  for (u32 i = 0; i < size && !changed; ++i)
   {
     if (currData[i] != Common::swap32(newData[i]))
-    {
       changed = true;
-      XFMemWritten(xf_state_manager, size, address);
-      break;
-    }
   }
   if (changed)
   {
+    XFMemWritten(xf_state_manager, size, address);
     for (u32 i = 0; i < size; ++i)
       currData[i] = Common::swap32(newData[i]);
+    if (XFReplay::g_transform)
+      XFReplay::g_transform(address, size);
   }
 }
 
diff --git a/module-template/CMakeLists.txt b/module-template/CMakeLists.txt
index 3b00450..f50ae8b 100644
--- a/module-template/CMakeLists.txt
+++ b/module-template/CMakeLists.txt
@@ -84,7 +84,12 @@ if(NOT CHUNK_INPUTS)
   message(FATAL_ERROR "DolRecomp output has no generated chunks in ${GENERATED_DIR}/chunks")
 endif()
 
-add_library(${MODULE_NAME} SHARED
+option(RECOMPCORE_MODULE_STATIC "Link the generated game module into the host application" OFF)
+set(MODULE_LIBRARY_TYPE SHARED)
+if(RECOMPCORE_MODULE_STATIC)
+  set(MODULE_LIBRARY_TYPE STATIC)
+endif()
+add_library(${MODULE_NAME} ${MODULE_LIBRARY_TYPE}
   module_export.c
   "${MODULE_TABLES}"
   "${GXRUNTIME_DIR}/src/core/cpu.c"
@@ -121,6 +126,15 @@ if(UNIX AND NOT APPLE)
 endif()
 
 option(RECOMPCORE_MODULE_ENABLE_IPO "Enable Clang module interprocedural optimization when supported" ON)
+option(RECOMPCORE_FAST_FP
+  "Compile generated chunks against GXRuntime/include/core/cpu_fast_fp.h: inline JIT-fidelity floating point (no FPRF/FI/FR bookkeeping on non-exceptional results)" OFF)
+if(RECOMPCORE_FAST_FP)
+  # Chunks only: the helpers and interpreter keep their exact definitions.
+  set_source_files_properties(${CHUNK_INPUTS} PROPERTIES
+    COMPILE_OPTIONS "-include;${GXRUNTIME_DIR}/include/core/cpu_fast_fp.h")
+  target_compile_definitions(${MODULE_NAME} PRIVATE RECOMPCORE_FAST_FP=1)
+  message(STATUS "Module fast floating point: on (cpu_fast_fp.h forced into generated chunks)")
+endif()
 set(RECOMPCORE_MODULE_OPT_LEVEL "2" CACHE STRING
     "Optimization level for generated module translation units (0, 1, 2, or 3)")
 set_property(CACHE RECOMPCORE_MODULE_OPT_LEVEL PROPERTY STRINGS 0 1 2 3)
diff --git a/GXRuntime/include/core/cpu.h b/GXRuntime/include/core/cpu.h
index 65f316f..ee3bef9 100644
--- a/GXRuntime/include/core/cpu.h
+++ b/GXRuntime/include/core/cpu.h
@@ -419,13 +419,22 @@ bool ppc_psq_store(CPUState* cpu, u8 frS, u32 ea, bool w, u8 gqr, bool indexed,
  *
  * The win is bigger than skipping a call: at every generated site w, gqr_index,
  * indexed and cia are literals, so inlining lets the compiler fold the LSQE
- * test and the w branch away per site, which it cannot do across a call. */
+ * test and the w branch away per site, which it cannot do across a call.
+ *
+ * Both words go through one 8-byte RAM translation instead of two 4-byte
+ * ones. Any address the 8-byte window does not cover (region edges, mirrors,
+ * external memory) falls back to the exact out-of-line helper, which keeps
+ * the two independent accesses; reservation clearing, journal order and
+ * conversions are unchanged, so this is behavior-identical on every input. */
-static inline bool ppc_psq_load_inline(CPUState* cpu, u8 frD, u32 ea, bool w,
+static inline __attribute__((always_inline)) bool ppc_psq_load_inline(CPUState* cpu, u8 frD, u32 ea, bool w,
                                        u8 gqr_index, bool indexed, u32 cia) {
     const u32 gqr = cpu->gqr[gqr_index & 7u];
     if (((gqr >> 16) & 7u) == 0u && (indexed || (cpu->hid2 & PPC_HID2_LSQE) != 0u)) {
-        cpu->fpr[frD] = f64_value(convert_to_double(mem_read32(cpu, ea)));
-        cpu->ps1[frD] = w ? 1.0 : f64_value(convert_to_double(mem_read32(cpu, ea + 4u)));
+        u8* ptr = get_ram_ptr(cpu, ea, 8, NULL);
+        if (ptr == NULL)
+            return ppc_psq_load(cpu, frD, ea, w, gqr_index, indexed, cia);
+        cpu->fpr[frD] = f64_value(convert_to_double(read_be32(ptr)));
+        cpu->ps1[frD] = w ? 1.0 : f64_value(convert_to_double(read_be32(ptr + 4u)));
         return true;
     }
     return ppc_psq_load(cpu, frD, ea, w, gqr_index, indexed, cia);
@@ -434,10 +443,21 @@ static inline bool ppc_psq_store_inline(CPUState* cpu, u8 frS, u32 ea, bool w,
-static inline bool ppc_psq_store_inline(CPUState* cpu, u8 frS, u32 ea, bool w,
+static inline __attribute__((always_inline)) bool ppc_psq_store_inline(CPUState* cpu, u8 frS, u32 ea, bool w,
                                         u8 gqr_index, bool indexed, u32 cia) {
     const u32 gqr = cpu->gqr[gqr_index & 7u];
     if ((gqr & 7u) == 0u && (indexed || (cpu->hid2 & PPC_HID2_LSQE) != 0u)) {
-        mem_write32(cpu, ea, convert_to_single_ftz(f64_bits(cpu->fpr[frS])));
-        if (!w)
-            mem_write32(cpu, ea + 4u, convert_to_single_ftz(f64_bits(cpu->ps1[frS])));
+        u32 offset;
+        u8* ptr = get_ram_ptr(cpu, ea, 8, &offset);
+        if (ptr == NULL)
+            return ppc_psq_store(cpu, frS, ea, w, gqr_index, indexed, cia);
+        clear_matching_reservation(cpu, ea);
+        if (g_mem_write_journal && offset != (u32)-1)
+            g_mem_write_journal(offset, 4, g_mem_write_journal_user);
+        write_be32(ptr, convert_to_single_ftz(f64_bits(cpu->fpr[frS])));
+        if (!w) {
+            clear_matching_reservation(cpu, ea + 4u);
+            if (g_mem_write_journal && offset != (u32)-1)
+                g_mem_write_journal(offset + 4u, 4, g_mem_write_journal_user);
+            write_be32(ptr + 4u, convert_to_single_ftz(f64_bits(cpu->ps1[frS])));
+        }
         return true;
     }
     return ppc_psq_store(cpu, frS, ea, w, gqr_index, indexed, cia);
