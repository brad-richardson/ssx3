diff --git a/CMakeLists.txt b/CMakeLists.txt
index 36f06e3..99ed51e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -659,7 +659,11 @@ if(ENABLE_VULKAN)
   endif()
 
 
-  if (ANDROID AND _M_ARM_64)
+  # NOTE(android-vulkan): same target-system test as the headless/EGL patches:
+  # ENABLE_HEADLESS sets the ANDROID var to 0, but VulkanLoader.cpp still
+  # references adrenotools_open_libvulkan under -DANDROID (which the NDK
+  # toolchain always defines), so the archive must stay in the build.
+  if ((ANDROID OR (ENABLE_HEADLESS AND CMAKE_SYSTEM_NAME STREQUAL "Android")) AND _M_ARM_64)
     add_subdirectory(Externals/libadrenotools)
   endif()
 endif()
diff --git a/Source/Core/VideoBackends/Vulkan/CMakeLists.txt b/Source/Core/VideoBackends/Vulkan/CMakeLists.txt
index e3e2992..2962956 100644
--- a/Source/Core/VideoBackends/Vulkan/CMakeLists.txt
+++ b/Source/Core/VideoBackends/Vulkan/CMakeLists.txt
@@ -46,7 +46,11 @@ PRIVATE
   xxhash::xxhash
 )
 
-if (ANDROID AND _M_ARM_64)
+# NOTE(android-vulkan): same target-system test as the headless/EGL patches:
+# ENABLE_HEADLESS sets the ANDROID var to 0, but VulkanLoader.cpp is compiled
+# with -DANDROID and calls adrenotools_open_libvulkan on arm64, so the link
+# must stay.
+if ((ANDROID OR (ENABLE_HEADLESS AND CMAKE_SYSTEM_NAME STREQUAL "Android")) AND _M_ARM_64)
   target_link_libraries(videovulkan
   PRIVATE
       adrenotools
