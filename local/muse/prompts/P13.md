# P13 — Analyzer silent-fold triage (files beyond elf_analyzer.cpp)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P1/REPORT.md` Part 24 §P24-1f/g first
(the `[drop]` census: coverage, the 12 exclusion rules, and the explicit
boundary — analyzer files beyond `elf_analyzer.cpp` were left for
triage), plus Part 15 §P15-2 or the P1g-era notes on the analyzer MMIO
fold (the shape of the bug class: the analyzer silently discarding or
narrowing guest semantics). This brief triages the rest of the analyzer
surface. Read-only audit + patch spec — NO implementation (a deferred
brief applies it; avoids colliding with P1ad's in-flight fix). P1ad/M16
run concurrently in other panes — you share nothing with them.

## Facts you start from

P1w's census covers `elf_analyzer.cpp` plus the runtime reject paths.
The recomp analyzer pipeline has more files (other analyzer passes,
disassembler front-ends, IR lowering, range/clamp logic), none audited
for silent folds. Three of eight ladder rungs were silent drops — this
brief asks how many more the analyzer could still hide.

## Gates and rules

- READ ONLY everywhere: no fork writes/commits/pushes, no fork boots,
  no lease, no `adb`. Installed tools only, no downloads.
- Disk: internal disk is tight — scratch under
  `/Volumes/Extreme SSD/ps2x-p13/` (new dir) if needed. Never touch
  `/tmp/p1-link`, `ps2x-i*`, `ps2x-p1*`, `/Volumes/Extreme SSD/m*/`,
  or other agents' dirs.
- Evidence dir `local/research/P13/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P13]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Inventory.** Every file in the analyzer pipeline (recomp +
   analyzer + ELF handling): path, pass role, input→output shape.
   Table with file receipts.
2. **Audit.** Per file: silent folds/drops (default cases that
   discard, range clamps, bit-width narrowings, unhandled
   opcodes/modes/relocs, assumed invariants). Each finding: file:line
   + the exact discard + a minimal trigger description.
3. **Reachability.** For each finding: does SSX3's ELF/boot hit it?
   Census receipts where countable (ELF sweeps, boot-log greps over
   `$W/P1/run/boot-*.log`, recomp-log greps) — reached / reachable /
   unreachable-with-receipt.
4. **Patch spec.** The deliverable: per reached-or-reachable site, the
   exact `[drop]` line to add (site label + reason + args, following
   P1w's `emitDrop` conventions) and any exclusion-rule updates the
   P24 list needs. Ranked by (reachability × silence-severity), with
   the implementation brief sketched (files, hunk count, proof:
   recomp + suite + 1 boot showing the new census lines).
5. **Report.** `local/research/P13/REPORT.md`: inventory, findings
   with reachability receipts, ranked patch spec, exact commands,
   receipt paths, "What I could not do". Commit, stop.
