# I5 — First Simulator launch: install, run, record window-or-crash

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/I4/REPORT.md`
first (all of it: the Release `.app` product, the SDL_main-unlinked note,
entry/lifecycle open questions, disk + Simulator constraints). This brief
does what I4 explicitly deferred: boot a Simulator, install the app, launch
it, and record whatever happens. No fixes in this brief — first launch is
a measurement, even if (especially if) it crashes on arrival.

## Facts you start from

Product: `/Volumes/Extreme SSD/ps2x-i4/ios-runtime-rel/ps2xRuntime/Release-iphonesimulator/ps2EntryRunner.app/ps2EntryRunner`
(120 MB, Mach-O arm64, SDL linkage proven). Fork HEAD has I4's commits
(`53bac61` + later P-lane work — record `git log --oneline -1`, do NOT
assume). No game ISO is in scope: reaching `main` / first window /
first crash is the bar. If the app needs aufake ISO path or crashes on
missing resources, that IS the finding — record the exact missing
thing, do not fabricate game data.

## Gates and rules

- Host + Simulator only. No `adb`, no device, no lease (Simulator runs
  don't take the emulator host lease). No fork writes (READ ONLY — no
  commits, no pushes, no edits in the fork clone).
- Simulator: use ONLY the installed iOS 27.0 runtime + ONE existing
  device (iPhone 17 `4DE6C37C-77EF-45AE-827E-26286E68D022` unless it is
  unavailable — record substitution). NEVER download runtimes or
  platforms. Boot it yourself (`simctl boot`), shut it down at the end
  (`simctl shutdown`) unless a later step needs it.
- Disk: internal disk is tight — logs/screenshots under
  `/Volumes/Extreme SSD/ps2x-i5/` (new dir); small logs (<5 MB total)
  may live in the evidence dir. Never touch `/tmp/p1-link`,
  `/tmp/ps2xgs-build*`, `/tmp/ps2x-ios-spike`, `ps2x-i3`, `ps2x-i4`
  (read the .app from it, never write into it), or other agents' dirs.
- Evidence dir `local/research/I5/` (commit with `git add -f`, prefix
  `[I5]`, trailer `Orchestrated-By: Muse Code`). Do not commit
  screenshots over 5 MB total (downscale or describe). NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Install.** Boot the device, install the Release `.app`
   (`simctl install`), record install exit + bundle id. If install
   fails, record the exact error and stop after Step 4's report (the
   blockers section carries it).
2. **Launch.** `simctl launch` (or `simctl spawn` with stdout capture —
   your call, record it): does `main` run? SDL init? first window?
   Record: exit behavior, first 100 log lines + last 100, crash report
   if any (`simctl diagnose` or the device's crash logs — path
   recorded), and one screenshot if a window appears
   (`simctl io screenshot`).
3. **Entry/lifecycle mapping (from evidence, not guesses).** From the
   Step-2 logs: who called what (`main` → SDL_Init? raylib InitWindow?
   `PS2Runtime::run`?), where it stopped, and the exact next gap (missing
   ISO path? unmapped lifecycle callback? GLES context? audio session?).
   Each gap: one row with the log line that proves it.
4. **Report.** `local/research/I5/REPORT.md`: install/launch receipts,
   log excerpts, screenshot (or its absence reasoned), lifecycle map,
   gap rows with the exact next brief each needs, exact commands,
   receipt paths, "What I could not do" (device slice, signing,
   bundle resources, touch — unchanged unless evidence moves them).
   No verdicts. Commit `local/research/I5/`, shut the sim down, stop.
