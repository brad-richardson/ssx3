# E27 — Chunk-walk static autopsy: who tagged the headers, and where it went instead of feeding (NO boot)

You are the E-lane worker (spawned under claude-cli opus) in the ssx3
repo (derive the repo root from this prompt's path: two up from
`local/muse/prompts/`). **Tables + receipts, no verdicts beyond the
missions.** Read first, all of each: `local/research/E26/REPORT.md`
(the e26a boot: post-park SILENT 0/6725; start code in RAM 48 B away
at `0xd49b1c`; guest pre-tagged both chunk length fields at lines
22351/22354 from `pc=0x3dff8c ra=0x3dff90`; mutual-wait reading
tabled) + `x-a-startcode.json` + `chunk-map.json`, and
`local/research/E24/REPORT.md` §picture + `NEXT-BRIEF.md` (X as
posed). X is now: *why does the guest stop one **chunk** short of
feeding, when the chunk it needs is already resident and already
parsed?* This brief answers the STATIC half. NO BOOT — no lease
claim, no capture driver run, no `stdbuf`, no title execution of any
kind. The e26a capture + the fork sources are your only inputs.

## Facts you start from

- ORCH DECISION (gated): E26 PASS — all four E24 branches decided;
  the mutual-wait reading (one `GetPicture` feeds exactly one chunk;
  `GetPicture` does not return until a frame appears; the parser
  cannot emit until the next start code) is CONSISTENT with every
  measurement and UNTESTED. This brief tests its static legs. Fix
  gate stays STOP (diagnose + table, never patch).
- The two anchor facts to start from (both gated): (1) lines 22351/
  22354 — thread 1 stores `0x02000028` at `0xd49af0` and `0x0100381c`
  at `0xd49b18` (the SCHl + next-MPCh length fields, top byte
  changed) from `pc=0x3dff8c ra=0x3dff90`, BEFORE the feed; (2) the
  feed is exactly one chunk (5028 ES + 12 pad = 5040), then 65 s of
  total guest silence except thread 4's sema-31 timer loop.
- Standing SSD rule (link DEGRADING — pattern-dependent corruption
  now proven: same offset returns different bytes by read pattern):
  lane-critical SSD bytes need 2+ matching reads separated in time
  AND independent corroboration against pinned committed bytes where
  any exists; single-read SSD evidence is suspect. Prefer the
  committed e26a receipts + fork `git` objects (content-hashed) over
  fresh SSD reads wherever the bytes already exist.

## Gates and rules

- Fork: `3adc0478` triple-agree FIRST; `status --short` exactly
  `?? ps2_log.txt` or table + stop. Read-only on the fork (sources
  + `git log/show` only). ZERO fork edits/commits/pushes.
- NO boot, NO lease (do not claim, do not probe-claim), no capture
  driver, no suite run required (suite binary may be used read-only
  for symbol checks, never executed as a gate — there is no boot to
  gate).
- Byte caps: internal ≤ 512 MiB (no build, text + small tools);
  SSD reads only except NEW `e27-*` scratch ≤ 1 GiB if needed;
  evidence `local/research/E27/` text-only; `COPYFILE_DISABLE=1`
  on every SSD step; zero deletions.
- Time box 8 h. Evidence commit `[E27]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 1 — Name the walker (static)

From `pc=0x3dff8c` / `ra=0x3dff90` + the function log around lines
22351–22354: identify the exact function(s) in the 9,457 generated
sources that walked the chunk list and tagged the two length fields
(symbol + file + line + disassembly excerpt). Reproduce the walk
logic in pseudocode from the source: what list does it traverse,
what does the top-byte tag mean (visited? claimed? state?), and —
the load-bearing question — what does it do AFTER tagging (return
to whom? feed decision where?).

## Mission 2 — The feed-once path (static + log)

(a) From the sources: who calls `GetPicture`, in what loop (if any)
— is the call site one-shot by construction, counter-bounded, or
re-askable? What enforces E23's "producer fires exactly once per
`GetPicture`" (latch? sequence check? structural)? (b) From the
e26a function log (thread-1 calls between line 22354 and the park
at 22790, then park → end): what actually RAN on that path — does
thread 1 ever revisit the walker, the feeder, or the `GetPicture`
call site after the first feed? Table the static prediction vs the
log observation for each leg (agree / disagree / log-silent).

## Mission 3 — The re-ask question, sharpened (read-only synthesis)

E26 bounded "would the title ever re-ask" at 65 s of silence. From
Missions 1–2 ONLY (no new dynamic data): is there a code path by
which the guest COULD re-ask (name it: function + condition), or is
the one-shot structural (name the latch/one-shot construct)? If a
path exists, state its trigger exactly (what event /
register / callback would fire it) and what dynamic capture would
observe it (that capture is E28's, not yours — design nothing,
name it in one paragraph). If one-shot is structural, say what that
implies for the mutual-wait reading (which leg breaks, in code).

## Success bars (tabled)

Walker named (symbol/file/line/pseudocode + post-tag path); feed
call-site + once-mechanism named; thread-1 22354→end path tabled
(static-vs-log per leg); re-ask leg named-or-excluded with trigger;
fix gate honored; tail receipt with byte-exact prefix sha; zero
fork edits/commits/pushes; zero boots/leases.
