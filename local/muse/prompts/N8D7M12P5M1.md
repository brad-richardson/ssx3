# N8D7M12 Part 5M1 — prepare same-binary Mac OFF replay (no run)

You are the N/G OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12/{REPORT.md,ORCH-GATE-P1.md}`, `local/research/N8D7M12P5C/{REPORT.md,ORCH-GATE.md}`, and Part 5D2 REPORT/gate. Cite local source/receipts, not web. Own only this brief and `local/research/N8D7M12P5M1/`. No replay/boot, build, device/lease action, fork/source/binary/stream edit, push, board/global edit or upstream contact. Time box 30 min; text <512 KiB. Write REPORT/checker early.

Goal: prepare a bounded, reviewed-SHA-held **Mac OFF** replay with the same Part 1 binary `~/dev/ssx3-work/N8D7M12/build/ps2xTest/ps2x_tests` (SHA `2a0446e8…ae5`) and exact N8D7M6 stream `~/dev/ssx3-work/N8D7M6/n8d7m6.gs` (SHA `f6a78f71…a593`). Mac ON uses these inputs and `parallel` backend, step50, PPM tick2050, selected/oracle/tile flags ON. OFF removes exactly those three flags; keep all other replay settings. This is a Mac control, separate from the Odin run-to-run variance and no device action is allowed here.

Prepare a one-run Python driver under `local/research/N8D7M12P5M1/` for later review. It must: refuse reuse of private output dir `~/dev/ssx3-work/N8D7M12P5M1/mac-off`; double-hash binary and stream and check sizes/pins before run; verify Part 1 ON hashes/PPM pins; dynamically claim one mini P-lane slot with `local/tooling/p_lane_lease.py` API (`claim(label)` returns slot or None, `release(slot)` in `finally`), never hardcode a slot; run the binary from its own private cwd with OFF env, explicit stream/backend/step50/PPM2050/output paths and `GRANITE_VULKAN_LIBRARY=/opt/homebrew/lib/libvulkan.1.dylib`; cap active wall at 300 s and stop its own PID only; bound log at 16 MiB and output at 64 MiB; release lease/terminate child in `finally` on failure. No broad kill. Do not count diagnostic wall as speed.

Static checker must prove pins, exact env semantic diff (ON minus three flags plus output paths), no run on `--self-check`, one-run/lease/PID/wall/log/output guards and expected 41 ordered ticks, PPM tick2050, no parse/backend error. Predeclare run outcomes: Mac OFF hashes/PPM byte-equal to Mac ON supports flag-unperturbed Mac output; pre-2050 difference voids the Mac ON/OFF causal test; tick2050-only difference suggests flag effect on Mac. All 41 rows and PPM must be compared after a separately gated run. No Mac OFF result or graphics cause in this preparation part.

Deliver `REPORT.md`, driver `mac_off.py`, checker and bounded self-check receipt with exact SHAs, planned command, acceptance/stop table and gaps. A = safe reviewed-SHA candidate, B = control contract cannot be established, OTHER = preparation failure. Commit `[N8D7M12] Part 5M1` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Hand back the table.
