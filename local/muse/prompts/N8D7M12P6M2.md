# N8D7M12 Part 6M2 — reusable source snapshot for the next APK

You are the OpenCode Go tooling worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P6M1/{REPORT.md,ORCH-GATE.md}`, and P3 `source_gate.py`. Cite repo files, not web. Own only `local/tooling/orch/source_manifest.py` and `local/research/N8D7M12P6M2/`; no board/ledger/fork/config edits, build, Odin, mini lease, ssh/network, package or push. Time box 25 min, ≤256 KiB receipt text. Write REPORT early.

Goal: implement the **source-input collector missing from P6M1** for use immediately before/after the next Android source transfer. It records all file bytes in four named clean input roots — fork `PS2Recomp`, `parallel-gs` including Granite, external codegen, `jniLibs` — and detects a changed, added or missing file across the transfer. This is a source snapshot, **not** proof that every file compiled or that any historical APK used current bytes. The package worker separately captures build-system inputs, flags, native-member SHA, Build ID and APK SHA.

Build one Python 3 script with CLI `snapshot --fork ROOT --parallel ROOT --codegen ROOT --jni ROOT --out FILE` and `verify --manifest FILE --fork ROOT --parallel ROOT --codegen ROOT --jni ROOT`. Use only stdlib. Walk each root deterministically, store scope-relative POSIX paths, size, SHA-256 from **two separate reads**, and symlink targets without following them. Fail on a changed read, unreadable file, path escaping a root, duplicate logical path, generated files under fork `ps2xRuntime/src/runner/`, or source-root changes during a snapshot. Exclude only explicit cache/control names (`.git` as a directory or worktree pointer file, `build`, `.cxx`, `.gradle`, `__pycache__`) and list those exclusions in the output; never silently skip another file. Include a stable aggregate SHA over canonical sorted entries and one concise Git HEAD/status/submodule field per Git-backed root, marked informational (byte entries remain authority). Write output atomically; verify reports added/missing/changed entries and nonzero exit. Keep a file-count/byte cap CLI option so a wrongly pointed root stops before flooding disk. Do not include file contents or game code in receipts.

Acceptance: make a tiny temporary four-root fixture with a nested Granite file, two codegen files, a fake JNI binary and a symlink. Two snapshots/verify must match; mutate one file and verify fails naming it; add and remove a file and verify reports those exact paths. Check that excluded cache dirs do not appear, generated runner file fails, JSON is deterministic except explicitly volatile metadata, and two-read mismatch is handled in code. Test only the fixture now; do **not** scan the large real roots or run a package/build. Save exact commands, fixture outcomes and gaps in `REPORT.md`; `check.py` and `check-result.json` should run/record the meaningful fixture cases. If the complete input-root model has a concrete ambiguity, stop with a table instead of silently relaxing it. Commit only the script and `local/research/N8D7M12P6M2/{REPORT.md,check.py,check-result.json}` with `[N8D7M12] Part 6M2` and `Orchestrated-By: opencode`; no push. Hand back the table, not a package/GPU verdict.
