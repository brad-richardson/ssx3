# T42 — ROI/gate retune for forest/wall scenes (T40-G1 second arm): ONE tracking variant, same hold, same `sleep 0.5` cadence (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/T41/REPORT.md` first (all of
it: R4 reproduced the chain to LIVE gameplay + ONE 1038.4 ms Left hold
@T+503.19 on live 2ND/6 + 10-snap blind series at 10×≈0.57 s; at ≈0.57 s
exposures SCPS40 leaves the hold hop off-rail but uncorrelated (+29 @
0.02), SCPS120 loses T40's clean resolve (+119 @ 0.37 weak), and RDC
floods 11/12 on forest/arch-shade/wall scenes with only a d-post5
singleton VALID — no gated pair straddles the hold). Also read
`local/research/T40/REPORT.md` (the G1 proposal: this brief takes the
ROI-RETUNE arm, NOT a further cadence change). This brief executes
T41 §G1/G2's recipe ONLY: ONE tracking variant at the SAME cadence.

## Facts you start from

- The variant (tabled BEFORE the run — exactly one): retune the RDC
  rider lock for forest/wall scenes WITHOUT touching the cadence or the
  input. Candidate shapes (pick ONE, table why): higher dark threshold,
  smaller ROI, or a gradient-based rider lock. Implement it as a COPY
  (`t42-track.py` — the frozen `t41-track.py`/`t40-track.py` stays
  byte-identical and scores every snap in parallel as the control).
- The run (same shape as T41 R4): T4 build reuse (verify all pins),
  chain to LIVE gameplay, pre-hold pair on a live race (phase-match
  T41's hold window §hold — table the targets BEFORE the run; AI
  lineup/RNG differ run to run, match phase not seed), ONE ≈1038 ms
  Left hold (same body as T38/T40/T41), blind back-to-back 10-snap
  series at `sleep 0.5` (≈0.57 s exposures, ZERO in-script scoring),
  all scored post-hoc.
- C: headroom ~5 GB+ pre-run gate stands (T40 lesson — check BEFORE
  booting); WSLg-audio modal watch stands (T41 R1/R2 lesson — table the
  pre-run audio state).
- Success bars (tabled): BOTH trackers score all 12 dense snaps
  (frozen control + retuned variant, same snaps); gate-out rates per
  tracker (retuned vs T41's 11/12 flooded); gated-pair verdict per
  tracker (does any VALID pair straddle the hold now?); SCPS40/SCPS120
  scored as usual (unchanged tools — report whether the unresolved
  hold hop repeats); full chain/hops/HUD tables in T41's shapes.
- The question this answers: does the retune produce a gated RDC pair
  straddling the hold where T41 had a singleton VALID? Alternatives —
  still floods (→ table the exact flooded rate + noise floors and STOP
  with a recipe), run never reaches live gameplay at matched phase
  (→ ONE bounded re-attempt allowed).

## Gates and rules

- No lease of any kind (bytesize needs none). 0 laptop boots. T-lane
  tools reused (copied, not modified); the ONLY new file is the
  retuned tracker copy + its threshold/ROI calibration table.
- One input variant per attempt (the hold is FIXED — same body, same
  slot); one tracking variant per attempt (the retune is FIXED once
  tabled); never blind multi-presses; stop at the rail-rate comparison
  + gated-pair verdict + recipe.
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared +
  tracked (trace stream to the T-lane SSD dir, verified size+sha+
  head/tail — T41F recipe). No `adb`. No fork changes.
- Evidence dir `local/research/T42/` (STANDALONE). Commit with
  `git add -f`, prefix `[T42]`, trailer `Orchestrated-By: Muse Code`.
  Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — retune (no run) + pins

1. Table the ONE retune choice (threshold/ROI/lock shape + calibration
   table on T41's committed dense snaps — the retune must separate the
   rider from forest/shade on STATIC snaps before any run). Frozen
   control reproduces T41's numbers on those snaps first (reproduction
   gate — table it).
2. Pre-run pins (T4 build/tree/inputs/NVM/bindings/refs/C: headroom/
   audio state). Table every receipt.

## Task 2 — run + post-hoc scoring

1. Chain → LIVE → pre-pair → ONE hold → blind 10-snap series (T41 R4
   shape). Fetch, stream the trace, score with BOTH trackers + full
   chain/hops/HUD tables. Rail-rate comparison vs T41 per tracker +
   gated-pair verdicts + recipe.

## Report

`local/research/T42/REPORT.md`: contract, retune calibration, pins,
chain, tracking (both), comparison, forensics (if any), exact commands,
gaps. Commit evidence (`[T42]`), stop.
