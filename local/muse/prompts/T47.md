# T47 — PCSX2 reference frames for the front-end screens the recomp now reaches, then one healthy-series run

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`). **Tables + receipts; recommend, the orchestrator
decides.** Read first: `local/research/T46/REPORT.md` (the PCSX2-on-bytesize
recipe, audio gate, staged scripts, snapshot method) and `AGENTS.md` +
`local/AGENTS.local.md`. You own bytesize for this brief; nothing else is
scheduled on it.

## Why

The PS2 recomp now reaches the title screen, the main menu, **Select
Character** and **Select Event** on the Mac (E31), and the title screen on
the Odin (N3). The recomp's Select Character frame shows suspect
rendering: stray glyph/HUD fragments in the corners, every stat bar at
1.0 with one cell, no 3D rider model. We need PCSX2 reference frames of
the same screens, reached with the same inputs, to tell recomp bugs apart
from correct behavior.

Recomp frames to match (512×448, `fbp=112`), with paths + SHAs:
- title: `/Volumes/Extreme SSD/ps2recomp-spike/P1/run/frames-e29b-1/upload-latest.png` (`2ab49bac…`)
- Select Character: `…/P1/run/frames-e31b-1/upload-latest.png`
- Select Event (Peak 3, "Happiness"): `…/P1/run/frames-e31d-1/upload-latest.png`
- E31's boot script and pad script (`PS2X_PAD_SCRIPT` in
  `…/P1/run/boot-attempt-e31*.json`) give the input sequence and timing.

## Mission 1 — reference captures (the priority)

Boot stock SSX 3 in PCSX2 on bytesize (T46 recipe, same BIOS/ISO, native
resolution, software renderer if available so the output is comparable
to the recomp's CPU GS) and drive it with the same button sequence E31
used (START, then Cross presses), skipping the intro movies the way a
player would. Capture a native-resolution frame of each screen above,
plus the main menu between title and Select Character. Record the
PCSX2 settings used.

For each screen, one table row: reference frame path + SHA, recomp frame
path + SHA, and a first-pass diff. That means resize to the same size if
needed, then a per-region note of where they differ (corner glyphs, stat
bars, rider model, text). Don't judge which side is right beyond what's
visible; the orchestrator reads both images.

## Mission 2 — one healthy-series run (only if Mission 1 is done within 3 h)

One T46-shape run to extend the healthy series (audio gate first;
T44-E1 baseline rules, append-only). Same bars as T46.

## Rules

- Audio gate and the T45 variant quarantine rules from T46 still apply.
- Byte caps: new bytesize dirs ≤ 20 GB; Windows C: must keep ≥ 10 GB free
  (check before each boot); traces bounded as T46.
- Mirror reference frames to the SSD under `ps2x-t47/` (2 matching SHA
  reads) and the share tier. Evidence `local/research/T47/` text only
  (frames by path + SHA). `[T47]` commit with `Orchestrated-By: Muse
  Code`, `git add -f`, no push; check `git log -1` first. Time box 5 h.

## Deliverable

Reference-vs-recomp table for the 4–5 screens with a per-region
difference list; PCSX2 settings; if Mission 2 ran, its T46-shape tables.
