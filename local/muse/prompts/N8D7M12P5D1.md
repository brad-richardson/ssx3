# N8D7M12 Part 5D1 — prepare same-APK Odin OFF launcher (no device)

You are the N-lane OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5C/{REPORT.md,ORCH-GATE.md}`, `local/research/N8D7M12P5A/{REPORT.md,ORCH-GATE.md,launch.py,check.py}`, and Part 5B REPORT/gate. Cite source/receipts, not web. Own this brief and `local/research/N8D7M12P5D1/` only. No adb/device command, install, launch, lease, stream/APK/source edit, build, fork edit, push, board/global edit or upstream contact. Time box 30 min; text <512 KiB. Write REPORT/checker early.

Goal: make a reviewed-SHA-held OFF launcher for one later Odin diagnostic replay of the **same** APK `caa1110297dc413d3b04e7442873e0fb5d5624af8417e1e30537d76184d5f512` and stream `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593`. This tests whether the tick-2050 selected/oracle/tile measurement flags caused the sparse ON frame. The Part 5C design is accepted; it released no run.

Copy the final P5A launcher to `local/research/N8D7M12P5D1/launch.py`. Change only: (1) the private scratch dir to `~/dev/ssx3-work/N8D7M12P5D1`, plus unique remote frame/hash basename and lease/result labels; (2) remove exactly these three lines from replay env: `PS2X_N8D7F_SELECTED_CAPTURE=1`, `PS2X_N8D7L_ORACLE=1`, `PS2X_N8D5_TILE_CAPTURE=1`; (3) adapt the receipt/census acceptance so OFF does **not** require missing selected/oracle/tile markers, but still requires replay summary, 41 step-50 hash rows through tick2050, tick2050 `GB4_FRAME`, PPM, marker2050 success, and intact cleanup. Preserve APK/stream/runner/Turnip/HAL pins, `PS2X_GS_REPLAY_ONDEVICE=1`, parallel backend, output ticks/stride, one-install/one-launch guard, double SHA reads, battery/lock/free-space checks, 600 s active wall and 180 s progress caps, log/output caps, BACK, force-stop, PID check, environment 2+2 restoration, device lease release and `finally` order. Preserve ON receipts untouched. No ON or OFF graphics verdict in this preparation part.

Check the exact env diff in a static checker: ON keys minus precisely those three flags plus only the new output paths. Verify removed census code cannot falsely fail OFF, while replay success/rows/PPM checks stay. Run no-device `--self-check` or equivalent, `py_compile`, exact script SHA, and checker. A = script ready for an independent orchestrator SHA gate; B = unprovable same-input/output contract; OTHER = preparation failure. A later part may release **one** install/launch only using the exact reviewed SHA; no device call now.

Deliver `REPORT.md`, `launch.py`, `check.py` and bounded check result with an unexecuted command, exact pins/SHAs, diff table, acceptance/stop table and gaps. Commit `[N8D7M12] Part 5D1` with `Orchestrated-By: opencode`, explicit paths, no push. Hand back the table, not a root-cause verdict.
