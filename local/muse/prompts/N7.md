# N7 — Odin APK on the folded `ssx3`: the drawn race on the device

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini;
Android builds on bytesize `ssh bytesize` → `wsl -d Ubuntu -- bash -lc "…"`;
the Odin via adb, serial in `local/odin-serial`). Follow `~/dev/AGENTS.md`.
**Tables + receipts; recommend, the orchestrator decides.** Read first:
- `AGENTS.md`: the Odin lease, keyguard `showing=false`, `dumpsys battery`
  (charging and ≥20 %) before each launch, **`am force-stop` after every
  run**, always `install`;
- `local/AGENTS.local.md`;
- `local/research/N5/REPORT.md` (the whole recipe: branch, build with the
  memory governor, launches, SurfaceFlinger layer, simpleperf);
- `local/research/E58/REPORT.md` (what the fold contains).

Write scope: `local/research/N7/`; on bytesize, the Android tree used by N5
(`~/n5` and a new branch `n7-android`) and `~/n7/`; the Odin under
`/data/local/tmp/n7` and the app. No push anywhere.

## Why

Fork `ssx3` is now `b9647f5`. It holds E53's semantics (the race terrain
draws, VU0 runs), E45's double VU1, the tap guard, the G46 fill rule and
the vsync-rate log. N5's APK predates all but E45. We want the drawn race
on the Odin and its clean speed numbers.

## Work

1. **Branch `n7-android`** on bytesize at fork `b9647f5` (fetch it). Cherry-pick
   N5's N-local commits that aren't in `b9647f5`: the codegen-dir port, the
   env shim, arm64-only, the N6 controller, and the rest from N5's step-1
   table. **Skip** N5's tap guard and vsync-rate commits: they're folded
   now. Report conflicts. No rebase.
2. **Codegen:** copy canonical `~/dev/ssx3-work/codegen-ssx3` (the E53
   regen) to bytesize. Check the tarball SHA ×2.
3. **One clean APK** (taps OFF by default now, logs off, no dumps), built
   with N5's `build3.sh` recipe and the memory governor.
4. **Launches (≤4).** Use the I26-FAST route (`local/research/I26/ROUTES.md`),
   `PS2X_PAD_SCRIPT_CLOCK=vsync`, `PS2X_SKIP_MOVIE=1`,
   `PS2X_VSYNC_RATE_LOG=1`.
   - Two speed runs through ≥45 s of racing.
   - One simpleperf race profile (the top 25 by self, and the thread
     split).
   - Screencaps: SC (rider) and the race at 2–3 moments. View them
     yourself and say what you see: terrain, rider, HUD, sky.
5. **Ledger-ready table:** phase × guest vsyncs/s × ratio ÷59.94 × APK SHA,
   compared with N5's (title 0.47×, menu 0.44×, SC 0.33×, race 0.19–0.26×)
   and E58's Mac numbers (menus 0.54×, race 0.13×).

## Budgets and stop rules

≤2 Android builds, ≤4 launches, 4 h. bytesize runs one heavy job at a time
(check `pgrep -f "gradle|ninja|clang|pcsx2"`). If the APK crashes at boot
or doesn't reach the title in 3 minutes, stop after one launch and hand back
logcat. Force-stop after every run and release the lease.

## Deliverable

`local/research/N7/REPORT.md` (tables, screencaps ≤10 MB as JPEG, profile
top-25, APK SHA ×2). `[N7]` commit (`git add -f local/research/N7
local/muse/prompts/N7.md`, `git log -1` first).
