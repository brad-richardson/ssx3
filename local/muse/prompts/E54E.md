# E54E — full-width signed branch predicates

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `local/research/E54D/REPORT.md`, the E54 queue in
`docs/todo.md`, and `local/research/X7/REPORT.md` (the last is an
**orchestrator audit**, not a completed Qwen verdict). Hand back the
table; the orchestrator decides the gate. One mechanism only: signed
branch predicates. Do not change link, likely or delay-slot policy.

Pin fork remote `ssx3` at `1aaed05` (verify). Create new worktree
`~/dev/ssx3-work/E54E/PS2Recomp`, branch `e54-sign-branches`.
All builds, generated code, runs and temporary files go under
`~/dev/ssx3-work/E54E/`. Use E54D's build/regen scripts as read-only
examples; do not change E54D or canonical `codegen-ssx3`.
Fork source edits are limited to
`ps2xRecomp/src/lib/control_flow_emitter.cpp` and focused
`ps2xTest` tests/registration. No other E, G, fork or prior receipt
edits. No fork or ssx3 push.

Correct behavior: `BLTZ/BGEZ/BLEZ/BGTZ` and their likely/link
variants test the signed **low 64 bits** of Rs. The upper 64-bit GPR
half is outside the predicate. Current emitter lines 407–425 use
`GPR_S32`; runtime macro extracts only the low 32 bits. Distinguishing
vectors: `0x0000000180000000` is positive as s64 but negative as s32;
`0xffffffff00000000` is negative as s64 but zero as s32. Zero and
sign-extended 32-bit values are controls. X7's orchestrator table
shows the source discrepancy; confirm the oracle against pinned PCSX2
`9056c0834` interpreter or dynarec source (`SD[0]` / 64-bit compare),
with exact file/line receipt. If the pinned oracle disagrees, stop
without a source change and hand back the evidence.

## Gates

1. Verify all affected case labels and `GPR_S32`/`GPR_S64` macros.
   Test before-fix behavior with decoded/generated branch code or a
   compiled expression, not only a string search. Include the two
   distinguishing vectors, zero, a sign-extended positive/negative
   control and a nonzero upper GPR half. Show per-family expected and
   before/after take/no-take. Check likely/link code is unchanged.
2. Change only the four signed predicate families to `GPR_S64`.
   Regenerate once with the new recompiler into `E54E/codegen` from a
   copied E54D/E56 TOML retargeted to this worktree. Verify generated
   signed branch expressions changed, and keep the generated files
   outside Git. Do not infer dynamic reach from static sites.
3. One Mac Release build/suite with taps OFF and one with taps ON using
   E54E codegen. Run `ps2x_tests` from the fork root. Then one
   mini-lease I26-FAST diagnostic boot (dev-only skip movie), target
   race tick ≥2050, wall cap 500 s, own PID, bounded logs; view SC and
   two race frames. This is a functional regression, not a clean speed
   or branch-reach claim. No free-running fixed-tick hash A/B before E55.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty; commit `[E54E]` on the local fork branch with trailer
   `Orchestrated-By: Codex`. No push.

Budgets: two configure/build attempts per tap mode (one clear repair),
one regeneration, one boot, ≤600 s absolute boot, ≤9 GiB new E54E
bytes and 200 GB global cap. Check disk before >1 GiB writes. Verify
ISO/ELF, generated register file and runner with two matching SHA
reads before use. Claim/release one mini slot, cap logs at 64 MiB.
Stop on a failed gate after one clear repair; no tuning loop and no
other E54 fix in this lane.

Deliver `local/research/E54E/REPORT.md` with pins, oracle, before/after
truth table, source diff, generated expression check, suites, boot and
frame table, SHAs, runner-dir check, budgets and gaps. Commit only the
report `[E54E]` with the same trailer and explicit paths; no push. The
orchestrator gates and promotes the new canonical codegen if it passes.
