# N8D7M12 Part 5M2 — one same-binary Mac OFF replay

You are the N/G OpenCode Go run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5M1/{REPORT.md,ORCH-GATE.md,mac_off.py,check.py}`, Part 1 REPORT/gate and Part 5C design/gate. Own `local/research/N8D7M12P5M2/` and the private scratch created by the gated driver `~/dev/ssx3-work/N8D7M12P5M1/mac-off/`. No driver/binary/stream/fork/source edit, second replay, device action, push, board/global edit or upstream contact. Time box 25 min plus active ≤300 s; text <512 KiB, output ≤64 MiB. Write REPORT/checker early, hand back the table, no graphics root-cause verdict.

Goal: test whether the three tick2050 capture flags affect Mac replay output when the binary and stream are held fixed. The Part 1 ON binary SHA is `2a0446e8…ae5`, stream `f6a78f71…a593`; ON hash/PPM pins `94b433df…`/`9490484c…`. The reviewed Mac OFF driver SHA is `bec9a4c626092b5a2e66a8f1597f90235c1335631aa2f2cba432851de6ecd9f1`. It removes exactly selected/oracle/tile flags, clears ambient `PS2X_*`, keeps backend parallel/step50/PPM2050/Vulkan loader, and uses unique output paths.

Preflight: exact driver SHA, `check.py --self-check` 32/32, `py_compile`, no prior private OFF result/output, binary/stream/ON artifact pins, disk budget <200 GB with ≥1 GB headroom, one free mini P-lane slot and no heavy host build. The driver double-hashes inputs, dynamically claims one slot, refuses reuse, runs from private cwd, caps wall/log/output, stops own PID, releases slot in `finally`. If any preflight fails, stop without replay and report; no retry.

One released command from repo root: `python3 -u local/research/N8D7M12P5M1/mac_off.py --released-sha bec9a4c626092b5a2e66a8f1597f90235c1335631aa2f2cba432851de6ecd9f1`.

Acceptance: exit0, parallel summary markers2050, `GB4_FRAME tick2050`, exact 41 ordered ticks 50..2050, PPM present, bounded outputs, child stopped and lease free. Compare every OFF row and PPM SHA with pinned Mac ON. Equal 41 rows+PPM supports flag-unperturbed Mac output; any ≤2000 difference voids this Mac ON/OFF comparison; tick2050-only difference suggests capture instrumentation effect. Give absolute OFF PPM path/SHA for orchestrator viewing. Do not quote diagnostic wall as speed. The exact renderer source bytes at the old Mac binary build remain unpinned, so do not attribute a result to current dirty source.

Deliver `REPORT.md`, a narrow receipt checker and bounded text receipts with exact pins, row/PPM comparison, first failure and cleanup. Commit `[N8D7M12] Part 5M2` with `Orchestrated-By: opencode` using **explicit paths only**, no push. Hand back the table.
