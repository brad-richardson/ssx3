# G12 — PCSX2-side per-draw execution order for dump-vsync#0 (existing dump)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G11.md` (all — first-black
localized to interface-side recording order inside the vsync flush:
composite(A, FBP=112∘TBP=0) recorded mid-stream BEFORE sprite(B, FBP=0)
recorded inside `flush()`; cross-k matrix forces PCSX2-current; §4 ONE next
action) + `docs/reports/G10.md` §3c as needed (gsrunner build/run recipe).
This brief executes G11's §4.

## Facts you start from

- G11 fully explains paraLLEl-behind-own-stream (every link observed);
  the matrix forces PCSX2 sprite-first-effective
  (post-#k = composite(k) ∘ sprite(k)). The asymmetry's other half is OPEN:
  HOW PCSX2 achieves sprite-first-effective on the SAME dump.
- The task: trace PCSX2 gsrunner's per-draw execution order for
  dump-vsync#0 — when the FBP=112-composite vs the FBP=0-sprite rasterize
  relative to each other and to the present — and compare batch order vs
  G11's H4 table (A: 17 prims FBP=112∘TBP=0 mid-stream; B: 17 prims FBP=0
  inside flush). Candidates (not claims): different batching granularity
  (no mid-stream FRAME-switch split), deferred composite rasterization, or
  present-path resolve.
- Experiment contract (required): hypothesis = the PCSX2-side trace names
  the stage where sprite-first-effective arises; observable = per-HW-draw
  FBP/TBP log + present markers for dump-vsync#0 (extend to #1–#2 if
  cheap); alternatives = (a) stage named → table + ONE next action (the
  adoption-relevant finding: fidelity bug vs batching choice); (b) trace
  inconclusive → table + recipe, stop. Stop: existing dump + ONE bounded
  log-only patch — no tuning loop, no renderer changes, no new content.

## Gates and rules

- Pinned tree `9056c08349` in `pcsx2-g7` (verify HEAD + dump sha first;
  G10's gsrunner build cache reused). ONE bounded log-only patch (log
  FBP/TBP per HW draw + present markers; uncommitted, recipe-mirrored as
  text); rebuild gsrunner target only (`-j2`); rerun the EXISTING dump.
  Byte caps declared up front, tracked in ALLOCATED bytes; SSD reuse only,
  nothing new on `/`.
- No contention with other lanes: no recomp boots/builds, no P-lane lease
  (this is PCSX2-side, T28 recipe shape, no lease), no fork writes, no
  `adb` installs.
- Commit in ps2xGS as you go (prefix `[G12]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G12/` (`git add -f`,
  `[G12]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — oracle-side draw-order trace (existing dump, one bounded patch)

1. Log-only patch + gsrunner rebuild + rerun: per-draw FBP/TBP table for
   dump-vsync#0 with present markers (batch order vs G11 H4, side by side).
2. Name the stage where sprite-first-effective arises, or table the exact
   inconclusive point + the recipe that would resolve it.
3. Hypothesis verdict + the ONE next action the numbers justify
   (adoption-relevant: fidelity bug vs batching choice).

## Report

Mirrored `REPORT.md`: contract, trace tables, H4 comparison, localization
(or inconclusive point), license receipts, exact commands, gaps. Commit
both repos as above, stop.
