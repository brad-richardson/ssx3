# R1 — pc-tagged reference: settle the paradox (pc at entry + interpreter-vs-recompiler counts)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/A0/REPORT.md` §G1 first (the
paradox: runtime opening events 1–25 install syscall patches
while the reference shows ZERO `(74)/(5b)/(5a)/(64)` over the
full boot — and no static branch explains the avoidance), plus
F3 §19 (milestone table) and Part 4 decision 1 (this brief,
in `docs/research/review-2026-09-19-progress.md`). This run
SETTLES it: pc-tagged entry events + interpreter-vs-recompiler
counts, one run each. No A-series successor launches before
this lands — your tables route everything after.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` (key
  auth). Devel PCSX2 v2.9.75 source at `/home/brad/pcsx2-t4/
  pcsx2` (rev `9056c08349…`, table at start; do NOT touch
  the committed binary — build a SEPARATE pc-tagged binary
  so the T4/T23 reference binary stays pristine).
- Run (a) — pc-tagged recompiler: one-line-class patch in
  `pcsx2/R5900OpcodeImpl.cpp` `SYSCALL()`: add `cpuRegs.pc`
  (hex) and `a0` to the `BIOS_LOG` line AS TRAILING FIELDS
  (default format otherwise unchanged — `tools/
  trace_align.py` must still parse: verify with
  `--format-check` on the new trace); incremental rebuild;
  boot SSX3 to +3 s game time; capture the trace.
- Run (b) — interpreter: SAME pc-tagged binary with the EE
  recompiler OFF (interpreter), 30 s post-`ExecPS2:2`
  (PCSX2's recompiler is known to skip FlushCache logging;
  the question is whether `(74)/(5b)/(5a)` are likewise
  never logged under the recompiler).
- Pins to reproduce or table the mismatch and stop: T4
  epoch anchors (`ExecPS2` @142369/142465-class lines —
  line numbers WILL shift under the patch; anchors are
  the `ExecPS2` events themselves, not line numbers).
- NO lease of any kind (all boots on bytesize). No `adb`.
  No fork changes. No copyrighted downloads (inputs exist).

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce the patch + rebuild + both runs from scratch.
- `export COPYFILE_DISABLE=1` on every ssh/scp step that
  touches the SSD (Part 4 hygiene 8a — no new AppleDouble).
- Evidence dir `local/research/R1/` (STANDALONE). Commit with
  `git add -f`, prefix `[R1]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 2 h (Part 4 box). Tables + receipts, no verdicts —
  but the outcome ROUTES (table which of the three Part 4
  outcomes the receipts match: pc-differs@3 → G1-a ExecPS2
  args; pc-identical + interpreter (74)>0 → logging artifact,
  fix = Part 4 item 3; else state dependency → E3).
- Hygiene: write the report in chunks with a tail receipt
  (truncated tail fails the gate); grep receipts must show
  working patterns (no vacuous BRE — A0 lesson).

## Task 1 — pc-tagged runs (a) + (b)

1. Patch table: file + line + before/after text (one-line
   class — table the exact diff) + rebuild receipt (exit 0,
   binary sha/size, T4 binary untouched — sha re-verified).
2. Format table: `--format-check` on the run-(a) trace
   (events + zeros — parser still accepts the trailing
   fields, or table the breakage exactly).
3. Epoch table: `ExecPS2` anchors in both runs (lines +
   log-times — line numbers shift, events must not).

## Task 2 — settler receipts (pc + counts)

1. pc table: pc of reference events 1–10, run (a) AND run
   (b) (hex pcs + a0 values — the event-3 comparison is
   the row everything hangs on).
2. Count table: `(74)/(5b)/(5a)/(64)` counts, run (a) vs
   run (b) over matched windows (30 s post-`ExecPS2:2` —
   is the recompiler skipping them?).
3. Routing table: the three Part 4 outcomes with match/no
   per outcome (pc-differs@3? / interpreter-(74)>0? /
   else?) + the named next (G1-a args / item-3 fix / E3).

## Report

`local/research/R1/REPORT.md`: patch + epoch + pc + count
+ routing tables, exact commands both ends, gaps.
Commit evidence (`[R1]`), stop.
