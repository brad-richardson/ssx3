# X14 — sparse Qwen resource-layout table

You are the **sparse Qwen3.6-35B-A3B** opencode worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, and `local/AGENTS.local.md`. This tests whether a small, numbered source excerpt plus a semantic checker supports a useful C++/GLSL extraction. Do not decide an Odin cause or fix.

Pin the local candidate fork `/Users/brad/dev/ssx3-work/N8D5B/PS2Recomp` at `ef344024ff1a899d56cd3df21545caa7ad706c5f` and the private paraLLEl copy at `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` with Granite at `16e7395f6a4858c1783dbf6f521f90b9d5f82ac5`; stop on a different pin. N8D5B's first Mac GPU tile probe failed its checkerboard control: standalone Granite disables SPIRV-Cross, and the candidate called `request_program` without an explicit layout. The complete evidence is in `local/research/N8D5B/REPORT.md`; for this trial, read **only** this brief and `local/research/X14/excerpt.txt` (5,468 bytes, SHA-256 `c9050d87a3011718660e3595a8305cad5f035fbc1e4e02a7dd9826f3c8149efa`). Do not search or read other source files or the checker. You may invoke the checker by path.

Write `local/research/X14/rows.tsv` **within 60 seconds of the first source read**, with columns `field`, `value`, `evidence`. In order, derive: set-0 `sampled_image_mask`, set-0 `fp_mask`, set-0 `storage_buffer_mask`, `push_constant_size` in bytes, and whether the standalone build needs an explicit layout or can reflect it. Use eight-digit `0x` hexadecimal for masks and exact comma-separated `filetag:line` anchors from the excerpt, in source order. The GLSL bindings are the input; the Granite reflection code names the fields. Use `unknown` if the excerpt does not establish a value. Do not infer a device verdict. Try `lsp goToDefinition` **once** at `request_program` in fork `ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp:305`; record the exact returned target or empty/error, with no inference from empty output.

Run `python3 local/tooling/orch/check_x14.py local/research/X14/rows.tsv`; acceptance is 5/5. Time cap 5 minutes, context target <18k. `REPORT.md` records pins, commands, checker output, elapsed wall time, context size if visible, LSP result and gaps. Commit only `rows.tsv` and `REPORT.md` as `[X14]` with `Orchestrated-By: opencode`; no push. No source edit, build, boot, device or web. Hand back the table, not a repair recommendation.
