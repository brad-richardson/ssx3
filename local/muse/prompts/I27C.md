# I27C — signed iPhone check-in for folded native viewport

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§1/4/6. I27A+B is now fast-forward pushed to fork `ssx3` at `04f3ace599e3d9d52b94803150688f54dbf330ec`. `local/research/I27B/REPORT.md` passed the Mac taps-OFF suite 585/585 and an optimized Simulator race to tick 2363: SDL window 874×402, drawable/render 2622×1206, four viewed frames. Guest font detail did not improve. This task is a **build and install check-in only**. Never launch, test, screenshot, or drive Brad's iPhone; do not touch the iPad.

Goal: build the same folded source for iphoneos, sign it with the existing development identity/profile and install it on Brad's paired iPhone. Correct behavior means the device binary is built from exact `04f3ace` with the pinned iOS raylib patch and canonical E54F2 codegen, is signed/verified, has matching staged stock ELF/ISO hashes, and `devicectl device install app` exits zero. Alternatives are a wrong source/configuration, patch failure, signing/profile failure, or install failure; distinguish them from pinned source, CMake/Xcode flags, package SHA reads, codesign and install receipts. Install success makes **no rendering or speed claim**.

Write scope: `local/research/I27C/` and scratch `~/dev/ssx3-work/I27C/` including a fresh local fork worktree at `~/dev/ssx3-work/I27C/PS2Recomp`; no fork source edit, push, generated code edit, upstream contact, Odin or mini boot. Pin `fork/ssx3` remote and local source to `04f3ace`, check `git diff --exit-code 14b1e5cb HEAD -- ps2xRuntime/src/runner`. Use the W1F2 device recipe (`~/dev/ssx3-work/W1F/build-install.sh`, `local/research/W1F2/REPORT.md`) adapted into I27C scratch, with the I27B **fresh optimized Release** configure flags (`-O3 -DNDEBUG`) and a private clone of pinned raylib `c1ab645c` so the checked-in iOS patch hook applies reproducibly. Reuse existing FFmpeg/SDL2 device prefixes and exact stock inputs; double-read SHA of every pinned input before use, signed binary and staged ELF/ISO. Check source app bundle, signing identity/profile, codesign verification and paired iPhone ID before install. Do not log private signing material.

Budget: one iphoneos configure/build/stage/sign/install, one named compile-repair retry only if a single mechanism is identified; stop at first failed brief step otherwise. Wait for any concurrent clang/ninja build. Use at most 8 GiB new scratch, 8 MiB bounded text receipts; check global `disk_budget.sh` (<200 GiB). Do not start a Simulator or device app process. A sandbox-only Xcode/git denial may be retried escalated per launcher; an approval rejection is a stop. If the device is unavailable or profile expired, report the exact blocker, do not improvise provisioning.

Deliver `local/research/I27C/REPORT.md` with a stage table, exact commands, full pins and binary SHAs, flags evidence, install result, source diff/runner guard, budget and gaps. Commit only bounded text receipts and scripts as `[I27C]` with `Orchestrated-By: Codex`; no push. Hand back the table, do not declare a device rendering verdict.
