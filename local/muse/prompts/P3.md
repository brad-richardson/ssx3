# P3 — Survey of other static-recomp / PS2 recomp efforts (read-only research)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/P1/REPORT.md` Parts 5 and 6 (the boot ladder, EE
interrupt/alarm/CD-callback stack problem; plus Part 7 if it exists),
and the P1f brief `local/muse/prompts/P1f.md` (the pending fix — your
job is alternatives and prior art, not re-deriving it). Evidence dir
`local/research/P3/` (commit with `git add -f`, prefix `[P3]`, trailer
`Orchestrated-By: Muse Code`). Clones go under `/Volumes/Extreme SSD/`
(never the laptop volume, never committed). Time box 4 hours.

## Rules

- Read-only research. Do not edit the PS2Recomp fork, this repo's
  `tools/`, `native/`, `vendor` trees, or any other agent's evidence
  dir. No builds, no boots, no emulator runs, no leases, no `adb`.
- Every claim cites a file you opened (path + line numbers) or a URL
  you fetched; `grep`/search only locates candidates. Quote decisive
  lines. Closed-source projects are recorded as uninspectable with
  what is known about them — never guess at their internals.
- Small clones only (`--depth 1`, no submodules unless the file you
  need lives in one); record rev for each. Never clone PCSX2 (P2 owns
  that angle) — you may cite its public docs/URLs but not its source.

## Questions

1. **PS2Recomp forks.** Enumerate the fork network of `ran-j/PS2Recomp`
   (`gh` CLI if authenticated, else the GitHub web UI/API): forks with
   branches ahead of upstream, open PRs, stale branches touching
   `Kernel/`, `Syscalls/`, scheduler, threads, or CD. For each
   promising one: shallow-clone, read the diff, one table row (fork,
   rev, what it changes, whether it bears on the interrupt/alarm/CD
   stack problem).
2. **Independent PS2 static recomp.** Search for any other attempt at
   statically recompiling PS2 games (exact or profile-guided, any
   completeness). Table: project, URL, rev inspected, approach,
   status, license.
3. **Sibling static recomps.** N64Recomp (Wiseguy) and any other
   shipped static-recomp runtime (N64, PSP, PS1, Saturn — whatever has
   readable source): how does each handle async events on stacks the
   game owns — VI/audio callbacks, timer callbacks, async DMA
   completion? One row per mechanism per project (behavior +
   file:line quote), plus how it maps onto PS2Recomp's
   `EeScheduler`/`reserveAsyncCallbackStack`/`queueInvocation`.
4. **Readable dynamic emus (bounded).** If questions 1–3 yield nothing
   borrowable for CD callbacks specifically, spend at most 45 minutes
   on one readable PS2 emulator with EE-kernel/HLE threading
   (e.g. DobieStation): its CD callback thread model, file:line.
5. **License verdicts.** For every source with a borrowable snippet:
   its license and whether that snippet can land in our GPL-3 fork
   (compatible / incompatible / needs rewrite). Cite the license file.

## Steps

1. Fork network first (question 1) — it is the most likely to pay.
2. Questions 2–4 in order; stop a question at 60 minutes and record
   partial coverage as unresolved.
3. **Borrowable-snippet table**: mechanism → source (project, rev,
   file:line, quote) → sketch of the adaptation to PS2Recomp
   (function-level prose, not applied) → license verdict.
4. **Report.** `local/research/P3/REPORT.md`: project table, mechanism
   table, borrowable-snippet table, license verdicts, clone revs +
   paths, exact commands, "What I could not do". Commit
   `local/research/P3/`. Stop.
