# G26 — Narrower hunk: deliver ONLY `disable_sampler_feedback` (ride-along off), rebuild + IMMEDIATE verify-push, ONE with-flag run + pixel-diff vs G13 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G25/REPORT.md` first (all of it: the
G24 binary rebuilt bit-identically, but the no-flag run died at O4 before
the discriminating phase — O6 attribution UNRESOLVED — while completing a
bidirectional A/B proving the flag kills O4). Also read G24 §2b (the
`feedback_render_target` ride-along) + §3c (O6). This brief executes G25
§4's narrower hunk ONLY: the one run that both reaches post-loop AND
separates the halves. No upstream contact of any kind (standing
no-upstream order — local hunks only, filing stays local).

## Facts you start from

- ZERO-DAMAGE IS ACUTE (4th recurrence, now within MINUTES): the G24
  binary was zero-destroyed AGAIN after G25's report-time re-sha read it
  intact (mtime frozen, 100% zeros — orchestrator-verified). G22's
  binary stays intact; small files are never affected. NEW STANDING
  RULE FOR THIS BRIEF: verify-then-push with NO gap — re-sha the binary
  and push to device in immediate succession, and re-verify the on-device
  sha match right before the run. Treat every on-disk device binary as
  ephemeral; if a pre-push re-sha ever misses, rebuild + re-verify
  before pushing (never push an unverified binary).
- The hunk (ONE, local-only, SSD clone `parallel-gs-g7` @`3a66c19…`):
  deliver ONLY `disable_sampler_feedback`, keeping
  `feedback_render_target` at its interface default `false` (i.e. the
  G24 move MINUS the ride-along — table the exact construction: field
  surgery on the delivered struct vs second struct, and why it preserves
  the G24 receipt shape). At most a one-line LOGI receipt rides along
  (table it). No other source edits; hunk UNCOMMITTED (G18/G22/G24
  precedent) with diff text committed beside the report. The G24 hunk
  it SUPERSEDES stays in the tree or is replaced by this one — table
  the choice (one tested shape only).
- Rebuild in a NEW SSD build dir (G22 recipe, `-j2`). Full identity
  table (size/sha/BuildID/ELF/`strings` — a NEW sha is expected; what
  matters is build→pre-push→report→on-device ALL MATCHING).
- The run: ONE bounded WITH-FLAG run (same dump/iterations, flag SET,
  `PGS_SKIP_…` UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT — table the
  knob matrix). Discriminators (ONE run terminates): O6 persists at
  the same Scudo site => the FLAG's full-upload path is implicated =>
  scoped ASan/HWASan instrumentation on that path is next; scanouts
  write (bright or black) => the RIDE-ALONG is implicated => this
  narrower hunk IS the adoption shape (attribution + adoption in one
  run). Either way: `g14-diff.py` vs G13 (zero new tooling), O4 legs
  (expect absent — flag kills O4 bidirectionally), O5 table.
- Success bars (tabled): hunk minimal + no-new-hazard reasoning;
  build exit 0 + full identity (4+ matching re-shas incl. on-device);
  ONE run (exit/wall/fate + receipts + tombstone triage if any);
  scanouts scored or N/A with the exact wall; O6-attribution verdict.

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g26/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (the narrowing + at most one LOGI line); ONE build (new
  dir); TWO bounded device runs max (flag run + one retry iff O1/O3
  fires instead of first draw); no tuning loop, no second shape, no new
  dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g26/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G26/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G26]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).

## Task 1 — hunk + build + verify-then-stage (no run until §2)

1. Re-verify pins (clone HEAD/tree, G22+G24 HUNK_MATCH, dump, dirs
   0-growth, device pre-check). Write the ONE hunk; table the
   construction + no-new-hazard reasoning + LOGI choice. Configure +
   build NEW dir (exit 0, warnings tabled); full identity table.
2. Verify-then-push with NO gap (host re-sha → push → on-device sha
   match → run). Table every step with timestamps.

## Task 2 — ONE flag run + score + attribution

1. Run per the knob matrix; pull logcat + tombstone (if any) +
   scanouts; remove the device dir. `g14-diff.py` vs G13 + pixel
   census. O4 legs + O6-compare + O5 table. Name the ONE next action
   the numbers justify (scoped instrumentation vs adoption shape).

## Report

`local/research/G26/REPORT.md`: contract, hunk + build + identity, run
table, score, attribution verdict, next action, exact commands, gaps.
Commit evidence (`[G26]` + hunk diff text), stop.
