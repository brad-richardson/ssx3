# E24 — Close the 0x548880 residual + follow the semaphore wait (one extended-watch boot)

You are the ssx3 E-lane worker (Claude Code) in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** E23 killed C2a/C4 (the producer fires once on
every path; the guest never refills its source while parked) and moved X
upstream. This brief executes E23 NEXT-BRIEF's two cheap follow-ups, in
order — plus one cheap same-boot add for the I-lane (objective 3).

Execute this brief as specified. The design below is settled by cited
evidence — if a step's rationale seems unclear, re-read the cited
receipt rather than re-litigating the approach. Table genuine blocks
with their receipts; do not stall on doubt.

## Read first (all of each)

1. `local/research/E23/REPORT.md` + `local/research/E23/NEXT-BRIEF.md`
   + `local/research/E23/CADENCE.md` (cadence reference, killed
   candidates, named residual, semaphore-wait lead — BINDING; do not
   re-propose C1/C2a/C4/C3/C5–C9).
2. `local/research/E22/REPORT.md` + `local/research/E18/NEXT-BRIEF.md`
   (ABI constraints — BINDING).
3. E23's dir is READ-ONLY reference — your dir is
   `local/research/E24/`. Reuse the proven dylib by copy + re-sha
   (`e4d88fdc…b38e6be`, 52,472 B — never rebuild; E21 spellings carry).

## Mission

1. Checkpoint re-verify (fork triple-agree `3adc0478`, 9,457-name audit,
   suite 458/458 UNLOADED + observer-LOADED, E16 closure, E15 rc0s, E18
   R1–R6). Any red → table + stop (no boot on red).
2. ONE guarded title boot (fresh T13 pre-claims, shared P-lane lease —
   lease occupied → table and stop; wall/progress/byte caps,
   REPORT_ALL=1, runner exec'd DIRECTLY with the stdbuf assertion):
   - Extended watch: add `0x548880`–`0x5488ff` (the successor descriptor
     node) and the `0xd48748+` data region to `PS2X_DIAG_WATCH`.
     Objective 1a: close E23's residual (does the successor node move
     post-park?). Objective 1b: attribute thread 4's ~300 wakes per 5 s
     (what do they touch?).
   - Objective 2: follow the semaphore wait — threads 2–6 parked on
     `waitId` 26/30/32/36 at `pc=0x423de8`. Table who is meant to
     signal each, what the signalers are doing instead (thread states
     + pcs + waits), and whether the MPEG stall reads as symptom.
   - Objective 3 (I-lane relay, same boot, cheap): the I23 H2 request
     asks for the title movie dims "if derivable" — the 5,040 B
     themselves are already retained (E22/E23 `observed/parser-input.bin`,
     FNV `0xd2a9588f0e0fd358`). From this boot's trace, table whether
     title movie dims are derivable (sequence-header parse? GetPicture
     args? register state?) and if so their values; if not, table
     exactly what is missing. Do not burn anything extra for this —
     trace-mining only.
3. Fix gate (strict, unchanged): implement ONLY if the diagnosis isolates
   ONE edge with a minimal ABI-preserving change + its own fail-before +
   full regression. No decoder/EOF/policy invention, no fabricated
   frames. The MPEG demand edge is CLOSED (all candidates killed) —
   a fix, if any, lives upstream now.

## Rules

- ONE boot max; ONE lease claim (atomic, clean release, post-release
  `pgrep` check).
- No CSV/regen (regen need = stop). No fork behavior commits except
  through the fix gate (fail-before + full regression + push, never
  force-push).
- Byte caps declared up front + tracked (SSD lane ≤16 GB in NEW `e24-*`
  paths; internal ≤3 GB delta with E23-A1's lesson priced in — the
  internal volume is the scarce side; 0 growth elsewhere);
  `COPYFILE_DISABLE=1` on SSD steps; large artifacts off internal.
- Evidence dir `local/research/E24/` (STANDALONE). Commit with `git add
  -f`, prefix `[E24]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 8 h. Report in chunks + tail
  receipt (truncated tail fails the gate). Fill in the ACTUAL time used.

## Errata in (carry, don't re-fix)

- E21-E1 (stale harness size/sha in E21's REPORT).
- E23-E1 (hex-substring rename corruption — repaired in-lane; use the
  hex-safe rename proof, and re-audit any mechanical rename the same way).
- E23-E2 (gate nit): E23 REPORT cites the `decodedFrames.empty()` gates
  at `:2469`/`:2496`; the `if (` lines are `:2467`/`:2498`. Semantics
  right, numbers off by two.
