# M35 — Shape-9 unnamed cells: per-site value table (δ, gaps, bulk status) (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M34/REPORT.md` first (all of it —
M34 gap 1 is this brief: shape 9's 15 unnamed moved cells
(310–316, 318, 322, 327, 332, 336, 337, 339, 351) incl. the only
mixed delta-5 (c315) and a tied delta-4 (c314) — located, not
attributed at byte level), plus `local/research/M27/REPORT.md`
(Task-1 per-site table + joins method to reuse verbatim,
s0-anchored). Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- Shape 9's unnamed cells (M34): 15 moved cells (k=15, the max
  of any shape); c315 mixed delta-5 (the ONLY mixed delta-5
  in 79 cells); c314 delta-4 (tied with the c298 seed); rest
  per the committed `m34-census.tsv` (row shape=9 — reproduce
  it exactly or table the mismatch and stop).
- The value question (M27 method per cell): for each moved
  unnamed cell on shape 9: per-site rows (offset/plane/row/col,
  |δ_9|, s0 status + |δ_s0|, signed gaps both), miss/extra
  standing, near-miss shares (H1/H2-style), band/decile joins
  (streak/carrier joins N/A off the named set — table as such).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34.txt` +
  `m34-census.tsv` (shape-9 unnamed row to reproduce: 15 cells
  + standings + deltas — match exactly or table the mismatch
  and stop). Work dir: new `/Volumes/Extreme SSD/m35/`;
  evidence `local/research/M35/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m34/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m35/` + `local/research/M35/`.
- Evidence dir `local/research/M35/` (STANDALONE). Commit with
  `git add -f`, prefix `[M35]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M34 precedent).

## Task 1 — per-site value tables (all 15 cells)

Shape-9 unnamed row recomputed from the dumps (15 cells +
standings + deltas must match M34 exactly or table the
mismatch and stop):

1. Per-site rows for every missing + extra site in all 15
   cells (M27-style rows; both directions where mixed).
2. Near-miss shares per cell (H1/H2-style: bulk vs non-cell
   on the other frame — do unnamed cells read like full
   swaps (100% non-cell), partials (bulk-majority), or the
   shape-9 cluster (split)?).
3. c315 (mixed delta-5) + c314 (delta-4): full row lists +
   value stats (the two headliners, tabled individually).

## Task 2 — joins (where do unnamed values live?)

1. Band/decile joins per cell set (M27 machinery verbatim —
   s0-anchored; cluster privates read 21/22 band-1; table
   the unnamed-cell equivalent).
2. Gap-sign table (signed gaps both frames per site —
   missings read +/+ in M27/M29/M33; table unnamed).
3. |δ| stats per cell (|δ| ranges/medians — extras read 8–9
   medians, missings 18.0 in M29; table unnamed).

## Task 3 — controls + determinism (M18–M34 precedent)

1. Positive control: synthetic truth with KNOWN unnamed-cell
   values (N cells, known δ/gaps) — recover the per-site
   values + standings exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M34).

## Report

`local/research/M35/REPORT.md`: DESIGN.md (bars first) +
per-site tables + joins + controls + explained-vs-standing
update (attribution, not explanation — table it) + gap rows.
Screenshot only if a value map discriminates (budget-noted,
pre-registered split required per M23–M34 precedent). Commit
evidence (`[M35]`, no push), stop.
