# G28 — Writer fix: gate `create_image_view`'s descriptorBuffer branch on `supports_descriptor_buffer_or_heap`, rebuild, ONE verification run expecting exit 0 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G27/REPORT.md` first (all of it: HWASan
named the writer — stride-0 storage-image slab (never initialized:
`init()` early-`true`) + unguarded use branch + driver's 64-byte
`vkGetDescriptorEXT` write through the aliased pointer, proven by 5
convergent links + on-device `MISMATCH=1`). This brief executes G27 §4's
ONE next action ONLY: fix the writer, verify with one run. No upstream
contact of any kind (standing no-upstream order — local hunks only,
filing stays local; note the filing content upgrades per G27 §4 but the
filing itself still needs user identity/tracker).

## Facts you start from

- The fix (ONE hunk, local-only): gate `create_image_view`'s
  descriptorBuffer branch (`Granite/vulkan/memory_allocator.cpp:1424`,
  condition `descriptor_buffer_features.descriptorBuffer &&
  need_image_view_object`) on `supports_descriptor_buffer_or_heap`
  — the guard its sibling paths already carry (`:1488`,
  `sampler.cpp:139`, `descriptor_set.cpp:488/:570`). Alternative
  shape (table the choice, pick ONE): initialize the slabs
  unconditionally and fall back cleanly. Either way: one minimal
  hunk, no-new-hazard reasoning tabled (the guarded-false path is
  the long-tested non-descriptorBuffer path).
- Granite is a SUBMODULE (`m Granite` in the SSD clone): edit its
  working tree, leave the hunk UNCOMMITTED there (G20-capture-hunk
  precedent), and commit the hunk's diff text beside the report.
  Zero commits in the submodule, zero commits in ps2xGS. The G22 +
  G26 hunks stay as-is (tested shape stays flag-only — table the
  full hunk state; this brief adds at most the ONE Granite hunk).
- Rebuild in a NEW SSD build dir (G22 recipe, `-j2`, NON-sanitizer —
  this is the fix-verification shape). Full identity table
  (size/sha/BuildID/ELF/`strings` — NEW values expected; 4+
  matching reads incl. on-device).
- ZERO-DAMAGE IS ACTIVE (6 recurrences): the standing verify-then-push
  rule applies with full force — host re-sha → push → on-device sha
  match → run with NO gaps; re-verify immediately pre-run.
- The run: ONE bounded WITH-FLAG run (same dump/iterations, flag SET,
  `PGS_SKIP_…` UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT — table the
  knob matrix). EXPECT exit 0. That single run discriminates THREE
  questions (§4): (1) O5/O6 gone => single-writer family confirmed;
  (2) scanouts BRIGHT => aliasing caused the black (G26 hunk = adopted
  shape) vs still BLACK => black is a separate device trait; (3) O4
  legs (expect absent — flag kills it bidirectionally). `g14-diff.py`
  vs G13 (zero new tooling) + pixel census either way.
- Success bars (tabled): hunk minimal + guard-choice rationale; build
  exit 0 + full identity (4+ matching reads incl. on-device); ONE run
  (exit/wall/fate + receipts + tombstone triage if any — exit 0
  expected, any other exit fully triaged); scanouts scored (bright vs
  black with means/PSNR); the three discrimination verdicts.

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g28/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (the Granite guard + at most one LOGI receipt line);
  ONE build (new dir); TWO bounded device runs max (verification run
  + one retry iff O1/O3 fires instead of first draw); no tuning loop,
  no second shape, no new dumps; no lldb unless the tombstone cannot
  triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g28/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G28/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G28]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).

## Task 1 — hunk + build + verify-then-stage (no run until §2)

1. Re-verify pins (clone HEAD/tree incl. submodule state, G22+G26
   HUNK_MATCH, G27 binary damage-confirm, dump, dirs 0-growth, device
   pre-check). Write the ONE hunk (guard choice tabled + no-new-hazard
   reasoning). Configure + build NEW dir (exit 0, warnings tabled);
   full identity table (size/sha/BuildID/ELF/`strings`).
2. Verify-then-push with NO gap (host re-sha → push → on-device sha
   match → run). Table every step with timestamps.

## Task 2 — ONE verification run + score + three verdicts

1. Run per the knob matrix; pull logcat + tombstone (if any) +
   scanouts; remove the device dir. `g14-diff.py` vs G13 + pixel
   census. O4 legs + wall census. The three discrimination verdicts
   + the ONE next action the numbers justify (adoption vs next wall).

## Report

`local/research/G28/REPORT.md`: contract, hunk + build + identity, run
table, score, three verdicts, next action, exact commands, gaps.
Commit evidence (`[G28]` + hunk diff text), stop.
