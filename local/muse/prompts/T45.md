# T45 — Re-drive T44-R2 on the next audio-healthy VM (CORRECTED baselines; recipe carried)

You are the ssx3 T-lane worker. T44 stalled on a WSLg-audio outage (6
consecutive down boots H53–H58, still down at last check) with R2 unrun;
R1 was an environment NO-ZC-PARK (not an attempt — crash budget
unscathed, Task 1 still T43-frozen). Read `local/research/T44/REPORT.md`
first (all of it: rename audit, controls, R1 forensics, outage table,
§T44-4 re-drive recipe, gaps). T44's dir is READ-ONLY reference except
as noted. Tables, no verdicts.

## CORRECTED baselines (T44-E1 orchestrator erratum — READ THIS FIRST)

The T44 brief's carried ROI-floor values ({+19.7, +1.8, +11.9}) and T43
hold centroid deltas (ROI +34.3/−25.1, gradient −71.9/+26.4) were
UNPROVENANCED orchestrator error (cropdiff scores + misattributed values
confabulated into deltas; T43's hold admits no centroid pair by
construction). T44 §reconciliation caught it. The REPORT-derived series
below (T42 §T42-6 + T43 §T43-5, gate-verified) is the ONLY baseline.
Append only; do not re-tune.

- ROI no-input floor n=3: T42 npre1npre2 −0.7/−22.2; T43 d5d6 +2.8/−12.7,
  d9d10 −4.5/−7.0
- Gradient no-input floor n=7: T42 +1.8/+4.8, +0.0/+10.6, +2.7/−3.9,
  −2.1/−4.2; T43 d4d5 −7.3/−4.5, d5d6 +1.6/−12.8, d9d10 −4.7/−7.7
- Hold-step ROI n=1 crash-confounded: T42 +11.2/−39.5 (crash inside);
  T43 0 (hold ungated)
- Hold-step gradient n=1 crash-confounded: T42 −4.4/−24.1 (crash inside);
  T43 0 (hold ungated)
- d7-class intruder: lock-QA-gated, never auto-counted (T42 rider-mixed,
  T43 off-rider under both)

## Recipe (T44 §T44-4, carried)

1. Audio gate FIRST: demand-boot (or reuse the live VM if audio-healthy)
   and check `/mnt/wslg/PulseServer` + `runtime-dir/pulse/` (immediate +
   delayed @uptime ≥30). If HEALTHY: proceed (healthy series — the
   preferred path; append to the corrected distributions).
   If DOWN after ≤2 demand-boots + one ≤10-min stopped wait: NULL-SINK
   VARIANT is AUTHORIZED (orchestrator approval 09-21, post-host-reboot —
   the outage survived a full host reboot, so waiting longer is not a
   plan). Configure a pulseaudio null-sink (derive the exact commands;
   receipt every step), prove cubeb negotiates against it, and re-run
   the BEHAVIORAL gate at boot (modal census — a modal still aborts).
   QUARANTINE: label every output `VARIANT-NULLSINK`, start PARALLEL
   variant distributions (do NOT append variant runs to the healthy
   ROI/gradient/hold series), and STOP after ONE live variant run
   (report it; the orchestrator decides whether more variant runs are
   worth it). The healthy-series re-drive stays queued for recovery.
   Host-side recovery remains out of authority — never attempt it.
2. On a healthy VM: re-pin the T43 way (btime/binary/tree/NVM-mtime/
   bindings/staged-shas/C:/socket) + X11 tmpfs + PPM 0.0000/0 +
   dmesg/eventlog pre. Reuse T44's staged scripts (re-verify shas;
   copy to `t45-*` ONLY if you must edit — renames-only rule carries).
3. Fetch R1's on-box boot logs FIRST (`logs/boot-t44.log`/`.stdout` —
   R1's content; R2 overwrites them; T44 gap). Then launch R2
   (background → sleep 35 → v2a → census).
4. On live reach: full T44-shape run (61 snaps, ONE ≈1037 ms Left hold,
   same slot) + post-hoc scoring (all three trackers + lock-QA) +
   REPORT R2 sections. Crash-free → append to the CORRECTED
   distributions, DONE. Crash-confounded → full 3-row crash receipts;
   the lottery budget is UNSCATHED (0 live runs T44), so ONE more run
   (R3) is authorized iff R2 crashes; R3 crash → freeze + STOP.
5. Commit `[T45]` evidence + push when done (verify-then-push).

## Frozen

- ROI: x[220,420] y[180,360], DARK_MAX 80, gate 200–6000.
- Gradient: GRAD_MIN 60 (Sobel |Gx|+|Gy|), gate 500–3000.
- SCPS: y[380,460] x[150,540], ±40/±120, untouched.
- Trackers `t41/t42/t43/t44-track.py` byte-identical (shas 58afa004/
  a951f782/2e532a2d/cb56c827 first-8).

## Receipts (same set as T44)

Stream B+sha + head/tail shas, poll + T45_DONE + full-precision hold
walls, census EE/IOP + top rows, 61 snaps + sizes, staged tool shas,
NVM post-run FULL sha, C: post-run, dmesg pre/post.
