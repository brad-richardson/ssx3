# GB7B — spatial title-glyph candidate probe (Muse Contributor Go)

You are an OpenCode Go G-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/GB7A/{REPORT.md,ORCH-GATE.md}`, `local/research/GB5D/REPORT.md`, and the GB7B entry in `docs/todo.md`. Work only in the clean `~/dev/ssx3-work/GB4/PS2Recomp` branch `gb4-parallel`, expected HEAD `f79666938a90b550a4d8f4860e9ec567ad00c48a`; build root is `~/dev/ssx3-work/GB4/build`, run root `~/dev/ssx3-work/GB4/run`. Pin `~/dev/ssx3-work/G43/parallel-gs` at `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` and the full capture SHA `a6f75fb34fceccf6fd92ad65950d16f05a140cf38a3cc2f028be32e56ad51851` with two independent reads before use. Use existing `run/gb4p4.paths.txt` as the corrected true-path sidecar; do not copy the 2.7 GB capture.

Question: which actual CPU GS sprite/draw batch first writes the **visible** title copyright glyph region by marker 300 or the lower button-label region at marker 700? Existing matched frames put damage at copyright tick 300 and first visible button labels at 700. The recurring tick-950 full-height sprite is a composite candidate, not a proved glyph producer. Correct behavior predicts a spatially overlapping textured primitive with glyph-shaped changed pixels and reproducible source texels; a full-crop fade/composite overwrite may overlap yet is not the glyph producer. The null control is packet-input identity in the same pinned replay, so do not claim guest upload divergence from identical input.

Implement **one default-OFF, bounded diagnostic** in the existing replay/CPU draw path, limited to `ps2xTest/src/ps2_gs_replay_tests.cpp`, `ps2xRuntime/src/lib/gs/gs_cpu_backend.cpp`, and its actual header `ps2xRuntime/include/runtime/gs/gs_cpu_backend.h`. Record candidate packet index/tick/true path, primitive and clipped screen rectangle, overlap with the two existing crops `(320,120)-(420,205)` and `(340,360)-(430,420)`, effective TEX0/CLAMP/TEST/ALPHA/FRAME and UV, and a bounded source-texel fingerprint if readily available at the CPU sample site. Report any unavailable fields honestly. Preserve byte-identical behavior with the flag unset; log at most 20,000 candidate rows and 8 MiB total, stop writing at the cap. Prefer existing draw hooks; do not add a general packet tracer or tune render output.

Budget: one incremental `ps2x_tests` build, one taps-OFF suite, **one** direct CPU replay through marker 700 with flag ON and PPMs only at 300/600/700. Reuse the GB5D replay command form with `PS2X_GS_REPLAY_STEP=50` and the path sidecar. No live boot, device, GPU replay, upstream contact, or push. Store logs and images under `run/gb7b/` (<64 MiB combined); text receipts under `local/research/GB7B/` (<256 KiB). If no unique glyph candidate can be isolated within the cap, stop with a candidate table and name the next observable; do not add a second probe. Do not infer glyph identity from overlap alone. View the three saved frames/crops and cite exact pixel/candidate evidence. Check runner-dir diff against `14b1e5cb` is empty.

Deliver `local/research/GB7B/REPORT.md`, a bounded `candidates.tsv`, command/pin/SHA/log-size receipts, and the default-OFF source patch committed locally on `gb4-parallel` as `[GB7B]` with `Orchestrated-By: opencode`; no fork push. Then commit only named text receipts and this brief in ssx3 as `[GB7B]` with the same trailer; no push. `candidates.tsv` needs `tick,packet_index,path,rect,crop,texture_state,source_fingerprint,classification,reason` and at most 30 selected rows, with full candidate log kept private. **Hand back the table; the orchestrator decides whether a glyph batch is identified or a cause is supported.**
