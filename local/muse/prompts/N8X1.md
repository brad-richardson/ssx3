# N8X1 — exploratory Odin GPU session (Opus, 2 h, free rein within device rules)

You are an exploratory worker, approved by Brad on 2026-09-24. Unlike a normal brief, **you choose the experiments.** The goal is to find why the Odin's GS output diverges from the Mac, and ideally to make one Odin replay frame look like the Mac's. Read `~/dev/AGENTS.md` (worker rules; the exploratory freedom below overrides the "follow the brief exactly" scope), repo `AGENTS.md`, `local/AGENTS.local.md` (Odin facts, bytesize access), then `local/research/N8D7M12P7/{ORCH-GATE-7B.md,launch.py}`, `local/research/N8D7M13/{ORCH-GATE-A.md,REPORT.md}` (and its Part B result if committed), `local/research/N8D7M12P5F1/REPORT.md` and `N8D7M12P5F2/REPORT.md` (renderer hash/sync/readback path map).

## Facts to start from
- Same recorded GS stream (`f6a78f71…a593`, 1,100,696,462 B, already on the Odin as `<FILES>/n8d7m6.gs`) replayed offline with the paraLLEl backend on Turnip. Harness: `N8D7M12P7/launch.py` (APK `~/dev/ssx3-work/N8D7M12P6M6/app-release.apk` `da9a41a8…ef262`; install, `ps2x.env`, one `am start`, pull hashes + `vq-002050.ppm`, restore env, force-stop). ~13 s per run at STEP=50.
- The GS worker consumes **identical commands** on the Odin and the Mac (41/41 PKTSEQ digests). Guest priv hashes are equal. **Odin VRAM ≠ Mac from tick50; two Odin runs agree until tick800 and then diverge.** Odin tick2050 frames are ~97% black with run-varying rectangular fragments; Mac frame is the full race (`~/dev/ssx3-work/N8D7M12P7/trio.png`).
- On the Mac, per-tick sampling (STEP=1) leaves VRAM unchanged, so the Mac rows at `~/dev/ssx3-work/N8D7M13/mac/parallel.hashes` (STEP=1) and `N8D7M12P5F4P2/replay-excerpt.txt` (STEP=50) are ground truth.
- `ps2x.env` lines are `setenv`'d before `main` (`ps2xRuntime/src/lib/ps2_android_runtime.cpp`), so **driver env vars like `TU_DEBUG`, `MESA_*`, `VK_*` reach Turnip on the existing APK without a rebuild.** Confirm with logcat (`ps2x.env: set …`).
- Candidate levers (your call, in any order): Turnip `TU_DEBUG` flags (e.g. `sysmem`, `gmem`, `nobin`, `forcebin`, `flushall`, `syncdraw`, `noubwc`, `nolrz`, `nomultipos`; check the flag names against the Turnip source in the bundled Mesa pin, not memory); Vulkan validation/sync-validation layers pushed into the app's lib dir; paraLLEl-GS debug/serialization knobs in `~/dev/ssx3-work/N8D7F/parallel-gs` or the staged copy; the stock Qualcomm driver vs Turnip; readback path changes; smaller replays (`PS2X_GS_REPLAY_BISECT_TO`, lower PPM ticks) for faster iteration.

## Rules that still hold
- **Odin:** claim `/data/local/tmp/mg/LEASE` for the whole session (tag `N8X1 explore`) and release it at the end. Before every launch check `showing=false` (a locked Odin: stop and tell the orchestrator, never work around it) and `dumpsys battery` (≥20 %, and charging, or status 3 on AC at ≥90 %). **`am force-stop com.ps2x.runner` after every run.** Restore the device's original `ps2x.env` at the end (keep a copy first). Never delete the stream; install APKs with `adb install -r`.
- **Code:** experiments go on a new local fork worktree branch under `~/dev/ssx3-work/N8X1/` (from `4fa0df1`); never push the fork, never `git add -f` inside it, no history rewrites. Android rebuilds go through bytesize WSL like `local/research/N8D7M12P6M6R/` (external pinned wrapper; one heavy job on bytesize at a time); **at most 4 APK builds.** Mac builds for host checks are fine (nice'd; claim a mini slot for Mac replays).
- **Storage:** private scratch `~/dev/ssx3-work/N8X1/` ≤ 20 GB; run `local/tooling/disk_budget.sh` before/after big steps. No game data or binaries in git. No upstream contact of any kind. No iPhone.
- **Time box: 2 hours of wall time from your first command**, then wrap up (you may use 15 more minutes for the write-up).

## Deliverables (text in git, commit `[N8X1] …` with explicit paths, `git add -f`, trailer `Orchestrated-By: Claude Code`, no push)
- `local/research/N8X1/NOTEBOOK.md`, **append-only, one row per experiment as you go**: time, what changed (env/code/binary SHA), run result (rows, first VRAM split vs Mac/previous run, non-black pixel fraction of the tick-2050 frame), frame path, one-line takeaway. Commit it at least every ~30 min so progress survives.
- `local/research/N8X1/REPORT.md` at the end: the best explanation you have, with evidence rows and what would confirm or refute it; any candidate fix as a diff on your experiment branch (SHA + `git diff --stat`), not merged; a short list of what to try next; gaps stated plainly. Label hypotheses as hypotheses. No speed claims (these are diagnostic builds).
- Frames stay in scratch; name the 3–5 most telling ones with absolute paths so the orchestrator can view them.

Hand back when the time box ends or earlier if you have a confirmed cause.
