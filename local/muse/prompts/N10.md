# N10 — clean Odin speed baseline on the fork-tip APK (2 runs, no rebuild)

You are the N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, then `local/research/N7/{REPORT.md,launch.py,phases.py}` (the clean-speed method) and `local/research/N9/REPORT.md` (the APK and its gate section). Cite local files. Own `local/research/N10/` and scratch `~/dev/ssx3-work/N10/`. No build, no source edit, no push, no board/ledger edit.

**Why.** N9 showed the stock race rendering live on the Odin from committed fork code (GPU GS on Turnip, sound and widescreen on). Every Odin speed number predates the GPU GS and the sound fold. This is the first clean baseline of the current product path, plus a check that the rider actually moves (N7 saw 0–1 MPH at 2% progress over 21–38 s on the CPU GS).

**Build.** Use N9's APK `~/dev/ssx3-work/N9/…/app-release.apk` (SHA `25711bfe…08152`; find the exact path in N9's REPORT; two SHA reads before install). Its CMake cache has all six diagnostic flags OFF (N9 §8), so it's a clean build; only N9's runtime env turned frame dumps on.

**Env.** N9's live env **minus** `PS2X_FRAME_DUMP_ONCE_TICKS` (and any other dump/trace key): parallel backend, bundled Turnip, `PS2X_SKIP_MOVIE=1`, I26-FAST route with `PS2X_PAD_SCRIPT_CLOCK=vsync`, empty `mc0`, `PS2X_VSYNC_RATE_LOG=1`. No `PS2X_PGS_*`. Record the exact env file SHA.

**Runs.** Copy N7's launcher to `local/research/N10/launch.py`; change only APK path/pins, env, labels, and caps: **wall cap 750 s** (granted), stop at guest tick 4500 (≥ 45 s of racing after the race HUD at ~1714). Device rules each run: lease, `showing=false`, battery `AC powered: true` and ≥ 20 %, `adb install -r`, one launch, BACK for the USB dialog, force-stop after, restore `ps2x.env`, release the lease. Take a device screencap at race ticks ~2100, ~3000 and ~4000 (HUD clock, place, progress %, MPH). Two runs (L1, L2), same env; no retries; stop on the first failed step.

**Deliverable.** `local/research/N10/REPORT.md`: per run and phase (title, menus, loading, race) guest vsyncs/s and ×(÷59.94) via `phases.py`, run-to-run spread, SurfaceFlinger presents/s, thermal/clock notes, screencap readings (does MPH/progress increase?), exact commands, env SHA, gaps. Label it clean speed only if both runs complete. Commit `[N10] …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. Budget 60 min, 2 launches.
