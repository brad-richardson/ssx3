# N1 — Android native preparation audit: manifest, CMake, logcat, FFmpeg, linkage (READ-ONLY, no build)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`).
**Tables + receipts, no verdicts beyond the missions.** Read first:
the fork's `android/README.md` in full, `docs/research/
review-2026-09-22-progress-and-parallelization.md` §Android (the
track deliverable: reproducible arm64 APK + linkage manifest +
retained logs + app-owned boot paths + one stock-title launch),
`local/research/I24/REPORT.md` §Task-2 (the iOS packaging/shaping
lessons that transfer: bundle layout, staged sets, vector
diagnostics) and §cheap-rechecks (what "served to guest" means).
New lane "N" (native Android). N1 audits and plans. It does not
build, does not install, does not run.

## Facts you start from

- The fork already has `android/README.md`, a NativeActivity
  manifest, and Android CMake paths (per the review — VERIFY each
  exists at `3adc0478` and quote what's there). The README records
  missing runtime stdout/stderr logcat routing, and FFmpeg defaults
  off on Android. iOS's working decoder (I23/I24) reduces design
  uncertainty but is not an Android build.
- Track deliverable (NOT N1's — N1 scopes it): reproducible arm64
  native APK, generated-title linkage/binding manifest, retained
  runtime logs, app-owned boot paths, one stock-title launch to a
  named checkpoint. A replayer binary or an emulator race does not
  pass. Runtime API edits go through E-lane ownership (name them,
  don't make them).
- Standing SSD rule: read-only audit, but quote shas/sizes for
  every file you cite (2+ matching reads where the bytes matter;
  `COPYFILE_DISABLE=1` on SSD steps).

## Gates and rules

- STRICTLY read-only: no builds, no installs, no device contact
  (no adb — not even presence checks; N1 never leaves the Mac),
  no boots, no lease, no fork operations beyond read-only
  (`rev-parse`/`status`/`ls-files`/`show`), zero fork edits/
  commits/pushes.
- Byte caps: evidence `local/research/N1/` text-only; no SSD
  writes at all (no `n1-*` scratch needed — if you need scratch,
  `/tmp/n1-*` ≤ 512 MB); zero deletions.
- Time box 6 h. Evidence commit `[N1]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 1 — Scaffold audit (quote, don't summarize)

Table the ACTUAL paths: `android/README.md` (exists? key claims
quoted), the NativeActivity manifest (package, activities, intent
filters, permissions — quoted), the Android CMake entry + toolchain
file (quoted), the logcat-routing gap (where stdout/stderr go today
— name the code), the FFmpeg-defaults-off site (name the flag +
what enabling entails), codegen/packaging reuse points (what iOS
or desktop already builds that Android can consume as-is). Per row:
path + exists/missing + quoted evidence + blocks-N2? (yes/no).

## Mission 2 — Linkage + boot-path inventory

From the fork sources (read-only): how does the runtime locate the
title (ELF path? ISO path? app-relative? env?). Table every
hardcoded or env-driven path the Android app will need to own
(`PS2X_CD_IMAGE` et al. — name each + default + owner). Inventory
the generated-title linkage surface (how many objects/symbols the
APK must link — counts + the codegen manifest if one exists).
Compare against the iOS staged set (I24 §Task-2: .app + ELF + ISO
+ provision + vector) row by row: what transfers, what differs.

## Mission 3 — N2 plan (scoped, costed, gated)

Write the N2 brief-input (not the brief — one section): the exact
first build (target, toolchain, flags, expected outputs + sizes),
its success bars (exit + `file` + symbols + size budget), the
stop rules (what failure tables N2 vs what failure re-scopes the
track), and the ordered N2→N5 milestone chain to the track
deliverable with per-step inputs. Name every runtime API edit the
track will need from E-lane (file + symbol + why) as a handoff
table — E owns them.

## Success bars (tabled)

Scaffold table with quoted evidence + blocks-N2 flags; path +
linkage inventory with iOS comparison; N2-input with build spec +
bars + stops + milestone chain + E-handoff table; zero mutations;
tail receipt with byte-exact prefix sha.
