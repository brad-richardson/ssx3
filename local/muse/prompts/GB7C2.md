# GB7C2 — one bounded CPU title-text pixel chain (Muse Contributor Go)

You are an OpenCode Go G-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, GB7B and GB7C1 `REPORT.md`/`ORCH-GATE.md`, and GB7C2 in `docs/todo.md`. Own only clean GB4 private fork worktree `~/dev/ssx3-work/GB4/PS2Recomp` at `09d583a36ee62c165ce9e0473b945821a7a128f7`, branch `gb4-parallel`, plus `~/dev/ssx3-work/GB4/run/gb7c2/` and `local/research/GB7C2/`. G43 stays `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd`. No source edits outside CPU GS backend/header and replay-test harness; no live boot/device/upstream/push. **Do not build/replay while N8D7L holds the mini build/replay slot; prepare the source patch and wait for my explicit release.**

Pin the same capture SHA `a6f75fb34fceccf6fd92ad65950d16f05a140cf38a3cc2f028be32e56ad51851` twice before replay and use `run/gb4p4.paths.txt` for true GIF path. I independently checked full GB7B TSV: the tick600 path2 off-screen T4 glyph candidates share packet **47176** (rects x343–407, y378–387), and the **first following** same-column `fbp=112`/`tbp0=0` display blit is tick601 path3 packet **47240** (columns x319–350, 351–382, 383–414). Packet47117 precedes the candidate and cannot carry it. Focus only on this C1 chain; no tick699 candidate.

Implement one default-OFF, <=8 MiB/20,000-row CPU diagnostic around the existing draw path. For each C1 sprite, capture an intra-packet batch id, FST/STQ, a small fixed set of interior pixel coordinates, actual texture sample/CLUT or RGBA, TEST/ALPHA outcome, and the destination pixel's independent old value immediately before a **successful** write plus the new value. The existing `WritePixel` old read at line1181 is conditional on `frmw`; do not call it proof when skipped. For each candidate batch, compute an off-screen ROI `(340,375)-(410,415)` before/after diff with changed-pixel count and bounding box/coordinates (not a handful of chosen pixels as a glyph-shape claim). Trace packet47240's source UV→destination pixels and take the displayed lower crop `(340,360)-(430,420)` before/after through the existing GB5B crop helper. Stop with an explicit unknown if a full-cover writer intervenes, the source pixel is resampled elsewhere, or the packet/ROI budget cannot be met. Flag unset must leave render output byte-identical; no tuning loop.

Correct model: if p47176 changes localized off-screen pixels with a traced texel chain and p47240 samples those pixels into the displayed crop, it identifies a CPU glyph producer/carrier. A coincident rectangle or crop hash alone does not. No CPU trace proves the paraLLEl damage cause. Predeclare PASS/OTHER and exact stop before replay. Validation after release: one incremental `ps2x_tests` build, one flag-OFF full suite, one direct CPU replay through marker700 with trace ON and saved PPMs at 600/601/700. Confirm same-stream frame hashes against GB7B OFF control and view saved frames/crops; logs/images <64 MiB, committed text <256 KiB. No GPU replay. Check runner-dir diff against `14b1e5cb` empty and global disk <200 GB.

Deliver REPORT, <=30-row selected chain table with `tick,packet,path,batch,src_xy,dst_xy,old,new,texel,roi_diff,crop_diff,classification`, exact commands/pins/caps and gaps. Commit fork patch `[GB7C2]` and ssx3 named receipts/brief `[GB7C2]`, both `Orchestrated-By: opencode`, no push. Hand back evidence; the orchestrator decides the glyph-producer verdict and any GPU comparison.
