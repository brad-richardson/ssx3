# E21 — Resume E20: prove observer forwarding, then Q1→Q2→Q3 (Muse worker)

You are the Muse E-lane worker in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Frontier stalled E20 on a `cyber_policy` content
flag (observer interposition code tripped it — first-party debugging of our
own harness; no policy issue for this brief). You resume from its preserved
partial work.

## Read first (all of each)

1. `local/research/E19/REPORT.md` + `local/research/E19/NEXT-BRIEF.md` (E19's
   stop + the parked Q1/Q2/Q3 order — they BIND this brief).
2. `local/research/E18/NEXT-BRIEF.md` (parked input→packet questions + ABI
   constraints — BINDING).
3. `local/muse/prompts/E20.md` (the mission you inherit: repair observation,
   THEN Q1 threshold → Q2 demand audit → Q3 guarded boot; fix gate unchanged).
4. E20's partial landing, commit `51d786c` (`local/research/E20/` — READ-ONLY
   reference; do not edit. Your dir is `local/research/E21/`).

## State E20 left (re-verify, don't re-derive)

- Fork `3adc0478` triple-verified post-remount; 9,457 generated + 1,725
  protected hashes equal (`reconnection.json`, 20:37Z).
- Checkpoint regression AFTER remount rc=0: suite 458, closure 6, E15 rc0s,
  boot 0 (`checkpoint-complete.json`, 20:39Z).
- Observer forwarding repair EDITED but UNPROVEN: `e20_parser_observer.c/.h`
  (direct-import bindings replacing `RTLD_NEXT` + parse/send/receive/exit
  recursion guards + proof events + schema-2 stats). This is your starting
  input — copy to `e21_*` and continue there.
- SSD scratch owned paths: `/Volumes/Extreme SSD/ps2recomp-spike/P1/e20-*`
  (fixtures + tooltmp). Reuse or cut `e21-*`; internal volume stays clean.
- SSD flapped twice today — re-run the admission/hash checks before trusting
  anything on it. `/tmp/p1-link` + `/tmp/e17-map-link` + `/tmp/e18-mpeg-link`
  + DerivedData are PROTECTED (never reclaim).

## Mission (E20 brief, resumed)

1. Re-verify checkpoint (fork SHA + `fork/ssx3`, 9,457-name audit, suite
   458/458 UNLOADED, E16 closure, E15 rc0s, E18 R1–R6). Any red → table + stop.
2. Build the repaired observer from the `e21_*` copy; prove 1:1 forwarding
   in ISOLATION first (minimal non-title harness: one real backend invocation
   per observer call, identical args/returns, byte-identical vs
   uninstrumented; prove real `_Exit` counter closure separately). Table the
   mechanism (why `RTLD_NEXT` lied here) — E20 chose direct imports; verify
   the claim, don't inherit it blind.
3. Loaded full regression (suite 458/458 + E18 fixtures + E16 closure + prior
   bindings WITH the repaired observer). Red → table + stop (no boot on red).
4. Q1 threshold fixtures (5,040 / 8,192 / 16,384 authored-byte marks; E19's
   retained first64 + authored cases carry over — re-verify). Table
   bytes→packets/frames + the packet-1 threshold.
5. Q2 static demand audit (who asks for more, with what signal, or the proven
   absence — E18's post-input silence is the baseline).
6. Q3 ONE guarded demand-watch boot (fresh T13 pre-claims, shared P-lane
   lease, wall/progress/byte caps, REPORT_ALL=1 + parser-boundary
   observations; lease occupied → table and stop).
7. Fix gate (strict, unchanged from E20): implement ONLY if the diagnosis
   isolates ONE edge with a minimal ABI-preserving change. "Guest never sends
   more without X" → NAME X and STOP.

## Rules

- No CSV/regen (pure runtime/diagnosis brief — regen need = stop).
- No fork behavior commits except through the fix gate (then: fail-before +
  full regression + push, no force-push).
- E20's failed dylib + E19's failed dylib are evidence artifacts — never load
  either for suite/title runs.
- Commit `[E21]` evidence + push when done (verify-then-push). REPORT.md +
  NEXT-BRIEF.md, same shape as E19/E20.
