# M39 — s3 all-delta-1 row: row-list + value comparison vs s2/s700's mixed cells (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M36/REPORT.md` first (all of it —
M36 gap 3 is this brief, taken on orchestrator judgment: all 7
of shape 3's cells read delta-1 (4 extra-only + 3
missing-only, zero mixed) — the only still-below row with no
mixed cell), plus `local/research/M38/REPORT.md` (the pooled
splits: missings 10/10 noncell on own Q, extras 14/10
bulk/noncell on s0; corrected pooled universe 24 = c54's 10 +
14 others). Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- s3's 7 cells (M36 — reproduce exactly or table the mismatch
  and stop): extra-only c311/c316/c336/c339 (extras
  (258,311) |δ| 34; (265,316) |δ| 8 bulk; (238,336) |δ| 8
  bulk; (238,339) |δ| 8) + missing-only c313/c314/c332
  (missings (276,313) |δ_s0| 8; (278,314) |δ_s0| 20;
  (295,332) |δ_s0| 10). Zero mixed.
- s2/s700's mixed cells (same method, same pins): s2 c313
  (extra (293,313) + missing (276,313)) + s2 c332 (extras
  (296,332)/(297,332) + missing (295,332)); s700 c332
  (extra (296,332) + missing (295,332)). Deltas >1 live
  only here (s2 c332 delta-2; check the rest against M36).
- The question: is s3's uniformity values-deep (its extras +
  missings read like the mixed cells' extras/missings at
  delta 1) or standing-only (mixed cells' delta-1 sides
  differ in |δ|/gaps/signs)? Answer by table: per-cell
  row-lists + per-site values + gap-sign splits, s3's 7 vs
  the 3 mixed cells.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m36/m36.txt`
  (s3's 7 cells + the 3 mixed cells to reproduce — match
  exactly or table the mismatch and stop),
  `/Volumes/Extreme SSD/m34/m34-census.tsv` (FULL TSV guard).
  Work dir: new `/Volumes/Extreme SSD/m39/`; evidence
  `local/research/M39/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m38/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m39/` + `local/research/M39/`.
- Evidence dir `local/research/M39/` (STANDALONE). Commit with
  `git add -f`, prefix `[M39]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M38 precedent).

## Task 1 — s3 row + mixed cells recompute (do the cells reproduce?)

s3's 7 cells + s2's 2 + s700's 1 mixed cells recomputed from
the dumps (must match M36 exactly — row lists, standings,
deltas, per-site |δ|/status/gaps — or table the mismatch and
stop):

1. Row-list table: all 10 cells s0-rows vs Q-rows vs
   extra/missing rows (M36-headliner style).
2. Standing table: 7 × delta-1-uniform vs 3 × mixed (extra
   counts + missing counts + deltas per cell).
3. Value table: per-site |δ_Q|/|δ_s0|, statuses, signed gaps,
   gap signs for all 10 cells' sites.

## Task 2 — uniformity comparison (values-deep or standing-only?)

1. Extra-side comparison: s3's 4 extras (|δ|, bulk status,
   gaps, signs) vs the mixed cells' extra sides (same
   columns — med/match per site, numbers only).
2. Missing-side comparison: s3's 3 missings vs the mixed
   cells' missing sides (same columns).
3. Delta split: per-cell |extra delta| vs |missing delta|
   across the 10 cells (where does magnitude >1 live?
   extra side, missing side, or both?).

## Task 3 — controls + determinism (M18–M38 precedent)

1. Positive control: synthetic truth with KNOWN uniform +
   mixed cells (N delta-1 cells, N mixed cells, known
   values) — recover the standing + value tables exactly.
2. Determinism: re-run Task 1 on s0+2+3+700, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M38).

## Report

`local/research/M39/REPORT.md`: DESIGN.md (bars first) +
recompute tables + comparison tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a uniformity map
discriminates (budget-noted, pre-registered split required
per M23–M38 precedent). Commit evidence (`[M39]`, no push),
stop.
