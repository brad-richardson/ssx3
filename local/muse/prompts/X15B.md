# X15B — same dense Qwen excerpt, short thinking budget

You are the **dense Qwen3.8-27B** opencode worker in a controlled retry. The pane sets `thinking_budget=512` through `OPENCODE_CONFIG_CONTENT`; do not edit global or project OpenCode config. Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, and `local/AGENTS.local.md`. Read only this brief and `local/research/X15/excerpt.txt` (11,013 bytes, SHA-256 `eac602143522d2ec2fe3c6b062070ee822c32a5fecd258400a9e66a1a102c146`). Cite the repo excerpt, not the web. No search, other source read, build, boot, device, web, push or source edit. `local/research/X15B/rows.tsv` and `REPORT.md` are your only write paths. X15's default-budget pane produced no table before stop; do not read its audit.

Within 60 seconds of the excerpt read, write `rows.tsv` with header `field<TAB>value<TAB>evidence` and exactly these eight rows in order: `EN1`, `EN2`, `INT`, `FFMD`, `force_progressive`, `is_interlaced`, `raw_circuit_early_return`, `deinterlace_branch`. Values are only `0`, `1`, or `unknown`. Interpret pinned `pmode=0xff21 smode2=0x1` through the excerpt's bitfields and follow backend→interface→renderer branch predicates. For raw-circuit return, use 1 only if every prerequisite allows it; for deinterlace, use 1 only if the shown branch executes. Evidence is exact `filetag:line` anchors separated by semicolons, no prose. Use `unknown` for any unsupported answer. Do not infer image quality or a driver cause.

Try one `lsp goToDefinition` at the actual file `/Users/brad/dev/ssx3-work/N8D5L/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp` line 255 on `vsync`; record exact target or empty/error. Do not read another file through LSP. Wall cap five minutes, context target <18k. `REPORT.md` lists excerpt SHA, commands, elapsed time, visible context if available, LSP result, the table and gaps. No checker is available to you; hand back the table for independent scoring. Commit only `rows.tsv` and `REPORT.md` as `[X15B]` with `Orchestrated-By: opencode`, no push.
