# G19 — O4 discriminator: verify/rebuild the fixed binary, re-run with PGS_SKIP_COMPILATION_TASKS=1 (Odin, no tree changes)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G18/REPORT.md` (ssx3 mirror) first (all
of it: static cause NAMED — raw-vs-derived descriptor-buffer flag
divergence at `shader.cpp:531`; one-hunk derived-gate fix; rebuild exit 0;
re-run exit 139 PASSED O2's exact site (main past the first-draw
texture-upload dispatch to `dispatch_triangle_setup`) then died on a NEW
wall O4: SIGSEGV fault `0x0` in `libllvm-qgl.so` inside
`vkCreateComputePipelines`, async worker tid 20753, main concurrently in
the same call; the ONE next action = this brief). This brief executes
G18's §4. No tree changes of any kind (see the sha erratum below — a
VERIFY-then-rebuild is explicitly authorized and is not a tree change).

## Facts you start from

- Pins (re-verify, do not move): paraLLEl-GS
  `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims + G11/G8+G10
  hunks + G18 fix hunk in `Granite/vulkan/shader.cpp` (all uncommitted in
  the SSD clone — verify the hunk bytes match `g18-fix.diff` BEFORE any
  build); rich dump sha `154d9d85…`.
- SHA ERRATUM (orchestrator-found, read carefully): G18's report records
  the fixed binary as `a62d8a2b…`, but the on-disk file now reads
  `c568dc23…` and contains NEITHER the report's bytes NOR the tombstone's
  build-id (`0b01b274…`) — a post-run tree mutation of unknown cause (same
  anomaly class as G15's link mismatch). The G18 run evidence stands
  (tombstone + logcat + behavioral proof of passing O2's 4/4 death site),
  but you must NOT trust the on-disk binary. Step 1 below is mandatory:
  rebuild the fixed binary from the pinned tree + verified hunk
  (`-j2`, may reuse the G18 build dir — record dir growth), verify the fix
  is in the fresh binary (rebuild log shows the `shader.cpp` compile +
  sha ≠ G14 baseline), and record the fresh sha. THAT binary is the one
  the knob run tests. If the fresh sha equals NEITHER prior value either,
  table all three + the build-id and proceed (link nondeterminism is then
  tabled, not blocking — the fix hunk + behavior are the identity).
- Device: Odin3 (`622c49b1`, Android 15). Transient dir
  `/data/local/tmp/g19/` ONLY; `mg/` never touched; remove the dir at end.
- The task: ONE bounded run of the verified fixed binary with
  `PGS_SKIP_COMPILATION_TASKS=1` (env knob at `gs_renderer.cpp:459-464`,
  skips async precompilation — re-verify the knob's file/lines before use).
  Branch (G18 §4): O4 disappears ⇒ crash needs the async/concurrent path
  (driver thread-safety vs async-path issue — table which the receipts
  support); O4 persists on main's sync compile ⇒ Adreno compiler bug on a
  specific shader (table the minimizing facts for a filing package).
  ONE retry allowed ONLY if O1/O3 fires instead of first draw (the G18
  lottery condition, carried forward).
- Success bars (tabled, not verdicts): hunk verified + binary rebuilt +
  fresh sha recorded; knob verified at its file/lines; ONE bounded knob
  run (exit, wall, frames, fate, tombstone/logcat triage); branch resolved
  with receipts (or the exact obstacle tabled per the stop rule); score
  via `g14-diff.py` (or N/A); hypothesis verdict + the ONE next action.

## Gates and rules

- Stop rule (G18 shape): TWO bounded device runs max (knob run + one retry
  iff O1/O3 fires instead of first draw) — no tuning loop, no renderer
  changes, no new dumps, no source fix in this brief. No lldb unless the
  run needs triage the tombstone cannot answer (table the decision).
- Byte caps: declare up front for the rebuild (dir growth, SSD) + retrieval
  (SSD `ps2x-g19/`); all prior `ps2x-g*` read-only (0 growth); internal `/`
  delta ≤1 GB, no clones/builds on internal; `export COPYFILE_DISABLE=1`
  on SSD steps. No P-lane lease (no recomp boots/builds, no fork writes).
  No bytesize/WSL. Host builds `-j2`.
- Clone discipline: ZERO clone edits (the rebuild changes no sources).
  No third-party code copied into any project tree.
- Evidence: ps2xGS commit `[G19]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G19/`
  (`git add -f`, prefix `[G19]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — verify hunk + rebuild + verify binary (no device yet)

1. Re-verify pins (clone HEAD/status, G18 hunk bytes vs `g18-fix.diff`,
   dump sha host-side, knob file/lines). Record the on-disk binary sha
   as-found (expect `c568dc23…` per the erratum — table what you find).
2. Rebuild the replayer (`-j2`); table binary path/size/fresh-sha +
   build-id + fix presence (rebuild log + sha ≠ G14). Table all known
   shas (report `a62d8a2b…`, as-found, fresh) + the conclusion.

## Task 2 — ONE bounded knob run + branch (Odin)

1. Push binary + dump (sha match on-device), run bounded with
   `PGS_SKIP_COMPILATION_TASKS=1` (`--iterations 2`, wall cap), capture
   exit/wall/frames/fate + logcat + tombstones (if any). Pull receipts;
   remove device dir.
2. Resolve the branch with receipts (async-path vs sync-compile bug) +
   filing/minimizing facts as applicable. Score with `g14-diff.py` (or
   N/A). Hypothesis verdict + the ONE next action.

## Report

`local/research/G19/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, rebuild record, run table, branch resolution,
score, verdict, exact commands, gaps. Commit evidence (`[G19]` both
trees), stop.
