# I32 — iOS virtual controls: analog stick on the left, D-pad on the right

You are the I-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, `local/research/I26/REPORT.md` (virtual pad), `local/research/I30/REPORT.md` (current build recipe) and `local/research/I31/` (Brad's save + manual-play deploy script, once it exists). Own `local/research/I32/`, scratch `~/dev/ssx3-work/I32/`, fork worktree `~/dev/ssx3-work/I32/PS2Recomp` on local branch `i32-controls` from fork `ssx3` (current tip). No push.

**Why.** Brad is playing the I30 build on his iPhone and wants a **virtual analog stick on the left** (SSX 3 steers and tricks with the left stick) and the **D-pad moved to the right side**. The runtime already reports an analog DualShock (`ps2xRuntime/src/lib/ps2_pad.cpp:8,36`, marker `0x73`, sticks centred `0x80`) and maps a physical gamepad's left stick into the pad bytes (`:100-104`); the touch overlay just needs to drive the same bytes.

## Part 1 — implement + show Brad (Simulator only)
1. Find the virtual-pad overlay code (touch hit-testing and drawing; start from `touchPoints` in `ps2_pad.cpp:188` and the I26 commits) and cite it.
2. Left side: a **floating analog stick** (touch anywhere in the left zone sets the centre; drag sets direction/magnitude; release recentres), dead zone ~10%, drawn as base + knob; writes left-stick X/Y into the pad bytes the gamepad path uses. Remove the left D-pad.
3. Right side: face buttons stay where they are; put the **D-pad** on the right where it doesn't overlap them or the shoulder/Start/Select buttons (your layout call; keep hit targets ≥ 44 pt). Multi-touch must work (stick + a button at once).
4. Unit test for the stick mapping (touch offset → byte values, dead zone, clamping). Suite from the worktree root; runner-dir check empty.
5. Simulator Release build: screenshots of the menu and race with the new overlay (title, Select Character, race), and a short log proving stick values reach the pad bytes (drive it with simulated touches or a debug env that injects a stick vector). Commit `[I32] Part 1` with the frame paths (scratch, not git). **Stop**: the orchestrator sends Brad the screenshots for approval.

## Part 2 — after Brad approves (orchestrator says go)
Device build (I30 recipe), install on the **iPhone (install only, never launch)** and the iPad, then re-apply I31's `deploy-ios.sh` (Brad's save + manual-play env). One iPad launch to confirm the stick moves the rider in the race (screenshots), terminate. Commit `[I32] Part 2`.

Text only in git. Budget: Part 1 90 min, 1 Simulator build; Part 2 45 min, 1 device build.
