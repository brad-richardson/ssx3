# E52 — Audit: R5900 FPU and COP2 (VU0 macro) translation semantics vs PCSX2

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md`;
- `local/AGENTS.local.md`;
- `local/research/E50/REPORT.md` (Parts 2–3: two FPU translation defects,
  CVT.W.S rounding and SQRT.S operand/semantics, each at hundreds of sites
  and each silently breaking game math).

## Why

Those two defects hid for weeks because nothing tested translation
semantics op by op. Before chasing more symptoms (E51 is chasing the race
terrain), check every FPU and COP2-macro op the recompiler emits against
PCSX2's reference semantics.

## Sources

- **Recomp:**
  - `~/dev/PS2Recomp`, fork `ssx3` `eac6cba`: `ps2xRecomp/src/lib/`
    (`fpu_translator.cpp`, `vu_translator.cpp`,
    `vu_translation_helpers.cpp`, `cop0_translator.cpp`,
    `mmi_translator.cpp`);
  - the runtime macros in `ps2xRuntime/include/ps2_runtime_macros.h`;
  - the VU core (`src/lib/vu/`), for VU0 macro ops if they route there.
- **PCSX2 reference:** on bytesize, `/home/brad/pcsx2-g7/pcsx2`
  (`pcsx2/FPU.cpp`, `pcsx2/VUops.cpp`, `pcsx2/VU0.cpp`, `pcsx2/VUflags.cpp`).
  Copy the files you need into `~/dev/ssx3-work/E52/pcsx2-ref/` with
  `scp` (read-only use).
- **Usage census:** count each op's sites in canonical
  `~/dev/ssx3-work/codegen-ssx3` (grep the instruction comments or use
  `local/tooling/ee/ee-*`).

## Work (no boots; builds + unit tests only)

1. **Op table:** every FPU op (ADD/SUB/MUL/DIV/SQRT/RSQRT/ABS/NEG/MOV,
   MADD/MSUB/MADDA/MSUBA/MULA/ADDA/SUBA, C.F/EQ/LT/LE, CVT.S.W/CVT.W.S,
   MIN/MAX, MFC1/MTC1/LWC1/SWC1 edge cases) and every COP2 macro op SSX 3
   uses (VADD*/VSUB*/VMUL*/VMADD*/VMSUB*/VOPMULA/VOPMSUB/VDIV/VSQRT/
   VRSQRT/VFTOI*/VITOF*/VCLIP/VMR32/VMOVE/QMFC2/QMTC2/CFC2/CTC2/VCALLMS*,
   and so on). For each op give:
   - the encoding fields used (and whether the generator reads the right
     ones: E50's SQRT.S bug was a wrong field);
   - PCSX2's semantics: clamping, denormal flush, rounding/truncation,
     overflow saturation, ACC use, flags;
   - the recomp's semantics;
   - site count in SSX 3;
   - verdict: same / differs / unsure.
2. **Unit tests** for every op marked "differs" or "unsure", with PCSX2's
   results as the expected values (vectors that exercise the edges: ±0,
   denormals, ±FMAX, overflow, negative sqrt, rounding boundaries).
   Show which fail on `eac6cba`.
3. **No fixes.** Rank the failing ops by site count, and by likely effect
   (geometry/UVs vs rarely used). The orchestrator batches the fixes.

## Budgets

Builds as needed (your own worktree `~/dev/ssx3-work/E52/PS2Recomp`, branch
`e52-audit` off `ssx3`), 6 h, cap 3 GB. `nice -n 10`; wait while another
lane's build is running.

## Deliverable

`local/research/E52/REPORT.md` (op table, failing tests, ranking) +
the test file. `[E52]` commit (`git add -f local/research/E52
local/muse/prompts/E52.md`, `git log -1` first, trailer `Orchestrated-By:
Claude Code`). Fork commits stay on `e52-audit`; no push.
