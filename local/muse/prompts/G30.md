# G30 — VRAM-content vs circuit-sampling split: (a) composite writes unlanded/misplaced vs (b) sample_quad mis-samples on Adreno (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G29/REPORT.md` first (all of it: ladder
P1 = P2 = P3 = uniform `(0,0,0,128)` on all 10 images — readback exonerated
three ways; opaque-blit math makes scanout == circuit1 == VRAM cleared
pattern; graphics execution provably works; the defect sits between VRAM
bytes and circuit-fragment output). This brief executes G29 §4's ONE next
action ONLY. No upstream contact of any kind (standing no-upstream order —
local hunks only, filing stays local).

## Facts you start from

- The split: (a) composite/scene GPU writes never landed or landed
  misplaced in VRAM (then the sampler reads true cleared bytes) vs (b)
  writes landed correctly but `sample_quad` mis-samples/mis-decodes on
  Adreno (then it reads the wrong bytes). G29's VRAM checksum (1,184,729
  nonzero of 4,194,304) is load-state-confounded — landing suggested, not
  proven byte-wise.
- Discriminator (G29 §4): dump post-run VRAM bytes (or checksum circuit
  images inside `vsync()`) and compare against dump-load state + expected
  upload dests (DBP 10756/10820/11017 + 38 stream dests, G13 §2e).
- The G29 binary is 100% zero-destroyed (8th recurrence — orchestrator-
  quantified: 0 nonzero of 265,846,144 B; destruction completed AFTER G29's
  report-time read, so treat destruction as progressive, not instant).
  The G28 binary is INTACT (`450471e2…`, re-sha at start per hygiene).
  The G29 ladder hunk is still uncommitted in the SSD clone; G22/G26/G28
  hunks untouched (HUNK_MATCH re-verify at start).
- Task 1 (no device until §2): STATIC design — table the exact dump site
  (post-run VRAM bytes vs in-`vsync()` circuit checksums vs both), the
  expected-upload map (DBP addrs + stream dests from G13 §2e, re-verified
  against the dump), and the decision matrix ((a)/(b) signatures +
  what mixed outcomes mean). Name the ONE hunk.
- Task 2 (ONE run max): the content probe — ONE hunk max (diagnostic-dump
  ONLY), ONE build max (NEW dir, G22 recipe, `-j2`), full identity,
  verify-then-push with NO gap, TWO bounded runs max (diagnostic + one
  retry iff O1/O3 fires instead of first draw). The ladder may be retained
  for continuity or removed — table the choice + why (retention keeps the
  P1/P2/P3 control; removal shrinks the delta).
- Success bars (tabled): design table (site + expected map + matrix); the
  (a)-vs-(b) VERDICT (with the discriminating bytes); if a run:
  exit/wall/fate + receipts + tombstone triage (if any) + scanouts scored;
  the ONE next action (fix shape vs next wall — adoption stays queued
  until brightness is understood AND this split closes).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g30/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (diagnostic-dump ONLY); ONE build max (new dir, G22 recipe,
  `-j2`); TWO bounded device runs max; no tuning loop, no second shape, no
  new dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g30/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G30/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G30]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).

## Errata in (carry, don't re-fix)

- G29-E1: the ladder's "FNV-1a" uses truncated offset basis
  1469598103934665603 (standard is 14695981039346656037 — dropped digit).
  Proofs hold (both sides same function); if you reuse the checksum
  lambda, keep the SAME basis for comparability and note it.
- G29-E2: G29's report-time binary sha (`e353aabf…`) was a mid-destruction
  read; current is all-zeros (`7c8593bd…`). Re-sha timing matters — take
  push-time + post-run + report-time reads.
