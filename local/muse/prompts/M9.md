# M9 — Propagation mechanism: VAT paths, texgen enablement, upload timing

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M8/REPORT.md` (all of it: slot-36 zero, Tex2-only
reference, the "seam writes don't reach rendering" re-scope, "What I
could not do" gaps 1–3) and `local/research/M7/REPORT.md` Step 3 (slot-0
PosNormal zero). Base header `local/research/M8/m8_replay_context.h`
(sha in the M8 report — record `shasum`, not memory; copy it to
`local/research/M9/m9_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M9/` (commit with `git add -f`, prefix
`[M9]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m9/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M9/` as a research header derived from M8's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M9\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M9/waits.log`. A P1p agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M8/build_replay_player.py` to
  `local/research/M9/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M8 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m9-det`, `m9-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M8-header copy, run `m9-det`
   once with the M8 slot-36 delta env, confirm M8's end state reproduces
   (53 hits/replay, 0/200 differ, guest/event clean). Control for later.
2. **Close the M8 gaps (VAT + texgen).** Decode per-draw VAT state
   (`VB_HAS_POSMTXIDX` / `VB_HAS_TEXMTXIDXi`) and track per-draw texgen
   enablement through the `numtex_w` in-stream writes. Receipts: per-draw
   path table (shared vs indexed, per position + per texgen), texgen
   enablement timeline, and the corrected answer to "which draws can
   slot 36 (Tex2) and slot 0 (PosNormal) actually move".
3. **Upload-timing test.** Does the perturbed word survive to the draw?
   Instrument (header-only): at each draw referencing a delta slot, log
   the live xfmem word value (perturbed or pristine?) alongside which
   matrix source the shader actually consumes (live xfmem read vs
   `SetConstants`-uploaded snapshot vs per-vertex indexed copy —
   code path + run receipt each). Receipts: the survival table per
   draw class + the named mechanism (upload timing / shadow copy /
   indexed-copy bypass / other), or the narrowed residue.
4. **PosNormal-path delta.** Apply +0.1 to a slot the Step-2 receipts
   prove flows through the SHARED position path on visible draws (if
   none exists, say so with the table and stop after Step 3). Receipts:
   `xform_stats`, per-replay xdiff/xdmax/xdmean, `done`, guest/event
   counters, continuation hash — the first nonzero-pixel run if the
   mechanism permits it.
5. **Report.** `local/research/M9/REPORT.md`: per-step tables (VAT/texgen
   closure, survival table + mechanism, PosNormal delta), wall rows,
   continuation, header diffs per step (`diff -u` against the M8 header),
   exact commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M9/` (header, driver copy, analyzer
   changes, REPORT, waits.log). Stop.
