# M46 — Shape-9 far/near private split: per-sub-block value/join split (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M33/REPORT.md` first (all of it —
M33 gap 1 = this brief via M36 gap 6, taken on orchestrator
judgment: 16/22 cluster privates (rows 226–252) read dmin
18–49 with 12/16 far-bulk on s0 (1 near, 3 non-cell), while
6/22 (rows 269–305) read dmin 1–7 interleaved with the
missings in two mixed union comps), plus `local/research/M35/
REPORT.md` (shape-9's per-site values — the value side of the
split). Fully offline — SSD dumps read-only, no harness code,
no replays, no lease. No device work.

## Facts you start from

- The 22 cluster privates + dmins (M33 displacement table —
  reproduce exactly or table the mismatch and stop):
  - Far block (16, rows 226–252, dmin 18–49): (226,351)/26,
    (232,327)/43, (233,327)/42, (234,337)/48, (235,337)/49,
    (239,336)/45, (239,339)/48, (240,318)/26, (241,318)/25,
    (243,318)/23, (244,316)/20, (245,315)/20, (245,316)/19,
    (246,314)/20, (248,312)/20, (252,310)/18.
  - Near block (6, rows 269–305, dmin 1–7): (269,313)/3,
    (270,313)/4, (274,322)/7, (289,315)/3, (290,315)/4,
    (305,332)/1.
- Statuses (M33): far block 12/16 far-bulk on s0 (1 near, 3
  noncell); near block interleaved with missings in two
  mixed union comps (reproduce the comp membership).
- The question: per-sub-block value/join split — do the far
  16 and near 6 differ in |δ|/gaps/signs/band/decile, or is
  dmin the only split? Answer by table: displacement
  reproduction + per-block value tables + join seats.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m33/m33.txt`
  (the 22 dmins + statuses to reproduce — match exactly or
  table the mismatch and stop), `/Volumes/Extreme
  SSD/m34/m34-census.tsv` (FULL TSV guard). Work dir: new
  `/Volumes/Extreme SSD/m46/`; evidence `local/research/M46/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m45/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m46/` + `local/research/M46/`.
- Evidence dir `local/research/M46/` (STANDALONE). Commit with
  `git add -f`, prefix `[M46]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M45 precedent).

## Task 1 — displacement reproduction (do the 22 dmins reproduce?)

The 22 dmins recomputed from the dumps (M33 method verbatim:
nearest-shared-site same-plane Manhattan; must match exactly
— or table the mismatch and stop):

1. dmin table: all 22 sites with recomputed dmins (far
   18–49 + near 1–7 + hist 1:1 2:0 3:2 4:2 5+:17).
2. Status table: far-bulk/near/noncell per site (far block
   12/1/3 + near block statuses — the new data).
3. Comp table: the two mixed union comps' membership
   (which near privates + which missings per comp?).

## Task 2 — per-sub-block value/join split (values-deep or dmin-only?)

1. Value tables: far-16 vs near-6 |δ_9| lists/meds, gap
   lists, gap-sign splits, bulk statuses (numbers only).
2. Join seats: band + gradient-decile distributions per
   block (M35 §Task 2 joins recomputed per block — do the
   blocks share seats?).
3. Missing-contrast table: the 8 missings (dmin 1–2, d1
   share 0.75) vs the near block (dmin 1–7 — do near
   privates read missing-like in values/seats?).

## Task 3 — controls + determinism (M18–M45 precedent)

1. Positive control: synthetic truth with KNOWN far/near
   blocks (N far-dmin + N near-dmin privates, known
   values) — recover the dmin + value + seat tables
   exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M45).

## Report

`local/research/M46/REPORT.md`: DESIGN.md (bars first) +
displacement tables + split tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a sub-block map
discriminates (budget-noted, pre-registered split required
per M23–M45 precedent). Commit evidence (`[M46]`, no push),
stop.
