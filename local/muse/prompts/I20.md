# I20 — E15-ABI agreement + sema-creator census + MPEG-fix-ready spec (NO implementation — fork owned by E-lane)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/I19/REPORT.md` first (all of
it: DIAG_SEMA census — 26/30/31/32/36 signalers named, creators UNOBSERVED
(H-b: separate `PS2X_DIAG_SEMA_CREATE` env unset); feeder audit (non-stream
callbacks ZERO readers, guest-only feeder, no-FFmpeg STUB decoder,
`decoderFailed` never true, CD EOF only via the unentered St path); fix
PROPOSAL F1–F10 with P1–P3 preconditions on E15). Then read
`local/research/E15/REPORT.md` + `local/research/E15/ABI.md` + `local/research/E15/NEXT-BRIEF.md`
(all of them: E15 LANDED — host-side type-1 request/delivery probe with
original-ELF ABI receipt (register/select/invoke/type-1-data/trigger/
ownership/callback/source shapes), measurement-incomplete ONLY on shutdown
footers; NEXT-BRIEF imposes closure-repair-before-any-implementation + the
§4 parked design constraints). This brief CLOSES I19's two gaps (E15
comparison + creator census) and produces the fix-ready spec. It does NOT
implement (E-lane owns the fork; E15 NEXT-BRIEF + no-stacking precedent put
closure repair first and the MPEG fix in a later E brief — table that
handoff, do not jump it).

## Facts you start from

- BASE: NO MOVE without cause — E-lane advanced to `67c0a632` (E15
  observation commit, pushed). Your worktree pins wherever the brief's
  reads need (table the base + why); ZERO ports expected (no build ⇒ no
  ports); ZERO fork commits (read-only audit + device probes).
- E15's ABI receipt is the P1–P3 input: build the AGREEMENT table
  host(E15)-vs-device(I19) per ABI edge (registration type word,
  select/invoke shape, type-1 data + forced v0, IPU trigger, caller-thread
  ownership, guest callback reorder, source path, no-source branch) with
  file/line or console receipts on BOTH sides; disagreements tabled with
  both receipts, never reconciled by fiat.
- Creator census: ONE config-only relaunch of the installed I18 app with
  `PS2X_DIAG_SEMA_CREATE=1` ADDED (keep `PS2X_DIAG_SEMA=1` +
  `PS2X_DIAG_PERIOD_MS=15000`) to census sema creators (I19's exact tabled
  obstacle). If the create gate yields nothing, receipt the obstacle and
  stop at one probe — no second shape.
- Fix-ready spec: starting from I19's F1–F10, produce the implementation
  spec the E-lane MPEG brief will consume — exact files/functions,
  delivery semantics preserving E15 §4 constraints (caller-thread
  ownership, no MPEG-mutex-held AddBs reentry, type-1-only trigger, no
  invented layouts) + the FFmpeg-OFF stub residual as the SEPARATE
  device-side edge (I19) vs host FFmpeg-ON. Regressions: E13 §5 + E15
  actual-wrapper cases (still fail-before) + teardown/duplicate-exit cases.
- Success bars (tabled): agreement table complete (every ABI edge, both
  receipts); creator census (or receipted obstacle); steady-state
  re-confirmation byte-identical (or drift tabled); fix-ready spec with
  file/line + tests + handoff to the E-lane MPEG brief.

## Gates and rules

- Device: iPad (same as I18/I19) via explicit `--timeout` devicectl;
  config-only probes (NO build/codegen/install/commits on the fork).
  Install policy: no install this brief. No PS2 boots → no lease; no `adb`.
- Worktree under a NEW SSD lane dir; caps declared up front + tracked;
  `._*` purge before staging; screenshot to internal path first, then `mv`.
- NEVER prompt/steer the E-lane pane (E16 may be mid-brief ACTIVE —
  read-only inspection only; remote never moved by you).
- Evidence dir `local/research/I20/` (STANDALONE). Commit with `git add -f`,
  prefix `[I20]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — agreement + creator census (one config-only probe)

1. Agreement table (E15-host × I19-device, every ABI edge, both receipts).
   Worktree recon (base tabled) + iPad pre-state (reachability, lock,
   running PIDs, installed apps — user sessions reported, never killed).
2. ONE config-only relaunch (`PS2X_DIAG_SEMA_CREATE=1` added); census
   creators × sema ids; steady-state re-confirmation; cleanup (only OUR
   PIDs signaled). Table every receipt.

## Task 2 — fix-ready spec + handoff

1. F1–F10 → implementation spec (files/functions/semantics/tests/E15-§4
   constraints/stub-residual split). Handoff table: what the E-lane MPEG
   brief consumes, what stays device-side, what E16's closure repair must
   deliver first.

## Report

`local/research/I20/REPORT.md`: contract, agreement, census, spec,
handoff, exact commands, gaps. Commit evidence (`[I20]`), stop.
