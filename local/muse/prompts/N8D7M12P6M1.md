# N8D7M12 Part 6M1 — exact input inventory for next Android package

You are the OpenCode Go provenance worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/ARCH1/{REPORT.md,ORCH-GATE.md}`, and `local/research/N8D7M12P3/{REPORT.md,source_gate.py,apk_gate.py}`. Cite local files and exact lines, not web results. Use LSP or build files to confirm source inclusion rather than inferring it from names. Own only `local/research/N8D7M12P6M1/`; never edit fork, package, board, ledger, global config or other receipts; no build, device, network/ssh, lease or push. Time box 25 min; ≤512 KiB text receipts.

Goal: prepare a **reviewable next-package input manifest plan**, so the Android build worker can pin actual compiled inputs instead of repeating the P3 attribution error. Hypothesis: P3's partial SHA list may miss a compiled GS interface, page tracker, Granite source/patch, shader or fork worker/frontend source; a file-list comparison against the actual build configuration distinguishes covered from uncovered inputs. Correct model: a SHA from a historical source gate proves only that named file, not the entire APK provenance. Do not claim an old APK matches current working trees.

Produce `REPORT.md` with a table of: (1) P3 APK and native-member SHA/Build ID pins already proved, (2) currently pinned source paths, (3) missing or ambiguous compiled inputs by exact path or bounded glob, (4) evidence that each path enters the Android link, and (5) the exact action needed at the next build. Include fork core/frontend/worker/backend, paraLLEl renderer/interface/page tracker/shaders, Granite dirty patches, build flags and Turnip/HAL. Explicitly distinguish historical unrecoverable bytes from a future baseline. Check the actual P3 backend pin `84a13a80686be8d4ed17750a9399d298ea6b998b98f48d3720b6b5bc839a4645`; predecessor `c6135b3c…` is not its package source. Include an artifact graph from source root to native member to APK, with each link labeled proven/planned/unknown.

Write a small `check.py` that mechanically verifies your table has all named categories, exact P3 pin and no blanket claim of historical equivalence, and emits `check-result.json`. Run it. Keep any proposed next-build commands in REPORT only: double SHA reads before/after source copy, record dirty Git diffs or hash all compiled inputs, capture CMake/Gradle flags and build system file list, tie runner Build ID and native-member SHA to APK SHA. Do not run them now. Stop if source inclusion cannot be proven; mark that row unknown and hand it back. Commit only `local/research/N8D7M12P6M1/{REPORT.md,check.py,check-result.json}` with `[N8D7M12] Part 6M1` and `Orchestrated-By: opencode`; no push. Hand back the table and gaps, not a causal verdict.
