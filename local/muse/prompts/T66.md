# T66 — PCSX2: the SC camera object at 0x00bc5920 (confirm E50's model)

Same rules and tree as T65. Read `local/research/E50/REPORT.md` (Outcome and
the camera tables) first. **Tables + receipts; the orchestrator decides.**

E50: the recomp's SC camera rotation is R + 0.4438·I. The error comes from
the quaternion at EE `0x00bc5950`: object `0x00bc5920`, position at +0x20,
quaternion at +0x30, matrix out at +0x40, read by `sub_0015D928`. In the
recomp it holds ≈ (0, 0, 0.52736, 0.94813). E50 predicts that in PCSX2 it
holds (0, 0, 0x3f3504f3, 0x3f3504f3) and pos.y = 200.

## Work (no rebuild if T65's binary allows a memory read; otherwise one small log-only hunk)

1. At settled SC (your statefile path is fine), read EE RAM
   `0x00bc5920..0x00bc59a0` once per vsync for 3 vsyncs: hex plus float
   decode. If the object isn't at that address in PCSX2, locate it by the
   SC camera words from T65 §5c and say so.
2. A store watch on `0x00bc5950..0x00bc595c` (T59's fixed fold) from a
   state *before* SC: the MENU state `t63-menu-state` → K → SC. Log the
   first 32 writes with pc, ra and regs (a0..a3, v0, v1, s0..s7), plus
   the f-register values if the store is SWC1/SQC2.

   E50 Part 2 is finding the recomp's writer at the same time; the pc/ra
   must be comparable.

≤1 build, ≤2 captures, 2 h, cap 1.5 GB.

## Deliverable

`local/research/T66/REPORT.md` with the object dump, the writer table and
preservation. No verdict. `[T66]` commit (`git add -f local/research/T66
local/muse/prompts/T66.md`, trailer `Orchestrated-By: Claude Code`,
`git log -1` first), no push.
