# T3 — IRX inventory (static, lease-free)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P8/REPORT.md` §P8-1/§P8-5 first
(the IRX list + `sid 0x534E44` + unclaimed-fallback rows this brief
grounds), plus the frontier's §13 IOP-decision note (this inventory
makes that decision one brief away when a sound-shaped park appears).
This is a SURVEY brief: data for a decision not yet due. No fork
changes, no boots, no lease. Peer lanes run concurrently — you share
nothing with them.

## Facts you start from

`DATA/MODULES/*.IRX` (22 modules) on the game ISO are the IOP side of
sound/pad/mc; EA's SND protocol (`SNDDRV.IRX` ↔ EE `Snd::System`)
is proprietary. Sample done by the frontier: `SNDDRV.IRX` 96,016 B =
EA's own SND library (`ps2/sndiop.c`), imports only intrman, libsd,
loadcore, sifcmd, sifman, sysclib, sysmem, thbase; `MSIFRPC.IRX`
7,377 B; `PADMAN.IRX` 43,813 B; `SDRDRV.IRX` 8,065 B. Re-derive all
of it — trust receipts, not memory.

## Gates and rules

- READ ONLY everywhere: no fork writes/commits/pushes, no fork boots,
  no lease, no `adb`. Installed tools only (`bsdtar`, `strings`,
  `readelf`/`llvm-objdump` — verify, never download). The ISO is
  read-only; extract to scratch.
- Disk: internal disk is tight — work under
  `/Volumes/Extreme SSD/ps2x-t3/` (new dir). Never touch
  `/tmp/p1-link`, `ps2x-i*`, `ps2x-p1*`, `/Volumes/Extreme SSD/m*/`,
  or other agents' dirs.
- Evidence dir `local/research/T3/` (STANDALONE — do NOT append to
  `P1/REPORT.md`). NOTE: the frontier's §12 names `local/research/P13/`
  for this, but that pane runs the analyzer triage (the read predates
  it) — T3 keeps its own dir. Commit with `git add -f`, prefix
  `[T3]`, trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box 4 hours (expected ~1 h).

## Steps

1. **Extract.** All 22 `DATA/MODULES/*.IRX` from the ISO
   (`bsdtar -xf`, read-only source): names, bytes, shas. Table.
2. **Inventory.** Per module: imports (IOP kernel libs), exports,
   registered RPC sids (`sceSifRegisterRpc` immediates),
   version strings, entry shape. Table with file:offset or
   string receipts per row.
3. **Sound focus.** `SNDDRV.IRX` deep row: import list (verify the
   8-lib claim), the RPC surface it serves (sids + fno handlers),
   and the EE-side callers (P8's `0x534E44` rows — confirm the
   pairing). One table answering: how big is the surface an HLE
   of EA's protocol would cover?
4. **Report.** `local/research/T3/REPORT.md`: inventory table,
   sound-focus table, the HLE-vs-LLE input rows (§13 routes
   a/b/c get their evidence columns, no recommendation),
   exact commands, receipt paths, "What I could not do".
   Commit, stop.
