# T3 — IRX inventory (static, lease-free): HLE-vs-LLE for sound, decided with data

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `docs/research/review-2026-09-19-progress.md` Part 2 §12
row T3 + §13 (the Play! HLE-IOP paragraph and the IOP-model decision row)
first. This is frontier tooling brief T3: when a sound-shaped park
appears, the IOP model for sound (HLE protocol / interpret IRX /
recompile IRX) must be one brief away, not a research project. Peer
lanes run concurrently — you share only the fork remote.

## Facts you start from

- ISO (read-only, never write to it): `/Volumes/Extreme SSD/ps2recomp-spike/SSX 3 (USA).iso`
  (3,005,415,424 bytes). Extract with `bsdtar -xf "<iso>" 'DATA/MODULES/*.IRX'`
  into a scratch dir — NOT into the repo, NOT onto the SSD lanes'
  dirs (`/tmp/t3-irx/` is fine).
- Sample already measured (verify, don't trust): 22 `DATA/MODULES/*.IRX`;
  `SNDDRV.IRX` 96,016 B = EA's own SND library (`ps2/sndiop.c`),
  imports only intrman, libsd, loadcore, sifcmd, sifman, sysclib,
  sysmem, thbase. `MSIFRPC.IRX` 7,377 B; `PADMAN.IRX` 43,813 B;
  `SDRDRV.IRX` 8,065 B.
- The decision this feeds (§13): lean Play!-route (HLE the RPC
  protocol, cf. Play!'s `Iop_LibSd`/`Iop_SifCmd`) unless T3 shows
  the SND RPC surface is tiny (then protocol HLE is trivially
  cheap) or huge/weird (then reconsider).

## Gates and rules

- NO lease of any kind (static brief — no boots, no harness runs).
  No fork changes at all. No `adb`. No device work.
- Read-only on the ISO and every other agent's dirs. Scratch in
  `/tmp/t3-irx/`; delete nothing you didn't create.
- Evidence: `local/research/P13/IRX-INVENTORY.md` (STANDALONE file —
  do NOT touch `local/research/P13/REPORT.md`; a peer brief owns
  it). Plus a machine-readable `local/research/P13/irx-table.csv`
  (one row per IRX, columns below). Commit with `git add -f`,
  prefix `[T3]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box: ~1 h. Tables, no verdicts — record the decision inputs,
  not the decision.

## Task 1 — extract + identify (all 22)

1. List all `DATA/MODULES/*.IRX` in the ISO with sizes; confirm the
   count is 22 and re-measure the four sample sizes above
   (mismatch = table it, don't silently adopt).
2. Per IRX: `file` type, `strings` version/build stamps, and any
   embedded module name. EA-built vs Sony-built call per module
   (evidence: strings + import shape, tabled).

## Task 2 — import/export/RPC surface (the decision table)

Per IRX, table:

- imports: IOP kernel libs + (where readable) function names
- exports: exported functions/libraries
- RPC server surface: `sceSifRegisterRpc` call sites with the sid
  immediate (disassemble or hex-scan; record sid + handler addr)
- RPC client surface: `sceSifBindRpc` / `CallRpc` sids referenced
- libsd use (for SND*: which `sceSd*` imports — the HLE surface)

Rank the table by HLE-relevant surface: SND audio path first
(`SNDDRV`, `SDRDRV`, `MSIFRPC`, `LIBSD`-adjacent), then input
(`PADMAN`), then the rest.

## Task 3 — the SND call (narrow, deep)

For the SND audio path modules ONLY:

1. Full import list with function names (every `sceSd*`, every
   `sceSif*`).
2. Every RPC sid registered AND every sid called, with direction.
3. Strings mentioning RPC commands, versions, or EA internals.
4. Size of the surface in one line per module: N imports / N
   exports / N sids — the number the §13 decision row consumes.

## Report

`local/research/P13/IRX-INVENTORY.md`: extraction receipt (ISO
bytes, file count, sizes) + the 22-row decision table + the SND
deep section + gap rows (anything a hex-scan couldn't resolve
that needs a disassembler brief). `irx-table.csv` mirrors the
22-row table. Commit evidence (`[T3]`, no push), stop.
