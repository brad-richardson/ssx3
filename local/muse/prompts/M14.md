# M14 — Camera-delta true delta: the milestone-2 payoff run

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M13/REPORT.md` (the drawscan instrument + guard-receipt
pattern), `local/research/M7/REPORT.md` Step 3 (the original +0.1 camera
delta, measured 0 only because the reference was uninit), and
`local/research/M10/REPORT.md` (RAMREF rendered-reference method). This
brief closes the loop M6–M13 opened: a REAL camera delta measured against
a RENDERED reference. Base header `local/research/M13/m13_replay_context.h`
(sha in the M13 report — record `shasum`, not memory; copy it to
`local/research/M14/m14_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M14/` (commit with `git add -f`, prefix
`[M14]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m14/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M14/` as a research header derived from M13's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M14\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M14/waits.log`. A P1v agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M13/build_replay_player.py` to
  `local/research/M14/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M13 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m14-det`, `m14-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M13-header copy, run `m14-det`
   once with an M13 slot-0 alternating env, confirm M13's end state
   reproduces (window rows + shares, guest/event clean). Control.
2. **Camera-delta true delta.** Apply a real camera delta (M7's +0.1
   translation shape, your choice of slot — record why) with RAMREF
   rendered references: per-replay moved-bytes table
   (gt0/uniform/xd2/xdmax/xdmean + refhashes), the FIRST honest
   camera-motion pixel measurement of the milestone. Receipts: ref2
   rows ×200 + the moved-bytes distribution (min/max/uniform?).
3. **Drawscan the camera delta.** Run M13's leave-one-out instrument on
   the Step-2 delta: per-draw carried-bytes table (which draws move
   the camera pixels? one carrier like draw 427, or spread?).
   Receipts: shape table + the top-10 carriers with samples.
4. **Report.** `local/research/M14/REPORT.md`: per-step tables
   (camera delta rows, drawscan carriers), wall rows, continuation,
   header diffs per step (`diff -u` against the M13 header), exact
   commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M14/` (header, driver copy, analyzer
   changes, REPORT, waits.log). Stop.
