# E55D2 — minimal pad/card guest-write probe map (Muse Contributor Go)

You are an OpenCode Go worker on a **read-only** E55 Part 1. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55D1/{REPORT.md,ORCH-GATE.md}`, `local/research/E55C2/REPORT.md`, and the `E55D2` open entry in `docs/todo.md`. Pin source `~/dev/ssx3-work/E55C2/PS2Recomp` at `ddaee780288adb076ce40050d87969b20bc4bb05`. No build, boot, device, source edit, web, upstream contact or push; do not claim determinism.

Question: can existing default-OFF diagnostics record **every successful** `scePadRead` 32-byte guest buffer and `sceMcGetDir`/`sceMcRead` guest byte write through stock race tick2053 with exact guest read order and a bounded log? Existing E55C2 boots matched VBlank hashes, but their logs contain no pad/card rows. E55D1 found synchronous guest writes and no in-tree ExternalWake producer. Correct A/A behavior predicts equal per-read pad bytes and equal card-call bytes on identical pinned inputs; a wall/live pad or altered card should first differ in its own call family before a combined VBlank hash. A probe that samples only one invocation cannot support that test.

Search only the existing E3/E44/stub/runtime logging gates and their direct call sites. Confirm env syntax, whether compiled in E55C2's ON runner, per-call coverage, guest tick/read ordinal, hash/byte capacity and effect on the normal path. Use LSP for C++ cross-file calls if available; otherwise cite exact caller and callee lines. Deliver `local/research/E55D2/REPORT.md` and `probe-map.tsv` (<64 KiB total), with columns `candidate,source_anchor,enable_flag,coverage,bytes_per_event,cap,limitation`. Include 5–8 rows and a one-paragraph recommendation of **one** existing hook or one minimal default-OFF edit site pair if no hook meets coverage. State exact expected `A/A`, `pad-B`, `card-B` observables and first-difference stop rule; do not write the implementation or run anything.

Acceptance is structural only: a `check.py` under this receipt dir may verify TSV header/row count and source path/line syntax; the orchestrator validates source meaning. Keep context under ~45k and stop broad exploration once the coverage table is complete. Check ssx3 `git log -1`; commit named receipts `[E55D2]` with `Orchestrated-By: opencode`, no push. **Hand back the table; do not choose a production policy or declare a cause.**
