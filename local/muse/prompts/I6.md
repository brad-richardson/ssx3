# I6 — Bundle ID fix: set identifier, rebuild sim .app, re-run install+launch

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/I5/REPORT.md`
first (all of it: install exit 13, the 19-key Info.plist with no
`CFBundleIdentifier`, the static entry table, the 4 gap rows). This brief
is I5 gap 1's exact next brief: set a bundle identifier, rebuild the
simulator `.app`, and re-run I5's install+launch measurement.

## Facts you start from

Fork `ps2xRuntime/CMakeLists.txt` (`add_executable(ps2EntryRunner …)`
near lines 618–625) sets no `MACOSX_BUNDLE` /
`MACOSX_BUNDLE_GUI_IDENTIFIER` / `XCODE_ATTRIBUTE_PRODUCT_BUNDLE_IDENTIFIER`
(I5 Step 3, read-only — verify, do NOT assume the lines are unchanged).
I4's Release `.app` is 120 MB arm64 with SDL linkage proven; I5's static
entry findings (`_main`, `_SDL_Init`, `_InitWindow`, 292 SDL symbols,
adhoc sig) are the baseline your rebuilt binary must reproduce plus the
new identifier. No game ISO is in scope: install success + reaching
`main` / first window / first crash is the bar. P-lane (P1v) is
concurrently active in the SAME fork clone — the single-file discipline
below is load-bearing.

## Gates and rules

- No PS2 boots in this brief → no host lease needed. Builds may run any
  time (`-j4`).
- Fork: edit ONLY the iOS bundle-identifier lines in
  `ps2xRuntime/CMakeLists.txt`. `git add` that ONE file only — never
  `-A`, never stage or touch `register_functions.cpp` or anything
  P-lane owns. Verify the diff is the one hunk before committing. One
  commit (`CMake: set iOS bundle identifier for the sim .app (I6)` +
  body + the fork's two-trailer convention copied from the previous
  fork commit). `git pull --rebase` before pushing; `git push` ONLY
  inside the fork clone to the `fork` remote. If the rebase shows any
  conflict outside your hunk, stop and report — do not resolve P-lane's
  side.
- Simulator: use ONLY the installed iOS 27.0 runtime + ONE existing
  device (iPhone 17 `4DE6C37C-77EF-45AE-827E-26286E68D022` unless it is
  unavailable — record substitution). NEVER download runtimes or
  platforms. Boot it yourself, shut it down at the end.
- Disk: internal disk is tight — build + logs under
  `/Volumes/Extreme SSD/ps2x-i6/` (new dir); small logs (<5 MB total)
  may live in the evidence dir. Never touch `/tmp/p1-link`,
  `/tmp/ps2xgs-build*`, `/tmp/ps2x-ios-spike`, `ps2x-i3`, `ps2x-i4`
  (read from it, never write into it), or other agents' dirs.
- Evidence dir `local/research/I6/` (commit with `git add -f`, prefix
  `[I6]`, trailer `Orchestrated-By: Muse Code`). Do not commit
  screenshots over 5 MB total (downscale or describe). NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Minimal fork edit.** Set the bundle identifier (your call:
   `MACOSX_BUNDLE_GUI_IDENTIFIER`, plist template, or the smallest
   thing that produces a real `CFBundleIdentifier` — record the
   choice). Show the diff is the one hunk; commit (single file),
   pull --rebase, push fork remote only.
2. **Rebuild.** Fresh Release-iphonesimulator build in the `ps2x-i6`
   scratch dir (same I4 configure shape). Record: configure exit,
   build result, `.app` bytes/arch/symbols (must reproduce I5's
   baseline: `_main`, `_SDL_Init`, `_InitWindow`, ~292 SDL symbols),
   and `Info.plist` now HAS `CFBundleIdentifier` (record the value).
3. **I5 Steps 1–3 re-run.** Boot, `simctl install` (exit + bundle id),
   `simctl launch` (or spawn with stdout capture — record it): does
   `main` run? SDL init? first window? Record exit behavior, first 100
   + last 100 log lines, crash report if any, one screenshot if a
   window appears. If the app needs an ISO path/args or missing
   resources: record the EXACT behavior observed (gap-3 strategy is a
   LATER brief — observe, don't design).
4. **Report.** `local/research/I6/REPORT.md`: fork diff receipt,
   build/install/launch receipts, log excerpts, screenshot (or its
   absence reasoned), entry/lifecycle map from REAL logs (close I5
   gaps 2/4 as far as evidence goes), new gap rows with the exact next
   brief each needs, exact commands, receipt paths, "What I could not
   do" (device slice, signing, bundle resources, touch — unchanged
   unless evidence moves them). No verdicts. Commit
   `local/research/I6/`, shut the sim down, stop.
