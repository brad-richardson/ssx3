# M33 — Shape-9 full-set attribution: mid-frame cluster privates + missings (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M29/REPORT.md` first (all of it —
M29 gap 2 is this brief: shape 9 holds 22 privates + 8 missings
outside c321 (rows 226–305) unattributed at byte level; the c321
r266 off-span extra is one site of that cluster), plus
`local/research/M27/REPORT.md` (Task-1 per-site table + joins +
displacement method to reuse verbatim, s0-anchored). Fully
offline — SSD dumps read-only, no harness code, no replays, no
lease. No device work.

## Facts you start from

- Shape 9 (M28/M29): tail 117 B at Jaccard 0.7520 vs s0 (below
  0.95 AND a mover); full sets (M29): priv 23 (1 in-column
  c321 r266 + 22 outside-col r226–305 cluster), miss 8 (all
  outside c321, r266–301), shared 94. The c321 delta row is
  attributed (M29); the other 30 sites are located, not
  attributed.
- The cluster question (M27 method): per-site rows for all 22
  outside-col privates + all 8 missings (offset/plane/row/col,
  cross-frame |δ| + status, signed gaps both frames), position
  joins (band/decile/streak/carrier, M27 machinery verbatim),
  displacement (nearest-shared-site dmin hist), component view
  (does the cluster form 8-conn comps?).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m29/m29.txt`
  (shape-9 full-set counts to reproduce: tail 117, priv 23,
  miss 8, shared 94 — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m33/`; evidence
  `local/research/M33/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m32/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m33/` + `local/research/M33/`.
- Evidence dir `local/research/M33/` (STANDALONE). Commit with
  `git add -f`, prefix `[M33]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M32 precedent).

## Task 1 — cluster per-site tables (what are the 30 sites?)

Shape-9 full sets recomputed from the dumps (tail 117 + priv
23 + miss 8 + shared 94 must match M29 exactly or table the
mismatch and stop):

1. Per-site rows for all 22 outside-col privates (M27-style:
   offset, plane, row, col, |δ_9|, s0 status + |δ_s0|,
   signed gaps both).
2. Per-site rows for all 8 missings (mirror: |δ_s0|, shape-9
   status + |δ_9|, signed gaps both).
3. Near-miss check: H1/H2-style shares per set (bulk vs
   non-cell on the other frame — full swaps read 100%
   non-cell, partials 7/9 bulk; table where the cluster
   falls).

## Task 2 — cluster joins (one cluster or scatter?)

1. Position joins (M27 machinery verbatim, s0-anchored):
   priv/miss/shared by band / decile / streak column /
   carrier mask + 701 split.
2. Displacement: nearest-shared-site dmin hist per set
   (same-plane Manhattan; full-swap privates read dmin
   5–19 with d1 share 0 — table the cluster's hist).
3. Component view: 8-conn comps on shape 9's tail (count,
   sizes, the r226–305 cluster — one comp or several?).

## Task 3 — controls + determinism (M18–M32 precedent)

1. Positive control: synthetic truth with KNOWN cluster sets
   (N privates + N missings at known sites) — recover the
   sets + Jaccard + per-site values exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M32).

## Report

`local/research/M33/REPORT.md`: DESIGN.md (bars first) +
per-site tables + joins + displacement + comps + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a cluster map
discriminates (budget-noted, pre-registered split required
per M23–M32 precedent). Commit evidence (`[M33]`, no push),
stop.
