# I27 Part 2 — make raylib's SDL2 viewport use the iOS drawable scale

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §§4/6. **Start source mutation only after the orchestrator says N8D3's runtime source/build mutation is complete**; one live PS2 runtime mutator. Read full `local/research/I27A/REPORT.md` and its four frames. Write scope: `local/research/I27B/` and a fresh local fork worktree `~/dev/ssx3-work/I27B/PS2Recomp` from I27A local commit `4ebb2ac`; build scratch under `~/dev/ssx3-work/I27B/`. No fork push, iPhone/iPad/Odin action, generated guest-code edit or upstream contact.

I27A's `FLAG_WINDOW_HIGHDPI` made SDL window 874×402 and drawable 2622×1206. Yet raylib `GetRenderWidth/Height` stayed 874×402, and four Simulator images drew only in the lower-left 874×402 pixels. Pinned raylib `c1ab645ca298a2801097931d1079b10ff7eb9df8` returns `(1,1)` for SDL2 in `src/platforms/rcore_desktop_sdl.c::GetWindowScaleDPI`; `src/rcore.c::SetupViewport` multiplies the GL viewport by that scale on Apple. Confirm exact source lines and the iOS window resize path before editing. The guest image is still 512×448/640-stride; native viewport scaling cannot add texture detail absent from it.

**Correct behavior model:** on iOS SDL2, derive DPI scale from `SDL_GL_GetDrawableSize / SDL_GetWindowSize` (guard zero sizes), preserve logical screen coordinates and use that scale for the GL viewport. The resulting render/viewport should cover 2622×1206 while W1 16:9/pad geometry remains in logical points. Alternatives: a separate FBO or another viewport reset stays 1×; image fills drawable but fonts remain unchanged because their source textures are low resolution; geometry or touch mapping regresses. Discriminate with live window/drawable/screen/render logs, a bounded GL viewport readback if needed, and viewed title, menu and race screenshots against W1F2/I27A. Report fonts honestly as visually better/same/worse; no speed claim.

Make **one iOS-only candidate**: the I27A fork change plus a reproducible patch to the pinned raylib SDL2 DPI path, applied from the fork's build configuration or a checked-in patch hook. Do not silently edit a generated `_deps` checkout as the sole deliverable. Preserve desktop behavior; do not change guest, GS or pad source. Use a fresh local branch from `4ebb2ac`, pin the remote `fork/ssx3` before work, and check runner-dir diff against `14b1e5cb`. Build one Mac taps-OFF suite against canonical E54F2 codegen and one iOS Simulator build/install; one named compile-repair retry allowed. Run one I26-FAST Simulator route ≤180 s under a mini lease with own-PID cleanup. At most four SHA-paired PNGs, ≤12 MiB images, ≤8 MiB logs, ≤8 GiB scratch, global disk cap check. If the full viewport is still not used or race route regresses, stop; no device build.

Acceptance script checks pinned raylib patch presence, Mac suite, Simulator build, window/drawable/render ratios, viewport or screenshot occupancy, route tick ≥1714, four PNG decodes/SHA pairs, lease/PID cleanup and budgets. Deliver `local/research/I27B/REPORT.md` with exact commands, source/patch diff, build/suite/run receipts, dimensions, viewed frame table and gaps. Commit fork candidate locally `[I27B]` and report/receipts in ssx3 `[I27B]`, trailer `Orchestrated-By: Codex`; no push. Hand back the table, not a fold verdict.
