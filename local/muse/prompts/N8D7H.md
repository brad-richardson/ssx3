# N8D7H — isolated Android selected-input probe package (DeepSeek Flash Go)

You are an OpenCode Go worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7F/{REPORT.md,ORCH-GATE.md}`, `local/research/N8D7G/ORCH-GATE.md`, and N8D6B `REPORT.md` plus its `prepare.sh`, `build.sh`, `source_gate.py`, `apk_gate.py`. Keep context <60k; no broad repo scan. **No Odin/install/launch, iOS, game replay, upstream contact or push.** You may SSH only to `bytesize` for this named WSL package task; do not edit global OpenCode config. Do not touch shared Mac G43, canonical fork, N8D6B/N8D5F source, status/todo/ledger or game data.

Goal: build one arm64 APK carrying the Mac-calibrated default-OFF `PS2X_N8D7F_SELECTED_CAPTURE` probe for a later same-Odin-frame V/S/T test. Mac calibration passed input=circuit1=GPU stage 448/448 at tick2050, one sample/nonpromoted; the Odin stream is not assumed byte-identical. The Android package is only a source/binary gate, not a rendering or speed verdict.

One private WSL root `/home/brad/n8d7h/` (<10 GiB), Mac APK copy `~/dev/ssx3-work/N8D7H/app-release.apk` (<500 MiB), text receipts `local/research/N8D7H/` (<8 MiB). Check bytesize has no other heavy job. Copy N8D6B's WSL PS2Recomp, parallel-gs and jniLibs snapshots to N8D7H, excluding `.cxx`, build caches and runner. Prove zero differences before candidate. Then overlay **only** the N8D7F fork backend (`~/dev/ssx3-work/N8D7F/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`, SHA `c6135b3c…d4fb41f`) and private G43 `gs/{gs_interface.hpp,gs_renderer.hpp,gs_renderer.cpp}` with SHAs `3a1751b4…f05954d9d` / `fae3261a…3e0a93a401a` / `85c29cb0…e3de77e`. Compare all noncache WSL source to N8D6B: **exactly one fork backend and these three G43 files differ**, jniLibs identical; double-read each overlay SHA and canonical codegen/Turnip/shim pins. Mac fork runner-dir diff from `14b1e5cb` must be empty. Stop on mismatch.

Adapt N8D6B's bounded scripts under `local/research/N8D7H/` for root N8D7H; preserve exact codegen, Turnip, HAL, Gradle and five OFF flags. Run **one** arm64 `assembleRelease` (one named compile repair only if needed), no other heavy bytesize job. Postbuild rerun source gate; APK gate: exactly runner/Turnip/HAL arm64 native members, double SHA reads for APK and members, Turnip/HAL pins unchanged, Build ID recorded, required N8D7F/stage/tile/PNG strings present, sole arm64 CMake cache points at N8D7H G43 and external codegen and has five diagnostics flags OFF. Copy APK to Mac and double-check its SHA/size. No APK or binary in git. Stop on gate failure before device action.

Runbook commands start from the known N8D6B report: use `ssh bytesize 'wsl -d Ubuntu -- bash -s' < <script>` for bounded prepare/build, `ssh bytesize 'wsl -d Ubuntu -- python3 -' < <gate.py>` for source/package gates, and `ssh bytesize 'wsl -d Ubuntu -- cat <APK>' > ~/dev/ssx3-work/N8D7H/app-release.apk` for transfer. The private pane explicitly permits `ssh bytesize`; if OpenCode denies it, report the denial and stop rather than changing global permissions. Use `local/tooling/disk_budget.sh` before/after Mac writes. No non-brief retries or tuning loops.

Deliver `REPORT.md` with exact commands, source pins/SHA pairs, source diff table, build tail, package member/flags table, WSL/Mac APK SHA pairs and gaps; named script/JSON/text receipts, not full APK/log. Check `git log -1`; commit only named receipts `[N8D7H]` with `Orchestrated-By: opencode`, no push. **Hand back the table; do not conclude a device cause or request an Odin launch.**
