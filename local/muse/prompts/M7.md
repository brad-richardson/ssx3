# M7 — Host replay milestone 2, item 3: pose interpolation + camera delta on the g_transform seam

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M6/REPORT.md` (all of it: the scoped bus, the +1 driver,
the guard, "What I could not do"), `local/research/S2/REPORT.md` Part 4
§"What a `ReplayContext` still has to own" item 5 (this brief IS that
item) and §"Arm 2 (s2-egl-b)" (the no-op `g_transform` probe = your
zero-cost baseline). Base header `local/research/M6/m6_replay_context.h`
(committed sha `e89ca9b8a487fb67…` — record `shasum`, not memory; copy it
to `local/research/M7/m7_replay_context.h`, keep every M5/M6 mechanism).
Evidence dir `local/research/M7/` (commit with `git add -f`, prefix
`[M7]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m7/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M7/` as a research header derived from M6's. No
  vendor change; the seam (`XFStructs.cpp:287-302`,
  `g_transform(address, size)` after each XF matrix write) is usable as-is.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M7\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M7/waits.log`. A P1l agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M6/build_replay_player.py` to
  `local/research/M7/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M6 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m7-det`,
  `m7-det2`, … (dual core + `SSX_S2_FORCE_DETERMINISM=1`,
  `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline + M6 gaps.** Build the unmodified M6-header copy, run
   `m7-det` once, confirm M6's end state reproduces (200 rows + `done` +
   `xfb_equal_scratch=200/200` + `live_xfb_untouched=1` +
   `dafter_live`/`dframe`/`dpres`/`dimx` all 0/0). Then close M6's three
   unordered runs on this same player: an `m7-single` (`det=0`) run, a
   `replay_disabled` continuation run, and one unsuppressed-with-tracing
   run attempting the direct mid-Trigger flag-flip observation M6's
   evidence-status note calls out (record whether the flip is observed
   or still inferred). Receipts: wall rows + the three gap tables.
2. **No-op transform parity.** Install a no-op `g_transform` (S2 arm-2
   shape) for the full sequence. Receipts: original-frame equality
   (`xfb_equal_scratch=200/200`), guest/event state unchanged
   (all M6 counters identical to baseline), clean continuation, wall
   delta vs baseline (S2: unmeasurable — confirm or refute on this
   header).
3. **Camera-delta transform.** Install a small deterministic camera delta
   (fixed rotation/translation, your choice — record it) applied
   through `g_transform` per XF matrix write. Receipts: per-replay
   frames differ from baseline exactly where the delta applies
   (quantify: # differing frames, max/mean pixel diff on a sample) yet
   `done` stands, guest state unchanged (watched-window re-hash clean),
   event state unchanged (`dafter_live`/`dframe` 0/0), continuation
   clean. If the delta leaks into guest or event state, that IS the
   finding — table it, do not fix the seam.
4. **Report.** `local/research/M7/REPORT.md`: per-step tables (baseline
   vs no-op vs delta: wall med/p95, equality counters, counter
   summaries, continuation hashes), the M6-gap tables, header diffs per
   step (`diff -u` against the M6 header), exact commands, player and
   run dir paths, "What I could not do". No verdicts. Commit
   `local/research/M7/` (header, driver copy, analyzer changes, REPORT,
   waits.log). Stop.
