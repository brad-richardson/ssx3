# T51 — PCSX2: the render DMA thread's state machine at Select Character (healthy side)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL). **Tables + receipts; the
orchestrator decides.** Read first: `AGENTS.md`, `local/AGENTS.local.md`,
`local/research/T50/REPORT.md` (the patched G7 clone and route; your
dmareg/mpgpay hooks), `local/research/E40/REPORT.md` Part-3/4.

## Why (orchestrator read of `sub_00382760`, the render DMA thread)

The thread loops: `WaitSema(*(s0+0x5ACC))` at `0x3827e0`, then tests the
state word `*(s0+0x5A8C)` (s0 = struct `0x61ba60`, so the word is EE
`0x6214EC`): `== 5` → `func_382AF0`; `!= 0` → jump to ~`0x382a18` (the
second kick site, TADR store @`0x382ad8`, ra still `0x3827e8`); `== 0` →
wait DMA/VIF/GS idle, set state 1 (`sw a1,0x5A8C(s0)` @`0x382920`), kick at
`0x382a04` (ra `0x382938`). T50 saw PCSX2 kick from **both** ra `0x382938`
and ra `0x3827e8` (the second pass uploads microcode set A from
`0x434990`); the recomp only ever kicks with ra `0x382938`. Something in
PCSX2 (likely a DMA-end/GS interrupt handler) moves the state off 0 and
signals the sema; we need that sequence.

## Change (bounded log-only patch on T50's clone, gated like T48–T50)

Over **5 consecutive vsyncs at Select Character settled**:
1. Every EE store to `0x6214E0–0x621520` (struct fields +0x5A80…+0x5AC0):
   `st vsync=<n> addr=0x<> value=0x<> pc=0x<> ra=0x<> intc=<0/1 if in an
   interrupt handler>`.
2. Every syscall `SignalSema/iSignalSema/WaitSema/PollSema` whose sema id
   equals the value of `*(0x6214E0+0x2C)` (0x5ACC) or `*(…+0x28)`: `sema
   vsync pc ra call=<name> id=<n>`.
3. Every INTC/DMAC interrupt delivered to the EE (cause bits, DMA channel,
   handler address): `irq vsync cause=<> ch=<> handler=0x<>`.
4. Each D1 kick (reuse T50's dmareg: pc/ra) and GS `SIGNAL/FINISH/LABEL`
   events if cheap.
Interleave all lines in one file in execution order. EE interpreter if needed.
Preservation proof as before.

## Budgets, deliverable

≤2 builds, ≤2 captures, 3 h, bytesize cap 5 GB. `local/research/T51/REPORT.md`:
patch (text), hooks, preservation, the per-vsync sequence table (state
values in order with writer pc/ra/intc, sema ops, irqs, kicks). No verdict.
`[T51]` commit (`git add -f local/research/T51 local/muse/prompts/T51.md`,
trailer `Orchestrated-By: Muse Code`, `git log -1` first), no push.
