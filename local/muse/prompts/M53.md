# M53 — Index-top argmax concentration: plateau-aware peak tables (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M26/REPORT.md` first (all of it —
M26 gap 3 = this brief, taken on orchestrator judgment: 6/8
s0 + 5/8 m15 argmax read top-third by index under the
first-tie rule; plateau peaks: c257's 29×4 run, c340's twin
47s), plus `local/research/M25/REPORT.md` (the 32 profile
lines — row→δ lists with holes). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- M26's hump table (reproduce exactly under the first-tie
  rule or table the mismatch and stop): s0 peaks 257:25/30,
  277:25/28, 296:26/47, 301:27/47, 321:25/27, 340:26/47,
  342:27/−16, 343:27/−26 with thirds top/top/top/mid/top/
  top/top/mid; m15 peaks per M26 (thirds top 5/8 —
  reproduce the table).
- Plateau cases (M26): c257's 29×4 run + c340's twin 47s
  (first-tie picks the first row — the convention under
  test).
- The question: plateau-aware peak tables — all-tie rows
  (every row attaining column max) per column per frame +
  run-center rules (peak = run center? run middle?) +
  thirds + peak-row agreement RECOMPUTED under run-center
  (does 6/8+5/8 top-third survive? does 5/8 agreement
  survive?). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles), `/Volumes/Extreme
  SSD/m26/m26.txt` + `/Volumes/Extreme SSD/m25/m25.txt`
  (hump lines + profile lines to reproduce — match exactly
  or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m53/`; evidence `local/research/M53/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m52/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m53/` + `local/research/M53/`.
- Evidence dir `local/research/M53/` (STANDALONE). Commit with
  `git add -f`, prefix `[M53]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M52 precedent).

## Task 1 — first-tie reproduction + all-tie rows (what ties?)

M26's hump rows recomputed M26-verbatim (must match exactly —
peaks, thirds 6/8+5/8, agreement 5/8 — or table the mismatch
and stop), then all-tie rows listed:

1. First-tie table: all 8 columns × s0/m15 peak/third
   (16 rows reproduced).
2. All-tie table: every row attaining column max per
   column per frame (tie counts + rows — c257's 4-run +
   c340's twins + any others — the new data).
3. Tie-shape table: per column per frame tie pattern
   (singleton / run / scattered? run lengths?).

## Task 2 — run-center rules (does top-third survive?)

1. Run-center table: peak recomputed as run center per
   column per frame (rule pinned in DESIGN first:
   middle-of-longest-run? mean-of-ties?) + thirds under
   run-center (6/8+5/8 survive?).
2. Agreement table: peak-row agreement under run-center
   (5/8 survive? which columns flip?).
3. Plateau table: c257's 29×4 + c340's twins under
   first-tie vs run-center (peak rows + thirds + TRI-p
   impact — M51's p values cited, not refit).

## Task 3 — controls + determinism (M18–M52 precedent)

1. Positive control: synthetic truth with KNOWN plateaus
   (N singleton + N run + N scattered-tie columns) —
   recover the first-tie + all-tie + run-center tables
   exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M52).

## Report

`local/research/M53/REPORT.md`: DESIGN.md (bars first) +
tie tables + rule tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a plateau map
discriminates (budget-noted, pre-registered split required
per M23–M52 precedent). Commit evidence (`[M53]`, no push),
stop.
