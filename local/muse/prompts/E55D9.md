# E55D9 Part 1 — prepare one observed Main Menu navigation boot (OpenCode Go)

You are the E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, E55D8 `REPORT.md`/`ORCH-GATE.md`, I26 `ROUTES.md`, E55D4 `REPORT.md` and `e55d4_boot.py`, and `docs/todo.md` E55D9. Write Part 1 only under `local/research/E55D9/`; `~/dev/ssx3-work/E55D9/` is reserved for a later released run. Read but do not edit the pinned E55D3 fork/runner, I26 scripts, card inputs or other lanes. No source edit, build, boot, device/card action, push, board/global config edit or upstream contact in Part 1. Time box 25 min; new text <512 KiB, prospective scratch <2 GiB.

Goal: prepare a bounded **single Mac boot** that observes the settled Main Menu and navigates one button at a time to identify whether an Options/Save item is actually displayed. E55D8 found only the title→race I26-FAST route; only Single Event is evidenced on Main Menu, with no save/login screen. Do not invent a save route or infer one from UI strings. Use E55D3 private fork `bab6eb3` and built diagnostic runner SHA `e282c8a79cb4ce3e616244d1cfb0b5ab6d522e2f262408c6ffa92860f211643f` only if two fresh SHA reads and runner-dir guard verify; otherwise stop as OTHER. Put the prepared boot driver in `local/research/E55D9/`; on later release it must create and boot from its own private cwd under `~/dev/ssx3-work/E55D9/`. It must claim one mini P-lane slot before launch, stop/kill only its recorded PID, release the lease, and enforce ≤500 s wall, ≤120 s no-progress, ≤16 MiB closed log, ≤2 GiB scratch. Movie bypass dev-only ON; use existing empty card and pinned ISO/codegen, with two matching SHA reads for pinned inputs. No speed claim.

Design a route with I26 title START at guest tick636, then **do not press Cross at tick766**. Dump a settled full Main Menu frame before the first navigation input. Issue at most four one-step Down pulses, each separated by ≥45 guest ticks; capture a full frame after each settle. Stop if an Options/Save/profile item is visibly selected, after four pulses, or on any unexpected screen. No Cross, card write, or seeded-card change in this part. Predeclare A = a clearly readable Main Menu entry list and button-by-button transition to a displayed Options/Save item; B = readable menu and four bounded presses with no such entry observed (not proof none exists); OTHER = title/menu not reached, ambiguous/unreadable frame, script/input mismatch, pin/cap/lease issue. Frame images are evidence for orchestrator viewing, not worker-only labels.

Part 1 deliver `e55d9_boot.py` or equivalent exact command/script (without executing it), pure route/self-check that asserts START and ≤4 Down pulses with gaps, exact input/runner pins, planned frame ticks, stop rules and A/B/OTHER table in `REPORT.md`. Record SHA of the script and intended command. **Do not boot until the orchestrator reviews the exact script SHA and explicitly releases Part 2 in your pane.** `check.py` should validate route/limits and cannot claim the observed menu before a run. Commit `[E55D9] Part 1` with `Orchestrated-By: opencode`, no push; hand back the table and wait.
