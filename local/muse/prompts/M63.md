# M63 — interval-head bulk pair: row-323/324 neighborhood bulk census across neighboring columns (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M62/REPORT.md` first (all of it —
M62 gap 1 = this brief, taken on orchestrator judgment: m15
(323,311) +3 and (324,311) +1, both dec 0 — the only bulk in
48 interval probes, 1–2 rows below the 322 flank), plus
`local/research/M61/REPORT.md` (the pair: c311 6/0 +129,
c312 13/10 +130, spans nested 19-in-63) and
`local/research/M59/REPORT.md` (column signs: which neighbor
columns read all-pos vs split). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- The pair (reproduce exactly or table the mismatch and
  stop): m15 c311 rows 323:+3/d0/bulk + 324:+1/d0/bulk
  (band 2 both); the other 46/48 interval probes noncell
  (M62 interval tables).
- The question: row-323/324 neighborhood bulk census
  across neighboring columns (310/311/312/313/314/315 +
  any union col with rows 323–324 in tail) — is the head
  bulk pair column-local to c311, and what do the same
  rows carry on neighbors (bulk? noncell? tail? what δ?)?
  Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m62/m62.txt` (interval tables to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m63/`; evidence
  `local/research/M63/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m62/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m63/` + `local/research/M63/`.
- Evidence dir `local/research/M63/` (STANDALONE). Commit with
  `git add -f`, prefix `[M63]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M62 precedent).
- Hygiene (F3 rec 17): write the report in chunks; put
  `tail -3 REPORT.md` in the analysis receipts (truncated
  tail fails the gate).

## Task 1 — pair reproduction (does the head bulk reproduce?)

Rows 323–324 on c311/c312 recomputed from the dumps (must
match M62 exactly — 323:+3/d0 + 324:+1/d0 bulk on c311,
noncell on c312 — or table the mismatch and stop):

1. Pair table: rows 323–324 on c311 + c312 with full value
   rows (kind/δ/decile/band/d).
2. Row-323 table: row 323 across ALL 33 union cols (kind +
   δ + decile per column — which columns read bulk/tail
   at 323?).
3. Row-324 table: row 324 across ALL 33 union cols (same).

## Task 2 — neighborhood census (is the pair column-local?)

1. Bulk-neighbor table: bulk counts at rows 323–324 per
   neighbor column (310/312/313/314/315 + union hits —
   is c311 alone?).
2. δ table: δ values of any neighbor bulk/tail at 323–324
   (+3/+1 echoed anywhere? what signs?).
3. Decile table: deciles at 323–324 per neighbor column
   (dec-0 echoed? decile gradient across columns?).

## Task 3 — controls + determinism (M18–M62 precedent)

1. Positive control: synthetic truth with KNOWN row bulk
   (1 column-pair + N neighbor rows, known kinds/δ) —
   recover the neighborhood tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M62).

## Report

`local/research/M63/REPORT.md`: DESIGN.md (bars first) +
neighborhood tables + census tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a neighborhood map
discriminates (budget-noted, pre-registered split required
per M23–M62 precedent). Commit evidence (`[M63]`), stop.
