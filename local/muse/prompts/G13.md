# G13 — Rich post-loading dump dual replay with corrected labels (retire the lag line)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G12.md` (all — file N =
present#(N+1) via Main-counter FIFO lag; corrected mapping file N ↔
post-vsync#N; lagged-S draw order; same-boundary agreement at every true
step on the loading dump; §4 ONE next action) + `docs/reports/G10.md` §3c
(gsrunner build/run recipe) as needed. This brief executes G12's §4. The
G8–G11 "lag" line is RETIRED — do not re-litigate it; cite G12 and move on.

## Facts you start from

- The loading-icon dump is FULLY CLOSED (every draw, present, file, and
  counter attributed; both sides agree) but too thin to discriminate
  batching: no uploads, S=1 sprite, U degenerate, C-geometry static. The
  comparison method is calibrated for the first time (labels corrected,
  S-lag understood, no-skip verified, Merge-IncDraw named).
- The task: capture a RICH post-loading dump (scene-scale content with real
  transfer/draw interleaving, multi-target ping-pong, depth — where
  batching choices actually diverge) and dual-replay it (PCSX2 gsrunner +
  paraLLEl) with CORRECTED labels (file N ↔ post-vsync#N) and S-lag-aware
  windowing. One bounded capture run (bytesize PCSX2-side, T28 recipe
  shape) + bounded dual replay + comparison.
- Experiment contract (required): hypothesis = the calibrated method
  attributes every draw/present/file on scene-scale content and names any
  batching divergence with receipts; observable = per-draw/per-present
  tables both sides + corrected-label diffs + divergence table (or
  agreement table); alternatives = (a) divergence found → table + ONE next
  action (adoption-relevant); (b) agreement throughout → table + recipe,
  stop (method holds at scene scale); (c) method breaks at scale → table
  the exact break + repair recipe, stop. Stop: ONE capture + bounded replay
  pair — no tuning loop, no renderer changes, no new content beyond the one
  dump.

## Gates and rules

- Pinned tree `9056c08349` in `pcsx2-g7` (verify HEAD + G12 hook state
  first; reuse or extend the log-only patch ONLY with the same bounded
  recipe discipline — table every hunk). Byte caps declared up front,
  tracked in ALLOCATED bytes; SSD reuse only, nothing new on `/`.
- No contention with other lanes: no recomp boots/builds, no P-lane lease
  (PCSX2-side, T28 recipe shape, no lease), no fork writes, no `adb`.
  Capture run carries a wall cap + progress cap (T28 shape).
- Commit in ps2xGS as you go (prefix `[G13]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G13/` (`git add -f`,
  `[G13]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — rich capture + calibrated dual replay (one bounded capture)

1. Capture the rich post-loading dump (table trigger point, packet/vsync
   census, dump sha; preserve the loading dump's receipt chain untouched).
2. Dual replay with corrected labels + S-lag-aware windowing: per-draw
   order tables, present/file attribution with the corrected mapping,
   corrected-label diffs.
3. Divergence-or-agreement table + hypothesis verdict + the ONE next action
   the numbers justify.

## Report

Mirrored `REPORT.md`: contract, capture record, replay + comparison tables,
verdict, license receipts, exact commands, gaps. Commit both repos as
above, stop.
