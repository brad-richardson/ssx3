# M11 — True-delta partitions: slot-36, indexed path, texture-mediated share

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M10/REPORT.md` (all of it: the 32954-byte true delta,
the "What I could not do" items 1–4 + 6, which this brief works through
in feasibility order). Base header `local/research/M10/m10_replay_context.h`
(sha in the M10 report — record `shasum`, not memory; copy it to
`local/research/M11/m11_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M11/` (commit with `git add -f`, prefix
`[M11]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m11/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M11/` as a research header derived from M10's. No
  vendor change. (Per-draw EFB readback, if attempted, is header-side
  orchestration of existing readback calls — still no vendor edits; if
  it cannot be done header-only, record that and move on.)
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M11\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M11/waits.log`. A P1s agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M10/build_replay_player.py` to
  `local/research/M11/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M10 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m11-det`, `m11-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M10-header copy, run `m11-det`
   once with the M10 true-delta env (`SSX_M10_SLOT=0 SSX_M10_RAMREF=1
   SSX_M9_SURV=1`), confirm M10's end state reproduces (`gt0=100`,
   `xd2min/xd2max=32954`, guest/event clean). Control for later.
2. **Slot-36 true delta.** Run the M10 instruments unchanged on slot 36
   (`SSX_M11_SLOT=36 SSX_M11_RAMREF=1`): order table, matrix table,
   epoch table, ref2 row. Receipts: does the Tex2 path move pixels
   against a rendered reference (close M10 gap 4 + the texcoord-stage
   residue row), with the same 100/100 uniformity test.
3. **Indexed-path exposure.** Decode per-vertex `pnmtxidx` into
   `transformmatrices` (M10 gap 2): per-frame count of indexed-position
   draws exposed to slot 0, and — if the decode admits it — the
   shared-vs-indexed partition of one true-delta run's moved bytes.
   Receipts: decode table + partition (or the exact reason it cannot
   be partitioned header-only).
4. **Texture-mediated share.** Forced-RAM NOPTEXT arm (NOP the early
   texture-copy epochs' draws only, per M10 gap 3): how much of the
   32954-byte effect survives when texture-mediated carriers are
   removed? Receipts: ref2-style row + the surviving-bytes count.
   (Per-draw depth, M10 gap 1: attempt only if steps 2–4 leave box
   time AND a header-only path exists; otherwise restate as open with
   one new sentence on why.)
5. **Report.** `local/research/M11/REPORT.md`: per-step tables (slot-36
   delta, indexed partition, NOPTEXT share), wall rows, continuation,
   header diffs per step (`diff -u` against the M10 header), exact
   commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M11/` (header, driver copy, analyzer
   changes, REPORT, waits.log). Stop.
