# G1 — GS capture hunk in the runner, reference captures from SSX 3, the real census

You are muse in a herdr panel, working directory
`/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` (our fork, branch `ssx3`,
remote `fork`). Runbook: follow the steps, produce the receipts, **hand
back tables, no verdicts**. Plan: `/Users/bradrichardson/dev/ssx3/docs/plan-gs-gpu-backend-2026-09-18.md`
§2–§3 and §8. Harness repo: `/Users/bradrichardson/dev/ps2xGS` (read
`docs/gscap-format.md`, `harness/recording_backend.h`, `harness/gscap.h`,
`docs/reports/G0.md`). P1 state: `/Users/bradrichardson/dev/ssx3/local/research/P1/REPORT.md`
Part 3 (how the runner is built and booted: `$W/P1/ssx3.toml`, function
map `$W/P1/ssx3-functions.csv`, disc tree `$W/P1/cd/`, env
`PS2X_CD_IMAGE`, boot command in P2-10/P3). `W=/Volumes/Extreme SSD/ps2recomp-spike`.

## Gates and rules

- Host only, no `adb`, no device. Take the host lease first:
  `printf 'G1 %s\n' "$(date +%s)" > /tmp/ssx3-host-lease` if the file is
  absent or empty; if it names another agent, wait and re-check every 5
  minutes. Remove it when you stop.
- Build in `/tmp/ps2x-build-p1b` (APFS) exactly as P1b did; captures and
  anything over 5 MB go under `/Volumes/Extreme SSD/ps2xgs/game/`. Never
  commit captures, generated runner sources (`ps2xRuntime/src/runner/sub_*.cpp`,
  `ps2_recompiled_*.h`), or logs.
- Commit on the fork branch `ssx3` as you go: plain commits, prefix
  `GS:`/`Pad:` as fits, trailers
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and
  `Claude-Session: https://claude.ai/code/session_01H9JEyNpHtANpAU2dB1YuC7`;
  `git push fork ssx3` after each commit. Do not edit `ps2xGS` except
  `docs/census.md` and `docs/reports/G1.md` (commit there with prefix
  `[G1]`, same trailers, push `origin main`).
- Time box 6 hours. Stop at the time box with the report written.

## Steps

1. **Capture hunk (fork).** Add a CMake cache variable
   `PS2X_GSCAP_SOURCE_DIR` (default empty) to `ps2xRuntime/CMakeLists.txt`.
   When set, compile `${PS2X_GSCAP_SOURCE_DIR}/gscap.cpp` and
   `recording_backend.cpp` into the runtime library, add that directory
   to its include path, and define `PS2X_GS_CAPTURE_ENABLED=1`. In
   `ps2xRuntime/src/lib/gs/gs_frontend.cpp` (the `GS` constructor at
   ~line 109 and `setRasterBackend` ~line 1633, whichever the runner
   actually goes through: check with a log line once) wrap the
   `GSCpuBackend` in `GSRecordingBackend` writing to `getenv("PS2X_GS_CAPTURE")`
   when that variable is set and the define is on. One hunk per file; no
   other behaviour change. Configure with
   `-DPS2X_GSCAP_SOURCE_DIR=/Users/bradrichardson/dev/ps2xGS/harness`,
   rebuild, boot once with `PS2X_GS_CAPTURE` **unset** and confirm the
   boot log is unchanged from P1b's last good boot (same last 10 lines
   modulo timestamps). Receipt: the diff, and both boot logs' tails.
2. **Scripted pad input (fork).** In `ps2xRuntime/src/lib/ps2_pad.cpp`
   add `PS2X_PAD_SCRIPT=<file>`: text lines `<frame> <buttons>` where
   buttons is a comma list of `up,down,left,right,cross,circle,square,
   triangle,l1,r1,l2,r2,start,select` held from that frame until the next
   line; frame counted at the pad poll. Keyboard input still works when
   the variable is unset. Receipt: the diff and a 10-line script that
   presses `start` at frame 300 and `cross` at frame 600, with the boot
   log showing the game leaving the first screen (or not: report which
   screen it reaches, with a screenshot of the runner window via
   `screencapture -l <windowid>` or the runner's own frame dump if one
   exists).
3. **Reference captures.** With `PS2X_GS_CAPTURE=/Volumes/Extreme SSD/ps2xgs/game/<state>.gscap`,
   capture 3–5 consecutive presents for each reachable state, in this
   order, stopping the runner between states (one capture file per
   state, restart the runner each time; use a pad script to reach the
   state deterministically and record the script next to the capture):
   `boot-first-frames`, `frontend-menu`, `snowjam-riding` (Snow Jam race,
   ~10 s into the run), `six-rider-start` (race start gate with all
   riders in view), `crows-nest-riding`, `metro-city-riding`. If a state
   is unreachable in 45 minutes of trying, record the furthest state
   reached, the pad script, and the last 30 log lines, and move on.
   Cap each capture at 5 presents (`PS2X_GS_CAPTURE_MAX_PRESENTS`, add
   it to the hunk if the recording backend has no cap: after N presents
   close the file and pass calls straight through).
4. **Identity replay.** For every capture:
   `/tmp/ps2xgs-build/gsreplay <cap> --backend cpu --repeat 3 --json <cap>.json`
   (rebuild ps2xGS in `/tmp/ps2xgs-build` first if the binaries are
   missing). Table: capture, size, presents, max diff, VRAM exact, median
   ms per present. Every row must be exact; if one is not, keep the
   capture and report the row, do not fix the backend.
5. **Census.** `/tmp/ps2xgs-build/gscensus /Volumes/Extreme\ SSD/ps2xgs/game/*.gscap --json /Volumes/Extreme\ SSD/ps2xgs/game/census.json --md /Users/bradrichardson/dev/ps2xGS/docs/census.md`.
   Then run it again per state (`--md` to `census-<state>.md` on the SSD)
   so the report can show which features appear only in riding. If the
   census tool cannot decode something the game emits (unknown register,
   unhandled prim path), add a row to the report's "census gaps" table,
   do not patch `gscensus` in this brief.

## Report

`/Users/bradrichardson/dev/ps2xGS/docs/reports/G1.md` and a copy at
`/Users/bradrichardson/dev/ssx3/local/research/G1/REPORT.md` (`git add -f`,
`[G1]` prefix, same trailers): the hunk diffs (step 1–2), state table
(state reached / not, pad script path, capture path, size, presents),
identity replay table, the whole-game census table (copied from
`docs/census.md`) plus the per-state deltas, census gaps, fork commits
pushed (`git log --oneline fork/ssx3 -5`), exact commands, receipt paths,
"What I could not do". No verdicts. Commit, remove the host lease, stop.
