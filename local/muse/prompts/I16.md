# I16 — Close the 0x2c5300 indirect-target gap (or port E12's fork row), re-codegen, re-link, re-probe on iPad

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I15/REPORT.md` first (all of it: base
moved to `ffdf58c` + 6 ports + 1-line CSV entry closed `0x3a0158` (count 0,
guest strictly past it), guest reaches a STRICTLY LATER wall: indirect JALR
`0x242150 → 0x2c5300` (mid-function of emitted `sub_002C52D8`
`0x2c52d8–0x2c5320`, head after `jr $ra` @`0x2c52f4`/ds `0x2c52f8`/nop
@`0x2c52fc`, tail `jr $ra` @`0x2c5318` → return, ends `0x2c5320`, observed
bounds `0x2c5300–0x2c5320`, caller `jalr $v0` @`0x242150` with table-loaded
target — `$v0` delta vs priors' `$v1`, 0 register refs/case/file; gap 1 =
this brief). This brief executes I15's ONE next action — the same 1-line
class as I11–I15. Reuse I15's worktree shape + device toolchain +
provision/install/ISO+env mechanism — table every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I15 app is STILL INSTALLED — plain `install` overwrites
  (closed 7× now). No fresh prompt needed for iPad installs. Confirm the
  iPad's state before installing (user sessions reported, not killed).
- BASE: create a SEPARATE fork worktree on the SSD at CURRENT `fork/ssx3`
  HEAD (table the hash — the E-lane may have advanced past `ffdf58c`;
  NEVER move it yourself).
- COORDINATION (read carefully — the E-lane converged on the SAME gap):
  E11's outcome (ii) parked `0x2c5300` as the E-lane's next single-entry
  repair, and a concurrent E12 brief repairs it fork-side. TWO cases:
  (a) if `fork/ssx3` HEAD already contains the `0x2c5300` row when you pin
  (check the CSV at HEAD BEFORE writing anything): do NOT write your own
  row — PORT E12's row as part of your base (table the E12 commit + row
  bytes) and proceed to codegen; (b) otherwise port I15's 7 local commits
  (plist, CMake wiring, `0x395730`/`0x14f2a8`/`0x156750`/`0x243a80`/
  `0x3a0158` rows — cherry-pick if clean, else re-apply by hand + table
  the merged diff) and write the `0x2c5300` row yourself (I15's observed
  row `sub_002C5300,0x2c5300,0x2c5320,0x20` — RE-VERIFY the `jr $ra`
  @`0x2c5318` bounds from the emitted slice BEFORE writing the row; table
  the verification). Either way the probe target is the same: PAST
  `0x2c5300`. Check whether the E-lane owns the shared clone before
  touching it (one `worktree add` + read-only inspection only, I15 shape).
- The task: re-run host `ps2_recomp`, re-link `ps2_game_objects`
  (`-DPS2X_GAME_CODEGEN_DIR`), rebuild the device runtime, install over
  the I15 app, and re-probe with the ISO+env recipe. One gap per brief: if
  a SECOND new wall appears, table it as the next gap — do not close it
  here.
- Success bars (tabled, not verdicts): entry present in the worktree (E12
  port with commit receipt, or local commit if case (b); fork-remote push
  ONLY if the E-lane is not mutating — else stays local, tabled); host
  codegen re-run exit 0 with the `0x2c5300` entry present (file +
  table-slot census); device Release build WITH game objects exit 0;
  overwrite install exit 0; argv-probe launch gets PAST `0x2c5300` (console
  + `.ips` + screenshot, tabled against I15's fate); diagnosis names the
  next layer with evidence if a new wall appears.

## Gates and rules

- Subordinate to the E-lane fork owner (if live): builds `-j2`, never
  `-j4`; build trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i16/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`. Host `ps2_recomp` runs are codegen (no lease),
  bounded + tabled.
- Fork discipline: worktree only; named-files `git add`; fork commits
  pushed to the FORK remote only, never upstream (and no `ssx3` push while
  the E-lane mutates); generated sources never staged (recompiler OUTPUTS
  are build artifacts — table paths + shas, do not commit them).
- Device discipline (extends I6–I15 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I16/` (STANDALONE). Commit with `git add -f`,
  prefix `[I16]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — entry (port or fix) + host codegen + device link (no iPad yet)

1. Pin at `fork/ssx3` HEAD (table hash); check for E12's `0x2c5300` row at
   HEAD. Case (a): port it (table commit + row bytes). Case (b): port I15's
   7 commits (table method + merged diff), re-verify bounds, write the row
   (named add, local commit).
2. Re-run host `ps2_recomp` (rebuild the tool at the new base ONLY if the
   old binary provably cannot run the new inputs — table the decision);
   table codegen exit/counts/shas + `0x2c5300` entry census.
3. Configure + build Release (`-j2`); table binary path/size/sha/identifier
   + table-populated proof (symbol census). Fix ONLY build breaks in the
   wiring class.

## Task 2 — overwrite install + re-probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I15 app (table overwrite behavior).
2. Ship ELF+ISO as in I15 (same mechanism unless the link changed the
   packaging — table deltas). argv-probe launch with ISO+env: console +
   `.ips` (sleep + re-`ls` before concluding none) + screenshot.
   Past-the-wall table vs I15 (`0x2c5300` gone? first post-entry code
   executes? new wall?).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled past the target: diagnosis table (failing layer + evidence).
   Attempt a fix ONLY if config-level — no second fork-code gap in this
   brief (one gap per brief; the next gap gets the next brief).
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I16/REPORT.md`: contract, caps, entry-port/fix + codegen +
link record, install/probe tables, diagnosis, exact commands, gaps. Commit
evidence (`[I16]`), stop.
