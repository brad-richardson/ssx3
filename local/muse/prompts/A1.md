# A1 — Resolve G1: why does the reference skip the unconditional installer (dynamic reads, no config flips)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/A0/REPORT.md` §G1 first (the
paradox this brief resolves: the ELF reaches the syscall-patch
installer unconditionally after RFU060/061 in the runtime, yet
the reference shows ZERO `(74)/(5b)/(5a)/(64)` over the full
boot — and NO static branch explains the avoidance, so the
"kernel-true return skips the patches" premise does NOT follow
from the caller graph), plus A0 tables (i-a)/(i-f) (the
unconditional path + the locator) and F3 §20 rank 2 (this
brief's original shape — OVERRIDDEN by G1: do NOT build the
kernel-true scanner; FIRST resolve the avoidance mechanism,
because if no branch exists there is nothing for a return
value to skip). A0's candidates: reference loader path
difference, HLE SetupThread/SetupHeap side effect, or an
unmodelled early state dependency.

## Facts you start from

- The paradox pins (reproduce or table the mismatch and
  stop): runtime opening events 1–25 (RFU060/061 → OSD ×4
  → 8×0x74 + 6×0x5b + 1×0x5a + 2×0x64) from
  `syscalls-t18-on.txt` head; reference zero
  `(74)/(5b)/(5a)/(64)` post-`ExecPS2:2` from `emulog-
  boot3.txt` (my proper EE-only greps: 0 over the window;
  A0's printed BRE pattern was vacuous — use `-E` with
  end-anchored `Bios call: NAME (hex)$`).
- The reads (dynamic, no config flips): (a) reference
  loader path — what PCSX2 executes between `ExecPS2`
  and the game's first EE event (ELF entry? loader stubs?
  `0x100008` ever entered? — answer from the reference
  trace + PCSX2 source reads, both ExecPS2 windows);
  (b) HLE setup side effects — what runtime HLE
  (SetupThread/SetupHeap/ModuleLoad) establishes before
  `0x100008` that the reference path may not (runtime
  early-log + source reads); (c) early state dependency —
  any register/memory precondition on the entry→installer
  path whose value could differ (recompiled-output reads
  of `sub_00100008` prologue + `sub_0042C300` head).
- Fork `$R` + OUT + ELF: read-only this brief (table HEAD
  at start; NO changes). PCSX2 source (bytesize clone at
  `/home/brad/pcsx2-t4/pcsx2`, rev `9056c08349…` — read
  via ssh, no builds) + `emulog-boot3.txt` (read-only).
- NO lease of any kind (no boots). No `adb`. No fork
  changes. No reference reruns (trace exists).

## Gates and rules

- Evidence dir `local/research/A1/` (STANDALONE). Commit with
  `git add -f`, prefix `[A1]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 4 h. Tables, no verdicts.
- Hygiene (F3 rec 17): write the report in chunks with a
  tail receipt (truncated tail fails the gate); grep
  receipts must show working patterns (no vacuous BRE).

## Task 1 — loader path vs HLE setup (where do the paths fork?)

1. Reference-entry table: reference events between
   `ExecPS2:2` @142465 and the first game-ELF-shaped
   event (names + lines + log-times, both windows —
   is `0x100008` entered? what runs first?).
2. HLE-setup table: runtime HLE calls before `0x100008`
   (SetupThread/SetupHeap/ModuleLoad + args/returns from
   the early boot log — what state exists at entry?).
3. Fork table: earliest point the two paths differ
   (event/line both sides — or table that the entry
   sequences match through RFU060/061 and fork after).

## Task 2 — avoidance mechanism (what skips the installer?)

1. Branch-hunt table: every conditional on the runtime
   entry→installer path (`0x100008` prologue through
   `42C300` tail-jump) with its condition + taken/not in
   the runtime + what value the reference would need to
   divert (PCs + disasm refs — exhaustive or tabled why
   a region was skipped).
2. State-dependency table: registers/memory read on that
   path before any write (the unmodelled-dependency
   candidates — which could plausibly differ?).
3. Verdict-shape table (no verdict — table the shape):
   for each A0 candidate (loader difference / HLE setup /
   state dependency): the reads for/against + the single
   dynamic experiment that would settle it (no execution
   — brief-shape only).

## Report

`local/research/A1/REPORT.md`: fork + avoidance tables,
exact commands (greps + line refs), gaps (incl. whether
the kernel-true scanner premise survives — tabled, not
decided). Commit evidence (`[A1]`), stop.
