# M21 — Static-site residual mechanism: maps per shape + EFB-copy diffing (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M19/REPORT.md` (M19 gap 2 is this
brief: 2368 raw bytes on s0 where v0==full≠mid, Y subset = P4's 463
px exactly, U/V majority 1905 B) plus `local/research/M20/REPORT.md`
(the δ- machinery and edge-distance/gradient-join precedents to
reuse) first. Fully offline — SSD dumps read-only, no harness code,
no replays, no lease. No device work.

## Facts you start from

- Static-site residual (M19 cell 6): bytes with v0==full where mid
  DIFFERS — genuinely non-interpolative content no blend/weight/
  direction rule can touch (10.4% of R_0 on s0, 1476 B on m15).
  Byte negrate 0.51/0.51 (unbiased — unlike every other cell);
  s0-Y band split 15/186/262 (top/mid/bottom — bottom-heavy);
  never above 0.0842 in any top-10 carrier mask (M19 Task 2).
- Two candidate characters: (a) EFB-copy/present-path artifacts
  (mid-frame capture differences independent of the draw
  sequence — test by diffing mid against v0/full on STATIC
  REGIONS, not just residual sites: is mid noisier there
  generally?); (b) per-shape deterministic content (test by
  static-site MAPS per shape: same sites across the 764
  triplets, or scattered?).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m19/m19.txt` (cell-6 counts/bands to
  reproduce exactly: 2368/1476 raw, 463/439 Y). Work dir: new
  `/Volumes/Extreme SSD/m21/`; evidence `local/research/M21/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m20/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m21/` + `local/research/M21/`.
- Evidence dir `local/research/M21/` (STANDALONE). Commit with
  `git add -f`, prefix `[M21]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M20 precedent).

## Task 1 — static-site maps (deterministic or scattered?)

Cell-6 membership recomputed from the dumps (counts must match
M19's 2368/1476 raw + 463/439 Y exactly or table the mismatch
and stop):

1. Per-shape static-site maps on s0 + m15 + the 764 coarse pass:
   site-count distribution (min/med/mean/max), site overlap
   between shapes (Jaccard or shared-site counts — same sites
   recurring, or every shape its own scatter?).
2. Value table: mid−v0 (= mid−full) signed histogram on static
   sites (the δ-analog; compare M20's interior δ shape —
   same ±1 mass or different?), per-plane split.
3. Position table: band/edge-distance/gradient-decile joins
   (M19/M20 machinery verbatim — same edges, same BFS).

## Task 2 — EFB-copy diffing (is mid noisier on static regions?)

1. On FULLY static regions (v0==full==mid excluded — use
   v0==full sites INCLUDING non-residual ones): mid-vs-v0
   difference rate inside vs outside the residual-site set. If
   mid carries capture noise on static regions generally, the
   residual sites should look like its tail — table both rates.
2. U/V-majority check: 1905/2368 static bytes are chroma (s0).
   Is the U/V static-site residual correlated across the two
   chroma planes at the same pixel (same-site U&V both differ?)
   vs independent — table the joint.
3. Carrier cut: static bytes inside each top-10 removed mask
   (M19: ≤0.0842 everywhere) + inside/outside-HUD-region split
   (bottom band holds 262/463 s0-Y static px — table HUD-region
   membership with the region definition recorded).

## Task 3 — controls + determinism (M18–M20 precedent)

1. Positive control: synthetic truth with KNOWN static-site
   injections (N sites, known values) — recover count + values
   + map exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M20).

## Report

`local/research/M21/REPORT.md`: DESIGN.md (bars first) + map
tables + value/position tables + EFB-diff tables + controls +
explained-vs-standing update (does any static-site rule shrink
cell 6? by how much?) + gap rows. Screenshot only if a
static-site map discriminates (budget-noted). Commit evidence
(`[M21]`, no push), stop.
