# A0 — Name the divergence: patch installer + hash-loop exit predicate + storm issuers (static, no boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read F3 §20 rank 1 first (this brief, in
`docs/research/review-2026-09-19-progress.md` Part 3), plus
F3 §19 (the milestone table — your tables answer its open
cells) and F3 F4/F5 (the claims you ground: ≤7 overridden
syscalls, GetThreadId/FlushCache issuers). Every later choice
(A1, E3-conditional, SIF peer) depends on table (ii). No boot:
static reads + committed traces/logs only.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`
  (read-only this brief — table HEAD at start; NO changes).
  Recompiled output `$W/P1/output` (read-only). Game GELF
  sources as needed (read-only). Committed streams:
  `syscalls-t18-on.txt` (runtime opening), `emulog-boot3.txt`
  (reference), `boot-t18-on.log` (the 6 drops @70–75).
- Table (i) — the patch installer: static read of
  0x42cbf0–0x42cc70 + the `0x42CBC0`/`0x42CBB0` wrappers:
  the seven (or fewer) overridden syscall numbers with
  handler addresses; what `InitSystemCallTableAddress`
  (ret0@0x42c1f0, toml:207) returns on a kernel it
  recognises vs 0; what the callers do with 0 (which
  branch installs the patches? table the PCs).
- Table (ii) — the hash-loop exit predicate: decode in
  `sub_00363490`/`sub_00376938` what is compared each
  iteration, what stops the loop at exactly 72,176 (a
  counter? a VBLANK timeout? a polled flag — and if a
  flag, WHAT delivers it: SIF/RPC result? OSD? neither?),
  and what the 20 `394ED0` lookups look up (key shape?
  table base? hit/miss semantics?).
- Table (iii) — storm issuers: attribute GetThreadId
  (290,210 vs 2) and FlushCache (14,356 vs 0) issuers
  (recompiled-output grep + override-table join — F3 notes
  no plain `addiu $v1,$zero,0x2f` site, so indirect/host
  path: table the actual sites).
- NO lease of any kind (no boots). No `adb`. No fork
  changes. No builds needed (pure static + streams).

## Gates and rules

- Evidence dir `local/research/A0/` (STANDALONE). Commit with
  `git add -f`, prefix `[A0]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 2 h (F3 §20 rank 1 box). Tables, no verdicts.
- Hygiene (F3 rec 17): write the report in chunks; put
  `tail -3 REPORT.md` in the receipts (truncated tail fails
  the gate).

## Task 1 — installer + exit predicate (tables i + ii)

1. Installer table: overridden syscall numbers + handler
   addresses + wrapper PCs + FlushCache sites (recompiled
   output line refs for every row).
2. Locator table: `InitSystemCallTableAddress` semantics
   (recognised-kernel return vs 0) + caller branch PCs
   (which path installs? which skips?).
3. Predicate table: per-iteration comparison + stop reason
   at 72,176 (counter/timeout/flag + evidence) + the 20
   lookups (key/table/hit-miss) + the flag-deliverer
   verdict (SIF/RPC? OSD? neither? — tabled with the
   code path).

## Task 2 — storm issuers (table iii)

1. GetThreadId table: issuer sites (recompiled PCs + call
   chains — indirect/host path spelled out) + per-VBLANK
   count reconciliation (20/VBLANK × VBLANKs ≈ 290,210?).
2. FlushCache table: issuer sites + per-VBLANK
   reconciliation (≈1/VBLANK × VBLANKs ≈ 14,356?).
3. Override-join table: issuers vs the (i) override table
   (do any issuers route through game wrappers?).

## Report

`local/research/A0/REPORT.md`: tables (i)+(ii)+(iii),
exact commands (greps + line refs), gaps (incl. what A1
needs from each table). Commit evidence (`[A0]`), stop.
