# G18 — Minimal fix for the null update_template[0]: static cause + guard, on-device re-run for O2 + score (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G17/REPORT.md` (ssx3 mirror) first (all
of it: O2's bad input NAMED — `update_template == nullptr` for set 0 with
`vk_set` valid at `flush_descriptor_set():3505`, fault `0xf0` at driver
`ldr w8, [x2, #0xf0]`; `:3504 VK_ASSERT` compiled out; NO "Failed to create
descriptor update template" in ANY logcat (creation-failed ELIMINATED);
three candidates open for WHY the slot is null: mask-skip at
`shader.cpp:551-554` vs heap/init path vs runtime-nulled slot; the ONE next
action = this brief). This brief executes G17's §3. No new dumps.

## Facts you start from

- Pins (re-verify, do not move except by the fix itself): paraLLEl-GS
  `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims + G11/G8+G10
  hunks; rich dump sha `154d9d85…`; G14 binary `c4cd63b4…` (pre-fix
  baseline — the fix rebuilds from the same tree + fix hunk).
- Device: Odin3 (`622c49b1`, Android 15). Transient dir
  `/data/local/tmp/g18/` ONLY; `mg/` never touched; remove the dir at end
  (no lldb this brief unless the re-run needs triage — table the decision).
- The task, in strict order: (1) STATICALLY name why `update_template[0]`
  is null for this compute pipeline's layout — decide among mask-skip
  (`shader.cpp:551-554` loop vs this layout's `descriptor_set_mask`),
  heap/init path (never created for this pipeline), or runtime-nulled slot
  (created then cleared) — with file/line receipts; creation-failed is
  already eliminated, do not re-litigate it; (2) add the MINIMAL
  guard/handling the cause justifies (one hunk, tabled: skip-the-call vs
  create-the-template vs assert-and-early-out — the cause decides; a guard
  that masks heap corruption is forbidden — G15's stop-rule rationale
  stands); (3) rebuild the replayer (`-j2`, new SSD build dir — G14 dir
  untouched), push + run on-device bounded (`--iterations 2`), and score
  with `g14-diff.py` (oracles + any scanouts; SCORE N/A if O2 persists or
  O1/O3 fires — tabled, never faked).
- Success bars (tabled, not verdicts): cause NAMED statically (one of the
  three, with receipts); minimal fix hunk (named files, diff tabled);
  rebuild exit 0 (binary path/size/sha); bounded on-device re-run (exit,
  wall, frames reached, fate vs O2); score table (or N/A with reason);
  hypothesis verdict + the ONE next action.

## Gates and rules

- Fix minimality: ONE hunk addressing the NAMED cause. If the static reads
  cannot decide among the three candidates: table the exact ambiguity +
  recipe and STOP with no fix (a guess-hunk is worse than none).
- No P-lane lease (no recomp boots/builds, no fork writes). No bytesize/WSL.
  Host builds `-j2`. No new dumps. TWO bounded device runs max (re-run +
  one retry if O1/O3 fires instead of first draw).
- Byte caps: declare up front for the new build dir (SSD) + retrieval
  (SSD `ps2x-g18/`); all prior `ps2x-g*` + build dirs read-only (0 growth);
  internal `/` delta ≤1 GB, no clones/builds on internal;
  `export COPYFILE_DISABLE=1` on SSD steps.
- Clone discipline: the fix hunk lives in the SSD clone, UNCOMMITTED (same
  standing as the G14 shims), precisely tabled (file/line/diff); no other
  clone edits. No third-party code copied into any project tree.
- Evidence: ps2xGS commit `[G18]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G18/`
  (`git add -f`, prefix `[G18]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — static cause + minimal fix + rebuild (no device yet)

1. Read the three candidate sites (mask loop + layout mask for this
   pipeline, template creation/init path, slot lifetime) and NAME the
   cause with file/line receipts. If undecidable: stop with ambiguity +
   recipe (no fix).
2. Write the ONE minimal hunk (table the diff + why this shape). Rebuild
   the replayer in a NEW SSD build dir (`-j2`); table binary
   path/size/sha + fix presence (symbol/string grep).

## Task 2 — bounded on-device re-run + score (Odin)

1. Push binary + dump (sha match on-device), run bounded
   (`--iterations 2`, wall cap), capture exit/wall/frames/fate + logcat.
   Pull logcat + any scanouts + tombstones (if any); remove device dir.
2. Score with `g14-diff.py` (oracles + scanouts, or N/A). Hypothesis
   verdict + the ONE next action (fix-works → adoption input; O2 persists
   → next naming/fix attempt; O1/O3 → lottery note + retry accounting).

## Report

`local/research/G18/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, cause table, fix diff, build record, re-run
table, score, verdict, exact commands, gaps. Commit evidence (`[G18]` both
trees), stop.
