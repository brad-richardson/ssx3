# E17 — Absorb the 5 verified I-rows into the fork (E14 retry, space now available; regen + regression + 1 boot)

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E14/REPORT.md` first (all of
it: the 5 I-rows VERIFIED byte-equal to the I-lane + 5 fail-befores
(absent-0/present-1) + predicted CSV `7c827add` == I17's full CSV — then
STOPPED at fresh APFS admission, 272 MB short with zero eligible trees;
0 edits/regen/commits/boots). Then read `local/research/E16/REPORT.md` +
`local/research/E16/NEXT-BRIEF.md` (checkpoint rebuilt from `67c0a632`,
run-exit closure repaired as fork `784f2b3e`, 1/1 probe spent with real
footers; absorb explicitly queued next). This brief executes the absorb
ONLY (the user's ~07:00 fork-merge decision: I-rows I11–I16 +
`0x2c5358` absorbed into one E-brief fork commit). No MPEG implementation
(that follows as its own brief once the absorb lands).

## Facts you start from

- BASE: fork `784f2b3e` (E16's closure repair, PUSHED — verify
  `fork/ssx3` before building). The 5 rows + fail-befores + predicted
  CSV carry over VERIFIED from E14 (all committed — re-verify, don't
  re-derive from scratch): per-row bytes vs I-lane + ELF slices +
  all 5 fail-befores re-run. Any row that no longer verifies STOPS
  the absorb (table which + why — no partial absorb).
- `/tmp/p1-link` EXISTS again (E16 rebuilt it — PROTECT it +
  DerivedData; never reclaim). SPACE IS AVAILABLE NOW: internal ~14 GB
  free (E14 needed 3.49 GB at sample), SSD ~240 GB free. Still declare
  an explicit budget + run FRESH admission before any edit (E14's
  ownership-proof machinery if you must reclaim anything — nothing is
  pre-approved for deletion). If admission fails: STOP (iii)-shaped
  with the exact shortfall, zero mutations.
- The task, in strict order: (1) checkpoint re-verify (fork SHA,
  9,452-name audit, suite 452/452, E16 closure fixture re-run,
  E15 MPEG fixtures still rc1 fail-before); (2) absorb the 5 rows
  (CSV edit + regen, rc=0, slot receipts); (3) FULL regression
  (suite 452/452 + leaf24/consumer3/predicate14/query4/DROP +
  E16 closure cases + E15 MPEG cases still fail-before); (4) ONE
  guarded boot (fresh T13 pre-claims, shared P-lane lease,
  wall/progress/byte caps, REPORT_ALL=1, E15/E7/E4 observations;
  lease occupied → table and stop); (5) fork commit (the 5 rows +
  regen, named files, NO other changes).
- Success bars (tabled): rows re-verified + fail-befores green;
  admission fits; regen rc=0 with the predicted CSV (`7c827add`
  unless E16's base legitimately shifts it — table any shift with
  the exact cause); full regression green; one boot with joins
  (or the exact missing receipt tabled); fork commit pushed.
- I-lane coordination: I-lane holds LOCAL ports of these rows and will
  dedupe like I17 did once the absorb lands (their brief, not yours —
  your handoff names the commit + per-row CSV lines for their dedupe).

## Gates and rules

- Fork owner: you. The absorb is a MAP commit (CSV + regen + generated
  staging per the E11/E12/E13 precedent — table the staged set;
  NOTHING else in the commit). Push to the `fork` remote (E16's
  precedent was orch-pushed; prefer pushing it yourself and receipt
  the `ls-remote` agreement like E13/E15 did).
- Boots: ≤1 guarded boot under the shared P-lane lease (fresh T13
  pre-claims, caps, REPORT_ALL=1). No boot without the lease; never
  wait on another lane's lease.
- `export COPYFILE_DISABLE=1` on SSD steps; allocated-byte caps declared
  + tracked (ExFAT). No bytesize/WSL. Host builds `-j2` unless provably
  otherwise (table it).
- Evidence dir `local/research/E17/` (STANDALONE). Commit with `git add -f`,
  prefix `[E17]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (the orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — re-verify + admission + absorb (no boot)

1. Checkpoint re-verify (fork/9,452/suite/E16-closure/E15-fail-befores).
   Per-row re-verify (bytes/slices) + 5 fail-befores. Fresh admission
   (must fit BEFORE any edit). Table every check.
2. CSV edit + regen (rc=0, slot receipts, predicted-CSV check). FULL
   regression (suite + all fixtures + E16 closure cases + E15 MPEG
   still-fail-before). Table every receipt.

## Task 2 — one guarded boot + fork commit

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Boot with E15/E7/E4
   observations; table the joins (no promotion without an actual
   request/completion dependency).
2. Fork commit (5 rows + regen only) + push + `ls-remote` agreement
   receipt. Handoff: per-row CSV lines for the I-lane dedupe + the
   MPEG brief now unblocked (queued, not this brief).

## Report

`local/research/E17/REPORT.md`: contract, re-verify + admission, regen +
regression tables, boot joins, handoff, exact commands, gaps. Commit
evidence (`[E17]`), stop.
