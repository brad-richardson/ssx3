# G15 — Catch O1's null-write live: instrumented on-device run + minimal fix + clean re-run

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G14.md` (all — O1: Granite
`CommandPool::trim()` null-`table` SIGSEGV at `command_pool.cpp:171`
(`ldr x9,[x9,#0x380]`, fault addr 0x380, pool live), reached via
`GSInterface::init → set_super_sampling_rate →
invalidate_super_sampling_state → wait_idle → trim_command_pools` BEFORE
dump open; static audit says live-pool + null-table is unconstructionable
in-tree; §3 ONE next action = this brief) + G13 §3c (oracle table) as
needed. This brief executes G14's §3. Init is PROVEN — do not re-litigate
it; the only unknown is the null-write site.

## Facts you start from

- The task: catch the null-write live with ONE instrumented bounded
  on-device run — lldb watchpoint on the faulting pool's `table` slot
  (preferred: names the write-site directly) or an HWASan build (fallback
  if the lldb path is blocked — table the choice with the forcing
  receipt) — then apply the MINIMAL fix the watchpoint names + ONE clean
  re-run scored by `g14-diff.py` against the G13 oracles. Odin: device
  `622c49b1` (re-confirm via `adb devices`).
- Experiment contract (required): hypothesis = the instrumented run names
  the exact write-site of the null `table` slot; observable = watchpoint
  / sanitizer backtrace + fixed-run exit + pulled scanouts + per-pair diff
  table vs G13 oracles; alternatives = (a) write-site named + minimal fix
  + clean re-run scored → table + ONE next action (adoption input);
  (b) write-site named but fix is NOT minimal (framework corruption,
  driver bug) → table + recipe, stop (no renderer changes, no heroics);
  (c) instrumentation blocked → table the exact obstacle + recipe, stop.
  Stop: TWO bounded device runs max (instrumented + verification) — no
  tuning loop, no renderer changes, no new dumps.
- The leading fix candidate (UNTESTED, G14's — not a verdict): a
  `table == nullptr` guard in `trim()`/`begin()`/dtor mirroring the
  existing pool guard, ONLY if the write-site proves an init-order edge
  rather than corruption. If corruption: stop at (b) with the evidence.
- No P-lane contention: no recomp boots/builds, no P-lane lease, no fork
  writes. `adb` in `/data/local/tmp/g15/` ONLY (never `mg/`); remove bulky
  device-side artifacts after retrieval. Reuse the G14 Android build dir +
  binary if it still verifies (sha `c4cd63b4…`); HWASan rebuild (if chosen)
  is a second build dir or a tabled rebuild — never clobber the G14 binary
  before the choice receipt exists. Byte caps declared up front, SSD reuse,
  nothing new on `/`.

## Gates and rules

- Host builds `-j2`. Wall cap + progress cap on every device run.
- Commit in ps2xGS as you go (prefix `[G15]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures,
  binaries, tombstones, or build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G15/` (`git add -f`,
  `[G15]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — instrumented run + minimal fix + clean re-run (2 bounded runs max)

1. Choice receipt: lldb path viable? (lldb-server on device, symbolized
   unstripped binary in hand) → watchpoint run; else HWASan rebuild with
   build receipts. Table the choice.
2. Instrumented run: backtrace of the null-write + faulting pool index +
   which `cmd_pools[i][j]` entry (G14's open delta) + write-site verdict
   (init-order edge vs corruption).
3. If (a): minimal fix + ONE clean re-run + `g14-diff.py` score table. If
   (b)/(c): finding + recipe, stop. Hypothesis verdict + the ONE next
   action the numbers justify.

## Report

Mirrored `REPORT.md`: contract, instrumentation + write-site tables, fix
(or obstacle), score table (or why none), license receipts, exact commands,
gaps. Commit both repos as above, stop.
