# M49 — Shape-732 c296→c298 move: dest-row listing + value table (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M28/REPORT.md` first (all of it —
M28 gap 5 = this brief via M48 gap 6, taken on orchestrator
judgment: 732's c296 full wipe with a fresh c298 dest
(rshift −1.5) — the only full single-column wipe with a
fresh dest), plus `local/research/M44/REPORT.md` (732's c298
dest rows [26,27] over kept s0 rows [28,29,34,35] — must
reproduce) and `local/research/M34/REPORT.md` (732's census
row — unnamed moves? reproduce it). Fully offline — SSD
dumps read-only, no harness code, no replays, no lease. No
device work.

## Facts you start from

- 732's move (reproduce exactly or table the mismatch and
  stop): tail 95, J 0.8942, moved [296] (named); c296 0/0
  full wipe → dest c298 (+2 rows, off +2, rshift −1.5,
  minshift +2, fresh); c298 dest extras [26,27] (M44).
- The question: dest-row listing (full c296 wipe rows +
  full c298 row-list: kept + [26,27]) + per-site value
  table for 732's full sets (wiped + dest + any unnamed
  732 moves per M34). Answer by table (list, don't assign
  wiped rows to dest rows).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28-census.tsv`
  + `/Volumes/Extreme SSD/m34/m34-census.tsv` (732 rows to
  reproduce — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m49/`;
  evidence `local/research/M49/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m48/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m49/` + `local/research/M49/`.
- Evidence dir `local/research/M49/` (STANDALONE). Commit with
  `git add -f`, prefix `[M49]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M48 precedent).

## Task 1 — 732 wipe + dest reproduction (do the columns reproduce?)

732's row recomputed from the dumps (must match M28/M34/M44
exactly — tail, J, moved cells, 0/0 wipe, dest rows [26,27],
displacement +2/−1.5/+2, unnamed moves? — or table the
mismatch and stop):

1. Wipe table: c296 s0-rows vs 732-rows (empty) vs miss
   rows (full wiped row list).
2. Dest table: c298 s0-rows [28,29,34,35] vs 732-rows
   (kept 4 + extras [26,27]) — M44 reproduced.
3. Displacement table: M28's c296→c298 row reproduced
   (+2/−1.5/+2, fresh — table the rule's row, don't
   re-judge).

## Task 2 — full-set value table (wiped + dest + unnamed?)

1. Wiped-value table: per-site |δ_s0|, s732 status, signed
   gaps, gap signs for ALL c296 wiped rows (M36-style).
2. Dest-value table: c298's [26,27] + kept rows with |δ|,
   statuses, gaps, signs (M44's values reproduced +
   kept-row values — the new data).
3. Accounting table: wiped count vs dest growth (+2) vs
   kept rows vs full priv/miss/shared (do counts close?
   numbers only — no assignment).

## Task 3 — controls + determinism (M18–M48 precedent)

1. Positive control: synthetic truth with KNOWN single move
   (1 wipe col + 1 fresh dest, known rows/values) —
   recover the wipe + dest + value tables exactly.
2. Determinism: re-run Task 1 on s0+732, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M48).

## Report

`local/research/M49/REPORT.md`: DESIGN.md (bars first) +
wipe/dest tables + value tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a move map
discriminates (budget-noted, pre-registered split required
per M23–M48 precedent). Commit evidence (`[M49]`, no push),
stop.
