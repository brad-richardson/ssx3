# T33 — Cross on Snow Jam from the Select Event park: next screen (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T32/REPORT.md` first (all of it: R1
reached Select Event via Cross on Race @T+266.01, Snow Jam highlighted
(orange bar) / Metro-City / Happiness, course-map `Race` panel, tagline
`Snow Jam is an exciting BEGINNER track with heavy forests and patches of
fog.`, 12 snaps over 133 s pairwise ≤0.064/p99 ≤1, no reclaim; G1 proposes
one 534 ms Cross (× Select) on Snow Jam, WITH A GATE WARNING — read it).
This brief executes T32's G1. Reuse T32's scripts (`t32-auto.sh`,
`t32-cropdiff.py`, `t32-analyze.sh`, `t32-vcount.sh` — copy, do not modify
the committed originals). REAL filenames only (T28 G9 lesson): T32's
arrival snaps are `t32r1-rc-post{1,3,8,15,25,40}.jpg` +
`t32r1-rc-stab{1…6}.jpg`; the Select Event ref recipe is PIL
`convert('RGB').save` (T28 §T28-1).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T32 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- GATE WARNING (T32 G1 — the SM→SE hop is SMALL): whole-frame SM→SE is only
  ~0.50 (hop) / ~0.53 (vs-SM); DEPARTED > 5.0 does NOT fire here. Derive a
  Select Event ref (from `t32r1-rc-stab6.jpg`) + calibrate a tighter gate
  (SE-vs-SE spread ≤0.064; nearest non-SE is SM at ~0.51 — a < 0.2-style bar
  or a header/list-region crop) BEFORE the run; table the calibration matrix
  as in T32 §T32-1.
- The task: reproduce the T32 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → ≤3 attempts) to the Select Event park
  with Snow Jam highlighted, then press ONE 534 ms-class Cross (× Select)
  and table EVERYTHING after: the press (button, hold ms, T+ time, pre/post
  snaps), each screen transition (snap sha + region-diff scores), and the
  arrival screen (snaps + stability count + content read off viewed snaps).
- If Cross does NOT advance (selection ignored, needs a different button,
  next screen loads slowly): table the exact observed behavior (snaps +
  scores + waits) and STOP with a recipe — one input per screen, never
  blind multi-presses. ONE bounded variant allowed only if the press
  provably never acted on the arrival screen (pre-press ≠ Select Event).
- Success bars (tabled, not verdicts): arrival park reproduced (snap match
  vs T32's `t32r1-rc-stab6.jpg` under the calibrated gate); Cross acted on
  it (pre-press = Select Event, Snow Jam highlighted); next-screen snaps +
  stability N; full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T32 G2: single-shot
  scripts, runs ≤6 min (T32 ran 417 s — keep added scoring tight or table
  the overrun with full dmesg coverage), preserve emulogs first, dmesg +
  btime + init age, UNFILTERED dmesg + event log around flaps, 534 ms holds
  NOT bisected, `;` chains only INSIDE one `wsl` call, one `wsl` call per
  ssh). Runs in flap windows must be effects-verified or re-run (T27 §4).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T33/` (STANDALONE). Commit with `git add -f`,
  prefix `[T33]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — arrival park + Cross on Snow Jam (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T32 §T32-0 shape): build/input receipts, NVM state, T32
   reference snaps present, SE-ref calibration matrix, free space both ends.
2. Per attempt table: park reproduction (chain scores + snap match vs
   `t32r1-rc-stab6.jpg`), press row (button/hold/T+/pre-post snaps),
   transition chain (snap sha + diffs per hop), arrival (snaps + stability
   N) or exact non-advance behavior.
3. Trace bytes + sha per run; WSL event table; NVM state at end.

## Report

`local/research/T33/REPORT.md`: pre-run receipts, attempt tables, press
log, arrival (or non-advance characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T33]`), stop.
