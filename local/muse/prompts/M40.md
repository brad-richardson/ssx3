# M40 — Shape-9 per-cell standing asymmetry: row-list + value comparison across the 4 missing-bearing cells (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M35/REPORT.md` first (all of it —
M35 gap 1 is this brief, taken on orchestrator judgment: shape-9's
missings concentrate in 4/15 cells (311/312/314/315) while 11
cells read extra-only; c314's 3 missings read all noncell
(|δ| 12/23/45) while c315's 2 read all bulk (|δ| 10/10)),
plus `local/research/M39/REPORT.md` (comparison precedent:
missing-side agreement vs extra-side split across uniform vs
mixed cells; corrected pooled universe 24 = c54's 10 + 14
others). Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- The 4 missing-bearing cells (M35 — reproduce exactly or
  table the mismatch and stop):
  - c311 (missing-only, d1, n/ov 3/3, n0 4): missing
    (296,311) off 379502, |δ_s0| 10, noncell on s9,
    gaps 24/7, ++.
  - c312 (mixed, d3, miss/extra 2/1): missings (299,312)
    off 383344 |δ| 10 bulk6 gaps 22/16 ++ + (301,312)
    off 385904 |δ| 9 bulk7 gaps 28/28 ++; extra
    (248,312) off 318064 |δ_9| 13 bulk1 gaps 33/10 ++.
  - c314 (mixed, d4, miss/extra 3/1): missings (269,314)
    |δ| 12 noncell gaps 41/29 ++, (288,314) |δ| 45
    noncell gaps 107/84 ++, (289,314) |δ| 23 noncell
    gaps 54/31 ++; extra (246,314) |δ_9| 13 bulk ++
    (offset per M35 tables).
  - c315 (mixed, d5, miss/extra 2/3): missings (266,315)
    |δ| 10 bulk2 gaps 64/46 ++, (273,315) |δ| 10 bulk3
    gaps 23/23 ++; extras (245,289,290) |δ_9|
    [11,13,16], 1 bulk / 2 noncell, gaps ++/−−/−0.
- The asymmetry: missings read all-bulk in c312/c315 vs
  all-noncell in c311/c314 (M35 §Step 3). The question: is
  the bulk/noncell split row-deep (the cells' extras and
  row-lists split the same way) or missing-only (extra
  sides + row shapes agree across the 4 cells)? Answer by
  table: per-cell row-lists + per-site values + standing
  splits, c311 vs c312 vs c314 vs c315.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m35/m35.txt`
  (the 4 cells to reproduce — match exactly or table the
  mismatch and stop), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (FULL TSV guard). Work dir: new `/Volumes/Extreme
  SSD/m40/`; evidence `local/research/M40/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m39/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m40/` + `local/research/M40/`.
- Evidence dir `local/research/M40/` (STANDALONE). Commit with
  `git add -f`, prefix `[M40]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M39 precedent).

## Task 1 — 4-cell recompute (do the cells reproduce?)

The 4 missing-bearing cells recomputed from the dumps (must
match M35 exactly — row lists, standings, deltas, per-site
|δ|/status/gaps — or table the mismatch and stop):

1. Row-list table: all 4 cells s0-rows vs s9-rows vs
   extra/missing rows (M35-headliner style).
2. Standing table: c311 missing-only d1 vs c312/c314/c315
   mixed d3/d4/d5 (extra counts + missing counts + deltas).
3. Value table: per-site |δ_9|/|δ_s0|, statuses, signed gaps,
   gap signs for all 4 cells' sites (8 missings + 5 extras).

## Task 2 — asymmetry comparison (row-deep or missing-only?)

1. Missing-side comparison: all-bulk c312/c315 (|δ|, gaps,
   signs) vs all-noncell c311/c314 (same columns —
   med/match per site, numbers only).
2. Extra-side comparison: c312's 1 + c314's 1 + c315's 3
   extras (|δ|, bulk status, gaps, signs — do the extra
   sides split along the cells' missing-side bulk line?).
3. Row-shape comparison: n0/n/ov per cell (does s0's row
   count or the overlap predict the bulk/noncell side?).

## Task 3 — controls + determinism (M18–M39 precedent)

1. Positive control: synthetic truth with KNOWN split cells
   (N all-bulk-missing cells + N all-noncell-missing cells,
   known values) — recover the standing + value tables
   exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M39).

## Report

`local/research/M40/REPORT.md`: DESIGN.md (bars first) +
recompute tables + comparison tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if an asymmetry map
discriminates (budget-noted, pre-registered split required
per M23–M39 precedent). Commit evidence (`[M40]`, no push),
stop.
