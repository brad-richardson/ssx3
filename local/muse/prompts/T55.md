# T55 — PCSX2: inputs and hash of `func_394ED0` calls from `0x362f68` (healthy side of E43)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). **Tables + receipts; the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T54/REPORT.md`.

## Why

T54: the 2 mode-6 draw records (w0 `0x1b0`) are written each vsync by stores
at `0x394fdc–0x394fe8` inside `func_394ED0`, called unconditionally from
`sub_00362DE8` @`0x362f68` as `func_394ED0(a0=s6, a1=s1, a2=hash)`, where the
8-bit hash is folded from the 4 words at `s1` (@`0x362f00–0x362f6c`). The
recomp (E43, same format) never produces mode-6 records. We need PCSX2's
per-call inputs to compare: different items, or different hash for the
same items.

## Change (log-only, on T54's clone; EE interpreter)

At the `jal func_394ED0` @`0x362f68` (hook like T54's JAL census), per call
over ~5 settled vsyncs: `h394 vsync=<n> s1=0x<> w0=… w1=… w2=… w3=…
hash=0x<a2 & 0xff> ret=0x<v0 at return> stores=<1 if 0x394fdc–0x394fe8
fired during this call>`. Return value and stores need a tiny call-scope
flag; keep it simple.

## Deliverable

≤1 build, ≤2 captures, 2 h, cap 2 GB. `local/research/T55/REPORT.md`
(patch, hooks, preservation, the per-call table). No verdict. `[T55]`
commit (`git add -f local/research/T55 local/muse/prompts/T55.md`, trailer
`Orchestrated-By: Muse Code`, `git log -1` first), no push.
