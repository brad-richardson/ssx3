# N8D5 Part 5 — one Odin tile-path discrimination run

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. Work only in `local/research/N8D5E/`, Mac scratch `~/dev/ssx3-work/N8D5E/`, and lease-held Odin data. **One install and one launch only.** No source edit, fork/main push, GS stream capture, speed claim, iPhone/iPad action, or second run. No device action until the orchestrator sends the N8D5D package PASS and APK SHA pin.

Why: N8D4's same-stream Mac replay shows broad HUD/snow where Odin's raw mapped/frontend frame is sparse at tick 2050. N8D5C independently validates a GPU sampled tile path on Mac: checkerboard control 128/128 and sampled/raw 896-word vectors identical, 567 active tiles. N8D5L adds short summary lines because Android's 1,024-byte logcat reader splits full vectors. N8D5D packages that exact source. The device run asks whether the live sampled image is sparse at `Present`, or the subsequent copy/map path loses pixels. **This live run uses the same route/tick but not a byte-identical GS stream to N8D4.**

Inputs: N8D5D APK at `~/dev/ssx3-work/N8D5D/app-release.apk`, SHA-256 `a6a0c3793e31310fc8794e7a0d6b6cc282e5060d59cfe116b919236b9f50612a` (read twice locally before use); packaged runner SHA `6a93af327b3cb1c5ebd9b3b81dc68ae1eb5e259633c9c61b78a82ba06428d162`, Build ID `ab1bdc3b61faf1fe99cd5844e8c90ffaee8f54f6`; Turnip SHA `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`; installed ELF SHA `1b49d05ca2793922180851b9e1ce9ae2291d61a7863565ac4e71f12e967af7bc`, ISO SHA `3c2f8eb182c9c6208a6e8172a41e61c98f420abe3f42c845f6829aeb9761ebf5`. Use [N8D4's launcher](../../research/N8D4/launch.py) for the proven lease/preflight/route/force-stop pattern and [I26-FAST](../../research/I26/ROUTES.md). Write `REPORT.md` before launch with A/B/OTHER thresholds, pins, stop rules, and a bounded `launch.py`; pin the new script SHA before its run.

Preflight: Odin serial `622c49b1`; one device lease `/data/local/tmp/mg/LEASE` must be free; check `adb get-state`, `dumpsys window policy` `KeyguardServiceDelegate showing=false`, `dumpsys battery` charging/full and >=20%, and free data storage. A locked device blocks launch: tell orchestrator to ask Brad to unlock. Claim lease, install APK even if present, double-read installed APK, ELF, ISO SHAs and require pins; `mc0` empty. Use a unique empty frame dir. Before launch and again immediately before `am start`, confirm unlocked/charged. Enable `PS2X_GS_BACKEND=parallel`, `PS2X_GS_TURNIP=1`, `PS2X_SKIP_MOVIE=1`, `PS2X_CD_IMAGE=<existing ISO>`, I26-FAST `PS2X_PAD_SCRIPT_CLOCK=vsync`, `PS2X_N8D5_TILE_CAPTURE=1`, `PS2X_FRAME_DUMP_DIR=<unique dir>`, `PS2X_FRAME_DUMP_ONCE_TICKS=2050,999999,999999`, and `PS2X_VSYNC_RATE_LOG=1`. Clear logcat, capture bounded same-PID main/crash log, send BACK once after 6 s for USB dialog.

Stop on the first aligned tick-2050 `[n8d5b] alignment` + `control=` + `sampled_summary` + `raw_summary` + frontend frame dump, or first fatal/process exit. Stop at tick 2100 without complete receipt, 180 s if below tick 1700, or 300 s wall; no boot exceeds 600 s. Keep logs <=16 MiB. **Force-stop after every run**, verify PID absent, release lease even on error. Pull only tick-2050 PNG/metadata and bounded log receipt; read each device SHA twice and compare two local SHA reads. Compress the closed full log in scratch; commit small receipt excerpts and review PNGs only. Do not use a screencap as a substitute for the frame dump.

Hand back an evidence table: exact APK/runner/Turnip pins from N8D5D, env, script SHA, launch PID, tick/FBP/PMODE/size, control value, each summary's 896-tile occupancy/active counts, frame metadata/SHA, cleanup, first failure. **A:** aligned tick 2050, FBP112, PMODE `ff21`, 512x448, control 128/128, sampled <=100 and raw <=100 active tiles. **B:** same control/alignment, sampled >=500 and raw <=100. Everything else is **OTHER**. These are handoff categories only; do not declare the causal verdict. The orchestrator views the frame and gates it. Commit `[N8D5E]` with `Orchestrated-By: Codex` and explicit named paths, no push.
