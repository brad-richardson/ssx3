# E19 — Diagnose the input→packet boundary: why 5,040 accepted bytes yield zero parser packets (measurement first; fix ONLY the single demonstrated edge)

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E18/REPORT.md` + `local/research/E18/NEXT-BRIEF.md`
first (all of both: E18 delivered 5,040 bytes via AddBs on the GetPicture
caller — fixtures flipped rc1→rc0, 458/458 — but the host parser output
zero packets/frames/completions; main still waits at `0x3b1028`; the
5,040 bytes OPEN with a real MPEG sequence header `000001b3`; R3 proves
the host decoder emits frames from authored input, so this is an
input-completeness/demand question, NOT a broken decoder). This brief
DIAGNOSES the boundary. It fixes something ONLY if exactly one edge is
demonstrated (per the S7/S10 stop rule — no stacking, no policy
invention, no fabricated frames/EOF).

## Facts you start from

- BASE: fork `3adc0478` (E18 BEHAVIOR, PUSHED — verify `fork/ssx3`
  before building). `/tmp/p1-link` + `/tmp/e17-map-link` +
  `/tmp/e18-mpeg-link` exist (PROTECT all + DerivedData; never
  reclaim). Checkpoint re-verify first (fork SHA, 9,457-name audit,
  suite 458/458, E16 closure cases, E15 cases rc0 now, E18 R1–R6).
- The questions, in order (each answered with receipts before the next
  opens): (1) PARSER: how many bytes does the host parser need before
  packet 1? (fixture: feed the retained first64 + authored continuations
  at 5,040 / 8,192 / 16,384-byte marks — table the emission threshold;
  NO full-payload replay claims beyond the retained bytes + authored
  bytes actually fed); (2) DEMAND: what signals further input — does
  `getMpegPicture` re-request after a successful-but-incomplete feed,
  does the guest issue a second GetPicture/AddBs, or does the runtime
  hold a buffered remainder nobody drains? (static demand-path audit
  + the E18 boot's post-input event silence as the baseline);
  (3) GUEST: in ONE guarded boot, watch for a second request/delivery
  cycle (extended observation past the first input — table whether the
  guest ever asks again and what changes if it does).
- The fix gate (strict): implement something ONLY if the diagnosis
  isolates ONE edge with a minimal, ABI-preserving change (e.g. a
  re-request/drain of an already-buffered remainder on the proven
  caller-owned path). If the edge is "the guest never sends more
  without X" — NAME X (with the exact missing signal) and STOP. If
  the edge needs decoder changes, EOF invention, or policy invention —
  STOP. All E18 binding constraints carry (caller-owned sync dispatch,
  v0-discard, valid-no-input waits, mutex discipline, delete/reset
  cancellation).
- Regressions: suite 458/458 + all E18 fixtures (R1–R6 + flipped E15
  cases STAY flipped) + E16 closure + prior bindings. Any red: table +
  stop (no boot on red). No CSV/regen (pure runtime/diagnosis brief —
  regen need = stop).
- ONE guarded boot max (fresh T13 pre-claims, shared P-lane lease,
  wall/progress/byte caps, REPORT_ALL=1 + parser-boundary observations;
  lease occupied → table and stop). The boot's job is the demand watch
  (second-request detection), not a second fix vehicle.
- Success bars (tabled): emission-threshold table (bytes→packets);
  demand-path audit (who asks for more, with what signal, or the
  proven absence); one boot with the second-request verdict; EITHER
  one minimal fix with full regression + the fix's own fail-before
  gate OR the named next edge with the exact missing signal.

## Gates and rules

- Fork owner: you. A fix (if the gate opens) is a BEHAVIOR commit
  (named files only — table the set; NOTHING else; push to `fork`
  yourself with the `ls-remote` receipt). Diagnosis-only outcome =
  zero fork commits (evidence only).
- Boots: ≤1 guarded boot under the shared P-lane lease. Fixtures:
  non-title only. No boot without the lease; never wait on another
  lane's lease.
- `export COPYFILE_DISABLE=1` on SSD steps; allocated-byte caps declared
  + tracked (ExFAT). No bytesize/WSL. Host builds `-j2` unless provably
  otherwise (table it). Declare + verify the budget BEFORE editing.
- Evidence dir `local/research/E19/` (STANDALONE). Commit with `git add -f`,
  prefix `[E19]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (the orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — threshold + demand audit (no title boot)

1. Checkpoint re-verify (fork/9,457/458/E16-closure/E15-still-rc0/
   R1–R6). Budget declared + verified-fitting before edits.
2. Emission-threshold fixtures (authored byte marks; table
   bytes→packets/frames). Demand-path audit (static + E18 silence
   baseline). Table the fix-gate verdict (one edge or STOP).

## Task 2 — one guarded demand-watch boot (+ fix ONLY if gated)

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1 + parser-boundary
   observations. Table the second-request verdict (asked again with
   what / never asked). First missing link after that → table + STOP.
2. Fix ONLY if Task 1 demonstrated exactly one edge (fail-before gate
   + full regression + fork commit + push + `ls-remote` receipt).
   Handoff: the exact next dependency (parser/demand/guest) with the
   missing signal named.

## Report

`local/research/E19/REPORT.md`: contract, threshold + audit tables,
boot verdict, fix-or-stop, handoff, exact commands, gaps. Commit
evidence (`[E19]`), stop.
