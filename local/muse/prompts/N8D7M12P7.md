# N8D7M12 Part 7 — PKTSEQ Odin replay pair (7A launcher, 7B run)

You are the N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, then in full: `local/research/N8D7M12P6M6R/ORCH-GATE-P2.md`, `local/research/N8D7M12P5A/{launch.py,check.py,REPORT.md}`, `local/research/N8D7M12P5E2/REPORT.md`, `local/research/N8D7M12P5F4P2/{REPORT.md,replay-excerpt.txt}`. Cite local files, not the web. Own only this brief's receipts in `local/research/N8D7M12P7/` and private scratch `~/dev/ssx3-work/N8D7M12P7/`. No source/fork/APK/stream edit, no build, no Mac replay, no push, no board/ledger edit, no upstream contact, no subagents. Text receipts < 512 KiB; device output ≤ 64 MiB per run. Write `REPORT.md` early and hand back tables, not a graphics verdict.

**Goal.** The last two same-settings Odin runs (old APK) had equal guest priv hashes 41/41 but GPU VRAM only 16/41 and present 25/41, first split at tick850; frames mostly black while the Mac frame is full. The new APK adds a worker-consumption digest (`PS2X_GS_REPLAY_PKTSEQ=1` → `GB4_PKTSEQ tick=<t> seq=<16 hex> commands=<n>`, 41 sampled rows) that says whether the GS worker consumed the same command sequence. Question: do two Odin runs, and the Mac ON control, consume identical command sequences while the GPU output differs?

**Pins (from ORCH-GATE-P2).** APK `~/dev/ssx3-work/N8D7M12P6M6/app-release.apk` `da9a41a8e808fe31d509489c60b3927587c367866c6c2c6582009c35de6ef262` 153,753,116 B; runner `e5a3302c6bef489b04a4a143c47b01e616735f8db0acdbda663710a326c11af1` (Build ID `4c9c9d149900cdc3494201c38124d11bfb616fa3`); Turnip `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`; HAL `1b49d27c839f25363237d8276c91a401602845ee8eef67de50d6540f4cdfc387`; stream `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593` (already on the Odin; verify only, never push). Mac ON control rows: `local/research/N8D7M12P5F4P2/replay-excerpt.txt` (41 `GB4_PKTSEQ` + 41 `GB4_REPLAY`).

## Part 7A — launcher only (no adb, no device)

Copy `local/research/N8D7M12P5A/launch.py` (SHA `287140bf…1334e`) to `local/research/N8D7M12P7/launch.py`. Allowed changes only: (1) APK/runner pins to the new values above (Turnip/HAL/stream unchanged); (2) add `PS2X_GS_REPLAY_PKTSEQ=1` to the replay env, which otherwise stays the P5A ON env byte for byte (it matches the Mac control's three capture flags); (3) add `PS2X_GS_REPLAY_PKTSEQ` and `GB4_PKTSEQ tick=` to the packaged-runner marker check; (4) a required `--run run1|run2` argument that sets scratch `~/dev/ssx3-work/N8D7M12P7/<run>`, lease tag and remote output basenames; (5) extract `GB4_PKTSEQ` lines from the full logcat into `<run>/pktseq.txt` and require exactly 41 rows with ticks 50..2050 in order, same as the replay rows. Everything else (double SHAs, keyguard/battery/free checks, lease, install-every-run, one launch, BACK, caps, force-stop + PID-absent, env 2+2 restore, `finally` cleanup) stays.

Write `check.py`: diff P5A vs new launcher and allow only the listed changes; prove env keys equal P5A plus exactly the one new key; run the no-device self-tests; `py_compile`; report the launcher SHA. Commit `[N8D7M12] Part 7A` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. **Stop and hand back the launcher SHA and check table. Do not touch the device.**

## Part 7B — two runs (only after the orchestrator releases the exact launcher SHA)

Preflight each run: launcher SHA equals the released SHA, serial `622c49b1`, lease `LEASE_FREE` or ours, app not running, `dumpsys battery` charging and ≥ 20 %, `/data` free ≥ 10 GiB, `dumpsys window policy` shows `showing=false`, and no other mini boot/heavy job. A locked Odin is a blocker: stop and report; never work around it. Then `python3 -u local/research/N8D7M12P7/launch.py --run run1 --released-sha <SHA>`, then the same with `--run run2`. One launch per run, no retries. The launcher force-stops after every run. Stop on the first failed step.

**Compare** (write `compare.py`; no judgment in it): for run1 vs run2 vs Mac ON, per tick 50..2050: PKTSEQ `seq` and `commands` equality, `GB4_REPLAY` priv/VRAM/present equality, first differing tick for each field, final PPM SHA. Give the absolute PPM paths so the orchestrator can view them.

Predeclared reading (the orchestrator decides): PKTSEQ equal 41/41 across all three while VRAM/present differ → the command stream reaches the worker intact and the divergence is downstream (renderer/Turnip/readback); stop adding input hashes. PKTSEQ differing between runs or from the Mac → a delivery/order problem; report the first differing tick. A `commands` mismatch with equal `seq` or the reverse → report it as-is. Any preflight/cleanup failure voids the pair.

**Deliverable.** `REPORT.md` with the preflight and run tables, the 41-row comparison table, first differences, PPM paths/SHAs, exact commands, gaps; `compare.py` + its output; commit `[N8D7M12] Part 7B` (explicit paths, `Orchestrated-By: Muse Code`), no push. Budgets: 7A 30 min; 7B 40 min plus ≤ 600 s active per run; 2 installs, 2 launches, 0 builds. Elapsed time is a run control, never a speed number.
