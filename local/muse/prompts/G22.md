# G22 — Workaround: gate the sampler_feedback dispatch behind a knob, validate on-device (no O4 + frame completes), pixel-diff vs G13 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G21/REPORT.md` (ssx3 mirror) first (all
of it: S3d re-verified 30/32 + 2 folded corrections; crashing pipeline =
`sampler_feedback` `7463dfd379df2855` (0 specs, `DESCRIPTOR_BUFFER_BIT`, no
heap); SPIR-V valid with a physical-addressing survivor differential;
minrepro built UNTESTED; filing draft complete and NOT submitted) + G20 §3
(the `dispatch_texture_analysis` site + pipeline flags). This brief executes
G21 §4's WORKAROUND arm — user-authorized (filing stays open and unfiled
per the standing no-upstream order; the workaround does NOT close it). No
new dumps.

## Facts you start from

- USER DECISION (recorded by the orchestrator): workaround NOW, filing
  later. The workaround is LOCAL behavior change behind a knob — no
  upstream contact of any kind in this brief (no accounts, trackers, email,
  or submission).
- Pins (re-verify, do not move except by the workaround itself):
  paraLLEl-GS `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims +
  G11/G8+G10 hunks + G18 fix hunk (all uncommitted in the SSD clone —
  verify hunk bytes before building); rich dump sha `154d9d85…`; G14
  binary `c4cd63b4…` (pre-workaround baseline for behavior comparison).
- Device: Odin3 (`622c49b1`, Android 15). Transient dir
  `/data/local/tmp/g22/` ONLY; `mg/` never touched; remove the dir at end.
- The task, in strict order: (1) implement the workaround as ONE hunk:
  gate the `sampler_feedback` dispatch in
  `GSRenderer::dispatch_texture_analysis` (`gs_renderer.cpp:3486`) behind
  an env knob (SKIP the dispatch when the knob is set — table the exact
  gate: which call is skipped, what state is left unset, and why skipping
  cannot corrupt later draws beyond the quantified diff); defer/emulate
  shapes are REJECTED for this brief (defer just moves the crash;
  host-emulation is out of scope — table this choice, do not relitigate);
  (2) rebuild the replayer (`-j2`, NEW SSD build dir — G14/G18/G20 dirs
  untouched); (3) bounded on-device run WITH the knob (`--iterations 2`):
  validate no-O4 + frame completes (or table the exact new fate);
  WITHOUT-knob run is NOT needed (G18/G19/G20 already prove the crash —
  do not spend a run re-proving it); (4) pixel-diff any produced scanouts
  vs the G13 oracles with `g14-diff.py` to QUANTIFY the rendering-behavior
  change (or SCORE N/A with reason — tabled, never faked).
- Success bars (tabled, not verdicts): pins + hunks verified; workaround
  hunk tabled (diff + gate semantics + why-skip); rebuild exit 0 (binary
  path/size/sha); ONE bounded knob run (exit, wall, frames, fate, O4
  absent/present with triage); score table quantifying the behavior change
  (or N/A); hypothesis verdict + the ONE next action (workaround-works →
  adoption input incl. the still-open filing; O4 persists → next attempt;
  new wall → its exact brief).

## Gates and rules

- Stop rule (G21 shape): TWO bounded device runs max (knob run + one retry
  iff O1/O3 fires instead of first draw) — no tuning loop, no second
  workaround shape, no new dumps, no source fix beyond the ONE hunk. No
  lldb unless the run needs triage the tombstone cannot answer (table the
  decision).
- Re-sha hygiene (standing — zero-reads are TRANSIENT read-path artifacts):
  sha the binary at build time AND immediately before push; on-device sha
  must FULL-match before the run counts; any mismatch → re-read/rebuild,
  never push blind.
- Byte caps: declare up front for the new build dir (SSD) + retrieval
  (SSD `ps2x-g22/`); all prior `ps2x-g*` + build dirs read-only (0 growth);
  internal `/` delta ≤1 GB, no clones/builds on internal;
  `export COPYFILE_DISABLE=1` on SSD steps. No P-lane lease (no recomp
  boots/builds, no fork writes). No bytesize/WSL. Host builds `-j2`.
- Clone discipline: the workaround hunk lives in the SSD clone, UNCOMMITTED
  (same standing as the G14 shims + G18 hunk), precisely tabled; no other
  clone edits. No third-party code copied into any project tree.
- Evidence: ps2xGS commit `[G22]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G22/`
  (`git add -f`, prefix `[G22]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — workaround hunk + rebuild (no device yet)

1. Re-verify pins (clone HEAD/status, all hunk bytes, dump sha). Write the
   ONE skip-gate hunk at `gs_renderer.cpp:3486` (table the diff + gate
   semantics + the defer/emulate rejection + what state skipping leaves
   unset). If the gate point cannot be implemented as a clean skip: STOP
   with the exact obstacle + recipe (no alternative shape in this brief).
2. Rebuild the replayer in a NEW SSD build dir (`-j2`); table binary
   path/size/sha + workaround presence (knob string grep). Re-sha
   immediately before the Task 2 push (hygiene rule).

## Task 2 — ONE bounded knob run + score (Odin)

1. Push binary + dump (sha FULL-match on-device or the run does not count),
   run bounded with the knob (`--iterations 2`, wall cap), capture
   exit/wall/frames/fate + logcat + tombstones (if any). Pull receipts;
   remove device dir.
2. O4 triage (absent with proof, or present with signature comparison vs
   G19/G20) + `g14-diff.py` score (behavior-change quantification, or
   N/A). Hypothesis verdict + the ONE next action (adoption input /
   next attempt / new-wall brief — with the filing still open).

## Report

`local/research/G22/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, hunk diff, build record, run table, O4 triage,
score, verdict, exact commands, gaps. Commit evidence (`[G22]` both
trees), stop.
