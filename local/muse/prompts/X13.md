# X13 — dense local Qwen on a tiny source-order map

You are the **default dense Qwen3.8-27B** opencode worker. This X-track trial tests whether a small pinned excerpt and one LSP lookup keep the model useful for locating a diagnostic seam. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, and `local/AGENTS.local.md`. Cite the repo, not the web. Write `local/research/X13/rows.tsv` **early**, before broader reasoning. Do not edit code, build, boot, touch a device, or decide the N8D4 cause/fix.

Pin `/Users/brad/dev/ssx3-work/N8B1/PS2Recomp` at `17e90ded3689685ad359b76a9168c80a1f752e2d`; if different, stop. Read only `local/research/X13/excerpt.txt` (4.3 KiB, numbered source lines 236–310 from `ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`) and this brief. The lookup table below gives the complete accepted order. Fill TSV columns `stage`, `line`, `source` with **the entire exact source line stripped only of outer whitespace**. No paraphrases, extra rows or source search. Use `lsp goToDefinition` once on `m_iface->vsync(vsync)` at line 253 in that pinned file; record the returned target or an empty/error result in `REPORT.md`. Do not infer a missing call from empty LSP output.

| Stage | Required source line | Meaning to check only against the excerpt |
| --- | ---: | --- |
| `scanout` | 253 | `vsync` yields `shot.image` |
| `barrier` | 275 | prepares image for transfer |
| `copy` | 284 | copies image to buffer |
| `submit` | 288 | submits commands |
| `wait` | 289 | waits before host read |
| `map` | 291 | maps buffer to host pointer |
| `pack` | 303 | copies mapped rows into frontend frame |

Run `python3 local/tooling/orch/check_x13.py local/research/X13/rows.tsv` for exact 7/7 acceptance. Time cap 8 minutes; context target <15k. If you hit the cap, leave the partial rows and report the gap. `REPORT.md` gives exact commands, elapsed time, context size if visible, checker result, LSP result, and no cause verdict. Commit only `rows.tsv` and `REPORT.md` as `[X13]` with `Orchestrated-By: opencode`; no push. Hand back the table, not a conclusion.
