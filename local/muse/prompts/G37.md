# G37 — Minimum-content threshold wall: shrunken tag, same raw legs ("ANY nonzero" vs higher thresholds, Odin)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS` — VERIFY at start, re-pin if
the Mac-mini migration moved it, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts
beyond the hypothesis.** Read `local/research/G36/REPORT.md` first
(all of it: pure-refined-H2 WINS — the 112-word tag renders EXACTLY
with zero write-path ordering ops, 16/16 B1 + 10/10 ladders + 10/10
scanouts == tag model; H1-as-barrier refuted 0/10). This brief
executes G36 §4's ONE next action ONLY. No upstream contact of any
kind (standing no-upstream order — local hunks only, filing stays
local).

## Facts you start from

- The ONLY open discriminator in the lane: pure-refined-H2 stands
  with its quantifier untested — every barrier/commit variant of H1
  is dead (H1-barrier 0/10 G35, H1-as-barrier 0/10 G36). The split:
  "ANY nonzero-RGB content" (threshold = 1 word: the shrunken tag
  renders EXACTLY) vs higher-threshold models (threshold > 1 word,
  or nonzero-alpha-only, or per-page-quorum: cleared-or-partial).
- Discriminator (G36 §4): place a SMALLER tag with the SAME
  raw-access legs as G36 (raw `begin_host_vram_access` + CPU copy,
  no flush/wait/commit, never closed). Task 1 MUST table the
  candidate thresholds (single word 0 of one page? single page?
  sparse subset? nonzero-alpha-only variant?) and pick EXACTLY ONE,
  with per-threshold pre-registered predictions (temp FNV/nz, B
  FNV/nz/head, ladder FNV/nz, scanout sha + white-set census —
  every scalar named before the run, derived from the predictor,
  not guessed).
- Gated G36 pins (re-verify every one; committed REPORT scalars are
  the authority, surviving `/tmp` oracles cross-check only): full
  112-word tag TB `c039a1d1c29cb293`/150057; cleared
  `aa2fa32572450383`/229376 + black `99418f1b…`; full-tag model
  `86ad7b887e140383`/229712 + PPM `d19e6beb…`; G36 binary
  265,853,160 B `4e68911d…` (NOTE: corrupt at gate time — 15th
  read artifact; re-pin with 2+ separated reads; table + stop ONLY
  on a validity-CHAIN mismatch, and the G36 binary is NOT in G37's
  chain); dump `154d9d85…`; clone HEAD `3a66c19…` with G36 ×3 /
  G35 ×0 / G34 ×0 / G33 ×0.
- Drive state (gated): link corruption is PATTERN-DEPENDENT (same
  offset, different bytes by read pattern). Standing rule:
  lane-critical SSD bytes need 2+ matching reads separated in time
  AND independent corroboration (on-device shas, pinned committed
  bytes) wherever available; single-read SSD evidence is suspect.
  Mirror the new `ps2x-g37/` receipts to the share tier.
- Task 1 (no device until §2): STATIC design — table the threshold
  candidates + the ONE picked shrunken tag (which words? what RGB?
  why that threshold splits "ANY" from the field); the premise rows
  (P1 writeback receipt MUST-hold on the shrunken temp; P2
  B-at-sample-time B1/B2/B-neither split with pre-registered verdict
  meanings; P3 controls); the reformulated per-threshold predictions
  (every scalar pre-registered via the predictor); the predictor
  reuse (re-derive every scalar from the dump as a self-check); the
  decision matrix (exact-render vs cleared vs partial, with what
  partial patterns mean — per-pixel triage rules BEFORE the run).
  Name the ONE hunk.
- Task 2 (ONE run max): the wall — ONE hunk max (G36's block
  excised + the shrunken-tag raw-access placement inserted in the
  same edit), ONE build max (NEW dir, G22 recipe, `-j2`), full
  identity, verify-then-push with NO gap (push-time + post-run +
  report-time re-shas), TWO bounded runs max (diagnostic + one
  retry iff O1/O3 fires instead of first draw).
- Success bars (tabled): design table (candidates + picked tag +
  premise rows + predictions + matrix); the threshold VERDICT (with
  the discriminating bytes); if a run: exit/wall/fate + receipts +
  tombstone triage (if any) + scanouts scored AND pulled by explicit
  filename list with cleanup as its own verified step (G31-E1 rule);
  the ONE next action (adoption stays queued per the standing rule
  until the orchestrator gates it; filing stays held).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g37/` transient
  dir ONLY, removed at end (`mg/` only). No P-lane lease, no
  bytesize/WSL. `COPYFILE_DISABLE=1` on all SSD steps.
- SSD clone: ONE hunk max in `gs/gs_interface.cpp` (G36 excised +
  G37 inserted, single edit); G22 + G26 + G28 + G29 + G30 + G31
  hunks untouched (HUNK_MATCH re-verified pre + post + pre-push +
  post-run + report); zero commits in submodule, zero in ps2xGS.
- Byte caps: NEW SSD build dir ≤ 6 GB; NEW SSD `ps2x-g37/`
  (retrieval by explicit list) ≤ 50 MB; all older SSD dirs 0 growth
  (pre + post `du -sk` snapshots must match exactly); internal
  volume ≤ 1 GB delta, no clones/builds; committed text only.
- Knobs: `--disable-sampler-feedback` SET, `PGS_SKIP_SAMPLER_FEEDBACK`
  UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT, sanitizer env ABSENT —
  the ONLY delta vs G36's run is the hunk-for-hunk swap.
- Stop: any validity-chain pin mismatch → table + stop (the G37
  binary and dump ARE the chain — G28–G36 binaries are
  corroboration only); no tuning loop, no second shape, no lldb
  unless a tombstone needs triage.
- Time box 6 h. Evidence `local/research/G37/` + `[G37]` commit +
  `Orchestrated-By: Muse Code` trailer, `git add -f`, NO push
  (orchestrator pushes at poll).
