# T16 — E1 longer boot: does the 31-drain break by ~7200 s (live monitor)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T15/REPORT.md` first (all of it —
T15's post-exit window: what the drain/freeze did past block
241 and at what wall/logline scale), plus P1 REPORT Part 33
(P1aj's drain-termination analysis: 1 iter/VBLANK fixed point
from iter 2, 13-row live-guard terminator table, E1/E2/E3 spec
with bound math — your trigger watchlist). This run is E1: a
longer boot testing whether the drain breaks. Peer lanes run
concurrently — you hold the P-lane lease for ONE boot.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight). Binary must
  reproduce the T13/T15 Release sha/size or table the deviation
  exactly (rebuild if the tree moved).
- Fixed baselines the run must reproduce before the exit (or
  table the deviation exactly): T15's exit-invariant row
  (block-235 collapse scale, N enters/exits, sema-30 shape,
  modal cycle phase, silent past block 2 — take T15's
  numbers, not T13's, as primary).
- Trigger = any drain break: P1aj §33 terminator table rows
  T1–T13 (T1/T2 end by starvation/loop-exit, T4/T10 latent
  fire, T5–T9 shape breaks) OR any T15 post-exit watch
  (main wake, new stub phase, first new guest event, new
  caller, dma/gif unfreeze, second halt). Cap = 7200 s
  (~430k iters at P1aj's 1 iter/VBLANK bound — still a
  complete result if the drain holds to cap: third absence
  window with a bound).
- T13's monitor led with full coverage — repeat that
  (background the monitor BEFORE the foreground boot).

## Gates and rules

- Lease `T16`: the run-to-trigger ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/t16-waits.log`. Max 1 boot ≤7200 s foreground.
  Builds any time (`-j4`). No `adb`.
- ZERO fork changes (no source edits, no commits, no push).
  Build from a recorded tree state (`git log`, `git status`,
  binary sha in the report).
- Evidence dir `local/research/T16/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T16]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 6 h (cap 7200 s + analysis headroom). Tables, no verdicts.

## Task 1 — run to trigger (or cap)

1. Derive scripts from T15's verbatim (LOG names + cap +
   signature rows only, diff-verified): monitor polls the
   T15 exit invariant PLUS the P1aj T1–T13 terminator rows
   PLUS T15's post-exit watches.
2. Pre-claim checks (T13 §T13-0 verbatim): lease absent,
   `pgrep -x ps2EntryRunner` exit 1, binary sha, ISO + ELF
   sizes, free space, waits-log tails.
3. Claim → monitor first (verify alive + log header) →
   foreground boot → on trigger: 30 s grace → SIGTERM →
   release AT ONCE → trace copy (lease-free) → analysis.
   If the exit invariant FAILS (no block-235 collapse),
   table the deviation exactly and still run to cap.

## Task 2 — drain-break tables (whatever the run shows)

1. Exit-invariant table: T15's row reproduced? (match/no
   vs T15's numbers).
2. Trigger table: first drain break (block, wall, logline,
   iter count, which terminator row fired — or NONE (drain
   holds to cap: bound in iters + wall)).
3. Ladder-extension table: T13 §T13-5 + T15 rungs + this
   run's rung (lines/bytes, blocks, handshakes, N/N2,
   wall-rate shape past the exit).

## Report

`local/research/T16/REPORT.md`: T13-0..T13-7 SHAPE (lease
record, trigger/invariant tables, N/N2, post-trigger state,
baselines-kept rows, ladder, exact commands, gaps). Commit
evidence (`[T16]`, no push), remove the lease if held, stop.
