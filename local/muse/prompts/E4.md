# E4 — First-missing-visible-result locator (inspect first, one bounded capture if needed)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables +
hypothesis + next-action recommendation, no verdicts.** Read first: the
review's "Follow-up at poll 57487ca" first-frame-successor section in
`docs/research/review-2026-09-20-first-frame-and-gs.md` (the branch-outcome
table is your classifier — restated below) + `local/research/E3b/REPORT.md`
(park signature: 403,224 kicks / 399,892 drawing-enabled, black upload) +
K1's P0 (verified black frame) + `runtime/gs/gs_frontend.h`
(`getDebugSnapshot`/`getDebugHistory`) + the backend `SnapshotVram`/
`ReadVram`/`Present` interfaces. The question: **does useful nonblack
content reach the active display buffer, and if so where does it disappear?**
 Guest/GIF → Submit → relevant framebuffer → Present. This is NOT another
0x501420 census (G1 closed, G3 dormant) and NOT an assumed resource-load
wait (that interpretation is retired).

## Facts you start from

- Kick counters tally BEFORE batch completion/Submit (`gs_frontend.cpp`
  vertex-kick path) — they are not pixel-write counters. Draw records
  follow Submit but do not prove surviving pixels.
- History defaults PAUSED and holds 512 entries: any capture must arm it
  EXPLICITLY and account for overwritten entries (a terminal tail is not a
  complete frame). Existing history omits full color/texture contents.
- Phase 1 (no boot): inspect EXISTING receipts for the boundary (E3b/K1
  logs, rows, PNGs, sidecars). If they already locate the missing result,
  NO boot runs — table the finding + the receipts that prove it.
- Phase 2 (ONE bounded capture, only if a missing observation requires it):
  observation-only taps at EXISTING interfaces (history arm, snapshot/
  readback, P0 at a named frame boundary); one complete steady frame,
  expanding to a second field/frame ONLY for display latching; preserve the
  pertinent submitted batch/state, relevant surface contents, and final
  presentation image. No new global tracer, no regen, no backend swap,
  semantics UNCHANGED. Cap all log streams by bytes AND wall time.
- Classify into exactly one branch (frontier table): (a) nonblack in display
  surface but black returned → follow presentation/address/decode/field/PMODE;
  (b) batch submitted, destination gains nothing → minimize batch, inspect
  clip/alpha/depth/mask/texture/CPU-raster vs actual state (clear or correct
  reject ≠ bug); (c) content drawn elsewhere, never selected → follow
  producer buffer/flip contract + display-reg writes; (d) only clears/empty/
  clipped/no-useful-batches → follow reached producer branch or missing
  completion (promote AC1/SIF/CD ONLY if that dependency names it);
  (e) truncated/unalignable observation → repair that measurement gap, no
  semantic fix follows.
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`.
  Plain commits, pushed to the FORK remote only, never upstream. Generated
  runner sources never staged. ONE P-lane mutator: coordinate via the lease,
  never parallel boots.

## Gates and rules

- Capture boot (Phase 2, if needed) ONLY while holding
  `/tmp/ssx3-p-lane-lease` (SHARED P-lane) — poll every 5 min + log waits to
  `$W/P1/run/e4-waits.log` if taken. Max 1 boot ≤600 s (Part 4 budget);
  wall + progress + BYTE caps recorded, abort past any cap. Builds `-j4`
  any time. No `adb`.
- `export COPYFILE_DISABLE=1` on every SSD step. Retain: the capture +
  receipts that prove the branch (compress closed raws; single canonical
  copies).
- Evidence dir `local/research/E4/` (STANDALONE). Commit with `git add -f`,
  prefix `[E4]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (orchestrator pushes at poll when the tree is clean). Fork commits pushed
  to fork remote (table SHAs; none if Phase 1 suffices).
- Experiment contract up front (hypothesis/observable/alternatives/stop +
  which branch each outcome selects). Time box: 6 h.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — inspect existing evidence (no boot)

1. Survey every committed K1/E3b receipt for GS-boundary signal (kicks,
   submits, surfaces, presentation, sidecars). Table what exists vs the
   branch table's needs.
2. Decision table: branch selectable from existing evidence (→ no boot,
   write the finding) vs the ONE missing observation (→ Phase 2 with the
   exact capture plan: taps, arming, boundary, caps).

## Task 2 — bounded capture (only if Task 1 names the gap)

1. Pre-claim checks (T13 §T13-0 verbatim + K1/E3b binary shas): lease absent,
   `pgrep` exit 1, ISO/ELF sizes, free space (SSD + internal `df`), selftest
   green, waits tails, THREE caps recorded. Suite re-green before any boot.
2. Claim → foreground capture → at first binding cap: SIGTERM → release AT
   ONCE → analysis (lease-free).
3. Branch classification table (exactly one of a–e) with the receipts that
   force it + the prescribed next action. If (e): the measurement repair,
   nothing else.

## Report

`local/research/E4/REPORT.md`: contract, Phase-1 survey + decision, capture
record (or why none), branch table, exact commands, gaps. Commit fork first
if touched (pushed fork remote), then evidence (`[E4]`), remove the lease if
held, stop.
