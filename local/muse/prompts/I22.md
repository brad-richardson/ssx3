# I22 — Cause-B feasibility: FFmpeg-for-iOS buildability + stub-completion interface inventory (NO behavior change, NO fork commits)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/I21/REPORT.md` first (all of
it: delivery lands on device — callback/producer/AddBs dispatched, stub
`feed()` entered per the ungated warning — but the FFmpeg-OFF stub
returns `false` and enqueues nothing, so main stays parked at `0x3b1028`
with 0 frames; G2 names this wall and offers two candidate shapes).
Then read `local/research/E18/NEXT-BRIEF.md` (host parser-output is the
E-lane's separate wall — do NOT conflate; the device wall stands on its
own receipts). This brief produces the FACTS a Cause-B decision needs. It
implements nothing, changes no behavior, and makes zero fork commits.

## Facts you start from

- BASE: fork `3adc0478` (E18 BEHAVIOR, PUSHED — verify before
  building; the E-line may have advanced — table the HEAD you find
  and PIN this brief to `3adc0478` regardless, with cause: the I21
  receipts this brief builds on were taken there).
- Question 1 — BUILDABILITY: can FFmpeg (or the smallest sufficient
  subset: an MPEG-1/2 video decoder) be built for iOS arm64 in this
  repo's toolchain and linked into the app? Time-boxed SPIKE (2 h
  max): fetch/configure/build the minimal decoder for the iOS target
  OUTSIDE the app tree (scratch dir, capped); table what builds,
  what fails (with the exact errors), licenses touched, and size
  impact. NO integration into the app (that's the follow-up's call).
- Question 2 — STUB INTERFACE: inventory what a stub-frame completion
  would need WITHOUT implementing it: the exact `MPEG.cpp` stub
  entry points (`:447-469`, `:1107-1114` retry path), what
  `getMpegPicture`'s wait condition requires (`decodedFrames` /
  `sawEof` / `streamEnded` / `decoderFailed`), what a
  completion-shaped change would touch (files/functions), and what
  test vectors would prove it (authored frames? E18's first64?).
  Static + the I21 probe's receipts only — no code changes.
- Question 3 — HOST PARITY: what does the host's FFmpeg-ON path
  produce that the device would need to match (parsed/packets/frames
  counters, frame shapes)? Read E18's receipts; table the parity
  checklist a device decoder must satisfy. No host runs (read-only
  inspection of committed E18 evidence; the P-lane lease is NOT
  yours this brief).
- Success bars (tabled): buildability verdict (builds / blocked-with-
  errors / partial — with logs); stub-interface inventory (entries,
  wait-condition terms, touch set, test vectors); parity checklist;
  a RECOMMENDATION row (FFmpeg-build vs stub-completion vs hybrid —
  with the cost/risk receipts for each, decision left to the
  follow-up brief, not this one).

## Gates and rules

- Device: iPad via explicit `--timeout` devicectl ONLY for
  read-only pre-state (reachability, installed apps, running PIDs —
  user sessions reported, never killed). NO rebuild, NO reinstall,
  NO probe this brief (the I21 build stays installed, untouched).
  No PS2 boots → no lease; no `adb`.
- NEVER prompt/steer the E-lane pane (read-only inspection only;
  remote never moved by you). ZERO fork commits (read-only worktree
  at most; the spike lives OUTSIDE any repo).
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared up
  front + tracked (spike scratch ≤10 GB in a NEW SSD lane dir,
  removed at end unless the follow-up needs it — table that).
- Evidence dir `local/research/I22/` (STANDALONE). Commit with `git add -f`,
  prefix `[I22]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — buildability spike (2 h cap) + stub inventory

1. Time-boxed FFmpeg-subset iOS build spike (scratch dir, capped);
   table builds/fails/errors/licenses/size. Static stub-interface
   inventory (entries, wait terms, touch set, test vectors). Table
   every receipt; the cap (not a result) may end the spike.

## Task 2 — parity checklist + recommendation

1. Host-parity checklist from committed E18 evidence (read-only).
   Recommendation row (shapes + costs/risks + what the follow-up
   consumes). Handoff: the exact question the Cause-B implementation
   brief answers first.

## Report

`local/research/I22/REPORT.md`: contract, spike log, inventory,
checklist, recommendation, handoff, exact commands, gaps. Commit
evidence (`[I22]`), stop.
