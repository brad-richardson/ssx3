# I2 — iOS fork fixes: BUNDLE DESTINATION + FFmpeg default + sse2neon guard, then configure

You are muse in a herdr panel, working directory
`/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` (fork, branch `ssx3`,
remote `fork`). This brief leaves spike mode: small CMake-only fixes in
the fork for I1's config-class gaps, then a configure (+ build, if it
gets that far). **Tables, no verdicts.** Read
`/Users/bradrichardson/dev/ssx3/local/research/I1/REPORT.md` first (all
of it: gap rows G4/G5/G6, the toolchain v1/v2 files under
`local/research/I1/logs/`, the disk constraint). Rules: lease `I2` is
NOT needed (no emulator boots here — CMake only); no `adb`; one commit
per fix with the two P-series trailers; push rule: `git push` ONLY
inside this fork clone to the `fork` remote, NEVER in ssx3. Disk:
internal disk is tight (42 GB free at I1) — heavy build dirs go on
`/Volumes/Extreme SSD/` (reuse I1's `_deps` via `FETCHCONTENT_SOURCE_DIR_*`
where possible, do NOT re-clone raylib); never touch `/tmp/p1-link`,
`/tmp/ps2xgs-build*`, or other agents' dirs. Simulator: use ONLY the
installed iOS 27.0 runtime + existing devices; NEVER download runtimes
or platforms. Time box 4 hours.

## Facts you start from

I1 (read-only) failed configure 4 ways and named 7 gaps: G4 (fork
`ps2xRuntime/CMakeLists.txt:592` `install()` lacks BUNDLE DESTINATION —
the single gate in front of any build), G6 (host macOS FFmpeg dylibs
accepted for the iOS target — mirror Android's `PS2X_ENABLE_FFMPEG=OFF`
default), G5 (sse2neon block silently skipped when
`CMAKE_SYSTEM_PROCESSOR` is unset — toolchain v2 works around it; harden
the condition), G1/G2/G3 (pinned raylib 5.5 has no iOS backend — OUT OF
SCOPE: do not touch raylib, do not upgrade pins), G7 (touch — out of
scope). Fork HEAD `e73e36a`.

## Step 1 — three CMake fixes (one commit each, build any time)

a. **G4:** add the missing BUNDLE DESTINATION (Apple-only stanza,
   desktop builds byte-identical — prove with a desktop configure
   diff or a no-op justification + macOS build still green).
b. **G6:** default `PS2X_ENABLE_FFMPEG=OFF` when targeting iOS
   (same pattern as the Android default; desktop unchanged).
c. **G5:** harden the sse2neon condition so an unset
   `CMAKE_SYSTEM_PROCESSOR` with an ARM toolchain still fetches
   (or errors loudly instead of silently skipping).
Receipts: three diffs, three commits, `fork ssx3` pushed; desktop
`ps2xTest` still 424/425 (the 1 pre-existing failure — re-confirm).

## Step 2 — configure (+ build if it passes)

I1's toolchain v2, runtime-only flags, build dir on the SSD. Receipts:
configure pass/fail with the FIRST error (expect the raylib wall:
G1/G2/G3 — record exactly where it stops, do NOT fix raylib); if
configure passes, attempt the runtime build and record the first
failure the same way. No Simulator launch in this brief (that needs a
built binary + a backend decision that is not yours to make).

## Step 3 — report

`local/research/I2/REPORT.md` in `/Users/bradrichardson/dev/ssx3`:
per-fix diffs + desktop-proof table, configure/build receipts with
first-failure records, the raylib-wall statement + the exact next
decision (upgrade raylib / fork raylib / SDL switch — options only,
no verdict), exact commands, receipt paths, "What I could not do".
Commit with `git add -f`, prefix `[I2]`, trailers; stop.
