# E51 — Race terrain: untextured and shard-like after the camera fix

You are the E50 worker (same pane and context). Follow `~/dev/AGENTS.md`.
**Tables + receipts; recommend, the orchestrator decides.**

## State

- The FPU fix `eac6cba` is pushed to fork `ssx3`, and its regen is now the
  **canonical `~/dev/ssx3-work/codegen-ssx3`**. The orchestrator renamed
  `codegen-ssx3-e50` to that path, so reconfigure `E50/build`'s
  `PS2X_GAME_CODEGEN_DIR` accordingly. `codegen-ssx3-e49` still exists.
- The race now draws sky, sun, fog, the rider and a terrain surface. But
  the terrain is dark and mostly untextured, with light-blue shard
  fragments (`local/research/E50/frames/e50f-race-tick8275.png` vs
  `T65/t65-shot-t65a-race.png`).
- Symptoms:
  - zero-area prims ~2.3k/vsync (PCSX2 max 461);
  - off-screen and straddle counts below PCSX2's minimum;
  - `vu_exhausted` 1–2 on most drawing vsyncs (0 before the fix);
  - startPCs missing vs T65: `0x741`, `0x2`, `0x8`, `0xe0`, `0x28c`; extra
    `0x189`.

## Work (≤3 boots, 5 h, cap 4 GB; host etiquette as before)

1. **Match the moment.** Get the recomp race to about 00:00:18 on the
   route (the timer is in the HUD). Use E4 head/tail plus the gfx window
   there, so the tables compare with T65's window and with T65's race GS
   dump
   (`/home/brad/pcsx2-g7/SSX 3_SLUS-20772_20260923162250.gs.zst`, 8
   vsyncs from 26784). Decode the dump's terrain draws if a reader
   exists; T48/G13 tooling may have one.
2. **The VU1 budget exits.** Run `PS2X_VU1_TRACE` over that window and
   hand back which startPC exhausts, and at what TPC it loops. Is it a
   program PCSX2 runs to its E-bit? T65 has cycles per start.
3. **The terrain draws** (0x44e / 0x459 and friends):
   - TEX0 (TBP/TBW/PSM/CLUT CBP/CSA) and TEX1;
   - share of zero-area prims per program;
   - whether each draw's texture page was uploaded (PATH2/PATH3 IMAGE
     census for those TBPs) versus PCSX2.

   Is the problem missing uploads, wrong TEX0 values, or degenerate
   geometry (for example UV/ST or Q values: NaN, 0, huge)?
4. **Any VU0-macro/FPU op** implicated along the way: write a unit test
   against PCSX2's semantics. E52 is auditing all FPU/COP2 ops in
   parallel, so coordinate through the orchestrator, not directly.

Diagnosis first. You may apply **one** fix if a single mechanism is
proven by a failing unit test, validated by one race boot (frame plus
counts).

## Deliverable

`local/research/E51/REPORT.md` + frames. `[E51]` commit (`git add -f
local/research/E51 local/muse/prompts/E51.md`, `git log -1` first,
trailer `Orchestrated-By: Claude Code`). A fix goes on fork `ssx3` and is
not pushed until the orchestrator gates it.
