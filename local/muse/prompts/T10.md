# T10 — Outer-driver search: the loop above depth-0 376938 issuing rep-bearing frames (read-only, no boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T8/REPORT.md` first (all of it —
T8 §T8-7 step 3a's missing input is this brief: "a loop above
depth-0 `376938` with a readable bound (unidentified; the outer
dispatcher sequence is the search area)"), plus T8 §T8-8 (the
non-rep depth-0 census: 78,709 frames in 5 kid-signatures, three
classes at exactly 13,107) and §T8-10 (continuation pairing not
established — that pairing IS this brief's Task 1). Peer lanes run
concurrently — read-only on the fork tree (never stage/touch/
commit anything; you make zero changes).

## Facts you start from

- Receipt (read-only): `$W/P1/run/ps2_log-p1ag-1.txt` (1.2 GB,
  35.9M lines). `W=/Volumes/Extreme SSD/ps2recomp-spike`,
  `R=$W/PS2Recomp` (fork, branch `ssx3`).
- Fixed structure (T8-verified, reproduce the top rows or table
  the mismatch and stop): 13,108 rep-bearing depth-0 `376938`
  frames (kids `37D090`,`363490`, prev depth-0 enter
  `395288`, next `38F300`); FRR ×13,108 exact; 78,709
  non-rep depth-0 frames in signatures (`64B88`,)×26268 /
  ()×13120 / (`423DE0`…3)×13107 / (7-kid)×13107 /
  (`361F90`,)×13107.
- The question: which outer sequence issues the 13,108
  rep-bearing frames — a loop with a readable bound (3a's
  prize), a counted dispatch chain (the 13,107-classes hint at
  1:1 pairing with reps-minus-1), or neither (→ 3c)?

## Gates and rules

- NO lease (no boots at all). ZERO fork changes (no source
  edits, no commits, no push). No `adb`. Read-only on the ISO,
  the receipts, and every other agent's dirs.
- Evidence dir `local/research/T10/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T10]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — continuation pairing (chain the dispatcher order)

Single streaming pass over depth-0 enters in dispatcher order:

1. Pair each rep-bearing frame with its neighbors: prev/next
   depth-0 frame kinds (T8: prev `395288`, next `38F300` —
   confirm + table the FULL neighbor-kind census, not just
   the mode).
2. Pair the 13,107-class frames to logical rep calls: does each
   rep call own exactly one (`423DE0`…3) + one (7-kid) + one
   (`361F90`,) frame (the reps−1 counts suggest a per-call
   chain with boundary effects — table the pairing or its
   absence with exact offsets)?
3. Empty frames (13,120 = reps+12) + (`64B88`,) frames (26,268
   = 2×13,134): position in the chain (periodic? paired?
   scattered?) — tabled, not explained.

## Task 2 — the issuing loop (disassemble the winner)

1. If Task 1 finds a periodic per-call chain: locate the guest
   loop issuing it (disassemble the dispatcher/owner function
   around the chain's pcs — same method as T7/T8: JAL
   inventory + back-edge targets + ELF words from
   `$W/P1/SLUS_207.72`, offset = vaddr − `0xFF000`) and table
   index/bound operands if readable (3a's prize).
2. If no loop (straight-line dispatch / counter-driven /
   data-driven): table the examined-and-absent (where looked,
   what reading kills each candidate) + the cheapest next
   receipt (counter probe? run-to-exit 3c? — name it, don't
   build it).
3. Either way: update T8's step-3 table as a TABLE (3a/3b/3c
   with the new inputs each now has — numbers only).

## Report

`local/research/T10/REPORT.md`: pairing tables + chain
structure + loop disassembly (or examined-and-absent) +
updated step-3 table + gap rows. Commit evidence (`[T10]`,
no push), stop.
