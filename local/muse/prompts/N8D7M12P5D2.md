# N8D7M12 Part 5D2 — one same-APK Odin OFF replay

You are the N-lane OpenCode Go run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5D1/{REPORT.md,ORCH-GATE.md}` and Part 5B/P5C REPORTs/gates, plus this brief. Own `local/research/N8D7M12P5D2/` and the private scratch directory created by the gated launcher `~/dev/ssx3-work/N8D7M12P5D1/`. No script/APK/stream/source/fork edit, second launch, Mac replay, push, board/global edit or upstream contact. Time box 30 min plus the launcher's ≤600 s active cap; text receipts <512 KiB, device output ≤64 MiB. Write REPORT/checker early, hand back the table, and leave the graphics verdict to the orchestrator.

Goal: test whether tick-2050 selected/oracle/tile measurement flags caused the sparse Odin frame. The ON replay used APK SHA `caa1110297dc413d3b04e7442873e0fb5d5624af8417e1e30537d76184d5f512` and stream SHA `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593`, producing 41 rows and a mostly-black PPM. The OFF script removes exactly those three flags, keeps replay/parallel/Turnip/step50/PPM2050, and has orchestrator-reviewed SHA `223fd15ec0aa45079f44bed7ceffd3e19381d12ac0c51587a0284afb9cd03170`. The first draft SHA `be8e9956…cacd` is not released.

Preflight: verify current script SHA and `check.py --self-check` 30/30, `py_compile`, no existing OFF `result.json`/`driver.log`, exact ON receipt pins still present, device serial `622c49b1`, no other Odin lease or app run, charging battery ≥20%, ≥10 GiB free and `KeyguardServiceDelegate showing=false`. Locked device is a blocker to report immediately, never worked around. No simultaneous host heavy job. The launcher independently double-hashes local APK/stream, installs even if present, double-hashes device APK/stream, leases, prepares unique outputs, rechecks keyguard before start, launches exactly once, caps wall/progress/log/output, sends BACK once, force-stops after the run, checks PID gone, restores prior env with 2+2 SHA reads and releases its lease in `finally`. If a preflight fails, stop without launch and report the first failure.

One released command from repo root: `python3 -u local/research/N8D7M12P5D1/launch.py --released-sha 223fd15ec0aa45079f44bed7ceffd3e19381d12ac0c51587a0284afb9cd03170`. No retry. Keep ON receipts and device stream untouched.

Acceptance: full `GB4_REPLAY_SUMMARY` markers2050, exact ordered 50..2050 step50 replay rows, `GB4_FRAME tick=2050`, `replay ok markers=2050`, PPM/hashes pulled with 2+2 SHA, one install/launch, force-stop/PID absent/env restored/lease free. Compare each OFF `parallel.hashes` row against the pinned ON file `~/dev/ssx3-work/N8D7M12P5A/parallel.hashes` (priv/VRAM/present, first difference), and OFF PPM SHA against ON `39b70d67…` (`~/dev/ssx3-work/N8D7M12P5A/vq-002050.ppm`); list absolute OFF PPM path/SHA for orchestrator viewing. If any row at ticks ≤2000 differs, mark the ON/OFF test void/nondeterministic or harness fault. If rows match through2000 and tick2050/PPM differs, report possible measurement effect. If all rows and PPM match, report persistence without measurement flags. These are evidence rows, not a root-cause verdict. No OFF selected tile census is expected. Diagnostic elapsed time is never a speed number.

Deliver `REPORT.md`, a narrow receipt checker and bounded text receipts with exact commands, pins, counts, comparison table, first failure and cleanup. Commit `[N8D7M12] Part 5D2` with `Orchestrated-By: opencode` using **explicit commit paths**; do not sweep unrelated staged files. No push. Hand back the table.
