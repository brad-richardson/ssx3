# I30 — iOS build with the fixed music, sound on (fork `ssx3` `d711506`)

You are the I-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief and `local/research/I29/{REPORT.md,build-install.sh}` (reuse its script and the persistent `~/dev/ssx3-work/ios-deps/`). Own `local/research/I30/` and scratch `~/dev/ssx3-work/I30/`. No fork edits, no push.

**Why.** I29's iPhone build is silent by default and predates AU8's fix (the tag-1 music PCM is planar; fork `ssx3` `d711506` reads it correctly and matches PCSX2's real output). Brad wants to listen on his phone.

**Changes vs I29 (only these):** fork pin `d711506` (worktree `~/dev/ssx3-work/I30/PS2Recomp`, runner-dir check empty); the bundled env = I29's `ps2x.env` plus `PS2X_SOUND=1` (write it to `local/research/I30/ps2x.env`, record both SHAs; don't edit `local/research/I26/`). Everything else as I29 (flags `-O3 -DNDEBUG`, diagnostics OFF, canonical codegen, E32 inputs, same identity/profile).

**Runs.** Simulator: one I26-FAST run to the race with **no launch-env override** for sound (the bundled env must turn it on); report the `[snd-output] stream rate=36000` line and underrun counts; view title/menu/race frames. iPad (`00008112-001224302184A01E`): install and one run to the race, same check. **iPhone: sign and install only; never launch, test or screenshot it.**

**Deliverable.** `local/research/I30/REPORT.md` (pins, env diff, Simulator + iPad results with frame paths under scratch, iPhone install receipt, gaps). Text receipts only; no PNGs in git. Commit `[I30] …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. Budget 60 min, 2 app builds, 1 Simulator run, 1 iPad run.
