# E55D14 Part 2B — one empty-card Load game path run

You are the E-lane OpenCode Go run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/E55D14P2A/{REPORT.md,ORCH-GATE.md}`, `local/research/E55D12/{REPORT.md,ORCH-GATE-P2.md}`, and this brief. Own only `local/research/E55D14P2B/` plus private `~/dev/ssx3-work/E55D14P2/run/S1` created by the gated script. Do not edit scripts, fork, cards, other lanes, board/ledger, or push. No upstream contact. Time box 30 min, one boot, text receipts <512 KiB, scratch ≤2 GiB. Cite exact receipts and hand back the table; do not make the orchestrator verdict.

Goal: learn the raw memory-card query the stock Load game menu requests, before considering any seeded card. The E55D12 empty-card run viewed the Load game result and saw a fifth GetDir after the choice, but its probe omitted the path. E55D14 Part 1B added a default-off `getdirpath` sibling. Part 2A prepared an exact-route script, SHA-256 `fa9444a9f80c89dfa2b1e8911d8870ddca87397db083e318f678ea4dc9285694`, accepted for **one** Mac diagnostic boot. Release applies to that exact script only.

Preflight: verify `git log -1`, script SHA, its 38/38 self-check, fork HEAD `80777cb36d5f84c1cd89122e8074afd48fe78b8e`, clean tracked fork with only `.work/` untracked, runner SHA `d8fa114d824a277592558425dd91357bcf2f75d0d09390a5680c41ba6002ef04`, and `git diff --stat 14b1e5cb 80777cb -- ps2xRuntime/src/runner` empty. Run `local/tooling/disk_budget.sh` and ensure room below 200 GiB; check `python3 local/tooling/p_lane_lease.py status`. Script itself double-hashes runner/ISO/ELF/codegen before boot, claims one mini slot, checks fresh empty-card lane, and releases lease/PID in `finally`. Do not compete with another heavy Mac job. If any preflight fails, stop without boot and report it.

Then run once, from repo root: `python3 local/research/E55D14P2A/e55d14_boot.py --label S1`. No manual retry or second boot. Existing script caps active wall at 500 s plus at most 100 s proof grace; probe 16 MiB, frames 2 GiB; stop sooner on its target or other cap. Capture command/stdout/stderr, `result.json`, bounded closed log/probe, hashes and file sizes. Preserve the exact nine input pulses and empty cards. Do not seed or copy a card.

Acceptance table, decided from receipts: exact pad route and viewed candidate pre/post frames; five `getdir` status rows (four early, one post-choice) with five `getdirpath` siblings paired by per-family ordinal/tick/port/slot/max, **not** global seq; post-choice path row fields `raw`, `query`, `parent`, `pattern`, `host`; no table bytes or `mcread`. A: same Load game scene and all path fields observed on empty card. B: path observed but scene/status differs (void for seed design). OTHER: no path, cap, input, frame-proof or preflight failure. Do not infer a valid save, deterministic card behavior, or speed from a diagnostic boot. Never copy raw game data into git receipts; text probe metadata only.

Write `local/research/E55D14P2B/REPORT.md` with the table, precise commands, pins, caps, output paths, first failure and gaps. Add a narrow checker for receipt counts/pairing and caps; keep original private frames for orchestrator viewing and give their absolute paths plus SHA in REPORT. Commit `[E55D14] Part 2B` with `Orchestrated-By: opencode` using explicit paths, no push. Hand back the table.
