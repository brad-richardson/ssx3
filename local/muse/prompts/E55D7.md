# E55D7 — save-manager ingress and route evidence (OpenCode Go)

You are the E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55D6/{REPORT.md,ORCH-GATE.md}`, `local/research/E55D3/ORCH-GATE.md`, `local/research/I26/ROUTES.md`, and the game's EE lookup tools under `local/tooling/ee/`. Write only `local/research/E55D7/` and this brief. **Read-only source/receipt survey**: no build, boot, device, card mutation, fork/source edit, push or status/todo/ledger edit.

Goal: find a supported menu/input-to-save-manager path that could invoke `sceMcGetDir` or `sceMcRead`. E55D6 mapped those callees and the `sub_002C5570` cluster, but its seeded-title and save-menu triggers have no proven route/tick. `BASLUS-20772` and save-menu strings establish names only. Do not infer a call edge from a string name or from the I26 empty-card setup rule.

Work backward from `sub_002C5570`, `sub_002C4210`, `sub_002C42B0`, `sub_002C6848`, `sub_002C6B78` with `ee-xref`, `ee-at`, `ee-func` and the pinned ELF/codegen. Work forward from at most two save-menu or title/login handlers identifiable by concrete string xrefs or dispatch tables. For each proposed edge, record caller PC, callee PC, the instruction or table entry, and source/ELF receipt. Mark indirect, callback or HLE jumps **unknown** unless the target is resolved by data. Use LSP for host code links if useful; validate key game links with the EE tools. Keep the search bounded to the relevant 0x2C cluster and at most two upstream UI layers; no exhaustive graph dump.

Deliver one table with at most two candidates: known call chain, missing edge, candidate UI screen/input, current I26-FAST tick if evidenced (else unknown), and exact next observation that distinguishes it from GetInfo-only or a write-only path. If a complete route cannot be proved statically, give a concrete one-boot empty-card probe design: starting menu and button sequence *only if supported by existing route/screenshots*, E55D3 probe flags, first-card-record stop, tick/wall/log caps, and what result would be OTHER. Do not invent a route to fill the table. A call that yields a status-only GetDir/Read record proves reachability; successful guest bytes need separate evidence. Card-file perturbation remains out of scope until this path is proven and a new A/A baseline exists.

Budget <20 min read-only work, new receipts <512 KiB. Cite every decisive claim to exact file:line, `ee-xref`/`ee-at` command output in a named bounded receipt, or existing report. Deliver `local/research/E55D7/REPORT.md` plus bounded evidence text; commit `[E55D7]` with `Orchestrated-By: opencode`, no push. Hand back the table and recommended next action; **the orchestrator judges the gate**.
