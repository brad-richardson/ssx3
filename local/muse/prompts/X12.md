# X12 — sparse Qwen exact rendering-path extraction

You are a local opencode worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, and `local/AGENTS.local.md`. This is an X-track model trial that supplies a source table for the N8C2 rendering diagnosis. Cite the repo, not the web. Write `local/research/X12/rows.tsv` early. Do not edit code, run builds, boot a runner, use the Odin, or decide a cause/fix.

Pin the N8B1 fork worktree at `17e90ded3689685ad359b76a9168c80a1f752e2d`; if different, stop and report. Read only these source files in that worktree: `ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`, `ps2xRuntime/src/lib/gs/gs_frontend.cpp`, and `ps2xRuntime/src/lib/ps2_runtime.cpp`. Use `rg -n` then read small line windows. Confirm the `Present` → `latchHostPresentationFrame` → `copyLatchedHostPresentationFrame` → `UpdateTexture` connection with `lsp` if it returns a result; report an empty result plainly. No other search. The source line itself is the lookup table: do not paraphrase it or infer an image defect.

Extract exactly seven source rows, IDs and target snippets below, into UTF-8 TSV columns `id`, `file`, `line`, `source` (one header, seven data rows). `file` is the path relative to that worktree; `line` is a decimal line number; `source` is the *entire exact source line, stripped only of leading/trailing whitespace*. Quote TSV fields with tabs if needed. Each target must occur on a line in its named file; if absent, write `NOT_FOUND` in `source` with line `0` and state the gap in `REPORT.md`.

| ID | File | Target text |
| --- | --- | --- |
| `shot_width` | `ps2_gs_parallel_backend.cpp` | `shot.image->get_width()` |
| `buffer_bytes` | `ps2_gs_parallel_backend.cpp` | `info.size =` |
| `copy_extent` | `ps2_gs_parallel_backend.cpp` | `cmd->copy_image_to_buffer(` |
| `host_map` | `ps2_gs_parallel_backend.cpp` | `m_device->map_host_buffer(` |
| `backend_stride` | `ps2_gs_parallel_backend.cpp` | `kStride = 640u` |
| `frontend_stride` | `gs_frontend.cpp` | `sourceRowBytes =` |
| `texture_upload` | `ps2_runtime.cpp` | `UpdateTexture(tex, s_uploadBuffer.data())` |

Run `python3 local/tooling/orch/check_x12.py local/research/X12/rows.tsv`. Acceptance is 7/7 exact-line rows, no extras, correct pin. Time cap 5 minutes. `REPORT.md` gives exact command, checker output, elapsed time, tool calls, any LSP gap, and no cause/verdict. Commit only report and TSV with `[X12]` and `Orchestrated-By: opencode`; no push. Hand back the table; the orchestrator decides.
