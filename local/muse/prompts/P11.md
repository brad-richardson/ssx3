# P11 — SIF poll-expectation fix + P10 ladder confirmation boots

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P10/REPORT.md` §P10-2c (the stale
`:949` expectation), §P10-4 (why both P10 boots were invalid), and
§P10-7 items 1–2 (the recovery recipe + the one-liner) first. This brief
closes P10's two carried items: the red SIF test and the unproven
ladder. P1ab/P9 run concurrently in other panes — you share only the
fork remote.

## Facts you start from

P10's fix (`69bb1ff`, miss → `KE_ERROR`) is pushed and unit-proven, but
(1) `ps2_sif_rpc_tests.cpp:949` still expects the old `-419` (suite
428/427/1), and (2) both P10 boots burned on a game-less binary (438 B
committed stub `register_functions.cpp` + default-OFF log opts), so no
ladder table exists. Baseline: P25-boot1 (`boot-p1aa-1.log`) — UNLESS
P9's split commit is on `fork/ssx3` when you boot, in which case the
baseline is the post-split ladder (state which baseline you used and
why; re-derive its key rungs, don't trust memory).

## Gates and rules

- Lease `P11`: boots ONLY while holding `/tmp/ssx3-host-lease` — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/p11-waits.log`. Builds + recompiles any time (`-j4`). Max
  2 boots, ≤90 s foreground each. No `adb`.
- Fork: the SIF test file ONLY (`ps2_sif_rpc_tests.cpp` — record the
  exact path). `git add` that NAMED file only — never `-A`, never
  stage or touch anything else (especially never the generated
  runner). Verify the diff before committing. One commit with the
  `Orchestrated-By: Muse Code` trailer. `git pull --rebase` before
  pushing; `git push` ONLY inside the fork clone to the `fork` remote.
  Stop on any foreign rebase conflict. Never commit generated sources
  or `._*`.
- Evidence dir `local/research/P11/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P11]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **SIF one-liner.** `:949`: expect `KE_ERROR` (retitle the message
   to match the kernel-miss spirit). Rebuild, full suite: must go
   all-green (state the totals — 428/428/0 unless a peer added tests,
   in which case name the new count + face if any).
2. **Boot binary, with the check P10 missed.** Rebuild the runner with
   `PS2X_ENABLE_RUNTIME_LOGS=ON` + `PS2X_ENABLE_AGRESSIVE_LOGS=ON`
   (verify in CMakeCache) and a game image regenerated from the
   CURRENT tree's CSV (recomp receipt: entry counts). **Receipt
   before booting:** `register_functions.cpp` size (~32 MB, not 438
   B) + `0x100008` ref count (8, not 0). Do NOT boot until both hold.
3. **Max 2 boots.** Receipts: boot reaches the `0x52BE04` stall
   steady state (33 creates, 0 `-1` waits, balanced F6/F7 handshake
   — F6's drain exits identically per P10-2b, confirm the exact
   line — same watch line, same `[drop]` census shape). One ladder
   table vs the stated baseline.
4. **Report.** `local/research/P11/REPORT.md`: SIF diff + suite
   receipts, binary-proof receipts (size + refs + log opts),
   ladder table, exact commands, receipt paths, "What I could not
   do". Commit, remove the lease if held, stop.
