# I23 — Cause-B implementation, shape A: integrate minimal FFmpeg 7.1.1 for iOS (owner-authorized)

You are the ssx3 I-lane worker in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** The owner has AUTHORIZED shape A (FFmpeg-build)
with FFmpeg pinned at 7.1.x both sides (I22's handoff Q1, answered).
Read `local/research/I22/REPORT.md` first (all of it: spike recipe +
archives + stub inventory S1–S4/W1–W5/T1–T8 + parity P1–P10 + V1–V5).
This brief implements the integration I22 proved feasible.

## Authorized decision (do not re-litigate)

- Shape A: build the minimal 7.1.1 subset for iOS (I22's recipe), wire
  the 3 archives into CMake for the iOS target, flip
  `PS2X_ENABLE_FFMPEG` ON for device.
- Pin 7.1.x BOTH sides: device integrates 7.1.1 now; host parity runs
  against a locally-built 7.1.1 subset (I22 §sufficiency recipe) — the
  shared host/brew/CI pin is NOT yours (table the exact change for
  E-lane concurrence, do not touch shared config).
- LGPL-2.1 static-link obligations accepted by the owner (I22 §licenses
  is fact, not advice). Ship the license notice + table the relink
  provision artifact; no legal interpretation beyond I22's row.

## Mission

1. Integrate: CMake iOS branch for 3 IMPORTED libs + include dir (≈40
   lines by the WIN32-block template `ps2xRuntime/CMakeLists.txt:282-376`
   @`3adc0478`); `PS2X_ENABLE_FFMPEG=ON` for the iOS target only.
   Fork branch `i23-ffmpeg-ios` (NEW branch, push branch, no force-push;
   NEVER touch `fork/ssx3` — E-lane owns it).
2. Build the iOS app (I21's build shape). Table build exits/errors/size
   delta (expect ≈+1.2 MB).
3. Install + probe on Brad's iPad (devicectl with explicit `--timeout`;
   overwrite-install over the I21 build is precedented 11×; diff
   pre/post state against I22's baseline: PID 4679, steady-state +
   census). Run the V1 device vector (`regression-frames.m2v`): the
   device MUST reproduce P1 (parsed240/packets3+1/frames2+2) and P2
   (FRAME served, resume-once, guest image nonzero).
4. Acceptance: parity checklist P1–P10 (I22 §Task 2) row by row —
   table PASS/FAIL per row with receipts. P4 is correctly NOT a
   match-target (device must produce frames, not reproduce the hold).
   P10 byte-exactness is expected via the 7.1.x pin — measure it.
5. Gaps to table (not necessarily close): V3 (full 5040 B — needs E-lane
   capture; request it, don't block on it), V4 (larger frames —
   author if cheap, else table), the 1/7 host SIGSEGV (re-observe only
   if the harness is reused), AGRESSIVE-gated `[MPEG:*]` taps (enable
   observation if the build change allows it).

## Rules

- Fork: branch `i23-ffmpeg-ios` ONLY; zero commits to `ssx3`; E-lane
  pane never prompted/steered (V3 request goes through the
  orchestrator — table it).
- Device: iPad via explicit `--timeout` devicectl; user sessions
  reported, never killed. Rebuild/reinstall/probe AUTHORIZED this
  brief (unlike I22).
- No PS2 boots → no lease; no `adb`.
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared up front
  + tracked (reuse `W/libs-ios/` + tarball; new scratch ≤2 GB in NEW
  `ps2x-i23/` paths; 0 growth elsewhere).
- Evidence dir `local/research/I23/` (STANDALONE). Commit with `git add
  -f`, prefix `[I23]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
