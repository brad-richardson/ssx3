# T17 — G1 pad automation: press ✕ at language-select, capture the next epochs (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T4/REPORT.md` first (all of it —
T4 gap G1 = this brief, taken on orchestrator judgment: the
reference run parks at language-select awaiting ✕; reaching
title/menu/gameplay — let alone the T15 drain region — needs
pad input), plus T4's `samples.txt` (trace format) and
`snap-language-select.jpg` (the park to break). User authorized
the bytesize track 09-20; continue while useful. This is the
follower T4 proposed: map ✕ to a key, single-press run,
screenshot-verify each new screen.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` (key
  auth, verified 09-20). Devel PCSX2 v2.9.75 already built at
  `/home/brad/pcsx2-t4/pcsx2/build/bin/pcsx2-qt` (T4 §T4-1:
  reuse, do NOT rebuild unless the tree moved — table binary
  sha + size at start, must reproduce `6719f5d6…` / 130929856
  B or table the deviation). Inputs staged at
  `/home/brad/pcsx2-t4/inputs/` + `dat/PCSX2/bios/`.
- The display: WSLg dead; Xvfb `:99` + `QT_QPA_PLATFORM=xcb`
  (T4 §T4-0 — reuse; start/stop recorded). Key injection:
  `xdotool` to Xvfb (install if missing), or PCSX2 pad
  mapping + input APIs — table the choice + what works.
- The goal: from a fresh boot, reach language-select (same
  park — table the epoch match vs T4: ExecPS2 lines,
  vblank count at park), press ✕ (single press, recorded
  timing), then capture what follows: screenshot each new
  screen + the continuous `EE.Bios`+`IOP.Bios`+`IOP.cdvd`+
  `MISC.sif` trace (same 4 channels, same ini + G2/G9 notes
  heeded: game printfs still off unless a rerun is free).
  Stop at the first new stable park OR 1200 s wall,
  whichever comes first (still a complete result: table
  epochs reached + inputs sent + trace captured).
- NO lease of any kind (all boots on bytesize). No `adb`.
  No fork changes. No copyrighted downloads (inputs exist).

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce the input run + capture from scratch.
- Evidence dir `local/research/T17/` (STANDALONE). Commit with
  `git add -f`, prefix `[T17]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 6 h. Tables + receipts, no verdicts. If input
  automation proves infeasible (no key path works), table
  exactly what was tried, commit what exists, stop.

## Task 1 — input path (does a key reach the pad?)

1. Path table: candidate key paths (xdotool → Xvfb, pad
   ini mapping, PCSX2 input APIs) with try-order + per-path
   receipt (works? proves how? — keypress visible at the menu?).
2. Park-match table: fresh boot reproduces T4's park? (BIOS
   line, ExecPS2 lines, vblank count at language-select,
   screenshot match — match/no vs T4's numbers).
3. Press table: the ✕ press (method, menu-wall timing,
   single-press proof — one press, one edge).

## Task 2 — next-epoch capture (what follows ✕?)

1. Epoch table: screens/menus reached after ✕ (guest events,
   log times, screenshot per screen — or the press does
   nothing: table that exactly).
2. Trace table: channels captured (same-4 format check vs T4
   samples), lines/bytes, paths + shas both ends (traces
   stay on bytesize if >1 GB, with head/tail samples
   committed).
3. Park table: first new stable park (screen + awaiting-what
   + vblank count — or NONE to 1200 s cap: bound tabled).

## Report

`local/research/T17/REPORT.md`: T4-0..T4-4 SHAPE (input
path, park-match, epochs, trace inventory, exact commands
both ends, gaps — incl. next-input proposal if deeper
epochs remain). Commit evidence (`[T17]`, no push), stop.
