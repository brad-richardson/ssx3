# N8D5 Part 1 — choose a same-stream Odin seam before another device run

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/6. This is **read-only diagnosis design** after `local/research/N8D4/REPORT.md`: one 1.10 GB GS stream from the Odin at tick 2050 replayed on Mac CPU and paraLLEl with matching packet counts/page metadata. Both Mac frames show HUD/slope/meter, while Odin's raw mapped/frontend image at that tick/page is mostly black. `local/research/N8D3/REPORT.md` showed the Odin raw mapped, backend packed and frontend RGBA bytes identical. The defect is at/before mapped scanout; image generation versus transfer/readback is open. `local/research/X13B/REPORT.md` pins the `vsync`→image barrier→copy→submit→wait→host map→row pack source order. Earlier G40/G41 studied Adreno page loss on a different dump; do not carry their verdict across scenes without a same-stream check.

Correct-behavior model: a diagnostic must compare **the same captured stream** and the same tick/page on Odin, and observe the image before or independently of the current `copy_image_to_buffer`/`CachedHost`/`map_host_buffer` path. Alternative A: Turnip/paraLLEl generates a sparse `shot.image`; alternative B: the image is complete but this transfer/readback path loses it; alternative C: a separate live-run integration/timing path differs from a standalone replay. Write predicted outcomes for each candidate measurement under A/B/C. If A and B predict the same observation, call it non-discriminating.

Read only the needed windows of: `local/research/N8D4/REPORT.md`, `local/research/N8D3/REPORT.md`, `local/research/G40/REPORT.md` §§2/3, pinned N8B1 `ps2_gs_parallel_backend.cpp` around `Present`, fork `ddaee78` `ps2_gs_replay_tests.cpp`, and pinned G43 paraLLEl/Granite source for `GSInterface::vsync`, `GSRenderer::vsync`, `tools/gs_stream_replayer.cpp`, and any proposed probe API. Confirm code connections with `lsp` if it yields results; an empty result is a tooling gap, not proof. Cite exact repo path:line for each factual source claim. Do not use the web.

Deliver `## Candidate probes` with a compact table of **at most three** probes: pre-copy vs post-copy observable, concrete source hook/API, same-stream Odin feasibility (existing Android replay binary/target vs new build), expected result under A/B/C, and estimated build/device/byte cost. In `## Format compatibility`, verify whether G43's `gs_stream_replayer` accepts `PS2XGSC1` directly; report parser magic/format evidence. In `## Recommendation`, state **one** probe for a Part-2 brief, with an explicit discriminatory gate and stop rules, but do not call the root cause. If no probe truly separates A/B with current tooling, say so and give the smallest additional mechanism needed.

Time cap 20 minutes, no build, boot, install, lease, source edit, fork push, generated code edit or upstream contact. Write `local/research/N8D5A/REPORT.md` early. Run `python3 local/tooling/orch/check_n8d5a.py` for the format gate; the orchestrator checks each causal claim. Keep new receipts under 1 MiB and commit only the report plus small source-line table as `[N8D5A]`, trailer `Orchestrated-By: Codex`; no push. Hand back the table, do not conclude a device fault.
