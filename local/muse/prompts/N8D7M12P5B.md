# N8D7M12 Part 5B — one gated Odin GS-stream replay

You are the N-lane device worker, OpenCode Go. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M12 Part 4 and Part 5A REPORTs/gates, N8D7M11 §§5–6, and N8D7M6 REPORT. Own this brief, `local/research/N8D7M12P5B/`, and private script scratch `~/dev/ssx3-work/N8D7M12P5A/`; only the reviewed launcher may write that scratch. The existing APK and stream are read-only. This pane has scoped permission for private scratch; test a tiny sentinel there before the run and stop on denial. No source/script/APK/stream edit, second install/launch or retry, device reboot/unlock, iOS action, fork edit, push, board/global edit, upstream contact, or generated game data in git. One Odin agent/lease at a time; no sibling Odin worker is assigned. Time box 25 min; launcher active replay ≤600 s, log ≤16 MiB, PPM/output ≤64 MiB, text receipts ≤512 KiB.

**Release:** run only `local/research/N8D7M12P5A/launch.py` with reviewed SHA-256 `287140bf370acfb105f1b64347a635822300699150e517e6b912340ae031334e`. Recompute its SHA and rerun `check.py --self-check` before launch; inspect that private `result.json` and `driver.log` are absent. Then execute exactly once:

```sh
python3 -u local/research/N8D7M12P5A/launch.py --released-sha 287140bf370acfb105f1b64347a635822300699150e517e6b912340ae031334e
```

The launcher must recheck local APK/stream and packaged members, fresh Odin battery/lock/storage/lease, install the APK, double-hash installed APK and device stream, preserve `ps2x.env`, then launch replay (not live EE). Its stop rule is first full tick2050 summary + numeric selected/oracle census/control + `vq-002050.ppm`, or first failure; it force-stops, restores env, and releases the lease in `finally`. Do not work around a locked Odin; report that blocker immediately. If the command fails or times out, **do not rerun**; collect bounded private receipts, check cleanup state, and hand back the exact first failure. If lease/app/env cleanup failed, report immediately so the orchestrator can recover it. No speed claim: elapsed time is run control only.

Deliver `REPORT.md` and a postrun checker in `local/research/N8D7M12P5B/` using the private `result.json`, `driver.log`, compressed logcat, frame and hash file; quote complete hashes, numeric census (sampled/raw/input/circuit/stage/oracle active tiles), descriptor and control rows, one install/one launch, cap/cleanup state, and exact source/result pins. Keep the full PPM/logcat and APK/stream outside git; commit text only with `[N8D7M12] Part 5B` and `Orchestrated-By: opencode`, explicit paths, no push. A = complete on-device replay receipt and intact cleanup, **provisional until orchestrator views the frame**; B = one runtime mismatch with exact first failing row; OTHER = preflight/permission/resource failure. Hand back the table; do not infer graphics cause, default-off behavior, determinism or speed.
