# X11 — dense Qwen scheduler extraction and LSP probe

You are a local opencode/Qwen **dense (default model)** worker in
`~/dev/ssx3`. Follow `~/dev/AGENTS.md` worker rules. This is a model/tool
trial, not an E55 implementation verdict. Write only
`local/research/X11/REPORT.md`; no source edit, build, boot, device, web,
push, or directory search. Cite the repo excerpt, not the web. The named
paths here count as your started folder for the report write.

The orchestrator made `local/research/X11/source-excerpts.txt` with
`git show 4f93216:ps2xRuntime/src/lib/Kernel/EeScheduler.cpp` and the
exact numbered windows for this task. Read only this brief, the excerpt,
and the report skeleton. Do not read E55B1's report or other fork files.
Do **one** LSP `findReferences` probe for `processDueDeadlines` at the
excerpt's definition in
`~/dev/ssx3-work/E55A2/PS2Recomp/ps2xRuntime/src/lib/Kernel/EeScheduler.cpp`.
Record whether LSP returned usable references and their locations, or
the exact error/gap. The source excerpt, not LSP, is the authority for
the four semantic rows. Do not infer calls from an empty LSP result.

Fill the four `FILL` rows in the skeleton. Distinguish the host-time
gate that selects a due batch from the comparator used **inside** one
batch. For idle choice, state how timer and scheduled event deadlines
are compared and when cycles advance. For `postEvent`, say whether the
queued event receives a guest-cycle timestamp in the shown code. Use
exact excerpt line citations. State what cannot be concluded from these
windows. Do not claim runtime reach or deterministic behavior.

Write a first draft within 3 minutes. Then run
`python3 local/tooling/orch/check_x11.py` and correct at most once.
Commit only the report with explicit path (`git add -f --
local/research/X11/REPORT.md`; `git commit ... --
local/research/X11/REPORT.md`), subject `[X11]`, trailer
`Orchestrated-By: opencode`; no push. Hand back the table and one short
LSP/context note, not an E55 verdict.

Budget: 10 minutes, ≤24k model context, ≤1 MiB report, one LSP probe.
At cap, stop and state the gap. A failure of your own exploratory
command is not a failed brief step; fix syntax once and proceed.
