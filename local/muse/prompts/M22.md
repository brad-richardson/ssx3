# M22 — Interior far-tail mechanism: locate + isolate the |δ|≥8 bytes (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M20/REPORT.md` first (all of it —
M20 gap 1 is this brief: 102 B with |δ|≥8 on s0, max 47, ALL
outside the 701 mask; 134 B on m15), plus M21's Task-1.1 map
machinery (per-shape persistence + Jaccard precedent) for the
cross-shape question below. Fully offline — SSD dumps read-only,
no harness code, no replays, no lease. No device work.

## Facts you start from

- Interior far tail (M20 cell 7): s0 |δ| hist [2127, 127, 119,
  28, 74] — the 102 bytes with |δ|≥8 (28 in 8–15, 74 in 16+,
  max 47) all fall OUTSIDE the shape-701 mask, on a cell whose
  bulk is ±1 luma hugging edges. m15: 134 tail bytes (50 +
  84), max 48. The question: are these a distinct population
  (region? plane? carrier? shape-persistent?) or the smooth
  tail of the ±1 mass?
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m20/m20.txt` (cell-7 δ tables to
  reproduce: 2475/2539 counts, 102/134 tail counts — match
  exactly or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m22/`; evidence `local/research/M22/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m21/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m22/` + `local/research/M22/`.
- Evidence dir `local/research/M22/` (STANDALONE). Commit with
  `git add -f`, prefix `[M22]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M21 precedent).

## Task 1 — locate (where are the tail bytes?)

Tail membership recomputed from the dumps (|δ|≥8 on cell-7
bytes; counts must match M20's 102/134 exactly or table the
mismatch and stop):

1. Position: band / edge-distance / gradient-decile joins (M20
   machinery verbatim — same edges, same BFS) for tail vs bulk
   side by side (do the tail bytes live where the bulk lives?).
2. Plane: Y/U/V split of the tail (bulk is 97.8%/94.2% Y —
   table the tail's split + per-plane |δ| ranges).
3. Region: connected-component clustering of tail sites (4/8-
   connectivity — table component count + size distribution:
   one region or scattered singles?).

## Task 2 — isolate (distinct population or smooth tail?)

1. Carrier cut: tail bytes inside each top-10 removed mask +
   inside/outside-701 split (M20: all 102 outside 701 — table
   per-carrier tail shares; any carrier CONCENTRATES the tail?).
2. Cross-shape persistence (M21 Task-1.1 machinery): tail-site
   maps on s0 vs the 764 coarse pass — same sites recurring
   (deterministic) or per-shape scatter? Jaccard + persistence
   table, s0-anchored like M21.
3. δ-shape join: tail |δ| values vs (v0−full) gap at those sites
   (ρ = δ/|gap| on the tail — same [0.3,0.4) peak as the bulk,
   or a different regime?).

## Task 3 — controls + determinism (M18–M21 precedent)

1. Positive control: synthetic truth with KNOWN tail injections
   (N sites, known δ) — recover count + values + locations
   exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M21).

## Report

`local/research/M22/REPORT.md`: DESIGN.md (bars first) + locate
tables + isolate tables + controls + explained-vs-standing
update (does any tail rule shrink cell 7? by how much?) + gap
rows. Screenshot only if a tail map discriminates
(budget-noted). Commit evidence (`[M22]`, no push), stop.
