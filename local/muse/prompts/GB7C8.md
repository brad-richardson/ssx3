# GB7C8 — matched GPU comparison design for packet5470 (read-only, OpenCode Go)

You are the G-lane source-design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, GB7C7P2 `REPORT.md`/`ORCH-GATE.md`/`tap.tsv`, GB7C6 `ORCH-GATE.md`, and GB7A `ORCH-GATE.md`. Pin private GB4 fork `~/dev/ssx3-work/GB4/PS2Recomp` at `f54adff`, `run/gb4p4.capture.bin` SHA `a6f75fb3…ad51851` with path sidecar, and the paraLLEl-GS source actually used by its build (`CMakeCache.txt` source-dir key; record source HEAD). Read only those source paths and directly linked draw/VRAM/readback helpers; use LSP if available, direct reads otherwise. Own only this brief and `local/research/GB7C8/`. No source edit, build, replay, device action, lease, push, board/global config edit or upstream contact. Time box 20 min, text <512 KiB.

Fact to build on: direct CPU replay executed tick259/path3/packet5470/batch10 at FBP112 pixel `(342,377)`, destination GS address `0x0019ae38`; sampled CT32 tap0 word `63353341` at GS address `0x000bae74`, then raw destination `dc302f3b→dc353341`. This is one pixel, not a whole-glyph or GPU cause. Capture marker259 at offset 6673264 precedes packet5470 at 6673427; a marker259 frame is a **pre-write** control. GB7A gate says same-stream packet-entry equality is an input/null control only.

Trace where packet5470 enters the paraLLEl backend, how its 17 textured sprite batches reach GPU command submission, and where/when texture source and destination VRAM can be read. Explicitly separate packet recording, command submission, GPU completion, and host readback. Test whether an observation at *exactly packet5470/batch10* can be made without forcing an extra flush/submit/wait or changing draw batching. If not, state so and design the smallest same-stream **marker300/301** matched CPU/paraLLEl comparison of full/cropped FBP112 content around `(342,377)` and source texture address. The marker must be after packet5470; name intervening packets/writes as a confounder, so a marker mismatch cannot be attributed to that one packet without an execution witness. Include A1 packet-byte/path equality as null control, active TEX0/FRAME/UV state as a source-control if observable, exact storage address/PSM conversion, and ON/OFF image-hash control for any future default-OFF tap.

Predeclare **A** = a source-grounded, nonperturbing per-packet GPU execution/readback witness with unique predictions for source-word divergence, write/composite divergence, and later overwrite; **B** = no such witness without an intervention, but a bounded post-packet marker comparison can narrow the stage (state its limits); **OTHER** = pin/path/citation gap. No GPU cause from source alone. `check.py` must verify pins/cited source rows and reject any A claim based only on packet-entry bytes, queued counts, marker259 frame, eight isolated words, or a flush/submit added before the selected observation. It cannot prove runtime behavior from strings. Deliver `REPORT.md`, source/observable table, checker/result and gaps. Commit `[GB7C8]` with `Orchestrated-By: opencode`, no push; hand back table, **orchestrator gates**.
