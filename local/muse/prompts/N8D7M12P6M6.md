# N8D7M12 Part 6M6 — one pinned Android package from staged sources

You are the OpenCode Go Android package worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, P6M5 REPORT + ORCH-GATE, P6M1 inventory, and P3 `build.sh`, `source_gate.py`, `apk_gate.py` and REPORT. Own only ignored `local/research/N8D7M12P6M6/` text scripts/receipts and private Mac `~/dev/ssx3-work/N8D7M12P6M6/app-release.apk` after successful build. Commit only bounded text scripts, REPORT and checker result under the receipt dir with `[N8D7M12] Part 6M6` / `Orchestrated-By: opencode`; no push. No source/fork/renderer/collector edit, no Odin/emulator action, no game bytes or binaries in Git, no upstream contact, no subagents. Time box 45 min; one `assembleRelease` only, no repair loop. Stop on failure and hand back evidence. This pane has a scoped SSH exception for `bytesize`; use it only for this brief.

Goal: build the next arm64 APK from **exact P6M5 staged bytes**, with a reviewable source→native→APK link. Correct model: four-root source manifest matches on Mac and WSL before and after build; build flags and actual compiled-input list are recorded; final native member SHAs/Build ID and APK SHA are double-read. These facts do not prove an Odin image or GPU cause. Historical P3 inputs that were unpinned remain historical unknowns.

Inputs: `local/research/N8D7M12P6M5/stage/{PS2Recomp,parallel-gs,codegen-ssx3,jniLibs}`, private manifest `source-manifest.json`, aggregate `6877de873b197bf915d068d1b03331e6488c529e6bdac64273513f338080316a`, counts 26,211 files/612,576,408 B. Target WSL root `/home/brad/n8d7m12p6m6` (fresh; never overwrite another lane). Toolchain and governor paths as P3: `/home/brad/n2/toolchain/{jdk-17,android-sdk}`, `/home/brad/n2/gradle-home`, `/home/brad/n8b1/mem_governor.sh`. Bytesize one heavy job at a time; preflight processes/free disk, require ≥20 GiB free and no active N/T build. WSL root ≤10 GiB, Mac APK scratch ≤500 MiB, receipts ≤512 KiB, mini project <200 GB. Verify 2 SHA reads for any binary transferred to Mac. Use explicit SSH target `bytesize` only.

1. Preflight Mac P6M5 checker24/24 and project disk budget; confirm stage and source manifest aggregate. On WSL create fresh root, transfer only the four staged directories plus collector script and manifest (stream a tar over SSH; no public remote). Record exact transfer commands/byte count. Run collector `verify` in WSL on those four roots with `--max-files 30000 --max-bytes 2000000000`; require `status=match`, empty add/missing/changed and expected aggregate before build. Check runner stub and no extra in-tree generated runner file. If SSH permission or transfer check fails, stop and report; do not work around policy.
2. Run exactly one `assembleRelease` using P3's flags/toolchain/governor, replacing root and `-Pps2xGameCodegenDir` with this root's `codegen-ssx3`; keep SHADOW=ON, six diagnostic defaults OFF, arm64-v8a, `--max-workers=4`. Capture exact argv, Java/SDK/NDK/CMake/Gradle versions, full CMakeCache path/text (private), and `PS2X_GAME_SOURCE_COUNT`. After build, rerun source-manifest verify on WSL; require same aggregate. Capture the actual CMake/Gradle source input file list from `compile_commands.json` or Ninja build graph; show required fork frontend/worker/backend, renderer interface/page tracker/Granite and codegen are in the compiled/linked graph. If no reliable file list, mark compiled-input proof open; do not invent it.
3. Gate package: APK size/SHA twice on WSL, exactly three arm64 native members with each member SHA twice, runner Build ID, necessary replay/fingerprint strings, CMake flags and JNI member equality. Transfer APK privately to Mac scratch, SHA twice and match WSL. Do not install/launch. Keep full logs private/bounded; commit text excerpts and summary only. Mechanical checker should assert pre/post source match, one build success/no retry, member/Build-ID/APK pins, build flags, runner-dir guard and compiled-list evidence status. REPORT table exact commands, all full SHAs, source→member→APK graph, gaps, and next action. Hand back table, not a GPU verdict.
