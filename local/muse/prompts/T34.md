# T34 — Cross on Continue from the My Rules park: next screen (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T33/REPORT.md` first (all of it: R1
NO-SE-PARK on a true SE park (remote TAG-crop gap ~260–280×, honestly
tabled); R2 with widened gate reached My Rules via Cross on Snow Jam
@T+297.80, Continue highlighted (orange bar), 12 snaps over 128 s pairwise
≤0.201/p99 ≤3 (snowflake drift), no reclaim; G1 proposes one 534 ms Cross
(× Enter game) on Continue). This brief executes T33's G1. Reuse T33's
scripts (`t33-auto.sh`, `t33-cropdiff.py`, `t33-analyze.sh`, `t33-vcount.sh`
— copy, do not modify the committed originals). REAL filenames only (T28 G9
lesson): T33's arrival snaps are `t33r2-sj-post{1,3,8,15,25,40}.jpg` +
`t33r2-sj-stab{1…6}.jpg`; the My Rules ref recipe is PIL `convert('RGB').save`
(T28 §T28-1).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T33 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- GATE NOTE (T33 G1 — the SE→MR hop is decisive): whole-frame hop ~10.58–
  10.61, so DEPARTED > 5.0 fires normally here. Derive a My Rules ref (from
  `t33r2-sj-stab6.jpg`) + calibrate the standard < 2.0-style whole bar
  BEFORE the run (nearest prior screen is SC at ~5.61–5.69; arrival shimmers
  ≤0.201/p99 ≤3 — wider than SE's, still far under 2.0). T33 G12 stands:
  text-dense crops need remote-measured bars — prefer the whole-frame bar
  here, or table a remote receipt first.
- The task: reproduce the T33 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam → ≤3
  attempts) to the My Rules park with Continue highlighted, then press ONE
  534 ms-class Cross (× Enter game, footer `Accept current rules and enter
  game.`) and table EVERYTHING after: the press (button, hold ms, T+ time,
  pre/post snaps), each screen transition (snap sha + diffs per hop), and
  the arrival screen (snaps + stability count + content read off viewed
  snaps).
- If Cross does NOT advance (selection ignored, needs a different button,
  next screen loads slowly): table the exact observed behavior (snaps +
  scores + waits) and STOP with a recipe — one input per screen, never
  blind multi-presses. ONE bounded variant allowed only if the press
  provably never acted on the arrival screen (pre-press ≠ My Rules).
- Success bars (tabled, not verdicts): arrival park reproduced (snap match
  vs T33's `t33r2-sj-stab6.jpg` under the calibrated gate); Cross acted on
  it (pre-press = My Rules, Continue highlighted); next-screen snaps +
  stability N; full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T33 G2: single-shot
  scripts, runs ≤6 min (T33 R2 ran 444 s — keep added scoring tight or table
  the overrun with full dmesg coverage), preserve emulogs first, dmesg +
  btime + init age, UNFILTERED dmesg + event log around flaps, 534 ms holds
  NOT bisected, `;` chains only INSIDE one `wsl` call, one `wsl` call per
  ssh; take a post-run dmesg read IMMEDIATELY after the run ssh returns —
  T33's R2 sequencing gap must not repeat). Runs in flap windows must be
  effects-verified or re-run (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T34/` (STANDALONE). Commit with `git add -f`,
  prefix `[T34]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — arrival park + Cross on Continue (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T33 §T33-0 shape): build/input receipts, NVM state, T33
   reference snaps present, MR-ref calibration matrix, free space both ends.
2. Per attempt table: park reproduction (chain scores + snap match vs
   `t33r2-sj-stab6.jpg`), press row (button/hold/T+/pre-post snaps),
   transition chain (snap sha + diffs per hop), arrival (snaps + stability
   N) or exact non-advance behavior.
3. Trace bytes + sha per run; WSL event table; NVM state at end.

## Report

`local/research/T34/REPORT.md`: pre-run receipts, attempt tables, press
log, arrival (or non-advance characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T34]`), stop.
