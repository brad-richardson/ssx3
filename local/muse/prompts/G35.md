# G35 — Commit-necessity wall: same tag, NO end_vram_write (H1-barrier vs refined-H2, Odin)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS` — VERIFY at start, re-pin if
the Mac-mini migration moved it, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts
beyond the hypothesis.** Read `local/research/G34/REPORT.md` first
(all of it: H1 WINS — the sparse 112-word tag renders EXACTLY,
10/10 ladders + 10/10 scanouts == pre-registered tag model; H2
refuted 0/10). This brief executes G34 §4's ONE next action ONLY.
No upstream contact of any kind (standing no-upstream order — local
hunks only, filing stays local).

## Facts you start from

- The surviving split: G34 refuted H2-as-registered, but the tag IS
  content — so refined-H2-threshold ("ANY nonzero-RGB content in B
  fixes sampling, commit or no commit") survives untested vs
  H1-barrier (the commit itself is the fix).
- Discriminator (G34 §4): the SAME W1 legs + the SAME sparse
  OR-mask, but SKIP the commit (map-write the tagged temp back with
  NO `end_vram_write`). H1-barrier predicts output stays CLEARED
  (ladder `aa2fa32572450383` ×10, scanouts byte-identical black
  `99418f1b…`); refined-H2 predicts TAG-VISIBLE (ladder
  `86ad7b887e140383`/229712 ×10, scanouts `d19e6beb…`).
- Gated G34 pins (re-verify every one — `/tmp` session files did NOT
  survive the restart; the committed REPORT scalars are the
  authority): tagged temp/B `c039a1d1c29cb293`/150057, head
  `ffffff00…`; cleared `aa2fa32572450383`/229376; tag model
  `86ad7b887e140383`/229712 + PPM `d19e6beb…` (112 whites);
  G34 binary 265,853,192 B `2b101dde…` (intact at gate); dump
  `154d9d85…`; clone HEAD `3a66c19…` with G34 ×3 / G33 ×0.
- Drive state (gated): the G33-binary morph (localized →
  fully-zeroed ×11 → BUILD sha, mtime frozen throughout) CLOSED the
  flash hypothesis — 12th read-artifact recurrence, every
  zero-destroyer event to date is transient link corruption. Standing
  rule: lane-critical SSD bytes need 2+ matching reads separated in
  time (or independent corroboration); single-read SSD evidence is
  suspect. Mirror the new `ps2x-g35/` receipts to the share tier.
- Task 1 (no device until §2): STATIC design — table the commit-less
  shape (exact sequencing vs G34's W1: which call is skipped, what
  stays), the premise rows (writeback receipt MUST still read the
  tagged temp `c039a1d1…`/150057 — host-side, commit-independent;
  AND the B-at-sample-time question: without the commit, does the
  read leg see tagged B or stale B? Table BOTH outcomes with what
  each means for the verdict BEFORE the run), the reformulated
  H1/refined-H2 predictions (pre-register every scalar), and the
  decision matrix (cleared vs tag-visible vs mixed vs premise-break,
  with the B-triage for each).
- Task 2 (ONE run max): the wall — ONE hunk max (G34's block excised
  + the commit-less tagged write-back inserted in the same edit),
  ONE build max (NEW dir, G22 recipe, `-j2`), full identity,
  verify-then-push with NO gap (push-time + post-run + report-time
  re-shas), TWO bounded runs max (diagnostic + one retry iff O1/O3
  fires instead of first draw).
- Success bars (tabled): design table (shape + premise rows +
  predictions + matrix); the H1-vs-refined-H2 VERDICT (with the
  discriminating bytes); if a run: exit/wall/fate + receipts +
  tombstone triage (if any) + scanouts scored AND pulled by explicit
  filename list with cleanup as its own verified step (G31-E1 rule);
  the ONE next action (adoption stays queued per the standing rule
  until the orchestrator gates it; filing stays held).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g35/` transient
  dir ONLY, removed at end (`mg/` only). No P-lane lease, no
  bytesize/WSL. `COPYFILE_DISABLE=1` on all SSD steps.
- SSD clone: ONE hunk max in `gs/gs_interface.cpp` (G34 excised +
  G35 inserted, single edit); G22 + G26 + G28 + G29 + G30 + G31
  hunks untouched (HUNK_MATCH re-verified pre + post + pre-push +
  post-run + report); zero commits in submodule, zero in ps2xGS.
- Byte caps: NEW SSD build dir ≤ 6 GB; NEW SSD `ps2x-g35/`
  (retrieval by explicit list) ≤ 50 MB; all older SSD dirs 0 growth
  (pre + post `du -sk` snapshots must match exactly); internal
  volume ≤ 1 GB delta, no clones/builds; committed text only.
- Knobs: `--disable-sampler-feedback` SET, `PGS_SKIP_SAMPLER_FEEDBACK`
  UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT, sanitizer env ABSENT —
  the ONLY delta vs G34's run is the hunk-for-hunk swap.
- Stop: any validity-chain pin mismatch → table + stop (G34's §2a2
  ratification does NOT extend to the G35 chain — the G35 binary and
  dump are IN the chain); no tuning loop, no second shape, no lldb
  unless a tombstone needs triage.
- Time box 6 h. Evidence `local/research/G35/` + `[G35]` commit +
  `Orchestrated-By: Muse Code` trailer, `git add -f`, NO push
  (orchestrator pushes at poll).
