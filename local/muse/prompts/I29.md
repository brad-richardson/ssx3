# I29 — iOS rebuild with sound (fork `ssx3`), Simulator + iPad run, iPhone install-only

You are the I-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, then `local/research/I27C/{REPORT.md,build-install.sh}`, `local/research/I25/{REPORT.md,build-install.sh}` (dependency recipe) and `local/research/I27B/REPORT.md` (Simulator run). Own `local/research/I29/` and scratch `~/dev/ssx3-work/I29/` plus the new persistent dependency dir `~/dev/ssx3-work/ios-deps/`. No fork source edits, no push, no upstream contact.

**Why.** Brad's iPhone has the 09-24 morning build (`04f3ace`). Fork `ssx3` now has the sound HLE (AU7: menu music equals PCSX2 to 1 LSB), so this is the first iOS build with audio. Brad tests on the phone himself.

**Rules.** **iPhone: build, sign and install only. Never launch, test or screenshot it.** The Simulator and the iPad (`00008112-001224302184A01E`, ask the orchestrator if it's locked) may be launched. Two SHA reads of every pinned input.

**Steps.**
1. **Dependencies** (the orchestrator's cleanup deleted `~/dev/ssx3-work/I25`): rebuild FFmpeg 7.1.1 (tarball SHA `73398439…a03b1`, recipe in I25's script and the fork's `ps2xRuntime/cmake/iOS-FFmpeg-7.1.1.md`) and SDL2 `release-2.32.10` for **iphoneos and iphonesimulator** into `~/dev/ssx3-work/ios-deps/`. Clone raylib at pin `c1ab645ca298a2801097931d1079b10ff7eb9df8` there too (the fork's `patch_raylib_ios_sdl2_dpi.cmake` patches it at configure). Record the new library SHAs (they may differ from I27C's; say so).
2. **Source:** worktree of fork `ssx3` at `fb11e18` in `~/dev/ssx3-work/I29/PS2Recomp`; runner-dir check empty; canonical codegen `~/dev/ssx3-work/codegen-ssx3`; ELF/ISO from `~/dev/ssx3-work/E32-inputs`.
3. **Simulator:** Release build, install, one I26-FAST run to the race (I27B's `sim-run.sh` pattern, one mini lease slot, ≤ 600 s). Report whether the sound path starts (SND clock start / host audio stream log lines) and view the title, menu and race frames yourself.
4. **iPad:** install the device build and run it once to the race (IPAD1 pattern: `devicectl` launch with `PS2X_VSYNC_RATE_LOG=1`, 4 screenshots, terminate after). Still an iPhone-compat window; that's I28, not this lane.
5. **iPhone:** sign with the existing identity and wildcard profile (I27C `sign`/`install`), `devicectl device install app` only. No process launch.

**Deliverable.** `local/research/I29/REPORT.md`: deps and SHAs, build flags (`-O3 -DNDEBUG`, diagnostics OFF), Simulator + iPad results with frame paths, iPhone install receipt, gaps. Commit `[I29] …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. Budget 120 min, 3 builds (sim, device, deps not counted), 1 Simulator run, 1 iPad run.
