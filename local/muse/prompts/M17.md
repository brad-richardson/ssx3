# M17 — Sub-pixel / local motion model for the synth residual (offline)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/M16/REPORT.md`
first (all of it: the leave-one-out shares, R_0=22815 with 93.6% of
residual Y px at |d|=1, all 764 integer warps reading (0,0)), plus the
M15 `synth.py` estimator section it reuses. This brief is M16 gap 1
(M15 gap 2, open twice): test whether the rigid component lives at
sub-pixel scale. Fully offline — no harness code, no replays, no
lease. No device work.

## Facts you start from

Integer global warp measured (0,0) on 764/764 shapes (M16) and on the
M15 frame — yet the residual's structure (overwhelmingly |d|=1,
edge-concentrated) points at sub-pixel shifts and filtering-scale
motion. Starting input: the committed dumps + synths + `loo.txt` on
the SSD under `/Volumes/Extreme SSD/m15/` and `/Volumes/Extreme
SSD/m16/` (read-only — copy what you extend). P-lane briefs run
concurrently; you share nothing with them (no lease needed at all).

## Gates and rules

- OFFLINE ONLY: no replay runs, no harness builds, no lease, no
  `adb`. Installed tools only (numpy/PIL/etc. — verify, never
  download).
- Disk: internal disk is tight — work under
  `/Volumes/Extreme SSD/m17/` (new dir). Never touch
  `/tmp/p1-link`, `ps2x-i*`, `ps2x-p1*`,
  `/Volumes/Extreme SSD/m14/`, `/Volumes/Extreme SSD/m15/`,
  `/Volumes/Extreme SSD/m16/` (read from them, never write into
  them), or other agents' dirs.
- Evidence dir `local/research/M17/` (commit with `git add -f`,
  prefix `[M17]`, trailer `Orchestrated-By: Muse Code`).
  Screenshots under 5 MB total (downscale or describe). NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Design (DESIGN.md first like M15/M16).** The estimator suite:
   half-pixel bilinear SAD (global + per-shape), block matching
   (block size + search range recorded before running), and what
   "explained" means numerically (residual-after-compensation vs
   R_0=22815, per-shape and total). State the falsification bar
   for "sub-pixel rigid" vs "needs local/non-rigid model".
2. **Run offline.** Over the M16 dumps (+ M15 frame as the second
   sample if cheap): per-shape sub-pixel shifts, per-block motion
   field, compensated residuals. Record wall time, exit, input
   shas, determinism (re-run receipt: same inputs → same
   numbers).
3. **Attribute.** How much of R_0 each model explains (bytes +
   distributions, M16-loo table shape); where sub-pixel succeeds
   vs fails (carrier overlay or region table); the updated
   deliverable: rigid (integer + sub-pixel + block) vs genuinely
   non-rigid remainder, with byte counts. Screenshot(s) if they
   discriminate.
4. **Report.** `local/research/M17/REPORT.md`: design + bars,
   run receipts, model comparison tables, gap rows with the exact
   next brief each needs (negative mechanism? Odin handoff
   format? top-HUD isolation?), exact commands, receipt paths,
   "What I could not do". No verdicts. Commit
   `local/research/M17/`, stop.
