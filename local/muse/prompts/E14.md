# E14 — Absorb the 5 proven I-lane map rows into the fork (single commit): fail-before, regen, truth-table regression, one guarded boot

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E13/REPORT.md` first (all of
it: `0x2c5358` restored — 24 leaf cases, 3 consumer checks, suite 452/452,
one guarded boot with 142 exact returns and state 3→6→29, parked at the
MPEG picture wait; outcome (ii); fork `83fb4d60` pushed; standing no-regen
resumed). Also read E13's `i17-row-crosscheck.json` (the absorb set is
NAMED there: the 5 device-only rows at `0x14f2a8`, `0x156750`, `0x243a80`,
`0x395730`, `0x3a0158` — no fork-only rows) and the I11–I15 REPORTs'
row tables (each row's bytes + bounds evidence). This brief renews the
bounded regeneration exception for THESE 5 NAMED ROWS ONLY — the user-
authorized merge vehicle for the I-lane's unpushed map work. Do NOT touch
the MPEG wall (E13's NEXT-BRIEF is queued as E15 — no stacking).

## Facts you start from

- USER DECISION (recorded by the orchestrator): the I-lane rows absorb
  into an E-brief rather than a topic branch. This is that brief. The
  absorb set is EXACTLY the 5 rows E13's cross-check names as device-only:
  `0x14f2a8` (I12), `0x156750` (I13), `0x243a80` (I14), `0x395730` (I11),
  `0x3a0158` (I15). (`0x2c5300` + `0x2c5358` are already fork-side via E12/
  E13 — verify their presence at HEAD, do not re-add.)
- Each row is PROVEN device-side (the I-lane closed each wall with it),
  but trust-nothing applies: re-verify every row's bytes + bounds against
  the I-lane's emitted-slice receipts AND your own emitted slice at the new
  base before writing. Any row whose bytes disagree with EITHER source is
  EXCLUDED with a tabled reason (a 4-row absorb + 1 exclusion beats a
  blind 5-row absorb).
- Baseline: fork `83fb4d60` (pushed); active checkpoint `/tmp/p1-link/runtime`
  (PROTECT it + DerivedData — never reclaim); registry `98753bfa…`; runner
  `b2099130…`; suite 452/452; leaf-24 + consumer-3 + predicate-14 + query-4
  + DROP all pass. Re-verify checkpoint identity first (E13 §1 shape) — do
  NOT repeat earlier repairs.
- The task, in strict order: (1) checkpoint re-verify + per-row fail-before
  (relink fixture against baseline runner objects: `hasFunction(<each>)=
  false`, expected-present tests fail — 5 fail-befores, one per row);
  (2) FRESH admission sample + persist BEFORE any CSV mutation (≥1 GiB
  logical + 2 GiB floor + 256 MiB guard; reclaim ONLY retired trees with
  per-tree ownership proof if short, E13 §4 shape); recheck immediately
  before spawn; (3) insert the 5 rows in ONE fork commit (named files;
  expected CSV sha computed-before-editing as in E12/E13 — table
  match/mismatch), regenerate via the APFS path, install changed bytes
  only; (4) per-row actual-binding presence + full regression (leaf-24,
  consumer-3, predicate-14, query-4, DROP, suite — every prior fixture
  must still pass against the new objects); (5) ONE guarded verification
  boot (fresh T13 pre-claims, lease, REPORT_ALL=1, E7/E4, wall/progress/
  byte caps) confirming the absorb (prior joins intact; any guest advance
  past the MPEG wait is tabled as observed, NOT pursued — E15 owns MPEG).
- Success bars (tabled, not verdicts): checkpoint identity re-verified;
  5 fail-befores recorded; admission passes on fresh samples (ordering
  repair honored); 5 rows absorbed in one pushed fork commit; entries
  generated with exact-PC bindings; all prior fixtures + suite green;
  one boot shows the joins intact (or the first remaining wall — tabled,
  not fixed).

## Gates and rules

- Renewed bounded exception: THE 5 NAMED ROWS ONLY (map rows + their
  generated entries). Any other gap (MPEG included) → table, do not close.
  Standing no-regen resumes at close. Fork discipline: named-files commits
  only, generated sources NEVER staged, fork-remote push only.
- Ordering repair (E10 lesson): admission check + persist BEFORE csv edit;
  recheck immediately before generator spawn. Never lower a declared guard
  after a rejection.
- Reclamation safety (E11–E13 shape): per-tree ownership proof BEFORE removal
  (lane-board check + no live file handles); oldest retired first; stop
  once admission passes + 512 MiB headroom; never touch `/tmp/p1-link`,
  DerivedData, or any live lane path. `export COPYFILE_DISABLE=1` on SSD
  steps; allocated-byte caps declared + tracked (ExFAT).
- Boots: ≤1 guarded boot under the shared P-lane lease (fresh T13 pre-claim
  checks, wall/progress/byte caps, REPORT_ALL=1, E7/E4 taps). No boot without
  the lease; never wait on another lane's lease — table and stop.
- Evidence dir `local/research/E14/` (STANDALONE). Commit with `git add -f`,
  prefix `[E14]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — checkpoint re-verify + 5 fail-befores + fresh admission (no regen)

1. Checkpoint identity: registry/runner SHAs, name audit vs
   `after/generated.json`, suite re-run, leaf-24 + consumer-3 + predicate-14
   + query-4 re-run. Fail-before: relink fixture vs baseline objects,
   `hasFunction(<each of the 5>)=false`, expected-present fails. Verify
   `0x2c5300` + `0x2c5358` already at HEAD (no re-add). Table every check.
2. Per-row byte verification: each row's bytes vs the I-lane's receipts vs
   your emitted slice at baseline. EXCLUDE any disagreement with a tabled
   reason (the absorb proceeds with the verified subset — never 0 rows
   without stopping: if <3 verify, STOP with outcome (iii)-shaped + recipe).
3. Fresh admission sample + persist (BEFORE csv edit). If short: reclaim
   retired trees oldest-first with ownership proofs until admission + 512
   MiB headroom. If STILL short: STOP with outcome (iii) + the exact next
   target — no csv mutation, no regen.

## Task 2 — absorb commit + regen + install + regression (no boot)

1. Insert the verified rows in ONE commit (expect the pre-computed CSV sha
   — table match/mismatch). Recheck admission immediately before spawning;
   generate; install changed bytes only (AppleDouble purge, proven-created
   only). Push the fork commit (named files only).
2. Relink fixtures against NEW runner objects; run per-row presence +
   full regression (leaf-24, consumer-3, predicate-14, query-4, DROP,
   suite). Table every receipt.

## Task 3 — one guarded verification boot

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Confirm the prior joins
   (MC/UI, query/predicate/leaf, Present) + record any guest advance (or
   the first remaining wall — MPEG expected, not pursued). Table the
   joins; no promotion without an actual request/completion dependency.

## Report

`local/research/E14/REPORT.md`: contract, checkpoint re-verify + fail-befores,
admission record, absorb + regen + regression tables, boot joins (or wall),
exact commands, gaps. Commit evidence (`[E14]`), stop.
