# M31 — K-boundary sweep: K ∈ {0,1} below + {15,20,50,100} above (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M30/REPORT.md` first (all of it —
M30 gap 1 is this brief: no named-column site in any of the 764
sits at span distance 3–10, so the window is identical at
K=2/5/10 and the rule's stability margin is unobserved on real
data — only synthetic W3 at d+7 flips), plus M30's DESIGN.md
(the ±K window definition to reuse verbatim). Fully offline —
SSD dumps read-only, no harness code, no replays, no lease. No
device work.

## Facts you start from

- K=2/5/10 identity (M30): 8 movers / 13 cells at every K (9 +
  22 un-moved); off-span exactly the 3 far extras (d+234,
  d+366, d+367); 0 flipping cells on both adjacent pairs; the
  three TSVs byte-identical. The unprobed regions: below K=2
  (734-c342's d=1 extras should flip between K=0 and K=1) and
  above K=10 (the first real-data flip at unknown K).
- K list for this brief (FIXED — record in DESIGN.md before
  running, no additions mid-run): {0, 1, 15, 20, 50, 100}.
  Predictions to table (not to believe): K=0 un-moves nothing
  beyond exact (or re-moves 9/22? — table it); K=1 keeps
  734-c342 extras in-window (d=1 ≤ 1); some K in {15..100}
  may re-admit far extras (d+234 needs K≥234 — outside the
  list, so expect NO upper flip; table the negative if so).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m30/m30.txt` +
  `m30-census-K2.tsv` (K=2 windowed census to reproduce: 8
  movers / 13 cells / standings — match exactly or table the
  mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m31/`; evidence `local/research/M31/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m30/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m31/` + `local/research/M31/`.
- Evidence dir `local/research/M31/` (STANDALONE). Commit with
  `git add -f`, prefix `[M31]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M30 precedent).

## Task 1 — the sweep (6 K values, full tables)

K=2 reproduction from the dumps (8 movers / 13 cells /
standings must match M30 exactly or table the mismatch and
stop):

1. Windowed census per K ∈ {0, 1, 15, 20, 50, 100} (movers /
   cells / standings tables — full per-cell rows like M30
   §Task 1, not just counts).
2. Off-span tail per K (pooled sites + per-shape rows —
   which K values admit which far extras?).
3. Adjacent-K diffs across the FULL ordered list
   {exact, 0, 1, 2, 5, 10, 15, 20, 50, 100} (flipping cells
   + mover flips per pair — the stability margin, tabled).

## Task 2 — boundary analysis (where does the rule bite?)

1. Lower boundary: 734-c342's d=1 extras across K=0/1/2
   (in-window at K≥1? out at K=0? — table the flip) +
   any other d≤2 sites (M30: none beyond ±2 except the
   far 3 — confirm).
2. Upper boundary: first real-data flip at K>10 (if none
   through K=100, table the negative + the minimum K that
   WOULD flip (d+234 → K≥234 — computed, not run)).
3. Below-0.95 join per K (M30's 2×2 recomputed — does any
   K move a shape across the mover line?).

## Task 3 — controls + determinism (M18–M30 precedent)

1. Positive control: synthetic truth with KNOWN boundary
   extras (N shapes at d ∈ {0,1,2,7,15} + one at d+234) —
   recover per-K windowed standings + off-span sets exactly.
2. Determinism: re-run Task 1 K=2 on s0+9+22, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M30).

## Report

`local/research/M31/REPORT.md`: DESIGN.md (bars + fixed K
list first) + per-K census tables + diff tables + boundary
tables + controls + stability-margin summary as a TABLE
(per-K movers/cells/off-span/join — no verdict) + gap rows.
Screenshot only if a K-sweep map discriminates
(budget-noted, pre-registered split required per M23–M30
precedent). Commit evidence (`[M31]`, no push), stop.
