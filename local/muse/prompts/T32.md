# T32 — Cross on Race from the Select Mode park: next screen (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T31/REPORT.md` first (all of it: R1
reached Select Mode via Cross on Peak 1 @T+235.37, Race highlighted (orange
bar) / Freestyle, tagline `Choose a mode and continue to select event.`,
12 snaps over 132 s pairwise ≤0.066/p99 ≤1, no reclaim; G1 proposes one
534 ms Cross (× Select) on Race). This brief executes T31's G1. Reuse T31's
scripts (`t31-auto.sh`, `t31-cropdiff.py`, `t31-analyze.sh`, `t31-vcount.sh`
— copy, do not modify the committed originals). REAL filenames only (T28 G9
lesson): T31's arrival snaps are
`t31r1-pc-post{1,3,8,15,25,40}.jpg` + `t31r1-pc-stab{1…6}.jpg`; the Select
Mode ref recipe is PIL `convert('RGB').save` (T28 §T28-1).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T31 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- The task: reproduce the T31 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → ≤3 attempts) to the Select Mode park with Race highlighted, then press
  ONE 534 ms-class Cross (× Select) and table EVERYTHING after: the press
  (button, hold ms, T+ time, pre/post snaps), each screen transition (snap
  sha + region-diff scores), and the arrival screen (snaps + stability
  count + content read off viewed snaps).
- If Cross does NOT advance (selection ignored, needs a different button,
  next screen loads slowly): table the exact observed behavior (snaps +
  scores + waits) and STOP with a recipe — one input per screen, never
  blind multi-presses. ONE bounded variant allowed only if the press
  provably never acted on the arrival screen (pre-press ≠ Select Mode).
- Success bars (tabled, not verdicts): arrival park reproduced (snap match
  vs T31's `t31r1-pc-stab6.jpg`); Cross acted on it (pre-press = Select
  Mode, Race highlighted); next-screen snaps + stability N; full input log
  + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T31 G2: single-shot
  scripts, runs ≤6 min (T31 ran 381 s — keep the added phase scoring tight
  or table the overrun with full dmesg coverage), preserve emulogs first,
  dmesg + btime + init age, UNFILTERED dmesg + event log around flaps,
  534 ms holds NOT bisected, `;` chains only INSIDE one `wsl` call, one
  `wsl` call per ssh). Runs in flap windows must be effects-verified or
  re-run (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T32/` (STANDALONE). Commit with `git add -f`,
  prefix `[T32]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — arrival park + Cross on Race (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T31 §T31-0 shape): build/input receipts, NVM state, T31
   reference snaps present, free space both ends.
2. Per attempt table: park reproduction (chain scores + snap match vs
   `t31r1-pc-stab6.jpg`), press row (button/hold/T+/pre-post snaps),
   transition chain (snap sha + diffs per hop), arrival (snaps + stability
   N) or exact non-advance behavior.
3. Trace bytes + sha per run; WSL event table; NVM state at end.

## Report

`local/research/T32/REPORT.md`: pre-run receipts, attempt tables, press
log, arrival (or non-advance characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T32]`), stop.
