# M55 — CONST cross-frame collapse: per-column median drift tables (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M25/REPORT.md` first (all of it —
M25 gap 2 = this brief via M26 gap 5, taken on orchestrator
judgment: s0 column medians score 0/68 on m15 (1/65 the other
way) vs 12/13 same-frame), plus `local/research/M26/REPORT.md`
(CONST-vs-M25 median guard match=True both frames — the guard
to reproduce). Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- CONST medians (reproduce exactly or table the mismatch and
  stop): s0 [29,27,35,32,26,41,−17,−29] over c257…c343
  (fallback 27); m15 [19,19,37,23,28,38,−19,−24] (fallback
  20). Median rule M25-verbatim (fallback for n<3? — pin
  the rule from M25 before running).
- Scores (reproduce exactly): same-frame 12/65 s0 + 13/68
m15 (exact hits, near-hits |err|≤1 separate: s0 near?,
  m15 near? — from m25.txt); cross-frame 0/68 fit-s0→m15
  (near 2) + 1/65 fit-m15→s0 (near 4).
- The question: per-column median drift tables (Δmedian =
  m15 − s0 per column + drift-vs-kept-hit join — which
  columns keep hits under drift?) + the 0/68 anatomy (near
  2 + err distribution of s0 medians on m15). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles), `/Volumes/Extreme
  SSD/m25/m25.txt` (medians + scores to reproduce — match
  exactly or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m55/`; evidence `local/research/M55/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m54/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m55/` + `local/research/M55/`.
- Evidence dir `local/research/M55/` (STANDALONE). Commit with
  `git add -f`, prefix `[M55]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M54 precedent).

## Task 1 — CONST reproduction (do the medians + scores reproduce?)

M25's CONST medians + scores recomputed M25-verbatim from
the dumps (must match exactly — 8+8 medians, 12/65 + 13/68,
0/68 + 1/65, nears 2/4 — or table the mismatch and stop):

1. Median table: all 8 columns × s0/m15 medians + fallback
   rows (which columns use fb?).
2. Same-frame table: CONST hits/near/miss per frame with
   per-column colhits (12 + 13 reproduced).
3. Collapse table: fit-s0→m15 0/68 (near-2 rows named) +
   fit-m15→s0 1/65 (the 1 kept hit named) + errvalues.

## Task 2 — median drift (which columns drift? who keeps hits?)

1. Drift table: Δmedian per column (m15 − s0) + |Δ| rank
   (which column drifts most? least?).
2. Drift-vs-keep table: per-column |Δmedian| vs same-frame
   colhits vs cross-frame kept hits (do low-drift columns
   keep hits? — cf. M51's L1 finding).
3. Median-vs-peak table: CONST medians vs M26 argmax peaks
   per column per frame (median==peak? median-vs-TRI-p?).

## Task 3 — controls + determinism (M18–M54 precedent)

1. Positive control: synthetic truth with KNOWN medians +
   KNOWN drift (N columns, known δ lists) — recover the
   median + drift tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M54).

## Report

`local/research/M55/REPORT.md`: DESIGN.md (bars first) +
median tables + drift tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a drift map
discriminates (budget-noted, pre-registered split required
per M23–M54 precedent). Commit evidence (`[M55]`, no push),
stop.
