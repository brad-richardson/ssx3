# X18 — DeepSeek V4.1 Flash Go parser repair pilot

You are an OpenCode Go **deepseek-v4.1-flash** worker in a bounded model trial.
Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, and
`local/AGENTS.local.md`. Do not edit any OpenCode configuration. This is a
host-only repair for the N8D6C logcat vector parser, which rejected a
comma-led continuation and left that run formally OTHER. The same-run
vectors were independently recovered, but the released launcher must stay
unchanged as evidence. The next Odin launcher will need a tested parser.

Read only this brief; `local/research/N8D6C/launch.py` lines 125-195;
`local/research/N8D6C/vector-audit.json`; and
`local/tooling/orch/check_x18.py`. The checker reads an existing 5.4 KiB
gzip log in `/Users/brad/dev/ssx3-work/N8D6C/logcat-all.txt.gz`; you may
read that log only through the checker. No other source search/read, game
build, boot, device, web, push, or OpenCode config edit. Write only
`local/tooling/orch/tile_vector_v2.py` and `local/research/X18/REPORT.md`
(each <20 KiB). Do not edit N8D6C's released `launch.py`.

Copy the pure `tile_vector(lines, kind)` function into the new module with
only the narrow parsing change needed for **one leading comma on a
continuation line**. Keep the original return schema and 896-word cap.
Reject missing words, double commas, nondecimal fields, overflow, and an
interrupted continuation. Do not treat arbitrary prefix text as a vector.
The exact acceptance command is `python3 local/tooling/orch/check_x18.py`.
Run it once; if it fails, one edit and one retry are allowed, then stop and
report. Also run `python3 -m py_compile` on the new module. No device run.

Within two minutes of reading the source, write the module; context target
<18k, wall cap eight minutes. `REPORT.md` records exact commands, checker
output, changed behavior, any failed attempt, byte size, and gaps. No GS or
device verdict. Hand back the table; **do not conclude a cause**. Commit
only the two allowed files as `[X18]` with `Orchestrated-By: opencode`,
explicit paths, no push.
