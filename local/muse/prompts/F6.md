# F6 — Brad's next device build: Vulkan present on the Odin + VU1 speed-ups (muse)

## What's in it (fork `ssx3` tip, pushed by the orchestrator; read `docs/status.md` pins)
VR2 stage 1 (VU1 per-pair levers, Mac race 1.095×), HP1 (VIF1 trace formatting guarded; Android `minSdk 29` → native TLS), VK1 (Android: Vulkan present via AHardwareBuffer + SurfaceControl, **default on**, GL fallback, 16:9 fills the screen, aspect-preserving), SS1/SS2 save states (compiled in, default off). paraLLEl-GS fork `ssx3` (`vk1-part2` tip, pushed with the fork). Reports: `local/research/{VR2,HP1,VK1,SS1,SS2}/REPORT.md` (orchestrator gates at the end).

## Part 1 — Odin (build, measure, stop before the play-build install)
1. APK on bytesize with the F5 recipe (`local/research/F5/build-android.sh`; paraLLEl at the fork `ssx3` tip; hold the ssh open; `ps aux | grep -v grep` first — BA1 builds there too, one heavy job at a time). Two SHA reads ×2 (remote + pulled).
2. Odin ABBA vs Brad's F5 play build (`4ff81032…`), F5 Part 2 method (`local/research/F5/launch.py` + `cooldown.py`, I26-FAST, unpaced, sound on, pipelined 1×, stop 4500, cool to status 0 + fixed 180 s): per-phase table + race rate, GPU busy, thermal. Also confirm on the new legs: `[present-vk]` layer active, `vk_dropped=0`, one screencap in the race viewed (fills the screen at 16:9, correct colours, rider + trail).
3. One 4×+hi-res leg (single, not ABBA) on the new APK: race rate vs F5's 0.168×, and a screencap viewed (now shown at full resolution).
4. Restore Brad's play state after every run (`local/research/VK1/restore-play.sh`; env `a8d651a7…`, save 6/6). **Stop and hand back** the table: the orchestrator decides 1× vs 4× for the play build (Brad's rule: 4× only if it costs ≤ 5 %) and releases the install after the RV5 code review is gated.

## Part 2 — iOS (after the push; may run alongside Part 1)
I33/F3/F5 iOS recipe (`local/research/F5/REPORT.md` Part 3, `~/dev/ssx3-work/F5i/ios/build-install.sh` as the template; point it at the new fork tip, the paraLLEl fork `ssx3` tip, canonical codegen, `vu1gen-ssx3`). Bundled env unchanged from F5 (`local/research/F5/ps2x.env`: 4×, hi-res, pipelined, zero-copy). iPad: install, `deploy-ios.sh ipad`, one fresh-card test launch (read the live container path first), view frames (race, trail, full brightness, pad), terminate. **Stop before the iPhone install**; the orchestrator releases it after RV5.

## Rules
Brad's save (`files/mc0/` on the Odin, `Documents/mc0` on iOS) is never touched; tests use empty scratch cards. Force-stop after every Odin run; keyguard `showing=false`, AC + ≥ 20 % before each launch; never touch the Odin screen/keyguard. iPhone: install only, never launch. Text only in git. Scratch `~/dev/ssx3-work/F6/` ≤ 20 GB. First failure: stop, save the error, hand back.

## Deliverable
`local/research/F6/REPORT.md` (pins, APK/IPA SHAs, ABBA + 4× tables, screencap readings, iPad frames, gaps) + `[F6] Part N …` commits (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push.
