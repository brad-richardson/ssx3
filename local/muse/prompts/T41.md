# T41 — Sub-second `sleep 0.5` blind cadence around the 1 s hold: does SCPS40 resolve the hold hop? (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T40/REPORT.md` first (all of it: R1
DISCARDED (C:-full guest wedge at T+185 — environment failure, full
forensics + C: fix: cleared 20 stale emulog transfer copies ~30 GB, C:
116 KB → 29.1 GB; C: headroom ~5 GB+ is now a pre-run gate); R2 reproduced
the chain to LIVE gameplay, ONE 1038.0 ms D-pad Left hold @T+503.09 on a
live race (pre-pair 00:01:28 6TH/6 36% / 00:01:29 6TH/6 36%), blind
back-to-back 10-snap series at ≈1.07 s exposures scored post-hoc to
00:01:46 6TH/6 41%: SCPS120 railing drops 7/11 → 3/11 with the hold hop
RESOLVED (+49 @ 0.79 — first resolved input hop in T36–T40), SCPS40 rails
4/11 INCLUDING the hold hop (+40 @ 0.74), RDC gated out 10/12 with no gated
pair straddling the hold; G1 proposes ONE variant — this brief takes the
`sleep 0.5` arm, NOT the ROI retune). This brief executes T40's G1 (first
arm only). Reuse T40's scripts (`t40-auto.sh`, `t40-cropdiff.py`,
`t40-track.py`, `t40-analyze.sh`, `t40-vcount.sh` — copy, do not modify the
committed originals).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T40 verified (re-verify sha/size/rev/
  status/pad-binding INCLUDING `Left = Keyboard/Left` + NVM state before
  running; table the receipts). PRE-RUN GATE (T40 G8): C: headroom ~5 GB+
  (a full run needs ~3.5 GB VHDX growth) — if C: is short, STOP before
  booting (do NOT repeat R1's wedge; clearing YOUR OWN stale transfer
  copies is allowed, touching the owner's installs is forbidden).
- GATE NOTE (T40 G1 — gate on the HUD, not the frame): gameplay hops are
  large because the race never stops moving. Live-race criterion = race
  clock advancing + position/progress HUD read off VIEWED snaps. The
  LIVE-LIKE proxy (departed + non-panel + motion ×2) still gates the input
  phase in-script.
- THE DELIBERATE CHANGE (the ONLY one vs T40): dense-phase cadence
  `sleep 1` → `sleep 0.5` (blind `bsnap` capture + ns stamps, post-hoc
  scoring unchanged — capture-bound at ~70 ms + scheduler). Everything
  else identical: same 1 s Left hold (T38's input class), same pre-pair +
  10-snap shape, same frozen tracking (`t40-track.py` — NO ROI/gate
  retune in this brief; that's the OTHER arm, never both at once). Table
  the expected cadence + the T40 rail rates you intend to beat BEFORE the
  run. The question: does SCPS40 resolve the hold hop too at ~0.6 s
  exposures?
- The task: reproduce the T40 R2 chain (fresh boot → Cross → detector-TITLE
  → Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross
  on Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → LIVE-LIKE proxy gate → ≤3 attempts) to LIVE gameplay, then
  run the dense phase: pre-pair (no input, back-to-back) + ONE 1 s D-pad
  Left HOLD + 10 back-to-back post-hold snaps at `sleep 0.5`. Post-hoc:
  per-snap HUD + RDC-gate/SCPS40/SCPS120 tracking + whole hops + rail-rate
  comparison vs T40 (did SCPS40 stop railing the hold hop? does a gated RDC
  pair now straddle it?).
- If SCPS40 STILL rails the hold hop or RDC still floods: table the exact
  observed rates + noise floors and STOP with a recipe — one input variant
  per attempt, never blind multi-presses. ONE bounded variant allowed only
  if the hold provably never acted on live gameplay (pre-hold ≠ racing).
- Success bars (tabled, not verdicts): live race reproduced at a matched
  phase (HUD advancing, phase-match tabled); hold acted on it (pre-hold =
  racing with HUD read); sub-second dense series (HUD + tracking + hops,
  post-hoc scored); rail-rate comparison vs T40 + gated-pair verdict;
  full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T40 G2: single-shot
  scripts, runs ≤6 min (table any overrun with full dmesg coverage),
  preserve emulogs first, dmesg + btime + init age, UNFILTERED dmesg +
  event log around flaps, 534 ms holds NOT bisected, `;` chains only INSIDE
  one `wsl` call, one `wsl` call per ssh; post-run dmesg read IMMEDIATELY
  after the run ssh returns). COUNT AcceptAsync lines in the committed
  dmesg files with exact counts + uptime-stamp positions relative to the
  run window. Runs in flap windows must be effects-verified or re-run
  (T27 §4 rule). C:-FULL WEDGE RULE (T40 R1): if a run stalls with C:
  exhausted, treat as environment failure (forensics + fix + re-run, never
  score the wedged run).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T41/` (STANDALONE). Commit with `git add -f`,
  prefix `[T41]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + ONE 1 s hold + `sleep 0.5` blind series (≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T40 §T40-0 shape): build/input receipts (INCLUDING the
   Left binding), NVM state, T40 reference snaps present, free space both
   ends + C: headroom gate (~5 GB+ — STOP if short). Table the blind-capture
   cadence + post-hoc scoring plan + the T40 rail rates you intend to beat
   BEFORE the run.
2. Per attempt table: race reproduction (chain scores + live-race gate),
   hold row (control/duration/T+/pre-post HUD), post-hoc dense series (HUD
   + tracking + hops per snap), rail-rate comparison + gated-pair verdict
   (or exact non-resolution finding).
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T41/REPORT.md`: pre-run receipts, attempt tables, hold log,
sub-second series + tracking + comparison (or non-resolution finding +
recipe), WSL table, exact commands, gaps. Commit evidence (`[T41]`), stop.
