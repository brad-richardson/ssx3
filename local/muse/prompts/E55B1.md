# E55B Part 1 — host-deadline nondeterminism in EE scheduler

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `docs/research/review-2026-09-23-frontier-2.md` §B
and `local/research/E55A1/REPORT.md`. This is a **read-only** source
audit for E55, not an implementation. Hand back the evidence table;
the orchestrator decides the cycle-only event mechanism. No fork edit,
build, boot, device access, or push.

Pin fork remote `ssx3` at `293fd81` and read source from
`~/dev/ssx3-work/GB6/PS2Recomp` (clean G fold). E54F2 owns the only
live E-fork mutator; do not touch its worktree. You may write only
`local/research/E55B1/REPORT.md` and ≤5 MiB bounded text receipts under
`~/dev/ssx3-work/E55B1/`. Use targeted `rg`/LSP and small line ranges;
do not put whole `EeScheduler.cpp` or a large generated file in context.

Question: in a proposed `PS2X_DETERMINISTIC=1` mode, which **specific**
host-time reads, priority comparisons, waits and idle fast-forwards can
change EE event order or the guest cycle seen at the next dispatch?
The correct-behavior model uses guest cycle deadlines and a stable
same-cycle tie order; host time may pace an already chosen event but
must not choose the guest event/cycle. Alternatives are current
host-deadline selection and a partial cycle-only change that still
orders ties by host deadline. Work out a small event sequence whose
predictions differ before recommending a fix.

Evidence table:

1. Map `scheduleEvent`, `processDueDeadlines`, `waitForEvent`,
   `accountCycles`, VBlank and alarm creation. Cite exact file/line and
   call connections. Identify where `steady_clock::now`, `wait_until`,
   host-deadline sorting and idle fast-forward affect guest state.
   Note any independent memory/EE timer pathway that could still vary.
2. List all event kinds and same-cycle tie behavior in current code.
   Give a table for two events sharing a cycle but different host
   deadlines, one earlier cycle with a later host deadline, and idle
   wait early/late wake. Show current and proposed ordering/cycle.
3. Recommend **one** narrow deterministic-mode interface and focused
   scheduler tests for a Part 2 brief, with default mode preserving
   current behavior. Identify any event kind whose semantics are too
   uncertain to modify yet. Do not conflate this with fixed RTC,
   scripted pad neutrality, memory-card isolation or the hash tap.

Time box 20 minutes. If a source edge is unproved, report the gap and
stop there without guessing. Deliver the complete REPORT.md with pins,
exact commands, citations, bounded receipt SHA and gaps. Commit only
the explicit report `[E55B1]` with `Orchestrated-By: Codex`; no push and
no edits to board/ledger/plans.
