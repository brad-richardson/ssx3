# N8D7M12 Part 5M4 — one corrected Mac OFF control run

You are the N/G OpenCode Go run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full Part 5M2 + Part 5M3 REPORTs/gates and drivers, `local/research/N8D7M12/REPORT.md` Part 1, and this brief. Own only this brief, `local/research/N8D7M12P5M4/`, and the script's private `~/dev/ssx3-work/N8D7M12P5M3/` output. No source/binary/stream/fork edit, build, device action, push, board/global edit or upstream contact. One Mac replay command only, no retries. Time box 25 min, text <512 KiB; private outputs ≤64 MiB, log ≤16 MiB. Write REPORT/checker early. Hand back the table; orchestrator decides.

Goal: test whether Mac ON and OFF replay outputs differ when the same pinned binary and stream run with only the three tick2050 capture flags removed. Part 5M2 produced 41 rows and PPM byte-equal to ON, but failed the predeclared suite exit0 gate because a cwd-relative CodeGenerator header fixture was missing. Part 5M3 added only the private-cwd fixture. Hypotheses: flag effects at tick2050 may change output; otherwise OFF rows/PPM match ON. Any difference before tick2050 voids that causal comparison. The fixture should make the unrelated suite test pass; if it does not, report FAIL without overriding it from matching replay artifacts.

**Released SHA:** `local/research/N8D7M12P5M3/mac_off.py` = `1056304d3f2d559936f56ae189c419813581bb46f7cf35ae26d0dc7ad12d6738`. Before run, rerun `check.py --self-check` (39/39), Python compile, independent two-read SHA/size pins of binary `2a0446e8…68ae5`, stream `f6a78f71…a593`, ON hashes `94b433df…10c290`, ON PPM `9490484c…14fce3e`, fork HEAD `24801bc…5e8` and header `b8de8745…970cd`. Confirm private output absent; disk <200 GB with ≥1 GB headroom, one mini P-lane slot free, no heavy build. Stop without run if any fails.

Run **once** from repo root:

```sh
python3 -u local/research/N8D7M12P5M3/mac_off.py --released-sha 1056304d3f2d559936f56ae189c419813581bb46f7cf35ae26d0dc7ad12d6738
```

The driver owns one dynamic lease, ≤300 s wall, 16/64 MiB caps and own-PID cleanup. No debugger or broad process kill. If it exits nonzero, capture first failure and do not retry. The **acceptance gate** is binary exit0, suite `Total Tests: 585 / Passed: 585 / Failed: 0`, no failed test/error, parallel summary markers2050, tick2050 frame marker, exact 41 ordered ticks 50…2050, PPM, output caps, PID gone and lease released. A checker must verify these independently of the driver's provisional verdict, plus pins/unchanged ON inputs. Then compare all 41 `parallel.hashes` rows and PPM SHA/bytes to pinned Mac ON; classify equal as `flag-unperturbed`, any pre2050 mismatch as `void-pre2050`, or tick2050-only mismatch as a possible flag effect. If acceptance fails, final run verdict FAIL even if artifacts match. Diagnostic wall is never speed.

Deliver `REPORT.md`, read-only `check.py`, `check-result.json`, bounded driver/run-log excerpts, result JSON, 41-row comparison, exact hashes and absolute OFF PPM path for orchestrator viewing; no full binary/PPM/stream in git. Commit `[N8D7M12] Part 5M4` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Stop after the one run.
