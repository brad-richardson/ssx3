# G11 — Per-vsync scanout series: localize the first-black cause (existing dump)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G10.md` (all — 7 exact-geometry
512×448 pairs, k=0 black-vs-icon = paraLLEl-side scanout behavior, k=1..6
icon-vs-icon PSNR 44.6–47.5 dB, per-pass 272/272 reconciles G8 exactly,
§4 ONE next action) + `docs/reports/G8.md` §3 as needed (alignment table +
flush series WITH the erratum: per-pass reset REQUIRED). This brief
executes G10's §4.

## Facts you start from

- The comparison method is unblocked: exact pairs exist at every step k=0..6
  on the EXISTING dump (no recapture, no new content — a rich dump's first
  frame would hit the same question). The content gap: at identical
  post-vsync#0 state PCSX2 presents the loading icon while paraLLEl
  presents black, although paraLLEl's vsync#0 executed 34 prims.
- The task: trace what paraLLEl presents vs renders at iterate-true #0 and
  localize the first-black cause. Candidates (not claims): one-field
  presentation offset between the scanout pipelines, `consume_vsync_result`
  lag, scanout double-buffer behind the draw. Use G10's 7 paired PCSX2
  frames as the oracle; k=1..6 coverage deltas (PNG leads PPM ~60–80
  nonblack px/step) are the same queued work.
- Experiment contract (required): hypothesis = the per-vsync series names
  the stage where iterate-true #0's rendered content misses presentation;
  observable = per-vsync presented bytes vs rendered state at #0..#2 on
  both sides; alternatives = (a) cause localized to a named stage → table
  + ONE next action; (b) series inconclusive → table + recipe, stop. Stop:
  existing dump + existing 7-frame oracle only — no recapture, no runner
  changes beyond bounded read-only instrumentation, no tuning loop.

## Gates and rules

- Reuse G10 clone/build/dump (verify `3a66c197` HEAD + dump + PPM/PNG shas
  first; rebuild `-j2` only if the tree moved). Byte caps declared up front,
  tracked in ALLOCATED bytes; SSD reuse only, nothing new on `/`.
- No contention with other lanes: no recomp boots/builds, no P-lane lease,
  no fork writes, no `adb` installs. Bytesize PCSX2-side work (if any) is
  read-only inspection in `pcsx2-g7`, T28 recipe shape, no lease.
- Commit in ps2xGS as you go (prefix `[G11]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G11/` (`git add -f`,
  `[G11]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — per-vsync presented-vs-rendered series (existing dump only)

1. Series table: per iterate-true #k (k=0..2 minimum, through #7 if cheap),
   what paraLLEl presents (bytes) vs what it rendered (flush counts /
   draw state), same-boundary PCSX2 frame alongside.
2. Name the stage where #0's content misses presentation, or table the
   exact inconclusive point + the recipe that would resolve it.
3. Hypothesis verdict + the ONE next action the numbers justify.

## Report

Mirrored `REPORT.md`: contract, series tables, localization (or
inconclusive point), license receipts, exact commands, gaps. Commit both
repos as above, stop.
