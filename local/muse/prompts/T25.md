# T25 — NVM-verify + scripted path: one fresh boot, then the whole chain to title in one script (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T23/REPORT.md` first (all of it —
T23 reached the STATIC Settings-completed park (6/6 snaps
sha-identical) with an `NVRAM saved` marker — the premise of
Part 4 decision 7, this brief), plus T23's `t23-post*.jpg`
(the completed park) and T23 gaps G2–G5 (WSL precautions:
single-shot scripts, runs ≤6 min, preserve emulogs first,
re-check dmesg + btime + init age; capture UNFILTERED `dmesg`
+ the Windows event log around any flap; 534 ms holds, 10/10
reliable — do NOT bisect; static screens may use whole-sha,
animated need region compare). This brief ENDS one-brief-per-
button: verify NVM carry once, then script the whole path.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` (key
  auth). Devel PCSX2 v2.9.75 at `/home/brad/pcsx2-t4/pcsx2/
  build/bin/pcsx2-qt` (reuse, do NOT rebuild — table binary
  sha + size at start, must reproduce `6719f5d6…` /
  130929856 B or table the deviation). Inputs staged.
  Display: Xvfb `:99` + `QT_QPA_PLATFORM=xcb` (T17 recipe).
  Keys: xdotool focus+XTEST to the PCSX2-Qt WID (T17 path 1).
- Phase 1 — NVM verify (ONE fresh boot, NO inputs): boot
  fresh and table whether the setup chain is SKIPPED
  (lands past Settings-completed without presses?) or
  REPLAYED (language park again?). If replayed: copy this
  laptop's PCSX2 `.nvm` (the Sep 10 run had settings done —
  locate it on the laptop first, table path + size + sha,
  scp to bytesize) and re-verify once. Receipt: snap +
  vblank count + trace markers either way.
- Phase 2 — scripted path (ONE script, timed key sequence):
  a single `t25-auto.sh` driving the whole input path from
  wherever Phase 1 lands to the title screen AND first menu
  (presses at T+ offsets with the proven 534 ms holds, one
  hold per screen, screenshot after each transition +
  continuous same-4-channel trace). Stop at title/first-menu
  OR 1200 s wall, whichever first (still complete: table
  screens + inputs + trace).
- Single inputs only inside the script (one hold per
  screen, never blind multi-presses). If a hold does
  nothing, the script tables it and stops (no retries
  beyond ONE ≤1000 ms, tabled).
- NO lease of any kind (all boots on bytesize). No `adb`.
  No fork changes. No copyrighted downloads (inputs exist).

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce both phases from scratch.
- `export COPYFILE_DISABLE=1` on every ssh/scp step that
  touches the SSD (Part 4 hygiene 8a).
- Evidence dir `local/research/T25/` (STANDALONE). Commit with
  `git add -f`, prefix `[T25]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 6 h. Tables + receipts, no verdicts. If the NVM
  path fails AND the laptop .nvm is missing, table both
  exactly, commit what exists, stop.
- Hygiene: write the report in chunks with a tail receipt
  (truncated tail fails the gate); on ANY WSL flap capture
  UNFILTERED `dmesg` + the Windows event log before
  continuing.

## Task 1 — NVM verify (does the chain skip?)

1. Fresh-boot table: screen reached with zero inputs (snap
   sha + vblank count + trace markers — skipped or
   replayed, tabled exactly).
2. NVM table (iff replayed): laptop .nvm located? (path +
   size + sha) → copied? → second fresh boot skips?
   (same receipts — or table the miss).
3. Park table: the no-input stable park (screen + vblank
   count — the script's start state).

## Task 2 — scripted path (one script to title/menu)

1. Script table: `t25-auto.sh` steps (T+ offsets, holds,
   per-step screenshot + expected screen — the timed
   sequence, committed).
2. Epoch table: screens reached (guest events, log times,
   screenshot per screen — title? first menu? or where
   the chain actually ends).
3. Trace table: channels captured (same-4 format check vs T4
   samples), lines/bytes, paths + shas both ends (traces
   stay on bytesize if >1 GB, with head/tail samples
   committed).

## Report

`local/research/T25/REPORT.md`: T4-0..T4-4 SHAPE (NVM
phase, script, epochs, trace inventory, exact commands
both ends, gaps). Commit evidence (`[T25]`), stop.
