# E55D3 — bounded ordered pad/card guest-write tap (Muse Contributor Go)

You are an OpenCode Go E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55D2/{REPORT.md,ORCH-GATE.md}`, E55D1 gate, and the E55D3 entry in `docs/todo.md`. Create a private fork worktree `~/dev/ssx3-work/E55D3/PS2Recomp` on `e55d3-pad-card-tap` from exact `ddaee780288adb076ce40050d87969b20bc4bb05`; leave E55C2's pinned worktree untouched. External codegen stays `~/dev/ssx3-work/codegen-ssx3`, register SHA `8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`. No generated guest source in git. E55D3 owns only `Pad.cpp`, `MemoryCard.cpp`, and one small shared diagnostic header/source under `ps2xRuntime` plus necessary CMake/tests. G work is concurrent but owns only GS/test files. No upstream contact or push.

Implement one default-OFF `PS2X_PAD_CARD_PROBE=<file>` tap that records **post-write bytes** for every successful `scePadRead` 32-byte fill, every nonempty `sceMcGetDir` guest table copy (`entryCount * 64` bytes), and every positive `sceMcRead` payload. A shared monotonic sequence must order all three families; include guest vsync tick, family, per-family/read ordinal if available, port/slot or fd, guest destination/length, and exact bytes (hex or lossless binary) with unambiguous record framing. Record enough status to distinguish skipped/failed calls in counts without claiming they wrote bytes. Do not change guest writes, results, timestamps, pad merge, or thread scheduling. Set a hard 16 MiB output cap with one explicit cap record and a disabled-after-cap state; no truncated payload record. Flag unset/empty must avoid file I/O and payload scans. One process-local log is enough; protect write/sequence under a lock if concurrent calls can interleave. If the design cannot meet all coverage and cap requirements in these sites, stop and hand back the exact gap before implementing a weaker tap.

Validation: configure Release/Ninja in `~/dev/ssx3-work/E55D3/build-taps` with E55C2 `build.sh on` flags and the pinned fetched dependency dirs; build `ps2x_tests` and `ps2EntryRunner` **once**, then run the full suite with the flag unset. Add one meaningful focused test only if the existing stub test harness can exercise at least pad+card ordering or the cap; do not create tests that copy the encoder's logic. A failure permits one named compile/test repair and one rebuild, then stop. No boot or device in this part. Verify the flag unset path and no probe output in suite, `git diff --exit-code 14b1e5cb HEAD -- ps2xRuntime/src/runner`, disk cap 200 GiB before/after, and no generated/binary files staged. Keep new E55D3 bytes <5 GiB and text receipts <512 KiB.

Deliver `local/research/E55D3/REPORT.md`, exact build/config/suite result, source patch and fork commit SHA, changed-file list, cap/record schema, and explicit untested live-coverage gaps. Commit fork source on local branch as `[E55D3]` with `Orchestrated-By: opencode`, no fork push. Commit only named text receipts and this brief in ssx3 as `[E55D3]` with the same trailer, no push. **Hand back the evidence table; the orchestrator decides the gate and any A/A boot.**
