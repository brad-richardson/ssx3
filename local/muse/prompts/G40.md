# G40 — Sub-vsync A→B wall at boundary 1 on Odin (F1/F2/F3 split), then ONE candidate fix if the split is decisive

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD clone
`parallel-gs-g7` @ `3a66c19` + the carried G26/G28 hunks — VERIFY at
start). **Tables + receipts; you may recommend, the orchestrator
decides.** Read first, all of each: `local/research/G38/REPORT.md`
(rule 4: Mac-B lands composite#1's alpha-masked blit at boundary 1,
Odin-B stays at load; §2 F0–F4 table, §4 discriminator design) and
`local/research/G39/REPORT.md` (both crash fixes HOLD; binary
`c91719a0…` reused; the run shapes and verify chain you inherit).
No upstream contact of any kind.

## Facts you start from (gated — do not re-litigate)

- G39 GATED PASS. Adoption mechanics DECIDED: carried-diff now
  (G26 + G28 stay uncommitted in the SSD clone; ssx3's committed
  diffs are the record), in-clone commit after the storage cutover.
  Do not commit in the clone this brief.
- G38 localized the fault: composite#1+ draws are SUBMITTED on Odin
  (`passes=2` both sides) but B never changes; scene A is byte-exact
  cross-GPU; sampling is proven exact (G32/G34–G37). F1 rejected
  draw / F2 write-back lost / F3 stale texture read all predict
  Odin-B == load at vsync granularity — this wall splits them.
- Correct-behavior model (F0) is KNOWN from the Mac: at boundary 1
  B must equal Mac-B `840cd308c91c4d8a` / 699,122 nonzero bytes.
  Every observable below has an F0 prediction taken from the Mac leg.

## Mission 1 — observation wall (one hunk, one build, ≤2 Odin runs)

Add ONE observation-only hunk (no behavior change; env-gated, default
silent) around the first composite pass at boundary 1 (span packets
#511..#1021, phase 0) that logs:

| # | observable | F1 predicts | F2 predicts | F3 predicts |
| --- | --- | --- | --- | --- |
| O1 | draw-accept count for the 17 composite prims (after cull/degenerate/resource checks) | < 17 (0 likely) | 17 | 17 |
| O2 | B bytes pre-composite-flush (FNV + nonzero count) | == load | == load | == load |
| O3 | B bytes immediately post-composite-flush, read back after an explicit full barrier + wait-idle | == load | == load | == load (blit of load-A) |
| O4 | the composite's texture input, read through ITS OWN descriptor/view at the draw (FNV of the sampled region) | n/a | == scene#0 (bright) | == load-A (stale) |
| O5 | flush reason (FBPointer vs TextureHazard) + render-pass/attachment the draws target (image, view, layout, format, write mask) | — | tabled | tabled |

Also run the SAME hunk on the Mac once (you own a Mac build slot; G22
recipe) so every O-row has an F0 column from a real run, not a
prediction. Budget: ONE Mac build, ONE Odin build, ONE Mac run, TWO
Odin runs (second only if the first is VOID with a named cause).
Verify-then-push with no gap (G39 §3b chain). Split verdict table:
exactly one of F1/F2/F3 (or OTHER, with its bytes).

## Mission 2 — ONE candidate fix (only if Mission 1 names one mechanism)

If Mission 1 names a single mechanism with the bytes to back it, you
may apply ONE behavioral hunk that addresses exactly that mechanism
(e.g. a missing barrier/layout transition, a rejected-draw condition,
a stale descriptor), rebuild once, and do ONE Odin run in G38's O1
shape. Acceptance: Odin-B at boundary 1 == Mac-B (`840cd308…`), and
the Odin scanouts change from 10/10 black to the Mac's content
pattern (1-frame lag). Record whether the G26/G28 regression
observables still HOLD in the same run. If the fix does not land B,
table it and stop — no second hunk, no tuning loop. If Mission 1 is
not decisive, skip Mission 2 and recommend the next wall.

## Rules

- Device: Odin3 `622c49b1`; `/data/local/tmp/g40/` only, removed at
  end. No P-lane lease (E29 holds it on the Mac host). Check
  `/data/local/tmp/mg/LEASE` is free before each run; nobody else is
  booked on the Odin this window.
- SSD: 2 matching reads separated in time for every lane-critical
  byte; `COPYFILE_DISABLE=1`; mirror receipts to the share tier. New
  SSD dirs ≤ 6 GB build + ≤ 50 MB `ps2x-g40/`; older dirs 0 growth.
  Mac internal volume has ~2.8 GB free — nothing big there.
- Hunks stay uncommitted in the clone; commit the diff texts into
  `local/research/G40/`.
- Time box 6 h. Evidence `local/research/G40/REPORT.md` + `[G40]`
  commit with `Orchestrated-By: Muse Code` trailer, `git add -f`, no
  push. Check `git log -1` right before committing (other lanes
  commit to main too).

## Success bars (tabled)

O1–O5 on Mac and Odin; F-split verdict; if Mission 2 ran, the fix
hunk, its B/scanout result vs Mac, and G26/G28 still-HOLD rows;
byte caps; the ONE next action.
