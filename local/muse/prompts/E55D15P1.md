# E55D15 Part 1 — inventory a real SSX 3 save source (read-only)

You are the E-lane OpenCode Go evidence worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full E55D14 Part 2B REPORT/gate and E55D13 REPORT/gate, plus E55D6 REPORT/gate. Cite local receipts, not web. Own only this brief and `local/research/E55D15P1/`. No game boot, build, fork/source/card edit, save seed/copy, extraction, device/lease action, push, board/global edit or upstream contact. Time box 25 min; report text <256 KiB. Write REPORT/checker early.

Question: does an already local, legitimately acquired **valid SSX 3 (USA)** save exist with enough provenance to design one private seeded-card test? E55D14 observed stock Load game query at tick1740: raw `BASLUS-20772-GAM*`, normalized `/BASLUS-20772-GAM*`, empty parent, host root `mc0`, max6; five status rows returned empty and no mcRead. `BASLUS-20772` was previously only a name string; do not infer that a matching name alone is a valid save.

Read-only inventory of bounded likely locations: existing ssx3 lane receipts and private `~/dev/ssx3-work` card/save-related directories, existing PCSX2 memory-card or save-export locations under `~/dev`, and the mounted share tier if accessible. Prefer filename/size/SHA and manifest/provenance receipts; do not scan unrelated personal files or print payload bytes. Report candidate path, format, region/game ID evidence, source/provenance, file list and sizes, two matching SHA reads for any candidate later used, and whether a host-directory layout can be established from the runtime `GetDir` implementation. If the candidate is a whole memory-card image, identify the needed extraction tool/process **without extracting now**. Inspect source with LSP for any claimed GetDir path mapping, and cite exact lines. Mark unknowns and conflicts plainly.

Predeclare A/B/OTHER: A = one existing save has evidenced provenance, correct region, exact wildcard-compatible file/directory name and sufficient layout to design a separate bounded seed; B = no such candidate or provenance/layout gap; OTHER = inventory blocked. A is a design input, **not** proof that the guest accepts the save. Do not create an invented file, seed a card, or claim a successful read. Give the smallest next observation for B. Deliver `REPORT.md` with a concise candidate table, exact commands, paths/SHAs when present, a checker for pinned claims, and no raw save bytes. Commit `[E55D15] Part 1` with `Orchestrated-By: opencode`, explicit paths, no push. Hand back the table.
