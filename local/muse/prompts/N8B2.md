# N8B Part 2 — one Odin Turnip app-process launch

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §6 and
`local/research/N8B1/REPORT.md`. Hand back tables/receipts; the
orchestrator decides the gate. Named paths are your write scope; “first
failure” means a failed brief step, not an exploratory typo. This is
**one install and one Odin launch**, no build, fork edit/push, iPhone or
upstream contact.

Pin the final N8B1 bytesize APK
`/home/brad/n8b1/PS2Recomp/android/app/build/outputs/apk/release/app-release.apk`:
153,687,506 B, SHA-256
`1096a28e2e343fbcfc1d28390ea72a8e3d2aee32f3c40b81102e773c50b190da`.
Runner member SHA `cdbaa6dd6eaf77a9026fb1fc5a291c15054f4d396e44b37ee3655705d10eb033`,
Build ID `28340bbe3652b251a6ecda201cf6ae01f6550add`; bundled Turnip
member SHA `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`.
Source branch `n8-turnip-apk` HEAD `17e90de`, based on fork `4f93216`;
the newer E55B2 scheduler is not in this APK. Copy the APK to
`~/dev/ssx3-work/N8B2/` with two matching SHA reads at source and
destination. Do not place binaries in Git. Odin serial `622c49b1`.

## Discriminating gate

Hypothesis: the Android app namespace can load bundled Turnip by
basename, `HMI` opens `vulkan0`, Granite uses its non-null proc address,
and the in-process paraLLEl backend reaches a visible stock race frame.
Alternatives and stop evidence: `dlopen` dependency/namespace error;
`dlsym`/HAL/open/get-proc failure; Granite/device init failure; system
Vulkan or CPU fallback; successful Turnip init but no GIF/presents or
no race frame. A packaged ELF alone is not a runtime pass.

1. Read `local/research/N7/REPORT.md`, `local/research/N7/launch.py` and
   `local/research/I26/ROUTES.md`. Copy/adapt the launcher under
   `~/dev/ssx3-work/N8B2/` or write a bounded equivalent. Before APK
   launch, check Odin connected, `/data/local/tmp/mg/LEASE` free,
   `KeyguardServiceDelegate showing=false`, battery >=20% and charging
   (status 2 **or full/charging status 5**). If locked, low, uncharged or
   leased, stop and report immediately. Claim lease as N8B2; release on
   every exit. Always `adb install -r` even if app present, after APK
   hashes. Force-stop after the run and verify PID gone.
2. Double-read SHA-256 of installed APK and the already staged stock
   `files/SLUS_207.72` ELF and `files/SSX3.iso` on device before use.
   ELF pin `1b49d05ca2793922180851b9e1ce9ae2291d61a7863565ac4e71f12e967af7bc`
   (`local/research/E55B2/REPORT.md`); ISO pin
   `3c2f8eb182c9c6208a6e8172a41e61c98f420abe3f42c845f6829aeb9761ebf5`
   (`local/research/N7/REPORT.md`). Fail on mismatch, do not launch.
   Write `files/ps2x.env` with exact `PS2X_GS_BACKEND=parallel`,
   `PS2X_GS_TURNIP=1`, dev-only `PS2X_SKIP_MOVIE=1`,
   `PS2X_CD_IMAGE=<files>/SSX3.iso`, and the I26-FAST vsync-clock pad
   route. `PS2X_VSYNC_RATE_LOG=1` is allowed solely for progress; do not
   quote speed. Use an isolated empty card if the N7 route requires it.
3. One launch of `com.ps2x.runner/android.app.NativeActivity`, wall cap
   600 s, title progress cap 180 s, race target tick >=2050 if reached.
   Capture bounded same-PID logcat, one title/menu image and at most two
   race images (tick targets about 1810/2050), bounded SF latency/present
   and GIF/backend counters, and the first fatal/loader error. Stop early
   on a definitive requested-Turnip failure. Inspect frames yourself.
   No second launch, profiling or tuning. On every outcome force-stop,
   confirm PID absent, release lease, and retain SHA-paired images.
4. Table: source APK/install/ELF/ISO SHA pairs, preflight/lease/PID,
   same-PID `dladdr(HMI)` mapped APK path and HAL operation addresses,
   Granite device/API/driver identity, `[gs:parallel] init` outcome,
   nonzero GIF/presents and visual race evidence. G43's expected Turnip
   identity is Adreno 830, Vulkan API 1.4.359, driver 26.2.99; the system
   control was API 1.3.284, driver 512.800.58. If identity log is absent,
   say unproved rather than infer from a bundled file. Report crash,
   timeout or first unmet gate with exact bounded log lines.

Budget: one install/launch, <=600 s absolute, <=1 GiB new Mac bytes,
<=32 MiB text logs, <=10 MiB images. Check disk budget before/after a
>1 GiB write (none planned). Deliver `local/research/N8B2/REPORT.md`
and <=2 MiB selected text receipts/images there; large APK/logs stay
under `~/dev/ssx3-work/N8B2/`. Commit only explicit report/text/small
images `[N8B2]`, trailer `Orchestrated-By: Codex`; no push.
