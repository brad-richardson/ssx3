# N8D5 Part 3 — explicit shader layout and Mac compile gate

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. Work only in `local/research/N8D5C/` and a fresh fork worktree `~/dev/ssx3-work/N8D5C/PS2Recomp` from local N8D5B candidate `ef344024ff1a899d56cd3df21545caa7ad706c5f`. Use the existing private G43 copy `~/dev/ssx3-work/N8D5B/parallel-gs` **read-only**, pinned at `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd`, Granite `16e7395f6a4858c1783dbf6f521f90b9d5f82ac5`, with N8D5B's carried diagnostic hunks and shader header. Do not edit shared fork `ssx3`, N8D5B candidate worktree/G43 copy, runner dir, generated guest code, or other sources. No Odin, bytesize, APK, game boot or push.

Evidence and mechanism: `local/research/N8D5B/REPORT.md` and `orch-replay-excerpt.txt` show the default-OFF GPU tile candidate compiled/suite 585/585 but its Mac control failed because the standalone paraLLEl build forces `GRANITE_VULKAN_SPIRV_CROSS=OFF` (G43 `CMakeLists.txt:38-41`). The candidate's `request_program` call omitted `ResourceLayout`; Granite `device.cpp:403-409` passed null through to `Shader`, which logged missing reflection (`shader.cpp:1095-1103`), MoltenVK rejected the compute pipeline and Granite dropped dispatch. The worker's sandbox blocked Vulkan instance creation; the orchestrator's outside-sandbox replay reached tick 2050 and measured raw Mac 567/896 active tiles, 128,292 occupied pixels, but GPU words were invalid. X14's local-model table failed acceptance; use the source mapping the orchestrator independently verified: GLSL `sampler2D` at set 0/bindings 0,1 → `sampled_image_mask=0x3`, `fp_mask=0x3` (`shader.cpp:909-925`); SSBO at binding 2 → `storage_buffer_mask=0x4` (`:1003-1014`); two-`uint` push constant → size 8 (`:1040-1047`). `ResourceLayout`/descriptor fields are in Granite `shader.hpp:52-60`, `descriptor_set.hpp:47-61`; `request_program(...,&layout)` is the existing pattern in G43 `gs/shaders/slangmosh.hpp:19684-19705`. Confirm exact source connections with LSP if available; report empty/error plainly.

**One repair:** supply an explicit `Vulkan::ResourceLayout` with those fields and push constant size to the N8D5B `request_program` call. Keep the shader, control texture, tile thresholds, tick/page gate and raw copy unchanged. Default flag OFF must preserve presentation. Before edits write a short `REPORT.md` with A/B/OTHER gate: Mac PASS requires control=128 and both GPU sampled/raw active tile counts >=500 at tick2050/FBP112/PMODEff21/512×448 on the pinned N8D4 stream; failed control, pipeline error, mismatched alignment or off-table pair is OTHER and stops before Odin. Mac PASS only validates the probe on Mac; it does not diagnose Odin.

Budget: one source candidate, one Release/Ninja configure/build of `ps2x_tests` with `PS2X_GS_SHADOW_PARALLEL=ON`, `PS2X_PARALLEL_GS_SOURCE_DIR` the pinned private G43 copy, canonical E54F2 external `PS2X_GAME_CODEGEN_DIR=~/dev/ssx3-work/codegen-ssx3`, diagnostic taps/runtime/aggressive logs OFF; one taps-OFF suite from the new fork worktree root. One named compile-repair retry if needed, no tuning loop. **Do not attempt the Vulkan replay in your sandbox**; pin binary SHA twice and hand the exact replay command to the orchestrator, who will run one outside-sandbox same-stream replay and gate it. Scratch ≤4 GiB, committed text ≤4 MiB, global disk <200 GB. Verify `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty before a local fork commit.

Deliver `local/research/N8D5C/REPORT.md` with exact source diff, source/stream/codegen pins and SHA pairs, commands/flags, build/suite counts, byte caps, and Mac replay explicitly pending. Commit the one fork source file locally on branch `n8d5c-layout` as `[N8D5C]` with `Orchestrated-By: Codex`; commit only bounded ssx3 receipts as `[N8D5C]` with the same trailer. No push. Hand back the table and binary path/SHA; do not declare a device verdict.
