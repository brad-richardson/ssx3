# E55D14 Part 2A — prepare one empty-card path-observation boot (no boot)

You are the E-lane OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full E55D14 Part 1B REPORT/gate, E55D12 Parts 1/2 REPORTs/gates, and `local/research/E55D12/e55d12_boot.py`/checkers. Cite source/receipts, not web. Own this brief and `local/research/E55D14P2A/` only. Fork/runner/codegen/ISO/ELF are read-only. No boot, build, card seed/copy, device/lease action, fork/source edit, push, board/global edit or upstream contact. Time box 25 min; text <512 KiB. Write REPORT/checker early.

Prepare one reviewed-SHA-held script by copying E55D12's proved Load game route and run controls. Change only the output lane, fork/runner/revision/SHA pins, and the receipt checks necessary for the new `getdirpath` sibling; preserve all nine pad pulses, frame targets, empty-card input, PID/mini-slot cleanup, 16 MiB probe, bounded log/frames and no-reuse guard. New lane `~/dev/ssx3-work/E55D14P2/run/S1`; fork `~/dev/ssx3-work/E55D14P1/PS2Recomp` at `80777cb36d5f84c1cd89122e8074afd48fe78b8e`, runner `.../.work/build/ps2xRuntime/ps2EntryRunner` SHA `d8fa114d824a277592558425dd91357bcf2f75d0d09390a5680c41ba6002ef04`; ISO/ELF/codegen pins stay E55D12's. The fork has **only `.work/` untracked**, so precheck must reject any tracked modification or unexpected untracked path but allow `.work/`. Keep the runner-dir guard vs `14b1e5cb`. Ensure active boot plus frame-proof grace cannot exceed 600 s without explicit orchestrator release; stop earlier on target. Preserve E55D12 script's `--self-check` pure no-boot path.

Predeclare an acceptance table: same pre/post Load game frames, exact pad route, five `getdir` status rows at the observed menu ticks (four early, one post-choice) and five `getdirpath` sibling rows paired by per-family ordinal/tick/port/slot/max; the new sibling shifts shared `seq`, so do not compare old global seq. A = post-choice path row reveals escaped raw query, normalized query, parent, pattern and host while the card stays empty and no copied bytes appear; B = path observed but scene/status differs (void for seed design); OTHER = no path, cap, input or preflight failure. Do not infer a valid save or determinism. `getdirpath` is dev-only/default-off. A future seed is a separate release.

Deliver `REPORT.md`, `e55d14_boot.py`, `check.py` with computed pin/route/no-run assertions, self-check output, exact script SHA, bounded planned commands and gaps. **Do not run the boot script beyond `--self-check`**; a separate part receives the approved SHA. Commit `[E55D14] Part 2A` with `Orchestrated-By: opencode`, explicit named paths, no push. Hand back the table.
