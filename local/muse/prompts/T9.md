# T9 — ladder_diff waiter-phase rule: waiter presence is SAMPLED, histories are the signal (0 boots)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T5/REPORT.md` §T5-7 first (the D
verdict: 1054 exact / 245 tol-ok / 2 KEY_DELTAs on
`sema.29.table` + `sema.32.table` waiter presence, 0
COUNT_DELTAs), plus `local/research/T1/REPORT.md` §T1-3 (the
verdict-class semantics you extend). The orchestrator proved from
the committed snapshots that waiter presence is a deterministic
function of the SIGTERM-sampled thread states (BASE t1-WAIT29 ⟺
sema29.waiters=1 / t5-Ready ⟺ sema32.waiters=0; RULE mirrored —
with cumulative 29/32 histories balanced both sides). The tool
classes live-thread status SAMPLED but waiter counts exact: that
asymmetry is this brief's fix. Peer lanes run concurrently —
read-only on other agents' files; named adds only.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3`, HEAD `1a76df4` at brief time (T5's rule — table the
  actual HEAD at start; record don't fight).
- Tool: `$R/tools/ladder_diff.py` (stdlib-only, no build refs).
  Snapshots to re-verdict (committed, read-only):
  `local/research/T5/park-snapshot-base.json` +
  `park-snapshot-rule.json` (D reruns exit 1 with the same 2
  KEY_DELTAs — reproduce this BEFORE changing the tool or
  table the mismatch and stop).
- The principled rule (not post-hoc): sema-table waiter counts
  are wall-clock samples exactly like thread pc/status — exact
  only when the sampled thread states agree on the wait; the
  deterministic signal is the cumulative wait/signal histories
  (already TOL_OK-gated). Implement: waiter-presence rows read
  SAMPLED when the complementary thread-wait sets differ,
  EXACT when they agree; print the correlation (which thread
  ⟺ which sema) on every such row.

## Gates and rules

- NO lease (0 boots — re-verdict existing snapshots only).
  `adb` not used.
- Fork: `tools/ladder_diff.py` + its tests ONLY. NOTE: the tool
  currently has NO test file — create the test next to the tool
  (`tools/test_ladder_diff.py`, stdlib unittest — no build refs;
  record the call). `git add` those NAMED files only — never
  `-A`; never stage generated/ignored files. `git pull
  --rebase` before pushing; `git push` ONLY inside the fork
  clone to the `fork` remote. Stop on any foreign rebase
  conflict. Purge `._*` before staging (ExFAT precedent).
- Evidence dir `local/research/T9/` (STANDALONE). Commit with
  `git add -f`, prefix `[T9]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — the rule (tests first, then tool)

1. Reproduce D exit 1 with the 2 KEY_DELTAs on the committed
   snapshots (unmodified tool) — the BEFORE receipt.
2. Tests first (`tools/test_ladder_diff.py`): waiter rows read
   SAMPLED + correlation printed when thread-wait sets differ
   (T5's two snapshots as fixtures — copy, don't modify);
   EXACT when they agree (same snapshot vs itself); histories
   still TOL-gated (unchanged verdicts on T1's D1/D2 pairs —
   rerun both, table old-vs-new verdicts per row-class).
3. Implement the rule. AFTER: full test-file green + T1's D1/D2
   re-verdicts tabled (any verdict change outside the waiter
   class = table it exactly, don't ship it silently).
4. Commit + push per the rules above.

## Task 2 — re-verdict (close T5's proof)

1. Re-run D (base-vs-rule) with the new tool: table the new
   verdict line + the two ex-DELTA rows (expected: SAMPLED
   with correlations; if anything else, table it exactly).
2. State the T5-proof closure as a TABLE (rule + counts +
   suite + behavior rows + new D verdict — the numbers only,
   no verdict word beyond the tool's exit code).

## Report

`local/research/T9/REPORT.md`: BEFORE/AFTER D tables + rule
spec + test table + T1 D1/D2 re-verdict tables + T5-closure
table + gap rows. Commit evidence (`[T9]`, no push), stop.
