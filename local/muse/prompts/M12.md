# M12 — Same-frame partition: alternating NOP streams + dual ref2 references

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M11/REPORT.md` (all of it: the cross-frame partition
rows, "What I could not do" item 2 — same-frame partition via
alternating NOP streams with dual ref2 references — which IS this
brief — plus items 1 and 3). Base header
`local/research/M11/m11_replay_context.h` (sha in the M11 report —
record `shasum`, not memory; copy it to
`local/research/M12/m12_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M12/` (commit with `git add -f`, prefix
`[M12]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m12/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M12/` as a research header derived from M11's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M12\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M12/waits.log`. A P1t agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M11/build_replay_player.py` to
  `local/research/M12/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M11 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m12-det`, `m12-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M11-header copy, run `m12-det`
   once with an M11 true-delta env (slot 0, RAMREF), confirm M11's end
   state reproduces (`gt0=100`, uniform, guest/event clean). Control.
2. **Alternating NOP streams.** Extend the NOP machinery: within ONE
   run, alternate which path is NOP'd across replay halves (or
   interleaved windows — your design, record it) with DUAL ref2
   references (one pristine ref per window shape). Receipts: per-window
   ref2 rows (gt0/uniform/xd2/xdmax/xdmean + refhashes) for
   shared-removed, indexed-removed, and texture-removed windows on the
   SAME recorded frame — the partition M11's cross-frame arms could
   not subtract.
3. **Slot-36 indexed decode.** Run the M11 decode event on slot 36
   (`SSX_M12_SLOT=36` on this header fills `tex_exp_draws`; M11 gap 3):
   per-frame indexed-Tex2 exposure of slot 36. Receipts: decode table +
   cross-checks vs the M9 VAT census. (Per-draw depth, M11 gap 1:
   attempt only if box time remains AND a header-only path exists;
   otherwise restate as open with one new sentence.)
4. **Report.** `local/research/M12/REPORT.md`: per-step tables
   (same-frame partition rows with the subtracted shares, slot-36
   decode), wall rows, continuation, header diffs per step (`diff -u`
   against the M11 header), exact commands, player and run dir paths,
   "What I could not do". No verdicts. Commit `local/research/M12/`
   (header, driver copy, analyzer changes, REPORT, waits.log). Stop.
