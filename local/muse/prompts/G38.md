# G38 — First-composite-pass comparison, Mac vs Odin: the A→B producer/composite boundary (NO injection)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS` — VERIFY at start, re-pin if
the Mac-mini migration moved it, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts
beyond the hypothesis.** Read first, all of each:
`local/research/G30/REPORT.md` (scene at FBP0, composite targeting
FBP112), `local/research/G31/REPORT.md` (B stale-unchanged at every
sampled boundary while A changes), `local/research/G33/REPORT.md`
§2c2 (B is zero-RGB — cleared output is the CORRECT render), and
`docs/research/review-2026-09-22-progress-and-parallelization.md`
§correction (the G30/G31 mis-sampling inference is WITHDRAWN;
G32/G34–G37 are positive controls). This brief REDIRECTS the lane to
the reached divergence. No upstream contact of any kind (standing
no-upstream order — local hunks only, filing stays local).

## Facts you start from (gated — do not re-litigate)

- The withdrawn premise: cleared scanout from stale B is CORRECT
  behavior (frag source replaces alpha with 0x80 on PSMCT24;
  verified git-object-equal). There is no sampler fault on this
  observation. The alpha-only successor is DECLINED (exact-alpha-
  only ≡ cleared — a non-discriminator). No color injection in
  this brief: remove the treatment, retain only observation.
- The reached divergence: scene region A (FBP0) CHANGES across the
  run while composite destination B (FBP112) REMAINS at loaded/
  stale contents. G30 recorded the composite pass targeting FBP112;
  G31 confirmed B unchanged at 16/16 sample times.
- Task: compare the SAME first composite pass on Mac and Odin at
  matched packet/pass/field boundaries. Observe source A →
  composite state + selected draw → destination B before/after →
  final scanout. Locate the FIRST differing input, rejection,
  write, or dependency. Preserve game/reference timing alignment
  (same dump `154d9d85…`, same iterations/flags shape as prior
  lanes unless Task 1 justifies a change with receipts).
- The Mac side is the reference: build the replayer for the Mac
  (new build dir, recipe from the repo's Mac path — audit it
  before promising anything) and run the same dump with the same
  observation. The Odin side reuses committed G30/G31 receipts
  where they already witness a boundary, plus AT MOST one new
  observation-only run (see Task 2).
- Standing SSD rule (link PROVEN pattern-dependent corrupt):
  lane-critical SSD bytes need 2+ matching reads separated in time
  AND independent corroboration; single-read SSD evidence is
  suspect. Mirror new receipts to the share tier.
- Process correction (applies to this brief): EVERY experiment
  tables "the implementation is behaving correctly on these
  inputs" as an explicit alternative with pre-registered
  predictions. Equal predictions cannot discriminate — if the
  correct-behavior model predicts the same bytes as a fault model
  on some observable, say so BEFORE the run and do not claim a
  split on that observable.

## Gates and rules

- Task 1 (no device, no Mac run until §2): STATIC design — table
  the matched boundaries (which packet/pass/field marks the first
  composite pass on each side, with byte receipts for the match);
  the observation set per boundary (A bytes + state + draw call +
  B before/after + scanout, each with its witness); the
  correct-behavior predictions per boundary (what Mac SHOULD show
  if the composite is correct); the fault alternatives (rejected
  draw? unwritten B? dependency stall? wrong target?) with
  per-observable predictions; the decision matrix (first-
  difference triage rules BEFORE any run). Name the excision +
  any observation hunk.
- Task 2 (bounded runs): AT MOST one Mac run + one Odin run of the
  SAME dump/pass shape. Hunk budget: excise G37's injection block
  (diff survives committed — lossless) and add AT MOST one
  observation-only hunk (retained-dump shape; no content writes).
  Build budget: ONE Mac build (new dir) + reuse of the existing
  observation binaries where valid (re-verify shas; do NOT rebuild
  the Odin observation binary unless no valid one exists — state
  which and why). Verify-then-push with NO gap for any new Odin
  binary; tombstone census; explicit pull lists (G31-E1 rule).
- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g38/` transient
  dir ONLY, removed at end (`mg/` only). No P-lane lease, no
  bytesize/WSL. `COPYFILE_DISABLE=1` on all SSD steps.
- SSD clone: hunk budget above; G22 + G26 + G28 + G29 + G30 + G31
  hunks untouched (HUNK_MATCH re-verified pre + post + pre-push +
  post-run + report); zero commits in submodule, zero in ps2xGS.
- Byte caps: NEW build dir(s) ≤ 6 GB total; NEW SSD `ps2x-g38/`
  (retrieval by explicit list) ≤ 50 MB; all older SSD dirs 0 growth
  (pre + post `du -sk` must match exactly); internal volume ≤ 1 GB
  delta, no clones/builds (Mac build goes on SSD); committed text
  only.
- Stop: any validity-chain pin mismatch → table + stop; no tuning
  loop, no second shape, no lldb unless a tombstone needs triage.
  Mac-run failures are OBSERVATIONS (table + stop that leg — do
  not fix the Mac build beyond the recipe; a broken Mac recipe is
  itself a finding for the next brief).
- Adoption explicitly OUT of this brief: G26+G28 adoption gets its
  own regression/adoption brief (G39), not a rider here. Filing
  stays held (its content must be rewritten to drop the sampling
  inference — owner call, not this lane).
- Time box 8 h (two-sided comparison). Evidence
  `local/research/G38/` + `[G38]` commit + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Success bars (tabled)

Boundary-match table with receipts; per-boundary observation table
(A/state/draw/B-before/B-after/scanout, Mac vs Odin); the FIRST
difference named with bytes (or NO-DIFFERENCE with the covered
span); correct-behavior alternative honored per observable; the ONE
next action (fix shape vs next wall — adoption stays queued).
