# G27 — Scoped sanitizer on the flag's full-upload path: name O6's heap-corruption writer (ONE instrumented with-flag run, Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G26/REPORT.md` first (all of it: the
narrower hunk delivers ONLY the flag and O6 persists byte-identically at
the same Scudo site — the FLAG's full-upload path is IMPLICATED, the
ride-along EXONERATED, pixels still N/A). This brief executes G26 §4's ONE
next action ONLY: instrument the implicated path and NAME the writer. No
upstream contact of any kind (standing no-upstream order — local hunks
only, filing stays local).

## Facts you start from

- The target: the `disable_sampler_feedback` fallback in
  `gs_interface.cpp` (the `:1698` + `:1761` gate region and the CPU-upload
  heuristics it selects — see G26 §4 + G15/G16). O6 is detection-at-free
  (Scudo `corrupted chunk header` inside `DescriptorSetAllocator::clear` →
  driver `free`, 16 ms post-Total-time, pre-first-write) — only an
  instrumented allocator sees the writer.
- Sanitizer choice (tabled BEFORE building — exactly one): ASan
  (`-fsanitize=address` + `wrap.sh` + `libclang_rt.asan` staging per the
  NDK docs) vs HWASan (hwasan build + `wrap.sh`; needs allocator
  interceptors to load on Odin3/Android 15 — verify loadability FIRST
  with a trivial instrumented probe before committing the full build).
  If NEITHER loads on-device: receipt the obstacle (loader errors,
  `wrap.sh` behavior) and STOP — do not invent a second shape (no
  LOGI-allocation tracing, no host-side guessing).
- Build in a NEW SSD build dir (G22 recipe + sanitizer flags, `-j2`,
  `parallel-gs-replayer` target). Keep the G26 narrower hunk in the
  tree (the tested shape stays flag-only — table the hunk state; ZERO
  new source edits beyond sanitizer-necessary build flags... and if a
  sanitizer build needs a source workaround, table it as its own hunk
  and count it against the one-hunk budget below).
- ZERO-DAMAGE IS ACTIVE (5th recurrence on the G26 binary, within
  ~minutes): the standing verify-then-push rule applies with full
  force — host re-sha → push → on-device sha match → run with NO
  gaps; re-verify immediately pre-run. If a pre-push re-sha ever
  misses, rebuild + re-verify before pushing.
- The run: ONE bounded WITH-FLAG run (same dump/iterations, flag SET,
  `PGS_SKIP_…` UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT — table the
  knob matrix; sanitizer options e.g. `ASAN_OPTIONS` tabled verbatim).
  Success = the sanitizer names the WRITER (allocation stack + free
  stack + overflow/underflow/use-after-free class at the flag-path
  site). O4 is expected absent (flag kills it bidirectionally); O6's
  Scudo signature should be REPLACED by the sanitizer report (table
  whichever fires — if Scudo still fires first, table why the
  sanitizer missed and what narrower scope would catch it).
- Success bars (tabled): sanitizer loads on-device (probe receipt);
  build exit 0 + full identity (size/sha/BuildID — NEW values expected,
  4+ matching reads incl. on-device); ONE run (exit/wall/fate +
  sanitizer report verbatim + tombstone triage if any); WRITER NAMED
  (file/line/function + allocation/free stacks) or the exact reason
  the writer stayed unnamed.

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g27/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (sanitizer-necessitated source workaround ONLY, else
  zero); ONE build (new dir); TWO bounded device runs max (probe-load
  check doesn't count as a run; flag run + one retry iff O1/O3 fires
  instead of first draw); no tuning loop, no second shape, no new
  dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤8 GB — sanitizer builds are fatter; new
  `ps2x-g27/` retrieval dir ≤50 MB; 0 growth elsewhere).
- Evidence dir `local/research/G27/` (STANDALONE, text only — no
  binaries; sanitizer report quoted VERBATIM). Commit with `git add -f`,
  prefix `[G27]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).

## Task 1 — sanitizer choice + build + verify-then-stage (no run until §2)

1. Re-verify pins (clone HEAD/tree, G22+G26 HUNK_MATCH — G24 stays
   superseded, dump, dirs 0-growth, device pre-check, G26 binary
   damage-confirm). Table the sanitizer choice + loadability probe
   FIRST (trivial instrumented probe on-device; wrap.sh + runtime
   staging tabled). STOP here if neither loads (receipted obstacle).
2. Configure + build NEW dir (exit 0, warnings tabled); full identity
   (size/sha/BuildID/ELF/`strings`); verify-then-push with NO gap
   (host re-sha → push → on-device match → run). Table every step
   with timestamps.

## Task 2 — ONE instrumented run + writer triage

1. Run per the knob matrix (+ sanitizer options verbatim); pull logcat
   + sanitizer stderr (COMPLETE, untruncated — the deliverable) +
   tombstone (if any) + scanouts (score if any write); remove the
   device dir. `g14-diff.py` vs G13 if scanouts exist. Name the ONE
   next action the numbers justify (writer fix brief vs narrower
   scope vs alternative tooling).

## Report

`local/research/G27/REPORT.md`: contract, sanitizer + build + identity,
run table, sanitizer report (verbatim), writer verdict, next action,
exact commands, gaps. Commit evidence (`[G27]` + hunk diff text if any),
stop.
