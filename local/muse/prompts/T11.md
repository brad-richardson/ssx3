# T11 — Run-to-exit: empirical N + first post-phase events (step 3c, 1 long boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T10/REPORT.md` §T10-4 first (step
3c is this brief: no readable bound exists in-trace — the host
loop is stop-flag-only, all owner loops miss their issue sites —
so `N` goes empirical via run-to-exit), plus P1 REPORT Part 31
§P31-6d (the 600 s boot shape + the 5 first-post-phase
signatures to watch for) and Part 30 (P1ag's 300 s absence
window this run extends). Peer lanes run concurrently — you
share only the fork remote (T6 measures builds, needs no
lease; still, record tree state).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight — HEAD has
  moved twice under peer lanes already).
- Fixed baselines the run must reproduce before exit (or table
  the deviation exactly): 54-frame modal dispatcher cycle
  (REP+53, T10); per-invocation 20/19/1/2534 (P1ah); stubs 222;
  sema-30 4w/3s parked; FRR ×N resume pattern (T8).
- Exit = EITHER the hash phase ends (first post-phase event per
  P31-6d's 5 signatures: new stub phase, 4th sema-30 signal +
  thread-3 release, `0x362DE8` invocation halt, new caller,
  dma/gif slope break) OR the 600 s cap (a third absence
  window with a tighter bound — still a complete result).

## Gates and rules

- Lease `T11`: boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (the SHARED P-lane lease) — if it names another agent, poll
  every 5 min and log waits to `$W/P1/run/t11-waits.log`. Max 2
  boots: ONE run-to-exit ≤600 s foreground + ONE optional ≤90 s
  confirm (only if the first exits the phase and the confirm
  adds a tabled row). No `adb`.
- ZERO fork changes (no source edits, no commits, no push).
  Build from a recorded tree state (`git log`, `git status`,
  binary sha in the report).
- Evidence dir `local/research/T11/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T11]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — run to exit (or cap)

1. Record tree state, rebuild `ps2EntryRunner` (`-j4`), record
   binary sha. Claim the P-lane lease; run ONE boot ≤600 s
   foreground (P1ag env/scripts verbatim: `PS2X_DIAG_394ED0=1`
   kept; sed the LOG name). Release the lease immediately at
   exit-or-cap.
2. If the phase exits: kill at first post-phase event + 30 s
   grace; table the exit (which of the 5 signatures fired,
   line + block + invocation ordinal = empirical `N`).
3. If the cap hits: table the absence window (final invocation
   ordinal, cycle count, stub/thread/sema end state — the
   tighter bound).

## Task 2 — exit-or-bound tables

1. Empirical `N` (exit case): invocation ordinal, dispatcher
   cycle + slot, wall time, rate shape across the full run
   (does the rate inflect before exit? — table per-60 s
   rates to the end).
2. First-post-phase table (exit case): what main does next
   (thread states, new drops/RPC/CD/SIF/GS rows vs the P1ag
   ladder — one ladder table, exact-vs-baseline per row).
3. Baselines-kept table (both cases): modal cycle intact?
   20/19 constancy over the window? stubs 222? sema-30
   shape? FRR pattern? — per-row exact-vs-baseline.
4. Bound table (cap case): N > final ordinal + the growth
   arithmetic (rate × window) + what a LONGER window would
   cost (lease + contention, numbers only).

## Report

`local/research/T11/REPORT.md`: lease record + tree-state +
exit-or-bound verdict tables + empirical-N (or bound) +
post-phase ladder (or absence window) + baselines table + gap
rows. Commit evidence (`[T11]`, no push), stop.
