# TC1 — per-thread guest state audit + vf0 read-only (muse, 2 h, Mac)

RD1 (`local/research/RD1/REPORT.md`) found every non-main guest thread started with VU0 vf0 = 0 because `R5900Context()` memsets itself and only the main context got hardware-fixed values (`ps2_runtime.cpp:876`, VU0 restore `:331`). Fix `51c759b` sets vf0 in the constructor (folding in F3). This lane finds the rest of that class.

1. **Audit:** list every field of `R5900Context` (and VU0/COP0/FPU/MMI state a thread sees) whose hardware reset or constant value differs from zero, and every place the main context is initialized that a thread start (`EeScheduler::startThread`, any context copy/restore) doesn't repeat: vf0, `vu0_r`, VU0 I/Q/P/status/clip, FCR31 / FPU control, COP0 Status/Config/PRId, MMI state, `$zero`. Cite file:line for each and what PCSX2 or the EE manual says the value is (cite). Table: field, main-context value, thread value, hardware value, used by SSX 3? (grep the codegen for reads where cheap).
2. **vf0 read-only:** the guest can write vf0 today (`lqc2 $vf0` at `0x3fe9bc` in `sub_003FE828`, RD1 gap); hardware ignores writes to vf0. Propose the smallest recompiler/runtime change (emitter skips writes to vf0, or the VU0 register write helper masks index 0), with a unit test. Implement it on branch `tc1-ctx` from fork `ssx3` `0ed07c4`; suite from the worktree root; runner-dir check empty. If the fix needs a codegen regen, stop after the proposal and report (the orchestrator coordinates regens).
3. One det boot to t2400 (paraLLEl env, one slot) only if you changed runtime code: det-hash vs a `0ed07c4` + `51c759b` baseline, frames at 2100 viewed.
Never push; text only in git. Deliverable `local/research/TC1/REPORT.md`; commit `[TC1] …` (`git add -f`, `Orchestrated-By: Muse Code`), no push. Budget 2 h.
