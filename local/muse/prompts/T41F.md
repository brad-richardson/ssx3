# T41F — Finish T41: full T41-1…T41-8 tables + SSD trace stream + complete report (bytesize originals, no new run)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/T41/REPORT.md` first (all of
it: the wrap note §T41-9 has the scored core — R1/R2 environment failures
(WSLg-audio cubeb modal, NO-PARK), R3 NO-SP-PARK forensics, R4 exit 0 with
FULL CHAIN + ONE 1038.4 ms Left hold @T+503.19 on live 2ND/6 + 10-snap
blind series at 10×≈0.57 s; scoring COMPLETE: RDC 11/12 gated, SCPS40
4/11 with hold hop OFF-rail +29 @ 0.02, SCPS120 1/11 with hold hop +119 @
0.37, dense hops 6.36–17.47 — but the full T41-1…T41-8 tables were never
written and the SSD trace stream was BLOCKED by the detached SSD). Also
read `local/research/T40/REPORT.md` (the comparison baseline: SCPS40 rail
+40 @ 0.74, SCPS120 resolve +49 @ 0.79). This brief FINISHES T41 — no new
bytesize run unless a receipt is unrecoverable (table that first; one
bounded re-attempt max, same stop rules as T41).

## Facts you start from

- Scoring is DONE (frozen `t41-track.py` + `t41-analyze.sh`, committed):
  do NOT re-score blind — verify the committed numbers reproduce, then
  WRITE the missing tables (T41-1…T41-8 in T40's shape: chain, hops,
  dense tracking, HUD-per-snap, rail-rate comparison vs T40, gated-pair
  verdict, forensics for R1/R2/R3, census/flaps).
- The SSD is BACK (`/Volumes/Extreme SSD`, ~236 GB free). Stream the R4
  trace from the bytesize original (`…/dat/PCSX2/logs/emulog.txt`,
  2745300674 B, `5b0a7349…53d3f` — verify sha/size BEFORE streaming) to
  the T-lane SSD dir; verify the stream (size + sha, head/tail slices).
  The T41-9 head/tail slices committed pre-restart are your cross-check.
- Bytesize/VM state may have shifted across the restart: re-verify the
  T4 build-reuse pins + input refs you cite (table any drift; drift does
  not invalidate the committed R4 snaps/scores, which are immutable).
- Success bars (tabled): T41-1…T41-8 complete in T40's table shapes;
  rail-rate comparison vs T40 exact (RDC gate-out, SCPS40/SCPS120 rails,
  hold-hop rows); gated-pair verdict explicit; R1/R2/R3 forensics with
  receipts; trace streamed + verified (or the exact blocker tabled);
  full REPORT.md with tail receipt.

## Gates and rules

- No lease of any kind (bytesize needs none). 0 laptop boots. T-lane
  `t41-*.py` tools reused (copied, not modified); no duplicate tooling.
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared + tracked.
  No `adb`. No fork changes (evidence + scripts only).
- Evidence completes `local/research/T41/` IN PLACE (this finishes the
  partial `[T41]` commit `64d252e` — do NOT amend; a second `[T41]`
  commit carries the finish). `git add -f`, prefix `[T41]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — verify + stream

1. Re-verify bytesize original (size + sha) + committed R4 slices match;
   reproduce the frozen tracking scores. Stream the trace to SSD; verify
   (size + sha + head/tail). Table every receipt.

## Task 2 — tables + report

1. Write T41-1…T41-8 (T40 shapes) + rail-rate comparison + gated-pair
   verdict + R1/R2/R3 forensics + recipe for the next input question.
2. Complete `local/research/T41/REPORT.md` (wrap note §T41-9 superseded by
   the full tables; keep the R4 core facts, correct nothing silently —
   errata table if any number moves).

## Report

Full `local/research/T41/REPORT.md`: contract, T41-1…T41-8, comparison,
forensics, exact commands, gaps. Commit evidence (`[T41]` finish), stop.
