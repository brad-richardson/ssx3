# G33 — Write-back control: same sync structure, stale bytes re-written (H1 vs H2 split, Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G32/REPORT.md` first (all of it: the
sampler reads EXACTLY under host-committed pattern (10/10 ladder ==
expected FNV, 10/10 scanouts byte-identical to the model); G32 changed TWO
things vs G31 (content + sync), so the defect reframes to
ordering/coherency (H1) vs content-dependent fault (H2)). This brief
executes G32 §4's ONE next action ONLY. No upstream contact of any kind
(standing no-upstream order — local hunks only, filing stays local).

## Facts you start from

- The split: H1 (ordering/coherency — the per-vsync host-write+commit
  flush+wait+invalidate+barrier for pages 112..223 was the fix) vs H2
  (content-dependent fault — the pattern content was the fix).
- Discriminator (G32 §4): the G32 hunk shape, but re-writing the SAME
  bytes it read (map-read B → temp → map-write B back → commit; no
  external data needed). H1 predicts output goes stale-correct (the
  commit/barrier fixed G31's cleared output); H2 predicts output stays
  cleared (the pattern content fixed it).
- Post-G32 state (gate-verified): the G32 binary is MID-DESTRUCTION
  (941,730 nonzero of 265,852,240 B — 10th recurrence, caught in
  progress; run validity unaffected). G31 (`c91719a0…`) and G28
  (`450471e2…`) binaries INTACT. Clone holds G22/G26/G28/G29/G30/G31/
  G32 hunks uncommitted (HUNK_MATCH re-verify at start; G29-E1
  truncated FNV basis carries for comparability).
- Task 1 (no device until §2): STATIC design — table the write-back
  shape (read-temp-write-commit sequencing vs G32's fill, barrier
  parity argument), the expected outputs under H1 vs H2 (which FNVs?
  stale-correct means WHAT bytes — name them), and the decision matrix
  (what mixed/partial outcomes mean). Name the ONE hunk.
- Task 2 (ONE run max): the write-back control — ONE hunk max
  (diagnostic-dump ONLY + the write-back it requires), ONE build max
  (NEW dir, G22 recipe, `-j2`), full identity, verify-then-push with
  NO gap (push-time + post-run + report-time re-shas — destruction is
  progressive), TWO bounded runs max (diagnostic + one retry iff O1/O3
  fires instead of first draw).
- Success bars (tabled): design table (shape + barrier parity +
  expectations + matrix); the H1-vs-H2 VERDICT (with the
  discriminating bytes); if a run: exit/wall/fate + receipts +
  tombstone triage (if any) + scanouts scored AND pulled by explicit
  filename list with cleanup as its own verified step (G31-E1 rule);
  the ONE next action (fix shape vs next wall — adoption stays queued
  until brightness is understood at the sample level).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g33/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max; ONE build max (new dir, G22 recipe, `-j2`); TWO bounded
  device runs max; no tuning loop, no second shape, no new dumps; no
  lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g33/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G33/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G33]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).
