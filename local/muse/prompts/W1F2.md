# W1F2 — Restore iOS startup plumbing on the pushed widescreen fold

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `local/research/W1F/REPORT.md`,
`local/research/I25/REPORT.md` §Part 2, and
`local/research/I26/REPORT.md` first. The orchestrator decides the gate.

W1F pushed fork `ssx3` to `8acb4b3` and passed Mac tests and race boot,
but its Simulator app exits before title: `Unable to determine executable
path`. The W1F branch cherry-picked I26's final presentation commit but
omitted I25's startup commits `31d988d` and `8a70aa6`. In I26,
`main.cpp` calls `ps2x::ios::prepareEnvironment()` and reads
`PS2X_BOOT_ELF` from bundled `ps2x.env` before resolving the ELF; W1F
lacks that path. The staged Simulator bundle already contains the ELF,
ISO and env file. Also check I25's UIWindowScene/CMake wiring; do not
assume the existing `.mm` is in the app target.

## Scope and hypothesis

Use the existing W1F fork worktree `~/dev/ssx3-work/W1F/PS2Recomp`
(`w1f-fold`) and W1F build area, plus `local/research/W1F2/` for the
report and bounded text receipts. Do not touch I25/I26 worktrees or
scripts, except to read them. Make a new commit on `w1f-fold` after
`8acb4b3`; no rebase, reset or history rewrite. You may adapt/cherry-pick
the **two I25 commits** into the current tree, resolving E/G/W conflicts
by preserving current behavior. Restrict changes to I25 startup, env,
scene, pad and their tests; don't change GS, generated code, guest data,
or W1 aspect behavior. If a conflict needs an E/G design choice, stop
and hand back the exact hunk.

Hypothesis: the missing I25 startup/scene wiring explains the Simulator
failure. Correct behavior: app loads bundled env and ELF with no argv,
shows title, I26-FAST reaches Select Character and a race HUD, while W1
defaults to 16:9. If the env path appears in console but ELF resolution
still fails, report the next concrete error instead of adding a second
unrelated fix.

## Gates

1. Fetch/check fork `ssx3` = `8acb4b3`. Inspect the source diff against
   I26 at `8a357ac` for the I25 path. Make one focused repair. Add/retain
   I25 env parser tests and an argv-free boot-path test if practical.
   Build Mac Release incrementally and run `ps2x_tests` from the fork
   root; keep W1 mode tests. No new Mac race boot is needed after W1F.
2. Reconfigure and rebuild the W1F Simulator app from the **current
   worktree HEAD** and E56 canonical codegen, using a W1F-local copy of
   `build-install.sh` with its HEAD assertion updated. Confirm the
   rebuilt target compiles and links the I25 iOS runtime/scene source.
   Stage/install, then one bounded Simulator run with I26-FAST to title,
   Select Character and race. View bounded screenshots and console.
3. If Simulator passes, install and run on the iPad **only if unlocked**.
   If locked, tell the orchestrator promptly and record the state. Then
   reread the iPhone device ID and build/sign/install the same-source
   device app on Brad's iPhone. **iPhone install only: never launch,
   test, screenshot or drive it.** Record `devicectl` receipt and two
   matching signed-binary SHA reads.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` is
   empty, and no generated guest source, game data or binary is tracked.
   Fast-forward push `w1f-fold:ssx3` only after source, host, Simulator
   gates pass and the runner-dir check passes. Verify remote tip. If an
   iPad lock prevents its run, report that gap but do not hold the
   authorized iPhone install. No push to ssx3 `main`.

Budget: one repair with at most one corrective rebuild per target, one
Simulator run, one iPad run, one iPhone install, 6 h, ≤8 GB additional
W1F bytes and 200 GB global cap (`local/tooling/disk_budget.sh`). Reuse
W1F build dirs and inputs; read ELF/ISO and binaries twice by SHA before
use/install. Simulator/iPad runs have wall caps and own-PID termination.
Stop at the first failed gate after its allowed correction. Report exact
commands, revisions, conflict resolution, suite count, hashes, viewed
frames, remote and device receipts, and gaps in
`local/research/W1F2/REPORT.md`. Commit `[W1F2]` report with trailer
`Orchestrated-By: Codex`; do not push ssx3 `main`.
