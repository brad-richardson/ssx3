# T22 — Aligner projection + milestones + channel pc= field (no boots, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T18/REPORT.md` first (all of it —
the aligner + channel you extend), plus F3 rec 14 (this brief:
`--drop-names`/`--project shared`, `--milestones`; channel
`pc=` field; both env-gated, PCSX2 shape default) and F3 §19
(the hand-made milestone table — your `--milestones` output
must reproduce its rows mechanically). Runs beside A0; no
dependency either way.

## Facts you start from

- The tool: `tools/trace_align.py` (selftest 17/17 green at
  T18 — must re-green before AND after your change). Inputs
  for proof: `/Volumes/Extreme SSD/ps2x-t4/emulog-boot3.txt`
  + `/Volumes/Extreme SSD/ps2x-t4/emulog-t17c.txt` +
  `/Volumes/Extreme SSD/ps2recomp-spike/P1/run/
  syscalls-t18-on.txt` (all read-only; stream, never copy).
- The deliverables: (a) `--drop-names N,...` + `--project
  shared` preset (drops the SIF layer, the sema pump,
  GetThreadId, FlushCache, RFU005 — the F3 §19 projection;
  table the exact drop-list in the report); (b) `--milestones`
  mode printing the rare-vocabulary sequence with stamps
  both sides (must reproduce F3 §19's rows: entry agree →
  SDK SIF → kernel-patch → thread/module/OSD/INTC → display
  → steady-state); (c) channel `pc=` field, env-gated
  (default OFF = PCSX2 byte shape unchanged; when on, each
  line gains the dispatch pc — table the format + the gate).
- NO boots, NO lease of any kind (tooling brief — the pc=
  field gets a compile check only; boot proof rides the
  next leased boot brief, tabled as such). Builds any time
  (`-j4`). No `adb`.

## Gates and rules

- Fork changes: the `pc=` field ONLY (default-off, no
  behavior path touched when unset — reviewer-grade minimal
  diff). Commit to the fork's `ssx3` branch ONLY (never
  push either repo from this brief — the orchestrator
  pushes ssx3 at poll when clean). ssx3: `tools/
  trace_align.py` edit + evidence dir.
- Evidence dir `local/research/T22/` (STANDALONE). Commit with
  `git add -f`, prefix `[T22]`, trailer `Orchestrated-By: Muse
  Code`. Do not push.
- Time box: 4 h. Tables + receipts, no verdicts.
- Hygiene (F3 rec 17): write the report in chunks; put
  `tail -3 REPORT.md` in the receipts (truncated tail fails
  the gate).

## Task 1 — projection + milestones (does §19 come out of the tool?)

1. Drop table: `--project shared` drop-list (names + why
   each — F3 §19's projection, pinned) + `--drop-names`
   parsing (comma list, unknown-name behavior tabled).
2. Milestone table: `--milestones` on boot3-vs-t18 (rows vs
   F3 §19's hand-made rows — match/no per row).
3. Selftest table: new selftests for drop + milestones
   (t9/t10…, all green + the old 17/17 still green).

## Task 2 — pc= field + proof (does the channel carry pc?)

1. Format table: `pc=` line shape (default-off byte-shape
   proof: diff of off-output vs T18's 781,372-line file —
   must be EMPTY) + on-shape sample (first 5 lines).
2. Build table: `-j4` compile receipt (exit 0, warnings
   census — 0 from the touched lines) + binary sha/size
   (recorded; boot proof explicitly deferred).
3. Proof table: `align-milestones.txt` for boot3-vs-t18
   AND t17c-vs-t18 committed (the §19 reproduction
   artifacts; k/divergence rows included).

## Report

`local/research/T22/REPORT.md`: drop + milestone + pc= +
proof tables, exact commands, gaps (incl. boot-proof
deferral + deeper-trace plug). Commit evidence + tool
(`[T22]`), stop.
