# T35 — Cross on X Continue from the pre-race panel: countdown/live gameplay (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T34/REPORT.md` first (all of it: R1
reached the pre-race panel via ENTER Cross on My Rules @T+326.56 (load 18%
→ 92% → black frame → race intro cinematic → panel), `Snow Jam - Race`,
8 snaps over 98 s pairwise 0.024–0.395/p99 ≤18 (panel animation), no
reclaim; G1 proposes one 534 ms Cross (X Continue) to start the race).
This brief executes T34's G1. Reuse T34's scripts (`t34-auto.sh`,
`t34-cropdiff.py`, `t34-analyze.sh`, `t34-vcount.sh` — copy, do not modify
the committed originals). REAL filenames only (T28 G9 lesson): T34's
arrival snaps are `t34r1-mr-post{1,3,8,15,25,40}.jpg` +
`t34r1-mr-stab{1…6}.jpg`; the pre-race ref recipe is PIL `convert('RGB').save`
(T28 §T28-1).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T34 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- GATE NOTE (T34 G1 — multi-stage path, gate on the PANEL): the Enter
  transition is decisive (MR→load ~12.43–12.47, DEPARTED fires), but the
  load→panel path runs loading % → black → cinematic → panel over ~30 s.
  The next arrival gate needs a SETTLED-PANEL criterion (static + non-MR +
  a pre-race ref from `t34r1-mr-stab6.jpg`) rather than a first-change
  trigger. The black frame at +8 s (21267 B) is a TRANSITION, not an
  arrival — the gate must not fire on it. Table the calibration matrix
  (panel-vs-panel spread ≤0.40/p99 ≤18; nearest non-panel content) BEFORE
  the run.
- The task: reproduce the T34 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → settled-panel gate → ≤3 attempts) to
  the pre-race panel with X Continue, then press ONE 534 ms-class Cross
  (X Continue) and table EVERYTHING after: the press (button, hold ms, T+
  time, pre/post snaps), each screen transition (snap sha + region-diff
  scores), and the arrival (countdown? live gameplay? snaps + stability
  count + content read off viewed snaps).
- If Cross does NOT advance (selection ignored, needs a different button,
  next screen loads slowly): table the exact observed behavior (snaps +
  scores + waits) and STOP with a recipe — one input per screen, never
  blind multi-presses. ONE bounded variant allowed only if the press
  provably never acted on the arrival screen (pre-press ≠ pre-race panel).
- Success bars (tabled, not verdicts): arrival park reproduced (snap match
  vs T34's `t34r1-mr-stab6.jpg` under the settled-panel gate); Cross acted
  on it (pre-press = pre-race panel, X Continue); next-screen snaps +
  stability N; full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T34 G2: single-shot
  scripts, runs ≤6 min (T34 R1 ran 468 s — keep added scoring tight or table
  the overrun with full dmesg coverage), preserve emulogs first, dmesg +
  btime + init age, UNFILTERED dmesg + event log around flaps, 534 ms holds
  NOT bisected, `;` chains only INSIDE one `wsl` call, one `wsl` call per
  ssh; post-run dmesg read IMMEDIATELY after the run ssh returns).
  COUNT AcceptAsync lines in the committed dmesg files and report the exact
  counts with uptime-stamp positions relative to the run window — T34's
  report undercounted session flaps (gate erratum: 0/1/2, all outside
  window). Runs in flap windows must be effects-verified or re-run (T27 §4).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T35/` (STANDALONE). Commit with `git add -f`,
  prefix `[T35]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — arrival park + Cross on X Continue (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T34 §T34-0 shape): build/input receipts, NVM state, T34
   reference snaps present, settled-panel calibration matrix, free space
   both ends.
2. Per attempt table: park reproduction (chain scores + snap match vs
   `t34r1-mr-stab6.jpg`), press row (button/hold/T+/pre-post snaps),
   transition chain (snap sha + diffs per hop), arrival (snaps + stability
   N) or exact non-advance behavior.
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T35/REPORT.md`: pre-run receipts, attempt tables, press
log, arrival (or non-advance characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T35]`), stop.
