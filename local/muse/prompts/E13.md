# E13 — Restore the exact 0x2c5358 leaf result (single entry): fail-before, regen, truth table + consumer regression, one guarded boot

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E12/REPORT.md` and
`local/research/E12/NEXT-BRIEF.md` first (all of both: `0x2c5300` restored
— both live calls return 0 for status 0, caller advances past `0x242158`,
NO UI+0x344 clear anywhere; outcome (ii) — remaining wall is the missing
`0x2c5358` entry (raw 9061 / E7 seq 5092: `0x2d3828→0x2c5358`, a0=MC, a1=0,
status0=0, stale v0=`0x2c5358`, policy 1; ELF returns 1 for status 0;
reached low-bit consumer `0x241c48/4c/58` computes `((v0^1)&1)<<3`:
stale-even contributes 8, exact-1 contributes 0; E11's nonzero-branch
comparison does NOT cover low-bit consumers — validate exact results, not
truthiness). This brief renews the bounded regeneration exception for the
`0x2c5358` entry ONLY, per E12's NEXT-BRIEF (which is now AUTHORIZED —
execute it, don't re-propose it). Reuse E12's scripts (copy to `e13_*`, do
not modify the committed originals).

## Facts you start from

- COORDINATION: the I-lane is on the SAME gap again — I16's new wall is
  `0x2d3828 → 0x2c5358`, and a concurrent I17 brief closes it device-side
  at your `9da21ff` base (deduping your `0x2c5300` row, writing its own
  `0x2c5358` row locally). Neither lane blocks on the other; the fork row
  is the canonical one. Watch for the I-lane's `0x2c5358` row bytes as an
  independent cross-check of your observed row (table agreement/delta, do
  not copy — derive your own from the emitted slice).
- Baseline: fork `9da21ff` (pushed); active checkpoint `/tmp/p1-link/runtime`
  (PROTECT it + DerivedData — never reclaim); registry `4fb59437…`; runner
  `97b8dc25…`; suite 452/452; 0x2c5300 fourteen-case test + busy-query four
  + DROP handler all pass. Re-verify checkpoint identity first (E12 §2
  shape) — do NOT repeat earlier repairs.
- The task, in strict order: (1) checkpoint re-verify + fail-before
  (relink fixture against baseline runner objects: `hasFunction(0x2c5358)=
  false`, expected-present test fails); (2) FRESH admission sample +
  persist BEFORE any CSV mutation (≥1 GiB logical + 2 GiB floor + 256 MiB
  guard; do NOT assume E12's final free space persists — reclaim ONLY
  retired trees with per-tree ownership proof if short, E12 §3 shape);
  recheck immediately before spawn; (3) insert the row
  (`sub_002C5358,0x2c5358,0x2c53b0,0x58` after owner `sub_002C5338`,
  before `sub_002C53B0` — expect CSV sha
  `0c6085744ab1a2afd60170a18ac261db02472441fa1d343ffd6756026e92d9f4`,
  computed by E12 without editing — table match/mismatch), regenerate via
  the APFS path, install changed bytes only; (4) actual-binding leaf cases
  (ports 0 AND 1: true for `-10001,-7,-5,-4,-3,-2,-1,0`; false for
  `-10002,-8,-6,1`; cross-port contradictory statuses; v0 EXACTLY 0/1) +
  preservation checks + consumer regression (status-0 result contributes 0
  to the low-bit calc; stale `0x2c5358` contributes 8) + full regression
  (0x2c5300 fourteen, busy-query four, DROP, suite); (5) ONE guarded
  verification boot (fresh T13 pre-claims, lease, REPORT_ALL=1, E7/E4,
  wall/progress/byte caps) with the NEXT-BRIEF probes (dynamic MC/UI,
  0x2c5358 call/return + wrapper/caller returns, UI+0x43c writes, state
  route, prior guards, copy/frame join).
- Success bars (tabled, not verdicts): checkpoint identity re-verified;
  fail-before recorded; admission passes on fresh samples (ordering
  repair honored); entry generated with exact-PC binding; leaf cases +
  consumer regression execute; regression green; one boot shows the exact
  result repaired and the flag/state path advancing (or the first
  remaining wall — tabled, not fixed).

## Gates and rules

- Renewed bounded exception: `0x2c5358` entry ONLY (one map row + its
  generated entry). Any second gap → table, do not close. Standing
  no-regen resumes at close. Fork discipline: named-files commits only,
  generated sources NEVER staged, fork-remote push only.
- Ordering repair (E10 lesson): admission check + persist BEFORE csv edit;
  recheck immediately before generator spawn. Never lower a declared guard
  after a rejection.
- Reclamation safety (E11/E12 shape): per-tree ownership proof BEFORE removal
  (lane-board check + no live file handles); oldest retired first; stop
  once admission passes + 512 MiB headroom; never touch `/tmp/p1-link`,
  DerivedData, or any live lane path. `export COPYFILE_DISABLE=1` on SSD
  steps; allocated-byte caps declared + tracked (ExFAT).
- Boots: ≤1 guarded boot under the shared P-lane lease (fresh T13 pre-claim
  checks, wall/progress/byte caps, REPORT_ALL=1, E7/E4 taps). No boot without
  the lease; never wait on another lane's lease — table and stop.
- Evidence dir `local/research/E13/` (STANDALONE). Commit with `git add -f`,
  prefix `[E13]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — checkpoint re-verify + fail-before + fresh admission (no regen)

1. Checkpoint identity: registry/runner SHAs, name audit vs
   `after/generated.json`, suite re-run, 0x2c5300 fourteen-case + busy-query
   four re-run. Fail-before: relink fixture vs baseline objects,
   `hasFunction(0x2c5358)=false`, expected-present fails. Table every check.
2. Fresh admission sample + persist (BEFORE csv edit). If short: reclaim
   retired trees oldest-first with ownership proofs until admission + 512
   MiB headroom. If STILL short: STOP with outcome (iii) + the exact next
   target — no csv mutation, no regen.

## Task 2 — row insert + regen + install + leaf cases (no boot)

1. Insert the row (expect the E12-predicted CSV sha — table match/mismatch).
   Recheck admission immediately before spawning; generate; install changed
   bytes only (AppleDouble purge, proven-created only).
2. Relink fixture against NEW runner objects; run the leaf cases (both
   ports, all statuses + indexing proof) + preservation checks + consumer
   regression + full regression (0x2c5300 fourteen, busy-query four, DROP,
   suite). Table every receipt.

## Task 3 — one guarded verification boot

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Capture the NEXT-BRIEF
   probes (dynamic MC/UI, 0x2c5358 calls/returns, UI+0x43c writes, state
   route, prior guards, copy/frame join) or the first remaining wall.
   Table the joins; no promotion without an actual request/completion
   dependency.

## Report

`local/research/E13/REPORT.md`: contract, checkpoint re-verify + fail-before,
admission record, regen + leaf-case + regression tables, boot joins (or
wall), exact commands, gaps. Commit evidence (`[E13]`), stop.
