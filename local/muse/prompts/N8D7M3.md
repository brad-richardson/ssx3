# N8D7M3 — selected raw-VRAM provenance discriminator design (OpenCode Go)

You are the N/G diagnostic-design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7M2/{REPORT.md,ORCH-GATE.md,result.json,tile-excerpt.txt}`, `local/research/N8D7L/{REPORT.md,ORCH-GATE.md}`, `local/research/N8D7J/ORCH-GATE.md`, `local/research/N8D7K/ORCH-GATE.md`, and the private oracle fork's selected-capture source. Write only `local/research/N8D7M3/` and this brief. **Read-only design:** no source edit, build, replay, boot, Odin/iOS action, push or status/todo/ledger edit. Cite the repo/source and named receipts, not the web. Confirm C++ links with LSP where available, and direct source reads if LSP is empty.

Goal: design one bounded **same-run** observation that distinguishes why the tick2050 FBP112 selected raw-VRAM snapshot is sparse. N8D7M2's independent fork-table oracle and G43 input agree 448/448, 15 active; selected input/circuit/stage agree, final is 29 active. All eight sampled raw control values differ from the Mac N8D7L replay, but Mac/Odin GS streams are not proved identical. Therefore do not compare a free-running Odin boot to the Mac stream as if it were controlled, and do not revisit the already-excluded G43-vs-fork decoder disagreement.

Trace the exact source path and timing from GIF/VRAM writes through selected frame choice, 4 MiB snapshot, oracle decode and GPU input upload. Identify the owner of FBP/FBW/PSM/phase/stride, the snapshot tick and any async boundary; pin code line and calling path for each. Separate these hypotheses with distinct predicted observables: (A) the intended FBP112 region has few writes/low occupancy before selection; (B) substantial written content exists at another valid base or phase and the selected descriptor points to the wrong region; (C) writes target FBP112 but the snapshot is taken before they become visible; (D) another concrete mechanism supported by code, else OTHER. A prediction must name a same-run field/byte/packet that differs under the alternatives; equal predictions are not a test.

Propose **one** diagnostic capture for a later worker: exact default-OFF tap sites/fields, packet/tick window, source VRAM address calculation (actual GS storage addresses, not a linear row approximation), selected descriptor and snapshot SHA/occupancy, and one bounded independent witness of writes or competing candidate content. Use the existing N8D7M1 APK/source and G43 stage probes as context; do not require rebuilding Android in this part. Predeclare controls, caps (one build and one Odin launch at most for a future brief; 16 MiB log; no >300 s launch), failure/OTHER conditions, and whether a Mac capture is needed to validate the tap first. Avoid logging game assets or raw 4 MiB dumps to Git; bounded checksums/counts and private scratch are enough. Explain what each outcome would change next, without declaring an upstream cause.

Budget <20 min source/receipt review, <512 KiB new text. Deliver `local/research/N8D7M3/REPORT.md` with a pinned source-path table, hypothesis/observable table, one proposed capture and exact commands read in this part; list missing links plainly. Commit only this brief and named text receipts as `[N8D7M3]`, trailer `Orchestrated-By: opencode`, no push. Hand back the table and recommended next action; **the orchestrator makes the gate decision**.
