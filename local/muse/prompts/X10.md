# X10 — sparse Qwen fixed-excerpt path classification

You are a local opencode/Qwen **sparse** worker in `~/dev/ssx3`.
Follow `~/dev/AGENTS.md` worker rules. This is a model-capability trial and
an E54 source inventory, not a fix verdict. Edit only
`local/research/X10/REPORT.md`. Cite the repo excerpt, not the web.

Read only this brief, `local/research/X10/source-excerpts.txt` (7 KiB,
fork `293fd81`) and the existing report skeleton. No other source reads,
`rg`, LSP, web, build, boot, device, source edit, or push. Do not infer a
missing path from these slices. You may run a short Python check after
filling the table; do not search for more code.

Fill the six table rows with the action and **domain** shown by each
excerpt. Keep INTC dispatch (`dispatchIrq(false, ...)`), DMAC dispatch or
queued cause (`true` or `queueCompletedDmacCause`), and VU status bits
distinct. For each row give an exact file:line citation from the excerpt.
For timer pending, show the full constant cause range implied by `timer`
0..3. List only the `dispatchIrq(false, ...)` cause values demonstrated
by the slices. State whether these slices contain a firing path for INTC
5 or 7; say `not shown in supplied slices` if so. Do not claim a global
absence from the fork or game.

Write the report within 3 minutes; use `python3
local/tooling/orch/check_x10.py` to check format and table completeness.
If it fails, correct once. Commit only the report with an `[X10]` subject
and `Orchestrated-By: opencode` trailer, explicit path, no push. Hand back
the table and tool/context/time notes, no E54 verdict.

Budget: 10 minutes, ≤18k context, ≤1 MiB report. Stop at the cap with a
plain gap. One report, no tuning loop.
