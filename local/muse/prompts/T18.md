# T18 — Brief 2: runtime trace channel + first-divergence aligner (boot-phase alignment first)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T4/REPORT.md` first (all of it —
the reference: Devel PCSX2 v2.9.75 on bytesize, 7.48M-line
4-channel trace to language-select, format notes in §T4-2:
EE lines carry name + hex syscall number only, IOP lines
carry `(a0, a1, a2, ra?)`, all lines carry host-wall ts —
plus T4's `samples.txt` verbatim format reference and
`census-final.txt` per-name census), plus
`docs/research/review-2026-09-19-progress.md` §12 T4 row
(Brief 2 = runtime trace channel + `tools/trace_align.py`).
T17 is concurrently deepening the reference toward gameplay
(bytesize, no lease); you align what exists (boot phase) and
build the tooling the deeper trace will plug into.

## Facts you start from

- The reference trace: `/Volumes/Extreme SSD/ps2x-t4/
  emulog-boot3.txt` (528,683,584 B, 7,483,481 lines —
  stream + seeks, never copy whole). Epoch anchor:
  `ExecPS2` ×2 @142369/142465 + first vblank @417152.
  T17's deeper trace does NOT exist yet — design the
  aligner against the boot-phase trace; note exactly what
  a deeper trace plugs into.
- The runtime side: fork `$R=/Volumes/Extreme SSD/
  ps2recomp-spike/PS2Recomp`, branch `ssx3` (table HEAD at
  start; record don't fight). This brief ALLOWS additive
  fork changes (a trace channel emitting EE-syscall events
  in the SAME `Bios call: NAME (hex)` shape — new channel
  + flag only, no behavior change): prove ladder-identical
  with a short proof boot (channel on vs off must not move
  guest behavior — table the proof, T13-ladder style).
- Proof boots need the SHARED P-lane lease
  `/tmp/ssx3-p-lane-lease` (poll every 5 min if held, log
  waits; max 2 boots ≤600 s each — boot-phase only, no
  drain runs). Builds any time (`-j4`). No `adb`.
- The question: (a) `tools/trace_align.py` aligning the
  reference EE.Bios stream with the runtime channel stream
  to FIRST divergence (syscall-name sequence + epoch
  anchor — args/returns don't exist on the EE side, table
  that limit), (b) the runtime channel itself, (c) the
  first boot-phase alignment table (where do the sequences
  agree? where's the first divergence? or table why the
  anchor fails).

## Gates and rules

- Fork changes: ADDITIVE trace channel only (new files /
  new flag paths; no edits to guest-behavior code). Fork
  commits go to the fork's `ssx3` branch ONLY if the
  ladder proof holds (else table + revert); ssx3 evidence
  commits never push. NEVER run `git push` in ssx3.
- Evidence dir `local/research/T18/` (STANDALONE) +
  `tools/trace_align.py` (the tool is the deliverable —
  committed in ssx3 with the evidence). Commit with
  `git add -f`, prefix `[T18]`, trailer `Orchestrated-By:
  Muse Code`.
- Time box: 6 h. Tables + receipts, no verdicts.

## Task 1 — channel + aligner (do they emit/align?)

1. Channel table: flag name + emission points + line format
   (byte-shape vs T4 samples — diff-verified on format).
2. Ladder table: proof boot channel-on vs channel-off
   (guest-behavior identity: N? exit block? handshake
   counts? — table the rows, T13 style).
3. Aligner table: `trace_align.py` inputs/outputs/exit codes
   + self-test on synthetic pairs (identical → no-diverge;
   grafted → first-divergence line named).

## Task 2 — first boot-phase alignment (where's the divergence?)

1. Anchor table: ExecPS2/first-vblank anchors found in both
   streams? (line numbers both sides — or table the miss).
2. Sequence table: syscall-name agreement from anchor
   (longest common run? per-name census both sides?).
3. Divergence table: first divergence (both line numbers,
   both names, epoch context — or NO divergence in the
   boot window: bound tabled + deeper-trace plug noted).

## Report

`local/research/T18/REPORT.md`: channel + ladder + aligner
+ alignment tables, exact commands, gaps (incl. what T17's
deeper trace unlocks). Commit evidence + tool (`[T18]`, no
push), remove the lease if held, stop.
