# N5 — Odin: current tree, dumps-off, first real speed numbers through the race

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini;
Android builds on bytesize `~/n2`, branch `n2-android`, via `ssh bytesize`
→ WSL; Odin on USB `622c49b1`). Follow `~/dev/AGENTS.md`. **Tables +
receipts; recommend, the orchestrator decides.** Read first:
- `AGENTS.md` (Odin lease, keyguard check, always `install`, force-stop
  when you're done; **speed numbers only from diagnostics-off builds**,
  stated as guest vsyncs/s ÷ 59.94);
- `local/AGENTS.local.md`;
- `local/research/N4/REPORT.md` and `local/research/N6/REPORT.md`;
- `local/research/PF1/REPORT.md`;
- `docs/numbers-ledger.md` (PS2 rows).

## Why

Everything that made the game work landed on the Mac in the last day:
- E46/E49: 19 extra function starts, and the MPEG end word;
- E50: the FPU fix, which fixed the camera;
- E45: VU1 `double` math, not yet on any branch Android builds.

The Odin hasn't seen any of it. We need its real numbers through the race.

## Work

1. **New branch `n5-android`** at fork `ssx3` `eac6cba` (no rebase; history is never rewritten): cherry-pick N6-tip `65c95d9`'s six N-local commits,
   and cherry-pick E45's `310b30f` (VuWide = double; its Android effect is
   no quad soft-float). Keep N4/N6's Android commits. Report conflicts.
   Don't push; that branch stays local on bytesize.
2. **Codegen:** copy canonical `~/dev/ssx3-work/codegen-ssx3` to bytesize,
   checking the SHA of the tarball ×2, and point the Gradle property at it.
3. **Two APKs:**
   - (a) **diagnostics OFF**: no frame dumps, no runtime/aggressive logs,
     no taps. This one gives the speed numbers.
   - (b) the PNG-dump build, for screencaps if (a) can't be verified.
4. **Odin runs.** Lease; keyguard `showing=false`; BACK after `am start`.
   - Pad script: I26's fast route from `local/research/I26/ROUTES.md` if
     it exists by then, else the E33 route. Vsync clock.
   - Measure guest vsyncs/s over the title, main menu, Select Character
     and race (the first 60 s of racing), using the dumps-off APK and
     SurfaceFlinger timestats for presents.
   - Screencaps at SC (rider) and in the race (world).
   - One `simpleperf` profile in the race (the profileable APK from N4),
     giving the top 25 by self and the thread split.
5. **Ledger-ready table:** phase × guest vsyncs/s × ratio to 59.94 ×
   build SHA × notes. Compare with PF1/N4 (0.36×/0.43× at the title and
   menus, ~0.2 vsyncs/s at SC before the fixes).

## Budgets

≤3 Android builds, ≤5 launches, 5 h. bytesize runs one heavy job at a
time, and G46 may run PCSX2 there: check `pgrep -f pcsx2` before
building. Force-stop the app and release the lease when you're done.

## Deliverable

`local/research/N5/REPORT.md` (tables, screencaps, profile top-25, APK
SHAs ×2). `[N5]` commit (`git add -f local/research/N5
local/muse/prompts/N5.md`, `git log -1` first, trailer `Orchestrated-By:
Claude Code`).
