# G23 — Validate the UPSTREAM `--disable-sampler-feedback` flag on-device (NO rebuild — G22 binary ships it) + pixel-diff vs G13 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G22/REPORT.md` first (all of it: the
skip-gate removes O4 — exit 134, crasher never logged, 0 driver frames,
`Done!` last line — but renders ALL-BLACK (quantified 0.0000); NEW
post-run wall O5 (Scudo teardown, writer unnamed); upstream ALREADY ships
`--disable-sampler-feedback` (full-upload fallback, no dispatch at all) —
UNTESTED). This brief executes G22 §4's ONE next action ONLY. No rebuild,
no second workaround shape, no upstream contact of any kind (standing
no-upstream order — the filing draft stays a local doc).

## Facts you start from

- NO REBUILD: the G22 binary at
  `parallel-gs-g22-android-build/tools/parallel-gs-replayer`
  (265,840,344 B, `5e1f7827…`, BuildID `c2dc902b…` — orchestrator
  re-verified post-restart, 4th matching re-sha, no zero-fill) ALREADY
  ships the upstream flag (usage `:36`, handler `:58`,
  `debug_mode.disable_sampler_feedback` default false). Re-sha it at
  start AND before any push (standing re-sha hygiene — zero-fill
  recurred twice before, transient-read class per G21).
- SSD clone `parallel-gs-g7` HEAD `3a66c19…` (orchestrator-verified) with
  the G22 hunk UNCOMMITTED in `gs_renderer.cpp` (HUNK_MATCH vs
  `g22-workaround.diff`). Do NOT commit or remove any clone hunk; this
  brief makes ZERO source edits.
- The task: ONE bounded on-device run (Odin3 `622c49b1`, transient dir
  ONLY, `mg/` never touched) of the SAME dump/iterations with the
  UPSTREAM flag (and WITHOUT `PGS_SKIP_SAMPLER_FEEDBACK`, so the upstream
  shape is tested independently — table the knob matrix explicitly,
  including whether `PGS_SKIP_COMPILATION_TASKS=1` stays to keep the
  async path from confounding, per G22's tabled choice). Then
  `g14-diff.py` pixel-diff vs G13 oracles (zero new tooling).
- Prediction to confirm-or-refute (both halves): no-O4 AND correct
  pixels. O5 is EXPECTED to re-fire harmlessly post-`Done!` either way
  (table whether it does — same signature or drift).
- Success bars (tabled): binary re-sha + on-device sha match; ONE run
  (exit/wall/fate + knob/flag receipts + crasher absence/presence +
  tombstone triage if any); scanouts pulled + scored (exact/le2/le32 +
  means — bright vs black decides the adoption shape); O5 re-fire tabled.

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g23/` transient dir
  ONLY, removed at end (`mg/` only). Install policy: force-install shape
  N/A (binary push, not an install). No P-lane lease, no bytesize/WSL.
- ONE hunk max: ZERO (no source edits this brief). TWO bounded device
  runs max (flag run + one retry iff O1/O3 fires instead of first draw);
  no tuning loop, no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new `ps2x-g23/` retrieval dir ≤50 MB; 0 growth elsewhere).
- Evidence dir `local/research/G23/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G23]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — pins + ONE flag run (no analysis until receipts are in)

1. Re-verify pins (clone HEAD, hunk presence, binary sha/build-id,
   dump sha, G14/G18/G20 dirs 0-growth, device pre-check). Push + run
   with the upstream flag per the tabled knob matrix. Pull logcat +
   tombstone (if any) + scanouts; remove the device dir. Table every step.

## Task 2 — score + adoption input

1. `g14-diff.py` vs G13 oracles + pixel census (means/psnr). O4-absent
   legs (same 4-leg shape as G22 §3b) + O5 re-fire table. Name the ONE
   next action the numbers justify (adoption shape vs next wall).

## Report

`local/research/G23/REPORT.md`: contract, pins, run table, score,
hypothesis verdict, next action, exact commands, gaps. Commit (`[G23]`),
stop.
