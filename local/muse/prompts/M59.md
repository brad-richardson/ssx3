# M59 — dec-1 all-positive δ: per-bin δ-sign census over m15 bins joined with columns (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M58/REPORT.md` first (all of it —
M58 gap 2 = this brief, taken on orchestrator judgment: all 12
dec-1 off-mode sites read δ>0 (+9..+33, sum +214) while dec-0
splits 6/7, dec-3 splits 12/7, and dec-4 splits 3/2), plus
`local/research/M57/REPORT.md` (hist-4: m15 0:13/1:12/3:19/
4:5/5:2 + 75 dec-9) and `local/research/M19/REPORT.md`
(P(δ>0) 0.8093/0.8019 — the background sign rate). Fully
offline — SSD dumps read-only, no harness code, no replays, no
lease. No device work.

## Facts you start from

- The 12 (reproduce exactly or table the mismatch and stop):
  dec-1 off-mode δ +9/+10/+10/+22/+19/+9/+14/+33/+15/+23/
  +29/+21, all positive, sum +214, on cols 307/309/310/311/
  312×6/313/315 (M58 site-51 + decile-bin table).
- The question: per-bin δ-sign census over ALL 10 m15
  tail-Y bins (0–9, n=134) joined with columns — is dec-1's
  12/12 positive unique among bins, and does the sign split
  track columns (c312? null-4? named streak cols?)? Answer
  by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m58/m58.txt` (bin table to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m59/`; evidence
  `local/research/M59/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m58/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m59/` + `local/research/M59/`.
- Evidence dir `local/research/M59/` (STANDALONE). Commit with
  `git add -f`, prefix `[M59]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M58 precedent).

## Task 1 — bin-sign reproduction (do the splits reproduce?)

All 10 m15 tail-Y decile bins recomputed from the dumps
(must match M58's decile-bin table exactly — counts, δ
lists, sums, pos/neg per bin, incl. dec-9's 59/16 — or
table the mismatch and stop):

1. Bin-sign table: per-bin n + pos/neg + pos share + δ
   min/max/sum (is dec-1 the only 1.0000?).
2. Dec-1 site table: the 12 sites' (row,col)/δ + seats
   (band/streak/carrier/701/at-peak — M33 machinery on
   12 sites).
3. Background table: dec-1's 12/12 against the M19 P(δ>0)
   background + the pooled off-51 35/16 + the dec-9
   59/16 (shares tabled, no tests beyond counts).

## Task 2 — sign-by-column census (does sign track columns?)

1. Column-sign table: pos/neg splits per column over the 33
   union cols on m15 tail-Y (which columns read all-pos?
   c312's 23? null-4? named streak cols?).
2. Bin-column table: dec-1's 8 columns vs other bins'
   columns (do the 12 share columns with dec-0/dec-3
   negatives?).
3. Mode table: per-column modal decile joined with modal
   δ-sign (does column decile mode predict sign?).

## Task 3 — controls + determinism (M18–M58 precedent)

1. Positive control: synthetic truth with KNOWN bin signs
   (N all-pos bin + N split bins, known δ/cols) —
   recover the bin-sign + column tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M58).

## Report

`local/research/M59/REPORT.md`: DESIGN.md (bars first) +
sign tables + column tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a sign map
discriminates (budget-noted, pre-registered split required
per M23–M58 precedent). Commit evidence (`[M59]`, no push),
stop.
