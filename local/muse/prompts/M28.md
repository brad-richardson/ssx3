# M28 — Streak-column census: which shapes move which columns, by how far (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M27/REPORT.md` first (all of it —
M27 gap 1 is this brief: 733 swaps s0's c301/c321 for c303/c323
(+2 columns), 731 swaps c257/c277 for c259/c279 (+2 columns) —
why these pairs, why +2, what moves the columns?), plus M22's
streak-column definitions (the 8 named columns + row spans to
reproduce as the s0 reference). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device work.

## Facts you start from

- Named s0 streak columns (M22/M25): c257/277/296/301/321/
  340/342/343 (single-column vertical tail runs, r23–34,
  8–11 tail bytes each). 733 and 731 each move exactly two of
  them by exactly +2 columns (different pairs); all 764 tail
  maps are 100% luma; 727/764 maps byte-identical to s0.
- The census question: across all 764 shapes, which named
  columns move (presence/absence per shape per column), where
  do movers go (displacement distribution: always +2? other
  offsets? other directions?), and do movers correlate (same
  shapes move the same pairs? pairs move together?).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m27/m27.txt`
  (733/731 swap rows to reproduce: swapped pairs + offsets —
  match exactly or table the mismatch and stop). Work dir:
  new `/Volumes/Extreme SSD/m28/`; evidence
  `local/research/M28/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m27/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m28/` + `local/research/M28/`.
- Evidence dir `local/research/M28/` (STANDALONE). Commit with
  `git add -f`, prefix `[M28]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M27 precedent).

## Task 1 — presence census (which shapes move which columns?)

733/731 swap rows recomputed from the dumps (pairs + offsets
must match M27 exactly or table the mismatch and stop):

1. Per-shape × named-column presence table (all 764 × 8:
   present/absent + row-overlap with s0 — the census matrix;
   commit the full matrix as TSV, summarize in the report).
2. Mover census: shapes moving ≥1 named column (count, list,
   how many columns each); per-column mover counts (which
   columns move most?).
3. Pairing: do movers move PAIRS (like 733/731) or singles?
   Table the pair co-occurrence (which pairs move together,
   how often).

## Task 2 — displacement census (where do movers go?)

1. Displacement distribution over all moved columns (signed
   column offset: always +2? table the full hist; row-shift
   distribution too).
2. Destination analysis: moved-to columns (c303/c323/c259/c279
   for 733/731 — table all destinations: fresh columns or
   swaps with each other?).
3. Below-0.95 join: the 9 M22 shapes (733/731/9/700/734/732/
   2/711/3) vs the mover census (are all 9 movers? are all
   movers below 0.95? — table the 2×2).

## Task 3 — controls + determinism (M18–M27 precedent)

1. Positive control: synthetic truth with KNOWN moved columns
   (N shapes, known pairs/offsets) — recover the census +
   displacements exactly.
2. Determinism: re-run Task 1 on s0+733+731, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M27).

## Report

`local/research/M28/REPORT.md`: DESIGN.md (bars first) +
census tables + displacement tables + below-0.95 join +
controls + explained-vs-standing update (attribution, not
explanation — table it) + gap rows. Screenshot only if a
mover map discriminates (budget-noted, pre-registered split
required per M23–M27 precedent). Commit evidence (`[M28]`,
no push), stop.
