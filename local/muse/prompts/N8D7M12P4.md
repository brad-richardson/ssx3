# N8D7M12 Part 4 — exact stream staging and Odin preflight (no launch)

You are the N-lane device staging worker, OpenCode Go. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M12 Part 3 `REPORT.md`/`ORCH-GATE.md`, N8D7M11 §§5–6, N8D7M6 `REPORT.md`/`launch.py`, and `docs/todo.md` Part 4. Own this brief and `local/research/N8D7M12P4/` only. Source stream `~/dev/ssx3-work/N8D7M6/n8d7m6.gs` and APK `~/dev/ssx3-work/N8D7M12P3/app-release.apk` are read-only inputs. Do not edit source/fork/stream/APK, run an app, install an APK, write `ps2x.env`, reboot or unlock a device, use iOS, push git, change board/global files, delete device files, or contact upstream. One device agent at a time: hold `/data/local/tmp/mg/LEASE` while touching Odin. Time box 25 min; at most one `adb push` of the 1,100,696,462-byte stream; local text <512 KiB. No other Odin worker is assigned.

**Question:** are the exact N8D7M6 stream bytes and gated N8D7M12 APK ready for a separately gated same-stream Odin replay? Pin stream SHA `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593`, size 1,100,696,462 B, APK SHA `caa1110297dc413d3b04e7442873e0fb5d5624af8417e1e30537d76184d5f512`, size 153,753,116 B. Use `local/odin-serial` and N8D7M6 FILES dir `/storage/emulated/0/Android/data/com.ps2x.runner/files/n8d7m6.gs`. Verify two matching local SHA reads of **both** inputs before any device action. Read-only preflight: `adb devices`, `dumpsys battery` (charging and ≥20%), `dumpsys window policy` (keyguard state), `df` (≥10 GiB free for a future run), app force-stopped state, and lease contents. If keyguard is showing, report this immediately to orchestrator and do not launch; staging may proceed if safe. If battery/space/lease fails, stop and hand back OTHER. Never overwrite a mismatching existing stream silently: if remote exists and size/hash differs, report OTHER with the two hashes and stop. If it matches, reuse it. If absent, claim the free lease with a unique tag, `adb push` once to the proven FILES path, and verify two independent device SHA reads plus size. For a matching existing stream, still claim and release the lease for device checks; never overwrite. Read lease after claim; release it in `finally` only when it still carries your exact tag. Check the app is stopped at the end. Use `adb -s` on every command. Do not launch or install.

Deliver `REPORT.md`, a bounded staging/preflight script (or exact commands), machine-readable check results, raw bounded `adb` receipts, SHA/size pairs, lease and device state, gaps, and a checker that computes pin/lease/result agreement. A = exact inputs and device stream verified, lease released and app stopped; B = remote stream mismatch with no overwrite; OTHER = preflight, permission or resource failure. A does **not** prove runtime behavior, default-off control, graphics cause or speed. Commit `[N8D7M12] Part 4` with `Orchestrated-By: opencode`, explicit named paths, no push. Hand back the table; the orchestrator gates any install/replay.
