# E22 — Observed title boot (stdbuf-free) + second-demand-signal hunt (X)

You are the ssx3 E-lane worker (Claude Code) in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** E21 repaired and proved observation, answered
Q1/Q2/Q3, and stopped at the fix gate with X named: a second input-demand
signal after successful-but-incomplete AddBs. You close e21a's observation
gap and hunt X.

Execute this brief as specified. The design below is settled by cited
evidence — if a step's rationale seems unclear, re-read the cited
receipt rather than re-litigating the approach. Table genuine blocks with
their receipts; do not stall on doubt.

## Read first (all of each)

1. `local/research/E21/REPORT.md` + `local/research/E21/NEXT-BRIEF.md`
   (forwarding proof, Q1 packet-1 = GOP + 5,461 B, Q2 static absence,
   Q3 one-callback park, stdbuf argv warning — they BIND this brief).
2. `local/research/E18/NEXT-BRIEF.md` + `local/research/E19/NEXT-BRIEF.md`
   (parked questions + ABI constraints — BINDING).
3. `local/muse/prompts/E21.md` (mission context; E21's dir is READ-ONLY
   reference — your dir is `local/research/E22/`).

## State you inherit (re-verify, don't re-derive)

- Fork `3adc0478` (E18 BEHAVIOR, PUSHED — verify `fork/ssx3` before
  anything; E21 made zero fork commits).
- Proven instrument: `local/research/E21/parser/e21-parser-observer.dylib`
  (52,472 B, SHA256 `e4d88fdc…b38e6be`) — REUSE it (copy to E22 space,
  re-sha); do not rebuild unless the re-sha misses. E19/E20 dylibs are
  failed artifacts — never load them.
- Receipt verifier: E21's `e21_parser_receipt.py` (copy + rename,
  functional-identity proof) — apply to every observer run.
- E21 spent 1/1 boots + 1/1 lease claims. This brief gets ONE title boot.
- `/tmp/p1-link` + `/tmp/e17-map-link` + `/tmp/e18-mpeg-link` +
  DerivedData are PROTECTED (never reclaim); 1,725 protected hashes.

## Mission

1. Checkpoint re-verify (fork triple-agree, 9,457-name audit, suite
   458/458 UNLOADED + observer-LOADED with the reused dylib, E16 closure,
   E15 rc0s, E18 R1–R6). Any red → table + stop (no boot on red).
2. ONE guarded observed title boot (fresh T13 pre-claims, shared P-lane
   lease — lease occupied → table and stop; wall/progress/byte caps,
   REPORT_ALL=1): exec the runner DIRECTLY — NO `stdbuf` anywhere in the
   boot argv (E21's controlled proof: stdbuf replaces
   `DYLD_INSERT_LIBRARIES`; e21a booted parser-unobserved because of it).
   Target: per-call parser timing inside the title window (e21a's gap).
3. X-signal hunt: with the observed boot + Q2's static map, table every
   candidate second-demand edge (runtime re-request sites? guest retry
   conditions? AddBs-wake broadenings? timer/vsync-driven re-polls?).
   For each: the exact code site, what would trigger it, and what
   observation would prove it fires or is absent. No code changes to
   probe — static + the one boot's trace only.
4. Fix gate (strict, unchanged): implement ONLY if the diagnosis isolates
   ONE edge with a minimal ABI-preserving change + its own fail-before +
   full regression. Policy invention (demand shapes the firmware's true
   behavior is unknown for) is explicitly excluded — "Guest never sends
   more without X" → NAME X and STOP.

## Rules

- ONE boot max; ONE lease claim (atomic `/tmp/ssx3-p-lane-lease`, clean
  release, post-release `pgrep` check).
- No CSV/regen (pure runtime/diagnosis brief — regen need = stop).
- No fork behavior commits except through the fix gate (then: fail-before
  + full regression + push, no force-push).
- Byte caps declared up front + tracked (SSD lane ≤16 GB in NEW `e22-*`
  paths; internal ≤3 GB delta; 0 growth elsewhere); `COPYFILE_DISABLE=1`
  on SSD steps; large artifacts off the internal volume.
- Evidence dir `local/research/E22/` (STANDALONE). Commit with `git add
  -f`, prefix `[E22]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 8 h. Report in chunks + tail
  receipt (truncated tail fails the gate). Fill in the ACTUAL time used.

## Errata in (carry, don't re-fix)

- E21-E1: E21 REPORT's harness size/sha (35,072 B `f6059aa0…`) is stale
  (pre-rebuild a1 values); the receipt + on-disk binary (35,232 B
  `f71f4a85…`) are authoritative. No verdict impact.
