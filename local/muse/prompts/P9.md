# P9 — Replace the CD.cpp shim with a CSV split at 0x3E3AD8

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P1/REPORT.md` Part 21 §P21-1a first
(the shim-vs-split table: clean `jr $ra` boundaries both sides, zero
collision — the split was rejected only because the CSV was untracked),
plus Part 24 §P24-1 (the CSV is now canonical at
`games/ssx3/ssx3-functions.sweep.csv`). This is a FIX brief: retire the
game-specific shim with the committable split. P1ab/P10 run concurrently
in other panes — you share only the fork remote.

## Facts you start from

`58c9144` put a hardcoded SSX 3 address + eight hand-emulated MIPS
instructions (`ssx3CdCallbackSemaSignal` in `Stubs/CD.cpp`) into runtime
code because a split produced no committable diff. That constraint is
gone: the CSV is tracked, splits are diffs. Current ladder state (P25):
boot parks on `0x52BE04` with 33 creates, 0 `-1` waits, and the 6-line
`[drop]` census — the proof bar is reproducing it exactly.

## Gates and rules

- Lease `P9`: boots ONLY while holding `/tmp/ssx3-host-lease` — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/p9-waits.log`. Builds + recompiles any time (`-j4`). Max
  2 boots, ≤90 s foreground each. No `adb`.
- Fork: the split files ONLY (`games/ssx3/ssx3-functions.sweep.csv` +
  `Stubs/CD.cpp` + a test if the harness allows — record the list).
  `git add` those NAMED files only — never `-A`, never stage or touch
  anything else. Verify the diff before committing. One commit per
  change with the `Orchestrated-By: Muse Code` trailer. `git pull
  --rebase` before pushing; `git push` ONLY inside the fork clone to
  the `fork` remote. Stop on any foreign rebase conflict. Never commit
  generated sources or `._*`.
- Evidence dir `local/research/P9/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P9]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Split + remove.** Add the `0x3E3AD8` split to the tracked CSV
   (verify boundaries/collision per §P21-1a — re-derive, don't trust);
   delete `ssx3CdCallbackSemaSignal` + its call path from `Stubs/CD.cpp`.
   Re-recomp from the tracked TOML (determinism receipt: entry counts
   + spot shas vs the P24 baseline). Rebuild; suite must stay
   all-green (name any failure face — baseline is 427/427/0).
2. **Max 2 boots (ladder-identical proof).** Receipts: callback still
   dispatched (the `:611/:613` + `ra=0x3e3af0` signal lines), worker
   still passes G6 / issues site #9, driver still runs, boot parks on
   `0x52BE04` with 33/0 creates, same 6-line census, same watch line.
   One ladder table vs P25-boot1 — any delta is a stop-and-report,
   not a shrug.
3. **Report.** `local/research/P9/REPORT.md`: split diff + removal
   diff receipts, recomp determinism, BEFORE/AFTER dispatch tables,
   ladder-identical table, exact commands, receipt paths, "What I
   could not do". Commit, remove the lease if held, stop.
