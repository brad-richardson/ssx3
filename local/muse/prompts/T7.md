# T7 — Emitter extension: driver-loop (i, N) receipt at call site 0x36356c (the P1ah deciding receipt)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P1/REPORT.md` Part 31 §P31-6b first
(the ONE minimal deciding receipt this brief builds: driver-loop
position + bound `(i, N)` for the loop issuing `0x362DE8`
invocations via call site `0x36356c` = `sub_00363490+0xdc`), plus
`local/research/T1/REPORT.md` (the snapshot emitter you extend —
same additive style, same schema conventions). Peer lanes run
concurrently — T5 owns analyzer files in the same fork tree (never
stage/touch/commit foreign files; named adds only).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3`, HEAD `a611702` at brief time (T1's `ladder_diff` —
  table the actual HEAD at start; if T5 landed first, your base
  is its commit — record, don't fight it).
- P31's baselines your proof must reproduce before extending:
  single caller `sub_00363490` for all `0x362DE8` enters;
  20/19/1/2534 per-invocation constancy; stubs 222; sema-30
  4w/3s parked. Any deviation = table it exactly.
- The loop lives in `sub_00363490` around call site `0x36356c`.
  The index and bound are guest registers or stack slots at that
  site — identify them by disassembling the caller (read-only:
  analyzer dumps, objdump-style reads, no guest writes), then
  capture per invocation (or per 5 s block if per-invocation is
  too hot — table the choice + cost).

## Gates and rules

- Lease `T7`: boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (the SHARED P-lane lease) — if it names another agent (T5's
  proof boots have priority: yield), poll every 5 min and log
  waits to `$W/P1/run/t7-waits.log`. Max 2 boots, ≤90 s each,
  foreground. No `adb`.
- Fork: the emitter extension + its unit tests ONLY (`git add`
  those NAMED files only — never `-A`; never stage
  `register_functions.cpp`, generated/ignored, or T5's analyzer
  files). Purely additive hunks (zero removed lines, verified by
  diff audit). One commit per change with the `Orchestrated-By:
  Muse Code` trailer. `git pull --rebase` before pushing; `git
  push` ONLY inside the fork clone to the `fork` remote. Stop on
  any foreign rebase conflict. Purge `._*` before staging.
- Evidence dir `local/research/T7/` (STANDALONE). Commit with
  `git add -f`, prefix `[T7]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — identify (i, N) + extend the emitter

1. Disassemble `sub_00363490` around `0x36356c`: table the loop
   (index reg/slot, bound reg/slot/imm, step, exit condition)
   with addresses + instructions. If the bound is NOT readable
   at the site (computed elsewhere, heap, etc.), table the
   examined-and-absent and STOP the emitter work — the report
   becomes the examined list + the next-cheapest receipt spec
   (still a complete result; do not burn boots on it).
2. Extend the emitter: capture `(i, N)` per `0x362DE8`
   invocation into a new snapshot section (`driver_loop[]` or
   per-invocation tail table — schema-doc it like T1 §T1-1),
   gated by the same `PS2X_DIAG_PARK` write path (counting
   always-on only if cheap — table the overhead choice).
3. Unit tests first (new hook: synthetic invocation sequence
   recovers `(i, N)` exactly), BEFORE/AFTER suite with
   per-face counts. Commit + push per the rules above.

## Task 2 — proof (1 boot: i advances, N constant?)

1. One ≤90 s boot with `PS2X_DIAG_PARK=1` (lease-gated):
   snapshot must contain the `(i, N)` series. Table: N value(s)
   seen (constant? changing?), i range covered, i rate/s, and
   the projected remaining invocations `N−i` at SIGTERM (table
   the projection inputs — rate + remainder — not a bare time).
2. Regression: P31 baselines reproduced (caller set, 20/19
   constancy over the window, stubs 222, sema-30 shape) —
   one table, exact-vs-baseline per row.
3. If a second boot is needed (first invalid for a tabled
   reason), it is allowed inside the max-2 box — record why.

## Report

`local/research/T7/REPORT.md`: loop disassembly table +
emitter schema delta + BEFORE/AFTER suite + proof-boot
`(i, N)` series + projection inputs + regression table + gap
rows. Commit evidence (`[T7]`, no push), stop.
