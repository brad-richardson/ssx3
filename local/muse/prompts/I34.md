# I34 — virtual pad layout v2 (Brad, 09-25) (muse, 1 h, Simulator only)

Brad's request, verbatim: "make the left analog stick 50% bigger and move the dpad so it is down and to the right from the action buttons? and like 80% of the size of that action button set?"

- Code: `ps2xRuntime/include/ps2_virtual_pad.h` (layout + hit tests; I32 added the floating stick and the right-side D-pad), tests in the I32 suite (`Ps2VirtualPad`). Fork branch `i34-pad` from fork `ssx3` `0ed07c4`.
- Changes: (1) stick radius ×1.5 (drawn base and knob, the drag radius and the re-anchor distance scale with it; dead zone stays 10 %); keep the stick zone on the left half, clear of L1/L2/SELECT. (2) D-pad: overall size = 80 % of the face-button cluster's size (measure the cluster's bounding box: △○✕□), placed **below and to the right of** the face buttons, fully on screen, not overlapping any drawn or hit area (assert in the layout test for the iPhone 16 Pro Max landscape size and the iPad sizes the tests already use). If "down-right" can't fit on the iPhone landscape without overlap, pick the closest fit and say exactly what you did.
- Suite from the worktree root. Simulator (iPhone landscape) screenshots: title with the stick knob deflected (`PS2X_VPAD_TEST_STICK`) and a race frame; view them. The Simulator uses the CPU backend (paraLLEl needs a device), fine for layout.
- Stop after the commit: the orchestrator sends Brad the screenshots and builds the combined device app (with DK1).
Never push; runner-dir check empty; text only in git (screenshots stay in scratch). Deliverable `local/research/I34/REPORT.md` (sizes before/after in points, screenshot paths); commit `[I34] …` (`git add -f`, `Orchestrated-By: Muse Code`), no push.
