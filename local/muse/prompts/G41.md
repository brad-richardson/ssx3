# G41 — Masked-write canary on Odin: do PSM1 / FBMSK writes vanish on Adreno? Then ONE fix if a condition is named

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`; working tree `/Users/bradrichardson/dev/ps2xGS`,
SSD clone `parallel-gs-g7` @ `3a66c19` + carried G26/G28 + G40's wall
hunk — VERIFY at start). **Tables + receipts; recommend, the
orchestrator decides.** Read first, all of it: `local/research/G40/REPORT.md`
(F2 execution-loss: 17/17 composite prims accepted, texture input
byte-exact, B unwritten even behind a barrier; O5 state; §4 next
action; §7 gaps). Also `AGENTS.md`. No upstream contact.

## Facts (gated — do not re-litigate)

- G40 PASS: F1 and F3 refuted with bytes; F2 execution-loss confirmed.
  The scene passes (PSM0, unmasked, 113 prims) land byte-exact on
  Adreno; the composite (FBP112, **PSM1**, **FBMSK=ff000000**, 17
  prims) writes nothing. Both GPUs take the UMA path.
- Correct behavior is known from the Mac: B at boundary 1 =
  `840cd308…` / 699,122 nonzero.

## Mission 1 — canary matrix (observation only, scratch pages, one hunk)

Add a quarantined, env-gated canary (default off) that, at boundary 1,
issues small synthetic sprite draws into **scratch VRAM pages that no
game pass uses** (never B), using the same pipeline path as the composite.
Use a 2×2 matrix plus the composite's exact state as a fifth cell:

| cell | PSM | FBMSK | predicted on Mac | predicted on Odin if "masked PSM1 dropped" | if "PSM1 dropped" | if "FBMSK≠0 dropped" |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | 0 | 0 | lands | lands | lands | lands |
| C2 | 0 | ff000000 | lands (masked) | lands | lands | lost |
| C3 | 1 | 0 | lands | lands | lost | lands |
| C4 | 1 | ff000000 | lands (masked) | lost | lost | lost |
| C5 | composite's full state (texture, blend, test regs from O5) | — | lands | lost | lost | lost |

Compute every expected byte pattern from the Mac run before the Odin
run. Also add G40's (b) piggyback: log the 8 O4m hash inputs raw +
desc.rect + TexInfo at cord1 so the `5a15…` vs `20dc…` hash
difference is explained (sampling vs coverage).

Budget: one Mac build + one Mac run (F0 column); one Odin build + up to
two Odin runs (second only if the first is VOID with a named cause).

## Mission 2 — ONE fix (only if Mission 1 names a single cell pattern)

If the matrix isolates the condition (e.g. only C4/C5 lost), find the
code path that handles it in paraLLEl-GS (masked write / PSM1 packing /
the shader variant it selects), identify the exact construct that
misbehaves on Adreno, and apply ONE correctness-preserving hunk
(e.g. replace the construct with an equivalent formulation, avoid the
variant), rebuild once, one Odin run in G38's O1 shape. Acceptance:
Odin-B at boundary 1 == `840cd308…`, scanouts show the Mac's content
pattern, the canary cells all land, G26/G28 still HOLD. If it doesn't
land, table and stop.

If every cell lands on Odin (canary refutes masking), stop and recommend
between: binning/coverage proof (work-list readback), or the Mesa Turnip
driver contrast (same binary under Turnip: correct → proprietary-driver
fault; still broken → our shader/pipeline).

## Rules

- Odin `622c49b1`, `/data/local/tmp/g41/` only, removed at end; check
  `/data/local/tmp/mg/LEASE` before each run. No P-lane lease.
- Two matching SHA reads for any binary that goes to the device;
  `COPYFILE_DISABLE=1`; mirror run receipts to the share tier.
- Hunks stay uncommitted in the clone; commit diff texts to
  `local/research/G41/`. Evidence `REPORT.md` + `[G41]` commit with
  `Orchestrated-By: Muse Code`, `git add -f`, no push; check `git log -1`
  first. Time box 6 h.

## Deliverable

Canary table (Mac vs Odin per cell, bytes); the hash-input breakdown;
the named condition (or its refutation); if Mission 2 ran, the fix hunk
and B/scanout vs Mac; the ONE next action.
