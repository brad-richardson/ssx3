# T5 — Analyzer rule: materialized `.text` pointers are function entries (closes the 10-brief class)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `docs/research/review-2026-09-19-progress.md` Part 2 §12
row T5 first, plus `local/research/P1/REPORT.md` Part 11 (the `0x3E3AD8`
mid-function callback park this rule would have prevented) and
`local/research/T1/REPORT.md` (the snapshot emitter + `ladder_diff`
you will use for the proof — both landed). Peer lanes run
concurrently — you share only the fork remote.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3`, HEAD `a611702` (T1's `ladder_diff`, pushed; base includes
  the T1 snapshot emitter — your proof boots emit snapshots).
- The rule (N64Recomp precedent): extend the LUI+ORI/ADDIU fold
  (`f2149e7`) so any folded constant landing inside `.text`
  becomes a function-entry split. Closes the mid-function
  callback class outright.
- Proof shape: recomp counts (new splits enumerated; the
  `0x3E3AD8`-class address covered) + a ladder-identical boot
  pair (emitter snapshot with vs without the rule, diffed with
  `tools/ladder_diff.py` — the first real emitter-vs-emitter
  ladder; T1 §T1-7 left it to the next park brief, this is it).

## Gates and rules

- Lease `T5`: boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (the SHARED P-lane lease) — if it names another agent, poll
  every 5 min and log waits to `$W/P1/run/t5-waits.log`. Max 2
  boots, ≤90 s each, foreground. No `adb`.
- Fork: the analyzer change + its unit tests ONLY (`git add`
  those NAMED files only — never `-A`; never stage
  `register_functions.cpp` or any generated/ignored file).
  Verify the diff before committing. One commit per change with
  the `Orchestrated-By: Muse Code` trailer. `git pull --rebase`
  before pushing; `git push` ONLY inside the fork clone to the
  `fork` remote. Stop on any foreign rebase conflict. Purge
  `._*` before staging (ExFAT precedent).
- Evidence dir `local/research/T5/` (STANDALONE — do NOT append
  to `P1/REPORT.md`; a peer brief owns it). Commit with `git add
  -f`, prefix `[T5]`, trailer `Orchestrated-By: Muse Code`. NEVER
  run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — the rule (fork commit + tests)

1. Record tree state at start (`git log`, `git status`; base
   must be `a611702` — if the tree moved, table the new HEAD
   and rebase your work onto it, don't build on stale).
2. Implement the rule in the analyzer (folded-constant → split
   when inside `.text`), following the `f2149e7` code shape.
   Unit tests first: a `.text`-interior folded constant splits;
   a data-segment constant doesn't; the `0x3E3AD8`-class case
   splits (construct from the Part-11 receipt, table the
   fixture).
3. Suite BEFORE (new tests fail) / AFTER (all green) with
   per-face counts. Commit + push per the rules above.

## Task 2 — proof (counts + ladder-identical boots)

1. Recomp counts: split count before/after on the SSX3 ELF (old
   vs new analyzer, same tree otherwise); enumerate every NEW
   split address (or count + first-50 with the full list in the
   receipt if long); confirm the `0x3E3AD8`-class address is
   covered.
2. Two ≤90 s boots (lease-gated): WITHOUT the rule (baseline
   binary) and WITH the rule (new binary), both with
   `PS2X_DIAG_PARK=1`. Emitter-vs-emitter `ladder_diff` must
   read 0 deltas (throughput rows TOL_OK per the tool's rules;
   any COUNT_DELTA/KEY_DELTA = the rule changed behavior —
   table it exactly, don't explain it away).
3. Table the boot pair (lines, stub series, thread ends, drops,
   crash/FATAL) in one ladder table.

## Report

`local/research/T5/REPORT.md`: rule + fixture table +
BEFORE/AFTER suite + split enumeration + boot-pair ladder +
D-verdict + gap rows. Commit evidence (`[T5]`, no push), stop.
