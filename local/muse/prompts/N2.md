# N2 — First Android build of the PS2 recomp runner on bytesize (stub APK, then full-title link)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`). **Tables
+ receipts; recommend, the orchestrator decides.** Read first, all of
it: `local/research/N1/REPORT.md` (scaffold audit, P1–P8 paths,
L1–L9 linkage, Mission 3 = your build spec, bars B1–B6, stop rules,
H1–H8 handoffs). Goal of the track: a reproducible arm64 APK of the
PS2Recomp runner that later launches stock SSX 3 on the Odin. This
brief builds; it never touches a device.

## Where you build: bytesize WSL, not the Mac

The Mac has ~2.8 GB free and no Android SDK/Gradle/JDK 17, and NDK
lld outputs on the ExFAT SSD read back as zeros. Build on bytesize:
`ssh bytesize` then `wsl -d Ubuntu -- bash -lc "..."` (Ubuntu 24.04,
20 cores, ~896 GB free, cmake + ninja + git present, no Java). Work
under `~/n2/` there. Install everything user-local, no sudo:
Temurin JDK 17 tarball, Android cmdline-tools → `sdkmanager`
`platforms;android-34` `build-tools;34.0.0` `ndk;28.2.13676358`
`cmake;3.22.1`, Gradle 8.9 distribution (generate the wrapper once, as
the fork's README says). Record every download URL + sha256.
Nothing on bytesize is shared with the T lane (parked) — you own it.

## Mission 1 — stub build (N1 Mission 3 verbatim)

Clone `github.com/brad-richardson/PS2Recomp` branch `ssx3` and check
out exactly `3adc0478b6d2260acdd28a249466f2eef9a20176`. Build
`./gradlew assembleRelease` with the fork's own cmake args and
`-Pps2xBootElf=/storage/emulated/0/Android/data/com.ps2x.runner/files/SLUS_207.72`.
Pass bars B1–B6 from N1 (exit 0; configure lines; `file`; `nm -D`
shows `ANativeActivity_onCreate` and name its provider; size budget
with `size` output; two builds → `.so` sha equal or cause tabled;
fork unmoved). Stop rules from N1 apply unchanged: a compile/link
error in runtime sources is tabled for the E lane, not fixed here.

## Mission 2 — full-title link (N3), only if Mission 1 passes

1. Copy the generated title set to bytesize: C17 set at
   `/Volumes/Extreme SSD/ps2x-i17/codegen-output` (9,455 `.cpp` + 2
   headers, 265,956,572 logical bytes). Tar it on the Mac side into
   the SSD scratch, scp, untar, and verify a sha manifest on both
   ends (2 reads on the SSD side, per the corrupt-link rule). This is
   generated guest code: it never goes to GitHub or any public place.
2. Port H1 locally: on a LOCAL branch `n2-android` in the bytesize
   clone only (never pushed), apply the `PS2X_GAME_CODEGEN_DIR`
   mechanism from `eb3fb16` + the I21 drop extension
   (`193451a:515–523`) — read both from the Mac fork clone at
   `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` with `git show`
   (read-only; that clone is the E lane's, do not check anything out
   there). Diff receipted in full.
3. Rebuild with the codegen dir set. Bars: arm64 `.so` links;
   defined `sub_*` count vs 9,441 (reconcile the 9,441 / 9,455 /
   9,457 delta N1 tabled); function table linked; `file`/`nm`/size
   re-run; linkage manifest (object count, symbol census, section
   sizes) committed as text.
4. Bring the APK back to the Mac SSD under `n2-android/` with sha
   verified on both ends (2 reads Mac side). Do NOT install it.

## Rules

- No device, no adb, no install, no boot, no leases needed.
- Zero commits/pushes on the fork's `ssx3` ref. Local branch only.
- Evidence `local/research/N2/REPORT.md` + build/configure log tails
  + manifests (text only) + `[N2]` commit with `Orchestrated-By: Muse
  Code` trailer, `git add -f`, no push. Check `git log -1` right before
  committing.
- Time box 6 h. If a step needs a password or a GUI, stop and say so
  in the pane — do not work around it.

## Success bars (tabled)

Toolchain table with URLs + shas; B1–B6; entry-provider row;
Mission 2 linkage manifest + APK identity (or the precise first
failure with file + error); what N3/N4/N5 now need (H2–H8 status).
