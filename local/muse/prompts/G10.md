# G10 — Same-boundary dual replay (PCSX2 + paraLLEl, per-pass reset)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G9.md` (all — aspect explanation
ACCEPTED, no 597 hunt, §4 next action SUPERSEDED by the redirect below) +
`docs/reports/G8.md` §3 (alignment table + flush series, WITH the erratum:
stats accumulate across passes unless reset — per-pass reset is REQUIRED).
This brief executes the frontier redirect, not G9's §4.

## Facts you start from

- Frontier redirect (authoritative, restated verbatim in effect): (1) STOP
  597 hunting — aspect explanation accepted (512 internal both sides; 597 =
  screenshot-path aspect). (2) Accounting: consume_flush_stats runs only on
  the last pass, baseline only pre-cold, no reset on restart — G8's 544 /
  first=306 span BOTH passes (first = 272 cold + 34 warm); viability holds,
  no recapture just for this. (3) Replacement task: replay the EXISTING G8
  dump through PCSX2 + paraLLEl at the SAME packet/vsync boundary and native
  geometry, with a bounded 8-scanout series and PER-PASS stats reset.
  ScreenshotSize=2 alone still compares REF pre-vsync0 vs candidate vsync0 —
  the same-boundary design fixes that. (4) Check the pinned reference's
  dump-replay capability first; if reference recapture is unavoidable,
  combine uncorrected size + same-boundary reference snapshots in ONE run.
- Experiment contract (required): hypothesis = same-boundary dual replay
  yields an exact-geometry comparable pair with per-pass raster counts;
  observable = boundary definition, per-side scanouts, per-pass flush
  series, FIRST-vs-REF diff at exact geometry if paired; alternatives =
  (a) reference cannot replay the dump → capability table + the ONE
  combined run, then stop; (b) no exact pair even same-boundary → table +
  recipe, stop; (c) pair found → diff + verdict + ONE next action. Stop:
  existing dump + at most ONE combined run — no tuning loop, no per-vsync
  series here (still queued after).

## Gates and rules

- Reuse G8/G9 clone/build/dump (verify `3a66c197` HEAD + dump shas first;
  rebuild `-j2` only if the tree moved). Byte caps declared up front,
  tracked in ALLOCATED bytes; SSD reuse only, nothing new on `/`.
- No contention with other lanes: no recomp boots/builds, no P-lane lease,
  no fork writes, no `adb` installs. The optional ONE combined capture run
  (only if reference replay is impossible) is bytesize PCSX2-side with a
  wall cap, T28 recipe shape, no lease.
- Commit in ps2xGS as you go (prefix `[G10]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G10/` (`git add -f`,
  `[G10]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — reference capability + boundary definition (no runs)

1. Can pinned PCSX2 (`9056c08349`) replay the existing G8 `.gs`? Table the
   mechanism (command/flags/build needs) or the blocking reason with the
   exact gap. Start from the official GS Dump Runner docs
   (https://pcsx2.net/docs/advanced/gsdumprunner/ — dump runner + image
   comparison + SW option) and VERIFY each facility in the pinned tree/build
   before adding tooling — the doc is a pointer, not proof.
2. Define THE boundary: packet index + vsync index both sides must share;
   table native geometry each side + the expected pair (FIRST-vs-which-REF).

## Task 2 — dual replay + 8-scanout series (bounded)

1. Replay existing dump on BOTH sides at the defined boundary (native
   geometry; paraLLEl with PER-PASS consume/reset — table cold vs warmed
   series separately).
2. Bounded 8-scanout series (existing content only). If the pair is exact-
   geometry: FIRST-vs-REF diff (G8 `g8-diff.py` shape). If recapture was
   unavoidable: the ONE combined run's table instead.
3. Hypothesis verdict + the ONE next action (per-vsync series only if the
   method now supports it).

## Report

Mirrored `REPORT.md`: contract, capability table, boundary definition,
replay + series tables, conditional diff, license receipts, exact commands,
gaps. Commit both repos as above, stop.
