# N8D7M12 Part 6M6R — resume pinned package on an SSH-capable worker

You are the Muse package worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, the complete `local/muse/prompts/N8D7M12P6M6.md`, and `local/research/N8D7M12P6M6/{REPORT.md,ORCH-GATE.md}`. Execute the P6M6 build brief using the unchanged P6M5 staged inputs/manifest. The prior OpenCode worker stopped before transfer because its tool policy denied `ssh bytesize`; this worker route is authorized to use SSH to the private build host. Verify that the fresh WSL root is absent before writing. Do not reuse the prior stop receipt as build evidence.

Own only `local/research/N8D7M12P6M6R/` text scripts/receipts and private Mac `~/dev/ssx3-work/N8D7M12P6M6/app-release.apk` after success. Same budgets, one build, stop rules, source and package gates as P6M6. No source/fork/renderer/collector edit, no Odin/emulator action, no public binaries/game bytes, no push, no subagents. If access, transfer, source verify or build fails, stop and report the table; no repair loop. Commit `[N8D7M12] Part 6M6R` with `Orchestrated-By: Muse Code`. Hand back table and gaps, not a GPU verdict.
