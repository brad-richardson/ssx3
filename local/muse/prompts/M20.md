# M20 — Interior-cell mechanism: mid-vs-blend offsets + filter-tap fits (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M19/REPORT.md` first (all of it —
M19 gap 1 is this brief), plus M18's Test W/P tables for the
weight/phase context this run builds on. Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device work.

## Facts you start from

- M19's cell 7 (strict interior: mid strictly inside (v0,full),
  v0≠full): 2475 B on s0 (10.9% of R_0; 2539 B on m15), byte
  negrate 0.81/0.80, 53.1% mid-band on s0-Y, 15.2% of interior Y
  inside shape-701's mask, zero interior bytes in HUD-draw masks
  366/370/376. The ONLY w-sensitive residual class (M18) — and
  the only cell still unattributed at value level.
- Per-byte offset δ = mid − blend(v0,full) (blend = floor-mean,
  M15-verified) is the observable: its distribution (spread?
  quantized? side-biased?) plus position-dependence (edge
  distance? gradient magnitude, reusing M18's P1 deciles?) plus
  filter-tap fits (does a fixed small tap set explain δ from
  the v0/full neighborhoods?) decide what this cell is.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m18/m18.txt` + `/Volumes/Extreme
  SSD/m19/m19.txt` (receipts: shas, baselines, curves, cell
  tables — re-verify input shas, don't trust them). Work dir:
  new `/Volumes/Extreme SSD/m20/`; evidence `local/research/M20/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m19/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m20/` + `local/research/M20/`.
- Evidence dir `local/research/M20/` (STANDALONE). Commit with
  `git add -f`, prefix `[M20]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18/M19 precedent).

## Task 1 — offset distribution (what δ looks like)

Over s0 + m15 interior bytes (cell-7 membership recomputed from
the dumps, counts must match M19's 2475/2539 exactly or table
the mismatch and stop):

1. δ histogram (signed, full range) + |δ| histogram + per-plane
   split (Y vs U/V — is interior a luma or chroma phenomenon?).
2. δ vs edge distance (pixels from nearest v0≠full boundary, both
   directions) and δ vs M18-P1 gradient decile (same decile edges
   — reuse, don't reinvent).
3. δ sign vs (v0−full) sign joint table (does mid lean toward one
   endpoint's side? how far — δ/|v0−full| ratio distribution?).

## Task 2 — filter-tap fits (does a tap set explain δ?)

1. Fit small neighborhoods: does mid == round/blend of a fixed
   2–4 tap stencil over v0 (or full) on the interior bytes?
   Table per-stencil hit rates (bilinear 2×2, h/v 2-tap, 4-tap
   cubic-ish — fixed candidate list in DESIGN.md, no fishing).
2. Positional fit: δ vs sub-pixel phase proxy (v0−full parity?
   coordinate parity?) — table, don't model beyond the bars.
3. Carrier cut: 701-mask interior vs rest (M19: 701 holds 15.2%
   of interior Y) — same δ shape or different?

## Task 3 — controls + determinism (M18/M19 precedent)

1. Positive control: synthetic truth with KNOWN δ (e.g. mid =
   blend+1 on interior sites) — recover the injected histogram
   exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M19).

## Report

`local/research/M20/REPORT.md`: DESIGN.md (bars first) + δ
tables + stencil hit-rate table + carrier cut + controls +
explained-vs-standing update (does any stencil shrink cell 7?
by how much?) + gap rows. Screenshot only if a δ map
discriminates (budget-noted). Commit evidence (`[M20]`, no
push), stop.
