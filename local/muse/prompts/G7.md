# G7 — paraLLEl-GS bounded adoption check (capability gate + one short SSX replay)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts**. Read first: `docs/reports/G6.md` (harness + sensitivity
method) and the frontier findings
`/Users/bradrichardson/dev/ssx3/docs/research/review-2026-09-20-first-frame-and-gs.md`
(§paraLLEl-GS + §GS gates — this brief executes its adoption experiment).
This candidate was absent from the P3/P4/P5 shortlist; it offers a standalone
Vulkan compute GS plus dump replay tools. Treat it as an independent
implementation, NOT an infallible oracle (its docs do not claim
hardware-exact interpolation).

## Facts you start from

- Pin source + deps: paraLLEl-GS `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd`
  (LGPL-3.0-or-later headers; Granite MIT + its dep inventory). Shallow clone
  under `/Volumes/Extreme SSD/` (NEVER the internal volume — ~20 GiB free;
  table clone bytes). No code copied into any GPL tree; any adoption hunk
  gets a receipt (URL, rev, file/range, license, notice, local changes,
  behavior checked).
- Experiment contract (required per experiment): hypothesis, observable
  signal, alternatives, stop condition, how each outcome changes the next
  action. An unsupported feature or material obstacle is a USEFUL result:
  table it and STOP that arm — do not launch a port before this gate.
- Dump: ONE short SSX GS dump (native res, 1× sampling). Sources in order:
  R1's running PCSX2 on bytesize (ssh/scp, bounded capture, no lease) or
  current recomp traffic small captures. Table provenance + dump version vs
  pinned parser version (verify, do not infer from README numbers).

## Gates and rules

- Mac/MoltenVK capability gate FIRST, per the §131 checklist (descriptor
  indexing, timeline semaphores, buffer device address, 8/16-bit storage,
  shaderInt16, scalar block layout, subgroup ops + size-control predicate,
  ≥32 KiB shared). Report EACH item pass/fail on the real implementation —
  README lists and "Vulkan 1.1" labels prove nothing.
- Odin arm (bounded): capability/interface check via existing tooling only;
  if it needs new device infra, table OPEN + exact recipe instead. No
  contention with K1: builds `-j2`, no recomp boots/builds, no P-lane lease.
- Reuse existing tools (T1/T5/T12/T22 class: aligner, discovery, captures);
  do not duplicate harness work. Byte caps on all runs; no multi-GB
  retention (briefs now carry byte caps — table yours).
- Commit in ps2xGS as you go (prefix `[G7]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` to
  `/Users/bradrichardson/dev/ssx3/local/research/G7/` (`git add -f`,
  `[G7]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — capability gate (Mac/MoltenVK, then Odin if cheap)

1. Pin + shallow-clone (table rev + bytes). Build the init/capability path
   only (`-j2`).
2. Per-item checklist table vs gs_renderer.cpp:798–829 on each attempted
   target. First hard fail → table, stop that arm, recommend.

## Task 2 — one short SSX replay (only if a target passes Task 1)

1. Capture/obtain ONE short SSX dump (table provenance + version check).
2. Replay at native res 1×: capture image, correctness diffs vs an
   independent reference, GPU time, memory. Table all four.
3. Verdict-shaped output is banned; instead: hypothesis verdict per arm +
   the ONE next action each outcome justifies.

## Report

Mirrored `REPORT.md`: contract table per experiment, capability checklist,
replay table (image + diffs + time + memory), license/provenance receipts,
exact commands, gaps. Commit both repos as above, stop.
