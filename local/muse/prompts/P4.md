# P4 — Mine the PS2Recomp fork network + downstream ports for borrowable fixes (read-only research)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/P3/REPORT.md` (all of §8: clone paths, license verdicts,
and the admitted gaps P1/P5 you are closing) and
`local/research/P1/REPORT.md` Part 8 (current rung: the LUI+ORI MMIO
fold — P1h owns that fix; your job is everything AFTER and AROUND it:
scheduler/IRQ/alarm/CD/VIF lifecycles). Evidence dir `local/research/P4/`
(commit with `git add -f`, prefix `[P4]`, trailer `Orchestrated-By: Muse
Code`). Time box 4 hours.

## Rules

- Read-only research. Do not edit the PS2Recomp fork, this repo's
  `tools/`, `native/`, `vendor` trees, or any other agent's evidence
  dir. No builds, no boots, no emulator runs, no leases, no `adb`.
- Every claim cites a file you opened (path + line numbers); `grep`
  only locates. Quote full diff bodies, not one-line summaries.
- Reuse the existing clones under `/Volumes/Extreme SSD/{fork-survey,
  q2-recomp-scan,q3-siblings,dobiestation-q4,upstream-baseline}`; no new
  clone over 200 MB without recording its size first. Never clone PCSX2
  (P2's angle) — cite P2's report instead.
- Re-cite (do not re-derive) P3 §8.6 license verdicts per snippet;
  anything PolyForm/unlicensed stays idea-level only.
- NEVER run `git push` in `/Users/bradrichardson/dev/ssx3` (the
  orchestrator owns that origin).

## Questions (in priority order; stop each at its cap and record partials)

1. **Fork diffs, with bodies (90 min cap).** For the P3 M-table revs
   (M1–M14: rogue-galaxy, sorachi00, smmathews-fiber, phmdacosta,
   hedgeg0d, mrcool, gtteancum, sinan, heap layouts), open the
   `fork-survey/` clones and read each diff vs `upstream-baseline`
   (`14b1e5cb`). Table per rev: files changed, mechanism in the
   author's own code (quoted), which ladder rung it bears on
   (handler/alarm stacks, CD callback, heap layout, dispatch, other),
   why it matters or doesn't for SSX 3.
2. **bt3-recomp: diff a WORKING port (90 min cap).** It boots through
   menus into rendered fights. Against our fork at `6046260`: how do
   its boot/loader path, `[mmio] TOML` handling (does it hit the same
   LUI+ORI fold? check its toml + analyzer), GS/GPU-path init, and CD
   handling differ? Table each difference with file:line on both
   sides. This is the highest-value target: a working end state.
3. **Other downstreams, boot learnings only (30 min each).**
   sm2 / halogen / drakengard / reo: what rung each reached, any fix
   or workaround we lack, file:line. Skip any whose clone has no
   boot-relevant delta vs upstream (one row saying so).
4. **Delivery point for message-not-nesting (30 min cap).** P3 §8.4
   covers the N64ModernRuntime pattern — do NOT redo it. Only answer:
   where exactly in `EeScheduler` would a queued-event handoff land
   (function + line + what the dequeue site would check)? One row.

## Steps

1. Questions 1–4 in order with caps; record partials as unresolved.
2. **Borrowable-snippet table**: mechanism → source (project, rev,
   file:line, quoted body) → adaptation sketch to our fork at
   `6046260` (function-level prose, NOT applied) → license re-cite.
3. **Report.** `local/research/P4/REPORT.md`: rev table, mechanism
   table, bt3 diff table, downstream rows, delivery-point row,
   borrowable-snippet table, clone revs + paths, exact commands,
   "What I could not do". Commit `local/research/P4/`. Stop.
