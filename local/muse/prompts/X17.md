# X17 — DeepSeek V4.1 Flash worker pilot

You are an OpenCode Go `deepseek-v4.1-flash` worker in a bounded model trial. Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, and `local/AGENTS.local.md`. Do not edit any OpenCode configuration. Read only this brief and `local/research/X15/excerpt.txt` (11,013 bytes, SHA-256 `eac602143522d2ec2fe3c6b062070ee822c32a5fecd258400a9e66a1a102c146`). Cite the repo excerpt, not the web. This task has no implementation or GS/device verdict. No search, other source read, build, boot, device, web, push or source edit. `local/research/X17/rows.tsv` and `REPORT.md` are your only write paths.

Within 60 seconds of the excerpt read, write `rows.tsv` with header `field<TAB>value<TAB>evidence` and exactly these eight rows in order: `EN1`, `EN2`, `INT`, `FFMD`, `force_progressive`, `is_interlaced`, `raw_circuit_early_return`, `deinterlace_branch`. Values are only `0`, `1`, or `unknown`. Interpret pinned `pmode=0xff21 smode2=0x1` through the excerpt bitfields and follow backend→interface→renderer predicates. For raw-circuit return, use 1 only if all prerequisites allow it; for deinterlace, use 1 only if the shown branch executes. Evidence is exact `filetag:line` anchors separated by semicolons, no prose. Use `unknown` for an unsupported answer. Do not infer image quality or a driver cause.

Try one `lsp goToDefinition` at `/Users/brad/dev/ssx3-work/N8D5L/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp` line 255 on `vsync`; record the exact target or empty/error. Do not read another file through LSP. Wall cap five minutes, context target <18k. `REPORT.md` lists excerpt SHA, commands, elapsed time, visible context if available, LSP result, the table and gaps. There is no checker in your workspace; hand back the table for independent scoring. Commit only `rows.tsv` and `REPORT.md` as `[X17]` with `Orchestrated-By: opencode`, no push.
