# E58 — Fold the 09-23 fixes into fork `ssx3`, regen, smoke, push

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md` (git rules, the runner-dir check, leases, the I26-FAST route);
- `docs/orchestration.md` §4 (fork procedure);
- the reports of each commit you fold (below).

Write scope: a new worktree `~/dev/ssx3-work/E58/PS2Recomp` from
`~/dev/PS2Recomp` (remote `fork`, branch `ssx3` @ `eac6cba`); build/run dirs
under `~/dev/ssx3-work/E58/`; a new codegen dir
`~/dev/ssx3-work/codegen-ssx3-e58`; `local/research/E58/`. **You may
fast-forward push `ssx3` to `fork`** after every check passes (below). Never
force-push; never rewrite history; generated code never enters the fork.

## Fold (in this order, `git cherry-pick -x`, resolve conflicts, report each)

| # | Commit(s) | What | Report |
|---|---|---|---|
| 1 | `e52-audit`: `96fc89b` (tests) + `d3f7508` | E52 audit tests + E53 batch 1 (COP2/FPU semantics, RTZ+FZ, no FMA, VU1 EFU tables, `log` unbound in the toml) | `local/research/E53/REPORT.md` |
| 2 | `e45-vu-double` `310b30f` | VU1 `VuWide = double` | `local/research/E45/REPORT.md` |
| 3 | the tap guard: apply `local/research/N5/logs/tap-guard-6c335e6.diff` as one commit (`[E58] PS2X_ENABLE_DIAG_TAPS guard (from N5 6c335e6)`) | compile-time guard for the E41/E43/E44/mpg_src guest-store taps. **Default OFF on every platform** (the desktop too); the dev lanes that need taps turn it on at configure | `local/research/N5/REPORT.md` §Tap guard |
| 4 | `i26-qol` `73b8b3a` | black (not magenta) presentation fallback. Its test registration expects I25's env-file test: register only the fallback test | `local/research/I26/REPORT.md` |
| 5 | `g46-gs` `104dd7f` | CPU backend triangle fill rule | `local/research/G46/REPORT.md` |

If a pick conflicts beyond test registration or include lines, stop and
hand back the conflict hunk.

## Build, regen, checks

1. Regenerate codegen from the folded toml into `codegen-ssx3-e58`. Diff
   against `codegen-ssx3-e53` (E53's regen): expect no differences, except
   the tap guard's effect if it touches generated text. Table it.
2. Build Release with `PS2X_ENABLE_RUNTIME_LOGS=OFF`,
   `PS2X_ENABLE_AGRESSIVE_LOGS=OFF`, `PS2X_ENABLE_DIAG_TAPS=OFF`, `nice`d.
   Wait for no other clang/ninja. Record the runner SHA ×2 and the size
   of guest `.text`, compared with E53's diagnostic runner.
3. Suite from the worktree root: every test passes, except the 3 deferred
   G2 assertions (they only run with `PS2X_TEST_DEFERRED=1`).
4. **Smoke boots** (one lease slot each, PID-tracked, I26-FAST route from
   `local/research/I26/ROUTES.md`, `PS2X_PAD_SCRIPT_CLOCK=vsync`,
   `PS2X_SKIP_MOVIE=1`, ≤600 s):
   - (a) reach Select Character with the rider drawn;
   - (b) reach the race: HUD live and the clock advancing, with terrain
     geometry as in `local/research/E53/frames/e53c-race-*.png`;
   - screenshot both and view them yourself.

   This runner is diagnostics-off: record the guest vsyncs/s per phase
   from `[vsync-rate]` (`PS2X_VSYNC_RATE_LOG=1`). **Take both lease slots
   for the boot you quote.** Note any other load on the host.
5. Runner-dir check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   is empty. Then `git push fork HEAD:ssx3` (fast-forward only).
6. Promote the codegen: `mv codegen-ssx3 codegen-ssx3-pre-e58` (delete
   the older `codegen-ssx3-pre-e46` if disk is tight), then
   `mv codegen-ssx3-e58 codegen-ssx3`. Run `local/tooling/disk_budget.sh`.

## Budgets and stop rules

≤3 builds, ≤3 boots, 4 h, 6 GB new bytes (plus the codegen dir). Stop
before pushing if the suite or either smoke boot fails. Hand back the
failure and the fold commit list.

## Deliverable

`local/research/E58/REPORT.md` (fold table with old→new SHAs, conflicts,
codegen diff, suite count, runner SHA ×2, `.text` sizes, smoke frames,
the vsyncs/s table labelled "clean build, Mac", push receipt). `[E58]`
commit (`git add -f local/research/E58 local/muse/prompts/E58.md`, `git
log -1` first).
