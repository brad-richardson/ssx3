# N8D5 Part 2 — prepare a same-frame GPU sampled tile probe

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. This is a **local source candidate and Mac build only**. Do not use the Odin, bytesize, APK install, a game boot, or push any repo. The orchestrator gates the design before a device build/run.

Facts: `local/research/N8D4/REPORT.md` captured one Odin GS stream through tick 2050 and same-stream Mac replays; the Mac paraLLEl image has a readable HUD/slope/meter while the Odin mapped image is mostly black. `local/research/N8D3/REPORT.md` established Odin raw mapped/backend/frontend RGBA identity. `local/research/X13B/rows.tsv` pins `shot.image` at `Present` before `copy_image_to_buffer`/host map. `local/research/N8D5A/REPORT.md` found G43's sampled display app is desktop-only on Android and its parser does not accept `PS2XGSC1`; a new Android surface is not ready. Use existing N8D4 Mac/Odin tick-2050 images as coarse occupancy controls, not exact per-pixel GPU equality.

**One mechanism to prepare:** default-OFF `PS2X_N8D5_TILE_CAPTURE=1` at backend `Present` tick 2050. Sample the *same* `shot.image` on the GPU through a separate shader path into bounded 16×16 tile occupied-pixel counts (or a similarly small, reviewable grid), write a known GPU control pattern through that shader/storage-buffer/map path, and retain the existing raw mapped capture. The control must verify both shader sampling and the small-buffer host read; otherwise report inconclusive. Preserve presentation and game behavior with the flag OFF. Correct-behavior predictions: A, sampled image itself sparse → valid control, tile occupancy sparse, current raw sparse; B, current image-to-buffer transfer/readback loses content → valid control, tile occupancy broad like Mac, current raw sparse. A failed control, mismatched tick/page/dimensions, or off-table pair is OTHER; no cause verdict. Tile thresholds must distinguish the already viewed Mac and Odin frames and be computed in a script from the pinned PNGs before source edit.

Work only in `local/research/N8D5B/`, `~/dev/ssx3-work/N8D5B/PS2Recomp` (fresh local branch from N8B1 `17e90ded3689685ad359b76a9168c80a1f752e2d`), and a private copy of `~/dev/ssx3-work/G43/parallel-gs` under `~/dev/ssx3-work/N8D5B/parallel-gs` if a shader source is needed. Preserve the G43 source/submodule pins and list every carried uncommitted diagnostic hunk before editing. No edits to shared N8B1/G43 trees, no generated guest code, no `ps2xRuntime/src/runner` edit; runner-dir diff against `14b1e5cb` must remain empty. Confirm source connections with `lsp` if it works; report empty output plainly. Cite exact source lines.

Budget: first write `REPORT.md`, then run `python3 local/tooling/orch/n8d5b_occupancy.py > local/research/N8D5B/occupancy.json` before source edit. The pinned 16×16/threshold-32 baseline has 567 active Mac paraLLEl tiles versus 55 Odin raw tiles at tick 2050. Then one candidate implementation, one Mac CMake configure/build with paraLLEl ON and canonical E54F2 external codegen, one taps-OFF suite from fork worktree root; no tuning loop. One named compile-repair retry is allowed. Scratch ≤8 GiB, text receipts ≤8 MiB, global disk <200 GB. If Granite has no bounded shader/sample/storage-buffer path within the named files, stop after the feasibility table **without** speculative code or build. The Mac build is only compile evidence; no image or Odin claim.

Deliver `local/research/N8D5B/REPORT.md` with A/B/OTHER gates, exact source/API lines, source diffs, pinned SHA/build flags, suite counts, byte caps and gaps; commit fork candidate locally `[N8D5B]` only if code exists, and commit bounded ssx3 report/scripts/patch receipts as `[N8D5B]`, trailer `Orchestrated-By: Codex`; no push. Hand back the table, do not declare a device verdict.
