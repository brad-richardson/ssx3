# M16 — Same-frame residual attribution: warp leave-one-out scan

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/M15/REPORT.md`
first (all of it: the 3-phase capture design, `synth.py`'s warp/blend
synth, the 13418-B synth-vs-truth residual, the arm-B carrier guide and
its own-frame caveat). This brief is M15 gap 1's exact next brief:
attribute the residual to draws ON THE SAME recorded frame. No device
work.

## Facts you start from

M15's residual (13418 B, 87% of residual Y px at |d|=1) is unattributed:
arm B's carriers come from a different recorded frame ("where to look",
not the answer). The proposed instrument: a warp leave-one-out scan —
3-phase capture × per-draw NOP shapes (M14 autoscan machinery) + offline
warp per shape (~3x autoscan replays + new header code). Starting
input: the M15 dumps + `synth.py` on the SSD (read-only — copy what you
extend). The HUD-edge residual (gap 3) falls under this scan. P-lane
briefs run concurrently and share the host lease.

## Gates and rules

- Host replay runs coordinate through `/tmp/ssx3-host-lease` (M14/M15
  protocol: if it names another agent, poll every 5 min and log waits
  to `local/research/M16/waits.log`; never force a claim). Builds any
  time.
- Disk: internal disk is tight — run dirs + probes under
  `/Volumes/Extreme SSD/m16/` (new dir); players stay under `local/`
  (the build driver refuses outputs outside it). Small logs (<5 MB
  total) may live in the evidence dir. Never touch `/tmp/p1-link`,
  `ps2x-i*`, `ps2x-p1*`, `/Volumes/Extreme SSD/m14/`,
  `/Volumes/Extreme SSD/m15/` (read from them, never write into
  them), or other agents' dirs.
- Evidence dir `local/research/M16/` (commit with `git add -f`,
  prefix `[M16]`, trailer `Orchestrated-By: Muse Code`). Screenshots
  under 5 MB total (downscale or describe). NEVER run `git push`
  in ssx3.
- Time box 6 hours.

## Steps

1. **Design (lease-free, `DESIGN.md` first like M15).** The
   leave-one-out mechanics (per-draw withheld synth? per-draw
   residual delta? — implementer's call, recorded before running),
   the arm table (shape schedule, replay counts with the M15
   determinism floors), and what "attributed" means numerically
   (residual shares summing to ~13418 B on the synth frame).
2. **Run.** Lease-coordinated replays per step 1. Record per-arm:
   wall time, exit, probe bytes + shas, determinism rows.
3. **Attribute.** Per-draw residual shares on the synth frame
   (top carriers + distribution + negatives/zeros, M14-autoscan
   shape), HUD-edge draw identification if the scan resolves it,
   and the updated deliverable table: which draws carry the
   non-rigid remainder. Screenshot(s) if they discriminate
   (per-carrier overlays or absence reasoned).
4. **Report.** `local/research/M16/REPORT.md`: design + mechanics
   choice, run receipts, attribution tables, gap rows with the
   exact next brief each needs (sub-pixel model? Odin port
   readiness?), exact commands, receipt paths, "What I could not
   do". No verdicts. Commit `local/research/M16/`, release the
   lease if held, stop.
