# M32 — Upper-K confirmation: bracket the far admissions at K ∈ {233,234,366,367} (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M31/REPORT.md` first (all of it —
M31 gap 1 is this brief: the nearest would-be real-data flip needs
K≥234 (d+234 → admit; d+366/+367 need K≥366/367) — computed, not
run, since K=234/366/367 sit outside M31's DESIGN-fixed list),
plus M30's DESIGN.md (the ±K window definition, reused verbatim).
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- M31's plateau: K=1..100 row-identical (8 movers / 13 cells,
  off-span exactly the 3 far extras, 0 flips on all 7 pairs).
  Off-span sites and span distances (M31): s9 c321 r266 d+234;
  s22 c296 r400 d+366, r401 d+367; (734-c342's d+1 pair is
  in-window at every K≥1). Minimum admitting Ks, COMPUTED not
  run: 234 / 366 / 367.
- K list for this brief (FIXED — record in DESIGN.md before
  running, no additions mid-run): {233, 234, 366, 367} —
  bracketing both far admissions (below/at each threshold).
  Predictions to table (not to believe): K=233 keeps all 3
  off-span; K=234 admits r266 only (s9 c321 → present? or
  still moved? — table it); K=366 admits r266 + r400;
  K=367 admits all 3.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m31/m31.txt` +
  `m31-census-K1.tsv` (K=1 census to reproduce: 8 movers / 13
  cells / standings — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m32/`; evidence
  `local/research/M32/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m31/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m32/` + `local/research/M32/`.
- Evidence dir `local/research/M32/` (STANDALONE). Commit with
  `git add -f`, prefix `[M32]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M31 precedent).

## Task 1 — bracket confirmation (4 K values, full tables)

K=1 reproduction from the dumps (8 movers / 13 cells /
standings must match M31 exactly or table the mismatch and
stop):

1. Windowed census per K ∈ {233, 234, 366, 367} (movers /
   cells / standings + dests/offsets — full per-cell rows).
2. Off-span tail per K (pooled sites + per-shape rows —
   which far extras remain off-span at each K?).
3. Adjacent-K diffs over {100, 233, 234, 366, 367} + the
   K=100 baseline (reproduced from M31's TSV, sha-verified):
   flipping cells + mover flips per pair.

## Task 2 — admission analysis (what crosses, what changes?)

1. Per-site admission table (r266 / r400 / r401 × K values:
   off-span vs in-window at each K — do the admissions land
   exactly at K=234/366/367?).
2. Standing response: s9-c321 / s22-c296 standings per K
   (present? moved? extra-only? — the cells the admissions
   flip, tabled).
3. Below-0.95 join per K (M31's 2×2 recomputed — does any
   admission move a shape across the mover line?).

## Task 3 — controls + determinism (M18–M31 precedent)

1. Positive control: synthetic truth with KNOWN far extras
   (N shapes at d ∈ {233, 234, 366, 367} ± 1) — recover
   per-K windowed standings + off-span sets exactly.
2. Determinism: re-run Task 1 K=233 on s0+9+22,
   byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M31).

## Report

`local/research/M32/REPORT.md`: DESIGN.md (bars + fixed K
list first) + per-K census tables + admission tables + diff
tables + controls + upper-boundary closure as a TABLE
(per-K movers/cells/off-span/join — no verdict) + gap rows.
Screenshot only if a K-sweep map discriminates
(budget-noted, pre-registered split required per M23–M31
precedent). Commit evidence (`[M32]`, no push), stop.
