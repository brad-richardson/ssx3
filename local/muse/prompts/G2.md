# G2 — Harness sensitivity: second backend + mip chains prove the diff table catches real differences

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables, no verdicts**. Read first:
`docs/reports/G0.md` (especially §9 upstream candidates and §10 gaps),
`docs/gscap-format.md`, `harness/backend_factory.h`,
`harness/gsregs.h`, and the plan
`/Users/bradrichardson/dev/ssx3/docs/plan-gs-gpu-backend-2026-09-18.md`
§1–§3. G1 (game captures) stays queued behind first frame; this brief
is the reordered next step: prove the harness catches real backend
differences on synthetics alone.

## Gates and rules

- Host only, no `adb`, no device, no emulator, no lease (all runs are
  short local `gsreplay`/`gscensus` invocations). Builds `-j4` any time
  in `/tmp/ps2xgs-build`. New captures and anything over 5 MB go under
  `/Volumes/Extreme SSD/ps2xgs/synth/` (new output under +200 MB;
  total synthetic set stays under 700 MB), never into the repo.
- Do not edit anything inside `upstream/`. The `cpu` backend's default
  behavior stays byte-identical (G0 identity table must still pass
  unchanged).
- Commit in this repo as you go (prefix `[G2]`, trailer
  `Orchestrated-By: Muse Code`); push `origin main` at the end. Never
  commit captures. Do not touch `/Users/bradrichardson/dev/ssx3`
  except to write `local/research/G2/REPORT.md` there at the end
  (`git add -f`, `[G2]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box 6 hours.

## Steps

1. **Mip-chain + TEX1 captures (gsgen).** Add generator cases: TEX1
   `mmin`/`mmag`/`mxl` varied with `MXL > 0` mip chains (at least 3
   levels, distinct per-level colors so wrong-LOD selection shows as
   pixel diffs), linear-vs-nearest pairs with TEX1 filtering fields
   set, STQ-with-mip if the interface admits it. Receipts: new capture
   list with sizes + present counts, all under the SSD path.
2. **Ignored-field isolation captures (gsgen).** Paired captures that
   differ in exactly one G0-§9-ignored field: DIMX/DTHE on-vs-off,
   COLCLAMP on-vs-off, SCANMSK set-vs-zero, `aa1` set-vs-clear, ZTE
   off-vs-on with identical ZTST (plus one ZTE-off control where ZTST
   would reject — the behavior change must be visible). Receipts: pair
   table (capture A, capture B, the single differing field).
3. **Second backend `strict`.** New factory backend honoring TEX1
   filtering + mip selection, DIMX/DTHE, COLCLAMP, SCANMSK, `aa1`,
   and ZTE gating of ZTST (wrap or copy the CPU backend on our side —
   your call; `cpu` untouched). Receipts: the diff, factory
   registration, `gsreplay --backend strict` runs.
4. **Sensitivity + specificity tables.** (a) All new captures replay
   exact on `cpu` (identity holds). (b) Cross table `cpu`-vs-`strict`
   per capture: max diff, % bad, VRAM exact — every honored field must
   flip ≥1 row from exact to diff (sensitivity), and every capture not
   touching an honored field must stay exact (specificity, no false
   positives). Map each flipped row to its field. Receipts: both
   tables in full.
5. **Census + tests.** Run `gscensus` on the new captures (record any
   undecodable records as census gaps — do not patch the census in
   this brief). Extend CTest: strict-diffs-on-mip, strict-exact-on-
   control, cpu-identity-on-new-captures. Whole suite passes.
6. **Report.** `docs/reports/G2.md` in the repo and a copy at
   `/Users/bradrichardson/dev/ssx3/local/research/G2/REPORT.md`: new
   captures, pair table, backend diff summary, both replay tables,
   census delta + gaps, CTest output, exact commands, receipt paths,
   "What I could not do". No verdicts. Commit both, push ps2xGS
   `origin main`, stop.
