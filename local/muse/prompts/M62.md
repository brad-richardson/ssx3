# M62 — c311 gap-13 interval: row-interval contents rows 323–334, both columns (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M61/REPORT.md` first (all of it —
M61 gap 1 = this brief, taken on orchestrator judgment: c311's
largest inter-site gap (322→335, 13 rows) vs its other gaps
(3/1/1/1) — the tabled locus where runs would break if c311
split), plus `local/research/M60/REPORT.md` (spans: c311 19,
c312 63 nested) and `local/research/M56/REPORT.md` (hole
machinery — the M56 c343-hole itemization is the shape model).
Fully offline — SSD dumps read-only, no harness code, no
replays, no lease. No device work.

## Facts you start from

- The interval (reproduce exactly or table the mismatch and
  stop): c311 m15 tail-Y rows [317,320,321,322,335,336]
  (gaps 3/1/1/13/1); c312 rows overlapping 323–334: NONE
  (c312 jumps 319→341 — the interval sits inside c312's
  22-row gap too). Rows 323–334 on either column are
  untabled except at c312's site rows (M61 §What-I-could-
  not-do 2).
- The question: per-row contents of rows 323–334 on BOTH
  columns (bulk/noncell/tail per row + δ where bulk +
  decile + band) — is the gap-13 interval empty on both
  columns, and what deciles/bands do its rows carry?
  Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m61/m61.txt` (pair rows to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m62/`; evidence
  `local/research/M62/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m61/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m62/` + `local/research/M62/`.
- Evidence dir `local/research/M62/` (STANDALONE). Commit with
  `git add -f`, prefix `[M62]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M61 precedent).
- Hygiene (F3 rec 17): write the report in chunks; put
  `tail -3 REPORT.md` in the analysis receipts; a truncated
  tail fails the gate.

## Task 1 — interval reproduction (do the flanking rows reproduce?)

c311's 6 + c312's flanking rows (319/341) recomputed from
the dumps (must match M61 exactly — rows, δ, deciles, gaps
— or table the mismatch and stop):

1. Flank table: c311 rows 322/335 + c312 rows 319/341 with
   full value rows (δ/decile/band/d).
2. Interval table: rows 323–334 on c311 (kind + δ + decile
   + band per row, 12 rows).
3. Interval table: rows 323–334 on c312 (same 12 rows).

## Task 2 — interval census (what's inside gap-13?)

1. Bulk table: any bulk sites in 323–334 either column?
   (rows + δ + deciles — or NONE tabled).
2. Decile table: per-row deciles over the interval both
   columns (do deciles drift across the gap?).
3. Hole-join table: gap-13 vs M56's c343 hole (28–288:
   bulk 1/8, 0 tail) — same shape class? (counts tabled,
   no verdict).

## Task 3 — controls + determinism (M18–M61 precedent)

1. Positive control: synthetic truth with KNOWN interval
   contents (N bulk + N noncell rows, known δ/deciles) —
   recover the interval tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M61).

## Report

`local/research/M62/REPORT.md`: DESIGN.md (bars first) +
interval tables + census tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if an interval map
discriminates (budget-noted, pre-registered split required
per M23–M61 precedent). Commit evidence (`[M62]`), stop.
