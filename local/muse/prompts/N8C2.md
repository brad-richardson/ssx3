# N8C Part 2 — app-local libhardware loader probe

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4/6.
Read `local/research/N8C1/REPORT.md` fully, then N8B1/B2 and the N8B2
launcher. Hand back tables/receipts; the orchestrator decides the gate.
Named report and `~/dev/ssx3-work/N8C2/` paths are your write scope.
“First failure” means a failed brief step, not an exploratory typo. This
is **one compatibility candidate**, at most two builds for one named
compile/packaging repair, and **one Odin install/launch**. No fork source
edit/push, upstream contact, iPhone action or system library copying
into the APK.

Pin N8B1 Android source branch `n8-turnip-apk` at
`17e90ded3689685ad359b76a9168c80a1f752e2d`, codegen register SHA
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`,
G43 paraLLEl source as N8B1, Turnip ELF SHA
`717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`.
Do not alter the canonical N8B1 APK. Use a fresh bounded bytesize WSL
`/home/brad/n8c2/` build tree with external codegen/driver, or an
isolated source snapshot; one heavy bytesize job at a time. Two matching
SHA reads before copying each pinned binary/input and after transfer.

## One candidate and discriminating gate

1. Write the exact diagnostic source at
   `local/research/N8C2/hwcompat.c` (text in Git) and copy it to bytesize.
   Build an arm64 `libhardware.so` with SONAME `libhardware.so` exporting
   only the C ABI `int hw_get_module(const char *, const struct
   hw_module_t **)`. For any call, set nonnull output to null, log a
   bounded call count, requested ID, return and its own mapped path,
   then return `-ENOENT`. Link only Android public NDK libs. This is a
   **probe**: N8C1 found one path that tolerates a missing gralloc
   module, but three that return null; normal gameplay choice is
   unknown. Do not implement callbacks, fake module success, package
   seven system private ELFs, patch Turnip, or add a fallback.
2. Check the shim ELF export and `DT_NEEDED`/SONAME, source+binary SHA
   pairs and license/source provenance. Stage it beside pinned Turnip
   in an out-of-tree `jniLibs/arm64-v8a`. Build at most two arm64 Release
   APKs with N8B1 Gradle properties and all five diagnostics-related
   CMake flags OFF. Second build only for one named compile/link/package
   repair. Gate APK: exactly runner, Turnip and shim arm64 members; no
   x86; double-read APK and each member SHA, runner Build ID; Turnip
   member equals pin. Stop before device if packaging or diagnostics fail.
3. If package gate passes, copy APK to Mac with two matching SHA reads.
   Odin `622c49b1`: preflight connected, lease free, keyguard
   `showing=false`, battery >=20% and charging/full (status 2 or 5).
   Claim lease; **always install** the new APK once; verify installed
   APK plus stock ELF SHA
   `1b49d05ca2793922180851b9e1ce9ae2291d61a7863565ac4e71f12e967af7bc`
   and ISO SHA
   `3c2f8eb182c9c6208a6e8172a41e61c98f420abe3f42c845f6829aeb9761ebf5`
   on two device reads each. Use N8B2's env, I26-FAST pad route and an
   empty card. Recheck keyguard/battery/your lease before launch; its
   prior self-check bug must not recur. One `NativeActivity` launch,
   title progress cap 180 s, race target tick >=2050, hard wall cap
   600 s. Stop early at first definitive failure. Force-stop after the
   run on every path, verify PID absent, release lease.
4. Same-PID bounded log must identify: shim mapped APK path and any
   `hw_get_module` calls/returns, `dladdr(HMI)` mapped Turnip path, HAL
   open/operation addresses, Granite Adreno 830 API 1.4.359 driver
   26.2.99 versus system identity, `[gs:parallel] init ok`, nonzero
   GIF/presents, and a viewed visible stock race frame. If `dlopen`
   advances but gralloc fails later, table that exact stage. Never infer
   Turnip identity merely from packaged ELF. No speed claim from this
   diagnostic launch. Pull at most three SHA-paired PNGs and inspect
   them; no frame dump stream, second launch or tuning loop.

Budget: <=20 GiB new bytesize scratch, <=1 GiB new Mac bytes, <=32 MiB
text logs, <=10 MiB images; check global Mac `disk_budget.sh` before/
after >1 GiB Mac writes. Stop at failed SHA, build, package, preflight,
first runtime error or cap. Deliver `local/research/N8C2/REPORT.md`
with source/build/ELF/APK tables, exact commands, one-launch stage,
same-PID log, frame views if any, cleanup/budgets and gaps. Commit only
explicit report, diagnostic source and bounded text/small images
`[N8C2]`, trailer `Orchestrated-By: Codex`; no push.
