# G42 — Mesa Turnip contrast on the Odin: is B's missing write the proprietary driver or our pipeline? Then G41's probes if needed

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`; working tree `/Users/bradrichardson/dev/ps2xGS`,
SSD clone `parallel-gs-g7` @ `3a66c19` + carried G26/G28 + the G40 wall +
the G41 canary hunks, verify at start). **Tables + receipts; recommend, the
orchestrator decides.** Read first: `local/research/G41/REPORT.md` (all
of it: masking refuted; C1-only loss; B-loss depends on pass context, not
draw state; §4 next action) and `local/research/G40/REPORT.md` §3h–4.
Also `AGENTS.md`. No upstream contact.

## Why Turnip first

G38–G41 have narrowed B's loss to "correct, accepted draws whose writes
never land, on Adreno only, depending on pass context." Before more walls,
one run answers the biggest question. Run the **same binary and the same
G38 O1 shape** with Mesa's open-source Turnip Vulkan driver in place of the
proprietary Adreno driver.
- B lands (`840cd308…`) and scanouts show content: proprietary-driver
  fault. The product path can bundle Turnip, as Android PS2/Switch
  emulators do, and a targeted workaround becomes optional.
- B is still lost: our shader/pipeline (or something both drivers
  share). Go to Mission 2.
- The run fails to start: a Turnip setup problem. Table it and stop; that
  is not a verdict.

## Mission 1 — Turnip run (loader swap only)

1. Get a Turnip build that supports Adreno 830 (Snapdragon 8 Elite): a
   recent Mesa release packaged for Android (adrenotools-style driver
   zip; community builds for the Odin 3 exist, as does Mesa 26.x). Record
   source URL, version and sha256. If no prebuilt supports A830, stop and
   say so. Don't build Mesa in this brief.
2. Load it in the replay binary without changing rendering code. Granite
   lets you pass a custom `vkGetInstanceProcAddr`
   (`Vulkan::Context::init_loader`), so `dlopen` the Turnip `.so` from
   `/data/local/tmp/g42/` and hand it its `vk_icdGetInstanceProcAddr` /
   `vkGetInstanceProcAddr`, gated on an env var (default = system loader).
   One small hunk. Log the driver name/version the device reports (it
   must say Turnip/freedreno) as the first line of the run.
3. One Odin run in G38's O1 shape with Turnip, then (same session) one
   control run with the system driver on the same binary. Table B at
   boundary 1 (FNV/nz), scanouts, G41's C1–C5 canary cells, and the
   G26/G28 HOLD rows, for both.

## Mission 2 — only if B is still lost under Turnip

G41's recommended combined wall, one hunk, same canary shape:
(a) C1-triage: C1-exact params at the first and last ordinals, plus
swapped FBPs (492↔508); (b) B-scale: C5-exact state at B's scale (17
prims, 511×447 coverage) into scratch; (c) B-target: C5-exact state at
B's scale into the B pages with save/restore. Use G41's decision rules.

## Mission 3 — ONE fix (only if Mission 1 or 2 names a construct)

Same rule as G40/G41: one correctness-preserving hunk, one Odin run,
acceptance Odin-B == `840cd308…` + content scanouts + G26/G28 HOLD.
Under a pure driver verdict, no fix is needed in this brief. Recommend
the Turnip-bundling path instead.

## Rules

- Odin `622c49b1`, `/data/local/tmp/g42/` only, removed at end (the
  Turnip `.so` included); check `/data/local/tmp/mg/LEASE` before each
  run. Budgets: 1 Mac build if a hunk needs a Mac F0 check, 1–2 Odin
  builds, up to 4 Odin runs total. Time box 5 h.
- Two matching SHA reads for any device-bound binary or driver;
  `COPYFILE_DISABLE=1`; mirror receipts to the share tier. Hunks stay
  uncommitted in the clone; diff texts go to `local/research/G42/`.
  `[G42]` commit with `Orchestrated-By: Muse Code`, `git add -f`, no push;
  check `git log -1` first.

## Deliverable

Turnip vs system-driver table (driver identity, B, scanouts, canary cells,
HOLD rows); the verdict branch taken; Mission 2/3 tables if run; the ONE
next action.
