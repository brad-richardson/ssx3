# G29 — Black-scanout diagnosis: readback-path vs render-path — where do the zeros originate (Odin; fixed G28 binary intact)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G28/REPORT.md` first (all of it: exit 0
with the writer demonstrably fixed, yet 10 scanouts byte-identical black
to G27 — black is a SEPARATE device trait, aliasing-caused REFUTED; flag,
no-flag, sanitizer, and fixed runs ALL agree on-device black vs BRIGHT
mac oracles). This brief executes G28 §4's ONE next action ONLY: diagnose
the black. No upstream contact of any kind (standing no-upstream order —
local hunks only, filing stays local).

## Facts you start from

- The G28 binary is INTACT (orchestrator-verified: the "7th recurrence"
  reclassified TRANSIENT — two stable `450471e2…` reads + BuildID
  `53af7a56…` AFTER the worker's zero read; G21 class, file healed or
  the read glitched — likely related to the 16:26 USB reset). Re-sha
  it at start (standing hygiene — if it ever misses, rebuild +
  re-verify; never push an unverified binary).
- The question: do the zeros originate in the RENDER path (the GS
  emulation writes black into its target on-device) or the READBACK
  path (rendered target is fine, zeros enter at swapchain/offscreen
  readback or PPM encode)? Device-trait candidates are constrained by:
  mac-oracle brightness (host renders bright from the same dump) vs
  on-device black across ALL shapes (flag on/off, sanitizer, fixed).
- Task 1 (no device until §2): STATIC + LOG forensics FIRST — table
  what the committed evidence already constrains: G28's 18 G10 lines
  (which passes complete? what `img=`/texture state?), the vsync/field
  handling vs the oracles' corrected labels, whether any bound texture
  or pass output is EVER nonzero on-device (grep all committed logcats
  for nonzero pixel/census evidence), and the readback chain
  (`save_scanout_ppm` path — what it reads, what could zero it).
  This task may CLOSE the case without a run (table that) or name the
  ONE discriminating probe.
- Task 2 (ONE run max): the discriminating probe — either re-run the
  INTACT G28 binary with a diagnostic hunk (ONE hunk max: buffer-dump
  instrumentation at the chosen site — table the site + why it
  discriminates readback vs render), or NO new run if Task 1
  discriminates (thenTask 2 = the adoption/filing recommendation with
  receipts). If a hunk is needed: rebuild in a NEW dir (G22 recipe,
  `-j2`), full identity, verify-then-push with NO gap (zero-destroyer
  still nominally active — 6 persistent + 1 transient).
- Success bars (tabled): forensics table (every constraint with its
  receipt); the readback-vs-render VERDICT (with the discriminating
  evidence); if a run: exit/wall/fate + receipts + tombstone triage
  (if any) + scanouts scored; the ONE next action (adoption shape vs
  next wall — G26+G28 adoption stays queued until brightness is
  understood).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g29/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (diagnostic-dump ONLY, and ONLY if Task 1 can't
  discriminate); ONE build max (new dir, only if the hunk exists);
  TWO bounded device runs max (diagnostic run + one retry iff O1/O3
  fires instead of first draw); no tuning loop, no second shape, no
  new dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB ONLY if built; new `ps2x-g29/`
  retrieval dir ≤50 MB; 0 growth elsewhere).
- Evidence dir `local/research/G29/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G29]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).

## Task 1 — forensics (no device)

1. Re-verify pins (clone HEAD/tree incl. Granite G28 hunk HUNK_MATCH,
   G28 binary re-sha INTACT + build-id, dump, dirs 0-growth, device
   pre-check). Static + log forensics per above; table every
   constraint + receipt. Verdict if discriminated (then §2 = writeup);
   else the ONE probe design (site + discrimination logic).

## Task 2 — ONE discriminating probe (if needed) + verdict

1. If Task 1 needs it: ONE hunk + ONE build (exit 0, identity) +
   verify-then-push (NO gap) + ONE run (knob matrix tabled) + pull
   (logcat + tombstone if any + scanouts + dumps); remove the device
   dir. `g14-diff.py` vs G13 + pixel census. The readback-vs-render
   VERDICT + the ONE next action the numbers justify.

## Report

`local/research/G29/REPORT.md`: contract, forensics, hunk/build/run
(if any), verdict, next action, exact commands, gaps. Commit evidence
(`[G29]` + hunk diff text if any), stop.
