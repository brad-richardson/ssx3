# M13 — Slot-36 same-frame partition + per-draw depth attempt

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M12/REPORT.md` (all of it: the slot-0 same-frame
partition, "What I could not do" items 1 — per-draw depth — and 3 —
alternating-NOP ran slot 0 only — which this brief takes in reverse
order). Base header `local/research/M12/m12_replay_context.h` (sha in the
M12 report — record `shasum`, not memory; copy it to
`local/research/M13/m13_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M13/` (commit with `git add -f`, prefix
`[M13]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m13/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M13/` as a research header derived from M12's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M13\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M13/waits.log`. A P1u agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M12/build_replay_player.py` to
  `local/research/M13/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M12 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m13-det`, `m13-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M12-header copy, run `m13-det`
   once with an M12 slot-0 alternating env, confirm M12's end state
   reproduces (4 windows 25/25 uniform, shares 68012/37207/36479,
   guest/event clean). Control for later.
2. **Slot-36 alternating partition.** Run the M12 alternating-NOP
   machinery on slot 36 (Tex2 path): per-window ref2 rows with dual
   references on ONE shared frame. Receipts: window table
   (kref/refhash/n2/gt0/uniform2/xd2/xdmax/xdmean + samples), removed
   shares vs the slot-36 verbatim total, and the cross-check that the
   Tex2-path partition shape matches or differs from slot 0's (tabled,
   no verdict).
3. **Per-draw depth attempt (time-boxed inside the box: 2h max).**
   Attempt ONE header-only per-draw occlusion instrument (the most
   promising of: per-draw EFB readback orchestration, draw-clock
   epoch refinement below window granularity, depth-buffer sampling
   at window boundaries). Receipts: the attempt + its result (works:
   per-draw table; fails: the exact header-side blocker with
   file:line, one new sentence beyond M12's statement). If it fails
   fast, spend remaining box time on a second slot-36 frame sample,
   not on a second instrument.
4. **Report.** `local/research/M13/REPORT.md`: per-step tables
   (slot-36 partition, depth attempt), wall rows, continuation,
   header diffs per step (`diff -u` against the M12 header), exact
   commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M13/` (header, driver copy,
   analyzer changes, REPORT, waits.log). Stop.
