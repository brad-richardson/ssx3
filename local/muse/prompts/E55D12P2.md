# E55D12 Part 2 — one Load game choice Mac boot (OpenCode Go)

You are the E-lane run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full corrected `local/research/E55D12/{REPORT.md,ORCH-GATE-P1.md,e55d12_boot.py,check.py}` and E55D11 Part 2 gate. The orchestrator releases **one** Mac boot with script SHA `9efe732d358a20605fcf9fd88725095dcefd1e15e9e69f5b4a95bc359d6ee8cd` only. Verify first; mismatch stops. This pane has a per-worker edit exception only for `~/dev/ssx3-work/E55D12/**`; verify with a tiny sentinel and remove it. Own only `local/research/E55D12/` and private `~/dev/ssx3-work/E55D12/`. No fork/source edit, build, Odin/iOS action, seeded-card change, push, board/global edit, upstream contact or second boot. Check for active heavy clang/ninja before claiming the mini lane; wait if present.

Run exactly `python3 local/research/E55D12/e55d12_boot.py --label S1` **once**. Its precheck must pass: E55D3 fork `bab6eb3`, runner-dir guard empty, two matching SHA reads of runner/ISO/ELF/codegen, fresh empty cards. It claims one mini slot, boots from its own cwd, enforces ≤500 s wall/≤120 s no-progress/≤16 MiB boot log/≤2 GiB frames, stops or kills only its recorded PID and releases the slot. Do not bypass a failed precheck, cap, permission or lease; report OTHER. Do not edit the approved script. Keep raw stdout/log/probe in private scratch, gzip only closed files; no speed claim.

Evidence: preserve result JSON, pins/route/stop/lease receipts, full-resolution PNG nearest the pre-C2 frame target tick1620 showing **Load game highlighted**, post-C2 target tick1800 showing the resulting screen, adjacent `.txt` metadata and any unexpected screen. If no pre-C2 full frame exists, classify OTHER. Filename tick is snapshotter-approximate; state actual metadata tick and SHA. Curated PNG total ≤2 MiB, text ≤512 KiB. View frames yourself and report exact visible title, selected item, result message and footer; orchestrator makes the visual verdict. Verify private `[padscript]` lines: Start×1, Square×1, Down×5 (four in Options, one in submenu), Cross×2, releases, no other input. Parse bounded `probe.log` by vsync: count GetDir/mcRead calls before and at/after **C2 tick1700**, with ok/reason/length. Any post-choice call counts as API reach; `ok=0` is not copied guest bytes. Distinguish the four early empty-card GetDir calls seen in E55D11. Verify cards unchanged or report game writes. Do not commit full probe payloads.

Predeclared A = readable Load game result screen plus any GetDir/mcRead call at/after tick1700; B = readable result without post-choice call or a different readable screen (state exact text); OTHER = missing/ambiguous frame or route/pin/lease/cap mismatch. Add `check-p2.py` for pins, route, pad/probe counts/ticks, caps, frame/probe SHAs and card manifests; it cannot prove visual labels. Append REPORT with one-run counts, gaps and next action. **Stage/commit only named `local/research/E55D12/` files and this brief by explicit path**, never pre-staged docs or another lane. Check `git log -1`/`git status`, commit `[E55D12] Part 2` with `Orchestrated-By: opencode`, no push. Hand back the frames/table; orchestrator gates.
