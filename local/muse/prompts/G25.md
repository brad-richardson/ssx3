# G25 — Separation: REBUILD the G24 binary bit-identical first (on-disk copy zero-destroyed), then run it WITHOUT the flag + pixel-diff vs G13 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G24/REPORT.md` first (all of it:
unconditional `debug_mode` delivery PROVEN (`G24:` receipt), O4 ABSENT
(4-leg proof), full loop through Total-time, then NEW pre-write wall O6
(Scudo in first scanout readback, 0 scanouts, N/A); the run cannot
separate the flag path from the `feedback_render_target` ride-along).
This brief executes G24 §4's separation ONLY, with one forced addition:
the on-disk G24 binary is 100% zero-destroyed (orchestrator-verified,
G18-damage class — size/mtime preserved, all 265,840,424 bytes zero),
so a verified-identical rebuild comes first. No upstream contact of any
kind (standing no-upstream order — local hunks only, filing stays local).

## Facts you start from

- ZERO-FILL (orchestrator-verified): `parallel-gs-g24-android-build/
  tools/parallel-gs-replayer` now shas `fc62d01a…` (was `562a0bcf…` at
  build/pre-push/report-time — three matching reads), first MB all-zero,
  ELF magic gone, 100.0000 zero fraction, mtime frozen at build time
  (12:49:21 — post-run storage damage, NOT a rebuild). The TESTED binary
  was good (tombstone_18 BuildId `c7f68343…` matches; G22 binary still
  intact) — run evidence stands per the G18/G20 precedent.
- Task 1 (forced): REBUILD in the SAME build dir (same recipe/NKT,
  `-j2`) and re-sha — the link MUST reproduce `562a0bcf…` + BuildID
  `c7f68343…` EXACTLY (G24 claims a byte-deterministic link: identical
  before/after the TU relink). If it does not reproduce: table the delta
  (size/sha/build-id/`strings` presence) and STOP — never test a
  different binary and call it the separation. Re-sha at start (damage
  confirm), post-build, pre-push, report-time (standing hygiene).
- Task 2 (separation): run the REBUILT binary WITHOUT
  `--disable-sampler-feedback` (same dump/iterations, `PGS_SKIP_…
  UNSET`, `PGS_SKIP_COMPILATION_TASKS=1` KEPT — table the knob matrix;
  the ONLY delta vs G24's run is the absent flag). Discriminators:
  O6 persists (same Scudo site) => the ride-along
  (`feedback_render_target` variants/debug images) is implicated =>
  narrower hunk (deliver ONLY the flag) is the next action; scanouts
  write (bright or black) => the flag's full-upload path is implicated
  => same-instrumentation-scoped-to-that-path + the bright-vs-black
  read unblocked. Either way: `g14-diff.py` vs G13 (zero new tooling),
  O4 legs (expect absent — delivery still unconditional), O5 table.
- Success bars (tabled): rebuild bit-identical (size/sha/BuildID/ELF/
  `strings` — or the tabled STOP); ONE run (exit/wall/fate + receipts +
  tombstone triage if any); scanouts scored or N/A with the exact
  blocking wall; O6-attribution verdict (flag path vs ride-along).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g25/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ZERO source edits (the G24 hunk stays as-is, uncommitted); ONE rebuild
  (same dir); TWO bounded device runs max (separation run + one retry
  iff O1/O3 fires instead of first draw); no tuning loop, no second
  shape, no new dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (rebuild in the EXISTING dir — no new 4.5 GB dir; new
  `ps2x-g25/` retrieval dir ≤50 MB; 0 growth elsewhere).
- Evidence dir `local/research/G25/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G25]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (G24 left a `~X h` placeholder — erratum).

## Task 1 — rebuild bit-identical (no device until §2)

1. Confirm the damage (size/sha/magic/build-id vs G24 §2c pins), then
   rebuild SAME dir (exit 0, warnings tabled). Identity MUST reproduce
   `562a0bcf…` + `c7f68343…` + 265,840,424 B + ELF + `strings` hits —
   else table + STOP. Re-verify clone HEAD/hunks + dump + dirs 0-growth
   + device pre-check. Table every step.

## Task 2 — ONE separation run + score + attribution

1. Push + run WITHOUT the flag per the knob matrix; pull logcat +
   tombstone (if any) + scanouts; remove the device dir. `g14-diff.py`
   vs G13 + pixel census. O4 legs + O6-compare + O5 table. Name the ONE
   next action the numbers justify (narrower hunk vs scoped
   instrumentation).

## Report

`local/research/G25/REPORT.md`: contract, rebuild identity, run table,
score, attribution verdict, next action, exact commands, gaps. Commit
evidence (`[G25]`), stop.
