# E16 — Rebuild checkpoint from fork + run-exit closure repair + ONE guarded closure probe (E15 NEXT-BRIEF, NO MPEG fix, NO absorb)

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E15/REPORT.md` and
`local/research/E15/NEXT-BRIEF.md` first (all of both: E15 spent its one
boot, joined the type-1 MPEG request + aligned copy/Present, but closed
measurement-incomplete — `main.cpp` calls `_Exit(0)`, bypassing the
destructor footer hook, so required source shutdown counters are absent).
This brief executes E15's NEXT-BRIEF §1–§3 ONLY (closure repair + one
guarded probe). No MPEG implementation, no CSV change, no regeneration of
generated code beyond what the rebuild requires, no E14 absorb retry
(absorb stays queued as the NEXT E brief — tabled, not this one).

## Facts you start from

- CHECKPOINT LOSS: the restart wiped `/tmp` — `/tmp/p1-link` (E15's
  protected build) is GONE, with the E15 runner + suite binaries. Nothing
  committed was lost: fork `67c0a632` (pushed, branch `ssx3`) holds E15's
  five observation files; the actual-binding fixture binary survives on
  SSD at `P1/e15-binding-tests/entry/binding-test` (165,103,200 B,
  `b0aefc01…`, orchestrator-verified). Your first task is to REBUILD the
  checkpoint from the fork (verify `fork/ssx3` == `67c0a632` before
  building; suite must re-green 452/452; re-run the SSD fixture binary if
  it still executes, else relink it from current objects).
- SPACE: internal `/` has ~15 GB free; SSD has ~236 GB free. Declare an
  explicit rebuild+repair+probe budget up front and verify it fits BEFORE
  building (E14 ownership-proof machinery if you must reclaim anything —
  nothing is pre-approved for deletion).
- The repair (NEXT-BRIEF §2, strict order): do NOT change `main` or
  rebuild its generated unity group to reach a destructor. Emit the SAME
  diagnostic closure from the existing handwritten `PS2Runtime::run()`
  exit path (after `gameThread.join()`, after the last diagnostic
  producers finish, before `run()` returns); retain an idempotent
  destructor fallback; prove the path with an actual-linked non-title
  fixture (fail-before-repair / pass-after — a synthetic appended footer
  or destructor-only test is INSUFFICIENT). No guest register/RAM/
  scheduling behavior change; no MPEG behavior edit.
- The probe (NEXT-BRIEF §3): ONE guarded boot under a fresh cap (fresh
  T13 pre-claims, shared P-lane lease, wall/progress/byte caps,
  REPORT_ALL=1, E15/E7/E4 observations; lease occupied → table and stop).
  Required joins: objects/lifetime, MPEG request by DYNAMIC object (not
  E15 numeric addresses), callback absence/delivery fields kept separate,
  guarded UI3→6 graphics alignment, REAL final source footers from the
  run-exit path with count match + pending/truncation checks.
- Success bars (tabled): checkpoint rebuilt + re-verified (fork SHA,
  9,452-name audit, 452/452, fixtures); closure repair proven by the
  non-title fixture; one boot with complete joins + real footers (or the
  exact missing receipt tabled per the stop rule); E15 ABI constraints
  preserved for the later MPEG brief.

## Gates and rules

- Fork owner: you. The repair is an OBSERVATION-class commit if it
  touches the fork (named files; `fork`-remote push only; generated
  sources NEVER staged; MPEG.cpp/h UNCHANGED). No map/CSV changes.
- Boots: ≤1 guarded probe boot (+ zero title boots for the fixture —
  non-title only). No boot without the lease; never wait on another
  lane's lease.
- `export COPYFILE_DISABLE=1` on SSD steps; allocated-byte caps declared
  + tracked (ExFAT). No bytesize/WSL. Host builds `-j2` unless provably
  otherwise (table it).
- Evidence dir `local/research/E16/` (STANDALONE). Commit with `git add -f`,
  prefix `[E16]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — rebuild + closure repair (no title boot)

1. Fork re-verify (`fork/ssx3` == `67c0a632`), rebuild, suite 452/452,
   9,452-name audit, prior fixtures re-run (SSD `binding-test` reuse or
   relink — table which). Space budget declared + verified-fitting FIRST.
2. Implement the §2 repair; prove it with the actual-linked non-title
   fixture (fail-before/pass-after). Full suite + all prior actual-binding
   cases stay green; the two MPEG delivery cases must STILL fail before
   any implementation.

## Task 2 — one guarded closure probe

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Capture the §3 joins +
   real run-exit footers. Table every join; no promotion without an
   actual request/completion dependency.
2. Handoff: closure complete + same missing MPEG delivery (→ table the
   complete diagnosis for the later MPEG brief, preserving E15 §4
   constraints), another measurement gap (→ name it, repair only it), or
   a different dependency (→ name the first edge, stop).

## Report

`local/research/E16/REPORT.md`: contract, rebuild receipts, repair +
fixture tables, boot joins, handoff verdict, exact commands, gaps.
Commit evidence (`[E16]`), stop.
