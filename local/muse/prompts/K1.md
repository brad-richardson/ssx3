# K1 — Item-3 trampoline fix: GetEntryAddress returns a dispatchable entry (fork fix + validation boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read first: `local/research/R1/REPORT.md` (outcome 2: the
runtime path is FAITHFUL — the game installs patches on hardware; installer
fires identically both modes, only (64) differs) + Part 4 decision 3 in
`docs/research/review-2026-09-19-progress.md` (the specified HLE shape — this
brief implements it) + `local/research/A1/REPORT.md` (the kernel-true-scanner
premise it tabled) + fork `ps2xRuntime/src/System.cpp` (`SetSyscall` :498,
`GetEntryAddress` :1006, the `0x80011F80` table) and `games/ssx3/ssx3.toml:207`
(ret0) + `Syscalls/Dispatcher.cpp` (override check :108).

## Facts you start from

- R1 settled it: the hardware-equivalent fix is NOT ret0. Per Part 4 dec 3:
  `GetEntryAddress(n)` must return a real, dispatchable entry so the game's
  wrappers chain to the kernel: a synthetic per-syscall trampoline address in
  a reserved kernel range that the dispatcher recognises and routes to its own
  handler for n; `SetSyscall` then installs the game wrapper as usual; the 6
  KE_ERROR drops → 0; ret0@0x42c1f0 retired with the scanner running against a
  synthetic table the HLE publishes at the trampoline base.
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`
  (table HEAD at start). Plain commits per fix for speed; push the FORK remote
  only, never upstream. Generated runner sources are gitignored there and must
  never be pushed.
- Success bars (tabled, not verdicts): 6 KE_ERROR drops → 0 on the same boot;
  game wrappers installed AND chaining to kernel handlers (evidence, not
  assertion); boot reaches the same park as pre-fix; `ps2xTest` suite green
  (427/427/0 baseline); before/after boot census table (events, drops,
  stub counts, park).

## Gates and rules

- Validation boots ONLY while holding `/tmp/ssx3-p-lane-lease` (the SHARED
  P-lane lease) — if it names another agent, poll every 5 min and log waits to
  `$W/P1/run/k1-waits.log`. Max 2 boots ≤600 s each (Part 4 boot budget),
  EVERY boot script gets BOTH a wall cap and a progress cap (stub-block or
  event-count — table which bound). Builds any time (`-j4`). No `adb`.
- `export COPYFILE_DISABLE=1` on every step touching the SSD.
- Evidence dir `local/research/K1/` (STANDALONE). Commit with `git add -f`,
  prefix `[K1]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3 (the
  orchestrator pushes at poll when the tree is clean). Fork commits pushed to
  the fork remote (table SHAs).
- Time box: 6 h. Tables, no verdicts.
- Hygiene: write the report in chunks with a tail receipt (truncated tail
  fails the gate).

## Task 1 — trampoline fix (fork)

1. Implement the Part 4 dec 3 shape: reserved kernel range, synthetic
   per-syscall trampoline addresses, dispatcher recognition + routing,
   `SetSyscall` installs the game wrapper, synthetic table published for the
   scanner, ret0 retired. Table every file+line touched.
2. Rebuild (`-j4`); table binary sha + size. Suite must re-green before any
   boot (table 427/427/0 or the exact delta).

## Task 2 — validation boot + before/after tables

1. Pre-claim checks (T13 §T13-0 verbatim): lease absent, `pgrep -x`
   exit 1, binary sha, ISO + ELF sizes, free space, selftest green,
   waits-log tails, BOTH caps recorded.
2. Claim → foreground boot (liveness monitor only) → at whichever cap binds:
   SIGTERM → release AT ONCE → trace copy (lease-free) → analysis.
3. Tables: KE_ERROR drops before/after (6 → ?); wrapper-install + chaining
   evidence; park reached (same/different, tabled); event/drop/stub census
   before/after; suite result on the fix commit.

## Report

`local/research/K1/REPORT.md`: fix table (files+lines), build record, suite
record, lease record, before/after tables, exact commands, gaps. Commit fork
(first, pushed to fork remote) then evidence (`[K1]`), remove the lease if
held, stop.
