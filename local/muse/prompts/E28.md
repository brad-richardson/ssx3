# E28 — Guest-half confirmation boot: queue head + tag bytes under watch (EXISTING binary, no fork edits)

You are the E-lane worker (spawned under claude-cli opus) in the ssx3
repo (derive the repo root from this prompt's path: two up from
`local/muse/prompts/`). **Tables + receipts, no verdicts beyond the
missions.** Read first, all of each: `local/research/E27/REPORT.md`
(the closed static case: walker `sub_003DFED0`, kind-1 queue slot at
`ctx0->[0x24]+0` with head parked at `0xd49b14` by `0x3e13c8`, tag
`0x0100381c` at `0xd49b18`, re-ask branch `0x3b10ac`, host latch
owned by its own wait) + `mission-3-reask.json`, and
`local/research/E26/REPORT.md` §Mission-2(a) + `chunk-map.json`
(the CD read, the chunk walk, the park at line 22790). This brief
settles the GUEST half of E27's falsifiable prediction dynamically.
The HOST half (`getMpegPicture` entry logging, `pending.lock()`
results, `completeExternalWait` callers) needs new MPEG.cpp
instrumentation and is E29's — NOT yours. No fork edits, no rebuild.

## Facts you start from

- ORCH DECISION (gated): E27 PASS — static case closed; mutual-wait
  leg 4 broken (one party holding its own key). E28 confirms the
  guest-side predictions of E27's Mission 3 on a fresh boot of the
  EXISTING byte-identical binary (`e462e448…`, `2152e5ad…`).
- E27's guest-half predictions (your falsifiable set, pre-registered
  here, not by you): (P1) the kind-1 queue head reads `0xd49b14`
  with a byte count ≥ 14,364 from the feed onward and NEVER changes
  to end of run; (P2) `0xd49b18` still reads `0x0100381c` at the
  park; (P3) ZERO guest stores to `0xd49b14`/`0xd49b18` after the
  feed; (P4) `[MPEG:GetPicture]` ×1 and `[MPEG:feedES]` ×1 in the
  new boot log (re-confirmation, capped at 32 so 1 is a true count).
- E26's named limit still binds: `diagWatchEmit` fires on GUEST
  STORES ONLY. Design your watches around what stores can show. If
  a prediction's at-park VALUE is unobservable with the existing
  instruments (no store → no emission → no value), table exactly
  that with the reason — the observable halves still stand on their
  own. Do not invent instrumentation; do not edit the fork.
- Standing SSD rule (link PROVEN pattern-dependent corrupt): every
  lane-critical SSD byte needs 2+ matching reads separated in time
  AND corroboration against pinned committed bytes; the e26a
  captures re-sha against E26's committed pins before you mine
  them. If the SSD disappears mid-run, table + stop, do not
  improvise paths.

## Gates and rules

- Fork: `3adc0478` triple-agree FIRST; `status --short` exactly
  `?? ps2_log.txt` or table + stop. ZERO fork source edits, ZERO
  fork commits, ZERO pushes from this lane.
- P-lane lease: ONE title boot this brief (e28a), claimed per the
  standing protocol, released at end. No second boot for any
  reason. No capture-driver changes: reuse E26's driver by
  mechanical rename ONLY (new label e28a for output paths, same
  rule as e24a→e26a); the ONLY intentional change permitted is the
  watch set (new addresses + tiers), recorded with before/after
  receipts. Preflight (pins + fresh 458 suite) runs as written.
- Fix gate: STOP (diagnose + table, never patch).
- E18 ABI/BINDING constraints preserved (no source touched at all).
- Byte caps: internal reservation 3 GiB (floor 2 GiB + 0.5 GiB
  guard); SSD NEW `e28-*` paths ≤ 16 GiB; evidence
  `local/research/E28/` text-only; `COPYFILE_DISABLE=1` on every
  SSD step; zero deletions.
- Time box 8 h. Evidence commit `[E28]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 0 — Instrument + preflight (no boot)

Verify-or-restore exactly as E26 Mission 0 (pins at paths + two
matching re-shas separated in time + E25/E26 corroboration; rebuild
per E25's recipe ONLY if the tree is gone). Run the capture
preflight gate as written (all pins, zero re-pinning, fresh 458).
Re-sha BOTH e26a captures against E26's committed pins before
mining them in Mission 1.

## Mission 1 — Pin the slot address (static, pre-boot, no lease)

E27's mechanics name the kind-1 slot at `ctx0->[0x24]+0` (byte
count at `+8`, head at `+0xC`) but E28 must table its GUEST
ADDRESS with receipts before arming a single watch. Derive it from
E26's capture + E27's receipts (candidate routes: the slot head is
a pointer whose VALUE `0xd49b14` may appear in watch emissions;
the park snapshot's register file; the walker's `ctx0` base from
surrounding stores — follow the bytes, not the hope). Table the
address + derivation fully. If it is underivable statically, table
+ STOP the boot (no blind watches): that outcome is itself the
finding, with the unblocking observable named for E29. If derived:
pre-register the armed set (slot count + head + `0xd49b14`/`18` +
any carried controls, each with its P1–P3 meaning) and the exact
P1–P4 pass/fail bytes.

## Mission 2 — ONE boot (e28a) + verdict table

Spend the boot with the Mission-1 watch set. Record wall/rc/fate,
lease receipts, capture shas, and the P1–P4 verdict table with
byte/line receipts per row (confirm / refute / unobservable-as-
designed). Mine nothing beyond P1–P4 + the carried controls — this
lane is a confirmation, not a fishing trip. Close with the single
host-side observable E29 needs (one paragraph, E27's Mission-3
paragraph as the starting point, updated by what e28a showed).

## Success bars (tabled)

Restore path + preflight green; slot address tabled with derivation
(or principled stop); ONE boot with wall/rc/fate + capture pins;
P1–P4 verdict table with receipts; E29 handoff paragraph; fix gate
honored; tail receipt with byte-exact prefix sha; zero fork
edits/commits/pushes.
