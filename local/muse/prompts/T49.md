# T49 — PCSX2 entry trace of the same VU1 programs (healthy side of the E37 diff)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 runs on bytesize via `ssh bytesize` → WSL). **Tables + receipts;
the orchestrator decides.** Read first: `AGENTS.md`,
`local/AGENTS.local.md`, `local/research/T48/REPORT.md` (the patched G7
clone, `vuThread=false`, `EnableVU1=false` interpreter, Capture A route to
Select Character), `local/research/E36/REPORT.md` (loop at byte PC
0x418–0x548, exit `IBNE vi03,vi13`), and `local/muse/prompts/E37.md` (the
recomp side; **use its exact line formats**).

## Why

The recomp's VU1 programs at byte startPCs 0x40 and 0x10 (PCSX2 units 0x8
and 0x2) never leave their vertex loop on Select Character; in PCSX2 they
finish in ≤2,090 cycles. A pair-by-pair trace from both sides, same format,
lets the orchestrator find the first differing value.

## Change (one bounded log-only patch on T48's clone, gated like T48)

For the **first** MSCAL at PCSX2 start_pc 0x8 and 0x2 once Select Character
is settled (T48 Capture A's settle point):
1. `vumem`: full 16 KB VU1 data memory at program start, E37 format.
2. `vif`: every VIF1 command since the previous MSCAL, E37 format.
3. `pair`: every executed pair until the first arrival at byte PC 0x418
   (PCSX2 unit 0x83), then 3 loop iterations, E37 format. **Print PCs as
   byte addresses** (PCSX2 unit × 8) so they line up with E37.
Interpreter only (as T48). Prove preservation as T48 did.

## Budgets, rules, deliverable

≤2 builds, ≤2 capture runs, 3 h, bytesize byte cap 5 GB; bring back the
trace text (compress if > 20 MB) to `~/dev/ssx3-work/T49/` + share mirror,
and into `local/research/T49/` if < 5 MB. No mini boots, so no P-lane
lease; bytesize one-heavy-job rule applies. `local/research/T49/REPORT.md`:
patch (as text), hook sites, preservation proof, trace paths + SHAs, and per
startPC the pair that first writes vi03 and vi13 with its operands/values.
No verdict. `[T49]` commit (`git add -f local/research/T49
local/muse/prompts/T49.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
