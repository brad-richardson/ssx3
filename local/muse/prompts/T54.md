# T54 — PCSX2: draw-record census by mode and the mode-6 producers (healthy side of E43)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). **Tables + receipts; the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T53/REPORT.md`,
`local/muse/prompts/E43.md` (**same line formats**).

## Change (log-only, on T53's clone; EE interpreter)

1. At EE pc `0x363cf4` (the `jal func_364CD0` in the render-list walker
   `sub_00363C20`), per vsync: count calls by `a1` (mode 0–15), and for the
   first 8 per mode the record address `s3` and its first 4 words:
   `drec vsync mode count` / `drecs vsync mode addr w0..w3`.
2. For mode-6 records: a mirror-folded store watch on their record words
   (addresses from (1), first capture), catching the producer:
   `dprod vsync addr value pc ra a0..a3 v0 v1 t0..t9 s0..s7`, first 64.
Over Select Character settled (statefile ok for (1); for (2) the producer
may run at scene build, so arm from a fresh boot if needed, foreground).

## Deliverable

≤2 builds, ≤3 captures, 3 h, cap 5 GB. `local/research/T54/REPORT.md`:
patch, hooks, preservation, tables (modes per frame; mode-6 producers with
pc/ra/regs). No verdict. `[T54]` commit (`git add -f local/research/T54
local/muse/prompts/T54.md`, trailer `Orchestrated-By: Muse Code`, `git log
-1` first), no push.
