# M54 — QUAD underfit: per-column quadratic-residual tables (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M26/REPORT.md` first (all of it —
M26 gap 4 = this brief, taken on orchestrator judgment: smooth
quadratic reads 4/65 + 3/68 with 32/23 mass in the 2–3 miss
bin — below TWO and CONST on both frames despite ∩ curvature
on every positive column), plus `local/research/M51/REPORT.md`
(TRI 18/16 for the fit comparison — QUAD 4/3 vs TRI 18/16 vs
TWO 15/13 vs CONST 12/13). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- QUAD pins (reproduce exactly or table the mismatch and
  stop): s0 a [−0.69,−0.64,−0.40,−1.16,−0.68,−1.39,+0.21,fb]
  + m15 a [−0.48,−0.48,−0.87,−1.14,−0.80,−0.94,−0.02,+0.01]
  (full a/b/c 6dp in `m26.txt` — match the receipt lines);
  scores 4/65 s0 + 3/68 m15 with miss mass 32/23 in the
  2–3 bin (per-column colhits + errvalues in m26.txt).
- The question: per-column quadratic-residual tables
  (residual = δ − QUAD pred per site — where does the
  smooth fit fail? at peaks? at holes? in runs?) + the
  2–3-bin census (which columns hold the 32/23 mass?).
  Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles), `/Volumes/Extreme
  SSD/m26/m26.txt` (QUAD a/b/c + scores to reproduce —
  match exactly or table the mismatch and stop). Work dir:
  new `/Volumes/Extreme SSD/m54/`; evidence
  `local/research/M54/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m53/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m54/` + `local/research/M54/`.
- Evidence dir `local/research/M54/` (STANDALONE). Commit with
  `git add -f`, prefix `[M54]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M53 precedent).

## Task 1 — QUAD reproduction (do the fits + scores reproduce?)

M26's QUAD fits recomputed M26-verbatim from the dumps (must
match exactly — a/b/c 6dp per column per frame + 4/65 + 3/68
+ miss bins + colhits — or table the mismatch and stop):

1. Fit table: all 8 columns × s0/m15 a/b/c (16 fits +
   the s0-c343 fb + m15 ~0 curvatures).
2. Score table: QUAD hits/near/miss-bins per frame (4 + 3
   with 32/23 in 2–3 — which columns?).
3. Fit-ladder table: QUAD 4/3 vs TRI 18/16 vs TWO 15/13
   vs CONST 12/13 per column (where is QUAD worst?).

## Task 2 — quadratic residuals (where does smooth fail?)

1. Residual tables: per site δ − QUAD pred + |res| per
   column per frame (peaks? holes? runs? — the new data).
2. 2–3-bin census: the 32 s0 + 23 m15 sites with |res|∈{2,3}
   by column + row seat (peak rows? band edges?).
3. Curvature table: per-column ∩ depth (a × span²?) vs
   residual spread (does deeper ∩ mean worse fit?).

## Task 3 — controls + determinism (M18–M53 precedent)

1. Positive control: synthetic truth with KNOWN quadratics
   (N parabolic columns + N non-parabolic, known a/b/c) —
   recover the fit + residual tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M53).

## Report

`local/research/M54/REPORT.md`: DESIGN.md (bars first) +
fit tables + residual tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a residual map
discriminates (budget-noted, pre-registered split required
per M23–M53 precedent). Commit evidence (`[M54]`, no push),
stop.
