# F8 — Odin play candidate: patched Turnip + SIMD runtime (muse, 3 h)

## Goal
Toward ~90 % on the Odin: build Brad's next Odin play APK = PS2Recomp fork `ssx3` `b97b241` (VR4 SIMD FMAC
default on; the tip at your start if newer — record it) + paraLLEl-GS `ssx3` `3d72467` (Granite `166ba21a`)
+ the **patched Turnip** `libvulkan_freedreno.so` `a315b74aba2307a51cd8aaebb88abb84890ecc7d855516dbbcd715cf87de6224`
(Mesa fork `5a406e36dd4`). Measure it against FS2's patched APK and, if it passes, install it as Brad's play build.

## Facts
- FS2 Part 3 (`local/research/FS2/REPORT.md`, gate at the end): patched Turnip on runner `5d5c382`, APK
  `202c6974…`: race **0.639×** (final ABBA vs play 0.425×). MTVU is now the busy thread (93–94 %), so VR4's SIMD
  (Odin +5.4 % on the old driver, `local/research/VR4/REPORT.md` D1) should matter at least as much now.
- Driver on bytesize: `/home/brad/fs2/turnip/out-poll/` (check its SHA = `a315b74a…` ×2 before use).
  jniLibs swap recipe: `local/research/FS2/turnip/apk-swap-so.sh` (+ FS2's `build-android.sh` / `build-jni.sh`;
  `libhardware.so` shim unchanged). Android build recipe: VR4's fold build (`local/research/VR4/build-android-fold.sh`).
- Play env stays `~/dev/ssx3-work/odin-play/ps2x.env` (`090cc981…`: MTVU + LAG + blocks + core pins).
- Tooling to reuse (copy into `local/research/F8/`, don't edit FS2's): `local/research/FS2/run1.sh`, `launch.py`,
  `syncsum.py`, `verify_play.sh` (vsync-rate ≥ 15 lines check, three-way, `--compare-ticks`).

## Hypothesis / observable
H: F8 = FS2-patched + SIMD is faster than `202c6974` on the race (MTVU run time per frame drops; rate up).
Null: equal within A–A noise (~1 %) — then the unit's remaining time isn't FMAC. Regression (B < A by > 1 %)
→ stop, no install. Pixels must stay `diff_px=0` at t1100/t3000 (SIMD is bit-exact; the driver is proven).

## Steps
1. Build on bytesize **under `local/tooling/bytesize_lock.sh run F8 -- …`** (one heavy job; `C:` check is built
   in), APK with the patched driver in jniLibs. Record APK SHA remote ×2 + local ×2, the three `.so` SHAs in
   the APK (runner, `libvulkan_freedreno.so` = `a315b74a…`, `libhardware.so`).
2. Gate launch G1 (screening cool-down OK): `--compare-ticks 1100,3000` → `diff_px=0`; one bg/fg cycle
   (HOME 8 s → foreground) at tick ~2000; `jobs=55501 violations=0` at 4500, 0 FATAL, no device lost.
3. **Final-mode ABBA** (`odin_cooldown.py --mode final`, stop 4500): A = `202c6974` (FS2 patched, from
   bytesize `/home/brad/fs2/`, SHA-verify), B = F8. Report race vs/s, ×, three-way (MTVU / GameThread /
   GsWorker run/runnable/blocked), `flush_submit`. Every leg ≥ 15 vsync-rate lines or it's void (re-run once).
4. If gates pass and B ≥ A × 0.99: install as play — keep the old APK as
   `~/dev/ssx3-work/odin-play/app-release.apk.pre-f8`, put F8's APK at `app-release.apk`, update `SHA256SUMS`
   (APK line only; env unchanged), then `local/tooling/odin_restore_play.sh F8` and `verify_play.sh`. Otherwise
   stop and hand back; the orchestrator decides.

## Rules / budgets
Every Odin install/launch/force-stop under `local/tooling/odin_lease.sh`; keyguard + battery (AC, ≥ 20 %)
before each launch; restore play state after every leg. Fan: leave it (no `--fan performance`).
Budgets: 1 Android build (+1 retry on infra failure), ≤ 7 Odin launches, 3 h. Write only in
`local/research/F8/`, `~/dev/ssx3-work/F8/`, bytesize `/home/brad/f8/`, and (step 4 only) `odin-play/`.
First failure: stop, save the error, hand back.

## Deliverable
`local/research/F8/REPORT.md`: pins (fork, PGS, driver, APK SHAs), gate table, ABBA table (mode: final),
install receipt (SHA256SUMS diff, restore + verify output), exact commands, gaps. Commit `[F8] …` with
`git add -f` of the report + small text receipts, trailer `Orchestrated-By: Muse Code`, no push.
