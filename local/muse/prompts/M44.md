# M44 — Same-column opposite standings: c298 wipe-vs-growth + c311 11-vs-1 (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M34/REPORT.md` first (all of it —
M34 gap 4 is this brief, taken on orchestrator judgment: c298
reads a full 4-row wipe on 709 but extra-only growth on 732
(keeps s0's 4 rows); c311 reads 11 extra-only vs 1
missing-only across 12 movers), plus `local/research/M35/
REPORT.md` (c311's missing-only mover is shape 9: missing
(296,311) |δ_s0| 10 noncell ++ — the 1 in the 11-vs-1) and
`local/research/M28/REPORT.md` (732's c296→c298 move context
+ 709-c298's [28,29,34,35] wipe rows). Fully offline — SSD
dumps read-only, no harness code, no replays, no lease. No
device work.

## Facts you start from

- c298 (M34 — reproduce exactly or table the mismatch and
  stop): s0 rows [28,29,34,35] (n0 4); 2 movers, 1
  miss-only + 1 extra-only + 0 mixed. 709: full wipe
  (n=0, miss rows [28,29,34,35], the M28 seed). 732:
  extra-only growth keeping s0's 4 rows (extra rows: NEW
  DATA — list them).
- c311 (M34 — reproduce exactly or table the mismatch and
  stop): s0 rows [296,297,298,299] (n0 4); 12 movers, 1
  miss-only + 11 extra-only + 0 mixed. The miss-only mover
  is shape 9 ((296,311) per M35). The 11 extra-only mover
  shapes + their extra rows: NEW DATA — name them
  (known: s2 [259], s3 [258], s700 [259] per M36).
- The question: row-list + per-site value comparison of
  opposite standings in the SAME column — wipe vs growth
  at c298 (values at wiped rows vs grown rows?), the lone
  c311 missing vs the 11 c311 extras. Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (mover counts + standings to reproduce — match exactly or
  table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m44/`; evidence `local/research/M44/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m43/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m44/` + `local/research/M44/`.
- Evidence dir `local/research/M44/` (STANDALONE). Commit with
  `git add -f`, prefix `[M44]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M43 precedent).

## Task 1 — c298 wipe-vs-growth (row-lists + values)

c298 recomputed from the dumps (must match M34 exactly —
s0 rows, 2 movers, 1/1/0 standings — or table the mismatch
and stop):

1. Row-list table: s0 [28,29,34,35] vs 709 [] (wipe) vs
   732's rows (kept 4 + extra rows listed — the new data).
2. Value table: 709's 4 wiped sites (|δ_s0|, s709 status,
   gaps, signs) + 732's extra sites (|δ_732|, s0 status,
   gaps, signs) — M36-style per-site rows.
3. Wipe-vs-growth comparison: wiped-row values vs
   kept-row values vs grown-row values (|δ|/status/sign
   splits, numbers only).

## Task 2 — c311 11-vs-1 (row-lists + values)

c311 recomputed from the dumps (must match M34 exactly —
s0 rows, 12 movers, 1/11/0 standings — or table the
mismatch and stop):

1. Mover table: all 12 shapes with standings + extra/missing
   rows (name the 8 unknown extra-only movers; s9's miss
   row [296] must reproduce M35).
2. Value table: s9's (296,311) vs all 11 extras' sites
   (|δ|, statuses, gaps, signs — M36-style rows).
3. Lone-vs-crowd comparison: the missing site's values vs
   the 11 extras' pooled stats (|δ| med, bulk split, sign
   split — numbers only).

## Task 3 — controls + determinism (M18–M43 precedent)

1. Positive control: synthetic truth with KNOWN opposite
   standings (1 wipe + 1 growth column; 1 miss-only + N
   extra-only column, known rows/values) — recover the
   row-list + value tables exactly.
2. Determinism: re-run Tasks 1–2 row-lists on s0+709+732+9
   + all c311 movers, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M43).

## Report

`local/research/M44/REPORT.md`: DESIGN.md (bars first) +
c298 tables + c311 tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a split-column map
discriminates (budget-noted, pre-registered split required
per M23–M43 precedent). Commit evidence (`[M44]`, no push),
stop.
