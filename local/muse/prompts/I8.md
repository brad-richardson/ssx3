# I8 — Audio-wall device repro: device slice + install + miniaudio diagnosis on iPhone

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I7/REPORT.md` first (all of it: the
entry/lifecycle map, gap 1 = this brief's parent, the standing measurement
rules) + `local/research/MF1/REPORT.md` device sections (the proven
fresh-bundle provisioning/install flow on this iPhone — reuse the mechanism,
table any deltas). I7 reached first window on sim; the next wall is
miniaudio CoreAudio `AudioUnitInitialize(RemoteIO)` RPC-timeout abort in
`InitAudioDevice` (probe C exonerates sim audio itself). This brief moves
the wall to DEVICE. iPhone confirmed by the user for this work.

## Facts you start from

- Target: iPhone 16 Pro Max, UDID `00008140-0002505001F3001C`, paired over
  WiFi (`xcrun devicectl list devices` to confirm). Bundle stays
  `org.ps2x.ps2entryrunner` unless provisioning forces a rename (table it).
- The device slice was NEVER attempted (I6/I7 sim-only by scope). No device
  toolchain exists — derive one from
  `local/research/I1/logs/ios-simulator.toolchain.v2.cmake` (table every
  delta: arch, sysroot, signing, bundle settings).
- E3b owns the shared fork clone RIGHT NOW (instrumentation + builds). Do
  NOT touch `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`: create a
  SEPARATE fork worktree on the SSD at a PINNED commit (table HEAD + why
  that pin; `b6252bb` unless a newer fork/ssx3 commit is strictly required
  and clean). All I8 fork reads/edits happen in the worktree.
- Success bars (tabled, not verdicts): device toolchain configures;
  Release device build exits 0; app installs on the iPhone; argv-probe
  launch reaches the SAME or a NEW wall (console + `.ips` + screenshot,
  tabled against I7's sim behavior); diagnosis names the failing layer
  with evidence.

## Gates and rules

- Subordinate to E3b: builds `-j2`, never `-j4`; build trees on the SSD
  only (`/Volumes/Extreme SSD/ps2x-i8/`); allocated-byte caps declared up
  front + tracked (ExFAT). No PS2 boots → no lease. No `adb`.
- Fork discipline: worktree only; named-files `git add` for any fork edit;
  fork commits pushed to the FORK remote only, never upstream; generated
  sources never staged. If the worktree base must move, table old/new HEAD
  + reason + re-verified receipts.
- Device discipline (extends I6/I7 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  never `--force-install` without a fresh orchestrator prompt (the user
  may be using the phone).
- Evidence dir `local/research/I8/` (STANDALONE). Commit with `git add -f`,
  prefix `[I8]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — device toolchain + build (worktree, no phone yet)

1. Pin + create the worktree (table HEAD). Derive the device toolchain
   (table deltas vs the sim toolchain). Configure Release (I7 flags shape;
   table yours).
2. Build (`-j2`). Table binary path/size/sha/identifier/signature. Fix
   ONLY build breaks (config/toolchain class); app-code changes are Task 3.

## Task 2 — provision + install + repro (iPhone)

1. Provisioning + sign + install (reuse MF1's proven mechanism; table every
   step + the profile/bundle outcome). Confirm the phone's state before
   installing (running sessions are the user's — report, don't kill).
2. argv-probe launch to the wall: console + `.ips` (sleep + re-`ls` before
   concluding none) + screenshot. Same-wall-vs-new-wall table vs I7 sim.

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. Diagnosis table: failing layer + evidence (audio-session setup? init
   threading/ordering? miniaudio backend selection? device-vs-sim API
   behavior?). Attempt a fix ONLY if it is config-level (flags, plist,
   session category, init order) — no fork refactor, no new subsystems.
2. If no config fix: exact fix recipe for I9 (files, change shape, proof
   lines, risks). Either way: hypothesis verdict + the ONE next action.

## Report

`local/research/I8/REPORT.md`: contract, toolchain table, build record,
provision/install record, repro-vs-I7 table, diagnosis (+ fix or recipe),
exact commands, gaps. Commit fork first if touched (pushed fork remote),
then evidence (`[I8]`), stop.
