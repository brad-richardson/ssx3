# F2 — fold SB1 + AU10 + CT1 + GB9 onto fork `ssx3` with the codegen promotion (muse)

Gates: `local/research/{SB1,AU10,CT1,GB9}/REPORT.md` (orchestrator sections). This fold changes the generated code: SB1's emitter now emits `PS2X_SBR_*` macros (64-bit sign branches) and retires the `0x3E3968` override, so **the fork move and the canonical codegen promotion happen together**.

## Part 1 — fold + codegen + Mac check (stop after; the orchestrator gates and pushes)
1. Worktree `~/dev/ssx3-work/F2/PS2Recomp`, branch `f2-fold` from fork `ssx3` `56a5e8a`. `git cherry-pick -x` in order: `a64ba5e 90df7e0` (SB1), `2fb2003` (AU10), `3344013` (CT1), `5706858` (GB9 `[gs-path]` print; needs paraLLEl-GS fork `ssx3` **`19d93b2`**). Conflict → stop, save hunks to `local/research/F2/conflict.txt`, hand back.
2. **Codegen:** regenerate from the folded tree's `ps2_recomp` with the tracked `games/ssx3/ssx3.toml` (SB1's recipe: `local/research/SB1/REPORT.md` Commands; output to `~/dev/ssx3-work/F2/codegen`). It must equal `~/dev/ssx3-work/SB1/codegen` (`diff -rq` empty; if not, stop and report the diff list). Then promote: `mv ~/dev/ssx3-work/codegen-ssx3 ~/dev/ssx3-work/codegen-ssx3-pre-sb1` and `cp -R ~/dev/ssx3-work/F2/codegen ~/dev/ssx3-work/codegen-ssx3`; record `register_functions.cpp` SHA (two reads) and file counts. Never commit generated code.
3. Suite from the worktree root; runner-dir check empty. Mac Release build with paraLLEl `19d93b2` (GB8 recipe, `PS2X_GS_SHADOW_PARALLEL=ON`), clean + det-hash runners.
4. Boots (one slot each; I26-FAST; empty mc0; `PS2X_SKIP_MOVIE=1`; paraLLEl env + `PGS_HIER_BINNING=force`; deterministic):
   - **B1** `PS2X_SOUND=1` to t2400, frames at ~1090/1800/2100/2400 viewed, cid0 = 531, `PS2X_COVERAGE_TICK=2400` (targets 0, syscalls 0, RPC pairs 4).
   - **B2** sound **off**, same: det-hash identical to B1 (AU10's property) and it races.
   - **B3** clean speed, `--exclusive`, sound on, per-phase table vs F1 B2 (race 0.250×).
5. Deliverable `local/research/F2/REPORT.md` (commit map old→new, codegen equality + promotion receipts, suite, runner SHAs ×2, boot tables, frames, gaps); commit `[F2] Part 1 …` (`git add -f`, `Orchestrated-By: Muse Code`). **No push.** Stop.

## Part 2 — Android (after the push; wait for the Odin lease: NP1 may hold it)
On bytesize, **refresh the codegen copy** from the promoted canonical codegen (two SHA reads of `register_functions.cpp` both sides) and paraLLEl-GS to `19d93b2`, then the F1 recipe (`local/research/F1/build.sh`). Two clean Odin race runs (F1 `launch.py`) vs F1's 0.140×; then install as Brad's play build + `deploy-odin.sh`, verify, no launch. Append `## Part 2`.

## Part 3 — iOS (after the push; can run alongside Part 2)
F1 Part 3 recipe (`~/dev/ssx3-work/F1/ios/build-install.sh` pinned to the new tip, canonical codegen); Simulator Select Peak + race frame; iPad install + deploy + one fresh-card test launch (live container path first); iPhone install + deploy, **never launch**. Append `## Part 3`.

Rules: text only in git; never push; never `git add -f` in the fork; scratch `~/dev/ssx3-work/F2/` ≤ 20 GB; check/kill runners by PID. First failure: stop, save the error, hand back. Budget: Part 1 1.5 h, Parts 2/3 1 h each.
