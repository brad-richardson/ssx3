# T43 — Accumulate lock-QA-gated on→on pairs: SAME shape, crash-free hold goal, hold-step vs no-input distribution separation (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/T42/REPORT.md` first (all of
it: the ROI retune converts 11/12-flooded into 9/12-gated with a
rider-locked pair straddling the hold (control floods 11/12 again on the
new stretch) — GATING PROVEN — but MOTION is not solved: per-snap
intrusion bias (±50 px) survives inside VALID pairs, the hold hop
contains a TREE CRASH (7 MPH, 5TH→6TH — crash-vs-input-vs-bias
UNRESOLVED), and the no-input floor is a single sample). This brief
executes T42 §recipe arm 1 ONLY: accumulate. (Arm 2 — the static
gradient-lock test — is Task 1 below as a cheap tabled comparison that
does NOT gate the run: motion needs runs, gating analysis doesn't
substitute for them.)

## Facts you start from

- Task 1 (no run): run T40's third shape (gradient lock) STATICALLY
  against T42 R3's 12 committed dense snaps (implement as a COPY,
  `t43-track.py` — frozen `t41-track.py` + `t42-track.py` stay
  byte-identical and score in parallel as controls). Table gates per
  snap + whether the gradient separates rider from shade/totem/trunk
  where the ROI leaves in-band bias (d5/d9/d1). Verdict tabled, run
  proceeds regardless (motion answers need the run).
- Task 2 (the run — SAME shape as T42 R3): T4 build reuse (verify all
  pins), chain to LIVE gameplay, pre-hold pair phase-matched to T42's
  hold window (§phase — table targets BEFORE the run; match phase not
  seed), ONE ≈1036 ms Left hold (same body, same slot), blind
  back-to-back 10-snap series at `sleep 0.5` (≈0.575 s exposures, ZERO
  in-script scoring), all scored post-hoc with all THREE trackers
  (frozen + ROI-retuned + gradient).
- The goal (tabled): a CRASH-FREE hold with an on→on straddle pair +
  accumulation toward the distribution verdict: does the hold-step
  distribution (across T42's + this run's on→on pairs) separate from
  the no-input distribution? If the hold crashes AGAIN: table the
  crash (speed/position/HUD) and STOP the motion verdict at one more
  data point — do NOT run a third time in this brief (crash lottery
  is a recipe input, not a retry condition).
- Lock QA is LOAD-BEARING (T42 lesson): every VALID pair gets a lock
  grade (on/borderline-on/off + named intruder) via the marked-crop
  montage method; deltas across lock-quality changes are reported as
  bias-contaminated, never as motion. Only on→on pairs enter the
  hold-step distribution.
- Environment (T42 carried, all tabled): gate audio BEHAVIORALLY
  (boot-probe the modal — socket-presence AND raw-liveness both gave
  false healthies); idle reaper kills VMs in ~1–4 min (hold with
  activity); parallel-launch protocol = run-background → sleep 35 →
  v2a → verify log → conditional v2b; C: 11 G (watch the ~2 GB/run
  drain — pre-run gate stands).
- Success bars (tabled): gradient static table (12 snaps, gates +
  bias notes); run chain/hops/HUD in T42's shapes; all three trackers
  on all 12 dense snaps; lock-QA per VALID pair; hold-step vs
  no-input distribution verdict (separates / doesn't / needs more
  samples — with the exact counts); crash tabled if it recurs.

## Gates and rules

- No lease of any kind (bytesize needs none). 0 laptop boots. T-lane
  tools reused (copied, not modified); the ONLY new file besides
  evidence is the gradient tracker copy + its calibration table.
- One input shape per attempt (the hold is FIXED — same body, same
  slot); never blind multi-presses; stop at the distribution verdict
  + recipe. ONE accumulation run max in this brief (crash or clean).
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared +
  tracked (trace stream to the T-lane SSD dir, verified size+sha+
  head/tail — T41F recipe). No `adb`. No fork changes.
- Evidence dir `local/research/T43/` (STANDALONE). Commit with
  `git add -f`, prefix `[T43]`, trailer `Orchestrated-By: Muse Code`.
  Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — gradient static test (no run)

1. Implement the gradient lock as a COPY; calibrate on T42's 12 snaps
   (gates + bias notes + montage method). Frozen + ROI controls
   reproduce T42's numbers on those snaps first (reproduction gate —
   table it). Table the gradient verdict (separates in-band bias or
   not). Run proceeds regardless.

## Task 2 — accumulation run + post-hoc scoring

1. Pins (T4 build/tree/inputs/NVM/bindings/refs/C:/audio-behavioral).
   Chain → LIVE → pre-pair → ONE hold → blind 10-snap series (T42 R3
   shape). Fetch, stream the trace, score with ALL THREE trackers +
   full chain/hops/HUD tables. Lock-QA every VALID pair. Distribution
   verdict + recipe.

## Report

`local/research/T43/REPORT.md`: contract, gradient static table, pins,
chain, tracking (all three), lock QA, distribution verdict, comparison,
forensics (if any), exact commands, gaps. Commit evidence (`[T43]`),
stop.
