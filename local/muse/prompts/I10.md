# I10 — Link SSX3 recompiled objects into the iphoneos runtime, re-probe on iPad

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I9/REPORT.md` first (all of it: real
`SLUS_207.72` shipped as a bundle resource, argv probe reaches `run()`, NEW
wall = Dormant-at-entry on the empty recompiled-function table
(`registerFunction` callerless, stub `register_functions.cpp`, device build
`PS2X_BUILD_RECOMP=OFF`); gap 1 = this brief). This brief executes I9's ONE
next action. Reuse I9's worktree shape + device toolchain + provision/install
mechanism — table every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I9 app (`org.ps2x.ps2entryrunner`) is STILL INSTALLED —
  this install tests OVERWRITE on an occupied target (I9 gap 3).
- INSTALL POLICY (corrected 09-21): `--force-install` does NOT exist in
  this `devicectl` (exit 64). Standing intent = always install even if
  present: run PLAIN `install` and table whether it overwrites cleanly on
  the occupied target. No fresh prompt needed for iPad installs. Confirm
  the iPad's state before installing (user sessions reported, not killed).
- E-lane owns the shared fork clone RIGHT NOW (check who before touching).
  Do NOT touch `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` except one
  `worktree add` + read-only inspection: create a SEPARATE fork worktree on
  the SSD at the SAME pin I9 used (`b6252bb` + `3006a07` topic checkout —
  table HEADs; move only if the recompiler genuinely needs a newer base,
  table old/new + reason). All I10 fork reads/edits happen in the worktree.
- The task: generate the SSX3 recompiled objects on the HOST (`ps2_recomp`
  over `games/ssx3/ssx3.toml`) and link them into the iphoneos device
  runtime (direct game objects or a game-objects static lib the device
  build consumes), then install + argv-probe the real ELF to `run()` and
  past the Dormant-at-entry wall. AOT first: on-device JIT
  (`dynamic-codesigning`/MAP_JIT) is OUT of scope unless receipts prove
  the AOT link cannot satisfy the table (table the decision either way).
- Success bars (tabled, not verdicts): host `ps2_recomp` emits SSX3
  registration + function objects; device Release build WITH game objects
  exits 0; install overwrites the I9 app (or the exact failure, tabled);
  argv-probe launch gets PAST `missing-target`/`ee:idle` at `0x100008`
  (console + `.ips` + screenshot, tabled against I9's fate); diagnosis
  names the next layer with evidence if a new wall appears.

## Gates and rules

- Subordinate to the E-lane fork owner: builds `-j2`, never `-j4`; build
  trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i10/`); allocated-byte
  caps declared up front + tracked (ExFAT). No PS2 boots → no lease. No
  `adb`. Host `ps2_recomp` runs are codegen, not boots (no lease), but
  keep them bounded and tabled.
- Fork discipline: worktree only; named-files `git add` for any fork edit;
  fork commits pushed to the FORK remote only, never upstream (and no
  `ssx3` push while the E-lane mutates); generated sources never staged
  (recompiler OUTPUTS are build artifacts — table paths + shas, do not
  commit them to the fork).
- Device discipline (extends I6–I9 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I10/` (STANDALONE). Commit with `git add -f`,
  prefix `[I10]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — host codegen + device link (no iPad yet)

1. Pin + create the worktree (table HEADs). Run host `ps2_recomp` for SSX3
   (table command, outputs, counts, shas). Wire game objects into the
   iphoneos runtime link (table the mechanism: direct objects vs static
   lib vs other + every CMake/packaging delta vs I9).
2. Configure + build Release (`-j2`). Table binary path/size/sha/identifier
   + proof the function table is populated (symbol/object census, not just
   "it linked"). Fix ONLY build breaks in the wiring class; runtime
   behavior changes are findings, not fixes.

## Task 2 — overwrite install + re-probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I9 app (table overwrite behavior — clean replace? error?
   version dance?).
2. Ship the guest ELF as in I9 (same mechanism unless the link changed the
   packaging — table deltas). argv-probe launch: console + `.ips` (sleep +
   re-`ls` before concluding none) + screenshot. Past-the-wall table vs I9
   (`missing-target` gone? first guest function executes? new wall?).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled past entry: diagnosis table (failing layer + evidence). Attempt
   a fix ONLY if config-level (flags, plist, paths, packaging) — no fork
   refactor, no new subsystems.
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I10/REPORT.md`: contract, caps, codegen + link record,
install/probe tables, diagnosis, exact commands, gaps. Commit evidence
(`[I10]`), stop.
