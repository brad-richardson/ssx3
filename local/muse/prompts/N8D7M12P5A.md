# N8D7M12 Part 5A — prepare one-run Odin replay script (no device action)

You are the N-lane OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M12 Parts 1–4 REPORTs/gates, N8D7M11 §§5–6, N8D7M6 `launch.py`/`accept.py` and REPORT, plus the gated APK and stream pins in `docs/numbers-ledger.md`. Cite code/receipts, not the web. Own this brief and `local/research/N8D7M12P5A/`. **No adb/device command, install, launch, lease, stream/APK/source edit, build, fork edit, push, board/global edit or upstream contact in this part.** Time box 30 min, text <512 KiB. Write REPORT/checker early.

Prepare a reviewed-SHA-held `launch.py` for exactly **one** diagnostic replay of the already-staged 1,100,696,462-byte N8D7M6 stream on the Odin, using gated APK `caa1110297dc413d3b04e7442873e0fb5d5624af8417e1e30537d76184d5f512` with runner `329e44db…a218a3d`, Turnip `717812c3…1ac29d`, HAL `1b49d27c…fc387`. Base device patterns on N8D7M6 `launch.py`, but replay must use `PS2X_GS_REPLAY_ONDEVICE=1`, `PS2X_GS_REPLAY_CAPTURE=<FILES>/n8d7m6.gs`, `PS2X_GS_REPLAY_BACKEND=parallel`, `PS2X_GS_TURNIP=1`, selected/oracle/tile capture flags, sample step50, named PPM tick2050, and bounded output paths. Do not set the live GS capture key or pad script: replay enters before EE boot. Confirm the shared core's actual frame filename and marker/census log syntax from source. Preserve the existing device `ps2x.env` bytes before replacing them, and restore them in `finally` after force-stop; never alter/delete the pinned stream. New frame/hash output paths must be unique and empty, capped, and live outside git.

Script gate before launch: exact local APK/stream SHA pairs and APK member hashes; Odin serial from `local/odin-serial`; lease free then claim/re-read unique tag; `adb install -r` even if app is present, two device SHA reads of installed APK and stream; battery AC/charging ≥20%, keyguard `showing=false`, ≥10 GiB free, app stopped immediately before `am start`; no launch if any check fails. Keep all adb calls serial-scoped. One launch only, ≤600 s wall with earlier progress cap, logcat ≤16 MiB, PPM/output cap ≤64 MiB, stop at first complete tick2050 summary/census/frame or first parse/backend/control error. Force-stop after the run even if the process `_Exit`s, drain/close/compress logs, pull PPM and hashes with two matching SHA reads, restore env, release lease only if the tag is still ours. Record first failure and cleanup. A missing keyguard unlock is a blocker to report, never worked around. The script should mark the outcome **provisional** until the orchestrator views the frame and checks same-binary controls; one replay does not establish default-off behavior, a root cause or speed.

Deliver `REPORT.md` with exact unexecuted command and acceptance/stop table, `launch.py`, `check.py` that statically checks pins/one-run guard/caps/finally cleanup and `--self-check` or equivalent no-device dry path, plus bounded checker receipt. A = script/checker ready for orchestrator release (no runtime result); B = source/receipt mismatch; OTHER = permission/resource failure. Commit `[N8D7M12] Part 5A` with `Orchestrated-By: opencode`, explicit named paths, no push. Hand back the table, not a graphics verdict.
