# T62 — PCSX2: who produces 0x1b0 after the SC input (exact-mode filter, post-K window)

Same rules and tree as T61. Read `local/research/T61/REPORT.md` first.

T61's filter admitted mode 3 (`0xcc`), and those transitions filled the tpl
cap by vsync 100, long before K (vsync ~1025). Orchestrator codegen read:
the three SC-scene appenders `0x37ad5c`, `0x37b48c` and `0x37b4ac` (in
`sub_0037A430`) use the same pattern as `0x3797ec`. Each copies the 20-byte
template at `*(s0+0xE84)` into item N = `list+0x80+N*0x80`. The copy sites
are `0x37ad44` and `0x37b474` (the ldl after the count increment).

## Change (on T61's tree; logging only from vsync ≥ 1025)

1. **Appender copy sites** `0x3797ec`, `0x37ad44` and `0x37b474`, on every
   execution:
   `appx vsync site count=<v1 = item index> t0 tw0..tw4 ra`.
   Emit every row where count ≤ 1 or (tw0>>6)&0xF == 6, cap 300.
2. **Exact-value store watch, any address:** any EE store (all widths; for
   64/128-bit, check each 32-bit lane) whose stored 32-bit value has
   `(v & 0x3FF) == 0x1b0`, or whose target is the template (`t0` from (1))
   with `((v>>6)&0xF) == 6`:
   `v1b0 vsync addr vaddr value pc ra a0..a3 v0 v1 s0..s7`, cap 200.
3. Keep T61's `appsum`, extended with a per-site count.

## Run

One build, G13 preservation, and one capture (T61's; `t58-pre-sc-state` →
K → SC settled). ≤1 build (+1 on compile failure), ≤2 captures, 2 h, cap
2 GB.

## Deliverable

`local/research/T62/REPORT.md`:
- which site writes item 0 at SC and its tw0;
- the first `v1b0` producers (pc, ra, regs, vsync relative to K);
- preservation.

No verdict. `[T62]` commit (`git add -f local/research/T62
local/muse/prompts/T62.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
