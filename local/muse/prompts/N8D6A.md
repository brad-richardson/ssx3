# N8D6A — expose scanout stages for one Mac replay

You are a Codex Sol worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. Work only in `local/research/N8D6A/`, a fresh fork worktree `~/dev/ssx3-work/N8D6A/PS2Recomp` from local N8D5L `ab8155b`, and a private **copy** `~/dev/ssx3-work/N8D6A/parallel-gs` of `~/dev/ssx3-work/N8D5B/parallel-gs`. Do not edit either source checkout, shared fork `ssx3`, runner, generated code or devices. No Android/APK, Odin, game boot, push or upstream contact. Preserve the copied G43 diagnostic changes and shader header; pin source SHA before edits.

Why: N8D5G category A (`local/research/N8D5G/REPORT.md`): Odin tick2050, FBP112/PMODE `ff21`, 512×448, checkerboard 128/128, sampled final image and raw copy exactly equal 896/896 but only 34 active tiles; same-run PNG was saved/viewed and mostly black. N8D4 Mac replay of one captured Odin GS stream shows a broad frame; N8D5L Mac tile probe measured final 567/896 active. The loss is by or before final GPU image at `Present`; its internal stage is open. PMODE `ff21` means EN1=1, EN2=0, MMOD=1, ALP=255 (`gs_registers.hpp:562-579`); same-run SMODE2=1 means INT=1, FFMD=0. Backend `Present` zero-initializes `VSyncInfo` and calls `m_iface->vsync` (`ps2_gs_parallel_backend.cpp:243-255`), so `force_progressive=0`; `scanout_is_interlaced` stays true (`gs_renderer.cpp:4381-4388`). `GSRenderer::vsync` creates circuit1 at ~4780, merges at ~4988-5120, then may deinterlace at ~5240-5275. The raw-circuit early return at ~4939 needs non-interlaced and cannot be assumed here. Confirm these source connections with LSP if available; record empty/error plainly.

Hypotheses/predictions for a **later** Odin run, measured with the same control/occupancy shader: (A) circuit1 sparse and final sparse ⇒ loss at/before CRTC circuit sampling (VRAM generation or sampling remains open); (B) circuit1 broad, pre-deinterlace merged sparse ⇒ merge fault; (C) circuit1 and pre-deinterlace merged broad, final sparse ⇒ deinterlace fault. Anything else, invalid control, stage mismatch or a missing stage is OTHER. A single on-device run is allowed only after the orchestrator reviews the Mac calibration and separately releases a packaged APK. Do not claim a precise cause from this Mac task.

One implementation candidate: add default-OFF `VSyncInfo` stage-capture flag and optional shared image handles in `ScanoutResult` for circuit1 and the merged image before deinterlace. Keep these handles alive and in readable Vulkan layout without changing normal output; do not replace a merge/deinterlace step or force progressive mode. At tick2050 only, have the fork backend set the flag, dispatch the **existing** validated N8D5 tile shader and checkerboard against circuit1, pre-deinterlace merged, and the existing final image. Reuse the explicit `ResourceLayout`, threshold 32, 16×16 tiles, and short summary format; size dispatch/buffers from each image's real dimensions. Log each stage name, width/height, tile count, occupied pixels and active tiles plus control. Preserve existing sampled/raw final vectors and copy path unchanged. If the source shape makes safe handle retention impossible, stop with a cited table before editing and hand back an alternative design; no speculative code.

Budget: one source candidate, one Release/Ninja configure/build of `ps2x_tests` from the fresh fork worktree with paraLLEl ON, private G43 path, canonical E54F2 external codegen, and taps/runtime/aggressive logs OFF; one taps-OFF suite. One named compile repair only, no tuning loop. **Do not attempt Vulkan replay in the worker sandbox.** Pin binary, N8D4 stream and codegen SHA pairs and hand the exact one-replay command to the orchestrator, who will run it outside sandbox. Mac acceptance: tick2050/FBP112/PMODEff21/512×448, three stage summaries and checker controls=128, final sampled/raw exact 896/896 and broad ≥500 active as N8D5L; circuit1/merged dimensions and tile counts internally consistent and both broad (provisional ≥50% active), with no pipeline errors. Otherwise OTHER and no Odin. Scratch ≤5 GiB; committed receipts ≤4 MiB; global ssx3 internal <200 GB. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty before a local fork commit.

Deliver `local/research/N8D6A/REPORT.md` with source-stage table/citations, exact source diff and pins, commands/flags, build/suite, SHA pairs, limits, gaps, and Mac replay explicitly pending. Commit only the new fork backend file as `[N8D6A]` with `Orchestrated-By: Codex`; preserve a bounded patch/hash receipt for the private G43 changes. Commit named receipts/brief to ssx3 as `[N8D6A]` with the same trailer. No push. Hand back the table, binary path/SHA and replay command; **do not conclude a device cause**.
