# X4 — Local Qwen read: guest-visible host time and input paths for E55

You are a **local opencode/Qwen worker** in `~/dev/ssx3`. Follow
`~/dev/AGENTS.md` worker rules. This is a read-only code investigation:
write only `local/research/X4/REPORT.md`, then commit that file and this
brief. **Hand back the table; do not conclude or propose a fix.**

Goal: give the orchestrator a source-grounded map for E55's deterministic
mode. Read `docs/research/review-2026-09-23-frontier-2.md` §B and
`local/research/X3/ORCH-CORRECTIONS.md` first. Cite the repo, not the web.

Source pin: `~/dev/ssx3-work/E56/PS2Recomp-fold` at `8e2864a`. Read it
only; do not fetch, checkout, build, boot, or write in it. Its C++ compile
database is at `~/dev/ssx3-work/E56/fold/build/compile_commands.json`.
Use `lsp` goToDefinition/findReferences or incomingCalls to confirm how
functions connect; use `rg -n` for exact source lines. If LSP fails,
record the error and use direct source references, marking the connection
as unconfirmed. Do not infer a call edge from names alone.

## Questions (one table per category)

1. RTC: `sceCdReadClock` host-time read (`Stubs/CD.cpp` near line 706),
   HLE registration/call path, and the guest seed consumer identified in
   review §B (`0x31ae80` → `func_3177C8`). For guest addresses use
   `local/tooling/ee/ee-at`, `ee-xref`, `ee-func`, and `ee-label`; do not
   invent names for an `unknown` label.
2. Scheduler: identify every use of a host `now()`/deadline that can
   choose a guest cycle advance or break a tie between guest events.
   Start with `EeScheduler.cpp` near 2836–2844. Separate a host pacing
   wait from a value that can change guest-visible order, citing the
   caller and field assignment.
3. Pad script: trace where live raylib state is combined with scripted
   pad state. State the merge operator and conditions from code, without
   guessing which input wins in a race.
4. Memory card: list the code that resolves the `mc0` root and any
   writes/reads during boot. If a per-run path can be injected already,
   name the exact parameter or environment variable; otherwise write
   `not found`.
5. Hash tap seam: locate the VBlankStart path and the current API for
   reading EE RAM, scratchpad, VU1 data/program memory, and `m_eeCycle`.
   List **only direct source anchors**, no design recommendation.

Each row has columns `claim | caller→callee or data flow | source:line |
LSP confirmed? | gap`. Quote no more than one short code expression per
row. Include a separate `Not found / unconfirmed` table. A valid row
must cite a real file and line in the pinned tree; don't cite the review
as proof of code behavior. Keep the report ≤250 lines.

## Acceptance and limits

Before committing, run this mechanical check from the repo root:

```
python3 - <<'PY'
from pathlib import Path
p=Path('local/research/X4/REPORT.md')
s=p.read_text()
for h in ('RTC', 'Scheduler', 'Pad script', 'Memory card', 'Hash tap',
          'Not found / unconfirmed'):
    assert h in s, h
assert len(s.splitlines()) <= 250
assert s.count('|') >= 30
print('X4 report shape: PASS')
PY
```

Time box 90 minutes, no builds or device access, no new binary files. Stop
if the pinned source is unavailable. Record exact revision, commands,
LSP successes/failures, and unresolved links. Commit `[X4]` with trailer
`Orchestrated-By: opencode`; no push. The orchestrator will verify every
label and relationship used in an E55 brief.
