# P5 — Mine 360/N64/PSP recomp tooling for the PS2 ladder + ps2xGS (read-only research)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/P3/REPORT.md` §8.3–§8.4 (sibling licenses — all
compatible — + the N64ModernRuntime mechanism, which you must NOT redo)
and `local/research/P4/REPORT.md` §4 (our EeScheduler delivery point)
plus the ps2xGS plan `docs/plan-gs-gpu-backend-2026-09-18.md` §5/S0–S8
(for question 3 only). Evidence dir `local/research/P5/` (commit with
`git add -f`, prefix `[P5]`, trailer `Orchestrated-By: Muse Code`).
Time box 4 hours.

## Rules

- Read-only research. Do not edit any clone, this repo's `tools/`,
  `native/`, `vendor` trees, or any other agent's evidence dir. No
  builds, no boots, no emulator runs, no leases, no `adb`.
- Every claim cites a file you opened (path + line numbers); `grep`
  only locates. Quote the decisive bodies.
- Reuse the existing clones under `/Volumes/Extreme SSD/q3-siblings/`
  (`XenonRecomp`, `UnleashedRecomp`, `N64Recomp`, `psprecomp`,
  `wtf-psp-recomp`); record rev per clone. No new clones over 200 MB.
  Do NOT touch `psxrecomp` (PolyForm, incompatible) or `SaturnRecomp`
  (unlicensed) beyond their existing license rows.
- NEVER run `git push` in `/Users/bradrichardson/dev/ssx3`.

## Questions (caps; stop each at cap, record partials)

1. **Kernel-export HLE tooling (60 min cap).** XenonRecomp (360 kernel
   imports by ordinal) + psprecomp/wtf-psp-recomp (NID imports): how is
   the stub table generated, how are unimplemented imports traced at
   runtime, and how is coverage tracked? Table per project: generator
   file:line + quoted mechanism, tracer file:line, coverage file:line.
   Map each onto our `Syscalls/Dispatcher.cpp` + TODO-stub ladder:
   what would the PS2Recomp analogue be (function-level prose, not
   applied)?
2. **Indirect-branch / function-detection tooling (60 min cap).**
   N64Recomp + XenonRecomp: function-boundary detection, noreturn
   analysis, jump-table handling, and any `JR`/`JALR`-by-register
   resolution at recompile time. Table: mechanism + file:line quote
   per project. Map onto our sweep CSV + strict-return diagnostics:
   which of their passes would have caught our constructor/alarm/
   comparator misses earlier?
3. **UnleashedRecomp GPU path vs ps2xGS (60 min cap).** How is the 360
   GPU command stream captured, replayed, and translated to the modern
   renderer? Table: capture point, replay/diff harness, translation
   layer, backend selection — file:line each. Map onto ps2xGS's
   gscap/gsreplay/backend-factory plan: same shape, or what would you
   change (cite the plan section)?
4. **Patch/override systems (30 min cap).** N64Recomp function
   overrides/patches + XenonRecomp equivalents (if any): config
   format, application point (recompile time vs runtime), one worked
   example with file:line. Map onto our TOML `stubs` + `untracked`
   mechanism: what do they express that ours cannot?
5. **Recompiler testing patterns (30 min cap).** How do N64Recomp /
   XenonRecomp test the recompiler itself (unit tests, golden-output
   tests, differential tests vs interpreter)? Table: harness +
   file:line + what it covers. Map onto our `ps2xTest` + record-test
   gaps (one row: what test would have caught the LUI+ORI fold?).

## Steps

1. Questions 1–5 in order with caps; record partials as unresolved.
2. **Borrowable/tooling table**: idea → source (project, rev,
   file:line, quoted body) → PS2Recomp/ps2xGS analogue (function-level
   prose, NOT applied) → license re-cite (P3 §8.3; no new verdicts).
3. **Report.** `local/research/P5/REPORT.md`: per-question tables,
   borrowable/tooling table, clone revs + paths, exact commands,
   "What I could not do". Commit `local/research/P5/`. Stop.
