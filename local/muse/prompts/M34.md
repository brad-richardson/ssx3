# M34 — Unnamed-column partial scan: which shapes move unnamed columns by ≤2 rows (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M33/REPORT.md` (M33 gap 2 is
this brief: 709's c298 holds 4 missing rows (r28/29/34/35) — a
second partial column on 709 OUTSIDE the census 8, located in
M29 but never scanned for across the 764) plus
`local/research/M28/REPORT.md` (the exact-match census method
to generalize: per-shape × per-column presence with row
overlap). Fully offline — SSD dumps read-only, no harness code,
no replays, no lease. No device work.

## Facts you start from

- Named census (M28): 8 streak columns, 10 movers / 15 cells.
  Unnamed columns (M34's domain): EVERY other tail-bearing
  Y column in the s0 tail map (hundreds) — scanned with the
  same presence rule (rows match s0's rows for that column
  exactly, else the shape "moves" it).
- The scan question: how many shapes move ≥1 unnamed column
  (count + list)? By how much (row-delta distribution —
  M33's 709-c298 reads 4 rows; table all)? Do unnamed moves
  correlate with named moves (same shapes? the 10 movers a
  subset?) or with below-0.95 Jaccard?
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28.txt` +
  `m28-census.tsv` (named census to reproduce: 10 movers +
  709-c298's 4 missing rows located — match or table the
  mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m34/`; evidence `local/research/M34/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m33/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m34/` + `local/research/M34/`.
- Evidence dir `local/research/M34/` (STANDALONE). Commit with
  `git add -f`, prefix `[M34]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M33 precedent).

## Task 1 — unnamed-column scan (all columns, all shapes)

Named census + 709-c298 rows recomputed from the dumps (must
match M28/M29 exactly or table the mismatch and stop):

1. Per-shape × unnamed-column presence matrix (all 764 × all
   tail-bearing non-named Y columns: present/absent + row
   overlap — commit the full matrix as TSV like M28).
2. Unnamed-mover census: shapes moving ≥1 unnamed column
   (count, list, cells each); per-column mover counts (which
   unnamed columns move most? — top-20 table).
3. Row-delta distribution over all unnamed moved cells
   (exact-match misses by 1 row? 4 rows like c298? full
   wipes like 711's pair? — table the hist).

## Task 2 — joins (named vs unnamed vs Jaccard)

1. Named∩unnamed 2×2 (are the 10 named movers also unnamed
   movers? is any unnamed mover named-still?).
2. Below-0.95 join (M28's 9 shapes recomputed — do unnamed
   moves explain 2/3/700's below-but-still readings? do
   still+below shapes move unnamed columns?).
3. 709-c298 row (the seed: 4 missing rows — reproduce +
   rank: how many unnamed cells match-or-exceed its
   row-delta?).

## Task 3 — controls + determinism (M18–M33 precedent)

1. Positive control: synthetic truth with KNOWN unnamed moves
   (N shapes, known columns/rows) — recover the census +
   row-deltas exactly.
2. Determinism: re-run Task 1 on s0+709, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M33).

## Report

`local/research/M34/REPORT.md`: DESIGN.md (bars first) +
scan tables + joins + controls + explained-vs-standing
update (attribution, not explanation — table it) + gap rows.
Screenshot only if a mover map discriminates (budget-noted,
pre-registered split required per M23–M33 precedent). Commit
evidence (`[M34]`, no push), stop.
