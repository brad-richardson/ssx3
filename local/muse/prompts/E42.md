# E42 — Name the EE function that re-stamps the chain CALL tags with 0x435bd0 each frame (recomp)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini).
**Tables + receipts; recommend, the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/E41/REPORT.md`,
`local/research/E40/REPORT.md` Part-6/7. You own `~/dev/PS2Recomp` (`ssx3` @
`7d7bbc6`) and the P-lane lease; ff pushes allowed after the runner-dir
check (add the `Orchestrated-By: Muse Code` trailer to fork commits).

## Facts

- E41: the CALL ADDR words `0x63b994/0x63bbe4/0x63bea4/0x63c134` are written
  by EE stores every even vsync from 1274 (185 `plant`, all `0x435bd0`),
  through `Ps2FastWrite*` and/or an uncached mirror; no pc/fn was captured.
- Orchestrator: the recomp's and PCSX2's `MDLPS2.BIG` + `ZOETXP.BIG` reads
  are sector-for-sector identical (165/165), so asset loading isn't the
  difference. PCSX2's chains CALL `0x434990` at four sites the recomp lacks.
  The value is chosen by EE code per frame.

## Change (default off, unit-tested)

Attribute each `plant` hit to its guest function: at the `Ps2FastWrite*` tap,
capture `__builtin_return_address(0)` (and one level up if the write sits in
a shared helper) and resolve it with `dladdr` to the host symbol (the
`sub_XXXXXXXX` / named game function), plus the guest `ctx` if reachable on
that path (pc, ra, and `$a0–$a3,$v0,$v1,$t0–$t9,$s0–$s7`). If symbols are
stripped in this build, say so and use a build with symbols.

## Run, then read

Boot A (E33 route, 300 s), plant watch window 1270–1300. Table: function(s),
pc, value, registers per hit. Then read the function in
`~/dev/ssx3-work/codegen-ssx3/`: how the stored value is formed (load from
which address, table base/index/stride, which state feeds the index), and
log that index's value if it's a memory word (add it to the same trace).

## Budgets, deliverable

Builds as needed, 2 boots, 3 h, cap 2 GB. `local/research/E42/REPORT.md`
(commit, suite, tables, codegen read). No verdict. `[E42]` commit
(`git add -f local/research/E42 local/muse/prompts/E42.md`, trailer
`Orchestrated-By: Muse Code`, `git log -1` first); ff-push the fork.
