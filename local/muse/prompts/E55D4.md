# E55D4 — pinned pad/card A/A preparation and release hold (Muse Contributor Go)

You are an OpenCode Go E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, E55D3 `REPORT.md` and `ORCH-GATE.md`, E55C2 `REPORT.md`, its private `~/dev/ssx3-work/E55C2/e55c2_boot.py`, and E55D4 in `docs/todo.md`. Use E55D3 private fork `~/dev/ssx3-work/E55D3/PS2Recomp` at exact `bab6eb3` and its built runner `~/dev/ssx3-work/E55D3/build-taps/ps2xRuntime/ps2EntryRunner`; do not modify E55D3 source or any other lane. Work in `~/dev/ssx3-work/E55D4/` and `local/research/E55D4/`. **Do not boot until the orchestrator reviews and explicitly releases your exact script SHA in your pane.** No device, upstream contact, fork push or ssx3 push.

Part 1 prepares **only an A/A null comparison** through guest vsync tick2053: two independent Mac mini boots from byte-identical pinned ISO/ELF/codegen and fresh empty-card roots, I26-FAST vsync pad script, deterministic mode, dev movie bypass, `PS2X_DET_HASH_EVERY=1`, and the new `PS2X_PAD_CARD_PROBE` output in each run. The two runs must use the same ON runner; record two SHA reads for runner, ISO, ELF and codegen. Reuse E55C2's progress-capped lease/PID boot method, but adapt it in a new `e55d4_boot.py` with **per-run cwd**, distinct probe files and fresh cards. One mini P-lane slot per run, hold it only during each boot; at most two runs after release, wall cap 500 s/run, no tick progress cap 120 s, boot log <=4 MiB, probe file <=16 MiB, stop at tick2053. Kill only the recorded PID, release lease even on failure. Check `git diff --exit-code 14b1e5cb bab6eb3 -- ps2xRuntime/src/runner` before any run and no other worker mutates this fork. Do not alter pad input or card contents in Part 1.

Predeclare acceptance in the script/report before release: A/A success requires both traces start at tick1 and continue consecutively to 2053 with identical `(tick,eeCycle,rdram,scratch,vu1Data,vu1Code,combined,count)` on every row, identical ordered pad/getdir/mcread records **including exact payload bytes** and skip/failure statuses through the stop tick, no cap marker, identical fresh card manifests including mtimes, and no unexpected live pad/physical input. A difference is a measured A/A failure, not proof of its cause. Record the first differing shared-seq record and first hash tick; distinguish a shorter/incomplete trace as OTHER. Predeclare a *future* pad-B and card-B one-change plan but do not run B in this part; the orchestrator decides from A/A.

Before release deliver `e55d4_boot.py`, a pure parser/comparator with a small synthetic self-check, `REPORT.md` containing pins, exact commands, table and SHA of both scripts. Wait. After release, at most two boots, write compact receipt `result.json`/comparison and bounded excerpts; keep full logs in scratch and compress closed logs. Global disk <200 GB, new scratch <2 GiB, committed text <512 KiB. No diagnostic wall time as speed. Commit only named receipts/scripts/brief `[E55D4]` with `Orchestrated-By: opencode`, no push. Hand back the evidence table; the orchestrator decides A/A and whether B is warranted.
