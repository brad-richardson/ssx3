# G4 — CPU-backend perf profile + one fast-path spike, identity-proven

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables, no verdicts**. Read first:
`docs/reports/G3.md` (§3 spike vehicle, §4b/§5b timing method:
`--time-submits`, interleaved backends, 5 runs, min-max ranges) and
`docs/reports/G0.md` §4 (the ~16 ms/present baseline shape). This is
the recorded G4 sequence step: profile first, then ONE fast path with
an identity proof. G1 (game captures) still waits on first frame.

## Gates and rules

- Host only, no `adb`, no device, no emulator, no lease. Builds `-j4`
  any time in `/tmp/ps2xgs-build`. No new captures needed (reuse the
  102); anything over 5 MB goes under `/Volumes/Extreme SSD/ps2xgs/`,
  never into the repo.
- Do not edit anything inside `upstream/`. The `cpu` backend's default
  behavior stays byte-identical (all 102 identity rows must still pass
  unchanged); implement the fast path in `spike` (fork already exists).
  `strict` keeps its 14 intentional diffs.
- Commit in this repo as you go (prefix `[G4]`, trailer
  `Orchestrated-By: Muse Code`); push `origin main` at the end. Never
  commit captures. Do not touch `/Users/bradrichardson/dev/ssx3`
  except to write `local/research/G4/REPORT.md` there at the end
  (`git add -f`, `[G4]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box 6 hours.

## Steps

1. **Profile.** Where does the ~16 ms/present go? Use `sample`,
   `xctrace`, or manual instrumentation (your call — record the tool):
   Submit vs Present split, then the top-5 hot functions with
   self/total share on a CLUT-heavy, a blend-heavy, and a
   transfer-heavy capture. Receipts: the profile tables + tool +
   exact commands. No conclusions about hardware targets — this CPU,
   observed.
2. **One fast path.** Implement the single highest-leverage item the
   profile justifies (UnleashedRecomp-style primitive batching is a
   candidate, not a prescription — follow the profile). Receipts: the
   diff, identity replay still exact on all 102, `submit_ms_total`
   delta on the affected captures (G3 §4b method: interleaved, 5
   runs, min-max), plus one control capture showing no change.
3. **Report.** `docs/reports/G4.md` in the repo and a copy at
   `/Users/bradrichardson/dev/ssx3/local/research/G4/REPORT.md`:
   profile tables, fast-path diff + identity table + timing deltas,
   CTest output (old 11 + new), exact commands, receipt paths,
   "What I could not do". No verdicts. Commit both, push ps2xGS
   `origin main`, stop.
