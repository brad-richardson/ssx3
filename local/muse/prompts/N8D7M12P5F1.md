# N8D7M12 Part 5F1 — read-only GPU hash/sync path audit

You are the N/G OpenCode Go evidence worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M12 Parts 1/5B/5C/5D2 REPORTs/gates, then inspect the pinned replay/backend source. Cite local source, not web. Own only this brief and `local/research/N8D7M12P5F1/`. No source edit, build, replay, boot, device/lease action, push, board/global edit or upstream contact. Time box 25 min; text <256 KiB. Write REPORT/checker early. Use LSP to verify function connections where available, then exact line reads; state when LSP is unavailable.

Question: with the exact same Odin APK and GS stream, why can GPU VRAM/present hashes vary while all 41 priv hashes match? The ON/OFF comparison differs first at tick850, before the three selected/oracle/tile flags act at2050. A same-settings OFF repeat is being prepared separately; **do not assume its outcome**. Build a source-grounded map of how `GB4_REPLAY` computes `priv`, `vram`, and `present` hashes and how/when paraLLEl-GS work is submitted, waited on, and read back on Android vs Mac. Pin source revisions or file SHAs for `~/dev/ssx3-work/N8D7M12P2/PS2Recomp` and `~/dev/ssx3-work/N8D7F/parallel-gs`; note dirty files rather than silently assuming a binary matches them. Trace actual call edges from replay core to backend and renderer, with exact file:line citations.

Hand back a table for three candidate mechanisms: (A) replay input/ordering variation before backend, (B) GPU submission/readback synchronization or uninitialized state, (C) deterministic backend behavior with a hashing/receipt artifact. For each, state source evidence, what remains unknown, and one bounded observable that would distinguish it from the others **without a new live game boot**. Do not nominate a fix or assert a root cause; the orchestrator decides after the same-settings repeat. Verify whether the three capture flags are truly unread before tick2050 in the scoped source. Avoid a broad codebase survey: only the hash-producing and synchronization call path. No diagnostic elapsed time is speed.

Deliver `REPORT.md`, a checker for pinned citations/claims and a compact call-path table. Outcome A = source map and discriminating observables grounded; B = source/binary provenance or call edge unproved; OTHER = access failure. Commit `[N8D7M12] Part 5F1` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Hand back the table.
