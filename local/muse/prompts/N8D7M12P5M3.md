# N8D7M12 Part 5M3 — prepare corrected Mac OFF driver (no run)

You are the N/G OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5M1/{REPORT.md,ORCH-GATE.md,mac_off.py,check.py}`, Part 5M2 REPORT/gate, and this brief. Own only this brief and `local/research/N8D7M12P5M3/`. No replay/boot, build, device/lease action, binary/stream/fork/source edit, push, board/global edit or upstream contact. Time box 25 min, text <512 KiB. Write REPORT/checker early.

P5M2 ran once with binary/stream pins and produced Mac OFF `parallel.hashes`/PPM byte-identical to Mac ON, but the overall binary exited 1 because one unrelated CodeGenerator test could not find `ps2xRecomp/include/ps2recomp/instructions.h` relative to the private cwd. Its source at `~/dev/ssx3-work/N8D7M12/PS2Recomp/ps2xTest/src/code_generator_tests.cpp:1141-1150` tries three relative candidate paths; the first would work if the private cwd held a `ps2xRecomp` symlink to the pinned fork source. The header exists at `~/dev/ssx3-work/N8D7M12/PS2Recomp/ps2xRecomp/include/ps2recomp/instructions.h`, SHA `b8de8745e16d6a814763f69b0e8d1d54d957bf427f94ec2bc57f63e509f970cd`. The P5M2 overall acceptance was FAIL; no retry is authorized by its one-run brief. This part prepares a new driver for a separate release.

Copy final P5M1 `mac_off.py` to `local/research/N8D7M12P5M3/mac_off.py`. Change only the private output root to `~/dev/ssx3-work/N8D7M12P5M3/mac-off`, result/lease labels, and a **pre-run private cwd fixture symlink** `cwd/ps2xRecomp` → pinned Part 1 fork's `ps2xRecomp` tree. Before making it, verify fork HEAD and header SHA twice; refuse if path/revision differs, if private cwd/output already exists or if symlink resolves outside that pinned tree. After creation, verify `cwd/ps2xRecomp/include/ps2recomp/instructions.h` resolves to the pinned header and hashes twice. The symlink is a test-suite fixture only; preserve replay env, binary, stream, ON artifact pins, ambient PS2X clearing, dynamic one-slot lease, own-PID stop, 300 s wall, 16/64 MiB caps and `finally` cleanup. No run here.

Checker must prove diff is limited to path/labels/fixture handling, and functionally test the relative candidate-path resolution using a temporary directory **without running the binary or claiming a lease**. Verify no `ps2x_*` replay env changes. Predeclare later acceptance: binary exit0 and suite 585/585, parallel summary2050, exact 41 ticks, PPM, lease cleanup; compare all rows and PPM with Mac ON. If suite still fails, report FAIL even if artifacts match. No diagnostic elapsed time is speed.

Deliver `REPORT.md`, corrected driver, checker, bounded no-run receipt, exact script SHA and unexecuted command. A = script ready for orchestrator SHA gate; B = fixture path/provenance cannot be grounded; OTHER = prep failure. Commit `[N8D7M12] Part 5M3` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Hand back table.
