# X8 — dense Qwen sign-branch source table, lower thinking budget

You are a local opencode/Qwen **dense** worker in `~/dev/ssx3`.
Follow `~/dev/AGENTS.md` worker rules. Read only the files named here;
write only `local/research/X8/REPORT.md`. No fork edit, build, boot,
device, network, or push. Cite repo source lines for every code claim;
name no guest function. Hand back tables and gaps, **no fix verdict**.

This repeats X7's bounded source-reading task with a per-pane lower
thinking budget. `docs/research/review-2026-09-23-frontier-2.md` §A2
hypothesizes that `BLTZ/BGEZ/BLEZ/BGTZ` and likely/link variants compare
only low 32 bits. Check pinned fork source at
`~/dev/ssx3-work/E54D/PS2Recomp` (`1aaed05`; read only)
`ps2xRecomp/src/lib/control_flow_emitter.cpp:395-430`, and the
`GPR_S32`/`GPR_S64` definitions in
`ps2xRuntime/include/ps2_runtime_macros.h`. Use `lsp` to confirm the
emitter function's caller; if LSP fails, state that gap and cite text.
Do **not** read generated `sub_*.cpp` or scan the game: source only.

First write a heading and source pins to `REPORT.md` within 3 minutes.
Then make one compact table with each of four branch families
(`BLTZ`, `BGEZ`, `BLEZ`, `BGTZ`), current emitted predicate and exact
line, and signed-64 expected/current truth values for these three
register bit patterns: `0x0000000180000000`, `0xffffffff00000000`,
`0x0000000000000000`. Include likely variants only if the inspected
switch shares their case. Distinguish what the source proves from the
review's hypothesis; no PCSX2 or MIPS oracle claim without a named
source. State whether 64-bit values could disagree in this table.

Budget: 12 minutes, ≤18k context, ≤1 MiB report. Stop and report if
LSP is unavailable or one source path differs; do not spend time on
tool discovery. Commit only this explicit report as `[X8]` with
trailer `Orchestrated-By: opencode`; `local/` is ignored, so `git add -f`
the named file. No push. The orchestrator will verify each row and
compare completion time with X7.
