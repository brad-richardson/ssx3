# T8 — Repetition-driver attribution: which 376938 edge issues the 13,108 repetitions (read-only, no boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T7/REPORT.md` first (all of it —
T7 §T7-7 steps 1+2 are this brief), plus P1 REPORT Part 31 §P31-2
(the single-caller census this run refines one frame up). T7 proved
the `0x362DE8` JAL sits in `sub_00363490`'s straight-line prologue
(once per fresh entry) with the repetition driver strictly above
it; the sole static caller is `sub_00376938` (392,030 enters).
This brief attributes each repetition to its issuing edge. Peer
lanes run concurrently — read-only on the fork tree (never stage/
touch/commit anything; you make zero changes).

## Facts you start from

- Receipt (read-only): `$W/P1/run/ps2_log-p1ag-1.txt` (1.2 GB,
  35,916,072 lines, 100% enter/exit). `W=/Volumes/Extreme
  SSD/ps2recomp-spike`, `R=$W/PS2Recomp` (fork, branch `ssx3`).
- Fixed numbers (T7-verified, reproduce or table the mismatch
  and stop): `362DE8` 13,108 enters; `363490` 39,324/39,324
  (= 3×); `376938` 392,030/392,029 (deficit 1 = live frame).
- T7's leads to confirm or kill: `376938` self-JALs at
  `0x376b5c`/`0x377208`/`0x3778e4` (recursion?); 14 back-edges
  in `376938`, none spanning `0x377b14` in-window (loop?);
  re-entry from above `376938` (outer driver?); the 26,216
  resume-frame theory (2× fresh via `ctx->pc` switch — needs
  an ancestry signature, not just the 3× ratio).

## Gates and rules

- NO lease (no boots at all). ZERO fork changes (no source
  edits, no commits, no push). No `adb`. Read-only on the ISO,
  the receipts, and every other agent's dirs.
- Evidence dir `local/research/T8/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T8]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — indent-aware attribution (the 13,108 repetitions)

Single streaming pass (T7-7 step 1): for each `0x362DE8` enter,
record the issuing ancestry — depth, the enclosing `363490`
frame (fresh vs resume: does THIS frame's prologue JAL fire?),
and the enclosing `376938` frame + edge (which self-JAL /
back-edge / re-entry issued it). Tables:

1. Repetitions per `376938` frame (distribution: 1:1? N:1?).
2. Issuing-edge census (self-JAL addrs / back-edge addrs /
   above-frame re-entry counts — must sum to 13,108).
3. Resume-frame signature: do the 26,216 non-fresh `363490`
   frames show a distinct ancestry (T7's theory) or another
   pattern — table the pattern, confirm or kill.

## Task 2 — disassemble the issuing edge (T7-7 step 2)

Same method as T7 §T7-1–3 (JAL inventory + back-edge targets +
ELF words from `$W/P1/SLUS_207.72`, offset = vaddr − `0xFF000`):

1. The winning edge from Task 1: full disassembly table (addr /
   word / instr / ELF word) + loop or recursion structure
   (index/bound operands if a loop; depth/arg counter if
   recursion).
2. Examined-and-absent rows for the losing candidates (where
   looked, what reading kills each).
3. The step-3 recommendation as a TABLE: 3a (loop+bound →
   emitter samples `(i,N)` per `363490` enter), 3b (recursion
   counter → progress proxy), or 3c (no readable bound →
   empirical `N` via run-to-exit) — with the inputs each needs,
   no verdict.

## Report

`local/research/T8/REPORT.md`: attribution tables + edge
disassembly + examined-and-absent + step-3 table + gap rows.
Commit evidence (`[T8]`, no push), stop.
