# E55A Part 2 — fixed RTC reply under deterministic mode

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4.
Read `local/research/E55A1/REPORT.md` in full. Hand back tables and
receipts; the orchestrator decides the gate. The paths named below are
your write scope. “First failure” means a failed brief step, not a typo
in an exploratory command. **One mechanism:** exact
`PS2X_DETERMINISTIC=1` makes `sceCdReadClock` return a fixed UTC calendar.
Unset, empty, `0`, or other values keep the current host-local reply.
Do not touch scheduler deadlines, pad, memory card, GS, Count, Compare or
interrupt policy.

Pin fork `ssx3` at `0cab7d733179e0706b6262e6b6f0a8cd89b274b9`
(E54F2 pushed; verify remote). Make a new worktree
`~/dev/ssx3-work/E55A2/PS2Recomp`, branch `e55-fixed-rtc`. Canonical
`~/dev/ssx3-work/codegen-ssx3` is E54F2 (9,457 files; register file SHA
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`).
HLE-only change: reuse it, no regeneration and no generated code in Git.
Allowed fork edits: `ps2xRuntime/src/lib/Kernel/Stubs/CD.cpp`, one focused
`ps2xTest` source/registration and directly needed test helpers. Build,
run and text receipts only under `E55A2`; report only at
`local/research/E55A2/REPORT.md`. No fork or ssx3 push.

## Gates

1. Implement fixed **2004-07-16 12:34:56 UTC** as eight guest bytes
   `00 56 34 12 00 16 07 04` through the existing BCD writer. Parse
   the env flag exactly `"1"` per call (not a process-cached switch), so
   tests can toggle it. Preserve default `std::time`/localtime behavior
   and return/error behavior. Label dev trace source accurately.
2. Focused tests call the actual HLE stub with valid guest memory:
   deterministic reply is byte-exact on repeated calls, including a TZ
   change; invalid pointer returns 0 without writing; unset and `"0"`
   use a host-local reply bracketed by before/after host time (accept
   either boundary and restore environment). Keep tests portable and
   isolate global environment changes. No test that merely mirrors the
   source string.
3. Mac Release/Ninja build and full suite taps OFF, then ON, using
   E54F2 codegen. Run `ps2x_tests` from fork root. Wait for other
   clang/ninja and use `nice`. One mini-lease diagnostic CPU boot with
   `PS2X_DETERMINISTIC=1`, `PS2X_SKIP_MOVIE=1` (dev-only), I26-FAST,
   target race tick ≥2050, wall cap 500 s, exactly three frame targets
   `830,1810,2050`, bounded logs. View SC and two race frames; inspect
   the park snapshot for `0x402520` reach if present. This is functional
   regression, not full-frame determinism or clean speed. Two matching
   SHA reads of ISO, ELF, runner and canonical register file before use.
4. Check `git diff --exit-code 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty. If gates pass, commit only allowed source/tests `[E55A2]` on
   the local fork branch with `Orchestrated-By: Codex`; no push.

Budgets: two configure/build attempts per tap mode (one clear repair),
one boot ≤600 s absolute, ≤5 GiB new E55A2 bytes, global 200 GB cap;
logs ≤32 MiB and frames ≤25 MiB. Stop after one failed gate and one
clear repair; no tuning loop. Claim/release mini lease; kill own PID
only. Stop if host default path changes or the fixed bytes cannot be
proved by the actual stub test.

Deliver `local/research/E55A2/REPORT.md` with pinned source, test
table, default/fixed branch evidence, suites, boot/frames and SHA pairs,
budgets, runner-dir check, exact commands and gaps. Commit only the
explicit report `[E55A2]` with the same trailer; no push. The
orchestrator gates and pushes if successful.
