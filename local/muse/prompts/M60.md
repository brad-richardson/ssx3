# M60 — c312 unique sign-split column: per-column sign-unanimity census joined with row-span/decile-span (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M59/REPORT.md` first (all of it —
M59 gap 1 = this brief, taken on orchestrator judgment: c312 is
the ONLY m15 tail-Y column with n≥2 reading both signs (13/10,
pos share 0.5652, sum +130); the other 18 multi-site columns
read unanimous — 12 all-pos, 6 all-neg), plus
`local/research/M58/REPORT.md` (c312's 23 sites: 22/23
off-mode, r316 δ+11 dec-7 the exception) and
`local/research/M56/REPORT.md` (33 union cols, d machinery).
Fully offline — SSD dumps read-only, no harness code, no
replays, no lease. No device work.

## Facts you start from

- The split (reproduce exactly or table the mismatch and
  stop): c312 m15 tail-Y 23 sites, 13/10 pos/neg, min −9
  max +33 sum +130; site list 316:+11/d7 317:+20/d5
  318:+19/d1 319:+9/d1 341:+14/d1 342:+22/d3 343:+9/d3
  353–356:−9/d0 362:−8/d3 363:−8/d4 364:−9/d3 365–366:−9/
  d0 367:−8/d0 369:+14/d3 373:+33/d1 374:+15/d1 377:+18/
  d0 378:+23/d1 379:+10/d3 (M59 column-sign detail).
- The background (reproduce exactly or table the mismatch
  and stop): 16 all-pos + 10 all-neg + 6 s0-only columns;
  per-column pos/neg/min/max/sum (M59 column-sign table).
- The question: per-column sign-unanimity census over all
  27 m15 columns joined with row-span + decile-span — what
  distinguishes c312 (span? decile mix? d profile?) from
  the 18 unanimous multi-site columns? Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m59/m59.txt` (column-sign table to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m60/`; evidence
  `local/research/M60/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m59/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m60/` + `local/research/M60/`.
- Evidence dir `local/research/M60/` (STANDALONE). Commit with
  `git add -f`, prefix `[M60]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M59 precedent).

## Task 1 — unanimity reproduction (does the split reproduce?)

All 27 m15 tail-Y columns recomputed from the dumps (must
match M59's column-sign table exactly — n, pos/neg, min/max/
sum per column, c312 13/10 +130 — or table the mismatch and
stop):

1. Unanimity table: per-column n + pos/neg + unanimous?/split?
   (is c312 the only split at n≥2?).
2. c312 row table: the 23 sites in row order with δ/sign/decile
   + run structure (do signs cluster in runs? where does
   r316 sit?).
3. Span table: per-column row-span (max−min row) + decile-span
   (distinct deciles) + n (does c312's span discriminate?).

## Task 2 — split-vs-unanimous census (what distinguishes c312?)

1. Span-join table: unanimous vs split columns by row-span and
   decile-span (do unanimous columns read narrower?).
2. Decile-mix table: per-column decile histogram class (does
   c312's 8/6/6/1/1/1 mix discriminate?).
3. d-profile table: per-column d histogram class (M56 d
   machinery — does c312's d profile discriminate?).

## Task 3 — controls + determinism (M18–M59 precedent)

1. Positive control: synthetic truth with KNOWN column signs
   (N unanimous + 1 split column, known δ/rows/deciles) —
   recover the unanimity + span tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M59).

## Report

`local/research/M60/REPORT.md`: DESIGN.md (bars first) +
unanimity tables + span tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a unanimity map
discriminates (budget-noted, pre-registered split required
per M23–M59 precedent). Commit evidence (`[M60]`, no push),
stop.
