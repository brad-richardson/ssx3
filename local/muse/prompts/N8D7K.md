# N8D7K — independent selected-address oracle (Muse Contributor Go)

You are an OpenCode Go N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, N8D7I and N8D7J `ORCH-GATE.md`, N8D7J `REPORT.md`, and N8D7K in `docs/todo.md`. Read-only source audit; write only `local/research/N8D7K/{REPORT.md,address-map.tsv}` and this brief. No source edit, build, boot, device, upstream contact or push. Cite repo lines and confirm call edges with LSP. Target <=12 minutes and bounded context; write the table early.

Question: can an address oracle **independent of G43 `swizzle_PS2`** locate the exact raw 32-bit words for the selected Odin frame (`FBP=112, FBW=8, PSM=1/PSMCT24, DBX=0, DBY=0, phase=0, stride=2, width=512, height=224`, 4 MiB VRAM mask) and provide literal golden offsets? Search the repo and local PCSX2 reference copies for a separately implemented GS local-memory address mapping; the fork CPU GS address helpers may qualify only if they do not call/copy G43's `swizzle_PS2`. Identify source provenance and copied-table risks. If no independent complete mapping is available locally, stop and state that gap; do not invent offsets. Do not fetch new external code.

Return 8–12 TSV rows `stage,file_line,algorithm_or_table,independence,observable,uncertainty`. If an independent oracle exists, compute at least four literal byte offsets: selected (x,y)=(0,0),(31,0),(0,2),(511,446) in 4 MiB VRAM, plus one row at a page boundary; check uniqueness/in-bounds and compare against G43 without using G43 to derive them. Note PSMCT24 shares 32-bit storage addressing but RGB ignores alpha only if source proves it. Propose a bounded same-run raw-word sample of these offsets and a representative selected-tile grid, with explicit A/B/OTHER conditions based on *exact* words/decoded pixels and a positive control. A high occupancy in an arbitrary contiguous window is never decisive. If the reference is partial, return the evidence and no run proposal.

Deliver source map, exact pins, calculations/checks and gaps in REPORT. Commit only named receipts and brief `[N8D7K]` with `Orchestrated-By: opencode`, no push. Hand back the table; the orchestrator decides whether any source hook or Odin run is justified.
