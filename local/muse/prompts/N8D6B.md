# N8D6B — package the Mac-validated stage probe for Android

You are a Codex Sol **medium** worker. Read `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. **Build/package only.** Work in `local/research/N8D6B/`, Mac scratch `~/dev/ssx3-work/N8D6B/`, and fresh WSL scratch `/home/brad/n8d6b/` on `ssh bytesize`. No Odin install/launch, game boot, fork/main push, shared source edit or upstream contact. Check bytesize has no other heavy job before building.

Facts: N8D5G (`local/research/N8D5G/REPORT.md`) proved the Odin final GPU image sparse at tick2050/FBP112/PMODE `ff21`, control 128/128, sampled/raw equal 896/896 and same-run PNG viewed. N8D6A (`local/research/N8D6A/REPORT.md`, `orch-replay-excerpt.txt`) built a default-OFF stage probe and one pinned same-stream Mac replay: circuit1 and pre-deinterlace merged each 512×224, 300/448 active, control128; final 512×448, 567/896 active, control128, sampled/raw equal. This calibrates the probe on Mac only. N8D5F (`local/research/N8D5F/REPORT.md`) is the previously packaged APK source with the PNG writer proven by N8D5G. Pin N8D6A fork backend SHA `e730b9f05994c9887f9c5605f9a34b2fe6f4ac1c239bb964f17cdfc515b1fb5c`, stage patch SHA `013b23508b678d2852f61fad066c4f498762e42aa6226aa1c7ea8cb4af0390a4`, shader header `19b9ba5fc747d8fcb70d712b86bd5738c64424d4b5ed58219f71bac9`, PNG writer `358d726bf39e319b906e7e15407e1bf361cd2cdbdd8e79d4a96b25a9588c3f96`, codegen `8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`. Mac fork runner-dir diff against `14b1e5cb` must be empty.

Make a **fresh** N8D6B WSL snapshot from `/home/brad/n8d5f/`, excluding `.cxx`, `build`, `.gradle`, and `ps2xRuntime/src/runner/`; copy its private `parallel-gs` excluding build caches and its `jniLibs` unchanged. The external codegen remains `/home/brad/n8b1/codegen-ssx3`. Transfer **only** the N8D6A fork backend file into the snapshot. Apply **only** `g43-stage.patch` to the N8D5F-based paraLLEl files `gs/gs_interface.hpp`, `gs/gs_renderer.hpp`, `gs/gs_renderer.cpp`; do not copy N8D6A's whole G43 tree (it carries earlier G40/G31 diagnostics). Pin patch input/output SHA pairs and require a source gate before build: relative to N8D5F, exactly one fork backend difference and exactly those three paraLLEl file differences; PNG writer, shader header, Turnip, HAL shim, codegen and every other file unchanged. The changed G43 hunks must match `g43-stage.patch` by content after path normalization. If the patch does not apply cleanly, stop and hand back the mismatch table; no speculative port.

Use N8D5F `build.sh` as the command pattern: one arm64 `assembleRelease` with shadow paraLLEl ON, JDK17, private G43/jniLibs, external codegen and diagnostics/runtime/aggressive/IOP RPC/debug UI OFF. One named compile-repair retry only after a specific error; no tuning loop. WSL scratch <10 GiB, Mac scratch <500 MiB, committed text <8 MiB, global mini ssx3 <200 GB. No app run.

Package gate: two APK SHA reads on WSL and after transfer to `~/dev/ssx3-work/N8D6B/app-release.apk`; exactly arm64 runner, bundled Turnip and HAL shim, no x86. Read each member SHA twice; Turnip `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d` and shim `1b49d27c839f25363237d8276c91a401602845ee8eef67de50d6540f4cdfc387` unchanged. Record runner SHA/Build ID, source/cache paths and five OFF flags. Require packaged strings for stage names `circuit1`, `pre_deinterlace_merged`, `stage=final`, existing tile control/summaries, and `PNG write failed path=`. Check source isolation again after build. The package proves contents only; the orchestrator separately releases one bounded Odin run with preregistered A/B/C/OTHER thresholds.

Write `local/research/N8D6B/REPORT.md` with source/build/package tables and exact commands before the build, then bounded script/log/JSON receipts. Commit only named receipts as `[N8D6B]` with `Orchestrated-By: Codex`, explicit paths, no push. Hand back the package table and gaps; do not declare an Odin or causal verdict.
