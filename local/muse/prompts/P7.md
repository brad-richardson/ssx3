# P7 — Stub the intro movies (HLE completion stub + disc survey)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `docs/research/review-2026-09-19-progress.md` §7.6
first (the ask: default to an HLE stub that reports completion, logged,
revisited after frames exist). User decision: stub movies so the ladder
reaches meaningful menu work first. P1w/P1y/I7 run concurrently in other
panes — you share only the fork remote.

## Facts you start from

No brief has looked at the EA intro movies (`.mpc` on the disc) or the
playback API surface the game uses. The ladder has NOT reached any movie
stage yet (current stall: thread-3 poll on `0x52BE04`) — so this stub
cannot be runtime-verified here; build-verified + honest about it is the
bar. The game ISO is at `/Volumes/Extreme SSD/ps2recomp-spike/SSX 3
(USA).iso` (read-only).

## Gates and rules

- NO fork boots in this brief (nothing to test against yet) → no lease,
  no `adb`. Builds any time (`-j4`).
- Fork: survey first (read-only), then the stub as NEW file(s) under
  `Stubs/` (or the tree's equivalent — record the path) following the
  existing stub/diag conventions. `git add` those NAMED files only —
  never `-A`, never stage or touch P-lane files. Verify the diff
  before committing. One commit per change with the `Orchestrated-By:
  Muse Code` trailer. `git pull --rebase` before pushing; `git push`
  ONLY inside the fork clone to the `fork` remote. If the rebase shows
  any conflict outside your files, stop and report — do not resolve
  other lanes' side. Never commit generated sources or `._*`.
- Evidence dir `local/research/P7/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P7]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Survey (read-only).** (a) Disc: `.mpc`/movie files in the boot
   path (ISO listing + boot-file refs — table names, sizes, order).
   (b) ELF: the playback API surface (sceMpeg*? streaming reads?
   strings/imports/JAL census — table every movie-adjacent call with
   file:line or address receipts).
2. **Stub.** HLE stub reporting successful completion, one diagnostic
   line per invocation (so its first real firing is visible in a boot
   log). Follow the tree's stub conventions; note where a future
   decoder would attach. Unit test where the harness allows.
3. **Build-verify.** Rebuild the runtime + suite (name the failure
   face); the stub compiles and links, tests show no new failure.
   Runtime behavior explicitly UNVERIFIED (ladder hasn't arrived) —
   record the exact boot-log line a future brief greps to confirm
   first firing.
4. **Report.** `local/research/P7/REPORT.md`: survey tables, stub diff
   receipt, build/test receipts, first-firing grep line, gap rows
   (decoder revisit after frames), exact commands, receipt paths,
   "What I could not do". Commit, stop.
