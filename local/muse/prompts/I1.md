# I1 — iOS spike: what does the PS2Recomp runtime need to build for Simulator?

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. This is a
**read-only spike**: you inventory and attempt, you fix nothing (beyond a
flag/config retry that costs minutes — record it, don't polish). **Tables,
no verdicts.** Target: iphonesimulator arm64, Debug. The phone comes later;
nothing in this brief needs a device, a paid dev account, or signing.

## Pointers (start here, not from zero)

- Fork clone `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` (branch
  `ssx3`, READ ONLY — no commits, no pushes, no edits there).
- Runtime deps: `ps2xRuntime/CMakeLists.txt` (raylib + imgui + rlImGui
  via FetchContent), top `CMakeLists.txt` (sse2neon on ARM).
- Platform precedents: `ps2xRuntime/src/lib/ps2_android_runtime.cpp`
  and `ps2_vita_runtime.cpp` — an iOS runtime file follows their shape.
- Entry: `ps2xRuntime/src/main.cpp` + `src/runner/`.

## Gates and rules

- Host only, no `adb`, no device, no lease (nothing here touches the
  emulator host runs). Scratch build dir `/tmp/ps2x-ios-spike` (fresh;
  leave other build dirs alone). FetchContent may hit the network (it
  already does in normal builds); no OTHER network writes.
- Evidence dir `local/research/I1/` in ssx3 (commit with `git add -f`,
  prefix `[I1]`, trailer `Orchestrated-By: Muse Code`). Do not commit
  build outputs. NEVER run `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Dep inventory.** raylib (pin + iOS backend fitness), imgui +
   rlImGui (debug panel: iOS-fine or must gate off?), audio backend
   (identify it + its iOS path), sse2neon, CMake + Xcode versions
   present. Receipts: table (dep, pin, iOS-ready Y/N/unknown, evidence
   file:line or doc).
2. **Platform surface.** From the android/vita precedents + `main.cpp`
   + runner: the exact surface an iOS runtime file must implement
   (window, input, audio session, bundle/FS paths, threads, main-loop
   shape). Receipts: function/file table + the android file's line
   count as a size proxy.
3. **Configure attempt.** CMake iOS toolchain for iphonesimulator
   arm64 (find or write the toolchain usage — record which). Receipts:
   pass/fail + EVERY error as a gap row (file, error text, fix class:
   flag / config / dep-code / platform-code).
4. **Build attempt (only if 3 passes).** Build the runtime target (not
   the whole suite). Same gap-row treatment. If the build passes:
   install to a booted Simulator + launch, record first window / first
   crash with the log tail (game ISO NOT in scope — reaching `main`
   is the bar).
5. **Report.** `local/research/I1/REPORT.md`: dep table, platform
   surface, gap rows with fix classes, effort read (count of gaps by
   class, NOT a verdict), exact commands, receipt paths, "What I could
   not do" (include the device follow-ups this spike can't answer:
   signing, bundle resources, touch input, Metal-vs-GL). Commit
   `local/research/I1/`, stop.
