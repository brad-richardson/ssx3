# M56 — m15 c343 row-289 site: far-row tail-site census (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M25/REPORT.md` first (all of it —
M25 gap 3 = this brief via M26 gap 6, taken on orchestrator
judgment: one m15 c343 tail site 262 rows below its column's
band (hole span 28–288), also m15 c343's argmax peak), plus
`local/research/M26/REPORT.md` (m15 c343: rows [25,26,27,289],
peak 289; TRI p=289) and `local/research/M51/REPORT.md` (r289
peak err_x −16; c343 kept hit = m15 r27 under s0 peak). Fully
offline — SSD dumps read-only, no harness code, no replays, no
lease. No device work.

## Facts you start from

- The 289 site (reproduce exactly or table the mismatch and
  stop): m15 (289,343), δ −15, m15 c343's argmax peak row
  (M26), TRI p=289 (M51), QUAD near-flat fit res 0 (M54),
  hole span 28–288 (262-row gap below the 25–27 band).
- m15 c343's rows: [25,26,27,289] (n=4); s0 c343's rows:
  [26,27] (n=2). Both columns' full row→δ lists in M25.
- The question: far-row tail-site census — ALL tail sites
  sitting ≥100 rows from their column's band limits on
  s0+m15 (which columns? how far? what δ?) + the 289
  site's full value row (|δ|, gaps, signs, band/decile
  seats, streak/carrier). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails), `/Volumes/
  Extreme SSD/m25/m25.txt` (c343 rows + 289 site to
  reproduce — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m56/`;
  evidence `local/research/M56/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m55/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m56/` + `local/research/M56/`.
- Evidence dir `local/research/M56/` (STANDALONE). Commit with
  `git add -f`, prefix `[M56]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M55 precedent).

## Task 1 — 289-site reproduction (does the site reproduce?)

m15 c343 recomputed from the dumps (must match M25/M26
exactly — rows [25,26,27,289], δ values, peak 289, hole
span 28–288 — or table the mismatch and stop):

1. c343 table: both frames' rows + δ + argmax + TRI p +
   QUAD a/res (M51/M54 cited values reproduced?).
2. 289 value row: |δ|, bulk status, signed gaps both
   frames, gap signs, band + decile seats, streak/carrier
   (M33–M35 machinery on 1 site).
3. Isolation table: 262-row gap itemized (nearest band
   row above? nearest tail row below? hole contents?).

## Task 2 — far-row census (is 289 alone?)

1. Census table: all s0+m15 tail sites ≥100 rows from
   their column band (column, row, distance, δ —
   threshold pinned in DESIGN; if 289 is alone, table
   the next-5 nearest as the null set).
2. Seat table: each far site's band/decile/streak/carrier
   (do far sites share seats?).
3. Column table: far-site counts by column (is c343 the
   only far-row column?).

## Task 3 — controls + determinism (M18–M55 precedent)

1. Positive control: synthetic truth with KNOWN far sites
   (N far-row + N band sites, known rows/values) —
   recover the census + value tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M55).

## Report

`local/research/M56/REPORT.md`: DESIGN.md (bars first) +
site tables + census tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a far-row map
discriminates (budget-noted, pre-registered split required
per M23–M55 precedent). Commit evidence (`[M56]`, no push),
stop.
