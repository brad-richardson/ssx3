# M41 — Near-miss triple: boundary-|δ| census at unnamed cells (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M35/REPORT.md` first (all of it —
M35 gap 2 is this brief, taken on orchestrator judgment: the
only extra-side near (c336, |δ_s0| 6) + the only 2
missing-side nears (both c312, |δ_9| 6/7, one gap-equal
28/28) — 3/30 sites), plus `local/research/M36/REPORT.md`
(the still-below near split: extras 14 near / 0 far / 10
noncell with all 14 nears at |δ_s0|∈{6,7}) and
`local/research/M40/REPORT.md` (the c312 missing values:
(299,312) bulk6 gaps 22/16, (301,312) bulk7 gaps 28/28).
Fully offline — SSD dumps read-only, no harness code, no
replays, no lease. No device work.

## Facts you start from

- The 3 near-miss sites (M35 — reproduce exactly or table
  the mismatch and stop):
  - Extra-side: (239,336) off 306592, Y, |δ_9| 8, bulk
    on s0, |δ_s0| 6, gaps 17/14, ++ (c336 extra-only d1).
  - Missing-side: (299,312) off 383344, |δ_s0| 10,
    bulk on s9, |δ_9| 6, gaps 22/16, ++; (301,312) off
    385904, |δ_s0| 9, bulk on s9, |δ_9| 7, gaps 28/28
    (gap-equal), ++.
- Near = |δ|∈{6,7} on the other frame (M35/M36
  convention). The question: is 3/30 exact (no other
  boundary-|δ| sites hiding at |δ| 5 or 8), and how does
  s9's 3/30 near rate compare with the still-below rows'
  14/24 extra-side near rate? Answer by table: the 3
  sites + full boundary census + cross-row near table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m35/m35.txt`
  (the 3 sites to reproduce — match exactly or table the
  mismatch and stop), `/Volumes/Extreme SSD/m36/m36.txt`
  (still-below near counts to reproduce),
  `/Volumes/Extreme SSD/m34/m34-census.tsv` (FULL TSV guard).
  Work dir: new `/Volumes/Extreme SSD/m41/`; evidence
  `local/research/M41/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m40/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m41/` + `local/research/M41/`.
- Evidence dir `local/research/M41/` (STANDALONE). Commit with
  `git add -f`, prefix `[M41]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M40 precedent).

## Task 1 — near-miss triple recompute (do the 3 sites reproduce?)

The 3 sites recomputed from the dumps (must match M35
exactly — offsets, |δ| both frames, statuses, signed gaps
both frames, gap signs — or table the mismatch and stop):

1. Per-site table: (239,336) + (299,312) + (301,312) with
   all pins + the gap-equal flag on (301,312).
2. Near-definition table: each site's other-frame |δ|
   against the {6,7} near band (in-band all 3?).
3. Row-list context: c336's + c312's s0/s9 rows around the
   sites (do the nears sit at row-list edges?).

## Task 2 — boundary-|δ| census (is 3/30 exact? how do rows compare?)

1. s9 boundary census: all 30 pooled sites' other-frame |δ|
   histogram at |δ|∈{5,6,7,8} (nears 3? boundary-5/8
   counts? far (|δ|≥8 noncell?) recount vs M35's 1/14/7 +
   2/2/4 splits).
2. Still-below near recount: 2/3/700 extras' |δ_s0|
   histogram at {5,6,7,8} (14 nears reproduced? boundary
   counts?) + missings' |δ_Q| histogram (M36: 0 nears?).
3. Cross-row near table: s9 vs s2/s3/s700 near/far/noncell
   counts extra-side + missing-side (is s9's 3/30 low vs
   still-below's 14/24 extra-side nears?).

## Task 3 — controls + determinism (M18–M40 precedent)

1. Positive control: synthetic truth with KNOWN near/far/
   noncell sites (N in-band {6,7}, N at 5/8, N far) —
   recover the boundary census exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M40).

## Report

`local/research/M41/REPORT.md`: DESIGN.md (bars first) +
triple tables + census tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a near-miss map
discriminates (budget-noted, pre-registered split required
per M23–M40 precedent). Commit evidence (`[M41]`, no push),
stop.
