# G3 — Present-heuristic tests + CLUT-cache / RMW-lookup spikes, all identity-proven

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables, no verdicts**. Read first:
`docs/reports/G2.md` (the `strict` backend + sensitivity tables),
`docs/reports/G0.md` §9 item 4 (the `Present` `fbp==0` fallback
heuristic), and the two upstream TODO markers (CLUT cache, RMW lookup)
in the CPU backend sources. This is the recorded G3 sequence step:
pin down present behavior, then spike the two upstream TODOs with
identity proofs.

## Gates and rules

- Host only, no `adb`, no device, no emulator, no lease. Builds `-j4`
  any time in `/tmp/ps2xgs-build`. New captures and anything over 5 MB
  go under `/Volumes/Extreme SSD/ps2xgs/synth/` (new output under +100
  MB; the set is at 691 MB — total stays under 800 MB), never into the
  repo.
- Do not edit anything inside `upstream/`. The `cpu` backend's default
  behavior stays byte-identical (all 98 identity rows must still pass
  unchanged); implement spikes in `strict` (or a flagged variant —
  your call, `cpu` default untouched).
- Commit in this repo as you go (prefix `[G3]`, trailer
  `Orchestrated-By: Muse Code`); push `origin main` at the end. Never
  commit captures. Do not touch `/Users/bradrichardson/dev/ssx3`
  except to write `local/research/G3/REPORT.md` there at the end
  (`git add -f`, `[G3]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box 6 hours.

## Steps

1. **Present-heuristic tests.** Author captures pinning the `fbp==0`
   fallback: all-black frame (fallback scans `contextFrames` — table
   which candidate it picks), non-black frame (no fallback), multiple
   non-black candidates (which wins?), empty `contextFrames` (what
   returns?). Receipts: capture list + behavior table + CTest cases
   asserting each behavior (fail if the heuristic changes).
2. **CLUT-cache spike.** Implement the upstream CLUT-cache TODO in the
   spike backend. Receipts: the diff, identity replay still exact on
   all 98 (+ new step-1 captures), `median_ms` delta on the
   CLUT-heavy captures (before/after, same machine, recorded as
   observed, not claimed).
3. **RMW-lookup spike.** Implement the upstream RMW-lookup TODO the
   same way: diff, identity exact everywhere, timing delta on the
   affected captures.
4. **Report.** `docs/reports/G3.md` in the repo and a copy at
   `/Users/bradrichardson/dev/ssx3/local/research/G3/REPORT.md`:
   present-behavior table, both spike diffs + identity tables +
   timing deltas, CTest output (old 9 + new), exact commands, receipt
   paths, "What I could not do". No verdicts. Commit both, push ps2xGS
   `origin main`, stop.
