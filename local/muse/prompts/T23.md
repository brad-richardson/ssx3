# T23 — G1 fourth input: confirm Standard at DST, capture the next epoch (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T21/REPORT.md` first (all of it —
T21 gap G1 = this brief, the exact proposal: boot fresh to the
language park, three single 534 ms K holds → User Prefs →
Time Zone → Daylight Savings Time, then a fourth single
534 ms K hold to confirm Standard (Winter Time);
screenshot-verify the next screen), plus T21's `t21-post*.jpg`
(the DST park) and T21 gaps G2–G5 (WSL killer still active —
single-shot scripts, runs ≤6 min, preserve emulogs first,
re-check dmesg + btime + init age; F3 rec 18: capture `dmesg`
+ the Windows event log around any flap, unfiltered dmesg
only — err-level misses AcceptAsync; hold threshold still
18 ms–534 ms — reuse 534 ms, do NOT bisect; spinner animates —
perceptual/region compare, never whole-file sha). The bytesize
track continues while useful.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` (key
  auth). Devel PCSX2 v2.9.75 at `/home/brad/pcsx2-t4/pcsx2/
  build/bin/pcsx2-qt` (reuse, do NOT rebuild — table binary
  sha + size at start, must reproduce `6719f5d6…` /
  130929856 B or table the deviation). Inputs staged.
  Display: Xvfb `:99` + `QT_QPA_PLATFORM=xcb` (T17 recipe).
  Keys: xdotool focus+XTEST to the PCSX2-Qt WID (T17 path 1).
- The run: fresh boot → language park (verify: snap sha
  `b6ca1aa9…` + ExecPS2 @142369/142465) → hold-1 → User
  Prefs (region-match vs T17c) → hold-2 → Time Zone
  (region-match vs T19) → hold-3 → DST (region-match vs
  T21) → hold-4 (confirm Standard, single 534 ms) →
  capture: screenshot series (press-relative
  +5/+15/+30/+60/+120/+240) + continuous same-4-channel
  trace. Stop at the first new stable park OR 1200 s wall,
  whichever first (still complete: table epochs + inputs
  + trace).
- Single inputs only: one hold per screen, never blind
  multi-presses. If hold-4 does nothing, table exactly that
  (snaps + trace markers) — ONE retry at ≤1000 ms max,
  tabled.
- NO lease of any kind (all boots on bytesize). No `adb`.
  No fork changes. No copyrighted downloads (inputs exist).

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce the input run + capture from scratch.
- Evidence dir `local/research/T23/` (STANDALONE). Commit with
  `git add -f`, prefix `[T23]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 6 h. Tables + receipts, no verdicts. If input
  automation regresses, table exactly what was tried,
  commit what exists, stop.
- Hygiene (F3 rec 17/18): write the report in chunks with
  a tail receipt (truncated tail fails the gate); on ANY
  WSL flap capture UNFILTERED `dmesg` + the Windows event
  log around it before continuing.

## Task 1 — park chain (do all four parks reproduce?)

1. Park-1 table: fresh boot reproduces the language park?
   (snap sha + ExecPS2 lines + vblank count — match/no
   vs T21's numbers).
2. Press-1/2/3 table: holds reproduce User Prefs + Time
   Zone + DST? (region-matches + trace markers — match/no).
3. Press-4 table: the confirm-Standard hold (method, timing,
   single-press proof — one down/up pair, exit 0s).

## Task 2 — next-epoch capture (what follows confirm?)

1. Epoch table: screens reached after press-4 (guest events,
   log times, screenshot per screen — or the press does
   nothing: table that exactly + the ≤1000 ms retry row).
2. Trace table: channels captured (same-4 format check vs T4
   samples), lines/bytes, paths + shas both ends (traces
   stay on bytesize if >1 GB, with head/tail samples
   committed).
3. Park table: first new stable park (screen + awaiting-what
   + vblank count — or NONE to 1200 s cap: bound tabled).

## Report

`local/research/T23/REPORT.md`: T4-0..T4-4 SHAPE (park
chain, presses, epochs, trace inventory, exact commands
both ends, gaps — incl. next-input proposal if deeper
epochs remain). Commit evidence (`[T23]`), stop.
