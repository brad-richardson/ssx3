# N8D7M12 Part 5F3 — GPU readback source coverage table (no run)

You are the N/G OpenCode Go source-audit worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7M12P5F2/{REPORT.md,ORCH-GATE.md}`, the Part 5F1 correction/gate, and this brief. Own only `local/research/N8D7M12P5F3/` and this brief. No build, replay, boot, device/lease action, source/fork change, fetch, push, board/global edit or upstream contact. Time box 30 min, text <512 KiB. Write the report/checker early. Confirm code connections with LSP where available, then exact source reads; state when LSP is unavailable.

Goal: give the orchestrator one source-grounded discriminating observable for the Odin same-settings replay variance at tick850. The identical APK/stream/settings OFF1/OFF2 produced equal sampled `priv` hashes 41/41, but VRAM 16/41 and present 25/41 equal, first difference tick850. `priv` does not prove packet order. The three packaged renderer files in `~/dev/ssx3-work/N8D7F/parallel-gs` match N8D7H SHAs (F2); `gs_interface.cpp`, Granite, and driver behavior are not package-pinned. Do not present unpinned source as built behavior.

Hypotheses for a later replay experiment: H1, packet delivery/order differs before the renderer; H2, identical packets reach the renderer but GPU state/readback differs. A sampled `priv` hash does not separate them. Find a **pre-submit packet-sequence fingerprint at or immediately after the replay queue drain** that would separate H1 from H2 in two same-settings runs, ideally without adding per-packet device readbacks. Also map the readback coverage as a second, source-only table so the orchestrator can judge whether an observed GPU hash difference might reflect stale data.

Tasks:

1. Double-hash the three pinned renderer files and record HEAD/dirty state. Pin SHA of every additional source file read; label those files *unpinned to APK*. Do not infer provenance from a matching filename.
2. Trace exact call edges for `SnapshotVram` / `map_vram_read(0,4MiB)` and `Present` scanout staging through `gs_interface.cpp`, Granite `Device::map_host_buffer`/invalidate/wait paths, and the page tracker. Table each GPU writer class visible in this bounded path (transfer, render-pass store, `copy_blocks`, promotion where present), whether it marks the tracker, and each reader's wait/invalidate step. Use file:line citations for each edge. Explicitly mark unknown driver/Turnip guarantees. Do not assert a bypass solely because a grep finds no name.
3. In the pinned replay core/fork path, locate the earliest point where ordered packet bytes can be hashed after queue drain, before any GPU submit/readback. Give the exact source site, packet fields included, and whether the existing 1.1 GB stream digest plus replay summary already proves that fingerprint (likely no; verify). Define two different predicted results: equal fingerprint + different GPU hash favors H2; different fingerprint before GPU hash favors H1. State what remains unproved in each case.
4. Hand back **one** bounded next observable: exact instrumentation/receipt design only, with expected log bytes, run cap and stop rule. No code change or run in this part. If no suitable fingerprint site exists, B with a cited explanation and the closest alternative.

Deliver `REPORT.md` with a compact provenance table, call/writer/reader table, fingerprint site and prediction table, one recommendation, gaps; `check.py` and `check-result.json` verifying SHA pins, cited line anchors and required table fields, no cause verdict. A = source map and discriminating design complete; B = source gap prevents it; OTHER = audit failure. Commit `[N8D7M12] Part 5F3` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Hand back the table; the orchestrator decides.
