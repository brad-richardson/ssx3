# M26 — Within-column hump profiles: shape tables + hump/quadratic fits (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M25/REPORT.md` first (all of it —
M25 gap 1 is this brief: s0 c257/277/301/321 read rise-then-fall
row profiles — e.g. c257 17→30→13, c301 18→47→13 — with LINEAR
slopes near 0 and top−bottom means within 5), plus M25's Task-1
profile tables (the row→δ lists this run fits). Fully offline —
SSD dumps read-only, no harness code, no replays, no lease. No
device work.

## Facts you start from

- Hump columns (M25): c257/277/301/321 on s0 (+ m15 analogs)
  rise then fall along rows (peak mid-column), which is why
  LINEAR (monotone) scores below CONST (flat) — neither
  captures a hump. c296/340 read wide ragged (sd 10.7–12.5,
  ranges 30–36); c342/343 read negative throughout.
- Candidate shape fits (FIXED list — record in DESIGN.md before
  running, no additions mid-run): quadratic (least-squares
  a·r²+b·r+c over present rows, rounded); triangle (peak row
  p + height h + half-width w, symmetric); two-level
  (top-half median / bottom-half median split at mid row).
  Score on named-column tail bytes: exact hits / near hits
  (|err|≤1, separate) / miss distribution.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m25/m25.txt` (column profiles to
  reproduce: per-column row→δ lists + shape stats — match
  exactly or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m26/`; evidence `local/research/M26/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m25/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m26/` + `local/research/M26/`.
- Evidence dir `local/research/M26/` (STANDALONE). Commit with
  `git add -f`, prefix `[M26]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M25 precedent).

## Task 1 — profile-shape tables (hump or not?)

Column profiles recomputed from the dumps (row→δ lists + shape
stats must match M25 exactly or table the mismatch and stop):

1. Hump metrics per named column per frame: peak row + value,
   rise (peak − first), fall (peak − last), symmetry
   (|rise − fall|), argmax position (top/mid/bottom third).
2. Non-hump columns tabled the same way (c296/340 ragged,
   c342/343 negative — same metrics, no special-casing).
3. Cross-frame shape match: per-column s0-vs-m15 peak rows +
   (rise, fall) pairs (same shape? same peak row?).

## Task 2 — hump fits (fixed list, exact-hit scoring)

1. Score each DESIGN.md-listed fit on s0 + m15 named-column
   bytes: exact hits / near hits / miss |err| distribution.
   Best fit's explained bytes + residual |err| hist.
2. Cross-frame check: fit on s0, score on m15 (and vice versa)
   — table both directions (M25's CONST collapsed 12/13 →
   0/1; does any shape fit transfer?).
3. Beat-the-baseline table: best shape fit vs M25 CONST
   (12/65 + 13/68) head-to-head per column (which columns
   does shape beat flat on?).

## Task 3 — controls + determinism (M18–M25 precedent)

1. Positive control: synthetic truth with KNOWN hump profiles
   (N sites, δ = listed triangle/quadratic + noise-free) —
   recover profiles + best-fit identity exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M25).

## Report

`local/research/M26/REPORT.md`: DESIGN.md (bars + fixed fit
list first) + shape tables + fit scores + transfer tables +
controls + explained-vs-standing update (does any shape fit
shrink the tail? by how much?) + gap rows. Screenshot only if
a shape-fit map discriminates (budget-noted, pre-registered
split required per M23–M25 precedent). Commit evidence
(`[M26]`, no push), stop.
