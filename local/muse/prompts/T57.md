# T57 — PCSX2: VU0 micro-program calls and VIF0 traffic at Select Character (healthy side of E44 Part 2)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). **Tables + receipts; the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T56/REPORT.md`.

## Why

An independent review (orchestrator-verified) found the recomp's VU0 path
uninstrumented and fragile: its VIF0 parser has no MSCAL/MSCALF/MSCNT/BASE/
OFFSET branch (an unknown opcode aborts the packet), and VU0 micro-calls
(`vcallms/vcallmsr`) run under a 4,096-cycle budget with a silent exit. The
lead: a VU0 cull/skinning program whose result gates the rider object's
per-frame packet (its scratchpad item keeps mode 0 and w3 = 0 in the
recomp; PCSX2 has mode 6 and a w3 packet pointer).

## Change (log-only, on T56's clone; EE + VU0 interpreters)

Over Select Character settled (statefile is fine), first 2000 lines:
1. Every VU0 micro-program start (COP2 `vcallms`/`vcallmsr` from the EE, and
   VIF0 MSCAL/MSCALF/MSCNT): `vu0call vsync caller_pc=0x<EE pc> via=<cop2|vif0>
   startPC=0x<byte addr> cycles=<to E-bit> vi1=0x<> vi2=0x<> (after)`.
2. VIF0 command census per vsync: `vif0op vsync op=<name> n=<count>`.
Mark the `vcallmsr` at EE `0x37deb8` (`sub_0037D968`) explicitly. Prove
preservation as T48–T56 did.

## Deliverable

≤2 builds, ≤2 captures, 3 h, cap 2 GB. `local/research/T57/REPORT.md`
(patch, hooks, preservation, tables: VU0 calls by startPC with cycle stats and
vi1/vi2 distributions; VIF0 opcodes per vsync). No verdict. `[T57]` commit
(`git add -f local/research/T57 local/muse/prompts/T57.md`, trailer
`Orchestrated-By: Muse Code`, `git log -1` first), no push.
