# N8D Part 3 — raw scanout versus host-frame copy on Odin

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §6. Read full N8D1 and N8D2 reports and their source/launcher receipts first. One live runtime mutator: work alone on this Android diagnostic until source/build are done. Write scope: `local/research/N8D3/`, `~/dev/ssx3-work/N8D3/`, and a fresh **non-git** WSL copy `/home/brad/n8d3/` on bytesize made from `/home/brad/n8d1/`. Do not edit `/home/brad/n8d1/`, other fork worktrees, generated guest code, game data, or main fork. No fork push/upstream contact.

N8D2's one race run (APK `ece8b84ce3bce6bb85e42e21c432f6a6123ccae6e61dd91293e9da696fc63ae2`) captured host uploads at ticks 1840/1950/2050 and a nearby screen. At tick 2050 the 512×448 host upload is already mostly black with HUD fragments; the screen has similar major loss plus stripes. The exact screen guest tick is unknown. In N8B1 source, `ps2_gs_parallel_backend.cpp:278-307` copies `shot.image` through a Vulkan host buffer and packs valid rows into a 640-stride `out.pixels`; `gs_frontend.cpp:~901` packs the host presentation frame; `ps2_runtime.cpp:~799` calls `UpdateTexture`. Confirm these lines in the N8D1 WSL copy before editing. The next question is **where the first damaged image appears**.

**Predictions:** raw mapped `shot.image` bytes already show the black/fragmented scene → damage is at/before Vulkan copy/readback (GPU scanout versus readback still open); raw image recognizable but backend `out.pixels` bad → stride/copy fault; backend pixels recognizable but frontend pre-`UpdateTexture` PNG bad → frontend selection/crop/packing fault. A same-tick mismatch in FBP/dimensions or unreadable raw format leaves the stage unresolved. Give a table, not a cause verdict. Do not infer Turnip version from packaging or report speed.

Create one minimal default-OFF diagnostic, enabled only by an N8D3 env flag. At **one** race tick near 2050, before the backend copies `px` into `out.pixels`, write bounded raw mapped bytes plus width/height/format/tick/FBP metadata; after the copy, write bounded packed bytes or a per-row comparison receipt; reuse N8D1's existing frontend `PS2X_FRAME_DUMP_ONCE_TICKS=2050` PNG. Use binary writes with explicit byte counts and errors, never unbounded per-frame logging. Compare raw valid rows to packed valid rows byte-for-byte and decode/view raw and frontend images with declared channel order. If channel order is uncertain, state it and use byte comparisons only. Keep the existing Turnip/shim and the five diagnostic CMake flags OFF. One APK build on bytesize WSL using N8D1's governor/build recipe; no tuning loop. Package gate: APK/arm64 members/flags/source hash, source diff restricted to this diagnostic.

One Odin install/launch under `/data/local/tmp/mg/LEASE`, I26-FAST, empty card, parallel/Turnip, dev movie bypass. Before launch check keyguard `showing=false`, battery ≥20% charging/full; always install and double-read installed APK, ELF and ISO SHAs. Capture raw/packed/frontend files at one tick, at most one nearby screencap if useful, with two device and local SHA reads. Stop by tick 2100 or first definitive failure; route cap 180 s, hard cap 240 s. Always force-stop, verify PID absent, release lease. No second launch if any capture misses. Do not run a simultaneous heavy job on bytesize.

Budget ≤10 GiB WSL scratch, ≤1 GiB Mac scratch, ≤16 MiB logs, ≤12 MiB images/row dumps, one build and one boot. Check global `disk_budget.sh` around any Mac copy >1 GiB. Acceptance script checks one build/one install/launch, pins, same tick/FBP/dimensions, binary length and row comparisons, PNG decodes, SHA pairs, cleanup and limits. Deliver `local/research/N8D3/REPORT.md` with exact commands, pinned source and APK, source diff, table of raw/backend/frontend image observations, acceptance output and gaps. Commit only named text receipts and small images `[N8D3]` with `Orchestrated-By: Codex`; no binary/APK commit or push. Hand back the table; orchestrator decides.
