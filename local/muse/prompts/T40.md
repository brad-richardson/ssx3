# T40 — Sub-second exposure series around the 1 s hold: post-hoc scoring, no in-script gaps (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T39/REPORT.md` first (all of it: R1
reproduced the chain to LIVE gameplay, sham window with NO input
(1003.7 ms gap @T+504.62) at a matched phase (pre-pair 00:01:24 4TH/6 36% /
00:01:29 4TH/6 38%), dense 10-snap no-input series to 00:02:10 6TH/6 55%
showing the CONTROL distribution — 4 position swaps + decay 4TH→6TH, RDC
gated out 9/12, SCPS40 rails 5/11, SCPS120 rails 4/11 incl. the pre-pair gap
and the sham hop — matching T36's tap / T37's tap / T38's hold behavior
class; G1 proposes T38 G1(b), still open: sub-second exposures — this brief
takes it). This brief executes T39's G1. Reuse T39's scripts
(`t39-auto.sh`, `t39-cropdiff.py`, `t39-track.py`, `t39-analyze.sh`,
`t39-vcount.sh` — copy, do not modify the committed originals).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T39 verified (re-verify sha/size/rev/
  status/pad-binding INCLUDING `Left = Keyboard/Left` + NVM state before
  running; table the receipts).
- GATE NOTE (T39 G1 — gate on the HUD, not the frame): gameplay hops are
  large because the race never stops moving. Live-race criterion = race
  clock advancing + position/progress HUD read off VIEWED snaps. The
  LIVE-LIKE proxy (departed + non-panel + motion ×2) still gates the input
  phase in-script.
- THE DELIBERATE CHANGE (the ONLY one vs T39): exposure gaps closer to
  true 1 s by scoring POST-HOC, not in-script. T36–T39's ~3 s exposure gaps
  (1 s sleep + ~2 s in-script scoring per snap) rail SCPS at ±40 AND ±120
  and flood RDC. In the dense phase ONLY: capture snaps back-to-back at
  ~1 s wall cadence (sleep 1, NO per-snap scoring calls), then score
  everything post-hoc from the fetched snaps with the committed
  `t40-cropdiff.py` / `t40-track.py`. Table the expected cadence + the
  post-hoc scoring plan BEFORE the run. All chain/arrival phases keep
  their in-script gates (they need them); only the dense phase goes blind.
- The task: reproduce the T39 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → LIVE-LIKE proxy gate → ≤3 attempts) to LIVE gameplay, then
  run the dense phase as: pre-pair (no input, back-to-back) + ONE 1 s
  D-pad Left HOLD (T38's input class — the hold whose response the control
  could not separate) + 10 back-to-back post-hold snaps. Post-hoc: per-snap
  HUD + RDC-gate/SCPS40/SCPS120 tracking + whole hops + rail-rate
  comparison vs T38/T39 (did sub-second exposures stop the railing? does a
  gated RDC pair now straddle the hold?).
- If SCPS still rails or RDC still floods at ~1 s exposures: table the
  exact observed rates + noise floors and STOP with a recipe — one input
  variant per attempt, never blind multi-presses. ONE bounded variant
  allowed only if the hold provably never acted on live gameplay (pre-hold
  ≠ racing).
- Success bars (tabled, not verdicts): live race reproduced at a matched
  phase (HUD advancing, phase-match tabled); hold acted on it (pre-hold =
  racing with HUD read); sub-second dense series (HUD + tracking + hops,
  post-hoc scored); rail-rate comparison vs T38/T39 + gated-pair verdict;
  full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T39 G2: single-shot
  scripts, runs ≤6 min (table any overrun with full dmesg coverage —
  back-to-back capture should SHORTEN the run; table the delta), preserve
  emulogs first, dmesg + btime + init age, UNFILTERED dmesg + event log
  around flaps, 534 ms holds NOT bisected, `;` chains only INSIDE one `wsl`
  call, one `wsl` call per ssh; post-run dmesg read IMMEDIATELY after the
  run ssh returns). COUNT AcceptAsync lines in the committed dmesg files
  with exact counts + uptime-stamp positions relative to the run window.
  Runs in flap windows must be effects-verified or re-run (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T40/` (STANDALONE). Commit with `git add -f`,
  prefix `[T40]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + ONE 1 s hold + back-to-back series (≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T39 §T39-0 shape): build/input receipts (INCLUDING the
   Left binding), NVM state, T39 reference snaps present, free space both
   ends. Table the blind-capture cadence + post-hoc scoring plan + the
   T38/T39 rail rates you intend to beat BEFORE the run.
2. Per attempt table: race reproduction (chain scores + live-race gate),
   hold row (control/duration/T+/pre-post HUD), post-hoc dense series (HUD
   + tracking + hops per snap), rail-rate comparison + gated-pair verdict
   (or exact non-resolution finding).
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T40/REPORT.md`: pre-run receipts, attempt tables, hold log,
sub-second series + tracking + comparison (or non-resolution finding +
recipe), WSL table, exact commands, gaps. Commit evidence (`[T40]`), stop.
