# F5 — fold VU1 stage B + 4×/pipelined present + post-race + carve trail + TZ/x86 onto fork `ssx3`; builds for Brad (muse)

Gates: `local/research/{VB1,HR1,PF1/POSTRACE-REPORT,RP1,LX1}` (orchestrator sections at the end). Brad approved device installs (iPhone install only) and asked for 4× + double-resolution output.

## Part 1 — fold + Mac check (stop after; the orchestrator gates and pushes)
1. Worktree `~/dev/ssx3-work/F5/PS2Recomp`, branch `f5-fold` from fork `ssx3` **`74e2df2`**. `git cherry-pick -x`, oldest first within each group:
   - **VB1 stage B:** `9983169 86509dc fda8364 b74d81d 09ebafd 9638b3d` (+ `9b71115`, hash-build counters). They sit on VR1's pre-fold commits; the VR1 code is already in `74e2df2` under new SHAs. Keep F4's musttail handoff (`PS2X_VU1_MUSTTAIL return next`) in any emitter conflict.
   - **HR1:** `cf7c0df 4825123 4a591d3 fb3dca0 d929048`.
   - **LX1:** `5f32212 20377a3 b4cb476 fc0cc67 14ea352`.
   - **PF1:** `3c037ab` (post-race unwind fix; if it depends on `a1eb9b9`'s dev knob, pick that too and say so).
   - **RP1:** `9bfd4aa` (VU MAX/MINI raw bits); skip `edce9dd`/`f233fc5` unless `9bfd4aa` needs them.
   A conflict you can't resolve mechanically → stop, save the hunks to `local/research/F5/conflict.txt`, hand back.
2. **Codegen:** promote the F4 regen to canonical now (`mv ~/dev/ssx3-work/codegen-ssx3 ~/dev/ssx3-work/codegen-ssx3-pre-f4`; `cp -R ~/dev/ssx3-work/F4/codegen ~/dev/ssx3-work/codegen-ssx3`; SHAs + counts; `local/tooling/ee/sbr-census.py --self-check` still 5/5). **VU1 images:** VB1 changes the emitter, so regenerate the 7 images (dump boot, `PS2X_VU1_RECOMP=0` + `PS2X_VU1_RECOMP_DUMP`) into `~/dev/ssx3-work/F5/vu1gen`; every handoff must still carry `PS2X_VU1_MUSTTAIL`; report the diff shape vs `~/dev/ssx3-work/vu1gen-ssx3`.
3. Suite from the worktree root; runner-dir check empty. Mac Release builds (clean + det-hash), paraLLEl `19d93b2`, the F5 VU1 images.
4. Boots (one slot each; FR1-R1 route = I26-FAST minus the down-hold (`local/research/FR1/`); empty mc0; `PS2X_SKIP_MOVIE=1`; paraLLEl + `PGS_HIER_BINNING=force`; deterministic):
   - **B1** 1× default, sound on, to t2400: frames 1090/1800/2100 viewed (rider, **carve trail**, brightness), cid0 531, coverage 0/0/4, VU1 100 % generated.
   - **B2** sound off + unpaced: det-hash identical to B1.
   - **B3** `PS2X_GAME_THREAD_STACK_KB=512`: reaches t2400 (small-stack rule).
   - **B4** 4×+hi-res + pipelined (`PS2X_PGS_SSAA=4 PS2X_PGS_HIRES_SCANOUT=1 PS2X_PGS_PRESENT_PIPELINE=1`): det-hash identical to B1; frames at 2100 viewed (1024×896).
   - **B5** the finished race: FR1-R1 to the results screen and ≥ 2 min past tick 20063 with no missing target (PF1 fix; ≤ 1,800 s approved).
   - **B6** speed, exclusive, `PS2X_UNPACED=1`, pipelined 1× vs F4 B3.
5. **Android compile** on bytesize (F4 recipe + the F5 VU1 images + the promoted codegen): `assembleRelease` succeeds; keep the APK.
6. `local/research/F5/REPORT.md` Part 1; commit `[F5] Part 1 …` (`git add -f`, `Orchestrated-By: Muse Code`). **No push.** Stop.

## Part 2 — Odin (after the push)
Fork `ssx3` = `a3efbfe` (pushed 09-25). Use the Wi-Fi adb serial in `local/odin-serial`; record the transport per run and any disconnect.
Four clean race runs, cool-down method, `PS2X_UNPACED=1`, ABBA: A = 1× pipelined, B = 4×+hi-res pipelined; report race rate, GPU busy, thermal per run. Then install as Brad's play build with **the faster of the two if 4×+hi-res costs ≤ 5 %, else 1× pipelined** (put the chosen knobs in Brad's Odin env via `deploy-odin.sh`, keeping his other keys), save + env verified, no launch.

## Part 3 — iPhone (after the push; alongside Part 2)
I33/F3 iOS recipe with the F5 VU1 images and codegen; bundled env adds `PS2X_PGS_SSAA=4`, `PS2X_PGS_HIRES_SCANOUT=1`, `PS2X_PGS_PRESENT_PIPELINE=1`, `PS2X_PRESENT_ZERO_COPY=1` (HR1's iOS IOSurface path; on the iPad confirm the zero-copy line in the console and a correct frame — if it fails, drop only that key and note it). iPad fresh-card launch (frames viewed, sharper picture, carve trail, pad v2), then iPhone install + `deploy-ios.sh iphone`, **never launch**.

Rules: text only in git; never push; never `git add -f` in the fork; generated code never in git; scratch `~/dev/ssx3-work/F5/` ≤ 20 GB (delete build dirs at the end); one mini slot per boot, exclusive holds ≤ 5 min. First failure: stop, save the error, hand back.
