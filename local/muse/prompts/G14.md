# G14 — Odin on-device replay of the rich dump, scored vs dual-agreed scanouts

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts.** Read first: `docs/reports/G13.md` (all — scene-scale
agreement, corrected labels, dual-agreed scanouts for the rich dump
`g13-dump.gs`; §5 ONE next action = this brief) + `docs/reports/G7.md` §5
(on-device init recipe: NDK r30 + CMake Android toolchain, arm64-v8a,
API 35, `ninja parallel-gs-replayer`, push to `/data/local/tmp/`,
wall-capped run, scanout pull). This brief executes G13's §5. The
emulator-vs-emulator comparison is CLOSED — do not re-run it; cite G13.

## Facts you start from

- The adoption question has exactly one unproven leg: on-device
  init/replay (OPEN since G7). The oracle is ready: G13's 8 PPMs + 7 PNGs
  for the rich dump, dual-agreed to ±2 LSB at every corrected pair.
- The task: build `parallel-gs-replayer` for Android arm64 (G7 §5 recipe),
  push it + the RICH dump (`g13-dump.gs`, sha `154d9d85…`) to the Odin,
  run ONE bounded on-device replay, pull the scanout(s), and score them
  against the dual-agreed emulator scanouts (same corrected-label,
  S-lag-aware method). Odin: device `622c49b1` (Adreno 830, Android 15 —
  G7 E2's 10/10 capability device; re-confirm via `adb devices`).
- Experiment contract (required): hypothesis = on-device replay produces
  scanouts matching the dual-agreed emulator scanouts within a tabled
  tolerance; observable = build receipts + run exit/wall/heap + pulled
  scanout bytes + per-pair diff table vs G13 PPM/PNG oracles; alternatives
  = (a) match → table + ONE next action (adoption input); (b) mismatch →
  table the divergence + the ONE experiment it prescribes, stop; (c) build
  or run blocked → table the exact obstacle + recipe, stop. Stop: ONE
  bounded on-device run — no tuning loop, no renderer changes, no new
  dumps.
- No P-lane contention: no recomp boots/builds, no P-lane lease (this is
  device-side replay, T28 recipe shape for any bytesize reads — none
  expected), no fork writes. `adb` push/run/pull in `/data/local/tmp/g14/`
  ONLY (never the `mg/` tree); this is not an app install (no device-install
  policy involved). Byte caps declared up front, SSD reuse, nothing new
  on `/` beyond the NDK build cache (tabled).

## Gates and rules

- Reuse the G11-end SSD clone state if it still verifies (HEAD + hooks);
  NDK r30 at `/opt/homebrew/share/android-ndk` (G7 receipt — re-confirm).
  Host builds `-j2`. Wall cap on the device run + progress cap.
- Commit in ps2xGS as you go (prefix `[G14]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures,
  binaries, or build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G14/` (`git add -f`,
  `[G14]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h (the NDK build may take 30–90 min first time — table it).
  Tables + hypothesis + next-action recommendation.

## Task 1 — build + ONE bounded on-device replay + score (rich dump only)

1. NDK build receipts (config flags, exit, binary sha/size); device
   reconfirm (model, SoC, Vulkan driver — G7 E2 shape, abbreviated).
2. Push + run (wall-capped) + pull: exit code, wall/VBlank, heap, scanout
   bytes + shas. Run from a clean `/data/local/tmp/g14/`; remove bulky
   device-side artifacts after retrieval.
3. Score table vs G13 oracles (corrected labels) + hypothesis verdict +
   the ONE next action the numbers justify.

## Report

Mirrored `REPORT.md`: contract, build/run/score tables, verdict (or
obstacle + recipe), license receipts, exact commands, gaps. Commit both
repos as above, stop.
