# GB7C1 — off-screen text pixel-trace design (Muse Contributor Go)

You are an OpenCode Go G-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, GB7B `REPORT.md`, `ORCH-GATE.md` and `candidates.tsv`, GB7A gate, and the GB7C item in `docs/todo.md`. Work read-only in clean `~/dev/ssx3-work/GB4/PS2Recomp` branch `gb4-parallel` at `09d583a36ee62c165ce9e0473b945821a7a128f7` and G43 `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd`. Cite repo lines and use LSP definitions/references to confirm call edges. Write only `local/research/GB7C1/{REPORT.md,trace-map.tsv}` and this brief. No source/build/replay/device/web/upstream/push. Keep context bounded; deliver the table in <=12 minutes.

Question: where can one default-OFF CPU diagnostic sample a **pixel's old/new value** immediately around an `fbp=0` T4 atlas sprite and track the resulting off-screen pixel into the `fbp=112` full-height blit and final displayed crop? The existing GB7B rectangles only show overlap; the sampled four-texel fingerprint cannot identify a glyph. Name one concrete candidate at tick600 packet47176 (lower crop, atlas `tbp0=11017, cbp=11016`) and one at tick699 packet59894. Map the exact CPU raster write site, texture sample/read site, VRAM address mapping, packet/tick/true-path propagation, framebuffer read before/after, and the following full-height display blit. Show whether a single pixel can be traced through these sites without changing render output. The existing pinned stream SHA `a6f75fb34fceccf6fd92ad65950d16f05a140cf38a3cc2f028be32e56ad51851` and `run/gb4p4.paths.txt` are the null-control inputs; do not copy the stream.

Return `trace-map.tsv` with 8–12 rows: `stage,file_line,packet_or_tick,source_or_dest,observable,hook_cost,uncertainty`. Give a precise next-brief proposal with at most one incremental build, one flag-unset suite and one CPU replay through tick700; propose hard log caps <=8 MiB and <=20,000 rows, specific pixel/ROI bounds, source-texel evidence and before/after crop evidence. Preregister PASS if a specific off-screen draw changes a glyph-shaped crop and a later display blit preserves it; OTHER if the hook misses the change, ordering is ambiguous, or a full-screen overwrite intervenes. Distinguish glyph producer from paraLLEl cause; no cause claim can follow from CPU-only data. If one probe cannot capture the chain within caps, identify the missing hook and stop.

Deliver the source table and a short `REPORT.md` with exact pin and gaps. Commit only report, TSV and brief `[GB7C1]` with `Orchestrated-By: opencode`, no push. Hand back the evidence table; the orchestrator decides the next implementation brief.
