# G16 — Mac-host ASan (+UBSan) run of the same replayer source: name-or-eliminate the app-side heap writer (no device)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G15/REPORT.md` (ssx3 mirror) first (all
of it: O1 flaky 1/6, O3 caught live with PROVEN scudo heap corruption
(`corrupted chunk header at 0x200007ea68423b0`, faulting pool `f=0,i=2`
TRANSFER with every app-side input sane), MTE naming BLOCKED by the kernel,
HWASan build silent and crashed on O2 (fault 0xf0, now 3/6 modal); the ONE
next action = this brief). This brief executes G15's §3. No device, no
Odin, no `adb`, no renderer changes.

## Facts you start from

- Pins (re-verify, do not move): paraLLEl-GS
  `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims + G11/G8+G10
  hunks (G15 §2a table — the clone state must reproduce it exactly); rich
  dump sha `154d9d85…`; G13's mac replayer recipe (G13: macOS runs this
  exact code clean to exit 0, 18 frames — read the G13 mac build/run
  receipts and reuse them + sanitizer flags).
- The task: ONE mac-host ASan (+UBSan) build (separate build dir — G14/G15
  dirs untouched; SSD only, allocated-byte cap declared up front) and ONE
  bounded ASan run of the same dump (`--iterations 2` or the G13 mac
  iteration count — table the choice). EITHER outcome is decisive: (a) ASan
  fires → table the writer (file/line + alloc/free traces) + the ONE next
  action (minimal-fix brief input); (b) ASan clean to exit 0 → the writer
  is Android-specific (Adreno driver or Android-only path) → table the
  clean run + the driver-bug-recipe next action. No tuning loop: no source
  fix, no second sanitizer variant, no renderer change in this brief.
- Success bars (tabled, not verdicts): pins reproduce G15 §2a (clone HEAD,
  shims, dump sha); ASan build exit 0 with instrumentation proof (sanitizer
  symbol census); ONE bounded run with complete stderr/log receipt;
  hypothesis verdict (writer named with traces, or clean-run elimination);
  the ONE next action the numbers justify.

## Gates and rules

- NO device work of any kind (no `adb`, no Odin, no tombstones, no lldb).
  No P-lane lease (no recomp boots/builds, no fork writes). No bytesize/WSL.
  Host builds `-j2` (one target per build). No new dumps.
- Byte caps: declare up front for the new ASan build dir (SSD) + retrieval;
  `ps2x-g13`/`ps2x-g14`/`ps2x-g15` read-only (0 growth); internal `/`
  delta ≤1 GB, no clones/builds on internal; `export COPYFILE_DISABLE=1`
  on SSD steps; device dirs untouched.
- Clone discipline: ZERO clone edits — sanitizer flags are build-dir
  configuration only, never source patches. No third-party code copied
  into any project tree.
- Evidence: ps2xGS commit `[G16]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G16/`
  (`git add -f`, prefix `[G16]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — pin verify + ASan build (no run yet)

1. Re-verify G15 §2a pins (clone HEAD/status, shims, dump sha host-side,
   NDK/toolchain only if the mac recipe needs it — table what the mac
   recipe actually uses).
2. Configure the ASan (+UBSan) build in a NEW SSD build dir (G13 mac recipe
   + sanitizer flags — table every flag delta vs G13); build exit 0;
   table binary path/size/sha + instrumentation proof (`__asan` symbol
   census).

## Task 2 — ONE bounded ASan run + verdict

1. Run the dump once, bounded (wall cap + `timeout`), full stderr captured.
   Table exit code, wall time, frames reached, and the COMPLETE sanitizer
   output (or its proven absence: exit 0 + frame count + clean tail).
2. Hypothesis verdict + the ONE next action: writer traces → minimal-fix
   brief input; clean → Android-specific elimination + driver-bug recipe.
   Score against G13 oracles with `g14-diff.py` if scanouts exist, else
   SCORE N/A (tabled, never faked).

## Report

`local/research/G16/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, pin table, build record, run table, verdict, exact
commands, gaps. Commit evidence (`[G16]` both trees), stop.
