# E11 — Reclaim stale /tmp build trees, admit query regen, repair 0x2c5140, one guarded boot

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E10/REPORT.md` and
`local/research/E10/NEXT-BRIEF.md` first (all of both: DROP checkpoint
COMPLETE — registry `5d4ce8c4…`, runner `f5eeea20…`, suite 452/452, actual
DROP handler returns normally, fail-before `hasFunction(0x2c5140)=false`;
outcome (iii) — query generation stopped at fresh APFS admission, free
3,240,579,072 B vs required 3,489,660,928 B, shortfall 249,081,856 B; CSV
restored byte-exact; 0 boots; fork `be0c9ee`; exception closed). This brief
renews the bounded regeneration exception for the query entry ONLY, with the
ordering repair (admission BEFORE csv mutation) and a space-reclamation
first step. Reuse E10's scripts (`e10_manifest.py`, `e10_generate.py`,
`e10_install.py`, `e10_link_test.py`, `e10_static.py`, `e10_add_entry.py` —
copy to `e11_*`, do not modify the committed originals).

## Facts you start from

- Root cause of E10's stop: internal APFS free ~2.4 GB vs 3.25 GiB
  admission. `/tmp` holds MANY stale lane build trees (~1.6 GB each:
  `t1/t5/t6/t12-link`, `p11-link`, …) from retired lanes; `/tmp/p1-link`
  is the ACTIVE E10 DROP checkpoint (runner path) — NEVER delete it.
  Xcode DerivedData (~1.4 GB) is NOT reclaimable (belongs to Xcode).
- The task, in strict order: (1) re-verify the E10 DROP checkpoint identity
  (registry SHA, runner SHA, all 9,449 names/hashes vs `after/generated.json`,
  suite green, binding-test absent mode — do NOT repeat DROP generation);
  (2) reclaim stale `/tmp/*-link` trees, OLDEST FIRST, each ONLY after
  proving no live owner (no live lane pane claims it, no running process
  holds files under it — `lsof`/`fuser` + board check, tabled per tree),
  until a FRESH admission sample passes with E10's full budget
  (1 GiB logical + 2 GiB floor + 256 MiB guard); table before/after `df`
  per removal; (3) ONLY then insert the query row
  (`sub_002C5140,0x2c5140,0x2c5168,0x28` after `sub_002C50E0…`, before
  `0x2c5168`, expect CSV `c17db90b…`), recheck admission IMMEDIATELY before
  spawning, regenerate the query entry, install changed bytes only, rebuild,
  run the actual-binding query truth table + DROP regression + full suite;
  (4) ONE guarded verification boot with the E10 joins (dynamic a0
  `[a0]=0x486f78`, constructor PC `0x2c3fc4`, UI store PC `0x23d5c8`,
  query return `0x2c44b0`, state-3 path, UI pending `+0x338`,
  packet→GS→Present).
- `e10_observe.py` / `e10_card_observe.inc` are prepared but NOT installed,
  compiled, or boot-validated — review before reuse, fresh evidence names,
  boots counted under this brief's limits.
- Success bars (tabled, not verdicts): DROP checkpoint identity re-verified
  (no regen repeated); admission passes on a fresh sample AFTER reclamation
  (per-tree ownership proof + df receipts); query entry generated with exact-
  PC binding; truth table pass-after executes (all 4 cases); suite green;
  one boot shows the query joins or the first remaining wall (incl.
  `0x2c5358`).

## Gates and rules

- Renewed bounded exception: query-entry regeneration ONLY (one map row +
  its generated entry). Any second gap → table as next gap, do not close.
  Standing no-regen resumes at close. Fork discipline: named-files commits
  only, generated sources NEVER staged, fork-remote push only.
- Ordering repair (E10 lesson): admission check + persist BEFORE csv edit;
  recheck immediately before generator spawn. Never lower a declared guard
  after a rejection. Stop on unmet admission — do not mutate.
- Reclamation safety: per-tree ownership proof BEFORE removal (lane-board
  check + no live file handles); oldest first; stop removing once admission
  passes + 512 MiB headroom; never touch `/tmp/p1-link`, DerivedData, or
  any path a live lane owns. `export COPYFILE_DISABLE=1` on SSD steps;
  allocated-byte caps declared + tracked (ExFAT).
- Boots: ≤1 guarded boot under the shared P-lane lease (fresh T13 pre-claim
  checks, wall/progress/byte caps, REPORT_ALL=1, E7/E4 taps). No boot without
  the lease; never wait on another lane's lease — table and stop.
- Evidence dir `local/research/E11/` (STANDALONE). Commit with `git add -f`,
  prefix `[E11]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — checkpoint re-verify + space reclamation + fresh admission (no regen)

1. Checkpoint identity: registry/runner SHAs, 9,449-name audit vs
   `after/generated.json`, suite re-run, binding-test absent mode (relink
   only if inputs changed). Table every check.
2. Per stale tree (oldest first): ownership proof (board + lsof/fuser),
   removal, before/after `df`. Stop at admission + 512 MiB headroom.
3. Fresh admission sample: record both `df` results, full budget math.
   If STILL short: STOP with outcome (iii) + the exact next reclamation
   target — no csv mutation, no regen.

## Task 2 — query entry regen + install + actual-binding truth table (no boot)

1. Revalidate ELF `0x2c5140..0x2c5168`; insert the row (expect `c17db90b…`).
   Recheck admission immediately before spawning; generate; install changed
   bytes only (AppleDouble companion purge as in E10, proven-created only).
2. Rebuild; relink `e10_binding_test.cpp` against the NEW runner objects;
   run query truth table (all 4 cases) + DROP handler regression + full
   suite. Table every receipt.

## Task 3 — one guarded verification boot

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Capture the E10 joins
   (dynamic a0, constructor/UI PCs, query return, state-3 path, UI pending,
   Present join) or the first remaining wall. Table the joins; no promotion
   without an actual request/completion dependency.

## Report

`local/research/E11/REPORT.md`: contract, checkpoint re-verify, reclamation
ledger, admission record, regen + binding tables, boot joins (or wall),
exact commands, gaps. Commit evidence (`[E11]`), stop.
