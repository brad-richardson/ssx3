# E7 — Value series: S address + slot values + G-sequence + M-confirm (frontier execution)

You are the frontier agent (Codex Astra) in a herdr panel in
`/Users/bradrichardson/dev/ssx3`, E-lane lead as of this brief. Finish your
in-flight EF prep commit first (if not yet committed), then execute below.
**Tables + hypothesis + next-action recommendation, no verdicts.** Read
first: `local/research/E6/REPORT.md` (all of it: outcome (i) via J2a-fixed
— M=`S+0x59e8`=1 boot-forced (M1 fall-through proof, no static clearer)
⇒ every clear-gate display call fixed-picks `S+0x5a88`:=PAIR-B=112;
parity path dead while M=1; gate G immaterial under M=1; S =
`PS2GraphicsMan` heap object, pointer global `0x4a289c`; sole S-writer W1
`0x382b68`; H-pair prediction `S+0x5a78`=`S+0x5a84`=fbp0-block; §4 E7a+E7b
recipe; §8 residuals) + E5 `e5-watch-series.txt` shape (burst-count join
key).

## Facts you start from

- E6 closed MECHANISM statically but VALUES are dynamics: S's numeric
  address + `0x5a78/0x5a7c/0x5a84` slot values + G-sequence at display
  calls + M-confirm + fp-user rate. This brief takes them with ZERO new
  code (existing P1f watch + E4 taps at fork `a13b66a`).
- E7a (ONE boot ≤600 s, E5 caps/shape): `PS2X_DIAG_WATCH=0x4a289c` (+ E5
  display set for the join) → the factory store's `value=` field IS S's
  runtime address (heap determinism ⇒ stable across boots; verify by
  teardown-null absence + display series re-match).
- E7b (ONE boot, addrs computed as S+off):
  `PS2X_DIAG_WATCH=S+0x5a78,S+0x5a7c,S+0x5a84,S+0x5a88,S+0x5a74,S+0x0f44,
  S+0x59e8` at 600→601 (+ boot window free, as in E5) → slot values
  (H-pair test), G-sequence at display calls (fp-user-rate inference:
  skip-vs-pick per burst), M-confirm (=1?), counter rate. Miner: join vs
  `e5-watch-series.txt` by burst count.
- Then answer the PARKING question the values unlock: what SHOULD clear M
  / re-fire the blit (guest state? emulator gap?). Outcomes (tabled, not
  verdicts): (i) values confirm J2a-fixed + parking named → the ONE next
  action (fix brief ONLY if the join is a named emulator gap — a guest-side
  gate is a finding with parking evidence); (ii) values surprise
  (M≠1 / slots≠112 / H-pair fails) → follow the residual (E6 §8 G5
  pointer-chase scan recipe for M3/G4 reachability) + revised next action;
  (iii) observation truncated/unalignable → repair that measurement gap,
  nothing else. No SIF/CD promotion unless that dependency names it; no
  raster/Present fix follows (faithful, two boundaries).
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`.
  Plain commits, pushed to the FORK remote only, never upstream. Generated
  runner sources never staged. ONE P-lane mutator: coordinate via the lease,
  never parallel boots. NO regen (still gated on the 0x426230 DROP
  disposition — observation taps only).

## Gates and rules

- Capture boots ONLY while holding `/tmp/ssx3-p-lane-lease` (SHARED P-lane)
  — poll every 5 min + log waits to `$W/P1/run/e7-waits.log` if taken. Max
  2 boots (E7a + E7b) ≤600 s each (Part 4 budget); wall + progress + BYTE
  caps recorded per boot, abort past any cap. Builds `-j4` any time. No
  `adb`.
- `export COPYFILE_DISABLE=1` on every SSD step. Retain: the captures +
  receipts that prove the finding (compress closed raws; single canonical
  copies).
- Evidence dir `local/research/E7/` (STANDALONE). Commit with `git add -f`,
  prefix `[E7]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (orchestrator pushes at poll when the tree is clean). Fork commits pushed
  to fork remote (table SHAs; none expected — zero-code plan).
- Experiment contract up front (hypothesis/observable/alternatives/stop +
  which outcome each selects). Time box: 6 h from this handover.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — E7a: locate S (ONE boot)

1. Pre-claim checks (T13 §T13-0 verbatim + E5 binary shas): lease absent,
   `pgrep` exit 1, ISO/ELF sizes, free space (SSD + internal `df`), selftest
   green, waits tails, THREE caps recorded. Suite re-green before any boot.
2. Claim → foreground capture → at first binding cap: SIGTERM → release AT
   ONCE → analysis (lease-free). S = factory-store `value=` @ `0x4a289c`.
3. Stability table: teardown-null absence + display series re-match vs E5
   (burst count + values).

## Task 2 — E7b: value series (ONE boot)

1. Same pre-claim shape. Watch S+off set at 600→601. Release at once.
2. Value tables: slots (H-pair verdict), G-sequence per burst, M-confirm,
   counter rate; join vs E5 by burst count.
3. Finding table (exactly one of i–iii) + parking answer + the prescribed
   next action. If (iii): the measurement repair, nothing else.

## Report

`local/research/E7/REPORT.md`: contract, E7a table, E7b tables, finding +
parking answer, exact commands, gaps. Commit fork first if touched (pushed
fork remote), then evidence (`[E7]`), remove the lease if held, stop.
