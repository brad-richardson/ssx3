# E54C — MMI halfword interleave semantics

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `local/research/E54B/REPORT.md`, the E54 queue in
`docs/todo.md`, and the current macro/test path. Hand back a table; the
orchestrator decides the gate. This is **one** instruction-family fix,
not all remaining E54 semantics.

Pin fork remote `ssx3` at `aa20d4a` (verify). Create new worktree
`~/dev/ssx3-work/E54C/PS2Recomp`, branch `e54-mmi-interleave`, with
build/run outputs only under `~/dev/ssx3-work/E54C/`. Use canonical E56
codegen at `~/dev/ssx3-work/codegen-ssx3` without changing it. The
source change is limited to `ps2xRuntime/include/ps2_runtime_macros.h`,
focused unit tests, and minimal test registration/CMake if needed. No
other fork, G worktree, input or prior receipt edits.

Correct behavior (orchestrator checked PCSX2 `pcsx2/MMI.cpp:1121-1137,
1498-1514` at `9056c0834`): with macro arguments `a=Rs`, `b=Rt`,
`PINTEH` destination halfword lanes 0–7 are
`Rt0,Rs0,Rt2,Rs2,Rt4,Rs4,Rt6,Rs6`; `PINTH` lanes are
`Rt0,Rs4,Rt1,Rs5,Rt2,Rs6,Rt3,Rs7`. Current fork macros at
`ps2_runtime_macros.h:859-860` use unpacklo/unpackhi after an identity
dword shuffle, which predicts different lanes. The canonical generated
code contains the four guest `PINTEH` sites at `0x3cb8b0–0x3cb8bc`
in duplicated `sub_003CB538`/`sub_003CB540` bodies; no `PINTH` site
was found by the current grep. Do not equate static presence with boot
reach.

## Gates

1. Confirm the PCSX2 lane oracle from the pinned source (or mark remote
   access unavailable and use the table above), macro operand order,
   and generated guest sites. Add distinctive Rs/Rt lane-vector tests
   for both macros, including Rs=0 and alias operands; show expected
   and observed lanes before the fix. Keep tests host-portable.
2. Change only the two macros to match the oracle. One Mac Release build
   with taps OFF and one with taps ON using the canonical E56 codegen;
   run `ps2x_tests` from the fork root for each and report counts. No
   guest regeneration or generated source commit.
3. One mini-lease I26-FAST boot with dev-only `PS2X_SKIP_MOVIE=1`,
   target at least race HUD tick 2050, wall cap 500 s, bounded logs and
   own PID. View Select Character and two race frames. This is a
   functional regression gate, not a claim of MMI instruction reach or
   clean speed. Do not compare free-running fixed-tick frame hashes
   before E55.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty; commit `[E54C]` to the local fork branch with trailer
   `Orchestrated-By: Codex`. No fork or ssx3 push.

Budgets: two configure/build attempts per tap mode (one correction),
one boot, ≤600 s absolute boot, ≤8 GiB new E54C bytes and 200 GB global
ssx3 cap. Verify pinned ISO/ELF, codegen register file and runner with
two matching SHA reads. Claim/release one mini slot; cap logs at 64 MiB.
If the suite or route fails after one clear repair, stop and report; no
tuning loop. Deliver `local/research/E54C/REPORT.md` with pins, lane
vectors, source diff, unit and suite results, boot/frame table, SHAs,
runner-dir check, budgets and gaps. Commit `[E54C]` report with the same
trailer; no push. The orchestrator will gate and fold it.
