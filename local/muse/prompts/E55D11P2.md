# E55D11 Part 2 — one Save/Load detour Mac boot (OpenCode Go)

You are the E-lane run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/E55D11/{REPORT.md,ORCH-GATE-P1.md,e55d11_boot.py,check.py}` and E55D10 Part 2 gate. The orchestrator releases **one** Mac boot with script SHA `f5434273f8384932502f47fc98f80ce5f5c313e3077c41ee8167c520535c476b` only. Verify it first; mismatch stops. This pane has a per-worker `OPENCODE_CONFIG_CONTENT` exception only for `~/dev/ssx3-work/E55D11/**`; verify with a tiny new sentinel and remove it. Own only `local/research/E55D11/` and private `~/dev/ssx3-work/E55D11/`. No fork/source edit, build, Odin/iOS action, seeded-card change, push, board/global config edit, upstream contact or second boot. Wait if any heavy clang/ninja job is active before claiming the mini lane; do not run against N's build.

Run exactly `python3 local/research/E55D11/e55d11_boot.py --label S1` **once**. Its precheck must pass: E55D3 fork `bab6eb3`, runner-dir guard empty, two matching SHA reads of runner/ISO/ELF/codegen, fresh empty cards. The script claims one mini slot, boots from its own cwd, enforces ≤500 s wall/≤120 s no-progress/≤16 MiB boot log/≤2 GiB frames, stops or kills only its recorded PID and releases the slot. Do not bypass any failed precheck, cap, permission or lease; report OTHER. Do not edit the approved script. Keep raw stdout/boot log and probe in private scratch; gzip closed log and probe only after the runner exits. No speed claim.

Evidence: retain result JSON, pins/route/stop/lease receipts, full-resolution PNG nearest the pre-Cross frame target tick1290 showing the highlighted row, post-Cross target tick1460 showing the resulting screen, plus any unexpected screen and adjacent `.txt` metadata. If no pre-Cross full frame exists, classify OTHER even if a later frame is readable. Snapshot filename tick is approximate; record actual metadata tick and SHA. Curated PNG total ≤2 MiB, text ≤512 KiB. View frames yourself; report exact visible title, selected item, result message and footer. The orchestrator makes the visual verdict. Verify private `[padscript]` lines show one Start, one Square, four separate Down and one Cross press/release, no other input. Inspect `probe.log` by vsync tick: count `getdir` and `mcread` calls before/after Cross, with `ok`/reason and lengths, and distinguish an `ok=0 reason=empty` call from copied guest bytes. Verify cards unchanged or report changes. Do not paste entire probe payloads into git.

Predeclared A = readable Save/Load result screen **plus** copied GetDir table or successful mcRead; B = readable Save/Load result screen without those bytes, or a different readable screen (state exact text); OTHER = ambiguous/missing frame or route/pin/lease/cap mismatch. A `getdir ok=0` still proves API reach even if outcome is B. Visual screen alone does not prove API reach. Add `check-p2.py` for pins, frame/probe SHAs, pad rows, caps and card manifests; it cannot prove visual labels. Append REPORT with exact commands, one-run counts, gaps and next action. **Stage and commit only named `local/research/E55D11/` files and this brief by explicit path; never pre-staged docs or another lane's gate.** Check `git log -1` and `git status` before committing `[E55D11] Part 2` with `Orchestrated-By: opencode`, no push. Hand back the frames/table; orchestrator gates.
