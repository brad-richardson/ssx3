# G9 — Geometry-alignment probe on the G8 dump (no new capture)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts**. Read first: `docs/reports/G8.md` (§3c + §4 — this brief
executes its ONE next action) and `docs/reports/G7.md` §3 (the retired
proxy — do not resurrect it). G8 proved draws execute (544 prims, icon
rendered) but comparability is blocked on geometry: replayer scanouts are
512 wide, PCSX2's aligned screenshot is 597 wide, and no same-geometry pair
exists. This brief resolves the width gap on the EXISTING dump.

## Facts you start from

- Experiment contract (required): hypothesis = the 85 px width gap is
  explained by display-state geometry derivable from the dump itself, and a
  replayer CRTC flag yields a 597-wide scanout; observable = derived
  expected width, per-flag scanout widths, FIRST-vs-REF diff at exact
  geometry if a pair is found; alternatives = (a) gap unexplained by
  PrivRegs → table + recipe, stop; (b) no flag yields 597 → table all
  tried widths + stop; (c) matched pair found but FIRST still black →
  table, queue the per-vsync series (NOT this brief). Stop: this dump
  only — NO new capture, no tuning loop, no per-vsync series here.
- Task 1 (derive): from the dump's own PrivRegisters (DISPLAY/DISPFB/
  SMODE2) + GSDump header, derive the expected scanout width; compare
  against replayer internal/mode width (512) and PCSX2's screenshot-crop
  width (597). Table each value + source bytes. Reuse the G8 clone/build
  (verify `3a66c197` HEAD first; rebuild `-j2` only if the tree moved).
- Task 2 (flag sweep, bounded): test each replayer CRTC-affecting flag
  (`--high-res-scanout`, `--conservative-crtc`, plus any width-affecting
  flag the `--help`/source names — enumerate first, table the list) for a
  597-wide scanout. If a matched-geometry pair is found, re-diff FIRST-vs-
  REF at exact geometry (G8 `g8-diff.py` shape, same methods).
- Per-vsync scanout series and any rich post-loading dump are explicitly
  OUT of scope (queued after this probe).

## Gates and rules

- No contention with the P-lane critical path: no recomp boots/builds, no
  P-lane lease, no `adb` installs. Byte caps declared up front, tracked in
  ALLOCATED bytes / free-space delta; SSD reuse only, nothing new on `/`.
- Reuse G8 tooling (census/diff scripts, dump dir); do not duplicate harness
  work. No third-party code copied into any project tree; license receipts
  for anything newly touched (expected: none).
- Commit in ps2xGS as you go (prefix `[G9]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G9/` (`git add -f`,
  `[G9]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 4 h. Tables + hypothesis + next-action recommendation.

## Task 1 — derive the expected width (no runs)

1. Parse DISPLAY/DISPFB/SMODE2 (+ screenshot-crop logic) from the G8 dump
   bytes and the PCSX2 screenshot path; derive expected scanout width.
2. Table: derived width vs replayer 512 vs PCSX2 597, with the source bytes
   + the explanation (or OPEN with the exact gap).

## Task 2 — flag sweep + conditional re-diff (Mac)

1. Enumerate CRTC/width-affecting replayer flags (table first), then run
   each (bounded: one replay per flag, same dump, defaults otherwise).
2. Width table per flag. If 597 appears: FIRST-vs-REF exact-geometry diff
   + hypothesis verdict + the ONE next action. If not: verdict + recipe.

## Report

Mirrored `REPORT.md`: contract, width-derivation table, flag table,
conditional diff, license receipts, exact commands, gaps. Commit both repos
as above, stop.

## Frontier redirect (same poll — supersedes the flag sweep, also sent to the worker)

- STOP 597 hunting: the aspect explanation is ACCEPTED (512 internal both
  sides; 597 = screenshot-path aspect). No further CRTC-flag width hunts.
- Accounting correction: consume_flush_stats runs only on the last pass,
  baseline only pre-cold, no reset on restart — G8's 544/first=306 span BOTH
  passes (first=272 cold + 34 warm). Viability holds; no recapture for this.
- Replacement task: replay the EXISTING G8 dump through PCSX2 + paraLLEl at
  the SAME packet/vsync boundary + native geometry, bounded 8-scanout series,
  PER-PASS stats reset. Check pinned reference dump-replay capability first;
  if recapture unavoidable, combine uncorrected size + same-boundary
  snapshots in ONE run. (ScreenshotSize=2 alone still misaligns.)
