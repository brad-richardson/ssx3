# M19 — Endpoint-direction split: which side is mid on (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M18/REPORT.md` first (all of it —
M18 gap 1 is this brief: the joint hypothesis its tables point at
but do not contain), plus the M16 carrier table for per-draw
context. Fully offline — SSD dumps read-only, no harness code, no
replays, no lease. No device work.

## Facts you start from

- M18's marginals on m16 s0 (R_0 = 22815 B) + m15 (R_0 = 13418 B):
  77.8% of residual bytes have mid EXACTLY at an endpoint
  (mid==v0 41.3% / mid==full 36.6% on s0); 11.4% are
  weight-unmatchable (mid outside [v0,full]); 91.7%/86.5% of
  residual Y px read synth<truth; P4: 96.9%/94.9% of residual Y
  px inside the moved mask (463/439 static-site px).
- The joint question M18 could not close: is mid the BRIGHTER or
  the DARKER endpoint on the 17751 endpoint bytes? Which side do
  the 2589 outside-interval bytes fall on (above max or below
  min)? How many RAW residual bytes sit on static sites (v0==full
  — P4's 463/439 are Y-px; the byte count is gap 1's ask)?
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m18/m18.txt` (receipt: shas, baselines,
  curves — re-verify input shas, don't trust them). Work dir:
  new `/Volumes/Extreme SSD/m19/`; evidence `local/research/M19/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`, `m16/`, `m17/`, `m18/` and every other
  agent's dirs. All outputs to `/Volumes/Extreme SSD/m19/` +
  `local/research/M19/`.
- Evidence dir `local/research/M19/` (STANDALONE). Commit with
  `git add -f`, prefix `[M19]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18 precedent).

## Task 1 — direction split (the joint test)

Over s0 + m15 (+ the 764-shape coarse pass where cheap), table
per residual byte with v0≠full:

1. mid==max(v0,full) vs mid==min(v0,full) counts (the bright/dark
   endpoint split), split further by mid==v0 vs mid==full
   (4 cells: v0-is-max-and-mid / v0-is-min-and-mid / full-…).
2. Outside-interval bytes: above-max vs below-min counts +
   distance distribution (how far outside?).
3. Static-site byte count: residual bytes with v0==full (raw
   bytes, both frames; reconcile with P4's Y-px 463/439).
4. Cross with sign: synth<truth rate inside each direction cell
   (does the −1 bias live in one cell?).

## Task 2 — carrier/band cut (where the direction lives)

Cut the Task-1 cells by band (top/mid/bottom thirds) and by M16
carrier rank (effect draws 694/693/701 vs HUD draws — M18 Test C
showed they behave differently). One table: direction-cell shares
per band + per top-10 carrier. If a cell concentrates in one
band/carrier, table it; do not explain it.

## Task 3 — controls + determinism (M18 precedent)

1. Positive control: synthetic truth with a KNOWN direction split
   (e.g. mid always the darker endpoint) — the estimator must
   recover it exactly.
2. Determinism: re-run the split on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17/M18).

## Report

`local/research/M19/REPORT.md`: DESIGN.md (bars first) + split
tables + carrier/band cut + controls + explained-vs-standing
update (does direction shrink the 22815 B? by what rule?) + gap
rows. Screenshot only if a split map discriminates (budget-noted).
Commit evidence (`[M19]`, no push), stop.
