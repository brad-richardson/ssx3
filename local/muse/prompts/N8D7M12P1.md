# N8D7M12 Part 1 — shared GS replay core, desktop parity (OpenCode Go)

You are the N-lane implementation worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M11 `REPORT.md`/`ORCH-GATE.md`, N8D7M6 same-stream `REPORT.md`/`comparison.json`, and N8D7M5 build receipt. Use LSP for code links when available and direct reads otherwise; cite the repo, not the web. The orchestrator created isolated `~/dev/ssx3-work/N8D7M12/PS2Recomp` branch `n8d7m12-replay-core` at `a8cfefa`; own that worktree, `~/dev/ssx3-work/N8D7M12/` build scratch, this brief and `local/research/N8D7M12/`. Do not touch N8D7L, E forks, paraLLEl source, global config or board docs. No Android code/build, device action, stream copy, push, upstream contact or game-generated source. Verify the per-pane write exception with a tiny sentinel and remove it; stop on denial. Budget: ≤1 desktop build, ≤1 full suite, ≤1 exact-stream Mac replay, ≤5 GB scratch (check disk_budget.sh before/after), ≤16 MiB closed logs, committed text <512 KiB. Wait for no clang/ninja before build; no mini boot lease for build, claim one mini P-lane slot for replay and release in finally.

Goal: implement only N8D7M11 §3's shared replay core so the desktop test and a future Android app branch can call the same parser without MiniTest in the runtime. Move record decoding/dispatch, ordered path+priv+transfer+marker processing, Present descriptor, tick2050 selected capture, 4 MiB snapshot and 448-tile census unchanged. Keep desktop-only N8D7M5 word-watch diagnostics in the test wrapper (or preserve them behind a desktop-only adapter); **do not silently drop existing behavior**. The existing MiniTest registration stays, and unset `PS2X_GS_REPLAY_CAPTURE` remains a skip. Expose a narrow result/error API from the new core; no change to normal runtime boot. Runner-dir diff vs upstream `14b1e5cb` must remain empty. One candidate implementation, one validation pass; stop and hand back failures rather than tuning.

Validation: configure isolated Ninja Release build under N8D7M12 scratch using the N8D7M5 cache values (`PS2X_BUILD_TEST=ON`, `PS2X_GS_SHADOW_PARALLEL=ON`, `PS2X_PARALLEL_GS_SOURCE_DIR=~/dev/ssx3-work/N8D7F/parallel-gs`, `PS2X_GAME_CODEGEN_DIR=~/dev/ssx3-work/codegen-ssx3`, compile_commands ON); build `ps2x_tests`, run full suite from fork cwd, then one parallel replay of the exact `~/dev/ssx3-work/N8D7M6/n8d7m6.gs` SHA `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593` (two reads before use). Compare parser counts, marker2050, selected 11-field descriptor, input byte hash, 448-tile vector and named present hashes with N8D7M6's Mac receipt, using a bounded `check.py`. Do not quote diagnostic wall time as speed. If suite or parity fails, report B with exact diff; if build/setup fails, OTHER. A = suite passes and exact-stream output is byte/field-equal to the pinned Mac baseline with the new shared core. The acceptance script checks actual receipts, not source-string presence alone.

Deliver `local/research/N8D7M12/REPORT.md`, `check.py`, concise result JSON/text, source diff receipt, exact commands/SHAs, and a fork `[N8D7M12]` commit (only named source files; `Orchestrated-By: opencode`, no push). Commit `[N8D7M12] Part 1` in ssx3 with this brief and named evidence only (same trailer, no push); stage by explicit paths, never board docs/other lanes. Hand back table and gaps. The orchestrator gates Part 2 Android branch/build separately.
