# T15 — Run past the exit: what follows the 31-drain (1 long boot + live monitor)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T13/REPORT.md` first (all of it —
the phase EXITED at block 235 (~1,218 s wall): stub collapse →
29-halt → 31-drain → main DORMANT → dma/gif freeze; N = 72,176;
T13's boot SIGTERMed 30 s after trigger so NOTHING past block
241 was ever observed), plus P1 REPORT Part 32 if it has
landed (P1ai's next-park spec — your signature watchlist; if
not landed, use §T13-1's exit table + the generic watches
below). This run extends T13's ladder past the exit. Peer lanes
run concurrently — P1ai diagnoses the old logs (no lease), you
hold the P-lane lease for ONE boot.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight). Binary must
  reproduce `81bee6c5…` / 163,460,272 B (T11/T13 Release) or
  table the deviation exactly (rebuild if the tree moved).
- Fixed baselines the run must reproduce before the exit (or
  table the deviation exactly): exit at block 235 (stub
  222→218 @ ~logline 2453403 scale); N = 72,176 enters/exits;
  sema-30 4w/3s parked; single caller; 54-frame modal cycle
  in-phase; CD/SIF/GS/RPC silent past block 2.
- Exit = any post-block-241 event: main wake from dormant
  (status ≠ 5 / pc ≠ 0), new stub phase after residue-13,
  first new guest event (drop/RPC/CD/SIF/GS), new caller,
  dma/gif unfreeze (nonzero delta after freeze), 31-drain
  end (pump fully idle), second halt. Cap = 2400 s (exit +
  ~20 min of post-exit window — still a complete result if
  the drain/freeze never breaks).
- T13's monitor led by 29 s with full coverage — repeat that
  (background the monitor BEFORE the foreground boot).

## Gates and rules

- Lease `T15`: the run-to-next-exit ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/t15-waits.log`. Max 1 boot ≤2400 s foreground.
  Builds any time (`-j4`). No `adb`.
- ZERO fork changes (no source edits, no commits, no push).
  Build from a recorded tree state (`git log`, `git status`,
  binary sha in the report).
- Evidence dir `local/research/T15/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T15]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — run past the exit (or cap)

1. Derive scripts from T13's verbatim (`/tmp/t13-boot1.py`,
   `/tmp/t13-monitor.py` — LOG names + cap + signature rows
   only, diff-verified): monitor polls the T13 exit
   invariant (block-235 stub collapse reproduced?) PLUS
   the 7 post-exit watches above (P1ai §P32-6 rows if
   landed, else these).
2. Pre-claim checks (T13 §T13-0 verbatim): lease absent,
   `pgrep -x ps2EntryRunner` exit 1, binary sha, ISO + ELF
   sizes, free space, waits-log tails.
3. Claim → monitor first (verify alive + log header) →
   foreground boot → on trigger: 30 s grace → SIGTERM →
   release AT ONCE → trace copy (lease-free) → analysis.
   If the exit invariant FAILS (no block-235 collapse),
   table the deviation exactly and still run to cap.

## Task 2 — post-exit tables (whatever the run shows)

1. Exit-invariant table: block-235 collapse reproduced?
   (stub series @ b235, N count, sema-30 shape — match/no
   vs T13's numbers).
2. Next-event table: first post-241 event (block, wall,
   logline, which watch fired — or NONE (drain/freeze
   holds to cap: second absence window with a bound)).
3. Ladder-extension table: T13's §T13-5 rungs + this run's
   rung (lines/bytes, blocks, handshakes, N/N2, wall-rate
   shape past the exit).

## Report

`local/research/T15/REPORT.md`: T13-0..T13-7 SHAPE (lease
record, exit/invariant tables, N/N2, post-exit state,
baselines-kept rows, ladder, exact commands, gaps). Commit
evidence (`[T15]`, no push), remove the lease if held, stop.
