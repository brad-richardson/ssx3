# E8 — Guest progression from the copy join toward the menu (frontier execution)

You are the frontier agent (Codex Astra) in a herdr panel in
`/Users/bradrichardson/dev/ssx3`, continuing as E-lane lead. **Tables +
hypothesis + next-action recommendation, no verdicts.** Read first:
`local/research/E7/REPORT.md` (all of it: H-FIFO confirmed + generic VIF1
FIFO ingress fix at fork `4acc59f` (pushed), suite 452/452, five-of-five
boundary copies consumed, D holds the memory-card-check message, Present
faithful with field processing; §7 parking answers; E7 stops at the join).
This brief starts where E7 stopped: the baseline is fork `4acc59f`.

## Facts you start from

- The goal line: advance guest execution from the copy join as far as
  receipts allow — toward the first menu/interactive frame. The guest sits
  at a memory-card-check message; that screen may imply a memory-card /
  storage / input dependency — TABLE it if receipts say so, do NOT promote
  SIF/CD work unless that dependency names it (E7 §7 precedent).
- Method (E7 discipline, larger scope): run the fixed runtime forward from
  the join with the existing taps (`ps2_e7.h` at `4acc59f` — extend, do not
  rebuild); table each successive wall or advance (guest PC progress,
  scheduler/EE/IOP signals, GS/Present state, VRAM/framebuffer deltas with
  the E7 join-proof shape: content joined to packet+source, not inferred
  from a changed image). Fix ONLY blocking emulator gaps, each with the
  E7 receipt shape (forcing receipt before the fix, generic implementation,
  fail-before/pass-after regressions, rebuild, re-probe). One gap per fix;
  never stack fixes.
- Outcomes (tabled, not verdicts): (i) guest reaches menu/interactive
  content → the join-proof table + the ONE next action; (ii) a wall needs a
  receipt this box cannot take (guest-side gate, storage/input dependency,
  coverage hole needing regen-class work) → exact probe/recipe brief, stop;
  (iii) observation truncated/unalignable → repair that measurement gap,
  nothing else. No SIF/CD promotion, no raster/Present rework, no regen
  (0x426230 DROP gate still binds — runtime/hand-written code only).
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`.
  Plain commits, pushed to the FORK remote only, never upstream. Generated
  runner sources never staged. ONE P-lane mutator: coordinate via the lease,
  never parallel boots.

## Gates and rules

- Capture/progression boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (SHARED P-lane) — poll every 5 min + log waits to `$W/P1/run/e8-waits.log`
  if taken. Max 4 boots ≤600 s each; wall + progress + BYTE caps recorded
  per boot, abort past any cap. Builds `-j4` any time. No `adb`.
- `export COPYFILE_DISABLE=1` on every SSD step. Retain: the captures +
  receipts that prove each advance/wall (compress closed raws; single
  canonical copies).
- Evidence dir `local/research/E8/` (STANDALONE). Commit with `git add -f`,
  prefix `[E8]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (orchestrator pushes at poll when the tree is clean). Fork commits pushed
  to fork remote (table SHAs).
- Experiment contract up front (hypothesis/observable/alternatives/stop +
  which outcome each selects). Time box: 6 h from this handover.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — progression survey (ONE boot: where does the guest go?)

1. Pre-claim checks (T13 §T13-0 verbatim + E7c binary shas): lease absent,
   `pgrep` exit 1, ISO/ELF sizes, free space (SSD + internal `df`), selftest
   green, waits tails, THREE caps recorded. Suite re-green before any boot.
2. Claim → foreground run → at first binding cap: SIGTERM → release AT
   ONCE → analysis (lease-free). Table the progression arc (PC/scheduler/
   GS/Present/VRAM per phase) + the FIRST wall past the join (or the menu,
   if reached) with forcing receipts.

## Task 2 — advance or wall (up to 3 more boots)

1. Same pre-claim shape per boot. For each wall: emulator gap → fix with
   the E7 receipt shape (forcing receipt, generic fix, regressions,
   re-probe); guest-side gate → park it with receipts, do not route around.
2. Finding table (exactly one of i–iii) with the receipts that force it +
   the prescribed next action. If (iii): the measurement repair, nothing else.

## Report

`local/research/E8/REPORT.md`: contract, progression arc, per-wall tables,
finding, exact commands, gaps. Commit fork first if touched (pushed fork
remote), then evidence (`[E8]`), remove the lease if held, stop.
