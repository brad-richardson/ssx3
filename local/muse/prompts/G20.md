# G20 — Capture-first O4 minimize: logging-only instrumented build names the crashing pipeline (hash + SPIR-V), assemble the Adreno filing package (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G19/REPORT.md` (ssx3 mirror) first (all
of it: sha erratum RESOLVED — as-found was all-zero post-run damage,
rebuild reproduces `a62d8a2b…` + build-id `0b01b274…` bit-for-bit; knob run
exit 139 with O4 PERSISTING on main's sync compile from
`dispatch_texture_analysis`, async worker provably absent — sync-path
Adreno compiler bug on a specific compute pipeline; minimizing facts §3c;
the ONE next action = this brief). This brief executes G19's §4. No
renderer behavior changes (logging-only instrumentation — see the gate).

## Facts you start from

- Pins (re-verify, do not move except by the logging patch): paraLLEl-GS
  `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims + G11/G8+G10
  hunks + G18 fix hunk (all uncommitted in the SSD clone — verify hunk
  bytes before building); rich dump sha `154d9d85…`; knob at
  `gs_renderer.cpp:459-464` (verified firing in G19).
- Logging-only gate (.strictly enforced): the instrumentation records the
  crashing compute pipeline's identity (Granite pipeline hash + SPIR-V
  bytes/hash) at the `dispatch_texture_analysis` dispatch WITHOUT changing
  any compiler input — log lines + a memory/disk capture of the ALREADY-
  COMPUTED pipeline key + shader blob, no pipeline-creation changes, no
  flag changes, no skip/defer. ONE bounded knob-env run (`--iterations 2`,
  `PGS_SKIP_COMPILATION_TASKS=1`) to capture it; the run is expected to
  crash O4 again (that's the capture condition — the identity is logged
  BEFORE the crashing `vkCreateComputePipelines`). ONE retry allowed ONLY
  if O1/O3 fires instead of first draw.
- Device: Odin3 (`622c49b1`, Android 15). Transient dir
  `/data/local/tmp/g20/` ONLY; `mg/` never touched; remove the dir at end.
- The task, in strict order: (1) write the logging-only capture patch
  (ONE hunk, tabled; verify it cannot alter compiler inputs — reads +
  logging only); (2) build it (`-j2`, NEW SSD build dir — G14/G18 dirs
  untouched); (3) ONE bounded knob-env capture run on-device; pull the
  pipeline hash + SPIR-V + logcat + tombstone; (4) assemble the COMPLETE
  Adreno filing package (device/driver/build-ids + repro + §3c fault shape
  + shader — tabled in the report, NOT filed: no upstream contact in this
  brief).
- Success bars (tabled, not verdicts): pins + hunk verified; capture patch
  tabled + logging-only proven (diff review: no input mutation); build
  exit 0 (binary path/size/sha); ONE bounded capture run (exit, wall,
  fate, O4 re-triage); crashing pipeline hash + SPIR-V captured (or the
  exact obstacle tabled per the stop rule); filing package assembled;
  hypothesis verdict + the ONE next action.

## Gates and rules

- Stop rule (G19 shape): TWO bounded device runs max (capture run + one
  retry iff O1/O3 fires instead of first draw) — no tuning loop, no
  behavior changes, no new dumps, no workaround in this brief (defer/skip
  stays premature until the shader is named). No lldb unless the run needs
  triage the tombstone cannot answer (table the decision).
- If the capture patch cannot be proven logging-only on static review:
  STOP with the exact doubt + recipe and NO build (a capture that perturbs
  the crash proves nothing).
- Byte caps: declare up front for the new build dir (SSD) + retrieval
  (SSD `ps2x-g20/`); all prior `ps2x-g*` + build dirs read-only (0 growth);
  internal `/` delta ≤1 GB, no clones/builds on internal;
  `export COPYFILE_DISABLE=1` on SSD steps. No P-lane lease (no recomp
  boots/builds, no fork writes). No bytesize/WSL. Host builds `-j2`.
- Clone discipline: the capture patch lives in the SSD clone, UNCOMMITTED
  (same standing as the G14 shims + G18 hunk), precisely tabled; no other
  clone edits. No third-party code copied into any project tree.
- Evidence: ps2xGS commit `[G20]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G20/`
  (`git add -f`, prefix `[G20]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — capture patch + build (no device yet)

1. Re-verify pins (clone HEAD/status, all hunk bytes, dump sha, knob
   file/lines). Design the capture (where the pipeline hash + SPIR-V are
   already computed at the `dispatch_texture_analysis` dispatch; what gets
   logged/written; why inputs can't change).
2. Write the ONE logging-only hunk (table the diff + the no-mutation proof).
   Build in a NEW SSD build dir (`-j2`); table binary path/size/sha.

## Task 2 — ONE bounded capture run + filing package (Odin)

1. Push binary + dump (sha match on-device), run bounded with the knob
   (`--iterations 2`, wall cap), capture exit/wall/fate + pipeline
   identity + logcat + tombstone (if any). Pull receipts; remove device
   dir.
2. Assemble the filing package (device/driver/build-ids/repro/fault/
   shader). Score with `g14-diff.py` (or N/A). Hypothesis verdict + the
   ONE next action (file vs workaround brief input).

## Report

`local/research/G20/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, patch diff + proof, build record, capture table,
filing package, score, verdict, exact commands, gaps. Commit evidence
(`[G20]` both trees), stop.
