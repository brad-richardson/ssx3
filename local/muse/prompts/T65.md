# T65 — PCSX2: race-time transform inputs (the healthy side of E50)

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). Follow `~/dev/AGENTS.md`. **Tables + receipts; the
orchestrator decides.** Read first:
- `AGENTS.md`;
- `local/AGENTS.local.md`;
- `local/research/T48/REPORT.md` (§T48-6: Capture B, the route into the
  Happiness Rival Challenge race, and its gates);
- `local/research/E47/REPORT.md` (Part 2);
- `local/muse/prompts/E50.md` (use the same table formats).

## Why

In the recomp, race 3D geometry lands off-screen: the PATH1 xy box is
pinned to 1023.5–3071.5 every vsync, while the viewport is 1792–2304 ×
1824–2272. VU1 and the GS state look healthy. The suspect is the
transform input (the view/projection or object matrices in VU1 data
memory). We need PCSX2's values at the same moment.

## Change (log-only; on the T64 tree at `/home/brad/pcsx2-g7/pcsx2`,
stack on it; EE + VU interpreters)

1. Per vsync over a race window: the PATH1 xy/z bounding box, plus counts
   of draws on-screen, off-screen, and straddling (XYOFFSET-relative,
   same definitions as E50).
2. At the start of the first VU1 world program in one race vsync (take
   the startPCs from E47's MPG/VU1 tables, or T48's VU1 census): dump
   VF00–31 and the whole VU1 data memory. Decode every 4×4 float block
   that looks like a matrix (hex + decimal, and flag NaN/Inf/denormals).
3. The same dump at Select Character (the control), to align block
   offsets with E50.

## Run

T48 Capture B's route to the race (cold rec boot, then interp for the
capture if needed; a statefile at the race is fine for (1)–(2)). G13
preservation as usual. ≤2 builds, ≤3 captures, 4 h, cap 3 GB.

## Deliverable

`local/research/T65/REPORT.md` with:
- patch;
- preservation;
- race bbox and on/off counts;
- the matrix tables (race and SC);
- the VU1 program startPC list for the race.

No verdict. `[T65]` commit (`git add -f local/research/T65
local/muse/prompts/T65.md`, trailer `Orchestrated-By: Claude Code`,
`git log -1` first), no push.
