# M36 — Still-below unnamed cells: per-site value attribution for 2/3/700 (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M35/REPORT.md` first (all of it —
M35 gap 4 is this brief: 2/3/700 move unnamed at k 6/7/8
(J 0.9159/0.9340/0.8462) — their below-but-still divergence is
located in unnamed columns by M34, not attributed — plus the
M35 Task-1/2 method to reuse verbatim per shape, s0-anchored).
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- Still-below unnamed rows (M34): shape 2 k=6 [311, 312, 313,
  314, 332, 339], shape 3 k=7 [311, 313, 314, 316, 332, 336,
  339], shape 700 k=8 [38, 54, 299, 311, 313, 314, 332, 620].
  21 cells total across 3 shapes (reproduce each row's
  n/ov/standings/deltas exactly or table the mismatch and
  stop).
- The value question (M35 method per shape × cell): per-site
  rows (offset/plane/row/col, |δ|, other-frame status + |δ|,
  signed gaps both), near-miss shares (H1/H2/H3-style),
  band/decile joins (streak/carrier N/A off the named set —
  table as such), gap-sign table, |δ| stats.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34.txt` +
  `m34-census.tsv` (2/3/700 unnamed rows to reproduce:
  k 6/7/8 + cell lists + standings + deltas — match exactly
  or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m36/`; evidence `local/research/M36/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m35/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m36/` + `local/research/M36/`.
- Evidence dir `local/research/M36/` (STANDALONE). Commit with
  `git add -f`, prefix `[M36]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M35 precedent).

## Task 1 — per-site value tables (all 21 cells, 3 shapes)

2/3/700 unnamed rows recomputed from the dumps (k 6/7/8 +
cell lists + standings + deltas must match M34 exactly or
table the mismatch and stop):

1. Per-site rows for every missing + extra site in all 21
   cells (M35-style rows; both directions where mixed).
2. Near-miss shares pooled + per shape (H1/H2/H3-style:
   bulk vs non-cell on the other frame — shape-9 reads
   extras 1/14/7, missings 2/2/4; table 2/3/700's).
3. Shape-700 headliner (k=8 max of the three + J 0.8462
   lowest): full row lists + value stats tabled
   individually (M35's c314/c315 treatment).

## Task 2 — joins (where do still-below values live?)

1. Band/decile joins pooled + per shape (M35 machinery
   verbatim — s0-anchored; shape-9 extras read 21/22
   band-1; table 2/3/700's).
2. Gap-sign table (signed gaps both frames per site —
   shape-9 missings read 8/8 ++; table 2/3/700's).
3. |δ| stats pooled + per shape (shape-9 extras med 13.0
   / missings med 10.0; table 2/3/700's + largest-|δ|
   sites).

## Task 3 — controls + determinism (M18–M35 precedent)

1. Positive control: synthetic truth with KNOWN still-below
   values (N cells, known δ/gaps/standings) — recover the
   per-site values + standings exactly.
2. Determinism: re-run Task 1 on s0+2, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M35).

## Report

`local/research/M36/REPORT.md`: DESIGN.md (bars first) +
per-site tables + joins + controls + explained-vs-standing
update (attribution, not explanation — table it) + gap rows.
Screenshot only if a value map discriminates (budget-noted,
pre-registered split required per M23–M35 precedent). Commit
evidence (`[M36]`, no push), stop.
