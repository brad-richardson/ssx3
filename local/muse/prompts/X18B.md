# X18B — DeepSeek Flash Go vector boundary repair

You are an OpenCode Go `deepseek-v4.1-flash` worker on a narrow follow-up.
Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, and
`local/AGENTS.local.md`. Read only this brief,
`local/research/X18/{REPORT.md,ORCH-GATE.md}`,
`local/tooling/orch/{tile_vector_v2.py,check_x18.py}`. No web, game build,
boot, device, OpenCode config edit, push, or source search. Write only
`local/tooling/orch/tile_vector_v2.py` and
`local/research/X18B/REPORT.md` (each <20 KiB).

Gate finding: X18 parses the saved N8D6C sampled/raw 896-word vectors,
but its `rstrip(',')` plus continuation leading-comma strip accepts two
commas split across lines as if one word separator. The expanded checker
tests four boundary forms: one comma before the split, one after, two
across, and zero across. Only the first two are valid. It also tests a
double trailing comma and the original saved log and malformed fields.

Change the parser narrowly to preserve exactly **one** separator across
every continuation boundary. Keep the existing return schema, 896 cap,
same-PID prefix behavior, and saved-log positive result. Do not edit the
checker. Run `python3 -m py_compile local/tooling/orch/tile_vector_v2.py`
and `python3 local/tooling/orch/check_x18.py` once after the edit; one
further edit/retry is allowed, then stop. Wall cap six minutes, context
target <18k. `REPORT.md` gives exact command/output, a table for four
boundary forms and saved-log result, failures, elapsed/context as visible,
and gaps. No GS verdict. Commit only the two allowed paths as `[X18B]`
with `Orchestrated-By: opencode`, explicit paths, no push. Hand back table,
**do not conclude a cause**.
