# E55D5 — one changed race-pad entry, preparation hold (Muse Contributor Go)

You are an OpenCode Go E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, E55D4 `REPORT.md`, `ORCH-GATE.md`, `e55d4_boot.py`, `e55d4_compare.py`, `comparison.json` and E55D5 in `docs/todo.md`. Own only `~/dev/ssx3-work/E55D5/` and `local/research/E55D5/`; read E55D4 scratch baseline A1, E55D3 private fork `bab6eb382673155ffd756fe8db265964eeff9703` and pinned runner, without editing them. No source/build/device/iOS/upstream/push. **Part 1 is script and self-check preparation only. Do not boot until the orchestrator reviews and releases the exact script SHA in this pane.** G lane currently owns mini build/replay.

Goal: test whether one predeclared vsync-pad change produces the expected first ordered guest-write difference after E55D4 A/A proved a common prefix. Use the same I26-FAST route except replace exactly the `33517:cross:200` entry with `33517:square:200` (one entry, after race start, around tick2010, before tick2053). Keep all other route entries, ISO/ELF/codegen, runner SHA `e282c8a79cb4ce3e616244d1cfb0b5ab6d522e2f262408c6ffa92860f211643f`, deterministic mode, movie bypass, probe/hash flags and empty cards as E55D4. No source or binary mutation. Make one fresh B1 boot with per-run cwd/cards and distinct probe/log; at most one boot after release, slot lease only during boot. Stop at hash tick2053 **and** persisted complete probe line with vsync>2053 as flush proof; same 500 s wall, 120 s no-progress, 180 s/60-tick extra-wait, 4 MiB log and 16 MiB probe caps; kill only the recorded PID, force release lease. Refuse output reuse. Check runner-dir guard and two matching SHA reads of inputs before boot.

Prepare a B comparator against E55D4 A1: decompress saved A1 `boot.log.gz`/`probe.log.gz` read-only into bounded private scratch. Require both complete consecutive hashes 1..2053, well-formed shared-seq pad/getdir/mcread records and flush proofs; compare all fields/payloads and identify **first differing guest-write seq/vsync/port/bytes** and first differing hash tick. A valid pad-B discriminator is identical ordered records before that write, one changed `pad` record during the predeclared `33517ms` pulse window, no earlier difference, no card manifest drift and no live pad row. If the pulse does not reach a guest write by tick2053, report OTHER; if the first difference is earlier, card/other family, malformed, cap or no flush proof, report FAIL/OTHER per predeclared reason. Do not infer that a later hash divergence was caused solely by the pad without the ordered write chain. Preserve both sides' first differences in the result. Build pure synthetic self-checks for identical prefix, expected pad change, earlier difference, unchanged/no-event, truncated/cap and both first-difference fields.

Before release deliver `e55d5_boot.py`, `e55d5_compare.py`, synthetic self-check output and a pre-run REPORT table with exact pinned paths, route diff, stop/classifier, caps and both script SHAs. No boot in Part 1. After release, run one B1 only, save results and bounded excerpts in git, gzip full closed logs in private scratch, global disk <200 GB, new scratch <2 GiB, committed text <512 KiB. No speed claim. Check `git log -1`, commit named brief/scripts/receipts `[E55D5]` with `Orchestrated-By: opencode`, no push. Hand back the evidence table; the orchestrator decides the isolation verdict and later card run.
