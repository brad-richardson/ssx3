# E55C Part 1 — deterministic VBlank hash-tap source map

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4.
This is a **read-only source audit**. Hand back a cited table and one
bounded Part-2 runbook; the orchestrator decides the mechanism/gate.
Your only writes are `local/research/E55C1/REPORT.md` and ≤2 MiB text
receipts under `~/dev/ssx3-work/E55C1/`. No fork source edit, build,
boot, device, codegen regeneration, push or upstream contact. The named
report/work paths count as your write scope. “First failure” means a
failed brief step, not an exploratory typo.

Pin fork `ssx3` remote and local E55B2 worktree at
`779e8048cd584586f6896fde0f049fa758f287db`. Read the full
`local/research/E55A2/REPORT.md` and `E55B2/REPORT.md`, and the
determinism proposal in `docs/research/review-2026-09-23-frontier-2.md`
§B. The two modes now fix RTC and scheduled-event selection; no
repeatability proof exists. This audit locates a dev-only
`PS2X_DET_HASH_EVERY=N` VBlankStart tap that could compare idle and
loaded boots without requiring screenshot timing. Do not design a new
host pacing policy here.

## Table to hand back

1. Cite the exact VBlankStart statement order in `EeScheduler.cpp` and
   choose one unambiguous moment for `(vsync, eeCycle, hash)` relative to
   tick increment, guest flag/tick writes, callback queueing and IRQ.
   State which regions can change on another thread at that moment and
   what evidence supports the statement.
2. For RDRAM, scratchpad, VU1 data/code and VU1 program count, give
   current source APIs, sizes, ownership/lifetime and whether each can
   be read without invoking guest behavior. Mark any region/count
   missing or ambiguous; do not invent a pointer or use a text label as
   a value. Include a stable serialization order and byte count for a
   proposed tap. `xxh64` was named in the review: check whether an
   implementation/dependency exists in the pinned fork; if absent,
   table the implementation choice for the orchestrator rather than
   silently substituting another hash.
3. Trace where a zero-cost-disabled diagnostic gate belongs: exact
   `PS2X_DET_HASH_EVERY=N` parsing (unset/0/default off), compile-time
   tap guard for release/speed builds, bounded one-line text output and
   overflow/invalid-input handling. Identify existing test/harness
   patterns to reuse, with citations. No full-memory logging or copies.
4. Part 2: one narrow source/test candidate and a discriminating run
   plan. Name the control: two idle boots and one loaded I26-FAST boot
   with identical pinned inputs, comparing `(tick, eeCycle, region hash)`
   through a common prefix; distinct guest state must be a mismatch,
   not a pass. The hash tap itself should be tested with a known byte
   vector and region perturbation. Keep pads/card isolation and
   ExternalWake timing as explicit possible divergence sources, not
   presumed fixed. Give build/boot/log/byte budgets and stop rules.

Budget: 20 minutes, narrow `rg`/small source windows/LSP connections,
≤2 MiB text receipt, no heavy job. Stop when a claim needs execution or
an unavailable pointer; write the gap. Cite current fork paths/lines
for each conclusion. Deliver and commit only the explicit report as
`[E55C1]` with trailer `Orchestrated-By: Codex`, no push.
