# M27 — Shape-733 tail divergence: per-byte attribution of private sites (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M22/REPORT.md` (M22 gap 2, carried
through M23 gap 4 → M24 → M25 → M26 gap 11, is this brief: shape
733's tail map reads Jaccard 0.6694 vs s0 on 100 B while 727/764
shapes read byte-identical, 731 next-lowest at 0.6833) plus
`local/research/M23/REPORT.md` (K45+ transfer table: 733 scores
14/100 like s0 — values transfer, the SET diverges). Fully
offline — SSD dumps read-only, no harness code, no replays, no
lease. No device work.

## Facts you start from

- Shape 733 (M22): tail 100 B, Jaccard vs s0 0.6694 (lowest of
  764); K45+ hits 14/100 + near 31 (M23 — same rate as s0);
  SIGN correction 21/100 (M24 — same rate as s0). So the
  divergence is membership (WHICH sites are tail), not values
  (δ behaves the same on the sites it has).
- Private sites = T_733 − T_s0; missing sites = T_s0 − T_733.
  The attribution question: are private sites near-misses
  (|δ| just below 8 on s0? just above on 733?), displaced
  versions of s0 sites (same component, shifted rows?), or a
  genuinely different set (different band/decile/carrier)?
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), the shape-733 triplet in particular,
  `/Volumes/Extreme SSD/m22/m22.txt` (733's tail membership +
  Jaccard to reproduce: 100 B, J 0.6694 — match exactly or
  table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m27/`; evidence `local/research/M27/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m26/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m27/` + `local/research/M27/`.
- Evidence dir `local/research/M27/` (STANDALONE). Commit with
  `git add -f`, prefix `[M27]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M26 precedent).

## Task 1 — private/missing site tables (what differs?)

733's tail membership recomputed from the dumps (100 B +
Jaccard 0.6694 vs s0 must match M22 exactly or table the
mismatch and stop):

1. Private-site table: per private site (offset, plane, row,
   col): |δ_733|, |δ_s0| at the SAME site (near-miss? far?),
   gap_733 vs gap_s0 (set change or δ change?).
2. Missing-site table: mirror (sites tail on s0, not on 733):
   |δ| on both, gap on both.
3. Position join: private/missing/shared sites by band /
   gradient decile / streak column / carrier mask (M22
   machinery verbatim) — does the divergence localize?

## Task 2 — displacement check (shifted or different?)

1. Nearest-shared-site distance for each private site (same
   plane, Manhattan px): 1-px displacements vs far scatters —
   table the distance histogram.
2. Component view: 733's tail components vs s0's (8-conn:
   count, sizes, the r23–33 streak columns — present?
   shifted? absent?).
3. 731 cross-check (second-lowest Jaccard 0.6833, 100 B): same
   private-site position table (is 731's divergence the same
   shape as 733's, or a different one?).

## Task 3 — controls + determinism (M18–M26 precedent)

1. Positive control: synthetic truth with KNOWN private sites
   (N sites forced tail on shape B only) — recover the
   private/missing sets + Jaccard exactly.
2. Determinism: re-run Task 1 on s0+733, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M26).

## Report

`local/research/M27/REPORT.md`: DESIGN.md (bars first) +
attribution tables + displacement tables + 731 cross-check +
controls + explained-vs-standing update (unchanged expected —
attribution, not explanation — table it) + gap rows.
Screenshot only if a private-site map discriminates
(budget-noted, pre-registered split required per M23–M26
precedent). Commit evidence (`[M27]`, no push), stop.
