# I31 — deploy Brad's save with the app (iOS now, Odin after TL1), manual play by default

You are the deploy worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, `local/research/E55D16/REPORT.md` (card extraction + `mc0` format), `local/research/I30/{REPORT.md,ps2x.env,build-install.sh}`. Own `local/research/I31/` and scratch `~/dev/ssx3-work/I31/`. Brad's save files are private: **never commit card bytes, save files or their contents**; SHAs and names only.

**Why.** Brad wants his real SSX 3 save (from his Odin AetherSX2 card) on the builds he plays, to test more runs by hand. The extracted save is `~/dev/ssx3-work/E55D16/mc0/BASLUS-20772-{GAM0001,SET0001}/` (6 files, SHAs in E55D16's report). The game autoloads it at boot (E55D16).

## Part 1 — iOS (now)
1. Find how the iOS env loader layers files (bundle `ps2x.env`, then `Documents/ps2x.env`?) and whether a later layer can **unset** a key (fork `ssx3`, cite file:line). Goal: Brad's default is **manual play** (no `PS2X_PAD_SCRIPT`), while our tests can still turn the I26-FAST script on. Prefer a no-rebuild route: a `Documents/ps2x.env` on Brad's devices that clears the pad script, if the loader supports it; otherwise report the smallest change and stop (no fork edits in this lane).
2. Copy the two save dirs into the app's Documents `mc0/` on the **iPhone** and the **iPad** with `xcrun devicectl device copy to --domain-type appDataContainer --domain-identifier org.ps2x.ps2entryrunner …` (plus the `Documents/ps2x.env` from step 1). Verify with `devicectl device info files` (names, sizes). **iPhone: copy/install only, never launch.** iPad: one launch to confirm the save autoloads (Select Character shows Mac's upgraded stats, as in E55D16) and that no scripted input fires; terminate after.
3. Write `local/research/I31/deploy-ios.sh` so future iOS installs re-apply the save and the manual-play env (idempotent, skips if present).

## Part 2 — Odin (only after the orchestrator says go; TL1 is using the Odin)
Push the save into the app's default `files/mc0/` and set the device's default `ps2x.env` to manual play: parallel backend, bundled Turnip, `PS2X_SKIP_MOVIE=1`, `PS2X_SOUND=1`, no pad script. Automated launchers must not touch Brad's card: document that test runs set `PS2X_MC_ROOT` to an empty `files/mc0-test/` (and restore Brad's env after). Lease, keyguard, battery (`AC powered: true`, ≥ 20 %), one launch to confirm autoload + sound stream, force-stop. `local/research/I31/deploy-odin.sh`.

Deliverable: `local/research/I31/REPORT.md` (loader semantics with citations, what was copied where, verification, scripts, gaps). Text only in git. Commit `[I31] Part N` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. Budget 45 min per part.
