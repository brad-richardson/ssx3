# M51 — TRI cross-frame collapse: per-column triangle-drift tables (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M26/REPORT.md` first (all of it —
M26 gap 1 = this brief via M29 gap 11, taken on orchestrator
judgment: TRI fit-s0→m15 reads 4/68 (2/65 the other way) vs
18/16 same-frame — the CONST-collapse phenomenon at shape
level), plus `local/research/M25/REPORT.md` (the CONST
collapse + peak-anchored residual method). Fully offline —
SSD dumps read-only, no harness code, no replays, no lease.
No device work.

## Facts you start from

- The 8 TRI columns (M26 Task 2 — reproduce exactly or table
  the mismatch and stop): 257/277/296/301/321/340/342/343
  with s0 p/h/e/w 25/30/13/7, 25/28/13/7, 26/47/15/8,
  27/47/13/6, 25/27/11/7, 26/47/17/8, 27/−16/−17/1,
  27/−26/−31/1 and m15 p/h/e/w 25/20/8/7, 25/20/8/7,
  26/46/16/8, 27/48/12/4, 26/31/14/6, 32/48/14/8,
  27/−13/−21/4, 289/−15/−22/1 (M26-verbatim fit + scoring).
- Same-frame scores to reproduce: TRI 18 s0 + 16 m15 hits;
  cross-frame collapse: 4/68 fit-s0→m15 (2/65 reverse).
- The question: per-column triangle-drift tables (Δp/Δh/Δe/Δw
  s0 vs m15 per column — which TRI leg collapses?) + the
  collapse recount (which columns keep hits cross-frame?).
  Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles), `/Volumes/Extreme
  SSD/m26/m26.txt` (the 8×2 fits + scores to reproduce —
  match exactly or table the mismatch and stop). Work dir:
  new `/Volumes/Extreme SSD/m51/`; evidence
  `local/research/M51/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m50/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m51/` + `local/research/M51/`.
- Evidence dir `local/research/M51/` (STANDALONE). Commit with
  `git add -f`, prefix `[M51]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M50 precedent).

## Task 1 — TRI fit reproduction (do the 8×2 fits reproduce?)

M26's TRI fits recomputed M26-verbatim from the dumps (must
match exactly — p/h/e/w per column per frame + same-frame
hit counts 18/16 — or table the mismatch and stop):

1. Fit table: all 8 columns × s0/m15 p/h/e/w (16 fits).
2. Same-frame score table: TRI hits/near/miss-bins per
   frame (18 + 16 reproduced with miss bins?).
3. Collapse table: fit-s0→m15 hits (4/68) + fit-m15→s0
   hits (2/65) with per-column kept-hit rows (which
   columns keep hits cross-frame?).

## Task 2 — triangle drift (which leg collapses?)

1. Drift table: Δp/Δh/Δe/Δw (m15 − s0) per column (which
   leg drifts most? peak p? height h?).
2. Drift-vs-collapse table: per-column drift magnitude vs
   cross-frame kept hits (do low-drift columns keep hits?).
3. c343/c342 table: the negative columns' fits + drifts
   (c343's m15 p=289 vs s0 p=27 — the peak jump itemized).

## Task 3 — controls + determinism (M18–M50 precedent)

1. Positive control: synthetic truth with KNOWN triangles
   (N columns with known p/h/e/w + known drift) — recover
   the fit + drift tables exactly.
2. Determinism: re-run Task 1 fits on s0+m15,
   byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M50).

## Report

`local/research/M51/REPORT.md`: DESIGN.md (bars first) +
fit tables + drift tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a drift map
discriminates (budget-noted, pre-registered split required
per M23–M50 precedent). Commit evidence (`[M51]`, no push),
stop.
