# G34 — Tagged write-back: same W1 sync, sparse nonzero-RGB tag OR-masked (H1 vs H2 split, Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G33/REPORT.md` first (all of it: the
write-back control is VOID because stale-correct ≡ cleared byte-for-byte
(0/229376 nonzero-RGB words), while write-back integrity is proven (16
commits, zero drift) — the mechanism is exonerated, the CONTENT was
wrong). This brief executes G33 §4's ONE next action ONLY. No upstream
contact of any kind (standing no-upstream order — local hunks only,
filing stays local).

## Facts you start from

- The split (unchanged): H1 (ordering/coherency — the commit/barrier
  fixed G31's cleared output) vs H2 (content-dependent fault — the
  pattern content fixed it). G33's control could not split them
  because both hypotheses predict the same bytes on all-zero-RGB
  content.
- Discriminator (G33 §4): the SAME W1 write-back shape, but
  OR-masking a sparse nonzero-RGB tag into the temp BEFORE the
  write-back commit — so H1 and H2 predict DIFFERENT bytes (H1:
  tag-visible output; H2: cleared output, tag or no tag).
- Post-G33 drive state (gate-verified — read carefully): G29/G30/G32
  all read their BUILD shas with frozen mtimes — ALL prior
  "destructions" are RECLASSIFIED as read-path artifacts of the dying
  USB link (progressive link death, not progressive writes; every
  run-validity conclusion stands). BUT the G33 binary shows a NEW
  stable localized mutation post-report (build `7e0ea803…` → now
  `d1238552…`, mtime frozen at build, ELF + all hunk markers intact)
  — flash-level suspect. PARANOIA IS JUSTIFIED: re-verify EVERY pin
  (dump sha, binary shas, HUNK_MATCHes, dir 0-growth) at pre-push AND
  post-run AND report time; any pin mismatch → table + stop. Mirror
  the new `ps2x-g34/` receipts to the share tier at end (drive
  distrusted).
- Task 1 (no device until §2): STATIC design — table the tag shape
  (which words? what RGB values? why sparse?), the mask site (temp
  pre-commit — exact sequencing vs G33's W1), the reformulated H1/H2
  predictions (exact FNVs? exact census? pre-register every scalar),
  the G33 predictor reuse (it must reproduce the VOID on stale input
  as a self-check), and the decision matrix (tag-visible vs cleared
  vs partial/mixed). Name the ONE hunk.
- Task 2 (ONE run max): the tagged control — ONE hunk max
  (diagnostic-dump ONLY + the tag it requires), ONE build max (NEW
  dir, G22 recipe, `-j2`), full identity, verify-then-push with NO
  gap (push-time + post-run + report-time re-shas), TWO bounded runs
  max (diagnostic + one retry iff O1/O3 fires instead of first draw).
- Success bars (tabled): design table (tag + mask site + predictions
  + matrix); the H1-vs-H2 VERDICT (with the discriminating bytes);
  if a run: exit/wall/fate + receipts + tombstone triage (if any) +
  scanouts scored AND pulled by explicit filename list with cleanup
  as its own verified step (G31-E1 rule); the ONE next action (fix
  shape vs next wall — adoption stays queued until brightness is
  understood at the sample level).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g34/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max; ONE build max (new dir, G22 recipe, `-j2`); TWO bounded
  device runs max; no tuning loop, no second shape, no new dumps; no
  lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g34/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G34/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G34]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (recomputed AFTER the last
  edit). Fill in the ACTUAL time used (no `~X h` placeholders).
