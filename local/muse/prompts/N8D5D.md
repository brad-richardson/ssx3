# N8D5 Part 4 — build the pinned Android tile-probe APK

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. **Build/package only.** No Odin install/launch, bytesize game/emulator boot, fork/main push, or shared-source edit. Work in `local/research/N8D5D/`, Mac scratch `~/dev/ssx3-work/N8D5D/`, and fresh WSL scratch `/home/brad/n8d5d/` on `ssh bytesize`. Do not alter `/home/brad/n8b1/`, `/home/brad/n8d3/`, or Mac N8D5B/C source copies. bytesize permits one heavy job at a time; check that no other build/trace is active before starting.

Facts: `local/research/N8D5C/REPORT.md` plus `orch-replay-excerpt.txt` pin fork `a847d0fe8ee6957154fe85986dc371c8e1ec0f1e`, backend SHA-256 `ed87b66d981186725469c8bfa85a857c47813aa85d3fd5ff843516e5b17e9a61`, shader header SHA `19b9ba5fc747d8fcb70d712b86bd5738c64424ceb15c24d4b5ed58219f71bac9`. Mac same-stream control passed 128/128 and sampled/raw vectors matched 896/896, 567 active tiles each. This validates the probe on Mac, not Odin. N8B1's WSL source `/home/brad/n8b1/PS2Recomp` has the **exact** fork-base backend SHA `38b7a5afe495dee479c3532e4b0f4ca3972f61ae3764710d94fb89609795e519`; its G43 `/home/brad/n8b1/parallel-gs` matches the Mac private G43 at the checked `gs_interface.cpp`, CMakeLists and Granite shader.cpp SHAs. The header is absent there. Canonical external codegen is `/home/brad/n8b1/codegen-ssx3`; `register_functions.cpp` SHA `8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`. Turnip input is `717812c3…1ac29d`; use N8D3's `jniLibs` for its proven HAL shim plus Turnip.

Prepare `/home/brad/n8d5d/` once: `rsync -a` N8B1 `PS2Recomp` **excluding** `.cxx`, `build`, `.gradle`, and `/ps2xRuntime/src/runner/`; copy N8B1 `parallel-gs` into a private snapshot and N8D3 `jniLibs` into a private snapshot. Transfer **only** the candidate backend source from Mac N8D5C fork and shader header from Mac N8D5B G43 into WSL inputs, reading each SHA twice on both hosts before use, then install them at the two corresponding snapshot paths. Check no other source file differs from N8B1 (excluding build/cache/runner and the named header). Keep guest code, game data, Turnip binary and APK outside git. Write `REPORT.md` before the build with source and package gates.

One `assembleRelease` from `/home/brad/n8d5d/PS2Recomp/android`, using N8D3's `build.sh` as the command pattern: JDK17/Android SDK/Gradle home under `/home/brad/n2/`, external N8B1 codegen, private N8D5D paraLLEl source, N8D3-proven private `jniLibs`, `-Pps2xGsShadowParallel=ON`, arm64, diagnostics/runtime/aggressive/IOP RPC/debug UI OFF. Reuse `/home/brad/n8b1/mem_governor.sh`; cap WSL scratch 10 GiB, Mac scratch 500 MiB, text receipts 8 MiB, global mini ssx3 usage <200 GB. One named compile-repair retry is allowed only for an explicit build failure; no tuning loop. Do not run the app.

Gate the APK before handoff: two matching APK SHA reads on WSL and after transfer to Mac; arm64 package members exactly runner, Turnip and HAL shim, no x86; two matching SHA reads for each member, Turnip equal to `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`; stripped shim equal to N8D3 package pin `1b49d27c839f25363237d8276c91a401602845ee8eef67de50d6540f4cdfc387` or stop with a difference table. Record runner build ID, cache flags/source paths and presence of `PS2X_N8D5_TILE_CAPTURE` and control strings in the packaged runner. Check Mac fork runner-dir diff against `14b1e5cb` empty. Deliver bounded scripts/log tails, SHAs, source diff and `REPORT.md` under `local/research/N8D5D/`; commit `[N8D5D]` with `Orchestrated-By: Codex` and explicit paths, no push. Hand back the package table, not a device verdict. The orchestrator gates before any Odin run.
