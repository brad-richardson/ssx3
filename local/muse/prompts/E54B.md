# E54B — GS CSR VSINT acknowledgement and FIELD preservation

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `local/research/E54A/REPORT.md`,
`local/research/W1F2/REPORT.md`, and the relevant queue note in
`local/research/GB3/REPORT.md` first. Hand back a table; the
orchestrator decides the gate. This is **one** CSR-status candidate,
not the whole E54 semantics batch.

Source pin: fork `ssx3` `bc1c70f` (verify remote). Make a new worktree
`~/dev/ssx3-work/E54/PS2Recomp` on local branch `e54-csr`, plus build
and bounded receipts under `~/dev/ssx3-work/E54/`; write report/text
receipts only under `local/research/E54B/`. Do not touch W1F2, GB4,
the canonical codegen, PCSX2, or game inputs. Existing fork CSR code
handles SIGNAL/FINISH bits 0–1 as W1C; preserve it and the GS queue's
atomic ordering.

Correct behavior: a guest write of CSR bit 3 **acknowledges** VSINT,
not sets it; FIELD (bit 13) is timing-owned and survives guest writes.
PCSX2 raises VSINT and updates FIELD at GS blank, about 3.5 hblanks
after VSync start; this fork currently has only VBlankStart. Candidate
for this part: include bit 3 in the CSR W1C mask for 32/64 guest writes,
preserve FIELD on those writes, and raise VSINT alongside the existing
FIELD update at VBlankStart. Keep the existing queue/atomic path. Label
the event timing as an approximation in the report; do not claim cycle
parity with PCSX2. Alternatives are an existing setter elsewhere or a
different CSR read path, which E54A's source table did not find.

## Gates

1. Add focused unit cases: reset CSR; guest `write64(8)` leaves VSINT
   clear; one VBlankStart sets it and advances timing-owned FIELD;
   guest `write64(8)` clears VSINT without clearing FIELD; next vblank
   raises VSINT. Cover 32-bit low-word writes too, and preserve existing
   SIGNAL/FINISH/FIFO tests. Remove the stale `0x4008` expectation only
   after replacing it with these assertions. If exposing VBlankStart to
   the test needs broad surgery, test the exact event helper and state
   the seam.
2. One focused source fix in `ps2_memory.cpp`, `EeScheduler.cpp` and
   tests only. Build Mac Release with canonical E56 codegen and run
   `ps2x_tests` from the fork root. Run taps OFF and ON. No generated
   sources in Git, no unrelated GS semantics changes.
3. One mini-lease I26-FAST boot with `PS2X_SKIP_MOVIE=1` (dev-only),
   `PS2X_PKLOG=1` and a bounded ring/log, target at least race HUD
   tick 2050, wall cap 500 s, own PID. View title/character and race
   frames. Extract CSR reads at PC `0x37c0e0` and `0x396090` if seen,
   with bit-3 values and tick; **absence means reach unknown**, not a
   failed semantic fix. No fixed-tick frame-hash gate until E55.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty and commit `[E54B]` on the local fork branch with trailer
   `Orchestrated-By: Codex`. No fork or ssx3 `main` push. The
   orchestrator will decide whether to fast-forward the fork.

Budgets: 2 configure/build attempts (one correction), one boot,
≤600 s boot absolute, ≤8 GB new E54 bytes and 200 GB global cap.
Verify pinned ISO/ELF, codegen register file and runner by two matching
SHA reads. Claim/release a mini slot, bound logs to 64 MiB, stop own PID.
If the suite or route fails after the one repair, stop and report; no
tuning loop. Deliver `local/research/E54B/REPORT.md` with revisions,
source diff, unit sequence, suite counts, boot behavior, CSR-site table,
frames viewed, SHAs, runner-dir check, timing limitation, and gaps.
Commit `[E54B]` report with `Orchestrated-By: Codex`; no push.
