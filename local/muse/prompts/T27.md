# T27 — Press-on-title within dwell: reach the first menu (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T25/REPORT.md` first (all of it): Phase 1
fresh boot SKIPS to attract (NVM `da021d2a` untouched); Phase 2
`t25-auto.sh` drove attract → TITLE 3× (P1 Cross 532.9 ms, P2 Start 533.0 ms)
but attract reclaims in ≤10/≤20/≤80 s so no press ever acted ON title — first
menu unreached, chain ends cycling. T25's G1 proposes press-on-title within
dwell: this brief runs it. Reuse T25's title snaps
(`p1a/p2a/p2d`, region-identical p99 0) as the TITLE detector reference.

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (`ssh` + `scp`, no lease of any kind).
  Reuse the bit-identical build + inputs T25 verified (re-verify sha/size/rev
  before running; table the receipts).
- The task: a scripted path that detects TITLE (snap-compare against T25's
  references, table threshold + match scores) and presses WITHIN the dwell
  window (T25: attract reclaims ≤10/≤20/≤80 s — table the dwell you actually
  observe this run), then drives to the FIRST MENU. Table every press (button,
  hold ms, T+ time, pre/post snaps) and every screen transition (snap sha +
  region-diff scores).
- Success bars (tabled, not verdicts): TITLE detected by snap (scores);
  press lands on title (pre-press snap = title); first menu reached (snap);
  menu stable across N snaps (table N + shas); full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). If WSL kills recur (T25: 7 flaps + one
  kernel restart), table them exactly (dmesg + event log, kill-to-remount
  times) and re-verify trace shas post-reboot; do NOT switch toolchains
  mid-brief (table a Windows-native-Devel recommendation instead).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on the
  SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T27/` (STANDALONE). Commit with `git add -f`,
  prefix `[T27]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Time box: 4 h. Tables, no verdicts.
- Hygiene: write the report in chunks with a tail receipt (truncated tail
  fails the gate).

## Task 1 — press-on-title run (or cap)

1. Pre-run checks: build sha/size/rev/status/pad-binding (T25 §receipts
  verbatim), NVM state (tabled, untouched expected), title-reference snaps
  present, free space both ends.
2. Run the detector + press script (bounded: ≤3 title attempts; table each
  attempt: detect time, dwell observed, press, outcome). Cap: 1200 s wall
  (T25 precedent: unexercised precaution).
3. Tables: dwell observed this run (attempt × seconds); press log (button /
  hold / T+ / pre-post snaps); screen chain (snap sha + diff scores per hop);
  first-menu arrival (snap + stability N); trace bytes + sha; WSL events.

## Report

`local/research/T27/REPORT.md`: pre-run receipts, attempt tables, press log,
screen chain, menu arrival (or the exact hop it ends at), WSL table, exact
commands, gaps. Commit evidence (`[T27]`), stop.
