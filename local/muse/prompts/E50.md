# E50 — Race 3D lands off-screen: find the bad transform input (recomp)

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md`;
- `local/AGENTS.local.md` (the `ee-*` helpers; use them for every code
  fact);
- `local/research/E47/REPORT.md` (Part 2 especially).

**E49 owns `~/dev/PS2Recomp`.** Work in a worktree
(`git -C ~/dev/PS2Recomp worktree add ~/dev/ssx3-work/E50/PS2Recomp -b
e50-diag ssx3`) with your own build dir against canonical
`~/dev/ssx3-work/codegen-ssx3`.

**Host etiquette:** before any build, wait until no other `clang++`/
`ninja` is running (E49 builds too). Use `nice -n 10` for your builds.

## Facts (E47)

- In the race (ticks 7600–7610, E47's route script), VU1 runs clean (0
  budget exits) and all MPG uploads land. The GS issues ~6,500 PATH1
  tstrips per vsync with sane FRAME/TEST/ALPHA/SCISSOR.
- But the PATH1 xy bounding box is pinned to **1023.5–3071.5 on both axes
  every vsync** (the viewport is 1792–2304 × 1824–2272). Every 3D draw in
  the E4 tail is off-screen. The rider at Select Character is fine (box
  inside the viewport).
- So the vertex positions from the VU1 world programs are wrong in the
  race. The inputs are suspect: the view/projection matrix, camera, or
  per-object matrices uploaded to VU1 data memory.

## Work

1. **Small dev-only taps** (default off, unit tested):
   - (a) a per-vsync count of PATH1 draws that are fully on-screen, fully
     off-screen, or straddling;
   - (b) an E4 option to keep the FIRST N draws of the frame (N = 4096),
     not only the tail.
2. **One race boot** (E47's route and window, one slot):
   - (a) + (b);
   - `PS2X_VU1_ENTRY_TRACE` for the world programs' startPCs (from E47's
     MPG and VU1 tables), dumping VF registers and VU1 data memory at
     entry for the first world program of one race vsync.

   Tables:
   - on/off/straddle counts;
   - the first 20 world draws (positions, TEX0);
   - every 4×4 float block in VU1 data memory that looks like a matrix
     (flag NaN/Inf/denormals, huge or zero scales, and rows that aren't
     orthonormal);
   - the same dump at Select Character (the healthy control, 1 boot, or
     the same boot if the window tooling allows).
3. **Code read:** from the dump, find which VU1 data-memory quadwords
   hold the view/projection matrix, which VIF UNPACK writes them, and its
   EE source address. Find the EE code that builds that matrix (`ee-xref`,
   a watch if needed). Note whether it uses COP2/VU0 macro ops (`vmul*`,
   `vmadd*`, `vdiv`, `vsqrt`, `vrsqrt`, `vclip`) or FPU `sqrt`/`rsqrt`/
   `div`, which are the recomp translations most likely to be wrong.

## Budgets and stop rules

≤3 boots, 5 h, cap 4 GB. Diagnosis only; you may *propose* one candidate
fix. T65 captures the same matrix in PCSX2; use the same table formats
(matrix blocks as 16 floats in hex + decimal).

## Deliverable

`local/research/E50/REPORT.md`. `[E50]` commit (`git add -f
local/research/E50 local/muse/prompts/E50.md`, `git log -1` first,
trailer `Orchestrated-By: Claude Code`). Fork commits stay on the local
branch `e50-diag`; no push.
