# GB7A — title-text producer/state source map (Muse Contributor Go)

You are an OpenCode Go worker on a **read-only** G-lane audit. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/GB5D/REPORT.md`, the relevant GB4 glyph receipt, and the GB6C paragraph in `docs/todo.md`. Pin the exact source HEADs before reading: `~/dev/ssx3-work/GB4/PS2Recomp` expected `f796669…` and `~/dev/ssx3-work/GB4/parallel-gs` if present; report any difference. No builds, replay, boot, device, source edit, web, upstream contact or push.

Question: existing matched CPU/paraLLEl title/menu frames show damaged copyright text by tick 300 and button-label strokes at first sampled appearance tick 700, bracket (600,700]. Identify the **first reachable draw/texture/state producer path that can be instrumented** for one same-stream comparison; do not claim the producer is proved by images alone. Correct rendering would preserve the CPU-visible glyph shape if the same texture pixels, clamp/format, alpha and draw coordinates enter both paths. Alternatives: guest upload/text source already differs; GS transfer/texture decode differs; composite or blend state changes correct glyph pixels. For each alternative, name the first exact observable that discriminates it and a null control. Do not infer packet identity solely from visual timing.

Deliver `local/research/GB7A/REPORT.md` and `source-map.tsv` (<64 KiB total). Table columns: `step,source_file,line_range,caller_or_data_source,observed_fact,unknown,proposed_probe`. Require 6–10 rows and literal line anchors in the pinned source. Use OpenCode LSP `goToDefinition`/`findReferences` to confirm C++ connections if available; if LSP is absent, cite the exact call and callee reads. Also inspect `local/research/GB4/glyph-packet-p6.txt` and say exactly what it does and does not identify. The next probe proposal must be one bounded same-stream diagnostic with expected CPU vs paraLLEl values at an already captured title marker, a byte/log cap, and stop rules; no implementation in this part. A small `check.py` under the receipt dir may validate TSV headers/row count and source paths, but it cannot certify semantic correctness. The orchestrator decides which probe to run.

Read-only commands should be limited to `rg`, `nl -ba`, `sed`, `git rev-parse/status`, `shasum`, and LSP. Cite the repo/receipts, not the web; label all unknowns. Check `git log -1` in ssx3 before committing only the named text receipts `[GB7A]` with `Orchestrated-By: opencode`; no push. **Hand back the table and proposed discriminator; do not conclude a GS cause.**
