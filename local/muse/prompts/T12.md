# T12 — Dev build config: ThinLTO-off variant + suite + ladder-identical proof (no adoption)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T6/REPORT.md` first (all of it —
T6 gap 4 is this brief: the miss is the ThinLTO runner link
(313–397 s of every incremental), and the fix class T6 was
allowed cannot touch it). This brief measures whether a dev
config (ThinLTO off; unity settings unchanged unless tabled)
recovers incremental builds WITHOUT changing guest behavior.
It does NOT adopt anything: adoption is an orchestrator/user
decision the numbers below inform. Peer lanes run concurrently
— build etiquette is mandatory (see rules).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight). T6 baselines
  to reproduce-or-explain (same flags, your own `/tmp/t12-link/`
  + `/tmp/t12-dev-link/` dirs — never touch peer build dirs):
  full build 472 s; cell (a) hot-cpp 315 s (link 312.8 s);
  cell (b) header 658 s clean; cell (c) 8 s; cell (d) 0 s;
  runner link 330 s full / 313–397 s incremental.
- IPO lives in `ps2xRuntime/cmake/ReleaseMode.cmake:38`
  (`INTERPROCEDURAL_OPTIMIZATION_RELEASE`). The dev variant
  must be a CONFIG (flag/cache option, default OFF — Release
  untouched), not an edit of Release behavior.
- Proof bar (T5 precedent): suite green on BOTH configs +
  emitter-vs-emitter `ladder_diff` exit 0 (dev boot vs
  Release boot, T1/T9 tooling) — behavior preserved or the
  config is tabled as diverging (still a complete result).

## Gates and rules

- Lease `T12`: boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (the SHARED P-lane lease) — if it names another agent (T11's
  run-to-exit has priority: yield), poll every 5 min and log
  waits to `$W/P1/run/t12-waits.log`. Max 2 boots, ≤90 s each,
  foreground (`PS2X_DIAG_PARK=1` both). No `adb`.
- Build etiquette (T6 §T6-6 method): `-j4` MAX; `pgrep -f` +
  self-excluding `ps` pre-check before EVERY build; if a peer
  builds, wait + log (don't halve both builds' throughput).
- Fork: the dev-config option ONLY (`git add` those NAMED files
  only — never `-A`; never stage generated/ignored files).
  `git pull --rebase` before pushing; `git push` ONLY inside
  the fork clone to the `fork` remote. Stop on any foreign
  rebase conflict. Purge `._*` before staging.
- Evidence dir `local/research/T12/` (STANDALONE). Commit with
  `git add -f`, prefix `[T12]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — the config (one option, both suites green)

1. Add the dev-build cache option (ThinLTO off for the runner
   link; name + docs in the report; default OFF — prove
   Release flags byte-identical with/without your commit via
   `ninja -t commands` on the link line or the cache diff).
2. Build BOTH configs (full, `-j4`, uncontended windows only):
   full-build walls side by side; incremental cells (a)+(c)
   only on dev (skip (b)-header on dev unless time allows —
   table whichever ran).
3. Suite green on BOTH binaries (total/passed/failed per
   config; any dev-only failure = table it exactly — the
   config diverges and Task 2 still runs to characterize).
4. Commit + push per the rules above.

## Task 2 — proof (dev-vs-Release ladder-identical pair)

1. Two ≤90 s boots (lease-gated, `PS2X_DIAG_PARK=1`): Release
   binary vs dev binary. Emitter-vs-emitter `ladder_diff`
   (current tool): table the verdict line + every non-EXACT
   row (any COUNT_DELTA/KEY_DELTA = behavior change —
   table it exactly).
2. Boot-pair table (lines, stub series, thread ends, drops,
   crash/FATAL) one ladder table, exact-vs-baseline.
3. Adoption inputs as a TABLE (dev incremental walls vs
   Release, suite both, D verdict, divergence rows if any —
   numbers only, no recommendation).

## Report

`local/research/T12/REPORT.md`: config spec + Release-flags
proof + build-wall tables + suite table + D verdict + boot
ladder + adoption-inputs table + gap rows. Commit evidence
(`[T12]`, no push), stop.
