# T24 — pc= boot proof + GetThreadId per-site split (short leased boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T22/REPORT.md` first (all of it —
the `pc=` field (fork `6359fb6`, env-gated default-off,
harness-proven, boot proof DEFERRED as gap G1 — this brief
closes it) plus `local/research/A0/REPORT.md` §(iii-a) (the
21 GetThreadId call sites whose per-site split needs `pc=`
— A0 gap G4, closed by the same boot). One short boot serves
both: the pc=-on stream proves the field in-boot and
attributes every GetThreadId event to its issuer.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`,
  branch `ssx3`: T22's `6359fb6` is HEAD (or table what
  moved — record don't fight; rebuild ONLY if the tree
  moved, `-j4`, table binary sha + size; verify EVERY hook
  in the linked binary before booting — E2a G1 lesson:
  `strings`/symbol/disassembly receipts for the pc= path).
- The run: ONE pc=-on boot, cap 600 s (in-phase + early
  steady suffices: GetThreadId runs ~20/VBLANK from boot,
  so ~600 s yields ~700k attributable events; still
  complete if shorter — table what the stream holds).
  Channel env: `PS2X_TRACE_SYSCALLS` ON + `PS2X_TRACE_
  SYSCALLS_PC` ON (record both). No trigger, no monitor
  beyond liveness (this is a capture boot, not an exit
  boot — but DO record whether the T16 exit shape holds
  to cap: match/no on block-235 collapse only).
- Reference streams (read-only): `syscalls-t18-on.txt`
  (pc=-off shape to diff against). Tools: `tools/
  trace_align.py` (selftest must re-green; `--format-check`
  must accept pc=-on lines per T22's parser-compat row).
- The question: (a) pc= boot proof (off-shape byte parity
  argument via ts-normalized diff pc-on-minus-pc= vs a
  pc-off boot — one boot only, so: pc-on lines minus the
  suffix must parse + match T18's opening event sequence
  exactly); (b) GetThreadId per-site split (each of the
  21 A0 sites' event counts from pc= attribution — which
  sites carry the 20/VBLANK? is the SYNCTASK cluster
  confirmed?).

## Gates and rules

- Lease `T24`: the boot ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/t24-waits.log`. Max 1 boot ≤600 s foreground.
  Builds any time (`-j4`). No `adb`.
- ZERO fork changes (the pc= field exists; use it, don't
  modify it. No source edits, no commits, no push).
- Evidence dir `local/research/T24/` (STANDALONE). Commit with
  `git add -f`, prefix `[T24]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 4 h. Tables, no verdicts.
- Hygiene (F3 rec 17): write the report in chunks with a
  tail receipt (truncated tail fails the gate); boot script
  gets a guest-progress cap (exit-past or event-count) ALONGSIDE
  the wall cap (first brief under the progress-cap rule —
  table both caps + which bound).

## Task 1 — pc=-on capture boot (or cap)

1. Pre-claim checks (T13 §T13-0 verbatim + pc=): lease
   absent, `pgrep -x ps2EntryRunner` exit 1, binary sha
   (T22's `7f155f4c…` unless rebuilt), pc= linkage
   receipts IN the binary (E2a G1 lesson), ISO + ELF
   sizes, free space, aligner selftest green, waits-log
   tails, BOTH caps recorded (600 s wall + event-count).
2. Claim → foreground boot (both env gates set; liveness
   monitor only) → at whichever cap binds: SIGTERM →
   release AT ONCE → trace copy (lease-free) → analysis.
3. Epoch table: T16 block-235 collapse to cap? (match/no —
   capture boot, not exit boot; label the stream window).

## Task 2 — boot proof + site split (what does pc= show?)

1. Proof table: pc-on stream (events, bytes, 200/200…
   full-file `pc=` line share) + suffix-stripped diff vs
   T18's opening sequence (exact-match run length — or
   first divergence tabled) + `--format-check` row.
2. Site-split table: GetThreadId events per A0 site (21
   rows: pc + enclosing function + count + share — which
   sites carry the storm? SYNCTASK cluster share?).
3. FlushCache table: same split for 0x424020 events (is
   steady state really only 0x382930? any silent-site
   activity?).

## Report

`local/research/T24/REPORT.md`: T13-0..T13-7 SHAPE (lease
record, epoch table, proof/split tables, exact commands,
gaps). Commit evidence (`[T24]`), remove the lease if held,
stop.
