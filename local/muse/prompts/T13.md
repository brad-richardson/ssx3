# T13 — Run-to-exit at 1800 s: empirical N or the fourth absence window (1 long boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T11/REPORT.md` first (all of it —
this run extends T11's 600 s absence window 3×: N > 29,469, all 5
first-post-phase signatures absent, all baselines kept), plus P1
REPORT Part 31 §P31-6c (the 5 signatures) and T11 §T11-5 (the
ladder this run extends). The phase is pure compute with no exit
signature and no readable bound (T10); longer windows are the
only empirical path to N. Peer lanes run concurrently — T12's
proof boots yield to NOBODY (see lease rule).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight).
- Fixed baselines the run must reproduce before exit (or table
  the deviation exactly): 54-frame modal cycle (T10); 20/19/1/
  2534 per invocation (P1ah); stubs 222; sema-30 4w/3s parked;
  FRR ×N (T8); wall-rate shape recorded (T11: 64→36/s decay).
- Exit = any of P31-6c's 5 signatures (new stub phase, 4th
  sema-30 signal + thread-3 release, `0x362DE8` halt, new
  caller, dma/gif slope break). Cap = 1800 s (fourth absence
  window with a 3× tighter bound — still a complete result).
- T11's monitor started 330 s late (tool-launch latency —
  T11 §T11-0 gap): start the live monitor WITH the boot this
  time (background it before the foreground boot, not after).

## Gates and rules

- Lease `T13`: boots ONLY while holding `/tmp/ssx3-p-lane-lease`
  (the SHARED P-lane lease) — T12's TWO ≤90 s proof boots have
  ABSOLUTE priority: check for a T12 wait notice
  (`$W/P1/run/t12-waits.log` tail) before claiming; if T12 is
  waiting or holding, yield the full window to T12 first (log
  your waits to `$W/P1/run/t13-waits.log`, poll every 5 min).
  Max 2 boots: ONE run-to-exit ≤1800 s foreground + ONE
  optional ≤90 s confirm (only if the first exits the phase).
  No `adb`.
- ZERO fork changes (no source edits, no commits, no push).
  Build from a recorded tree state (`git log`, `git status`,
  binary sha in the report).
- Evidence dir `local/research/T13/` (STANDALONE — do NOT append
  to `P1/REPORT.md`). Commit with `git add -f`, prefix `[T13]`,
  trailer `Orchestrated-By: Muse Code`. NEVER run `git push`
  in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — run to exit (or cap)

1. Record tree state, rebuild `ps2EntryRunner` (`-j4`), record
   binary sha. Check T12's wait state; claim the P-lane lease;
   start the live signature monitor FIRST (background, 15 s
   polls, same 5 signatures + trace-stall trip as T11);
   run ONE boot ≤1800 s foreground (P1ag/T11 env/scripts
   verbatim: `PS2X_DIAG_394ED0=1` kept; sed the LOG name).
   Release the lease immediately at exit-or-cap.
2. If the phase exits: kill at first post-phase event + 30 s
   grace; table the exit (which signature, line + block +
   invocation ordinal = empirical `N`).
3. If the cap hits: table the absence window (final ordinal,
   cycle count, stub/thread/sema end state).

## Task 2 — exit-or-bound tables (T11 shape, extended)

1. Empirical `N` (exit case) or bound (cap case) + per-60 s
   rate table to the end (does the rate inflect? — tabled).
2. First-post-phase table (exit case): thread states, new
   drops/RPC/CD/SIF/GS rows vs the T11 ladder — one ladder
   table, exact-vs-baseline per row.
3. Baselines-kept table (both cases): modal cycle, 20/19
   constancy, stubs 222, sema-30 shape, FRR — per-row
   exact-vs-baseline.
4. Window ladder (T11 §T11-5 extended): P1af / P1ag / T11 /
   T13 rungs side by side.

## Report

`local/research/T13/REPORT.md`: lease record + tree-state +
exit-or-bound verdict tables + empirical-N (or bound) +
post-phase ladder (or absence window) + baselines table +
4-window ladder + gap rows. Commit evidence (`[T13]`, no
push), stop.
