# Q1 — research: how PCSX2's VU recompiler (microVU) handles pipeline hazards statically (local Qwen, dense, 2 h)

## Why
VR1 (running) compiles SSX 3's VU1 programs ahead of time but keeps our interpreter's dynamic hazard scoreboard (25.7 ms of a 121 ms Odin race frame, NP1). "Stage B" would precompute stalls per basic block (`local/research/E57/REPORT.md` "Static recompilation…" step 3). PCSX2's microVU already does this in a JIT. We want its design as input, with sources.

## Questions (answer each with citations: a URL **and** a verbatim quote of ≤ 3 lines, or a file:line in PCSX2 source)
1. How microVU computes stalls: the analysis pass over a block (`mVUanalyze*`), what state it tracks per instruction (FMAC/FDIV/EFU/IALU pipeline latencies, register write-back cycles, Q/P registers), and how it handles unknown state at block entry.
2. Flag pipelines: how MAC/status/clip flag reads (FMAND, FSAND, FCAND, FMEQ…) see "4-cycle-old" flags; how microVU decides which flag instances to keep.
3. Block boundaries and caching: what a block is, how blocks are keyed (program memory hash? start PC + state?), what happens on a branch into the middle, and the E-bit/D-bit/T-bit delay slots.
4. XGKICK timing and the VU1→GS path: does microVU model XGKICK progress per cycle or at kick time?
5. Known accuracy shortcuts microVU takes (options like "VU flag hack", "VU1 instant", "MTVU") and what they trade.

## Sources and rules
- Use your `web_search` tool and web fetch for: the PCSX2 GitHub repo (`pcsx2/x86/microVU*.h/.inl`, `microVU_Analyze.inl`, `microVU_Flags.inl`, `microVU_Branch.inl`), PCSX2 wiki/forum/dev blog posts, and any papers or talks. Prefer the source over blog posts. Read-only local copies may exist on bytesize (`ssh bytesize`, `~/pcsx2-g7`); use them only if the web copy is unreachable.
- **Every claim needs a citation.** No citation → write "not found". Don't guess how our runtime works; you're describing PCSX2 only.
- Output file (write it early, refine it): `local/research/Q1/REPORT.md`: one section per question with cited bullets, then a short "Mapping to our stage B" list where each item names the PCSX2 mechanism and the citation (no claims about our code). Commit `[Q1] …` with explicit paths (`git add -f`) and trailer `Orchestrated-By: opencode`. No push. Don't edit anything else.
