# I4 — SDL2-for-iOS build + raylib-SDL integration to a runtime configure

You are muse in a herdr panel, working directory
`/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp` (fork, branch `ssx3`,
remote `fork`). This brief executes raylib-wall Option C (user chose SDL
over upgrade/fork-patch): build SDL2 for the iOS SDKs, integrate it with
raylib's `PLATFORM=SDL` path, and drive the runtime to a configure (+ build
if it passes). **Tables, no verdicts.** Read
`/Users/bradrichardson/dev/ssx3/local/research/I3/REPORT.md` first (all of
Step 3: C2 configure shape, TU probes, the SDL-version wrinkle, open
questions C1–C5) plus I2 (`local/research/I2/REPORT.md`: toolchain v2,
G4/G5/G6 fixes now in the fork). Rules: no lease (no emulator boots);
no `adb`; one commit per change with the two P-series trailers; push rule:
`git push` ONLY inside this fork clone to the `fork` remote, NEVER in ssx3.
Disk: internal disk is tight — ALL clones/builds/installs go on
`/Volumes/Extreme SSD/` (new dir `ps2x-i4/`); nothing over 500 MB on
internal; never touch `/tmp/p1-link`, `/tmp/ps2xgs-build*`,
`/tmp/ps2x-ios-spike`, `ps2x-i3`, or other agents' dirs. Simulator: use
ONLY the installed iOS 27.0 SDK/runtime + existing devices; NEVER download
runtimes or platforms. Network reads + source clones allowed; no pushes
outside `fork ssx3`, no PRs. Time box 6 hours.

## Facts you start from

I3 proved: raylib 5.5 `PLATFORM=SDL` + `OPENGL_VERSION="ES 2.0"`
configures against the iOS 27.0 Simulator SDK (C2 exit 0, glad-bundled
GLES2, no system headers needed); `rcore.c` TU-compiles clean;
`raudio.c` needs ObjC mode; ES3 needs ANGLE/shim (stay ES2 in this
brief). Blockers this brief closes: no SDL2-for-iOS library exists on
hand (host brew SDL is macOS-only); no CMake integration (C1 scoping);
entry/lifecycle mapping untested. A P1u agent shares the fork tree
(different files) — pull --rebase-and-retry-once on push reject, else
leave unpushed and report. Scope: simulator arm64 first; device slice
(`ios-arm64`) only if the simulator path completes with box time left.
No Simulator boot/launch in this brief (binary + lifecycle mapping come
first; I5 owns first launch).

## Step 1 — SDL2 for iphonesimulator (no fork writes)

Build SDL `release-2.32.10` for iphonesimulator arm64 on the SSD
(CMake + I1 toolchain as a starting point, your call on exact flags —
record them). Receipts: configure + build logs (tails), the produced
lib + headers layout, `SDL2_DIR`-style config usable by a consumer,
exact commands. If SDL2 fails, record the first error and STOP after
one documented retry variant (do not thrash) — the report then carries
the SDL2 blocker instead of Step 2.

## Step 2 — raylib-SDL against it (no fork writes yet)

Configure pinned raylib 5.5 `PLATFORM=SDL` + ES2 with the Step-1 SDL2
(C2 shape + `SDL2_DIR` pointing at the Step-1 install): full raylib
static-lib BUILD for the simulator (not just syntax probes).
Receipts: build log tail, produced `libraylib.a` (arch check),
`raudio.c` ObjC-mode handling recorded (how CMake selects it), first
failure if any (stop at the first, one retry variant max).

## Step 3 — fork integration (CMake-only commits, then configure)

Minimal fork changes so the runtime's raylib FetchContent (or
`SDL2_DIR` passthrough — your call, record why) consumes an
SDL2-for-iOS: one commit per change, desktop build MUST stay green
(rebuild + `ps2xTest` 424/425 re-confirm, same single pre-existing
failure). Then configure the RUNTIME for iphonesimulator (I2 toolchain
pattern, new SSD build dir). Receipts: diffs + desktop-proof table,
runtime-configure pass/fail with first error; if configure passes,
attempt the runtime build and record its first failure the same way
(one retry variant max per failure).

## Step 4 — report

`local/research/I4/REPORT.md` in `/Users/bradrichardson/dev/ssx3`:
per-step tables (SDL2 build, raylib build, fork diffs + desktop
proof, runtime configure/build with first-failure records), exact
commands, receipt paths, "What I could not do" (entry/lifecycle
mapping, device slice, first launch → I5). Commit with `git add -f`,
prefix `[I4]`, trailers; stop.
