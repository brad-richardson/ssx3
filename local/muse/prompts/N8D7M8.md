# N8D7M8 — execution witness for the original Odin selected copy (read-only, OpenCode Go)

You are the N-lane source-design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, N8D7M6 and corrected N8D7M7 `REPORT.md`/`ORCH-GATE.md`, and N8D7M5 `ORCH-GATE.md`. Pins: fork `~/dev/ssx3-work/N8D7L/PS2Recomp` at `a8cfefa`, paraLLEl source `~/dev/ssx3-work/N8D7F/parallel-gs` at `3a66c19`, one Odin capture SHA `f6a78f71…a593` (Mac selected 300/448 active, Odin 23/448). Confirm C++ links with LSP if available, direct source reads otherwise. Own only this brief and `local/research/N8D7M8/`; no source edit, build, replay, device action, lease, push, board/global config edit or upstream contact. Time box 20 min, new text <512 KiB.

Goal: design a **nonperturbing execution witness** that says whether relevant tick≤2050 GPU draws completed before the *existing* selected-VRAM copy was executed, using the original Present command and no extra flush, submit, wait or changed packet order. N8D7M7 established that the 4 MiB selected copy is recorded before circuit sampling, and rejected a second post-wait flush: it would execute additional work and cannot distinguish late submission from missing writes. N8D7M5's CPU packet histories prove packets can write selected addresses in a different renderer, not that Odin GPU writes completed. No raw word or 8-word set may stand in for a full 448-tile image.

Trace, with exact source lines, one known selected-address packet from N8D7M5 through EE queue → GS worker → GIF transfer/draw setup → renderer command recording → GPU submit/timeline completion → selected `copy_buffer` → host wait/map. Identify where packet/tick identity is lost and whether a GPU-side query/timestamp/debug marker, page write audit, or same-command full-buffer sample can bind **executed** work to the copy without changing its order. If a candidate only counts queued packets, recorded draws, or host submits, label it insufficient. If adding a query/barrier itself could change visibility or order, say so. Give bytes, frame/log caps and exact default-OFF hook points for a single bounded Mac validation before any Odin package.

Predeclare **A** = a source-grounded witness with a unique observable for each of (i) relevant GPU writes complete before the original copy but copied snapshot sparse, (ii) those writes complete after that copy, (iii) writes never complete for the original Present; include positive/negative controls and a full 448-tile snapshot comparison. **B** = source does not expose a sound three-way witness without an implementation experiment or device-specific extension; give the smallest next *Mac-only* measurement and what it can settle. **OTHER** = pin/citation/call-path gap. Equal predictions → B, not A. Do not declare a Turnip, shader or barrier cause from static code.

Deliver `REPORT.md` with a command-order table, candidate witness table (recording clock vs GPU execution clock, observational vs intervention, each hypothesis's prediction), explicit missing links, and recommended next action. `check.py` should check pins and cited source rows and reject any A table that relies only on host enqueue/submit counts, 8 words, later tick or an extra flush. It must say that it cannot prove execution from source strings alone. Commit `[N8D7M8]` with `Orchestrated-By: opencode`, no push. Hand back the table; **the orchestrator gates**.
