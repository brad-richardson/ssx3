# E26 — Spend the designed e24a boot (X: the missing start code + sema-36 signaller)

You are the E-lane worker (spawned under claude-cli opus) in the ssx3
repo (derive the repo root from this prompt's path: two up from
`local/muse/prompts/`). **Tables + receipts, no verdicts beyond the
missions.** Read first, all of each: `local/research/E24/REPORT.md` +
`NEXT-BRIEF.md` (the FULLY DESIGNED 230-entry e24a boot — argv,
preflight gate, watch-set, fix gate STOP), `local/research/E25/
REPORT.md` (the rebuild: runner `e462e448…` + suite `2152e5ad…`
BIT-IDENTICAL, no re-baseline; restore procedure + recipe), and
`local/research/E15/REPORT.md` §no-input + `local/research/E16/
REPORT.md` §closure (the semantics the boot must preserve). X is
unchanged: the feed is one structurally complete picture missing only
its terminating start code — **why does the guest stop one start
code short?** — and, separately, why does semaphore 36's only
signaller never run.

## Facts you start from

- MACHINE MIGRATION: this brief may run on the Mac mini (new host),
  not the machine E25 ran on. NOTHING in `/tmp` survives the move.
  Mission 0 exists for exactly this reason — do not assume any
  `/tmp/e18-mpeg-link` state; VERIFY then restore.
- ORCH DECISION (gated): E25's re-baseline table is ACCEPTED — the
  1725-pin is CLOSED as superseded by the 575 rebuilt-tree pins +
  snapshot. E26 spends E24's designed e24a boot UNCHANGED (argv,
  preflight gate, `PS2X_DIAG_SEMA` design all hold as written).
- I-lane P4 (gated): the V3 vector serves ONE 512×448 frame via
  parser-flush drain on host AND device (RGBA `048b41af…`
  byte-identical) — the decoder is exonerated; X is an input-shape /
  guest-behavior question, not a decoder question.
- Standing SSD rule (SanDisk link artifacts): lane-critical SSD bytes
  need 2+ matching reads separated in time (or independent
  corroboration) before entering a gate; single-read SSD evidence is
  suspect. Dual-home critical restores (SSD + share tier).

## Gates and rules

- Fork: `3adc0478` triple-agree FIRST (HEAD + `fork/ssx3` +
  `ls-remote`); `status --short` exactly `?? ps2_log.txt` or table +
  stop. ZERO fork source edits, ZERO fork commits, ZERO pushes from
  this lane.
- P-lane lease: e24a is a title boot — claim the lease per the
  standing protocol, exactly one boot this brief, release at end.
  No second boot for any reason (no retry shape, no "quick check").
- Fix gate: E24 set it to STOP and designed the boot around it —
  honor it exactly as written (diagnose + table, never patch).
- E18 ABI/BINDING constraints preserved (caller-owned sync dispatch,
  word0-only cbData, `v0` discarded, valid-no-input waits, dispatch
  outside the MPEG mutex, delete/reset cancellation).
- Byte caps: internal reservation 3 GiB (E25's declared value for a
  lane that builds; floor 2 GiB + 0.5 GiB guard unchanged); SSD NEW
  `e26-*` paths ≤ 16 GiB; evidence `local/research/E26/` text-only;
  `COPYFILE_DISABLE=1` on every SSD step; zero deletions.
- Time box 8 h. Evidence commit `[E26]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 0 — Restore + preflight (no boot)

1. Verify-or-restore the instrument, in this order: (a) if
   `/tmp/e18-mpeg-link/runtime/ps2xRuntime/ps2EntryRunner` exists
   AND re-shas `e462e448…` (163,529,696 B) twice, keep it; else (b)
   full rebuild per E25's recipe (`local/research/E18/
   configure-command.json` argv + `ninja -j2`, ~10 min measured) —
   preferred, restores pins too; or (c) two-binary restore from
   `/Volumes/share/ssx3/e25-restore/` (re-sha BOTH against the pins
   before use — sufficient for the boot, pins unrestored, SAY which
   path you took).
2. Run E24's capture preflight gate AS WRITTEN against all five pins
   (runner, suite, both ELF copies, observer dylib) — all five must
   pass with zero re-pinning, or table + stop.
3. Re-verify `PS2X_DIAG_SEMA` is compiled into the runner you will
   boot (strings check), and that the boot argv's exact paths exist.

## Mission 1 — Spend e24a (ONE boot)

Execute E24's designed e24a boot EXACTLY as designed (argv,
environment, 230-entry watch-set + tiers, capture window, teardown).
Pre-register NOTHING new — E24 pre-registered everything; your job is
fidelity. Record every line of the watch-set, the wall/rc/fate, and
the lease claim/release receipts.

## Mission 2 — X first light (read-only analysis)

Against the e24a capture ONLY (no new probes, no second boot): (a)
where does the guest's final start-code write land relative to the
feed the parser consumed (timing + bytes)? (b) does semaphore 36's
sole signaller appear anywhere in the capture (function-log +
4-instruction closure from E24)? Table the answers with byte/line
receipts; if the capture cannot answer, say exactly which designed
follow-up would (that follow-up is E27's, not yours).

## Success bars (tabled)

Restore path + preflight 5/5; ONE boot with wall/rc/fate + full
watch-set; X (a)+(b) tabled with receipts; fix gate honored; tail
receipt with byte-exact prefix sha; zero fork edits/commits/pushes.
