# G39 — G26+G28 regression/adoption gate: prove the isolated crash fixes hold, recommend adoption mechanics (NO new hunks)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS` — VERIFY at start, re-pin if
the Mac-mini migration moved it, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts
beyond the hypothesis.** Read first, all of each:
`local/research/G26/REPORT.md` (debug_mode narrowing: ONLY the flag,
O6 persists byte-identically, flag path IMPLICATED), `local/research/
G28/REPORT.md` (create_image_view writer fix: exit 0, O5/O6 gone,
black separate trait), and `docs/research/
review-2026-09-22-progress-and-parallelization.md` §G-lane (adoption
gets its OWN regression/adoption gate and must NOT wait on later
experiments' interpretation). This brief spends that gate. The
sub-vsync F1/F2/F3 wall is G40 — explicitly OUT here. No upstream
contact of any kind (standing no-upstream order — local hunks only,
filing stays local).

## Facts you start from (gated — do not re-litigate)

- G26 isolated the flag path (field surgery + swapped LOGI, +8/−1):
  exit 134 with receipt `:29` (=1,=0,=0), 18 frames, O6 stack
  byte-identical to the un-narrowed crash, feedback writes gone.
- G28 fixed the writer (`create_image_view` guard in Granite's
  `memory_allocator.cpp`): exit 0, O5/O6 gone; black output is a
  SEPARATE trait (now explained: cleared-from-stale-B is correct).
- Both hunks are still UNCOMMITTED in the SSD clone only. Every
  G29–G38 binary compiled them in (guard engaged receipts where
  applicable). "Adoption" does NOT mean upstream and does NOT mean
  editing the fixes: it means (1) regression proof that each fix
  still holds its defect closed on current HEAD + (2) a
  no-perturbation proof that valid behavior is unchanged + (3) an
  adoption-mechanics RECOMMENDATION (carried-diff vs in-clone
  commit — recommend, do not execute either; the orchestrator
  decides).
- Standing SSD rule (link PROVEN pattern-dependent corrupt):
  lane-critical SSD bytes need 2+ matching reads separated in time
  AND independent corroboration; single-read SSD evidence is
  suspect. Mirror new receipts to the share tier.
- Process correction (applies): table "the implementation is
  behaving correctly on these inputs" as an explicit alternative
  wherever a run's output is scored — equal predictions cannot
  discriminate.

## Gates and rules

- Task 1 (no device until §2): STATIC gate design — table (a) each
  fix's defect receipt (what failed before, with line numbers from
  the G26/G28 reports); (b) the regression observables (which
  receipt proves the defect STAYS closed, with exact expected
  bytes/lines — G28 guard-engaged receipt, exit codes, O-absence);
  (c) the no-perturbation observables (which valid outputs must be
  byte-equal to their adopted baselines — name the baselines:
  which committed run's which receipts); (d) the decision matrix
  (hold vs regressed vs perturbed, per fix, with triage rules).
  Name the binaries under test (prefer REUSE of valid binaries —
  state which + their pins; rebuild ONLY what no valid binary
  covers, with the reason).
- Task 2 (bounded runs): regression runs on Odin, SAME shapes as
  the original defect demonstrations (G26's narrow run shape, G28's
  writer run shape — quote the shapes from the reports, do not
  redesign them). Hunk budget: ZERO (no new hunks, no excisions —
  the worktree already carries exactly the fixes under test; if it
  does not match G38's end state, table + stop). Build budget:
  ZERO unless no valid binary exists for a shape (then ONE build,
  new dir, G22 recipe, `-j2`, full identity, verify-then-push NO
  gap). Run budget: TWO bounded Odin runs max (one per fix shape).
- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g39/` transient
  dir ONLY, removed at end (`mg/` only). No P-lane lease, no
  bytesize/WSL. `COPYFILE_DISABLE=1` on all SSD steps.
- SSD clone: ZERO hunks this brief (read-only worktree + HUNK_MATCH
  re-verified pre + post + pre-push + post-run + report); zero
  commits in submodule, zero in ps2xGS. You do not commit the
  adoption — you recommend its mechanics.
- Byte caps: build dir (if any) ≤ 6 GB; NEW SSD `ps2x-g39/`
  (retrieval by explicit list) ≤ 50 MB; all older SSD dirs 0 growth
  (pre + post `du -sk` must match exactly); internal volume ≤ 1 GB
  delta, no clones/builds; committed text only.
- Stop: any validity-chain pin mismatch → table + stop; no tuning
  loop, no second shape per fix, no lldb unless a tombstone needs
  triage. A REGRESSED fix is a finding, not a failure — table it
  with the discriminating bytes and STOP that leg (do not repair).
- Filing stays held (its content must drop the sampling inference —
  owner call, not this lane).
- Time box 6 h. Evidence `local/research/G39/` + `[G39]` commit +
  `Orchestrated-By: Muse Code` trailer, `git add -f`, NO push
  (orchestrator pushes at poll).

## Success bars (tabled)

Per-fix defect receipts + regression observables + baselines +
matrix (Task 1); per-fix HOLD/REGRESSED verdict with bytes +
no-perturbation verdict (Task 2); adoption-mechanics recommendation
with risk + rollback notes (carried-diff vs in-clone commit —
orchestrator decides); tombstone census; the ONE next action (G40
sub-vsync wall queued behind this gate).
