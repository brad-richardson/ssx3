# P8 — Input/audio API surface survey (pad + sound, read-only)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Context: the user wants pad/sound work parallelized ahead
of menu work; implementation waits for runtime contact (P1w's `[drop]`
census + the ladder), but the static surface can be mapped now. No fixes,
no fork writes. P1w/P1y/P7/I7 run concurrently in other panes — you share
nothing with them.

## Facts you start from

No brief has mapped what input/audio APIs the game calls (scePad*?
sceSd*/sceSdr*? libsd? IOP modules?) or what the fork does with such
calls today. The ladder has NOT reached pad/sound stages yet. The game
ELF is in the fork workdir (ask the tree, do NOT assume the path); the
ISO is at `/Volumes/Extreme SSD/ps2recomp-spike/SSX 3 (USA).iso`
(read-only).

## Gates and rules

- READ ONLY everywhere: no fork writes/commits/pushes, no fork boots,
  no lease, no `adb`. Installed tools only, no downloads.
- Disk: internal disk is tight — scratch under
  `/Volumes/Extreme SSD/ps2x-p8/` (new dir) if needed. Never touch
  `/tmp/p1-link`, `ps2x-i*`, or other agents' dirs.
- Evidence dir `local/research/P8/` (STANDALONE — do NOT append to
  `P1/REPORT.md`). Commit with `git add -f`, prefix `[P8]`, trailer
  `Orchestrated-By: Muse Code`. NEVER run `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Game side (static).** Every input/audio-adjacent call the ELF can
   make: strings/imports/JAL census over pad (`scePad*`, padman),
   sound (`sceSd*`, `sceSdr*`, libsd, sndrv), and controller/rumble
   paths. Table each with address or import receipt + call count where
   countable.
2. **Fork side (static).** What exists today for each call in step 1:
   implemented / stubbed (what it returns) / silently dropped / crash
   (`Stubs/*`, `IopBios`, SIF/RPC paths — file:line receipts). Note
   which ones P1w's `[drop]` census will illuminate at runtime.
3. **Host backends (static).** What input/audio backends exist in-tree
   for the runtime (SDL? desktop? anything wired to host devices?).
   Table with file receipts. No implementation, no wiring changes.
4. **Work list.** The implementation-ready ordered list: which calls
   must work for MENUS vs which can wait for gameplay, each with the
   step-1 + step-2 row that justifies its rank. Gap rows with the exact
   next brief each needs (implementation briefs fire when the ladder
   makes runtime contact).
5. **Report.** `local/research/P8/REPORT.md`: the three survey tables,
   the ordered work list, exact commands, receipt paths, "What I could
   not do". Commit, stop.
