# E55D14 Part 1 — default-off GetDir path tap, source and test gate

You are the E-lane implementation worker, OpenCode Go. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full E55D13 `REPORT.md` and `ORCH-GATE.md`, E55D3 `REPORT.md`, and E55D12 `ORCH-GATE-P2.md`. Cite repo source/receipts, not the web. Use LSP to confirm definitions/references when available, then direct source reads. Own this brief, `local/research/E55D14P1/`, and a **new** private worktree/build under `~/dev/ssx3-work/E55D14P1/`; E55D3 worktree/build, codegen, all other lanes are read-only. This pane has scoped permission for that new private root only; test a tiny sentinel write there before source work and stop on denial. No boot, card seed/copy, device, lease, push, board/global edit, upstream contact, generated guest source in git or `git add -f` in the fork. Time box 40 min; one configure/build plus one repair if the single tap patch fails compilation; private root ≤3 GiB, text receipts ≤512 KiB; check mini 200 GB budget before/after.

**Goal:** create one bounded, dev-only `getdirpath` sibling record that reveals the raw guest query and its normalized query/pattern at the E55D12 post-choice GetDir. Keep the existing `getdir`, pad and mcread record schemas, their per-family `ord`, guest bytes/results, sort/copy behavior, and cap semantics unchanged. `PS2X_PAD_CARD_PROBE` unset/empty must remain default-off, with no I/O. A new sibling record will advance global `seq`, so do not assert old `seq` values. Pin exact new schema in `REPORT.md`; escape `\\`, quotes, newline/control bytes and bound each field/whole line so the 16 MiB cap remains hard. Include port/slot, vsync, raw path, normalized query, parent, pattern and host directory for the **normal formatted path**; for bad-port/unformatted, record raw path and a clear unresolved sentinel without moving normalization into those branches. Emit one sibling record for every GetDir exit without changing the existing status tap or guest behavior. Add focused unit tests for disabled mode, special-character escaping, normal path, early exits, ordered `ord`, and cap/no partial record. Source needs only `ps2xRuntime/include/ps2_e55d3_pad_card_probe.h`, `ps2xRuntime/src/lib/Kernel/Stubs/MemoryCard.cpp`, and `ps2xTest/src/ps2_e55d3_probe_tests.cpp`; stop if more source is needed.

Create a new fork worktree from exact E55D3 `bab6eb382673155ffd756fe8db265964eeff9703` (new branch `e55d14p1-getdir-path`), leaving E55D3 pinned. Configure a new Release/Ninja build with the E55D3 `build-taps/CMakeCache.txt` diagnostic/codegen/compiler choices and pinned dependency source dirs (inspect its cache; do not use it as an in-place build). Build `ps2x_tests` and `ps2EntryRunner` once; run the full suite from the fork root with the flag unset and the focused tests. Verify two matching SHA reads of runner, external codegen unchanged, runner-dir diff against `14b1e5cb` empty, fork changed-files list exactly the three named source files, disk/byte caps. If build fails, one correction only for this named patch and one rebuild; report every attempt. No diagnostic boot in this part.

Deliver `REPORT.md`, a checker script with computed source/receipt/test/runner pins, bounded logs and exact commands/SHAs/costs/gaps. A = source/tests/build and runner guard pass; B = bounded compile/test failure with exact row; OTHER = permission/pin/resource mismatch. Commit fork `[E55D14] Part 1` with `Orchestrated-By: opencode`, no push; commit ssx3 `[E55D14] Part 1` with same trailer, explicit named paths only, no push. Hand back the table. Do not claim a guest query, seed validity, card determinism or speed; a separate part owns the one empty-card boot after orchestrator gate.
