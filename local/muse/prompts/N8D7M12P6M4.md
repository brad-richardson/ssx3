# N8D7M12 Part 6M4 — permit the exact upstream runner stub

You are the OpenCode Go tooling worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7M12P6M3/{REPORT.md,ORCH-GATE.md}`, and all of `local/tooling/orch/source_manifest.py`. Own only that script, `local/research/N8D7M12P6M2/check.py`, and `local/research/N8D7M12P6M4/` REPORT/check result. No board/ledger/fork/config edits, package, device, emulator, network, mini lease, or push. Time box 15 min; receipts ≤128 KiB. Write REPORT early. Use LSP to confirm any code relationship you rely on; cite exact paths/lines and commands. No subagents.

Gate correction: P6M3 fixed excluded-cache traversal, but its blanket `ps2xRuntime/src/runner/` rejection cannot work on the real fork. Current private fork `/Users/brad/dev/ssx3-work/N8D7M12P5F4/PS2Recomp` has one **tracked upstream** file `ps2xRuntime/src/runner/register_functions.cpp` (438 B, SHA-256 `cf62c485072f07c230e60296b77afd733f130f587955fe608322939ebb87f068`); `git ls-tree -r 14b1e5cb` and HEAD both name blob `85cc2e348d60cfae0a4f220fc2a0535f7a756fa2`, and runner-dir diff against upstream is empty. The project forbids *generated or changed* in-tree runner files, not this exact upstream stub. `source_manifest.py` currently rejects any runner file in both snapshot and verify.

Correct model: allow exactly that path with exactly those bytes as a regular file, include it in the manifest and aggregate, and reject any added runner path or byte change. Enforce this in both snapshot and verify using a shared function; do not rely only on Git status (ignored/untracked generated files still matter). No broad exceptions. Add fixture rows that the exact stub passes, an extra generated file fails, and a changed stub fails on both snapshot and verify. Preserve existing 16 checks. Run one bounded **real fork-only probe** by supplying the real fork as `--fork` and three small existing repo source dirs for other scopes; max-files/bytes explicit, no full codegen/renderer scan yet. If the fork has unrelated transient changes, stop and report. Do not edit the fork. Put the real-probe manifest privately under ignored `local/research/N8D7M12P6M4/` and commit only REPORT/check result plus allowed script/checker. Include counts, aggregate, runner entry SHA, exact command and gaps. No APK/GPU verdict.

Commit `[N8D7M12] Part 6M4` with `Orchestrated-By: opencode`, no push. Hand back tables and gaps, not a readiness verdict.
