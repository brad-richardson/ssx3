# I17 — Close the 0x2c5358 indirect-target gap at the E12 base (deduplicate E12's row), re-codegen, re-link, re-probe on iPad

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I16/REPORT.md` first (all of it: base
stayed `ffdf58c` (case (b) — no E12 commit existed) + 7 ports + 1-line CSV
entry closed `0x2c5300` (`0x2c5300` executes mid-trace with successors
`→ 0x2c67a8 → 0x2d3928 → …`), guest reaches a STRICTLY LATER wall: indirect
JALR `0x2d3828 → 0x2c5358` (mid-function of emitted `sub_002C5338`
`0x2c5338–0x2c53b0`, head after `jr $ra` @`0x2c534c`/ds `0x2c5350`/nop
@`0x2c5354`, INTERNAL branches + tail `jr $ra` @`0x2c53a0`/`0x2c53a8` →
return, ends `0x2c53b0`, observed bounds `0x2c5358–0x2c53b0`, caller `jalr
$v0` @`0x2d3828` with record+offset table load, 0 register refs/case/file;
gap 1 = this brief). This brief executes I16's ONE next action — the same
1-line class as I11–I16. Reuse I16's worktree shape + device toolchain +
provision/install/ISO+env mechanism — table every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I16 app is STILL INSTALLED — plain `install` overwrites
  (closed 8× now). No fresh prompt needed for iPad installs. Confirm the
  iPad's state before installing (user sessions reported, not killed).
- BASE: create a SEPARATE fork worktree on the SSD at CURRENT `fork/ssx3`
  HEAD (table the hash — expect `9da21ff` or later; NEVER move it
  yourself). The E12 row HAS now landed fork-side — DEDUPLICATE, don't
  duplicate: verify E12's `0x2c5300` row bytes at HEAD (expect
  `sub_002C5300,0x2c5300,0x2c5320,0x20`, byte-identical to I16's `b07f738`
  row), then port I16's 7 non-`0x2c5300` commits (plist, CMake wiring,
  `0x395730`/`0x14f2a8`/`0x156750`/`0x243a80`/`0x3a0158` rows —
  cherry-pick if clean, else re-apply by hand + table the merged diff)
  and SKIP porting I16's `0x2c5300` row (already in base — table the
  dedupe explicitly with both shas). Check whether the E-lane owns the
  shared clone before touching it (one `worktree add` + read-only
  inspection only, I16 shape).
- The task: add the missing function entry for `0x2c5358` (I16's observed
  row `sub_002C5358,0x2c5358,0x2c53b0,0x58` — RE-VERIFY the internal
  branches + `jr $ra` @`0x2c53a0`/`0x2c53a8` bounds from the emitted slice
  BEFORE writing the row; table the verification), then re-run host
  `ps2_recomp`, re-link `ps2_game_objects` (`-DPS2X_GAME_CODEGEN_DIR`),
  rebuild the device runtime, install over the I16 app, and re-probe with
  the ISO+env recipe. One gap per brief: if a SECOND new wall appears,
  table it as the next gap — do not close it here.
- Success bars (tabled, not verdicts): E12-row dedupe verified (HEAD row
  bytes + I16 row bytes both tabled); entry fix lands in the worktree
  (named files, local commit; fork-remote push ONLY if the E-lane is not
  mutating — else stays local, tabled); host codegen re-run exit 0 with
  the `0x2c5358` entry present (file + table-slot census); device Release
  build WITH game objects exit 0; overwrite install exit 0; argv-probe
  launch gets PAST `0x2c5358` (console + `.ips` + screenshot, tabled
  against I16's fate); diagnosis names the next layer with evidence if a
  new wall appears.

## Gates and rules

- Subordinate to the E-lane fork owner (if live): builds `-j2`, never
  `-j4`; build trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i17/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`. Host `ps2_recomp` runs are codegen (no lease),
  bounded + tabled.
- Fork discipline: worktree only; named-files `git add`; fork commits
  pushed to the FORK remote only, never upstream (and no `ssx3` push while
  the E-lane mutates); generated sources never staged (recompiler OUTPUTS
  are build artifacts — table paths + shas, do not commit them).
- Device discipline (extends I6–I16 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I17/` (STANDALONE). Commit with `git add -f`,
  prefix `[I17]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — dedupe + entry fix + host codegen + device link (no iPad yet)

1. Pin at `fork/ssx3` HEAD (table hash); verify E12's `0x2c5300` row bytes
   at HEAD + table the dedupe vs I16's row. Port I16's 7 non-`0x2c5300`
   commits (table method + merged diff). Re-verify the `0x2c5358` bounds
   from the emitted slice; write the CSV row (named add, local commit).
2. Re-run host `ps2_recomp` (rebuild the tool at the new base ONLY if the
   old binary provably cannot run the new inputs — table the decision);
   table codegen exit/counts/shas + `0x2c5358` entry census.
3. Configure + build Release (`-j2`); table binary path/size/sha/identifier
   + table-populated proof (symbol census). Fix ONLY build breaks in the
   wiring class.

## Task 2 — overwrite install + re-probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I16 app (table overwrite behavior).
2. Ship ELF+ISO as in I16 (same mechanism unless the link changed the
   packaging — table deltas). argv-probe launch with ISO+env: console +
   `.ips` (sleep + re-`ls` before concluding none — I16 filed none, so
   the first `ls` also checks for late I16 filing) + screenshot.
   Past-the-wall table vs I16 (`0x2c5358` gone? first post-entry code
   executes? new wall?).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled past the target: diagnosis table (failing layer + evidence).
   Attempt a fix ONLY if config-level — no second fork-code gap in this
   brief (one gap per brief; the next gap gets the next brief).
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I17/REPORT.md`: contract, caps, dedupe + entry-fix +
codegen + link record, install/probe tables, diagnosis, exact commands,
gaps. Commit evidence (`[I17]`), stop.
