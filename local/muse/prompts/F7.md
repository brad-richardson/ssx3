# F7 — device build with MTVU + VU1 blocks + the fixed Vulkan present (muse)

## What's in it
Fork `ssx3` **`703a554`** (pushed): MT1 MTVU (`PS2X_MTVU`, `PS2X_MTVU_LAG`, `PS2X_MTVU_CPUS`; default off), VR2 stage 4 VU1 blocks (`PS2X_VU1_BLOCKS`, default off) + VB1 stop drain, VK2 present ledger (Vulkan present default on for Android), plus F6's contents. VU1 images: canonical `~/dev/ssx3-work/vu1gen-ssx3` (set `d28e3fc6…`, VR2's new set — the old set won't compile). paraLLEl fork `ssx3` `1b3a294`. Reports: `local/research/{MT1,VR2,VK2,F6}/REPORT.md`.

## Part 1 — Odin measurements (stop before the play install)
1. APK: reuse MT1's `~/dev/ssx3-work/MT1/odin/app-release.apk` (`b91f827f…`, built from `703a554` with these inputs; two SHA reads ×2). Rebuild only if the SHA check fails (MT1's `local/research/MT1/build-android-mt1.sh` recipe on bytesize, hold the ssh).
2. **Odin lease through `local/tooling/odin_lease.sh` for every install/launch/force-stop.** BA1 may hold the Odin: wait for `LEASE_FREE`.
3. Runs (F6 method: `local/research/F6/launch.py` + `cooldown.py`, I26-FAST, unpaced, sound on, pipelined 1×, stop 4500, cool to status 0 + fixed 180 s; all env via `--env`; CPU pins `PS2X_GAME_THREAD_CPUS=6 PS2X_MTVU_CPUS=7` on every leg):
   - C0 census: `PS2X_MTVU=census PS2X_MTVU_CENSUS_OUT=<device path>` (pull the file; one run).
   - Speed legs, one each, then repeat the best two as an ABBA pair: **A** knob 0; **B** `PS2X_VU1_BLOCKS=1`; **C** `PS2X_MTVU=1`; **D** `PS2X_MTVU=1 PS2X_VU1_BLOCKS=1`; **E** D + `PS2X_MTVU_LAG=1`. (≤ 9 launches total incl. C0.)
   - Per leg: race rate + per-phase table, GPU busy, thermal, `[mtvu] threaded` summary lines (waits, violations — must be 0), `[vu1-blocks]` if present, `[present-vk]` counters, one race screencap viewed.
4. Restore Brad's play state after every run (`local/research/F6/restore-play.sh`). **Stop and hand back the table.** The orchestrator picks the play-build knobs (LAG's default is Brad's call; it stays off unless he says otherwise).

## Part 2 — iOS (may run alongside; stop before the iPhone install)
F6's recipe `~/dev/ssx3-work/F6/ios/build-install.sh` (MoltenVK now at `~/dev/ssx3-work/moltenvk-1.4.2`; point `PIN`/worktree at `703a554`, `VU1GEN` at `vu1gen-ssx3`). Bundled env = F5's `local/research/F5/ps2x.env` **plus** `PS2X_MTVU=1` and `PS2X_VU1_BLOCKS=1` (copy to `local/research/F7/ps2x.env`). iPad: install, `deploy-ios.sh ipad`, one fresh-card test launch (read the live container path first), frames viewed (race, trail, pad), `[mtvu] threaded` line present with 0 violations, terminate. Also one iPad launch with both knobs forced off (`-e PS2X_MTVU=0 -e PS2X_VU1_BLOCKS=0`) for a diagnostic pace comparison (label it diagnostic). **Stop before the iPhone install.**

## Rules
Brad's save untouched on every device; tests use empty scratch cards. Force-stop after every Odin run; keyguard/battery checks. iPhone: never launch. Text only in git. Scratch `~/dev/ssx3-work/F7/` ≤ 20 GB. First failure: stop, save the error, hand back.

## Deliverable
`local/research/F7/REPORT.md` (pins, SHAs, Odin table incl. census, iPad results, screencaps viewed, gaps) + `[F7] Part N …` commits (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push.
