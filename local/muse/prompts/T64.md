# T64 — PCSX2: catch the 0x1b0 write into template 2 (churn-excluded rerun of T63 run 2)

Same rules and tree as T63. Read `local/research/T63/REPORT.md` first.

T63 bounds the write: it's an EE store into `0x61c910` (template 2 w0) in
run-2 vsync (6, 1121]. The 2000-line cap died at vsync 6 on the
`0xc↔0xcc` churn.

## Change (on T63's tree)

1. For template-2 w0 (`0x61c910`) only: emit `tw` rows when the new value
   is **not** `0x0`, `0xc` or `0xcc`. Keep full regs, and add the 8
   instructions before pc as raw words, so the value's source (immediate
   vs load) is readable. Cap 200.
2. An uncapped per-vsync line: `t2 vsync w0=<last> n=<writes> pcs=<distinct
   writer pcs>`, from vsync 0 to the end.
3. Drop all other `tw` emission (keep the shadows).

## Run

1 build (+1 on compile failure). One capture: **run 2 only**, from
`t63-menu-state` → K → SC settled. G13 preservation.

## Deliverable

≤2 captures, 1.5 h, cap 1.5 GB. `local/research/T64/REPORT.md`:
- the first non-churn writer of `0x61c910` (vsync relative to K, pc, ra,
  the full regs, the 8 preceding instruction words);
- the `t2` per-vsync table around the change.

No verdict. `[T64]` commit (`git add -f local/research/T64
local/muse/prompts/T64.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
