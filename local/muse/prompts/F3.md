# F3 — Frontier Part 3 read: drain survives 3 briefs + E2/E3 firm-up review (read-only)

You are claude in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
You are the frontier reviewer (Parts 1–2 live in
`docs/research/review-2026-09-19-progress.md` — read the whole
doc first for shape: findings table + numbered sections + rec
table + concerns). This Part 3 fires on the standing rule
(drain/park survives 3 briefs: T15 → P1aj Part 33 → T16 E1,
now extended by P1ak Part 34) and lands BEFORE the first
deliberate guest-behavior change (E2a stimulus boots
concurrently — your read is advisory to the orchestrator, not
a gate on E2a).

## Facts you start from

- Evidence to review (read all): P1 REPORT Parts 33–34
  (drain fixed point + 13-row terminator table + 5×
  reconfirmation + D4/D5 unification + E2/E3 brief-shape
  rows), `local/research/T16/REPORT.md` (E1: no break to
  7200 s, 359,748 iters), `local/research/T18/REPORT.md`
  (trace channel + aligner, k=2 divergence),
  `local/research/T17/REPORT.md` (reference deepened to
  User Prefs), `docs/numbers-ledger.md` (top ~15 rows),
  `docs/todo.md` Now section (gate-read verdicts +
  errata: M60 13→9, P1ak 431897→431893).
- Standing questions: (a) is the VBLANK-paced fixed-point
  claim + terminator table sound at 5× evidence? (b) is E2a
  (pad stimulus) the sharpest next experiment, or should
  E3 (read-only probe) or E2b/E2c go first? (c) does the
  k=2 alignment change the park-break picture (HLE skips
  BIOS init — what does that imply for S1/S2/S3)? (d) any
  methodology concerns (errata rate, wall-vs-progress caps,
  WSL flakiness, trace-format limits)?
- Output: Part 3 in the SAME doc
  (`docs/research/review-2026-09-19-progress.md`, append a
  `Part 3 (2026-09-20)` section in the Part-2 shape:
  findings + rec table with disposition + concerns). Tables
  + judgments — this is the one lane allowed verdicts.

## Gates and rules

- READ-ONLY: no boots, no lease of any kind, no fork
  changes, no `adb`, no builds. Evidence is the review
  text only.
- Commit with `git add -f`, prefix `[F3]`, trailer
  `Orchestrated-By: Muse Code`. NEVER run `git push` in ssx3.
- Time box: 2 h. Stop when Part 3 is committed.
