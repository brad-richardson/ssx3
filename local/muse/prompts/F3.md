# F3 — fold FP1 + DK1 + I34 + IN2 + UV1 onto fork `ssx3`, then iPhone + Odin builds (muse)

Gates: `local/research/{FP1,DK1,I34,IN2,UV1}/REPORT.md`. Brad approved the phone install (install only; he launches it himself).

## Part 1 — fold + Mac check (stop after; the orchestrator gates and pushes)
1. Worktree `~/dev/ssx3-work/F3/PS2Recomp`, branch `f3-fold` from fork `ssx3` `0ed07c4`. `git cherry-pick -x` in order: `51e730f` (FP1 pacing), `6cba433` (DK1 alpha), `3d52208` (I34 pad v2), `43b61a4` (IN2 latch), `8545eb2 6acf5bb` (UV1). Conflict → stop, save hunks, hand back.
2. Suite from the worktree root; runner-dir check empty. Mac Release build (F2 recipe, paraLLEl `19d93b2`, canonical codegen; no regen needed).
3. Boots (one slot each, I26-FAST, empty mc0, `PS2X_SKIP_MOVIE=1`, paraLLEl + `PGS_HIER_BINNING=force`, deterministic): **B1** sound on to t2400, frames ~1090/1800/2100 viewed (check brightness: the pre-present PNG alpha should now be 255), cid0 = 531, coverage 0/0/4. **B2** sound off + `PS2X_UNPACED=1`: det-hash identical to B1. **B3** speed, `--exclusive`, `PS2X_UNPACED=1`, per-phase table vs F2 B3 (race 0.250×).
4. **Android compile check** on bytesize (F2 recipe: refresh the PS2Recomp archive to `f3-fold`'s HEAD; codegen/paraLLEl/jni unchanged): `assembleRelease` must succeed; keep the APK (Part 2 uses it after the push, so rebuild from the pushed SHA if it differs).
5. `local/research/F3/REPORT.md` Part 1; commit `[F3] Part 1 …`. **No push.** Stop.

## Part 2 — iPhone (after the push)
I33 recipe (`local/research/I33/build-install.sh`, paraLLEl default, MoltenVK embedded, bundled env `local/research/I33/ps2x.env`) pinned to the pushed tip. iPad: install + `deploy-ios.sh ipad` + one fresh-card launch (live container path), frames viewed (brightness, pad v2 layout), terminate. iPhone: install + `deploy-ios.sh iphone`, **never launch**; if "unavailable", stop and report at once. Append `## Part 2`; commit.

## Part 3 — Odin (after the push; only when the Odin lease is free — NP1 may hold it)
APK from the pushed tip; two clean race runs with `PS2X_UNPACED=1` added to the F2 launcher env (vs F2 0.139×), screencaps viewed for brightness; then install as Brad's play build + `deploy-odin.sh`, no launch. Battery ≥ 20 % and thermal ≤ 2 before each launch; never toggle the screen. Append `## Part 3`; commit.

Rules: text only in git; never push; never `git add -f` in the fork; scratch `~/dev/ssx3-work/F3/` ≤ 15 GB (delete your build dirs when done); check/kill runners by PID. First failure: stop, save the error, hand back.
