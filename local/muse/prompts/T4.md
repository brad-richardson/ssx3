# T4 — PCSX2 Devel on bytesize + SSX3 reference trace (Brief 1 of 2)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `docs/research/review-2026-09-19-progress.md` §11.4–§12
first (the T4 row: PCSX2 Devel on bytesize + first-divergence
aligner — this brief is Brief 1: build + boot + capture; Brief 2
is the runtime trace channel + `tools/trace_align.py`, NOT this
brief), plus `local/research/T15/REPORT.md` (the park epoch to
reach: block-235 exit scale, N = 72,176 enters/exits, 31-drain
past block 241) and P1 REPORT Part 33 (drain-termination
tables: what the runtime does at the epoch — your comparison
target later, not now). User authorized 09-20; continue while
useful.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` from
  this laptop (verified 09-20, key auth, no password).
  Windows x86_64, RTX 4070, WSL2 Ubuntu. Build Devel PCSX2
  natively: WSL2 cmake OR Visual Studio, whichever the box
  supports today (table the choice + versions; prefer the
  path with full EE/VU/IOP recompilers + trace channels —
  Devel, NOT Release: Release compiles trace channels out).
- The goal: boot SSX3 in Devel PCSX2 on bytesize to the
  park's epoch (EE boot → game ELF running → the
  post-exit/drain region, aligned by guest event not wall
  time) and capture the `EE.Bios` syscall trace (+ any
  INTC/DMA/SIF channels Devel offers) to a file.
- Inputs (locate, do NOT download copyrighted files): the
  SSX3 ISO (lives on this laptop/SSD — `scp` a copy to
  bytesize over Tailscale, record sizes+shas both ends) and
  a PS2 BIOS dump (if none exists on either machine, STOP
  and table it as a blocker — do not fetch one from the
  internet). PCSX2 source: clone upstream git (tag/commit
  recorded); no fork changes needed.
- NO lease of any kind (all boots run on bytesize, not the
  laptop — the P-lane lease does not apply). No `adb`. No
  changes to the PS2Recomp fork. Fan/noise on bytesize is
  fine; keep laptop-side work to ssh/scp only.

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce the build + capture from scratch.
- Evidence dir `local/research/T4/` (STANDALONE). Commit with
  `git add -f`, prefix `[T4]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 6 h. Tables + receipts, no verdicts. If blocked
  (no BIOS, no toolchain, no disk), table the blocker
  exactly with what was tried, commit what exists, stop.

## Task 1 — Devel build on bytesize (does it build?)

1. Survey table: OS/WSL2/VS/cmake/ninja/git versions on the
   box + GPU/driver + free disk (build needs ~10 GB).
2. Build table: clone (URL + commit), configure flags,
   build command + wall, binary path + size + sha, Devel
   proof (trace-channel flag present, recompilers on).
3. Boot-smoke table: BIOS detected? ISO boots to ELF?
   (screenshots NOT needed — log lines suffice).

## Task 2 — reference capture (does the epoch trace?)

1. Epoch table: how far the boot gets (guest events, not
   wall — first EE syscalls, module loads, game ELF entry;
   does it reach the drain region? if it overshoots or
   parks elsewhere, table where + why).
2. Trace table: channels captured (EE.Bios + others),
   lines/bytes per channel, file paths on bytesize +
   sizes + shas, retrieval method (scp home? stay remote
   with paths? — traces stay on bytesize if >1 GB, with
   exact paths + head/tail samples committed).
3. Sample table: first ~50 EE.Bios lines + ~20 lines at the
   deepest epoch reached (committed verbatim — Brief 2's
   format reference).

## Report

`local/research/T4/REPORT.md`: survey + build + capture
tables, exact commands (both ends), epoch reached, trace
inventory, gap rows (Brief 2 inputs: trace format sample +
epoch anchor). Commit evidence (`[T4]`, no push), stop.
