# E55D16 — seed our memory card from Brad's real SSX 3 save, then load it

You are the E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/facts.md`, this brief, then `local/research/{E55D12,E55D14P2B,E55D15P1}/REPORT.md`. Cite the fork and local reports, not the web. Own `local/research/E55D16/` and scratch `~/dev/ssx3-work/E55D16/`. No fork source edits unless Part 1 proves one is needed (then stop and report). No push.

**Why.** Load game on our runtime queries `/BASLUS-20772-GAM*` at tick 1740 and finds an empty card (E55D14). Brad's real save is on the Odin: AetherSX2 card image `~/dev/ssx3-work/E55-odin-memcards/Mcd001.ps2` (8,650,752 B, SHA `ac98cf37…97bc`), which holds `BASLUS-20772-GAM0001` and `BASLUS-20772-SET0001`. Private data: never commit card bytes or extracted save files.

**Part 1 (no boot).** (a) Establish our runtime's `mc0` host format from the fork source (how `mcserv` GetDir/Open/Read map to host files and directories; cite file:line). (b) Parse the PS2 memory-card image (superblock, cluster size, FAT, directory entries; the format is documented in PCSX2 or mymc sources if present locally, otherwise decode from the image with a small Python reader and verify with checksums or known header fields). (c) Extract exactly the two `BASLUS-20772-*` directories with every file, sizes and SHAs, into a scratch copy of our `mc0` layout. Stop and hand back if our layout can't represent something (attributes, timestamps) the game checks.

**Part 2 (one boot).** One Mac I26-FAST-derived boot with the seeded `mc0`, using E55D12's Load game route (Start, Square, Down×4, Cross, Down, Cross), one mini lease slot, ≤ 600 s. Observable: the MEMORY CARD screen lists the save instead of six `<EMPTY>` rows, and selecting it loads (frames viewed by you, plus the mcserv/card log lines). No speed claims.

**Deliverable.** `local/research/E55D16/REPORT.md` with the format map, the extraction table (names/sizes/SHAs, no bytes), the boot result with frame paths, gaps. Commit `[E55D16] …` (explicit paths, trailer `Orchestrated-By: Muse Code`), no push. Budget 90 min, 1 boot.
