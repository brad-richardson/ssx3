# M23 — Tail value mechanism: per-site value tables + predictor fits (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M22/REPORT.md` first (all of it —
M22 gap 1 is this brief: the 102/134 tail δ values at ρ≈0.42
regime, sign following the gap on ~90%, gaps ≥17), plus M20's
stencil-fit precedent (fixed candidate list in DESIGN.md, no
fishing) and M21's persistence machinery if the cross-shape
question recurs. Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- Interior far tail (M22): 102 B s0 / 134 B m15 with |δ|≥8 (max
  47/48), 100% luma on all 764 shapes, top-band + dec-9 heavy,
  25/31 scattered 8-conn components (top streak columns recur
  both frames), no carrier mask holds more than 1 B, 727/764
  maps byte-identical to s0 (m15 Jaccard 0.4132), gaps 17–115
  (mean ~54), ρ peaking [0.4,0.5) (mean|ρ| 0.42/0.40), sign
  agreeing with gap direction 90.2%/89.6%.
- The value question M22 tables but does not fit: given (gap,
  position, carrier-context) at a tail site, is δ predictable?
  Candidate predictors (FIXED list — record in DESIGN.md before
  running, no additions mid-run): δ = round(k·gap) for
  k ∈ {0.4, 0.45, ±}; δ = gap-side step (sign(gap)·c for small
  integer c); per-component-mean predictor (component id →
  mean δ); position-only predictor (band/decile cell →
  median δ). Hit = exact byte match; near-hit (|err|≤1) tabled
  separately, never merged.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m22/m22.txt` (tail membership to
  reproduce: 102/134 counts + max 47/48 — match exactly or
  table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m23/`; evidence `local/research/M23/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m22/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m23/` + `local/research/M23/`.
- Evidence dir `local/research/M23/` (STANDALONE). Commit with
  `git add -f`, prefix `[M23]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M22 precedent).

## Task 1 — per-site value tables (what δ goes with what)

Tail membership recomputed from the dumps (102/134 + max 47/48
must match M22 exactly or table the mismatch and stop):

1. (gap, δ) joint table per frame (gap bins from M22's hist
   edges; δ exact values — is δ a function of gap?).
2. (position, δ) joint: δ stats per band × decile-9/not cell +
   per top-streak-column (c257/277/296/301/321/340–343 — same
   δ across the recurring columns?).
3. (carrier-context, δ): δ stats inside/outside each top-10
   mask (expect ~all outside per M22 — table to confirm) +
   inside/outside-701 (all outside per M22 — confirm).

## Task 2 — predictor fits (fixed list, exact-hit scoring)

1. Score each DESIGN.md-listed predictor on s0 + m15 tail
   bytes: exact hits / near hits / miss |err| distribution.
   Best predictor's explained bytes (tail bytes it removes
   from cell 7) + residual |δ| hist after applying it.
2. Cross-frame check: fit constants on s0, score on m15 (and
   vice versa) — table both directions (do the constants
   transfer?).
3. Cross-shape check (cheap): score the best predictor on 3
   non-s0 shapes (shapes 1, 2 + one below-0.95-Jaccard shape
   from M22's table) — transfer or overfit, tabled.

## Task 3 — controls + determinism (M18–M22 precedent)

1. Positive control: synthetic truth with KNOWN tail values
   (N sites, known δ = f(gap) for a listed predictor f) —
   recover values + best-predictor identity exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M22).

## Report

`local/research/M23/REPORT.md`: DESIGN.md (bars + fixed
predictor list first) + value tables + fit scores + transfer
tables + controls + explained-vs-standing update (does any
predictor shrink the tail? by how much?) + gap rows.
Screenshot only if a predictor-residual map discriminates
(budget-noted). Commit evidence (`[M23]`, no push), stop.
