# E30 — Faithful MPEG fix, design phase: why 15,056 consumed bytes complete zero packets (read-only on the fork)

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`). **Tables + receipts; recommend, the orchestrator
decides.** Read first, all of each: `local/research/E29/REPORT.md` (§E30
handoff: four requirements), `local/research/E27/REPORT.md` Mission 2a/3
(the host latch: `nonStreamDeliveries`, re-entry `false`, lambda-held
`delivery`, `EeWaitState` ownership), `local/research/E28/REPORT.md`
(guest half closed), and `docs/research/review-2026-09-22-progress-and-parallelization.md`
§"faithful E-lane question". Also `AGENTS.md`.

## Why this brief is read-only

E31 owns the fork worktree and the P-lane lease right now (it's driving
the bypass build to a race). You may read the fork only through
`git show <ref>:<path>` / `git grep <ref>` at `3adc0478` and
`e29-movie-bypass`, and read `/Volumes/Extreme SSD/ps2recomp-spike/P1/run/`
boot logs. No checkout, no build, no boot, no lease. For experiments,
copy `MPEG.cpp` and the parser/decoder files you need into
`/Volumes/Extreme SSD/ps2x-e30/scratch/` and compile small standalone
harnesses there (host clang is fine); the real regression lands later.

## Mission 1 — the zero-packet question

E22–E29: every movie's feed is fully consumed (`offered=15056
consumed=15056`) yet `packets=0 frames=0`. First feed starts `000001b3`
(sequence header). Work out, from source + the committed 5,040-byte
feed vector (sha `cde8a830…2a1c875a`, its location is in E22–E28's
reports), why the parser completes no packet. Candidates to split, each
with a prediction you compute before running anything: (a) the parser
needs more bytes than one chunk and the second chunk is never
requested (host latch, E27); (b) the parser mis-frames elementary
stream vs program stream; (c) the decoder path is the no-FFmpeg stub on
this build (check `PS2X_ENABLE_FFMPEG` in E18's configure and which
branch compiled); (d) correct behavior (these movies really need more
data before the first packet). Feed the vector (and vector + a synthetic
second chunk) through a standalone copy of the parser and table the
packet/frame counts.

## Mission 2 — the regression and the fix, as diffs

1. Write the authored two-chunk regression as a test file diff against
   the fork's test suite (first producer callback supplies bytes but no
   complete parser packet, second chunk queued; assert eventual frame
   delivery, exactly one guest resume, no duplicate callbacks, no
   unbounded retry). Also cover empty input, end-of-stream,
   cancel/reset, and per-movie re-arm after `sceMpegCreate` (E29 saw 3
   movies). Include the 5 tests the bypass breaks as must-stay-green.
2. Write ONE candidate fix as a diff (must keep E18's ABI and callback
   semantics; no "flush every chunk"; no synthetic success).
3. Prove both on your standalone harness as far as it reaches, and say
   plainly what only the real suite + a boot can prove.

## Rules

- Zero fork mutations, zero builds in the fork, zero boots, zero leases.
- Scratch on the SSD under `ps2x-e30/` (≤ 2 GB logical); evidence
  `local/research/E30/` text only; `COPYFILE_DISABLE=1`.
- Time box 5 h. Evidence commit `[E30]` with `Orchestrated-By: Muse
  Code`, `git add -f`, no push; check `git log -1` first.

## Deliverable

Zero-packet cause table with predictions vs results; the regression diff;
the fix diff; what the apply-and-boot brief (E32) must run to validate
it: suite, then an A/B boot against E29's flag-on title screen.
