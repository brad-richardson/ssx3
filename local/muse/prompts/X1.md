# X1 — Snow Jam 99% stall: static read of the loop E31 saw (read-only, exploratory)

You are a local worker (opencode) in a herdr panel in `~/dev/ssx3` on the
Mac mini. **Read-only. Hand back tables; do not conclude or fix.** Read
`AGENTS.md` (rules) first.

## Context (facts, with sources)

The recomp's Snow Jam race load stalls at 99%: a live loop, zero CD reads
after ~478 s, zero errors; a pump at guest `0x27CEA8` fans out via
`0x27D330` / `0x284B58` at ~43.5k calls/s; thread 1 sits Ready at
`pc=0x423dc8` (`ra=0x377b6c`), thread 5 running at `pc=0x423de8`
(`local/research/E31/REPORT.md`, "Snow Jam" rows ~L200–215). In PCSX2 the
same load finishes in ~109 emu-s with one continuous 990-iteration
`_sceCdSC` read loop, a 1.75 MB SPU voice upload, and an EE semaphore spin
only while CD reads continue (`local/research/T47/REPORT.md` §T47-5/6).
Happiness (Rival) loads fine in the recomp, so the stall is course-specific.

## Inputs (read only; never write under these paths)

- Generated C++ for each guest function:
  `~/dev/ssx3-work/codegen-ssx3/sub_XXXXXXXX_0x....cpp` (e.g.
  `sub_0027CEA8_0x27cea8.cpp`, `sub_0027D330_0x27d330.cpp`,
  `sub_00284B58_0x284b58.cpp`, `sub_00423DC0_0x423dc0.cpp`).
- The game ELF `~/dev/ssx3-work/E32-inputs/cd/SLUS_207.72` (disassemble with
  `/opt/homebrew/opt/llvm/bin/llvm-objdump -d --triple=mipsel
  --start-address=… --stop-address=…`).
- Function map `~/dev/PS2Recomp/games/ssx3/ssx3-functions.sweep.csv` (read only).

## Tasks

1. For `0x27CEA8`, `0x27D330`, `0x284B58`, and the functions containing
   `0x423dc8`, `0x423de8` and return address `0x377b6c`: one row each with
   start address, size, callers you can find (grep `sub_<addr>` across the
   codegen dir), callees, and every **loop exit condition** in plain words:
   which memory address or register it tests, against what value.
2. List every **guest memory address** those loops poll (absolute addresses,
   or `base + offset` with the base's source) and, for each, the functions
   that **write** it (grep the codegen for stores to that address/offset).
   Mark any writer that looks like it runs in an interrupt/callback, the
   IOP/SIF/CD path, or a sound path (`sce*`, `Sif*`, `Cd*`, `Sd*` names in
   the map or nearby strings).
3. Syscalls these functions reach (the `syscall`/`SYSCALL` sites and their
   `$v1` numbers where visible), e.g. `WaitSema`, `SignalSema`,
   `ReferThreadStatus`, `iWakeupThread`.

## Budgets and stop rules

Time box 90 min. No builds, no boots, no device use, no leases, no network.
Write only under `local/research/X1/`. If a function isn't in the codegen
dir, say so and use the ELF disassembly instead.

## Deliverable

`local/research/X1/REPORT.md`: the three tables above, each row citing the
file:line or objdump address it came from, plus a short "gaps" list. Then
`git add -f local/research/X1 local/muse/prompts/X1.md` and commit `[X1]
Snow Jam stall static read` with trailer `Orchestrated-By: opencode`
(`git log -1` first). No push.
