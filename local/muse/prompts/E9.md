# E9 — Apply the 0x426230 DROP disposition + preflight, restore the 0x2c5140 query entry (frontier execution)

You are the frontier agent (Codex Astra) in a herdr panel in
`/Users/bradrichardson/dev/ssx3`, continuing as E-lane lead. **Tables +
hypothesis + next-action recommendation, no verdicts.** Read first:
`local/research/E8/REPORT.md` (all of it: outcome (ii) — progression parks
at the absent `0x2c5140` busy-query entry, 1251 attempts, E7 copy fix intact,
D 12358, Present exact) + `local/research/E8/NEXT-BRIEF.md` (the exact
recipe this brief executes: prerequisite table, forcing receipt, required
checks, guarded watch addresses) + P13b's `0x426230` selector record
(`local/research/P13b/REPORT.md` § + ledger row: `0x426230` enterable, sole
T5-flip — regen swaps handler for TODO trap) + the frontier DROP decision
(todo Queue line + `[orch] 634dbd5`: DROP from canonical + used config,
preserve guest translation, no new HLE). Baseline: fork `4acc59f`.

## Facts you start from

- REGEN IS AUTHORIZED FOR THIS BRIEF ONLY, under the DROP disposition: the
  disposition (frontier-decided, never applied) is the gate, and this brief
  applies + preflights it before any regeneration. Standing no-regen rule
  resumes the moment this brief's scope closes.
- Phase 1 — DROP + preflight: drop the `0x426230` selector from canonical +
  used config per the disposition (preserve guest translation, no new HLE);
  preserve config + generated-source + binding manifests before/after;
  preflight = full suite green + explicit `0x426230` binding/behavior receipt
  satisfying the disposition (current binding `sub_004261F0` must not be
  silently replaced by the selector/HLE trap).
- Phase 2 — entry repair (E8 NEXT-BRIEF recipe): produce a real exact-PC
  entry for `0x2c5140` through the sanctioned generator/splitting path
  (NOT a handwritten replacement — the body is already emitted after
  `sub_002C50E0`'s return; registering the owner alone would wrongly start
  `0x2c50e0`); fail-before/pass-after entry test on the ACTUAL generated
  binding (before: hasFunction false; after: reaches the query body,
  returns to supplied RA with unchanged SP, does not call sceMcGetInfo as
  part of the query); query truth table (state(+4)=0/outstanding(+0x40)=0
  → v0=0; either nonzero → v0=1; exercise both nonzero cases; preserve
  callee-saved registers + guest memory).
- Phase 3 — ONE verification boot: same shared lease, fresh T13 pre-claim
  checks, new binary hashes, wall/progress/byte caps, no parallel boot,
  release before analysis; `PS2X_DIAG_REPORT_ALL=1` + E7/E4 copy/frame taps
  kept; capture the object dynamically from the query's a0 (require
  [a0]=`0x486f78`); guard watch addresses with the constructor write at
  `0x2c3fc4` + UI pointer store (E8b candidates in NEXT-BRIEF, never assume
  another boot's heap); E7 copy guard (singleton + S fields, packet→GS→D→
  field-processed Present).
- Outcomes (tabled, not verdicts): (i) entry restored + verification boot
  shows the guest advancing past the wait (port-selection v0 recorded,
  intended-request advance observed) → join-proof table + ONE next action;
  (ii) entry restored but a REMAINING wall appears first (`0x2c5358`
  sibling residual or other) → table it, do NOT claim menu, stop with the
  exact next recipe; (iii) DROP/preflight/entry fails → table the exact
  failure + repair recipe, stop (no stacking). Only an actual guest
  request/completion dependency can promote memory-card storage, SIF/CD or
  input work. A changed frame requires the full packet/source/display/
  Present join before claiming progression.
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`.
  Plain commits, pushed to the FORK remote only, never upstream. Generated
  runner sources follow the owning lane's staging rules (table them).

## Gates and rules

- Boots ONLY while holding `/tmp/ssx3-p-lane-lease` (SHARED P-lane) — poll
  every 5 min + log waits to `$W/P1/run/e9-waits.log` if taken. Max 2 boots
  ≤600 s each; wall + progress + BYTE caps recorded per boot, abort past
  any cap. Builds `-j4` any time. No `adb`.
- `export COPYFILE_DISABLE=1` on every SSD step. Retain: manifests +
  receipts that prove each phase (compress closed raws; single canonical
  copies).
- Evidence dir `local/research/E9/` (STANDALONE). Commit with `git add -f`,
  prefix `[E9]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (orchestrator pushes at poll when the tree is clean). Fork commits pushed
  to fork remote (table SHAs).
- Experiment contract up front (hypothesis/observable/alternatives/stop +
  which outcome each selects). Time box: 6 h from this handover.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — DROP + preflight (no boot)

1. Apply the disposition (canonical + used config), manifests before/after,
   full suite green, `0x426230` binding/behavior receipt. If preflight
   fails: outcome (iii), stop.
2. Decision table: preflight green (→ Task 2) vs the exact failure + repair
   recipe (→ stop).

## Task 2 — entry repair + verification boot (max 2 boots)

1. Entry repair via the sanctioned path + fail-before/pass-after test +
   truth table (both nonzero cases). Pre-claim checks + suite re-green
   before any boot.
2. Claim → foreground verification boot → at first binding cap: SIGTERM →
   release AT ONCE → analysis (lease-free). Dynamic a0 capture + vtable
   guard + copy guard.
3. Finding table (exactly one of i–iii) with the receipts that force it +
   the prescribed next action. If (iii): the measurement repair, nothing else.

## Report

`local/research/E9/REPORT.md`: contract, DROP + preflight record, entry
repair + truth table, verification boot, finding, exact commands, gaps.
Commit fork first if touched (pushed fork remote), then evidence (`[E9]`),
remove the lease if held, stop.
