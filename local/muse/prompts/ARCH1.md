# ARCH1 — independent ssx3 architecture and direction review (Astra)

You are an independent **read-only architecture reviewer**, not the orchestrator or an implementation worker. Brad asked for Astra feedback on today's direction. Read repo `AGENTS.md`, `docs/status.md`, `docs/orchestration.md` §§1, 10, current open `docs/todo.md` E/G/N sections, `docs/numbers-ledger.md` current live rows, `docs/research/review-2026-09-23-frontier-2.md`, and the full reports/gates for `N8D7M12P5E2`, `P5F2`, `P5F3`, `P5M4`, `P5F4` (permission stop). Read source only as needed to verify a key edge. No code/build/boot/device/config/branch change, push or upstream contact. Do not prompt other panes. Time box 25 minutes, report <12 KiB. Write `local/research/ARCH1/REPORT.md` early; commit only that report with `[ARCH1]` and `Orchestrated-By: Codex`, no push. If writing/committing would delay the review, give the full handback in the pane and say so.

Milestone: stock SSX 3 race with input and a visible, advancing world through our PS2 static recomp runtime on Odin 3; later 120 Hz simulation. Current state: race advances and Mac renders fuller snow/terrain; Odin paraLLEl GS replay is mostly black and **varies across identical same-settings runs** from tick850, with sampled priv hashes equal. Mac same-binary/stream ON/OFF capture-flag control now passes, 41 rows and final PPM byte-equal. Source audits located exact packaged renderer files but did not prove a GPU cause. The next planned observable is a dev-only fingerprint at GS worker command consumption, followed by two same-settings Odin replays. That implementation's first worker stopped on a tool edit permission before source changes; it can be relaunched safely from inside the private worktree.

Give the orchestrator a short, source-grounded **steering memo**, not a new project plan. Answer:

1. Is the current milestone chain and priority (fix Odin race visibility before deep speed work) still sound? Name one concrete correction if needed.
2. Is the worker-consumption fingerprint the best next discriminating observation for the tick850 variance? Compare it with one alternative, stating **different predicted observations** under packet delivery vs GPU state/readback. Account for opaque `PrivWrite`, hash collisions, and source-vs-APK provenance. Identify if the proposed measurement would be tautological under a fixed stream and FIFO worker.
3. What is the single highest-value architecture or integration risk today (runtime/GS/Android boundary, determinism, source provenance, or measurement), supported by exact receipts/source lines? Give one bounded next action with an explicit stop rule.
4. Name any important work we are over-investing in or under-investing in, using the current board rather than historical queue text. Separate measured facts from inference; no speed claims from diagnostic runs.

Hand back a table: recommendation, supporting evidence, what would falsify it, cost/lease/build impact, and confidence. Include the exact document/source anchors you relied on. Do not declare a root cause or decide the project direction on Brad's behalf. The orchestrator owns the verdict.
