# N9 — Android fold: paraLLEl-GS fix + Turnip packaging onto the fork tips, one Odin APK, live race

You are the N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, then in full `local/research/N8X1/{REPORT.md,ORCH-GATE.md,n8x1-fix-clean.diff}`, `local/research/N8B1/REPORT.md` (Android build recipe + the two N8B1 commits), `local/research/N8D7M12P6M6R/REPORT.md` (bytesize build with the external pinned wrapper), `local/research/N8D2/REPORT.md` (live route). Cite local files, not the web. Own `local/research/N9/` and scratch `~/dev/ssx3-work/N9/`. **No pushes** (the orchestrator pushes all three repos after Part 1). No upstream contact of any kind; the Granite submodule's `origin` is upstream Themaister/Granite, so never push or fetch-write there. No subagents.

**Why.** N8X1 solved the Odin black frame (paraLLEl-GS hierarchical binner at wave128 on Adreno; fixed wave64 fixes it) and showed a live race on the Odin, but on a diagnostic APK built from **dirty working copies**. Every GPU build so far used `3a66c19` + uncommitted edits (G43 → N8B1, N8D7F → N8D7M12/N8X1), never our forks' `ssx3` branches. Goal: product code lives on the forks, and the Odin APK is built from fork tips with sound (AU7) and widescreen (W1) included.

**Inputs.** PS2Recomp fork `ssx3` = `959f4ea` (sound + widescreen + GB6 opt-in GPU backend). N8B1 commits `a4babc6` (package opt-in Turnip backend) and `17e90de` (APK with diagnostics off), branch `n8-turnip-apk`. paraLLEl-GS: our fork `https://github.com/brad-richardson/parallel-gs` branch `ssx3` = `3b1ca9e` (G26, Turnip HAL loader, build shims, Granite gitlink to our Granite fork) and `wip/ssx3-snapshot` = `faf6400`; the N8D7F build clone snapshot, committed locally as branch `ssx3-n8d7f-snapshot` in `~/dev/ssx3-work/N8D7F/parallel-gs` (`b413754`, Granite `36ae0d0b`); upstream's unmerged `hier-binning` branch (`bf1b60f`, see `d566592 "Actually use largest wave size for binning"`, read only).

## Part 1 — sort, branch, Mac build (no device, no pushes)

1. Fresh clones under `~/dev/ssx3-work/N9/` of our paraLLEl-GS fork (`ssx3`) with its Granite submodule from `brad-richardson/Granite`. Diff `ssx3` against `ssx3-n8d7f-snapshot` (superproject and Granite). Put each hunk in a table: **product** (needed to build or run the recomp's parallel backend on Mac/Android: interface additions the fork's backend calls, build shims, HAL loader, G-lane fixes) or **diagnostic** (N8D5 tile probe, N8D7F selected capture, replay-tool additions, logging). Confirm each "needed" claim with `rg` or `lsp` against PS2Recomp `959f4ea` (what does `ps2_gs_parallel_backend.cpp` actually call?).
2. Local branches: `ssx3-next` = fork `ssx3` + the product hunks + `n8x1-fix-clean.diff` (commit the fix separately with a message citing N8X1); `wip-next` = `ssx3-next` + the diagnostic hunks. Same pattern in Granite if Granite has product hunks. Record each commit SHA.
3. PS2Recomp: worktree from fork `ssx3` `959f4ea`, local branch `n9-android`; cherry-pick `a4babc6` and `17e90de`. If the Granite gitlink or `PS2X_PARALLEL_GS_SOURCE_DIR` handling must change to point at the fork clone, do it in a separate commit. Conflicts: pure adjacent additions keep both (record them); anything else stops.
4. Mac: build `ps2x_tests` with `PS2X_GS_SHADOW_PARALLEL=ON` against the `ssx3-next` clone, taps OFF; run the suite from the worktree root; run the N8D7M12 Mac replay control only if the replay core is present (it isn't on `959f4ea`; say so). Runner-dir check `git diff --stat 14b1e5cb n9-android -- ps2xRuntime/src/runner` must be empty.
5. Commit `[N9] Part 1` (receipts + classification table), trailer `Orchestrated-By: Muse Code`, explicit paths. **Stop and hand back** the table, branch SHAs, suite count and the runner-dir check.

## Part 2 — APK + live Odin (only after the orchestrator has pushed the branches and says go)

One bytesize `assembleRelease` from the pushed fork tips (N8D7M12P6M6R recipe; one heavy job at a time on bytesize), diagnostics OFF, `PS2X_GS_SHADOW_PARALLEL=ON`, canonical codegen; APK/runner/Turnip/HAL SHAs ×2, Build ID. Odin: lease, keyguard `showing=false`, battery (≥20 % and charging, or status 3 on AC at ≥90 %), `adb install -r`, N8D2 live route with **no `PS2X_PGS_*` env**, device screencaps at the menu and at race tick ≥ 2050, force-stop after the run, restore `ps2x.env`, release the lease. One launch; if the screen is black, stop and report. Commit `[N9] Part 2`.

Budgets: Part 1 90 min, 1 Mac build; Part 2 60 min, 1 APK build, 1 Odin launch. Scratch ≤ 20 GB (`local/tooling/disk_budget.sh`). Diagnostic numbers only; no speed claims.
