# RV5 — pre-release code review of Brad's next device build (F6) (GPT Astra, read-only, ≤ 2.5 h)

F6 puts the most new code on Brad's Odin and iPhone yet. Review it for bugs that a det-hash and a few device runs would miss: crashes, leaks, lifecycle races, silent divergence, platform-only breakage. You are a reviewer: read and reason; no builds, boots or device use; write one document.

## What goes into F6 (all on fork `~/dev/PS2Recomp`, remote `fork`; paraLLEl `~/dev/ssx3-work/parallel-gs-ssx3` / `~/dev/ssx3-work/VK1/parallel-gs`)
1. **VK1 Android Vulkan present** (default on): fork worktree `~/dev/ssx3-work/VK1/PS2Recomp` branch `vk1-part2` (`git log 5474956..vk1-part2`; new files `ps2_present_vk.h`, `ps2_present_vk_android.cpp`, changes in `ps2_gs_parallel_backend.cpp`, `ps2_runtime.cpp`, `cmake/patch_raylib_android_egl_alpha.cmake`) + paraLLEl `vk1-part2` counters. Report: `local/research/VK1/REPORT.md` (Part 2B, Gaps). Focus: AHardwareBuffer import/ownership and release, fence and release-fence handling (fd leaks, double close, timeouts), queue-family ownership transfer, window lifecycle (TERM/INIT_WINDOW, same-pointer reuse), fallback correctness, thread safety between the main thread and the GsWorker, the raylib EGL patch.
2. **SS1/SS2 save states** (default off, but compiled into every build): `git log a3efbfe..173b31f -- ps2xRuntime` on fork `ssx3` (`ps2_savestate.h`, `src/lib/ps2_savestate.cpp`, `EeSchedulerSavestate.cpp`, `Syscalls/Savestate.cpp`, per-TU registration in `Support.h`). Reports `local/research/SS1/REPORT.md`, `SS2/REPORT.md`. Focus: any cost or behaviour change **with the knobs unset** (static init, per-dispatch checks, registration side effects); then missing state that would silently diverge a load.
3. **VR2 stage 1** VU1 levers (`git log 5474956..d4fc12e` on fork `ssx3`; report `local/research/VR2/REPORT.md` Stage 1 table + "Why it is exact"). Focus: the exactness arguments for levers 2 and 3 (budget/pc-mask/pending-until).
4. **HP1** (may still be in progress; branch `hp1` in `~/dev/ssx3-work/HP1/PS2Recomp` if present): E37 trace formatting guarded by `ps2_vu1_entry_trace::enabled()`; Android `minSdk 29`.

## Deliverable
`docs/research/review-2026-09-26-astra-f6.md`: findings ranked by severity (**blocker / should-fix / nit**), each with file:line, the failure scenario (inputs/state → wrong result or crash), and a suggested fix; plus "areas checked, no issue" so coverage is visible. Take the space you need (≤ ~400 lines). Commit `[RV5] …` with the explicit path, trailer `Orchestrated-By: Codex`, no push. Read-only everywhere else.
