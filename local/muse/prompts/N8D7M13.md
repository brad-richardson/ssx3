# N8D7M13 — first tick where the Odin's GS VRAM departs from the Mac (13A prep + Mac run, 13B Odin run)

You are the N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, this brief, then in full: `local/research/N8D7M12P7/{ORCH-GATE-7A.md,ORCH-GATE-7B.md,launch.py,compare.py}` and the Part 7B section of its `REPORT.md`, plus `local/research/N8D7M12P5F4P3/REPORT.md` §commands (the Mac replay recipe). Cite local files, not the web. Own only `local/research/N8D7M13/` and private scratch `~/dev/ssx3-work/N8D7M13/`. No source/fork/APK/stream/build edit, no rebuild, no push, no board/ledger edit, no upstream contact, no subagents. Text receipts < 512 KiB; outputs ≤ 64 MiB per run. Write `REPORT.md` early; hand back tables, not a graphics verdict.

**Why.** Part 7 proved that the GS worker consumes identical commands on the Odin and the Mac (41/41 digests), yet Odin VRAM differs from the Mac at the first 50-tick sample (tick50), and two Odin runs differ from each other from tick850. The Odin frames are ~97% black. We need the **first tick** where Odin VRAM departs from the Mac. The replay samples every tick with `PS2X_GS_REPLAY_STEP=1` (`gs_replay_core.cpp:210`), and each sample reads VRAM back from the GPU, so the same run also acts as a sync probe.

**Pins.** Mac binary `~/dev/ssx3-work/N8D7M12P5F4/build/ps2xTest/ps2x_tests` `3b21ce6149b3e4b6ddb6757589cb0b6a688c553d016d53bcd8fb2221cbd56153` (same fork source `4fa0df1` as the APK); run from the worktree cwd P5F4P3 used, with its `GRANITE_VULKAN_LIBRARY`. Odin APK/runner/Turnip/HAL/stream: exactly as in `N8D7M12P7/launch.py`. Stream `f6a78f71…a593`. Two SHA reads of every pinned input before use.

## 13A — Mac STEP=1 run + Odin launcher (no device)

1. Mac: claim one mini lease slot (`local/tooling/p_lane_lease.py`), run the P5F4P2 ON env (three capture flags + `PS2X_GS_REPLAY_PKTSEQ=1`) with **only** `PS2X_GS_REPLAY_STEP=1` changed and private output paths under `~/dev/ssx3-work/N8D7M13/mac/`, own PID, 600 s wall cap; release the slot. Require 2050 `GB4_REPLAY` rows (ticks 1..2050) and 2050 `GB4_PKTSEQ` rows. Control: its rows at ticks 50,100,…,2050 must equal the P5F4P2 Mac rows (`local/research/N8D7M12P5F4P2/replay-excerpt.txt`) exactly; if not, stop and report (STEP=1 would then perturb the Mac).
2. Copy `N8D7M12P7/launch.py` (released SHA `5983f315…b934aa`) to `local/research/N8D7M13/launch.py`. Allowed changes only: `PS2X_GS_REPLAY_STEP=1`; the expected-row gates become ticks 1..2050 (2050 rows) for both `GB4_REPLAY` and `GB4_PKTSEQ`; a single run label `step1` (scratch, lease tag, remote basenames); the progress cap stays 180 s and the wall cap 600 s. Write `check.py` (diff vs P7, allowed changes only, self-tests, `py_compile`, SHA). Commit `[N8D7M13] Part A` (explicit paths, `git add -f`, `Orchestrated-By: Muse Code`), no push. **Stop and hand back the Mac control result and the launcher SHA.**

## 13B — one Odin run (only after the orchestrator releases the exact SHA)

Preflight as in Part 7B (serial, lease, app stopped, battery per the amended gate, free space, `showing=false`, no mini heavy job). A locked Odin is a blocker: report it, never work around it. `python3 -u local/research/N8D7M13/launch.py --released-sha <SHA>`, one launch, no retry, force-stop after.

**Compare** (`compare.py`, no judgment): per tick 1..2050, Odin vs Mac equality of PKTSEQ seq/commands, priv, VRAM, present; the first differing tick per field; the longest leading run of equal VRAM ticks; the VRAM-equal count. Separately, the Odin STEP=1 rows at ticks 50..2050 vs P7 run1 and run2 (does per-tick readback change the Odin result?). PPM SHA and absolute path for viewing.

**Predeclared reading (the orchestrator decides):**
- **First VRAM departure at a tick T ≤ 50, and still ≤ 850 split behavior:** early deterministic renderer difference; next = packet-level bisect inside tick T (`PS2X_GS_REPLAY_BISECT_TO`, `PS2X_GS_REPLAY_PACKET_TRACE`).
- **Odin VRAM now matches the Mac through tick 850 or later (much longer than at STEP=50):** per-tick readback hides the fault, so missing GPU synchronization/flush is implicated; next = one sync candidate.
- **PKTSEQ differs anywhere:** report the tick; that contradicts Part 7.
- Any preflight/cleanup failure voids the run.

**Deliverable.** `REPORT.md` (Mac control table, Odin preflight/run table, comparison table with first differences, PPM paths/SHAs, exact commands, gaps); `compare.py` + output; commit `[N8D7M13] Part B` (explicit paths, `Orchestrated-By: Muse Code`), no push. Budgets: 13A 35 min (one Mac run); 13B 30 min + ≤ 600 s active; 1 install, 1 launch, 0 builds. Elapsed time is a run control, never a speed number.
