# M24 — K45+ near-hit mass: residual-sign tables + sub-rounding fits (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M23/REPORT.md` first (all of it —
M23 gap 1 is this brief: 32 s0 / 49 m15 tail bytes read |err|=1
under δ=round(0.45·g) vs 14/15 exact, pooled near 81 vs exact
29), plus M22's ρ-regime tables for the gap-context precedent.
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- K45+ residual structure (M23): s0 errvalue counts `0:14 1:32
  2:25 3:12 …` (max residual 93); m15 `0:15 1:49 …` (max 46).
  The near-hit mass (81 pooled) DWARFS the exact mass (29
  pooled) — if the ±1 residuals carry a sign/position/gap
  pattern, a sub-rounding correction (sign rule? fractional-
  part rule? gap-parity rule?) converts near-hits to hits.
- Residual r = δ − round(0.45·g), signed. Candidate corrections
  (FIXED list — record in DESIGN.md before running, no
  additions mid-run): sign(gap) rule (r follows ±gap side);
  fractional-part rule (r follows frac(0.45·g) above/below
  0.5); gap-parity rule (r follows gap even/odd); position
  rule (r follows band/decile cell — M23's POS cells verbatim).
  Score = exact hits after correction, near-hits (|err|≤1)
  tabled separately, never merged.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m23/m23.txt` (tail membership + K45+
  scores to reproduce: 102/134 counts, 14/15 exact, 32/49
  near — match exactly or table the mismatch and stop). Work
  dir: new `/Volumes/Extreme SSD/m24/`; evidence
  `local/research/M24/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m23/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m24/` + `local/research/M24/`.
- Evidence dir `local/research/M24/` (STANDALONE). Commit with
  `git add -f`, prefix `[M24]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M23 precedent).

## Task 1 — residual-sign tables (where does ±1 live?)

Tail membership + K45+ residuals recomputed from the dumps
(102/134 counts, 14/15 exact, 32/49 near must match M23 exactly
or table the mismatch and stop):

1. Residual-sign joint: r ∈ {−1, 0, +1, other} vs sign(gap),
   vs frac(0.45·g) half, vs gap parity, vs POS cell — one
   table per split per frame (does any split separate −1
   from +1?).
2. (gap, r) joint: residual stats per M23 gap bin (16–31 /
   32–63 / 64+) — does the ±1 mass concentrate in one bin?
3. Position join: residual stats per streak column (M23's
   columns verbatim) + per band×decile cell — same sign
   within a column?

## Task 2 — sub-rounding fits (fixed list, exact-hit scoring)

1. Score each DESIGN.md-listed correction on s0 + m15 tail
   bytes: exact hits / near hits / miss |err| distribution.
   Best correction's explained bytes + residual |err| hist
   after applying it.
2. Cross-frame check: fit rule constants on s0, score on m15
   (and vice versa) — table both directions.
3. Cross-shape check (cheap): score the best correction on the
   same 3 shapes M23 used (1, 2, 733) — transfer or overfit,
   tabled.

## Task 3 — controls + determinism (M18–M23 precedent)

1. Positive control: synthetic truth with KNOWN residuals
   (N sites, r = +1 where frac(0.45·g) ≥ 0.5 else −1, or the
   lead correction's rule) — recover the residual pattern +
   best-correction identity exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M23).

## Report

`local/research/M24/REPORT.md`: DESIGN.md (bars + fixed
correction list first) + residual tables + fit scores +
transfer tables + controls + explained-vs-standing update
(does any correction shrink the tail? by how much?) + gap
rows. Screenshot only if a residual-sign map discriminates
(budget-noted, pre-registered split required per M23
precedent). Commit evidence (`[M24]`, no push), stop.
