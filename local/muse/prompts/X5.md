# X5 — Sparse Qwen mechanical index for GB5 glyph replay

You are a local opencode/Qwen **sparse** worker in `~/dev/ssx3`. Follow
`~/dev/AGENTS.md` worker rules. This is a read-only extraction job. Write
only `local/research/X5/index.py`, `packets-949-950.csv`, `REPORT.md`,
and this brief; commit those explicit paths. **Hand back the table; do
not conclude which packet draws a glyph.**

Goal: give GB5 a checked packet index near the broken Setup Character
text at ticks 949–950. Source inputs, read only:

- `~/dev/ssx3-work/GB4/run/gb4p4.capture.bin` (2,752,955,786 bytes;
  SHA-256 `a6f75fb34fceccf6fd92ad65950d16f05a140cf38a3cc2f028be32e56ad51851`)
- `~/dev/ssx3-work/GB4/run/gb4p4.paths.txt` (corrected true paths)
- `~/dev/ssx3-work/GB4/run/pklog-gb4p4-1.txt` (packet FNV, tick, length,
  and source path)

Read `local/research/GB4/glyph-packet-p6.txt` and
`~/dev/ssx3-work/GB4/run/check_packet_prefix_p2.py` before parsing.
The capture format source is
`~/dev/ssx3-work/GB4/PS2Recomp/ps2xRuntime/src/lib/gs/gs_stream_capture.cpp`.
Use `rg -n` for exact anchors. Cite the repo, not the web. Do not use LSP:
this is a binary-format extraction, not a call graph.

Deliver a CSV with **one row per GIF packet whose capture tick is 949
or 950**. Columns: `packet_index,tick,record_offset,corrected_path,
embedded_path,length,fnv32`. `packet_index` is zero-based over all GIF
packets; `record_offset` is the byte offset of the record length field.
FNV-1a is over GIF data bytes only, as in the prefix checker. Both paths
are numeric. Sort by packet index. Include all records at these two ticks,
not just sprite packets. Keep the script bounded in memory by streaming
the capture; do not copy or edit the input files.

In REPORT.md give exact input hashes/sizes, source line citations for
record layout and path source, invocation, row count per tick, first/last
packet index, and a table comparing every CSV row to the same-index pklog
tuple `(tick, fnv32, length, corrected_path)`. Report the number of exact
matches, mismatches, missing pklog rows, and gaps. Do not infer a texture
or raster cause. Include a separate row for packet 144266: expected
tick 950, 1696 GIF bytes, FNV `cc6dd8df`, corrected path 3. If any pin
or count disagrees, report it and stop without silently changing it.

Mechanical acceptance: script exits nonzero unless capture SHA matches,
all records are length-bounded and reach EOF, paths file has exactly
1,982,063 indexed rows, every selected CSV tuple matches the same-index
pklog tuple, total CSV count is 922, and packet 144266 matches the known
tuple above. It prints one compact summary line. Run it once and paste
that line into REPORT.md. Do not launch a game, build, modify the fork,
write outside this repo, or use the network. Time box 50 minutes; stop
and report if a pin fails. Commit `[X5]` with trailer
`Orchestrated-By: opencode`; no push.
