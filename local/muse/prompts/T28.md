# T28 — Single Event Cross from the menu park: first submenu (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T27/REPORT.md` first (all of it): R3
reached the Main Menu via detector-driven Start on title (≤0.73 s after
exposure) with Single Event HIGHLIGHTED at park (`t27r3-menuN.jpg`, N=10
snaps over 91 s, pairwise ≤0.0862/p99 ≤2). This brief executes T27's G1:
press Cross on the highlighted Single Event and map what comes next. Reuse
T27's scripts (`t27-auto.sh`, `t27-cropdiff.py`, `t27-analyze.sh`,
`t27-vcount.sh` — copy, do not modify the committed originals) and its
PPM text-band detector method.

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T27 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- The task: reproduce the R3 menu park (detector-driven Start on title),
  then press Cross with Single Event highlighted and table EVERYTHING
  after: the press (button, hold ms, T+ time, pre/post snaps), each screen
  transition (snap sha + region-diff scores), and the arrival screen
  (snaps + stability count + what is highlighted/selected).
- If Cross does NOT advance (menu ignores it, needs a different button,
  submenu loads slowly): table the exact observed behavior (snaps + scores
  + waits) and STOP with a recipe — do not improvise a button-mashing
  survey. ONE bounded variant is allowed only if the first press provably
  never acted on the menu (pre-press snap ≠ menu).
- Success bars (tabled, not verdicts): menu park reproduced (snap match
  scores vs T27's `t27r3-menu01.jpg`); Cross acted on the menu (pre-press
  snap = menu); arrival screen snaps + stability N; full input log + trace
  sha.

## Gates and rules

- NO lease (bytesize needs none). If WSL kills recur, table them exactly
  (dmesg + event log, kill-to-remount times) and re-verify trace shas
  post-reboot; attempts inside flap windows must be effects-verified or
  re-run (T27 §4 rule — quote it, don't reinvent it).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T28/` (STANDALONE). Commit with `git add -f`,
  prefix `[T28]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — menu park + Cross (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T27 §1 shape): build/input receipts, NVM state, T27
   reference snaps present, free space both ends.
2. Per attempt table: park reproduction (detector hits + snap scores),
   press row (button/hold/T+/pre-post), transition chain (snap sha + diffs
   per hop), arrival (snaps + stability N) or exact non-advance behavior.
3. Trace bytes + sha per run; WSL event table; NVM state at end.

## Report

`local/research/T28/REPORT.md`: pre-run receipts, attempt tables, press
log, arrival (or non-advance characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T28]`), stop.
