# E32 — Fold every side branch onto the fork's `ssx3` branch (one primary branch)

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`). **Tables + receipts; recommend, the orchestrator
decides.** Read `AGENTS.md`, `local/research/E29/REPORT.md` (build
recipe, AppleDouble fix, CWD-sensitive suite), `local/research/E31/REPORT.md`
(the pad script), and `local/research/N1/REPORT.md` §H1–H3.

## Goal

Brad wants everything on one primary branch. Fold the fork's side work
onto `ssx3` as clean, deduplicated commits, prove nothing regressed, and
push `ssx3` (fast-forward only). You don't delete any branch; the
orchestrator prunes them after reading your report.

Fork: `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, remote `fork`
(`github.com/brad-richardson/PS2Recomp`), `ssx3` @ `3adc0478`. You own
the worktree and the P-lane lease for this brief (E31 has closed).

## Inputs to fold (all backed up on `fork` already)

| Source | What | Notes |
| --- | --- | --- |
| `e29-movie-bypass` (+ E31's pad-script commit) | `PS2X_SKIP_MOVIE`, `PS2X_PAD_SCRIPT` (dev-only, default off) | keep the dev-only labels |
| `i23-ffmpeg-ios` (5 commits over ssx3) | iOS FFmpeg wiring, C3 `PS2X_MPEG_VECTOR_PATH`, C5 `PS2X_MPEG_FEED_TRACE` | N1 H3; unset = no behavior change |
| `i8-device-bundle-name` (`3006a07`) | iOS CFBundleName | one-liner |
| `archive/i10-codegen-dir{,-alt}` | `PS2X_GAME_CODEGEN_DIR` (N1 H1) | many rebased copies exist; fold ONE |
| `archive/i21-drop-a/-b` | drop in-tree `sub_*.cpp` when the codegen dir is set | pick the one I23/N2 used, table why |
| `archive/i11-map` … `archive/i17-map` | 7 function-map CSV rows | check each against `ssx3` first; E11–E14 already absorbed some rows |
| `archive/i18-tap` | frame/GS console-sampler line | fold only if env-gated/silent by default, else table and skip |

## Steps

1. Table every input: commits, files, whether the change is already on
   `ssx3` (compare content, not SHAs), and the fold decision. Stop and
   hand back the table if anything is ambiguous (e.g. two variants
   differ in behavior).
2. On a local branch `fold` cut from `ssx3`, cherry-pick or re-apply
   in dependency order, one commit per logical change, clear messages
   (no lane IDs needed in subjects; a trailer line is fine). Never commit
   generated guest sources, game data or binaries: `git status` must show
   only intended files, and gitignored generated dirs stay untouched.
3. Rebuild with E18's recipe (incremental in the E29 build dir is fine),
   run the full suite from the fork worktree root: 458 + any new tests,
   all green with every dev flag unset.
4. Two boots, one at a time under the lease, ≤ 120 s each: (a) all flags
   unset: must reproduce e28a/e29a's terminal state (park on the movie,
   feed SHA `cde8a830…2a1c875a`); (b) `PS2X_SKIP_MOVIE=1` (+ E31's pad
   script if it reached further): must reach at least E31's furthest
   screen (frame by path + SHA).
5. If 1–4 are green: fast-forward `ssx3` to `fold` and `git push fork
   ssx3`. No force, no rewriting `ssx3`'s existing history. If anything
   fails, push nothing and hand back the failure.
6. Leave the worktree on `ssx3` at the new head, status clean except the
   pre-existing `?? ps2_log.txt`.

## Rules

- Budget: 1 configure if needed, builds as needed, 2 boots, time box 4 h.
- `COPYFILE_DISABLE=1`; purge or rename `._*` in `_deps` before building
  (E29's AppleDouble fix).
- Evidence `local/research/E32/` text only + `[E32]` commit with
  `Orchestrated-By: Muse Code`, `git add -f`, no push of ssx3's own repo;
  check `git log -1` first.

## Deliverable

Input table with fold decisions; the new `ssx3` commit list (old head →
new head); suite result; both boot results; push receipt (`git ls-remote
fork ssx3`); the list of branches that are now fully contained in
`ssx3` (for the orchestrator to delete) and any that aren't, with why.
