# M15 — Host interpolated-frame synthesis demo (seam → synth vs truth)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/M14/REPORT.md`
first (all of it: the step-2 true-delta arms, the step-3 autoscan
carriers, the header/player/analyzer system, the determinism evidence).
This brief is milestone 3 on host: turn the proven camera seam into a
real synthesized intermediate frame and measure it against ground truth.
No device work.

## Facts you start from

M14 proved slot-0 +0.1 moves 53636/573440 B identically on 100/100
delta replays vs a rendered reference, with one draw carrying 8293 B
(19%) and 619/644 shapes uniform. The open thesis: a rigid camera
interpolation between two base renders synthesizes a valid intermediate
frame, with residuals concentrated where dynamic content breaks the
rigid assumption. The harness (env-gated header arms, replay players,
`analyze.py`, 240 s runs) is the instrument — extend it, don't fork it.
P-lane briefs run concurrently and share the host lease.

## Gates and rules

- Host replay runs coordinate through `/tmp/ssx3-host-lease` (M14's
  protocol: if it names another agent, poll every 5 min and log waits
  to `local/research/M15/waits.log`; never force a claim). Builds any
  time.
- Disk: internal disk is tight — run dirs + probes under
  `/Volumes/Extreme SSD/m15/` (new dir); players stay under `local/`
  (the build driver refuses outputs outside it — M14 item 5). Small
  logs (<5 MB total) may live in the evidence dir. Never touch
  `/tmp/p1-link`, `ps2x-i*`, `ps2x-p1*`, `/Volumes/Extreme SSD/m14/`
  (read from it, never write into it), or other agents' dirs.
- Evidence dir `local/research/M15/` (commit with `git add -f`,
  players included per M14 precedent, prefix `[M15]`, trailer
  `Orchestrated-By: Muse Code`). Screenshots under 5 MB total
  (downscale or describe). NEVER run `git push` in ssx3.
- Time box 6 hours.

## Steps

1. **Design (lease-free).** The synth-vs-truth experiment: base pair
   (renders at slot-0 v0 and v1), synth mid-frame from the pair via
   camera interpolation (method = implementer's call: mid-matrix
   re-render vs warp — record the choice and why), ground truth
   (render at the true mid matrix — the harness can do this
   exactly). N≥10 replays per arm (M14's 100 proved determinism;
   re-verify cheaply, don't re-prove expensively). Record the arm
   table before running.
2. **Run.** Lease-coordinated replays per step 1. Record per-arm:
   wall time, exit, probe bytes + shas, determinism rows
   (same-shape-as-M14 baseline table).
3. **Compare.** Synth vs truth: byte diffs, moved-bytes distributions
   (M14 §analyze.py shape), carrier attribution of the RESIDUAL
   (which draws carry the non-rigid remainder — the autoscan tells
   you where to look), and screenshot(s): base / synth / truth /
   diff-map. The deliverable table: what interpolates cleanly vs
   what breaks, with byte counts.
4. **Report.** `local/research/M15/REPORT.md`: design + method
   choice, run receipts, synth-vs-truth tables, screenshots (or
   absence reasoned), residual-carrier table, gap rows with the
   exact next brief each needs (Odin port? seam generalization?
   dynamic-content handling?), exact commands, receipt paths, "What
   I could not do". No verdicts. Commit `local/research/M15/`,
   release the lease if held, stop.
