# E43 — Who submits the mode-6 draw records (PCSX2 has them, the recomp doesn't)? Recomp side

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini).
**Tables + receipts; recommend, the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T53/REPORT.md`,
`local/research/E42/REPORT.md` if it has landed, `local/research/E41/REPORT.md`.
You own `~/dev/PS2Recomp` (`ssx3` tip) and the P-lane lease (E42 may still
hold it: wait); ff pushes allowed after the runner-dir check (trailer
`Orchestrated-By: Muse Code`).

## Facts (orchestrator, from T53 + a codegen read)

- The render-list walker `sub_00363C20` reads each draw record `*(s3)` and
  calls `sub_00364CD0` @`0x363cf4` with `a1 = (*(s3) & 0x3C0) >> 6` (a mode
  0–15; `sub_00364CD0` jump table @`0x492040`, 9 entries, translated
  correctly: all 8 distinct targets are in the recomp's switch).
- Mode 6 → `0x3658d4` path, which stamps uploader `0x434990` (store @pc
  `0x365994`); mode 3 → `0x365190` path, which stamps `0x435bd0` (store
  @`0x3651d8`). PCSX2 stamps both every frame; the recomp only `0x435bd0`.
- Assets load identically (165/165 sectors of `MDLPS2.BIG`/`ZOETXP.BIG`).
  So in the recomp **mode-6 records are never submitted** (or never walked).
  One candidate (unproven): 3D objects culled by VU0-macro visibility math.

## Change (default off, unit-tested)

1. Census at the walker: per vsync, per mode, the count of records walked by
   `sub_00363C20`, plus for the first 8 records per mode the record address
   and its first 4 words: `drec vsync mode count` / `drecs vsync mode addr w0..w3`.
2. Producer watch: log guest stores (fast-write path, mirror-folded, as E41)
   that write a word whose bits 6–9 equal 6 into the render list region the
   walker reads (find the region from (1)): `dprod vsync addr value pc ra fn`.
   If no mode-6 records ever exist, instead log stores of *any* mode into
   that region, grouped by fn (who produces records at all).

## Run, then read

Boot A (E33 route, 300 s), window 1270–1300. Tables: modes per frame;
producers by fn. Then read the producer(s) of mode-3 records and the code
path that would produce mode-6 records (the same producer with a different
flag? a different object type?): name the branch or test that decides it,
and log the tested values in a second boot if needed (budget 2 boots).

## Deliverable

`local/research/E43/REPORT.md` (commit, suite, tables, codegen read). No
verdict. `[E43]` commit (`git add -f local/research/E43
local/muse/prompts/E43.md`, `git log -1` first); ff-push the fork.
