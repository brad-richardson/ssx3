# M50 — 731/733 full-set attribution: static rows on the other frame? (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M29/REPORT.md` first (all of it —
M29 gap 10 = this brief, taken on orchestrator judgment: 733's
[303] + 731's [259,279] dest extras read gap 0 on s0 — one
frame's growth sits static (|δ|=0) on the other frame), plus
`local/research/M28/REPORT.md` (731: tail 100, J 0.6833,
wipes [257,277] → dests 259/279 +2/+2 rows 25–33; 733: tail
100, J 0.6694, wipes [301,321] → dests 303/323 +2/+2) and
`local/research/M43/REPORT.md` (the 4 dest cells WITH values:
37 sites, 28/41 g_s0==0 — by reference for dests, reproduce
the 28/41 count, then attribute the WIPED sides + full sets).
Fully offline — SSD dumps read-only, no harness code, no
replays, no lease. No device work.

## Facts you start from

- 731/733 rows (reproduce exactly or table the mismatch and
  stop): 731 tail 100 / J 0.6833 / named wipes c257+c277
  (0/0?) → dests c259 (+9) + c279 (+9); 733 tail 100 / J
  0.6694 / named wipes c301+c321 → dests c303 (+9) + c323
  (+10). No other 731/733 moves per M34 (verify).
- M43's dest values (cite, don't redo): c259/c279/c303/c323
  extras with |δ|/status/gaps; pooled g_s0==0 on 28/41.
- The question: full-set attribution for 731/733 (wiped
  source columns' rows + values — NEW DATA) + static-on-
  other-frame census (g==0 sites on BOTH sides: wiped rows
  with g_Q==0? dest rows with g_s0==0 = 28/41 reproduced?
  kept rows?). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28-census.tsv`
  + `/Volumes/Extreme SSD/m34/m34-census.tsv` (731/733 rows
  to reproduce — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m50/`;
  evidence `local/research/M50/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m49/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m50/` + `local/research/M50/`.
- Evidence dir `local/research/M50/` (STANDALONE). Commit with
  `git add -f`, prefix `[M50]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M49 precedent).

## Task 1 — wipe reproduction + dest recount (do the rows reproduce?)

731/733 recomputed from the dumps (must match M28/M34/M43
exactly — tails, J, moved cells, wipe/dest standings,
28/41 g_s0==0 — or table the mismatch and stop):

1. Wipe tables: c257/c277/c301/c321 s0-rows vs Q-rows
   (empty?) vs miss rows (full wiped row lists — new data).
2. Dest recount: c259/c279/c303/c323 extras + g_s0==0
   count (28/41 reproduced? kept-row g==0 counts?).
3. Full-set table: 731 + 733 priv/miss/shared/tail with
   0-outside checks (are the wipes + dests the whole sets?).

## Task 2 — wiped values + static census (where does g==0 live?)

1. Wiped-value table: per-site |δ_s0|, Q status, signed
   gaps, gap signs for ALL wiped rows (M36-style).
2. Static census: g==0 sites by side (wiped g_Q==0 n? dest
   g_s0==0 n? kept g==0 n?) + |δ| at static sites (is
   staticness a value regime?).
3. Wipe-vs-dest comparison: wiped |δ|/status/signs vs M43's
   dest stats (numbers only — same regime or not?).

## Task 3 — controls + determinism (M18–M49 precedent)

1. Positive control: synthetic truth with KNOWN wipe+dest
   sets incl. KNOWN g==0 sites (N static + N non-static
   per side) — recover the census + value tables exactly.
2. Determinism: re-run Task 1 on s0+731+733, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M49).

## Report

`local/research/M50/REPORT.md`: DESIGN.md (bars first) +
wipe/dest tables + static census + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a static-site map
discriminates (budget-noted, pre-registered split required
per M23–M49 precedent). Commit evidence (`[M50]`, no push),
stop.
