# M43 — Big-delta fresh columns: dest-row listing + value table for 761/731/733 (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M34/REPORT.md` first (all of it —
M34 gap 3 is this brief, taken on orchestrator judgment: the 5
delta≥9 cells are all fresh-column growth — 700-c54 (+10),
733-c323 (+10), 731-c259 (+9), 731-c279 (+9), 733-c303 (+9)
— plus 761-c54 (+4); 731/733's moves are their M27/M28
named-swap destinations), plus `local/research/M36/REPORT.md`
(700-c54's 10 extras WITH values — by reference, do not
re-attribute) and `local/research/M37/REPORT.md` (761's 4
in-mask tail bytes at (259,54)/(261,54)/(263,54)/(274,54)
— row-list cross-check). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- The 5 cells to attribute (M34 big-delta table — n/ov +
  miss/extra + deltas must reproduce exactly or table the
  mismatch and stop):
  - 761 c54: n/ov 4/0, miss/extra 0/4, delta 4 (fresh).
  - 731 c259: n/ov 9/0, miss/extra 0/9, delta 9 (fresh).
  - 731 c279: n/ov 9/0, miss/extra 0/9, delta 9 (fresh).
  - 733 c303: n/ov 9/0, miss/extra 0/9, delta 9 (fresh).
  - 733 c323: n/ov 10/0, miss/extra 0/10, delta 10 (fresh).
- 700-c54 (+10) is attributed WITH values in M36 (rows
  [245,246,247,252,253,258,259,278,279,283], all |δ| 8
  bulk −−) — cite it, do not redo it. 761's 4 rows are
  known at (259,261,263,274)×c54 from M37's probe — the
  row-list must reproduce them (or table the mismatch).
- The question: dest-row listing + per-site value table
  (|δ|, bulk status, gaps, signs) for the 5 fresh cells,
  side-by-side with 700-c54's uniform-8/bulk/−− values. Is
  fresh-column growth values-uniform like c54, or does
  each dest cell read its own values? Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (n/ov + miss/extra + deltas to reproduce — match exactly
  or table the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m43/`; evidence `local/research/M43/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m42/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m43/` + `local/research/M43/`.
- Evidence dir `local/research/M43/` (STANDALONE). Commit with
  `git add -f`, prefix `[M43]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M42 precedent).

## Task 1 — dest-row listing (do the 5 cells reproduce?)

The 5 cells recomputed from the dumps (must match M34
exactly — n/ov, miss/extra, deltas — or table the mismatch
and stop; 761's rows must match M37's 4):

1. Row-list table: all 5 cells s0-rows (empty — fresh) vs
   Q-rows vs extra rows (full row lists, the new data).
2. Fresh-column proof table: ov 0 + n0 0 + miss 0 per cell
   (fresh growth, not moves).
3. Cross-check table: 761's 4 rows vs M37's
   (259,261,263,274) + 700-c54's 10 rows cited from M36
   (do 761's rows overlap c54's?).

## Task 2 — value table (uniform like c54, or per-cell?)

1. Per-site value table: all 41 sites (4+9+9+9+10) with
   offset, |δ_Q|, s0 status + |δ_s0|, signed gaps both
   frames, gap signs (M36-style).
2. Per-cell value stats: |δ| list/med, bulk/noncell split,
   gap-sign split per cell (5 rows).
3. c54 side-by-side: each cell's stats vs 700-c54's
   uniform-8/bulk/−− (numbers only — uniform or not?).

## Task 3 — controls + determinism (M18–M42 precedent)

1. Positive control: synthetic truth with KNOWN fresh cells
   (N fresh columns with known rows + values) — recover
   the row-list + value tables exactly.
2. Determinism: re-run Task 1 on s0+761+731+733,
   byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M42).

## Report

`local/research/M43/REPORT.md`: DESIGN.md (bars first) +
row-list tables + value tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a dest-cell map
discriminates (budget-noted, pre-registered split required
per M23–M42 precedent). Commit evidence (`[M43]`, no push),
stop.
