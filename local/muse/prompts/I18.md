# I18 — Observability brief: instrument guest execution past 0x2c5358 to NAME the next layer (no map entry; local taps only)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I17/REPORT.md` first (all of it: base
moved to `9da21ff` + E12-row dedupe + 7 ports + 1-line CSV entry closed
`0x2c5358` (I16 wall GONE — normalized dedup diff = exactly the wall line
deleted, 0 additions), and NO successor wall is named: guest alive the full
90 s with a byte-identical event prefix, no crash, no new `.ips` two briefs
running, screen still black; gap 1 = this brief; the I11–I17 map-entry line
is DONE until a wall names an address again). This brief executes I17's ONE
next action — a NEW class: runtime/device-harness observability, NOT a map
entry. Reuse I17's worktree shape + device toolchain + provision/install/
ISO+env mechanism — table every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I17 app is STILL INSTALLED — plain `install` overwrites
  (closed 9× now). No fresh prompt needed for iPad installs. Confirm the
  iPad's state before installing (user sessions reported, not killed).
- BASE: create a SEPARATE fork worktree on the SSD at CURRENT `fork/ssx3`
  HEAD (table the hash — the E-lane may have advanced past `9da21ff`;
  NEVER move it yourself). Port I17's 8 local commits (plist, CMake wiring,
  `0x395730`/`0x14f2a8`/`0x156750`/`0x243a80`/`0x3a0158` rows, `0x2c5358`
  row — cherry-pick if clean, else re-apply by hand + table the merged
  diff; DEDUPE against HEAD first: skip any row the E-lane has since landed
  fork-side, table the dedupe with both shas as in I17).
- The task: add MINIMAL local-only guest-execution taps (pc/trace taps or
  periodic pc sampling + frame/GS observation — pick ONE mechanism, table
  why it can see post-`0x2c5358` execution where the console cannot) as
  LOCAL worktree commits (same standing as the wiring commits — unpushed
  while the E-lane mutates), rebuild the device runtime, install over the
  I17 app, and re-probe to NAME the next layer: where is guest pc? is it
  spinning, advancing, or waiting (on one of the 4 unhandled RPC sids? on
  vsync? on file I/O?)? One gap per brief: NAME the layer with evidence —
  do not fix it here (the fix gets the next brief).
- Success bars (tabled, not verdicts): tap commits land in the worktree
  (named files, local commits; fork-remote push ONLY if the E-lane is not
  mutating — else stays local, tabled); device Release build WITH game
  objects + taps exit 0; overwrite install exit 0; probe names the next
  layer (guest pc region/behavior + the evidence) or the exact obstacle
  (taps blind too — with the receipt proving they ran); diagnosis names
  the next layer with evidence.

## Gates and rules

- Tap minimality: the SMALLEST tap that can name the layer (periodic pc
  sample beats a full trace; a frame/GS counter beats a pixel dump). Taps
  must PROVE they run (a tap that emits nothing and observes nothing is
  not evidence — table the tap's own liveness receipt). No map entries, no
  recompiler changes, no behavior changes to guest execution in this brief.
- Subordinate to the E-lane fork owner (if live): builds `-j2`, never
  `-j4`; build trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i18/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`. Host `ps2_recomp` runs are codegen (no lease),
  bounded + tabled (re-run ONLY if the base move requires it — table the
  decision; the map should be unchanged from I17's).
- Fork discipline: worktree only; named-files `git add`; fork commits
  pushed to the FORK remote only, never upstream (and no `ssx3` push while
  the E-lane mutates); generated sources never staged.
- Device discipline (extends I6–I17 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I18/` (STANDALONE). Commit with `git add -f`,
  prefix `[I18]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — tap commits + device link (no iPad yet)

1. Pin + create the worktree at `fork/ssx3` HEAD (table hash); dedupe
   check vs HEAD; port I17's 8 commits (table method + merged diff).
2. Read the tap surfaces (E7 ordered sink, console sink, pc/trace
   facilities, frame/GS counters — table what's available and what each
   can see); write the ONE minimal tap (named adds, local commits; table
   the diff + the tap's liveness receipt plan).
3. Re-run host `ps2_recomp` ONLY if the base move requires it (table the
   decision); configure + build Release (`-j2`); table binary
   path/size/sha/identifier + table-populated proof. Fix ONLY build breaks
   in the wiring class.

## Task 2 — overwrite install + instrumented probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I17 app (table overwrite behavior).
2. Ship ELF+ISO as in I17 (same mechanism unless the link changed the
   packaging — table deltas). Instrumented probe launch with ISO+env:
   console + tap output + `.ips` (sleep + re-`ls` before concluding none —
   first `ls` also checks for late I17 filing) + screenshot. Table: tap
   liveness + the named next layer (or the exact obstacle).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. Diagnosis table (next layer + evidence). Attempt a fix ONLY if
   config-level — no second fork-code gap in this brief (one gap per
   brief; the fix gets the next brief).
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief the named layer needs).

## Report

`local/research/I18/REPORT.md`: contract, caps, tap + link record,
install/probe tables, named layer (or obstacle), exact commands, gaps.
Commit evidence (`[I18]`), stop.
