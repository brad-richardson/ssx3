# N11 — Odin per-stage time budget on the current play build (muse, 2 h)

## Goal
The Odin race runs at 0.113× (N10, sound off): ~148 ms of wall per guest frame, almost all on GameThread (~85% of one core). Our only profile (N5) predates the world drawing and used the CPU GS, so we don't know where the 148 ms goes today. Produce a **per-stage ms-per-guest-frame table** for the race and one menu on the current play build, plus sound-on vs N10 and one GameThread-pinning run. Evidence only; the orchestrator decides what to split or speed up.

## Facts
- APK: TL1's `~/dev/ssx3-work/TL1/app-release.apk` (`aeb60d4d…`; PS2Recomp `f949ff0` + paraLLEl-GS `963cb57`, diagnostics off). Two SHA reads before install. Always `install`.
- Driver: copy `local/research/N10/launch.py` + `phases.py` into `local/research/N11/` and adapt (it already does I26-FAST, `PS2X_VSYNC_RATE_LOG=1`, SurfaceFlinger polls, screencaps, `--profile-after-tick`/`--profile-secs` for `simpleperf record -g --app com.ps2x.runner`). N5 §profile has the simpleperf recipe and symbol grouping (`local/research/N5/REPORT.md`, `scripts/`).
- **Brad's card and env are live on the Odin** (I31 Part 2): his save is in `files/mc0/` and his default `ps2x.env` is manual play. Never touch `files/mc0/`. Save his `ps2x.env` to scratch (SHA), run with `PS2X_MC_ROOT=<FILES>/mc0-test` (empty dir you create) plus the route script, and restore his env byte-exact after every run (SHA check). N10's "mc0 empty" precondition becomes "mc0-test empty".
- Pinning: the app runs under its own UID; set affinity via `run-as com.ps2x.runner` + `taskset` on the GameThread TID (memory note: Odin display/presents method). Prime core is cpu7.

## Runs (≤ 4 launches, each ≤ 600 s wall, stop tick 4500)
| Run | Settings | Purpose |
| --- | --- | --- |
| S1 | `PS2X_SOUND=1`, no profiler | clean speed with sound (vs N10 0.113× sound off) — per-phase table as N10 |
| S2 | same + simpleperf 30 s from race tick ~2400 | race profile |
| S3 | same + simpleperf 30 s during Main Menu→Select Character (ticks ~700–1000; lengthen the menu dwell only if the route allows without derailing — else profile the title/menu window it has) | menu profile |
| S4 | as S1, GameThread pinned to cpu7 right after launch | pinning effect |

## Deliverable table (the point of the lane)
Per profile, group self-time by stage and convert to **ms per guest frame** = share × (thread CPU-s ÷ guest vsyncs in the window). Stages: guest code; EE runtime helpers (memory/TLB/syscalls); VU1 interpreter (split hazard bookkeeping vs execute); VIF1/DMA; GIF/GS packet handling; paraLLEl CPU-side submit; SND/audio; scheduler/sync/waits; PLT; libc/kernel; other. One row per thread for GameThread, GsWorker, audio, main (CPU-s per guest frame). Also GPU busy % (`/sys/class/kgsl/kgsl-3d0/gpubusy` sampled every 5 s) and thermal status per phase. Every symbol→stage mapping listed in an appendix so it can be checked.

## Rules
Lease `/data/local/tmp/mg/LEASE`; keyguard `showing=false` (else stop and report — ask Brad); `dumpsys battery` AC powered + ≥ 20 % before each launch; **force-stop after every run**; Brad's env restored after every run. Thermal: wait until thermal status ≤ 2 between runs (record waits). No fork edits, no builds. Text only in git (no PNG/binaries/perf.data). Scratch `~/dev/ssx3-work/N11/` ≤ 5 GB. First failure: stop, save the error, hand back.

## Deliverable
`local/research/N11/REPORT.md` (tables above, exact commands, APK SHA, run receipts, gaps) + adapted scripts; commit `[N11] …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push. Hand back the tables; don't conclude.
