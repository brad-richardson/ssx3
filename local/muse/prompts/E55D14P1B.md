# E55D14 Part 1B — resume GetDir path tap inside the fork worktree

You are the E-lane OpenCode Go implementation worker. Your **start folder** is `/Users/brad/dev/ssx3-work/E55D14P1/PS2Recomp`; all writes must stay inside it. Read `~/dev/AGENTS.md`, `~/dev/ssx3/AGENTS.md`, `~/dev/ssx3/local/AGENTS.local.md`, the full prior brief `~/dev/ssx3/local/muse/prompts/E55D14P1.md`, its `REPORT.md` and `ORCH-GATE.md`, plus E55D13 `REPORT.md`/gate. Cite source/receipts, not web. Use LSP when available and direct reads when not. The prior worker left this exact worktree clean at `bab6eb382673155ffd756fe8db265964eeff9703` on branch `e55d14p1-getdir-path`. Check that pin and status first; stop if either differs. Do not recreate the worktree. Test one tiny `edit` inside the worktree before broad work; stop on denial, do not evade it via bash.

Implement the prior brief's **one** default-off `getdirpath` sibling tap, preserving existing `getdir`/pad/mcread rows and per-family ord, guest writes/results, 16 MiB cap, and disabled behavior. Resolve bad-port/unformatted fields as explicit unknown sentinels. Use only the three source files named in P1. Build in this worktree at `.work/build` with pinned E55D3 diagnostic/compiler/dependency/codegen choices; `.work/` is untracked scratch and must never be staged in the fork. One configure/build, with one compile-repair rebuild only for the named patch. Run focused tests and the full suite from the fork root, flag unset. Double-hash runner, guard `ps2xRuntime/src/runner` against upstream `14b1e5cb`, recheck external codegen, disk cap 200 GB and lane cap 3 GiB. No boot, card seed, device, lease, push, upstream contact, global/board edit or generated guest code in git. Time box 40 min, text receipts ≤512 KiB.

Write `REPORT.md`, checker, and bounded text logs **inside `.work/receipts/`**. The orchestrator will review and import them to ssx3 after your fork commit; do not write outside the start folder, do not try to commit the ssx3 repo. A = source/tests/build and guard pass; B = bounded compile/test failure; OTHER = pin/permission/resource failure. Commit only the three named source files in this fork as `[E55D14] Part 1B` with `Orchestrated-By: opencode`, no push; check `git status` so `.work/` remains untracked. Hand back the table and exact `.work/receipts/` path. No claim about the guest query, save validity, determinism or speed. A separate worker part owns the one empty-card boot after the orchestrator gate.
