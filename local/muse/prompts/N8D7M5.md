# N8D7M5 — executed FBP112 word provenance on the Mac (OpenCode Go)

You are the N/G Mac-diagnostic worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7M4/{REPORT.md,ORCH-GATE.md}`, `local/research/N8D7L/{REPORT.md,ORCH-GATE.md,replay-excerpt.txt}`, and `local/research/N8D4/{REPORT.md,ORCH-GATE.md,replay-result.json}`. Work only in private fork `~/dev/ssx3-work/N8D7L/PS2Recomp` (branch `n8d7l-oracle`, pin `d1ba1d4`), your scratch `~/dev/ssx3-work/N8D7M5/`, this brief and `local/research/N8D7M5/`. No Android build, device/iOS action, push, shared E/G worktree edit or status/todo/ledger edit. Cite repo/source, not web; use LSP where available, direct source reads otherwise.

Goal: prove which **executed** GS packets change a few selected FBP112 VRAM words in the captured N8D4 stream (SHA `38ace1a3e8e99e1d5d7d24e611dfa9cedd6e6d44a45c800223991401cd93f97d`). This is Mac validation before another Odin launch. N8D7M4 correctly found the existing capture + selected-snapshot paths but its proposed test-harness read immediately around `gs.processGIFPacket` is invalid if that call only queues work. Its eight controls include dark/zero-tile pixels, 6/8 matches cannot prove a full-frame census, and tick2052 output cannot prove copy order. Do not reuse those as a causal classifier.

First pin the queue/execution order in `ps2_gs_replay_tests.cpp`, frontend and CPU backend. Give the observed source line that ensures each logged old→new word is after that packet **executed**, not merely enqueued. The backend `WriteVramUnlocked` is called by draw, transfer and clear paths; use its actual GS-storage address and old/new 32-bit word, not an RGBA output offset or linear row. If packet identity cannot be carried reliably into that execution path, a bounded test-only `drainQueue` after selected packets is allowed **only if** ON/OFF replay frame hashes and PPM SHA pairs match at tick2050; record the altered scheduling and stop on mismatch. Do not drain all 862,993 packets without a measured cap; identify a narrow tick/packet window first.

Pick 4–8 candidate FBP112/FBW8/PSMCT24 pixels spanning tiles with broad Mac selected-input occupancy, including at least two different VRAM pages. Prefer pixels where the N8D4 Odin **output image** lacks visible Mac content, but keep output RGBA and raw VRAM words separate. The N8D7L selected-oracle 448-tile vector and G43 circuit path can guide candidates. For each chosen address, require a packet/tick/path, before→after word, packet-operation kind (draw/transfer/clear), and post-marker2050 word; list unknown if no changed write. Prove at least two changed words in different pages if feasible. A word that is already nonzero at capture start or is dark in both images is a weak candidate; flag it. No cross-run N8D7M2 value comparison.

Implement one default-OFF test-only diagnostic, one incremental Release build and flag-OFF suite. Use at most one ON CPU replay through marker2050 and one OFF control of the pinned stream (N8D4 replay scripts/input); each replay has progress cap ≤600 s, one mini P-lane slot claimed/released, PID-scoped cleanup. Wait while GB7C5 is building/replaying; do not run clang/ninja or a mini boot concurrently. ON/OFF tick2050 frame hash and PPM SHA must match each other and N8D7L baseline. Trace ≤8 MiB, text receipts ≤1 MiB, scratch ≤2 GiB, global internal work ≤200 GiB; runner-dir diff against `14b1e5cb` empty. No speed claim.

Scripted acceptance in `local/research/N8D7M5/check.py`: verify input SHA, source/runner guard, suite result, ON/OFF frame/hash/PPM equality, trace ordering and cap, unique GS addresses with old→new transitions tied to executed packet IDs, post-marker words, and at least two pages if claimed. A missing execution-order witness or changed ON/OFF frame is OTHER. If exact source coverage cannot be proved within one build, stop and hand back the missing call edge without an Odin recommendation.

Deliver `REPORT.md`, bounded word/packet table, trace excerpt, checker and result, exact commands/pins/gaps. Commit private fork diagnostic `[N8D7M5]`, then ssx3 brief/text receipts `[N8D7M5]`, each with `Orchestrated-By: opencode`; no push. Hand back the table and recommended next action; **the orchestrator gates**.
