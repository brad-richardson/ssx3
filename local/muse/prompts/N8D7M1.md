# N8D7M1 — isolated Android independent-oracle package (DeepSeek Flash Go)

You are an OpenCode Go N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/N8D7L/{REPORT.md,ORCH-GATE.md}`, N8D7H `REPORT.md` and its `prepare.sh`, `build.sh`, `source_gate.py`, `apk_gate.py`, `mac_apk_gate.py`. Keep reads narrow, context <60k. Own only `/home/brad/n8d7m1/` on bytesize, `~/dev/ssx3-work/N8D7M1/` on Mac and `local/research/N8D7M1/`. SSH only to `bytesize` for this named WSL build. No Odin/install/launch, game replay, iOS, upstream, other lane edits, global OpenCode config or push. No binary/APK in git.

Goal: package N8D7L's compiled-on-Mac default-OFF independent `PS2X_N8D7L_ORACLE` for a later same-run Odin 448-tile test. The N8D7L Mac oracle matches G43 input 448/448, but does not classify the sparse Odin selected snapshot. Package and source checks only; hand back evidence, no device-cause verdict.

Preflight: bytesize has no heavy job; global Mac ssx3 disk <200 GiB. Use N8D7H's WSL tree as the exact baseline. Copy its PS2Recomp, parallel-gs and jniLibs snapshots to fresh `/home/brad/n8d7m1/` excluding `.cxx`, build caches and runner; prove zero noncache source differences before overlay. Only overlay the N8D7L fork backend `~/dev/ssx3-work/N8D7L/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`, SHA `84a13a80686be8d4ed17750a9399d298ea6b998b98f48d3720b6b5bc839a4645`. The existing fork header `ps2_gs_psmct32.h` must match `9635a40e11bae56189b64d1d9660fb68d0375444d355db43246973adb95e71f7` in both N8D7H and new WSL roots. G43 three selected/stage diagnostic files must stay byte-identical to N8D7H. Pre/postbuild full source gate versus N8D7H: exactly **one** fork backend differs, zero G43/jniLibs differences. Double-read overlay/header/codegen/Turnip/HAL pins, and require Mac N8D7L runner-dir diff vs `14b1e5cb` empty. Stop on mismatch.

Adapt N8D7H's bounded prepare/build/source/APK/Mac gates under `local/research/N8D7M1/`; preserve exact external codegen, Turnip, HAL, Gradle and five diagnostics/UI OFF flags. Check that Android's `PS2X_GS_SHADOW_PARALLEL` and selected-capture source path compile N8D7L oracle, with no test-only or Mac-only preprocessor fence. Run **one** arm64 `assembleRelease` (one named compile repair only if needed), no other heavy bytesize job. Postbuild source gate; APK gate: exactly arm64 runner/Turnip/HAL, double SHA APK and each member, unchanged Turnip/HAL, new runner Build ID, required `PS2X_N8D7F_SELECTED_CAPTURE`, `PS2X_N8D7L_ORACLE`, `[n8d7l]`, oracle control/equality and prior stage/tile/PNG strings present, sole arm64 CMake cache points to N8D7M1 G43/external codegen with five flags OFF. Copy APK to Mac and double-check size/SHA. No device action even if package passes.

Use known N8D7H commands: `ssh bytesize 'wsl -d Ubuntu -- bash -s' < <script>` for prepare/build, `ssh bytesize 'wsl -d Ubuntu -- python3 -' < <gate.py>`, then `ssh bytesize 'wsl -d Ubuntu -- cat <APK>' > ~/dev/ssx3-work/N8D7M1/app-release.apk`. The pane permits `ssh bytesize`; if OpenCode denies it, report the denial and stop. WSL root <10 GiB, Mac APK copy <500 MiB, text receipts <8 MiB. Do not retry a failed gate by loosening it.

Deliver `REPORT.md` with exact commands, SHA pairs, one-file source diff, package member/cache table, build tail, byte caps and gaps; named scripts/JSON/text receipts. Check `git log -1`, commit only named brief/receipts `[N8D7M1]` with `Orchestrated-By: opencode`, no push. Hand back the table, not a GS/device verdict.
