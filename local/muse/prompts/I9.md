# I9 — Boot REAL ELF on iPad: ship guest binary + argv probe to run()

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I8/REPORT.md` first (all of it: device
toolchain + SDL2 iphoneos prebuilt + Release device build, provision/install
via MF1's mechanism, `3006a07` CFBundleName fix on topic branch
`i8-device-bundle-name`, miniaudio green on device, loadELF RAN on the
probe path; gap 4 = this brief). I8 reached loadELF on iPhone; the next
wall is a REAL guest boot on device. Target for this brief: the user's
iPad (confirmed below).

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E`, connected (`xcrun devicectl list devices`
  to re-confirm). Bundle stays `org.ps2x.ps2entryrunner` unless
  provisioning forces a rename (table it).
- INSTALL POLICY (user-confirmed, standing): ALWAYS force-install on iPad
  (and Odin). No fresh prompt needed for `--force-install` on this target.
  Confirm the iPad's state before installing (running sessions are the
  user's — report, don't kill).
- E5 owns the shared fork clone RIGHT NOW (flip-contract taps + builds). Do
  NOT touch `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`: create a
  SEPARATE fork worktree on the SSD at I8's pin `b6252bb` (table HEAD).
  Apply `3006a07` (CFBundleName fix) INSIDE the worktree only
  (cherry-pick or topic-branch checkout — table which); do NOT push to
  `fork/ssx3` while E5 mutates (the merge stays queued).
- Reuse I8's device toolchain (`local/research/I8/logs/
  ios-device.toolchain.cmake`), SDL2 iphoneos prebuilt, and provision/
  install mechanism — table every delta (iPad vs iPhone: arch, bundle,
  profile, screen/screenshot path).
- The task: ship the REAL SSX3 guest ELF (`SLUS_207.72`, 3890784 B — plus
  whatever companion inputs the boot path provably requires, tabled, no
  speculative bundles) to the device and argv-probe to `run()`. First
  observe HOW the app locates guest inputs (container path? bundle
  resource? argv?), then ship through that mechanism.
- Success bars (tabled, not verdicts): device Release build exits 0;
  force-install on the iPad exits 0; guest ELF present via the app's own
  input mechanism; argv-probe launch reaches `run()` or a NEW named wall
  (console + `.ips` + screenshot, tabled against I8's probe-path fate);
  diagnosis names the failing layer with evidence if walled.

## Gates and rules

- Subordinate to E5: builds `-j2`, never `-j4`; build trees on the SSD
  only (`/Volumes/Extreme SSD/ps2x-i9/`); allocated-byte caps declared up
  front + tracked (ExFAT). No PS2 boots → no lease. No `adb`.
- Fork discipline: worktree only; named-files `git add` for any fork edit;
  fork commits pushed to the FORK remote only, never upstream (and no
  `ssx3` push while E5 mutates); generated sources never staged.
- Device discipline (extends I6/I7/I8 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts (healthy app = timeout, I8 gap 6); `._*` purge
  before configure + staging (ExFAT).
- Evidence dir `local/research/I9/` (STANDALONE). Commit with `git add -f`,
  prefix `[I9]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — input mechanism + worktree + build (no iPad yet)

1. Pin + create the worktree (table HEAD); apply `3006a07` in-worktree
   (table method). Derive/confirm the device toolchain for iPad (table
   deltas vs I8's iPhone record).
2. Determine the app's guest-input mechanism from source (argv? container
   relative path? bundle resource?) — table the code path BEFORE shipping
   anything. Build (`-j2`); table binary path/size/sha/identifier.

## Task 2 — provision + force-install + real boot (iPad)

1. Provisioning + sign + force-install (reuse I8's mechanism; table every
   step + the profile/bundle outcome). Confirm the iPad's state before
   installing (user sessions reported, not killed).
2. Ship the guest ELF (+ provably-required companions only) through the
   Task-1 mechanism; verify presence on-device by the app's own path.
3. argv-probe launch toward `run()`: console + `.ips` (sleep + re-`ls`
   before concluding none) + screenshot. Same-fate-vs-new-wall table vs
   I8's `Failed to open/load ELF` probe fate.

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled: diagnosis table (failing layer + evidence). Attempt a fix
   ONLY if config-level (flags, plist, paths, packaging) — no fork
   refactor, no new subsystems. Guest-boot failures are findings, not
   fix mandates.
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I9/REPORT.md`: contract, caps, input mechanism, toolchain/
build record, provision/install/boot tables, diagnosis, exact commands,
gaps. Commit evidence (`[I9]`), stop.
