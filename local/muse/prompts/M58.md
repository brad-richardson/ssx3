# M58 — m15 off-mode decile mass: per-site tables of the 51 (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M57/REPORT.md` first (all of it —
M57 gap 1 = this brief, taken on orchestrator judgment: m15
tail-Y carries ALL 51 pooled off-6+ non-9 sites (dec 0:13 /
1:12 / 3:19 / 4:5 / 5:2) while s0 tail-Y reads 0 in deciles
0–5 and shared reads 67/69 dec-9), plus
`local/research/M33/REPORT.md` (decile-seat machinery) and
`local/research/M56/REPORT.md` (far census: 33 union cols,
221 non-far + 14 singletons). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- The 51 (reproduce exactly or table the mismatch and stop):
  m15 tail-Y decile histogram 0:13 / 1:12 / 2:0 / 3:19 /
  4:5 / 5:2 / 6:1 / 7:2 / 8:5 / 9:75 (n=134); s0 tail-Y 0
  in deciles 0–5; shared 67/69 dec-9 (M57 Task 2).
- The question: per-site tables of the 51 m15 off-mode
  sites (row/col/δ/decile + band/streak/carrier/701 seats
  + d where ranked) — where do they sit (columns? bands?
  near the far site?), and what δ do they carry? Answer
  by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m57/m57.txt` (histograms to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m58/`; evidence
  `local/research/M58/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m57/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m58/` + `local/research/M58/`.
- Evidence dir `local/research/M58/` (STANDALONE). Commit with
  `git add -f`, prefix `[M58]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M57 precedent).

## Task 1 — histogram reproduction (do the 51 reproduce?)

m15 + s0 + shared + pooled tail-Y decile histograms
recomputed from the dumps (must match M57 exactly — m15
0:13/1:12/3:19/4:5/5:2, s0 0 in 0–5, shared 67/69, pooled
167/236 — or table the mismatch and stop):

1. Hist-4 table: all four histograms recomputed + M57
   match flags.
2. Site-51 table: each off-mode site's (row,col)/δ/decile
   + band/streak/carrier/701 seats (M33/M56 machinery on
   51 sites).
3. Decile-bin table: per-bin δ summaries (the 19 dec-3s?
   the 13 dec-0s? — what δ does each bin carry?).

## Task 2 — off-mode geography (where do the 51 sit?)

1. Column table: 51 counts by column over the 33 union
   cols (clustered? on null columns 313/332/314/311? on
   c343? — M56's column table joined with decile).
2. Band/d table: band seats + nearest-neighbor d for the
   51 (banded? near-far? — M56 d machinery on 51 sites).
3. Far-join table: the far site (289,343) against the 51
   (nearest off-mode site by row? by column? same
   dec-3 bin members?).

## Task 3 — controls + determinism (M18–M57 precedent)

1. Positive control: synthetic truth with KNOWN decile
   seats (N mode + N off-mode sites, known deciles/cols)
   — recover the site + column tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M57).

## Report

`local/research/M58/REPORT.md`: DESIGN.md (bars first) +
site tables + geography tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if an off-mode map
discriminates (budget-noted, pre-registered split required
per M23–M57 precedent). Commit evidence (`[M58]`, no push),
stop.
