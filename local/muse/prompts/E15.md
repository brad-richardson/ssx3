# E15 — MPEG type-1 callback→AddBs→GetPicture request/delivery probe + actual-binding fixture (NO regen, NO fix)

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E13/REPORT.md` and
`local/research/E13/NEXT-BRIEF.md` first (all of both: `0x2c5358` restored,
boot parks at the MPEG picture wait — `GetPicture` with `sawInput=0`, main
at PC/RA `0x3b1028`, registration `0x402c08` reached once with type 1 +
callback `0x3b0b10`, ZERO hot-PC dispatches at `0x3b0b10`/`0x3b0b40`/
`0x4029d0`, `MPEG.cpp` records AddCallback as `stream=false` while the
map's only selecting reader requires `stream=true`). Also read E14's REPORT
(absorb VERIFIED but stopped at admission — the absorb stays queued; this
brief does NOT retry it). This brief executes E13's NEXT-BRIEF §2–§4 ONLY
(the probe + fixture — §5's implementation shape stays a proposal for a
later authorized brief). No CSV change, no regeneration, no fork map commit.

## Facts you start from

- CONVERGENCE: the I-lane independently named the SAME layer device-side —
  I18's taps show main frozen in `sceMpegGetPicture` @`0x3b1028`
  (`waitReason=Mpeg`, 0 decoded frames) with workers parked in `WaitSema`
  and ALL GS/DMA counters frozen since boot. Your probe characterizes the
  fork-side request/delivery path; a concurrent I19 brief censuses sema
  ownership + audits the feeder device-side. Neither lane implements the
  fix — implementation follows once both diagnoses agree.
- Baseline: fork `83fb4d60` (pushed); active checkpoint `/tmp/p1-link/runtime`
  (PROTECT it + DerivedData — never reclaim); registry `98753bfa…`; runner
  `b2099130…`; suite 452/452; leaf-24 + consumer-3 + predicate-14 + query-4
  + DROP all pass. Re-verify checkpoint identity first (E13 §1 shape).
- SPACE DISCIPLINE (E14 lesson): internal `/` is at ~3.4 GB free with ZERO
  eligible reclamation targets left (`/tmp/*-link` = only the protected
  checkpoint). This brief needs NO regen and NO rebuild of generated code —
  declare a SMALL explicit budget up front (tap-extension compile + fixture
  link + boot, all bounded) and verify it fits BEFORE extending anything.
  If the budget does not fit: STOP with outcome (iii)-shaped + the exact
  shortfall — no mutation, no reclaim of anything you cannot prove retired
  (E14's ownership-proof machinery, not weaker).
- The task, in strict order: (1) checkpoint re-verify (suite + prior
  fixtures green); (2) extend the CURRENT taps read-only per NEXT-BRIEF §3
  (Create/register identity, picture request, callback selection/delivery,
  guest data source, input/completion, lifetime, existing guards — table
  each probe's required receipt BEFORE the boot); (3) build the
  fail-before fixture linked against the ACTUAL generated wrappers (must
  exercise registration + the real request path — calling the guest
  callback manually and counting that as delivery is FORBIDDEN; must
  distinguish selection vs invocation vs valid-no-input vs delivery);
  (4) ONE guarded probe boot (fresh T13 pre-claims, lease, REPORT_ALL=1,
  E7/E4, wall/progress/byte caps) with graphics capture ALIGNED to the
  state 3→6 transition per NEXT-BRIEF §4 (E13's 599–603 window missed the
  copy phase — do not repeat that) + explicit shutdown-closure receipt.
- Success bars (tabled, not verdicts): checkpoint identity re-verified;
  tap extensions tabled + read-only proven; fail-before fixture exercises
  the real path (selection/invocation/no-input/delivery distinguished);
  one boot captures the §3 probe receipts (or the exact missing receipt
  tabled per the stop rule); graphics joined to an aligned window (or the
  explicit gap tabled); the request/delivery gap ESTABLISHED with ABI
  receipt (or the blocker named — no fix attempted).

## Gates and rules

- NO regeneration, NO map changes, NO fork map commit. Tap extensions are
  OBSERVATION commits if they touch the fork (named files; fork-remote push
  only; generated sources NEVER staged) — or worktree-local if the E-lane
  owner prefers (table the decision). No MPEG code change in this brief.
- Boots: ≤1 guarded probe boot under the shared P-lane lease (fresh T13
  pre-claim checks, wall/progress/byte caps, REPORT_ALL=1, E7/E4 taps).
  No boot without the lease; never wait on another lane's lease — table
  and stop.
- `export COPYFILE_DISABLE=1` on SSD steps; allocated-byte caps declared +
  tracked (ExFAT). No bytesize/WSL. Host builds `-j2` unless the task
  provably needs otherwise (table the decision — E13 used `-j4` as fork
  owner; you are too, but justify it).
- Evidence dir `local/research/E15/` (STANDALONE). Commit with `git add -f`,
  prefix `[E15]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — checkpoint re-verify + tap extensions + fixture (no boot)

1. Checkpoint identity: registry/runner SHAs, name audit vs
   `after/generated.json`, suite re-run, all prior fixtures re-run. Space
   budget declared + verified-fitting BEFORE any extension. Table every
   check.
2. Write the read-only tap extensions (table each probe's required receipt
   first); prove read-only (no guest-state writes, no behavior change when
   untriggered). Link the fail-before fixture against actual generated
   wrappers; run it (selection/invocation/no-input/delivery distinguished).
   Table every receipt.

## Task 2 — one guarded probe boot

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Capture the NEXT-BRIEF
   §3 probes + §4 aligned graphics + shutdown-closure receipt. Table the
   joins; no promotion without an actual request/completion dependency.
2. Gap verdict: request/delivery gap ESTABLISHED with ABI receipt (→ the
   §5 fix brief is next), a DIFFERENT dependency named (→ its exact probe),
   or the measurement repaired (→ table what's missing).

## Report

`local/research/E15/REPORT.md`: contract, checkpoint re-verify, tap +
fixture tables, boot joins, gap verdict, exact commands, gaps. Commit
evidence (`[E15]`), stop.
