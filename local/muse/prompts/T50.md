# T50 — PCSX2: who computes the microcode source address? (healthy side of E40)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL). **Tables + receipts; the
orchestrator decides.** Read first: `AGENTS.md`, `local/AGENTS.local.md`,
`local/research/T49/REPORT.md` (the patched G7 clone, Capture A route to
Select Character, interpreter settings), and `local/muse/prompts/E40.md`
(**use its line formats exactly**).

## Why

The recomp uploads VU1 microcode for `MPG addr=0` from EE `0x435bf8`; PCSX2
runs the code that sits at EE `0x4349b8` (0x1240 bytes earlier in the same
microcode library). We need PCSX2's REF-tag source addresses and the EE
store that writes them, to diff against E40.

## Change (one bounded log-only patch on T49's clone, gated like T48/T49)

1. In PCSX2's VIF1 DMA chain processing (`Dmac`/`Vif1Dma`, REF/REFS/REFE
   tags): `mpgsrc` lines as E40 (tag EE address, id, qwc, ref addr, the 8
   TTE bytes) for tags whose addr falls in `0x430000–0x440000` or whose TTE
   bytes carry an MPG.
2. An EE memory-write watch on each new `tag_at+4` (in the EE memory write
   path, bounded to those addresses): `tagwrite` lines as E40, with
   `cpuRegs.pc`, `ra`, and the GPRs `$a0–$a3,$v0,$v1,$t0–$t9` at the store.
   Interpreter/EE-recompiler caveat: if the EE recompiler hides the PC, run
   the EE interpreter for this capture and say so.
3. Capture over Select Character settled (T49's route and settle point).
Prove preservation as T48/T49 did.

## Budgets, deliverable

≤2 builds, ≤2 captures, 3 h, bytesize cap 5 GB. Traces to
`~/dev/ssx3-work/T50/` + share mirror + `local/research/T50/` if < 5 MB.
`local/research/T50/REPORT.md`: patch (text), hook sites, preservation,
tables (distinct ref addrs for `MPG addr=0` uploads, `tag_at`, storing
function, computing registers). No verdict. `[T50]` commit (`git add -f
local/research/T50 local/muse/prompts/T50.md`, trailer `Orchestrated-By:
Muse Code`, `git log -1` first), no push.
