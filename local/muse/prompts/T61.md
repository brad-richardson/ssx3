# T61 — PCSX2: the mode-6 template setter (value-filtered rerun of T60)

Same rules and tree as T60. Read `local/research/T60/REPORT.md` first.

T60's caps (300/300) filled during the MENU cycle (tw0 `0xcc→0→0xc→0xcc`),
before any `0x1b0`. Rerun the identical capture with two changes:

1. **Value filter.** Emit `tpl` only when `new` tw0 has mode bits set
   (`new & 0x3C0 != 0`), and log **both old and new tw0/tw1**. Emit `app`
   only when the sampled tw0 has `& 0x3C0 != 0`. Caps: 200 each.
2. **Unfiltered per-vsync census:** `appsum vsync n_app n_tpl
   mode_hist=<m:count,…>`, where m = (tw0>>6)&0xF over every app that
   vsync. First 3000 lines.
3. **Appender pc census:** once per vsync, the distinct pcs that store
   into the list item area `[0x8095f0+0x80, +0x80*64)` (T59 fold), with
   counts: `apc vsync pc=count,…`. This covers T60's missing counts 7–36.

One build, G13 preservation, one capture (`t58-pre-sc-state` → K → SC
settled). ≤1 build (+1 on compile fail), ≤2 captures, 2 h, cap 2 GB.

`local/research/T61/REPORT.md` with these tables:
- mode-6 setters (pc, ra, regs, first vsync relative to K);
- the mode histogram per vsync around SC entry;
- the appender pcs.

No verdict. `[T61]` commit (`git add -f local/research/T61
local/muse/prompts/T61.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
