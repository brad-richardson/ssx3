# N4 — Android APK on the folded fork: rebase, lock-screen-safe, profileable; first on-device CPU profile (title + Select Character)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
Android builds run on bytesize via `ssh bytesize` → WSL; the Odin is on
the mini's USB). **Tables + receipts; recommend, the orchestrator decides.**
Read first: `AGENTS.md`, `local/AGENTS.local.md` (Odin: keyguard check, the
"Use USB for" dialog → BACK after `am start`), `local/research/N3/REPORT.md`
(build recipe, env shim, staging), `local/research/PF1/REPORT.md`
(profiling blocked on a non-profileable APK), `local/research/E32/REPORT.md`
(fold commit list).

## Why

The fork is one branch now (`fork/ssx3` @ `e57b5f8`: codegen dir, bypass,
pad script, weave deinterlace). The Odin runs the recomp at 0.36–0.43×
with the game thread saturated, and PF1 couldn't say *where* the time goes
because the APK isn't profileable. Before any threading work, we need a
per-function profile on the device. We also need runs to survive the lock
screen.

## Missions

1. **Rebase** bytesize's local `n2-android` (`619d48a`) onto `e57b5f8`
   (fetch `origin`). Drop commits whose content is now on `ssx3` (N2's
   codegen-dir port, the bypass); keep the Android build files and the
   `ps2x.env` shim. Table: commit → kept/dropped/why. Local branch only.
2. **Manifest:** `android:showWhenLocked="true"`, `android:turnScreenOn="true"`
   on the NativeActivity, and `<profileable android:shell="true"/>`
   (release build stays release: optimization flags unchanged, symbols
   kept in an unstripped copy of `libps2EntryRunner.so` for simpleperf
   reports; the APK may stay stripped).
3. **PNG frame export on Android** (N3/PF1: `.txt` sidecars write, PNGs
   don't; raylib FILEIO path). One small fix so `PS2X_FRAME_DUMP_DIR`
   PNGs land.
4. Build (`./gradlew assembleRelease`, N3 recipe), APK SHA two reads,
   copy to the mini, `adb install` (always install).
5. **Odin, one lease session:** keyguard `showing=false`, BACK after
   launch, screencap to confirm focus.
   - Launch 1: clean env (CD image + `PS2X_SKIP_MOVIE=1`) to the title;
     `simpleperf record -g` 30 s on the app at the title; `simpleperf
     report` top-25 by self and by children (host-side with the unstripped
     `.so`). Also confirm one PNG frame dump landed.
   - Launch 2: pad script `25000:start:5000,90000:cross:5000` (screencap
     to confirm Select Character, adjust nothing mid-run; if it lands on a
     different screen, profile that screen and say so), then 30 s
     `simpleperf record -g` on the settled 3D menu.
   - Group the profile into buckets: guest code (`sub_*`), VU1 interpreter,
     GS CPU rasterizer (`gs_cpu_backend`), GIF/VIF/DMA, present/upload
     (raylib/GL), kernel/syscalls, other. Guest vsyncs/s from `[frame:dump]`
     ticks alongside, as PF1 did.
6. Force-stop, release the lease, clean `/data/local/tmp` staging.

## Budgets and stop rules

- ≤3 builds, 2 launches (+1 only if a launch is voided by lockscreen/USB
  dialog), time box 5 h. bytesize: one heavy job at a time (T48 may be
  running PCSX2: check and wait, don't contend). bytesize byte cap 10 GB.
- If `simpleperf` is still denied with `profileable`, table the exact error
  and stop after launch 1's other checks.
- Speed numbers only from a logs-off build; say which build the numbers
  come from. No push anywhere.

## Deliverable

`local/research/N4/REPORT.md`: rebase table, manifest diff, PNG fix diff,
APK pins, launch tables, **the two profile tables (top-25 + buckets)**,
guest rate per launch, gaps. Text receipts in `local/research/N4/`
(`simpleperf report` text, not the raw `perf.data`: that goes to
`~/dev/ssx3-work/N4/` + share mirror). `[N4]` commit, `git add -f
local/research/N4 local/muse/prompts/N4.md`, trailer `Orchestrated-By: Muse
Code`, `git log -1` first, no push.
