# I19 — Sema census + MPEG feeder audit + completion-fix PROPOSAL (no implementation; DIAG_SEMA relaunch first)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I18/REPORT.md` first (all of it: P1c
sampler + `[diag:frame]` tap prove liveness and NAME the layer — H-c, guest
WAITING: main frozen in `sceMpegGetPicture` @`0x3b1028` (`waitReason=Mpeg`,
0 decoded frames, none of the 7 `completeExternalWait` sites fired),
workers parked in `WaitSema` on semas 26/30/32/36 (thread 4's vsync pump on
31 the only execution), ALL GS/DMA counters frozen since boot, CD reads +
callbacks ALL in the first ~15 s then 75+ s silence; gap 1 = this brief).
This brief executes I18's gap-1 FIRST HALF (census + audit + proposal).
Implementation is explicitly NOT in this brief (see Coordination).

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I18 app is STILL INSTALLED (with the P1c + `[diag:frame]`
  taps compiled in). No fresh prompt needed for iPad installs. Confirm the
  iPad's state before probing (user sessions reported, not killed).
- COORDINATION (read carefully — two lanes, one subsystem): the E-lane
  owns the MPEG fix decision fork-side (E13's NEXT-BRIEF §5 proposes the
  runtime shape; a concurrent E15 brief probes request/delivery + ABI).
  Your job is the DEVICE-side census + audit + a fix PROPOSAL with
  file/line receipts — NOT an implementation. If your audit and E15's
  probe disagree on the mechanism, table the disagreement exactly (both
  receipts) — do not reconcile by fiat. No fork-code behavior change in
  this brief (reads + config-only probe only).
- The task, in strict order: (1) RELAUNCH the installed I18 app with
  `PS2X_DIAG_SEMA=1` ADDED to the env (config-only — NO rebuild, NO
  reinstall unless the app is gone, tabled) and census sema 26/30/31/32/36
  ownership (creator thread, signalers, counts — what P1c's `waitId` alone
  cannot answer); (2) AUDIT the MPEG feeder path in the worktree sources
  (read-only): who calls `sceMpegAddBs`/flush, why the CD-stream pump stops
  before EOF, why 0 frames decode, how `stream=false` registration
  interacts with the selecting reader — with file/line receipts for every
  claim; (3) write the completion-fix PROPOSAL (exact files/functions, the
  delivery semantics, the regression tests it needs, what E15's ABI receipt
  must confirm first).
- BASE: you need sources to audit but NO build — create a SEPARATE fork
  worktree on the SSD at CURRENT `fork/ssx3` HEAD (table the hash; NEVER
  move it), port NOTHING (no build ⇒ no ports needed), and audit read-only
  (zero commits expected — table `status` clean at end). Check whether the
  E-lane owns the shared clone before touching it (one `worktree add` +
  read-only inspection only). If the I18 app is GONE from the iPad (user
  removed it?), table that + STOP with a rebuild recipe (no rebuild here).
- Success bars (tabled, not verdicts): DIAG_SEMA probe names sema
  ownership (or the exact obstacle tabled); feeder audit complete with
  file/line receipts; fix proposal written (files/functions/semantics/
  tests/preconditions); E15-agreement/disagreement tabled if E15 has landed
  (else the comparison points the next brief needs).

## Gates and rules

- NO build, NO codegen, NO install (unless the app is gone — then STOP,
  not rebuild), NO fork commits. This brief is reads + ONE config-only
  probe. All trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i19/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`.
- Device discipline (extends I6–I18 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before staging evidence.
- Evidence dir `local/research/I19/` (STANDALONE). Commit with `git add -f`,
  prefix `[I19]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — DIAG_SEMA census probe (iPad, config-only)

1. Confirm the iPad's state + that the I18 app is still installed (user
   sessions reported, not killed). Relaunch with `PS2X_DIAG_SEMA=1` added
   (same ISO+env recipe + `PS2X_DIAG_PERIOD_MS=15000` kept).
2. Census semas 26/30/31/32/36 (creator, signalers, counts) + re-confirm
   the I18 steady state (thread pcs/status, stub/syscall sets). Console +
   `.ips` (sleep + re-`ls` before concluding none — first `ls` also checks
   for late I18 filing) + screenshot.

## Task 2 — feeder audit + fix proposal (read-only sources)

1. Audit (file/line receipts): AddBs callers, CD-stream EOF path, decoder
   wake sites, `stream=false` vs selecting reader, userdata/ABI shapes.
   Table every claim with its receipt; table every unanswered question as
   a named gap (not a guess).
2. Fix proposal: exact files/functions, delivery semantics (incl.
   re-entrancy/cancellation per E13 NEXT-BRIEF §5), regression tests,
   preconditions on E15's ABI receipt. E15-agreement table if landed.

## Report

`local/research/I19/REPORT.md`: contract, caps, census tables, audit with
receipts, proposal, E15 comparison (or comparison points), exact commands,
gaps. Commit evidence (`[I19]`), stop.
