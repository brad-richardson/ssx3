# N8D7A — static pre-circuit1 probe design (OpenCode Go Muse Contributor)

You are an `opencode-go/muse-spark-1.3-contributor` worker on a **read-only code study**. Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md` and `local/AGENTS.local.md`. Cite the repo, not the web. Read only this brief; `local/research/N8D6C/{ORCH-GATE.md,vector-audit.json}`; `local/research/N8D6A/orch-replay-excerpt.txt`; `local/research/N8D4/REPORT.md`; and the named source files below. Use `lsp` goToDefinition/findReferences to confirm code connections when available; record empty/error instead of guessing. No build, boot, device, source/config edit, web, push or upstream contact. Only write `local/research/N8D7A/REPORT.md` and `source-map.tsv` (each <100 KiB). Wall cap 12 minutes; context target <35k. Produce the source map early, then the report. No engineering verdict.

Facts to start from: N8D6C one Odin frame at tick2050/FBP112/PMODE `ff21` was already sparse at circuit1, 21/448 active, then merged 21/448 and final 39/896, all controls128; final sampled/raw vectors equal 896/896 and the orchestrator viewed the same-run mostly black PNG. Its **formal** launcher category remains OTHER because a comma-led logcat continuation blocked the live vector parser. N8D6A one pinned N8D4 stream Mac replay had circuit1/merged 300/448 and final 567/896. The live Odin and Mac replay GS streams are not byte-identical, so that comparison alone cannot identify an Android driver cause.

Question: what **one** bounded observable upstream of circuit1 would separate these alternatives on an aligned Odin run? **V:** the GPU VRAM bytes for the display rectangle at `DISPFB1.FBP` are already sparse/wrong. **S:** those bytes are broad/correct, but `sample_crtc_circuit` or `sample_quad` reads/draws them sparsely. **T:** both input bytes and circuit image are broad, but the stage tap reports sparse on Odin. Correct behavior model: with the same GS packet/priv state as a broad Mac replay, decoded VRAM input and circuit1 should both be broad; if the stream differs, compare only within one Odin run and state that limit. Give each alternative a predicted input-byte and circuit-image observation; if two predictions coincide, say what additional check separates them.

Read these source paths only: `/Users/brad/dev/ssx3-work/N8D6A/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`, `/Users/brad/dev/ssx3-work/G43/parallel-gs/gs/gs_renderer.cpp`, `gs/gs_renderer.hpp`, `gs/gs_interface.cpp`, and the directly named shader source reached from `sample_crtc_circuit` (locate by `rg`; read that file only). Focus on backend `syncPriv`/`Present`, renderer VRAM flush/order at `vsync`, `sample_crtc_circuit` (around line 4262), `sample_quad` shader mapping, buffer ownership and any existing VRAM dump or copy API. Confirm whether promoted backbuffer can bypass VRAM for this frame; state exactly what evidence is missing if the excerpt does not prove it. Avoid unrelated source scans.

`source-map.tsv`: header `step<TAB>source_anchor<TAB>input<TAB>output<TAB>uncertainty`; exact `file:line` anchors, one row each for backend priv sync, VRAM flush/barrier, circuit input binding, shader address/decode, circuit1 attachment, stage tap. `REPORT.md`: pinned revisions/paths and exact commands; source-path table; V/S/T prediction table; **one** candidate probe with placement, byte cap, default-OFF flag, Mac calibration and single Odin acceptance conditions, plus its failure/stop branches; gaps and LSP result. Keep the candidate at design level. The observable must sample the actual circuit input independently of the output-stage tile shader; if that cannot be done without a new decoder, hand back the feasibility evidence and stop. Hand back tables and recommendation, **do not conclude a cause**. Commit only the two receipts as `[N8D7A]` with `Orchestrated-By: opencode`, explicit paths, no push.
