# M38 — Shared-site triple: cross-shape value/gap comparison at (278,314) + (295,332) + (276,313) (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M36/REPORT.md` first (all of it —
M36 gap 2 is this brief, taken on orchestrator judgment: the
(278,314) + (295,332) missings on all three shapes and the
(276,313) missing on s2+s3 — 3 sites recurring across shapes
with identical s0 values but per-shape gaps), plus
`local/research/M37/REPORT.md` (the corrected pooled universe:
24 pooled extra sites = c54's 10 + 14 others — M36's "23"
matches no M36 extras count, M37 §Brief-count note; use 14).
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- Shared sites (M36 missing-site tables — reproduce exactly or
  table the mismatch and stop):
  - (278,314) off 356468, Y, |δ_s0| 20, g_s0 42, missing on
    s2+s3+s700 (all noncell on Q): g_2 40, g_3 42, g_700 42.
  - (295,332) off 378264, Y, |δ_s0| 10, g_s0 −22, missing on
    s2+s3+s700 (all noncell on Q): g_2 −21, g_3 −21,
    g_700 −21 (the pooled missings' 3 −−, M36 §Step 3).
  - (276,313) off 353906, Y, |δ_s0| 8, g_s0 17, missing on
    s2+s3 only (both noncell on Q): g_2 16, g_3 16.
- The question: at shared (r,c), s0's values are identical — do
  the Q shapes' gaps agree (same standing, same magnitude) or
  diverge per shape (s2's 40 vs 42 at (278,314) already hints
  divergence)? Answer by table: per-site per-shape values +
  recurrence context (do these rows/cols recur at other
  missing/extra sites?) + gap-sign pattern vs the pooled
  missings (7/10 ++, M36).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m36/m36.txt`
  (the 3 shared sites + per-shape gaps to reproduce — match
  exactly or table the mismatch and stop),
  `/Volumes/Extreme SSD/m34/m34-census.tsv` (FULL TSV guard).
  Work dir: new `/Volumes/Extreme SSD/m38/`; evidence
  `local/research/M38/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m37/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m38/` + `local/research/M38/`.
- Evidence dir `local/research/M38/` (STANDALONE). Commit with
  `git add -f`, prefix `[M38]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M37 precedent).

## Task 1 — shared-site recompute (do the 3 sites reproduce?)

The 3 shared sites recomputed from the dumps (must match M36
exactly — offsets, |δ_s0|, s0 gaps, per-shape statuses, |δ_Q|
n/a, per-shape gaps — or table the mismatch and stop):

1. Per-site per-shape table: (r,c) × s2/s3/s700 (s700 n/a at
   (276,313)): Q status, gaps, gap signs, |δ_s0|.
2. Divergence table: per-site range of Q gaps across shapes
   (agree / diverge-by-1 / diverge-more — numbers only).
3. Sign table: per-site signs vs the pooled-missing 7/10 ++
   split (which side does each shared site fall on?).

## Task 2 — recurrence + standing context

1. Row/col recurrence: rows {276, 278, 295} + cols
   {313, 314, 332} at OTHER missing/extra sites across the 24
   pooled extras + 10 pooled missings (shared rows beyond the
   shared sites? shared columns?).
2. Standing context: the 3 sites' noncell-Q standing vs the
   pooled missings' standings (all 10 noncell on Q per M36 —
   reproduce the split) + vs the extras' bulk/noncell split.
3. s700-absent check: (276,313) is s0-present and
   s700-present (not missing on s700) — table s700's row-list
   at c313 around r276 (does s700 hold the row s2/s3 drop?).

## Task 3 — controls + determinism (M18–M37 precedent)

1. Positive control: synthetic truth with KNOWN shared sites
   (N sites missing on known shape subsets, known gaps) —
   recover the per-site per-shape table exactly.
2. Determinism: re-run Task 1 on s0+2+3+700, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M37).

## Report

`local/research/M38/REPORT.md`: DESIGN.md (bars first) +
shared-site tables + context tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a shared-site map
discriminates (budget-noted, pre-registered split required
per M23–M37 precedent). Commit evidence (`[M38]`, no push),
stop.
