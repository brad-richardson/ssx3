# P12 — Settle KE_UNKNOWN_SEMID (-408) vs kernel -1 + sweep dead -419 consts

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P10/REPORT.md` §P10-7 item 3 first
(the carried divergence: fork returns `KE_UNKNOWN_SEMID` (-408) for
unknown sema ids; kernel behavior unverified), plus `local/research/P11/REPORT.md`
§P11-7 item 2 (4 dead `-419` constexprs), plus `local/research/P1z/REPORT.md`
§P1z-1/§P1z-3 (the BIOS re-derivation method that settled P10). This
brief settles the -408 divergence kernel-first per the review's §7.5
rule — same shape as P10, smaller blast radius expected. P1ad/M16 run
concurrently in other panes — you share only the fork remote.

## Facts you start from

P10 settled the poll-miss value (-1) but left the unknown-id value
open: what the stock kernel returns for semaphore syscalls with an id
that was never created (or already deleted), and what the fork returns
instead (`KE_UNKNOWN_SEMID`). The 4 dead `-419` constexprs (`SIF test
:31`, `EeScheduler.cpp:44`, kernel tests `:32`, `State.h:37`) are
leftovers of the settled divergence. Suite baseline is all-green
(P11: 428/428/0 at `bfa0213`; count the current total yourself —
peers add tests).

## Gates and rules

- Lease `P12`: boots ONLY while holding `/tmp/ssx3-host-lease` — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/p12-waits.log`. Builds any time (`-j4`). Max 2 boots,
  ≤90 s foreground each. No `adb`.
- Fork: the divergence files ONLY (unknown-id paths + the 4 constexpr
  sites + tests — record the list). `git add` those NAMED files only
  — never `-A`, never stage or touch anything else. Verify the diff
  before committing. One commit per change with the
  `Orchestrated-By: Muse Code` trailer. `git pull --rebase` before
  pushing; `git push` ONLY inside the fork clone to the `fork` remote.
  Stop on any foreign rebase conflict. Never commit generated sources
  or `._*`. The in-repo BIOS stays pristine (work on a copy).
- Evidence dir `local/research/P12/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P12]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Re-derive + blast radius (lease-free).** From the BIOS copy:
   the kernel's unknown-id return(s) for the sema syscalls (words +
   addresses, P1z method). From the fork: every `KE_UNKNOWN_SEMID`
   return site + every consumer (game-visible? boot-observed?
   test-only?) — table what changes value under each candidate
   truth, P10-2 style.
2. **Implement the §7.5 winner + sweep.** Kernel-first (match the
   kernel unless the blast-radius table shows a game-visible
   regression — then keep `-408` with the evidence as the recorded
   justification). Ride-along: delete the 4 dead `-419`
   constexprs (same commit or second — recorded). BEFORE/AFTER
   receipts (same tests, both trees). Suite must stay all-green
   (state totals, name any face).
3. **Boots only if reachable.** If the divergence fires anywhere in
   the boot path (census/boot greps decide): up to 2 lease-gated
   boots + one ladder table vs the then-current baseline (state
   which). If unreachable: the census receipt showing zero
   unknown-id returns across the reference logs instead of boots —
   evidence decides, recorded.
4. **Report.** `local/research/P12/REPORT.md`: re-derivation,
   blast-radius table, winner + diff, BEFORE/AFTER, boots-or-why-
   not, exact commands, receipt paths, "What I could not do".
   Commit, remove the lease if held, stop.
