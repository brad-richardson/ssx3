# M52 — Peak-row vs value agreement: peak-anchored residual tables (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M26/REPORT.md` first (all of it —
M26 gap 2 = this brief, taken on orchestrator judgment: 5/8
columns share peak rows cross-frame but only 6/65 shared-site
δ values agree (M25 H5) — positional vs value structure
split), plus `local/research/M25/REPORT.md` (H5: 6/65 pooled,
per-column agrees 257:0/277:0/296:1/301:0/321:2/340:2/342:0/
343:1; M26 peak table: peaks agree on 257/277/296/301/342,
differ on 321/340/343). Fully offline — SSD dumps read-only,
no harness code, no replays, no lease. No device work.

## Facts you start from

- H5 pins (reproduce exactly or table the mismatch and stop):
  6/65 shared (col,row) sites agree exactly in δ (share
  0.0923) with per-column agrees 0/0/1/0/2/2/0/1 over
  n 10/10/9/11/10/8/5/2; no column all-equal.
- Peak pins (M26 cross-frame table — reproduce exactly):
  peak rows agree 5/8 (257: 25/25, 277: 25/25, 296: 26/26,
  301: 27/27, 342: 27/27; differ 321: 25/26, 340: 26/32,
  343: 27/289).
- The question: peak-anchored residual tables — residuals
  (δ − column-peak-δ? row-offset profiles?) per column per
  frame + agreement-vs-distance-from-peak (do the 6
  agreeing sites sit AT peaks? do peak rows agree in
  residual while disagreeing in δ?). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles), `/Volumes/Extreme
  SSD/m25/m25.txt` + `/Volumes/Extreme SSD/m26/m26.txt`
  (H5 + peak rows to reproduce — match exactly or table
  the mismatch and stop). Work dir: new `/Volumes/Extreme
  SSD/m52/`; evidence `local/research/M52/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m51/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m52/` + `local/research/M52/`.
- Evidence dir `local/research/M52/` (STANDALONE). Commit with
  `git add -f`, prefix `[M52]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M51 precedent).

## Task 1 — H5 + peak reproduction (do the splits reproduce?)

M25 H5 + M26 peak rows recomputed M25/M26-verbatim from the
dumps (must match exactly — 6/65, per-column 0/0/1/0/2/2/0/1,
5/8 peaks — or table the mismatch and stop):

1. Agreement table: all 65 shared sites with s0-δ vs
   m15-δ + agree flag (name the 6 agreeing sites).
2. Peak table: all 8 columns' argmax rows both frames +
   agree flag (5/8 reproduced; tie rule M26-verbatim).
3. Peak×agree join: per column peak-agree × n-agree (do
   peak-agreeing columns hold the 6 agrees?).

## Task 2 — peak-anchored residuals (positional vs value split?)

1. Residual tables: per column per frame δ − peak-δ +
   row-offset profiles (M25 peak-anchored method verbatim).
2. Distance table: each shared site's row distance from its
   column peak vs agree flag (agrees at distance 0?).
3. Residual-agreement table: H5 re-scored on residuals
   (peak-subtracted δ — does agreement rise above 6/65?).

## Task 3 — controls + determinism (M18–M51 precedent)

1. Positive control: synthetic truth with KNOWN peaks +
   KNOWN agreeing sites (N peak-agree cols, N agreeing
   sites at known distances) — recover the H5 + residual
   tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M51).

## Report

`local/research/M52/REPORT.md`: DESIGN.md (bars first) +
reproduction tables + residual tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a residual map
discriminates (budget-noted, pre-registered split required
per M23–M51 precedent). Commit evidence (`[M52]`, no push),
stop.
