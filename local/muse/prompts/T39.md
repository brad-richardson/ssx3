# T39 — No-hold control run at a matched race phase: dense series with NO input (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T38/REPORT.md` first (all of it: R1
reproduced the chain to LIVE gameplay, ONE 1037.3 ms D-pad Left HOLD
@T+504.54 on a live race (pre-pair 00:01:25 2ND/6 40% / 00:01:31 2ND/6 44%),
dense 10-snap post-hold series to 00:02:12 6TH/6 58% with NO separable hold
response — position decays 2ND→6TH distributed post-hold with no
hold-coincident step, RDC gated out 10/12, SCPS120 still rails ±120 on 7/11
hops incl. the no-input pre-pair gap and the hold hop; race clock ~1.4× wall
under turbo; G1 proposes (a) no-hold control, (b) sub-second exposures —
this brief takes option (a)). This brief executes T38's G1(a). Reuse T38's
scripts (`t38-auto.sh`, `t38-cropdiff.py`, `t38-track.py`, `t38-analyze.sh`,
`t38-vcount.sh` — copy, do not modify the committed originals).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T38 verified (re-verify sha/size/rev/
  status/pad-binding + NVM state before running; table the receipts).
- GATE NOTE (T38 G1 — gate on the HUD, not the frame): gameplay hops are
  large (~10.07–22.33 dense) because the race never stops moving. Live-race
  criterion = race clock advancing + position/progress HUD read off VIEWED
  snaps. The LIVE-LIKE proxy (departed + non-panel + motion ×2) still gates
  the dense phase in-script.
- The task: reproduce the T38 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → LIVE-LIKE proxy gate → ≤3 attempts) to LIVE gameplay, then
  run the dense phase with NO input at all: pre-pair (`npre1`, `sleep 2`,
  `npre2`) + a sham "hold window" (same 1 s wall gap the hold occupied,
  but NO keydown — table the wall timestamps proving the gap) + the dense
  d-post1..10 series. Match PHASE, not seed (AI lineup/RNG differ run to
  run — table the phase match: race clock/position/progress at the sham
  window vs T38's hold window). Table per-snap HUD + tracking (T38's
  RDC-gate/SCPS40/SCPS120 with validity channels) + whole-frame hops.
- This run's purpose: the control distribution — position volatility,
  rail rates, RDC gate-out rates WITHOUT any input, against which T36's
  tap, T37's tap, and T38's hold are judged. If the control shows the same
  decay/rails/floods: table that finding + the recipe (T38 G1(b)
  sub-second exposures, or the next input class) and STOP. ONE bounded
  re-attempt allowed only if the run provably never reached live gameplay
  at a matched phase.
- Success bars (tabled, not verdicts): live race reproduced at a matched
  phase (HUD advancing, phase-match tabled); sham window with proven
  no-input gap; dense 10-snap control series (HUD + tracking + hops);
  control-vs-input comparison table (T36/T37/T38 hold/tap windows vs this
  control); full input log (proving NO input) + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T38 G2: single-shot
  scripts, runs ≤6 min (table any overrun with full dmesg coverage),
  preserve emulogs first, dmesg + btime + init age, UNFILTERED dmesg +
  event log around flaps, 534 ms holds NOT bisected, `;` chains only INSIDE
  one `wsl` call, one `wsl` call per ssh; post-run dmesg read IMMEDIATELY
  after the run ssh returns). COUNT AcceptAsync lines in the committed
  dmesg files with exact counts + uptime-stamp positions relative to the
  run window. Runs in flap windows must be effects-verified or re-run
  (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T39/` (STANDALONE). Commit with `git add -f`,
  prefix `[T39]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + sham window + dense control series (≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T38 §T38-0 shape): build/input receipts, NVM state, T38
   reference snaps present, free space both ends. Table the phase-match
   targets (T38's hold-window clock/position/progress) BEFORE the run.
2. Per attempt table: race reproduction (chain scores + live-race gate),
   sham-window row (wall gap timestamps, proven no-input, phase match),
   dense 10-snap series (HUD + tracking + hops per snap), control-vs-input
   comparison.
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T39/REPORT.md`: pre-run receipts, attempt tables, sham
log, control series + comparison (or phase-miss finding + recipe), WSL
table, exact commands, gaps. Commit evidence (`[T39]`), stop.
