# T56 — PCSX2: who writes scratchpad item 0x70000000 (w0 = 0x1b0)? Healthy side of E44

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). **Tables + receipts; the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T55/REPORT.md`,
`local/muse/prompts/E44.md` (**same line formats**).

## Change (log-only, on T55's clone; EE interpreter)

A write watch on scratchpad words `0x70000000..0x7000000c` and
`0x70000500..0x7000050c` covering every path into scratchpad: EE stores
(memWrite*/SQ/SWC1/SQC2 to the `0x70000000` range) **and** SPR DMA
(`toSPR`, channel 9: log `via=spr-dma src=0x<EE madr>` when the transfer
covers the watched words): `spw vsync addr value via pc ra a0..a3 v0 v1
t0..t9 s0..s7`, first 64 per word, over Select Character settled. If a DMA
copy, add a second capture watching that EE source word (writer pc/ra/regs).

## Deliverable

≤2 builds, ≤3 captures, 3 h, cap 2 GB. `local/research/T56/REPORT.md`
(patch, hooks, preservation, writer tables). No verdict. `[T56]` commit
(`git add -f local/research/T56 local/muse/prompts/T56.md`, trailer
`Orchestrated-By: Muse Code`, `git log -1` first), no push.
