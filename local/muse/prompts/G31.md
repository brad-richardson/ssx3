# G31 — (b1) vs (b2): VRAM-sample misaddressing vs promoted-image content (Odin; in-vsync state dump)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G30/REPORT.md` first (all of it:
174/174 uploads land byte-exact, scene + Z raster land, control unchanged,
zero stray — yet circuit1 emits uniform cleared pattern from stale
non-cleared FBP112, with no cleared 512×448 region anywhere in VRAM: (b)
CONFIRMED, (a) refuted per write class). This brief executes G30 §4's ONE
next action ONLY. No upstream contact of any kind (standing no-upstream
order — local hunks only, filing stays local).

## Facts you start from

- The split: (b1) `sample_circuit` VRAM-sample misaddressing/mis-decode
  on Adreno vs (b2) promoted-backbuffer-image content (promotion engaged
  but fed cleared bytes, or engaged differently than mac).
- Discriminator (G30 §4): in-`vsync()` state dump — `promoted1` null? +
  DISPFB-region VRAM bytes at sample time + promoted-image checksum if
  present — on the SAME dump/iterations (G13 dump, 2 iterations, flag
  SET, `PGS_SKIP_COMPILATION_TASKS=1`, non-sanitizer shape).
- The G30 binary is 100% zero-destroyed (9th recurrence, completed after
  G30's report-time intact read — orchestrator-quantified: 0 nonzero of
  265,847,904 B; run validity unaffected). The G28 binary is INTACT
  (`450471e2…`, re-sha at start per hygiene). Clone holds G22/G26/G28/
  G29/G30 hunks uncommitted (HUNK_MATCH re-verify at start; G29-E1
  truncated FNV basis carries for comparability).
- Task 1 (no device until §2): STATIC design — table the exact dump
  site(s) in `vsync()` (promotion-state read + DISPFB-region bytes +
  promoted checksum), the expected values under (b1) vs (b2), and the
  decision matrix (what mixed outcomes mean). Name the ONE hunk.
- Task 2 (ONE run max): the state probe — ONE hunk max (diagnostic-dump
  ONLY), ONE build max (NEW dir, G22 recipe, `-j2`), full identity,
  verify-then-push with NO gap (push-time + post-run + report-time
  re-shas — destruction is progressive), TWO bounded runs max
  (diagnostic + one retry iff O1/O3 fires instead of first draw).
- Success bars (tabled): design table (sites + expectations + matrix);
  the (b1)-vs-(b2) VERDICT (with the discriminating bytes); if a run:
  exit/wall/fate + receipts + tombstone triage (if any) + scanouts
  scored; the ONE next action (fix shape vs next wall — adoption stays
  queued until brightness is understood AND this split closes).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g31/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (diagnostic-dump ONLY); ONE build max (new dir, G22 recipe,
  `-j2`); TWO bounded device runs max; no tuning loop, no second shape, no
  new dumps; no lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g31/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G31/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G31]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).
