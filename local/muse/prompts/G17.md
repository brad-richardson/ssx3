# G17 — O2-argument debugger run: name the bad descriptor input at flush_descriptor_set():3505, package the Adreno repro (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G16/REPORT.md` (ssx3 mirror) first (all
of it: outcome (b) CLEAN-RUN ELIMINATION — ASan+UBSan exit 0, 18/18 frames,
0 sanitizer lines, 7/7 scanouts pixel-identical to G13; the writer is
Android-specific; the ONE next action = this brief) + G15 §§2c–2e (O2's
frames: SIGSEGV fault 0xf0 in Adreno `vkUpdateDescriptorSetWithTemplate` ←
`flush_descriptor_set():3505`, modal 3/6 incl. under HWASan). This brief
executes G16's §4. No renderer changes, no new dumps.

## Facts you start from

- Pins (re-verify, do not move): paraLLEl-GS
  `3a66c1976170cbc2cb53a3593fabbc7c4b2ccfbd` + G14 S1–S3 shims + G11/G8+G10
  hunks (G15 §2a/G16 §2a clone state); G14 binary `c4cd63b4…` (the same
  bytes that produce O2 — reuse, do not rebuild); rich dump sha
  `154d9d85…`; NDK r30 lldb-server + Apple-lldb entry-stop flow (G15 §2b
  friction table — `process kill` before `quit`, pc+4 `debug_break` skip).
- Device: Odin3 (`622c49b1`, Android 15). Transient dir
  `/data/local/tmp/g17/` ONLY; `mg/` never touched; remove the dir +
  the `tcp:5041` forward at end.
- The task: ONE bounded on-device lldb run breaking at
  `flush_descriptor_set():3505` (the O2 site) to name WHICH of `vk_set /
  update_template / bindings` is bad: capture the descriptor-set state at
  the break (the three inputs' values + which is null/wild + the
  surrounding call's arguments), the backtrace, and the minimal facts a
  driver-bug filing needs (device, driver version, API call, bad input,
  repro = G14 binary + dump + args). TWO bounded device runs max
  (instrumented + one retry if O1/O3 fires instead of O2 — O2 is modal
  3/6 but the victim lottery stands; classify every launch as in G15 §2c).
- Success bars (tabled, not verdicts): pins reproduce G16 §2a; O2 caught
  under the debugger at `:3505` (or the exact launch census if the lottery
  misses); the bad input NAMED with captured values (or the exact obstacle
  tabled per the stop rule); tombstones/logcats/lldb capture pulled;
  device dir removed; the ONE next action (driver filing package, or the
  next naming attempt the numbers justify).

## Gates and rules

- Stop rule (G15 shape): TWO bounded device runs max — no tuning loop, no
  renderer changes, no new dumps, no source fix in this brief (a fix needs
  the named input first; the fix brief comes next).
- Byte caps: declare up front for retrieval (tombstones + logcats + lldb
  log + scripts, SSD `ps2x-g17/`); `ps2x-g13`–`ps2x-g16` + all build dirs
  read-only (0 growth); internal `/` delta ≤1 GB, no clones/builds on
  internal; `export COPYFILE_DISABLE=1` on SSD steps. No P-lane lease (no
  recomp boots/builds, no fork writes). No bytesize/WSL.
- Clone discipline: ZERO clone edits. No third-party code copied into any
  project tree.
- Evidence: ps2xGS commit `[G17]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G17/`
  (`git add -f`, prefix `[G17]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — pin verify + O2-argument debugger run (Odin)

1. Re-verify pins (clone HEAD/status, G14 binary sha host-side, dump sha);
   push binary + dump + lldb-server to `/data/local/tmp/g17/` (sha match
   on-device); forward `tcp:5041`.
2. lldb run with a breakpoint at `flush_descriptor_set():3505`: on the
   break capture `vk_set` / `update_template` / `bindings` values +
   backtrace + call arguments. If O1/O3 fires instead: classify the launch
   (G15 §2c shape) and spend the ONE retry. Pull tombstones + logcats +
   lldb log; remove the device dir + forward.

## Task 2 — package + verdict

1. Name the bad input with captured values (or table the exact obstacle).
   Assemble the driver-bug filing facts (device/driver/API/input/repro).
   Score any scanouts with `g14-diff.py`, else SCORE N/A (never faked).
2. Hypothesis verdict + the ONE next action (filing package → minimal-fix/
   workaround brief input, or the next naming attempt).

## Report

`local/research/G17/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, pin table, launch census, capture tables, verdict,
exact commands, gaps. Commit evidence (`[G17]` both trees), stop.
