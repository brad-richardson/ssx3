# M48 — Shape-734 triple move: dest-row listing + value table (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M28/REPORT.md` first (all of it —
M28 gap 4 = this brief via M29 gap 7, taken on orchestrator
judgment: 734's c340 + c343 full wipes with dest collision at
c342), plus `local/research/M29/REPORT.md` (734-c342: n/ov 7/5,
rows [26,33], |δ| 21,11, gaps −50,−26/−8,−13, bulk ×2, dec
9,9) and `local/research/M34/REPORT.md` (734's census row —
unnamed moves? reproduce it). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- 734's move (reproduce exactly or table the mismatch and
  stop): tail 94, J 0.8846, moved [340,342,343]; c340 0/0
  wipe → dest c342 (+2 rows); c343 0/0 wipe → dest c342
  (−1 row? M28 rows: +2/+0.5/+2 vs −1/+3.0/+0 — reproduce
  the displacement rows); c342 7/5 (+2) extra-only with
  dest rows [26,33].
- The collision: TWO wiped columns drain into ONE dest
  column. The question: dest-row listing (full c342
  row-list: kept + [26,33]) + per-site value table for
  734's full sets (wiped c340/c343 rows + c342 dest rows
  + any unnamed 734 moves per M34). Answer by table
  (list, don't assign rows to source columns).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28-census.tsv`
  + `/Volumes/Extreme SSD/m34/m34-census.tsv` (734 rows to
  reproduce — match exactly or table the mismatch and
  stop). Work dir: new `/Volumes/Extreme SSD/m48/`;
  evidence `local/research/M48/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m47/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m48/` + `local/research/M48/`.
- Evidence dir `local/research/M48/` (STANDALONE). Commit with
  `git add -f`, prefix `[M48]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M47 precedent).

## Task 1 — 734 wipe reproduction (do the 3 columns reproduce?)

734's row recomputed from the dumps (must match M28/M34
exactly — tail, J, moved cells, standings, unnamed moves? —
or table the mismatch and stop):

1. Wipe table: c340 + c343 s0-rows vs 734-rows (empty) vs
   miss rows (full wiped row lists).
2. Dest table: c342 s0-rows vs 734-rows (kept + [26,33])
   vs extra rows ([26,33] must reproduce M29).
3. Displacement table: M28's c340→c342 + c343→c342 rows
   reproduced (+2/+0.5/+2, −1/+3.0/+0 — table the rule's
   rows, don't re-judge).

## Task 2 — full-set value table (wiped + dest + unnamed?)

1. Wiped-value table: per-site |δ_s0|, s734 status, signed
   gaps, gap signs for ALL c340/c343 wiped rows (M36-style).
2. Dest-value table: c342's [26,33] + kept rows with |δ|,
   statuses, gaps, signs (|δ| 21,11 bulk must reproduce).
3. Collision table: wiped-row count (c340 + c343) vs dest
   growth (+2) vs kept rows (do counts admit a full
   accounting? numbers only — no assignment).

## Task 3 — controls + determinism (M18–M47 precedent)

1. Positive control: synthetic truth with KNOWN collision
   (2 wipe cols + 1 shared dest, known rows/values) —
   recover the wipe + dest + value tables exactly.
2. Determinism: re-run Task 1 on s0+734, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M47).

## Report

`local/research/M48/REPORT.md`: DESIGN.md (bars first) +
wipe/dest tables + value tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a collision map
discriminates (budget-noted, pre-registered split required
per M23–M47 precedent). Commit evidence (`[M48]`, no push),
stop.
