# E47 — Why is the race world dark? Triage with existing diagnostics (recomp)

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md` (two mini boot slots, `local/tooling/p_lane_lease.py`, PID
  tracking);
- `local/AGENTS.local.md` (including the `local/tooling/ee/ee-*` lookup
  helpers; use them for any code fact);
- `local/research/E46/REPORT.md` (Part 2: boot scripts, the race route,
  the frames).

## Facts

- E46 (fork `ssx3` `2e21cdc`, canonical codegen now
  `~/dev/ssx3-work/codegen-ssx3` with 18 `extra_function_starts`) fixed the
  menu 3D: the Select Character rider renders.
- On the race route (E46 boot `e46g`, race from tick ~7403), gameplay runs:
  HUD live, speed, tricks, "+5000 STYLE BONUS". But **the world is black
  apart from faint streaks** (`local/research/E46/frames/e46g-race.png`).
  The only missing-target left is `0x3b1140` ×3 (E48 owns it).
- Select Character in the same boot is now a healthy 3D control.

## Hypotheses (the tables must tell these apart)

- **H1:** world draw records are never produced. The walker's mode
  histogram shows no world modes, or the VU1 world programs never start.
- **H2:** VU1 runs the world programs but exits on the 65,536-cycle budget
  or with wrong microcode.
- **H3:** GS draws are issued but invisible. Check alpha, ZBUF/TEST
  (ztest fails), FRAME/FBMSK, SCISSOR, and fog/colour.
- **H4:** the world is drawn, then overwritten or cleared later in the
  frame.

## Run (no code changes; existing env flags; build `~/dev/ssx3-work/E47-build` from `~/dev/PS2Recomp`)

- **Boot A** (E46's race-route script, 300 s, one slot, own cwd):
  - `PS2X_GFX_STATS` + `PS2X_VU1_TRACE` + `PS2X_E43_TRACE` (draw-record
    census) + `PS2X_VIF_MPG_LOG`, all windowed on two windows: SC steady
    (~ticks 1300–1310, the healthy control) and race (~ticks 7600–7610);
  - frame dumps on.

  If the flags can only take one window, use two boots, one per window.
  Check each flag's window semantics in the source first.

- **Tables (SC vs race side by side):**
  - GS prims per frame by type, context, textured, and alpha on/off;
  - for the 10 largest-area race draws: FRAME, ZBUF, TEST, ALPHA,
    SCISSOR, TEX0;
  - VU1 program starts by startPC, with cycles and budget exits;
  - draw-record modes per frame;
  - MPG uploads.

## Budgets and stop rules

≤2 boots, 3 h, cap 3 GB. Diagnosis only: no fix.

## Deliverable

`local/research/E47/REPORT.md` with the tables, which hypothesis the
evidence supports, and exact commands. `[E47]` commit (`git add -f
local/research/E47 local/muse/prompts/E47.md`, `git log -1` first,
trailer `Orchestrated-By: Claude Code`). No push.
