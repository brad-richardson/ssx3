# N8D7M7 — locate an executable selected-VRAM divergence boundary (read-only, OpenCode Go)

You are the N-lane design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, the complete N8D7M6 `REPORT.md` and `ORCH-GATE.md`, then N8D7M3/M4 `ORCH-GATE.md` and N8D7M5 `ORCH-GATE.md`. Read only the relevant source in private fork `~/dev/ssx3-work/N8D7L/PS2Recomp` at `a8cfefa` and paraLLEl source `~/dev/ssx3-work/N8D7F/parallel-gs` at its current pinned rev; record both pins. Confirm code links with LSP when available, direct source otherwise. Write only this brief and `local/research/N8D7M7/`. No source edit, build, replay, boot, Odin/iOS, lease, push, project-board or global config edit. Keep this design under 20 min and committed text <512 KiB.

Observation to explain: N8D7M6 captured one complete Odin tick2050 GS stream (SHA `f6a78f71…a593`) and replayed those exact bytes on Mac paraLLEl. All 11 selected-image descriptor fields match; Mac input/oracle 300 active of 448, Odin input/oracle 23; viewed race frame broad on Mac and mostly black on Odin. The loss is at or before the selected-VRAM staging snapshot on this same stream. N8D7M3/M4's draw counts, pending flags, isolated words and later tick2052 frame do not separate writes from copy timing; do not revive those categories without an execution witness.

Trace the actual renderer command sequence from GIF packet acceptance through GPU buffer mutation, `copy_buffer` to `selected_vram_staging`, submit/fence/wait and map. Name the memory owner and execution clock at each point, with source line citations. Find one *bounded, default-OFF, same-tick* checkpoint or paired checkpoints that could tell apart: **W** source GPU VRAM is sparse after all relevant submitted work is complete; **O** source becomes broad only after the selected copy (ordering); **C** source is broad at the copy boundary but staging is sparse (copy/barrier/readback); **D** source and staging are broad but selected decode is sparse (currently disfavored by independent oracle); **OTHER** no nonperturbing checkpoint or gate failure. Spell out precise predictions for all four on the **full 448-tile census**, not eight words. If a proposed wait, barrier or second copy changes the execution order, label it an intervention and explain what it can and cannot prove. Do not claim a shader/Turnip/driver root cause from this design.

Your deliverable is a table of candidate tap sites, command/fence order, bytes and synchronization required, whether each is observational or perturbing, and which W/O/C/D pairs it separates. Include an explicit one-run acceptance gate: same exact stream and 11-field descriptor, marker2050, source/staging full-vector SHA and active counts, positive/negative controls, output caps, no extra device launch; or state that no such one-run gate is sound and give the smallest next **Mac-only** experiment. Add `check.py` to verify source-citation rows and that every claimed category has a unique observable; it must mark nondiscriminating rows OTHER. Hand back evidence, gaps and recommended next action; **the orchestrator decides**. Commit `[N8D7M7]` with `Orchestrated-By: opencode`, no push.
