# M57 — far vs null-5 decile seats: does the far site's dec-3 seat discriminate? (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M56/REPORT.md` first (all of it —
M56 gap 1 = this brief, taken on orchestrator judgment: the
lone far site m15 c343 r289 reads s0-P1 decile 3 while tail
seats read dec-9 heavy — M33 shared 85/94 dec-9; H5 0/1),
plus `local/research/M33/REPORT.md` (decile-seat machinery +
dec-9 mode: shared-tail decile histogram, per-shape seats) and
`local/research/M19/REPORT.md` (P(δ>0) + s0 P1 edges). Fully
offline — SSD dumps read-only, no harness code, no replays, no
lease. No device work.

## Facts you start from

- The far site (reproduce exactly or table the mismatch and
  stop): m15 (289,343), δ −15, d=262, band 1, s0-P1 dec 3,
  streak 343, carrier-none, 701-out, at-peak (M56 seat table).
- The null set (reproduce exactly or table the mismatch and
  stop): next-5 nearest non-far by d desc — s0 c313 r292
  d16 δ−16; s0 c332 r295 d9 δ+10; s0 c332 r304 d9 δ−12;
  s0 c314 r269 d7 δ+12; m15 c311 r317 d3 δ+9 (M56 null-5).
- The question: decile seats of far vs null-5 sites — does
  dec-3 discriminate the far site from its 5 nearest
  neighbors, and where does each sit against the tail's
  dec-9 mode (M33 shared 85/94)? Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` + `/Volumes/
  Extreme SSD/m15/` (column profiles + tails + s0 P1 edges),
  `/Volumes/Extreme SSD/m56/m56.txt` (far seat + null-5 to
  reproduce — match exactly or table the mismatch and stop).
  Work dir: new `/Volumes/Extreme SSD/m57/`; evidence
  `local/research/M57/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m56/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m57/` + `local/research/M57/`.
- Evidence dir `local/research/M57/` (STANDALONE). Commit with
  `git add -f`, prefix `[M57]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M56 precedent).

## Task 1 — seat reproduction (do the 6 seats reproduce?)

Far seat + null-5 recomputed from the dumps (must match M56
exactly — far dec 3 / band 1 / streak 343 / none / out /
at-peak; null-5 rows/cols/d/δ — or table the mismatch and stop):

1. Seat-6 table: far + null-5 rows with band + s0-P1 decile +
   streak + carrier + 701 + at-peak + d + δ (M33 machinery
   on 6 sites).
2. Decile-context table: each site's decile against the M33
   shared-tail decile histogram (dec-9 85/94 mode — where
   does dec-3 sit? where do the null-5 deciles sit?).
3. Distance-decile table: d vs decile for the 6 (does decile
   track distance from the band?).

## Task 2 — decile-margin census (is dec-3 off-mode?)

1. Tail-decile table: full s0+m15 tail-Y decile histogram
   recomputed (M33 shared + per-frame splits — dec-9 share
   on each frame?).
2. Margin table: far dec-3's distance from the mode in rank
   and share (how many tail sites share dec-3? null-5 deciles'
   shares?).
3. Column table: decile seats by column for c343 + the 4
   null columns (313/332/314/311 — do far/null columns differ
   from the tail mode?).

## Task 3 — controls + determinism (M18–M56 precedent)

1. Positive control: synthetic truth with KNOWN decile seats
   (N mode-seat + N off-mode sites, known deciles) —
   recover the seat + histogram tables exactly.
2. Determinism: re-run Task 1 on s0+m15, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M56).

## Report

`local/research/M57/REPORT.md`: DESIGN.md (bars first) +
seat tables + histogram tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a decile map
discriminates (budget-noted, pre-registered split required
per M23–M56 precedent). Commit evidence (`[M57]`, no push),
stop.
