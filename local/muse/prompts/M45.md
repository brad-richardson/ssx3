# M45 — Shape-22 bottom cluster: full-set attribution (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M33/REPORT.md` first (all of it —
M33 gap 3 = this brief via M34 gap 5, taken on orchestrator
judgment: 2 outside-col privates (393,307)/(402,295) + the 2
c296 far extras form a rows-393–402 group), plus
`local/research/M29/REPORT.md` (s22 c296: n/ov 11/9, extras
[400,401], |δ| 8,8, gaps 22,24/−1,−2, noncell ×2, dec 9,9)
and `local/research/M34/REPORT.md` (s22's census row — mover
list + k). Fully offline — SSD dumps read-only, no harness
code, no replays, no lease. No device work.

## Facts you start from

- Shape-22 moved set (reproduce exactly or table the
  mismatch and stop): c296 extras (400,296)/(401,296)
  |δ_22| 8,8, noncell on s0, gaps gQ 22,24 / gO −1,−2,
  dec 9,9; outside-col privates (393,307) + (402,295)
  (values: NEW DATA — attribute them).
- s22's FULL moved-cell set comes from `m34-census.tsv`
  (M34's s22 row: k + cells + standings — reproduce the
  row first; the bottom group may not be the whole set).
- The question: full-set per-site attribution for shape 22
  (|δ|, statuses, gaps, signs, deciles) + bottom-cluster
  geometry (rows 393–402: pairwise distances, shared
  rows/cols with the c296 pair?). Answer by table.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (s22 row to reproduce — match exactly or table the
  mismatch and stop). Work dir: new `/Volumes/Extreme
  SSD/m45/`; evidence `local/research/M45/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m44/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m45/` + `local/research/M45/`.
- Evidence dir `local/research/M45/` (STANDALONE). Commit with
  `git add -f`, prefix `[M45]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M44 precedent).

## Task 1 — s22 moved-set census (is the bottom group the whole set?)

s22's row recomputed from the dumps (must match M34 exactly
— k, cells, n/ov, standings, deltas — or table the mismatch
and stop):

1. Moved-cell table: all s22 cells with n/ov + standings +
   deltas + row-lists (c296's [400,401] must reproduce M29).
2. Private table: (393,307)/(402,295) with s0 rows of their
   columns (are their columns s0-empty? s0-bearing?).
3. Completeness table: bottom-group sites vs full s22 moved
   set (is anything outside rows 393–402?).

## Task 2 — full-set attribution + cluster geometry

1. Per-site value table: ALL s22 moved sites with offset,
   |δ_22|/|δ_s0|, statuses, signed gaps both frames, gap
   signs, deciles (M36-style rows).
2. Cluster geometry: pairwise row distances over the 4
   bottom sites + column seats (do 307/295 relate to
   c296? shared rows anywhere?).
3. c296 side-by-side: s22's pair (8,8/noncell) vs the
   pooled extra splits (near/far/noncell — which side?).

## Task 3 — controls + determinism (M18–M44 precedent)

1. Positive control: synthetic truth with KNOWN shape set
   (N in-cluster + N outside sites, known values) —
   recover the census + value tables exactly.
2. Determinism: re-run Task 1 on s0+22, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M44).

## Report

`local/research/M45/REPORT.md`: DESIGN.md (bars first) +
census tables + attribution tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a cluster map
discriminates (budget-noted, pre-registered split required
per M23–M44 precedent). Commit evidence (`[M45]`, no push),
stop.
