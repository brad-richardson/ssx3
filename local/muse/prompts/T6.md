# T6 — Build cache check: measure incremental pain, ccache/split only if it bites (no boots)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `docs/research/review-2026-09-19-progress.md` Part 2 §12
row T6 first (the whole spec: clean build 511 s; generated
`register_functions.cpp` 398k lines; every brief rebuilds;
measure incremental time; if >3 min, add ccache and split the
generated output into N files for parallel compile). This is a
MEASUREMENT brief: one brief only if it bites. Peer lanes run
concurrently — coordinate build parallelism (see rules).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight). Build dirs
  in use: `/tmp/p1-link/runtime`, `/tmp/t1-link/runtime`,
  `/tmp/t5-link/runtime`, `/tmp/t5-base-link/runtime`,
  `/tmp/t5-rule-link/runtime` (do NOT delete or reconfigure
  other agents' dirs — measure in your own `/tmp/t6-link/`).
- Recent build data points (verify, don't trust): T1 full
  configure+build inside a ~50 min session; T5 configure 63 s;
  P-lane briefs routinely build `-j4` (P1af/P1ag/T1/T5 all
  built this way); unity build batch size 32
  (`PS2X_RUNNER_UNITY_BUILD_BATCH_SIZE=32`); sccache flag
  `PS2X_ENABLE_SCCACHE=ON` exists in the configure lines.
- The question: what does a one-file runtime edit + rebuild cost
  a P-lane brief TODAY (wall time, and what dominates it)?

## Gates and rules

- NO lease (no boots at all — builds + measurements only).
  `adb` not used. No `git push` anywhere except the fork rule
  below (only if you change the fork at all — expected: no).
- Build etiquette: `-j4` MAX, and check `pgrep -f 'cmake --build|ninja|clang\+\+'`
  before starting: if a peer brief is mid-build, table the
  contention + wait for it (log waits to
  `$W/P1/run/t6-waits.log`) rather than halving both builds'
  throughput. Builds are throughput-contended by rule.
- Fork changes: ONLY if incremental >3 min AND the fix is
  contained (ccache enablement and/or generated-output split
  into N files). If you change the fork: named `git add` ONLY,
  one commit + `Orchestrated-By: Muse Code` trailer, `git pull
  --rebase` first, `git push` ONLY in the fork clone to `fork`.
  If incremental ≤3 min: ZERO fork changes, the report IS the
  deliverable (close T6 as measured-not-biting).
- Evidence dir `local/research/T6/` (STANDALONE). Commit with
  `git add -f`, prefix `[T6]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — measure (the deliverable unless it bites)

In your own `/tmp/t6-link/` (T1's configure flags verbatim —
copy from an existing `CMakeCache.txt`, record the flag set):

1. Full configure + full build wall time (one shot, `-j4`,
   no peer contention — wait for a quiet window per the
   etiquette rule; table contention if any occurred anyway).
2. Incremental matrix (median of 1 run each is fine, table
   exact): touch-and-rebuild (a) one runtime `.cpp` (hot:
   `ps2_runtime.cpp`), (b) one runtime header (cold:
   `ps2_log.h` — wide inclusion), (c) one test `.cpp`, (d)
   no-op rebuild (nothing touched — the floor). Per cell:
   wall + what rebuilt (targets/objects count from ninja).
3. Dominance table: which step owns the incremental wall
   (unity-batch recompile? link? generated-runner rebuild?) —
   from `ninja -d explain` or timing output, tabled.

## Task 2 — fix ONLY if it bites (>3 min incremental)

1. If any of (a–c) exceeds 3 min: implement the smallest fix
   (ccache first if the miss is recompile; generated-output
   split second if the miss is one giant TU), re-measure the
   SAME matrix, table before/after. Suite must stay green
   (run it once after the fix; table total/passed/failed).
2. If all ≤3 min: table "measured, not biting" with the
   numbers + close (no fork changes, no Task-2 build).

## Report

`local/research/T6/REPORT.md`: flag set + full-build wall +
incremental matrix + dominance table + (fix + before/after
only if it bit) + gap rows. Commit evidence (`[T6]`, no
push), stop.
