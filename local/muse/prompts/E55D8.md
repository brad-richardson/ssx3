# E55D8 — find a real save/login menu route (OpenCode Go)

You are the E-lane read-only route worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55D7/{REPORT.md,ORCH-GATE.md}`, `local/research/E55D6/ORCH-GATE.md`, and `local/research/I26/ROUTES.md`. Write only this brief and `local/research/E55D8/`. No fork/source edit, build, boot, device/card action, push or status/todo/ledger edit. Cite repo/receipts, not the web. For EE code links use `local/tooling/ee/ee-at`, `ee-func`, `ee-xref`, `ee-label`; name nothing that `ee-label` calls unknown.

Goal: locate an **observed** save/login screen and supported button route so the next worker can test whether `sceMcGetDir` or `sceMcRead` is reached. E55D7 proved three static callers feed a save-manager cluster but found no menu-to-cluster edge. String labels such as `cFEStateLogin` and `Title_SaveProfile` do not prove an on-screen route. I26-FAST empty-card race boots already gave zero GetDir/Read calls; do not propose simply repeating one.

Inventory only existing title/menu screenshots, videos, route logs and input scripts under `local/research/` and `~/dev/ssx3-work/` as needed, with a bounded search (<40 files opened, no exhaustive scan). Prefer a current folded-build capture; record its binary/stream pin, visible screen text, tick/time and exact buttons from the route receipt. If an existing screen path is found, link each button transition to a screenshot/log or replay input; distinguish a displayed save UI from a card API call. Use at most two EE xref chains to check whether its identified handler could reach the E55D7 three callers, marking unresolved indirect jumps unknown. If no real route is evidenced, say so with the nearest observed screen and the missing transition. Do not invent button presses.

Predeclare A = observed save/login screen plus evidenced button sequence from title and a bounded card-probe route; B = observed screen but no evidenced button transition; C = only names/handlers and no observed screen; OTHER = pin/tick mismatch or ambiguous source. For A, design one future empty-card detour boot: exact route change from I26-FAST, E55D3 probe flag, one mini P-lane slot, <=500 s/<=2055 ticks unless justified, first GetDir/Read record stop, 4 MiB log, isolated empty card, PID-scoped cleanup, and OTHER for no record or a write-only card manifest. **Do not run it here.** For B/C, give a specific next observation required to produce a route, and no boot. A GetDir/Read status line proves reachability only; guest bytes and changed-card baseline need later work.

Script acceptance: supply `local/research/E55D8/check.py` that validates each proposed route step has a named existing frame/log/input receipt, file SHA and tick/sequence citation; it must reject an unsupported step. If outcome B/C, checker validates that the candidate table has explicit unknown fields and no runnable detour command. Orchestrator checks meaning and frames. Budget <20 min read-only search, <512 KiB new text. Deliver `REPORT.md`, bounded evidence index and checker result. Commit `[E55D8]` with `Orchestrated-By: opencode`, no push. Hand back the table; **the orchestrator judges the gate**.
