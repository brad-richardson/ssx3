# T63 — PCSX2: who writes SC template 2 at 0x61c910 (w0 = 0x1b0)? Whole-boot watch

Same rules and tree as T62. Read `local/research/T62/REPORT.md` first.

T62 found that the SC appenders (`0x37ad44`/`0x37b474`) copy a second
template at `0x61c910`, whose tw0 is already `0x1b0` at K+66. The menu
template sits at `0x61c8fc`, so the two are 0x14 apart: probably an array
of 20-byte templates. Its writer ran before the window.

## Change (on T62's tree)

1. **Watch words `0x61c8fc..0x61c94b`** (4 templates × 0x14) with T59's
   fixed fold, over every EE store path plus T58's DMA paths:
   `tw vsync addr vaddr old new via pc ra a0..a3 v0 v1 s0..s7`.
   Log only when the value **changes**, with no per-word cap and 2000 lines
   total.
2. Keep T62's `appx` and `axfirst`.

## Runs

- **Run 1: cold interp boot → MENU → F1** (T58 run 3's script, dat-t57).
  This covers ELF load through MENU.
- **Run 2: the resulting state → K → SC settled** (T62's capture shape).
  Use the fresh MENU state from run 1, so no write can hide inside a
  savestate load. If run 1's save fails, use `t58-pre-sc-state` and say so.

## Deliverable

≤1 build (+1 on compile failure), 2 runs, 2.5 h, cap 3 GB.
`local/research/T63/REPORT.md`:
- every change to template 2's w0–w4 (and templates 1, 3, 4) in time
  order, with pc/ra/regs;
- the first writer of `0x1b0` into `0x61c910`;
- where the value comes from: an immediate, a load (with its address), or
  a DMA source;
- preservation.

No verdict. `[T63]` commit (`git add -f local/research/T63
local/muse/prompts/T63.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
