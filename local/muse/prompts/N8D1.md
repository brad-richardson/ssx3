# N8D Part 1 — locate the first bad Android image stage

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§4/6. Read `local/research/N8C2/REPORT.md` in full when it is available, plus N4's PNG-export finding and `local/research/X12/{REPORT.md,rows.tsv}`. Work only in `local/research/N8D1/`, `~/dev/ssx3-work/N8D1/`, and isolated bytesize WSL `/home/brad/n8d1/`; the pinned N8B1/N8C2 inputs are read-only. Commit text/small frames only, no fork source edit/push. No upstream contact or iPhone work.

N8C2's app-local error-return `libhardware.so` cleared the loader: bundled Turnip HMI mapped, HAL opened, paraLLEl initialized, GIF/presents ran, and the route reached tick 2087. All three viewed Android screenshots were mostly black, with striped rectangular fragments already at menu tick 809. The same backend's Mac GB6C race is recognizable. Exact Vulkan device/driver identity was not logged. N4 found `ExportImage` fails to write PNGs on the Odin's app files path while `ExportImageToMemory` plus `std::ofstream` works (`local/research/N4/REPORT.md` §Mission 3). The current N8B1 source still uses `ExportImage` in `dumpPresentationFrame`.

**Discriminator:** a same-run host upload PNG and Android screencap at the menu. If both show the same fragmented blocks, corruption is at or before the host upload buffer; if the PNG is recognizable but the screencap is fragmented, investigate raylib/GL presentation. If host PNG is absent/invalid, record an instrumentation failure, not a renderer result. Different capture ticks can change animation, so compare scene legibility and artifact shape, not byte equality. This probe cannot distinguish Turnip GS output from readback/frontend packing when the host PNG is bad; a later part will sample those stages. Do not infer a gralloc cause or a speed gain.

1. Pin N8B1 source `17e90ded3689685ad359b76a9168c80a1f752e2d`, N8C2 APK SHA `86ee7b723979c6727f03aaeaa031e9bd6dd3481a640af3b1eb06fd41e6fc7640`, stock ELF `1b49d05c…67af7bc`, ISO `3c2f8eb1…ae3f5`, Turnip member `717812c3…1ac29d` and shim member `1b49d27c…cdfc387`; verify full hashes from N8C2 receipts, twice before copy/use. Copy N8C2's isolated source/package setup into fresh WSL `/home/brad/n8d1/`, excluding build caches as practical. Preserve source and package pins.
2. One diagnostic candidate: replace only the two `ExportImage(img, pngPath)` calls in `dumpPresentationFrame` with the N4-proven `ExportImageToMemory(img, ".png")` + binary `std::ofstream` + `MemFree` pattern, with bounded error log. Diff and source SHA receipt. Do not alter backend, pixels, dimensions, padding, shaders, Turnip, or pad route. Build one arm64 APK, second build only for one named compile/package repair. Gate exactly runner, pinned Turnip, and shim in arm64; no x86; all five N8C2 diagnostics flags OFF; double-read APK/member hashes and runner Build ID. No device work if packaging fails.
3. Odin `622c49b1`: check lease free, keyguard `showing=false`, battery ≥20% charging/full. Claim lease. **Always install** this APK. Verify installed APK, stock ELF and ISO with two matching device SHA reads. Set the N8C2 backend/Turnip/movie/pad env plus `PS2X_FRAME_DUMP_DIR` to an app-writable external-files subdir and `PS2X_FRAME_DUMP_ONCE_TICKS=780,810,840`. Ensure the dump dir is empty. One NativeActivity launch, capture one screencap around menu tick 809–850, stop at tick ≥850 or first definitive failure; title cap 180 s, hard cap 240 s. Force-stop on every exit path, confirm PID absent, release lease.
4. Pull at most three selected host PNG+txt pairs and one SHA-paired screencap. Require PNG decode, bounded dimensions, nonempty content, metadata ticks, same PID log, and report `upload` vs fallback. View every image and hand back a table of appearance; the orchestrator gives the verdict. If the app-file PNG writer fails again, stop after this one run. State exact device/driver identity as unproved unless the same-PID log actually names it.

Budgets: ≤2 WSL builds, one Odin install/launch, ≤10 GiB new WSL scratch, ≤1 GiB Mac scratch, ≤16 MiB logs, ≤10 MiB images, ≤240 s launch. Check Mac `disk_budget.sh` before/after writes >1 GiB. No speed number from this diagnostic run. Deliver `local/research/N8D1/REPORT.md`, bounded logs, build/source hashes, APK gate, device SHA pairs, PNG+txt/screencap SHA pairs and cleanup receipt. Run a small Python acceptance script that checks the declared files exist, each SHA pair matches, and all PNGs decode; include its source/output. Commit explicit report/receipts/small images `[N8D1]` with `Orchestrated-By: Codex`; no push. Hand back the table, do not declare the renderer cause.
