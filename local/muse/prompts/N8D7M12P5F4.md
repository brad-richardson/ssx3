# N8D7M12 Part 5F4 — queued-GS command fingerprint, host validation only

You are the N/G OpenCode Go implementation worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5F3/{REPORT.md,ORCH-GATE.md}`, Part 5M4 gate, Part 1 N8D7M12 REPORT, and this brief. Own only `local/research/N8D7M12P5F4/` in ssx3 and private worktree/build/run under `~/dev/ssx3-work/N8D7M12P5F4/`. Fork worktree `…/PS2Recomp` already exists, clean on branch `n8d7m12-p5f4` at `a608ed1e161f60334a0cf3a80d1af3e54b692bd2`; verify. This pane has a per-worker edit exception only for that private root; verify with a tiny sentinel and remove it. No other fork worktree edit, Android build/device action, fork/main push, global/board/ledger edit or upstream contact. Time box 40 min; new private bytes ≤10 GiB, receipt text ≤512 KiB. Write REPORT/checker early.

Goal: add a **dev-only, default-off** fingerprint of commands in the order the GS worker actually consumes them, to distinguish packet-delivery variation from later GPU state/readback variation in a future same-settings Odin pair. Same APK/stream/settings OFF1/OFF2 already had sampled priv41/41 equal but VRAM16/41 and present25/41 equal, first difference tick850. The corrected Mac ON/OFF control is byte-equal. Do not infer a GPU cause from this part.

Source model: `GsWorker::threadMain` pops FIFO and handles a `GsCommand`, then signals its RPC. `GS::executeQueuedCommand` sees actual consumed kind/fields; `GS::drainQueue` queues a Fence unless quiescent. Part 5F3's original parser-side hash was rejected because a fixed input stream would trivially match. The corrected design is at worker consumption; `PrivWrite` carries an opaque callable so its content is unavailable there. The digest is a **diagnostic sample of hashed fields**, never proof of full command or GPU execution identity. A 64-bit digest is preferred to reduce accidental collision risk; still state the limit.

Allowed source changes: `ps2xRuntime/include/runtime/gs/gs_frontend.h`, `ps2xRuntime/src/lib/gs/gs_frontend.cpp`, `ps2xRuntime/src/lib/gs/gs_replay_core.cpp`, focused `ps2xTest/src/ps2_gs_queue_tests.cpp` only if needed. Add a replay-only env gate such as `PS2X_GS_REPLAY_PKTSEQ=1` (default off). Hash command kind + consumed fields for `GifPacket` (current path + bytes), `NoteGifPath`, `RegWrite`, `UploadImageNative`, `NativePacked`, clears and `WriteVram`; hash `PrivWrite` kind only and declare its content gap. Snapshot with a Fence or an equivalent queue-ordered mechanism that remains valid when `drainQueue` sees a quiescent worker. Emit `GB4_PKTSEQ tick=<t> seq=<16-hex> commands=<n>` on the same 41 sampled markers in the existing stdout/logcat path; leave existing `parallel.hashes` format and default-off behavior intact. No per-packet log, GPU readback or new GPU wait. If the design requires a broader API change, stop with a source-grounded table instead of expanding scope.

Acceptance: a focused queue test must show the same consumed sequence gives the same digest, reordering two distinguishable commands changes it, and default-off emits nothing. Reuse the existing queue test file; do not write tests that only mirror the hash implementation. Checker should inspect exact changed paths, branch/rev, gate, log grammar, limits and test output. At most **one** Release/Ninja configure+build (`ps2x_tests`) and one no-replay suite run, plus **one** same pinned-stream Mac replay validation (one dynamic mini lease, 300 s wall, 16 MiB log, 64 MiB output, own PID; no retry). Wait for no heavy clang/ninja before build or replay. Use the Part 1 CMake flags in its REPORT §4 with this worktree and a unique `…/build`, external `PS2X_GAME_CODEGEN_DIR`; never generate guest code in the fork. If any build/suite/replay gate fails, stop and report the first failure, no tuning loop. Require suite585/585 or document any intentional new test count, exact 41 ordered `GB4_PKTSEQ` rows, exact 41 original replay rows, binary/stream SHA pairs, lease/PID cleanup; diagnostic wall is not speed. If budget or permissions prevent validation, commit the bounded source and report B/OTHER with no Android claim.

Before committing the fork: `git diff --check`; list exact source diff; `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` must be empty; no generated/game data. Commit source `[N8D7M12] Part 5F4` with `Orchestrated-By: opencode` on the private branch, **no push**. In ssx3 commit `local/research/N8D7M12P5F4/{REPORT.md,check.py,check-result.json}` plus bounded log/receipt excerpts with the same trailer, explicit paths only, no push. Hand back the table, private fork commit, binary SHA, and gaps; the orchestrator gates any Android package separately.
