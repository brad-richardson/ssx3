# N8D7M12 Part 6M5 — stage exact next-package source inputs

You are the OpenCode Go source-staging worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, P6M1/P6M4 REPORT + ORCH-GATE, P3 build.sh, and source_manifest.py. Own only ignored `local/research/N8D7M12P6M5/` (stage, script, manifest and receipts). Commit only `REPORT.md`, `check.py`, `check-result.json` with `[N8D7M12] Part 6M5` / `Orchestrated-By: opencode`; no push. Do not edit board/ledger, fork, renderer, codegen, source collector or global config. No build, network/ssh, device/emulator, lease or package. No subagents. Time box 20 min; stage ≤1 GiB and project total <200 GB. Check disk budget before and after. Keep game/codegen/APK/binary bytes out of Git.

Goal: prepare a **clean, reproducible source-byte staging root** for the next Android package. Hypothesis: P6M4's real-fork probe includes untracked receipts, while P3 lacked compiled-input pins. A clean export plus an all-root manifest distinguishes exact next-package input bytes from historical old-APK guesses. Correct model: only bytes in the staged roots can be claimed for the next build; no claim that the old APK used them.

Source pins to start from (verify independently): private fingerprint fork `/Users/brad/dev/ssx3-work/N8D7M12P5F4/PS2Recomp` at `4fa0df1`, no tracked modifications and no runner diff vs `14b1e5cb`; dirty renderer `/Users/brad/dev/ssx3-work/N8D7F/parallel-gs` (pinned P3 renderer SHA `85c29cb0…3de77e`, interface `3a1751b4…05954d9d`), generated code `/Users/brad/dev/ssx3-work/codegen-ssx3`; Turnip source `/Users/brad/dev/ssx3-work/G43/inputs/libvulkan_freedreno.so` SHA `717812c3…54c1ac29d`; HAL *packaged member* from `/Users/brad/dev/ssx3-work/N8D7M12P3/app-release.apk`, `lib/arm64-v8a/libhardware.so` SHA `1b49d27c…cdfc387`. The historical HAL **source** is unproved; staging the known packaged member starts a new baseline. Confirm exact full SHAs from P3 receipts.

1. Create fresh ignored `local/research/N8D7M12P6M5/stage/{PS2Recomp,parallel-gs,codegen-ssx3,jniLibs}`. Export the exact private fork HEAD with `git archive` so untracked `local/receipts` is absent; do not copy `.git`. Stage renderer including Granite and dirty/untracked shader sources, but omit only `.git`, `build`, `.cxx`, `.gradle`, `__pycache__` cache components; record the exact copy command and verify included source bytes match original (e.g. `rsync --checksum --dry-run` with identical exclusions). Stage complete codegen similarly. Stage exactly two arm64 JNI libs, Turnip copied from pinned source and HAL extracted from pinned APK member; double-read source and stage SHAs. If any source pin, copy check, cap or fork guard fails, stop and report, no wider changes.
2. Run `python3 local/tooling/orch/source_manifest.py snapshot` on the four staged roots with explicit `--max-files 30000 --max-bytes 2000000000`, then `verify` against those same roots. Cap each operation at 12 min. Manifest stays ignored/private; commit only aggregate, counts, exact root mapping, exclusions/skips and bounded checker receipt. Ensure manifest contains fork frontend/worker/backend, parallel `gs_interface.cpp`, page tracker, shader inputs and Granite, complete codegen including `register_functions.cpp`, and the two JNI members. Ensure only the exact upstream stub is under fork `ps2xRuntime/src/runner/`, and no `local/receipts` or game bytes are staged.
3. Write a small meaningful checker for required path/SHA/count invariants, stage-copy parity and manifest verify; run it once. REPORT tables: source revision/status/dirty files, input→stage method and SHA proof, per-scope file/byte counts and aggregate, checker rows, exact commands, gaps. Do not say the source manifest proves what compiled; the package worker must still capture CMake/Gradle flags, file-list, native SHA/Build ID and APK SHA. Hand back table, not a package/GPU verdict.
