# M25 — Within-streak-column δ profiles: per-row gradients + column fits (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M23/REPORT.md` (M23 gap 2 is this
brief: streak columns read single-sign δ means — 22.7–36.5
positive except c342/343 negative — but per-row δ gradients along
the r23–33 columns are untabled beyond receipt δ lists) plus
`local/research/M24/REPORT.md` (column r-coherence tables: c257
r∈{−3,−2,0}, c296 all |r|≤1, c342 all r≥1 — the residual-level
precedent for column-local structure). Fully offline — SSD dumps
read-only, no harness code, no replays, no lease. No device work.

## Facts you start from

- Streak columns (M22/M23, both frames identical bboxes):
  single-column vertical runs at c257/277/296/301/321
  (r23–32/34, δ means +22.7…+36.5) and c340–343 mixed
  (c340 positive ~+35, c342/343 negative ~−18/−28, c341
  ~empty). 8–11 tail bytes each; the top-band tail's spine.
- The profile question: is δ constant along a column (one
  value per column), graded (top-to-bottom slope), or ragged
  (per-row noise)? And do the two frames' profiles match
  column-by-column (same sites — M22: identical bboxes — but
  same VALUES?)?
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m15/` (triplet),
  `/Volumes/Extreme SSD/m22/m22.txt` (column membership to
  reproduce: per-column n/rows/means — match exactly or table
  the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m25/`; evidence `local/research/M25/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m24/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m25/` + `local/research/M25/`.
- Evidence dir `local/research/M25/` (STANDALONE). Commit with
  `git add -f`, prefix `[M25]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M24 precedent).

## Task 1 — per-row δ profiles (what the columns look like)

Column membership recomputed from the dumps (per-column n/rows/
means must match M22/M23 exactly or table the mismatch and stop):

1. Per-row δ lists for each named column (c257/277/296/301/321/
   340/342/343) on s0 + m15: row → δ table (gaps where the
   column has no tail site — table the holes too).
2. Column shape stats: range, sd, runs (sign runs, value runs),
   top-vs-bottom-half means (gradient sniff, tabled not
   fitted here).
3. Cross-frame profile match: per-column s0-vs-m15 δ vectors
   (same rows? same values? mean abs diff?) — identical
   bboxes, but identical profiles?

## Task 2 — column-gradient fits (fixed list, exact-hit scoring)

Fixed candidate list (record in DESIGN.md before running, no
additions): column-constant (median δ), column-linear (least-
squares slope over rows, rounded), column-sign (sign of the
column mean × |δ| from K45+?). Score on s0 + m15 tail bytes in
named columns: exact hits / near hits (|err|≤1, separate) /
miss distribution. Best fit's explained bytes. Cross-frame
check: fit on s0, score on m15 (and vice versa).

## Task 3 — controls + determinism (M18–M24 precedent)

1. Positive control: synthetic truth with KNOWN column profiles
   (N sites, δ = column constant + listed gradient) — recover
   profiles + best-fit identity exactly.
2. Determinism: re-run Task 1 on s0, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M24).

## Report

`local/research/M25/REPORT.md`: DESIGN.md (bars + fixed fit
list first) + profile tables + fit scores + transfer tables +
controls + explained-vs-standing update (does any column fit
shrink the tail? by how much?) + gap rows. Screenshot only if
a column-profile map discriminates (budget-noted,
pre-registered split required per M23/M24 precedent). Commit
evidence (`[M25]`, no push), stop.
