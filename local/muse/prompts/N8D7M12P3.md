# N8D7M12 Part 3 — isolated Android APK build/package (OpenCode Go)

You are the N-lane Android build worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M1 `REPORT.md`/scripts, N8D7M12 Part 1/Part 2 reports and gates, and `docs/todo.md` N8D7M12 Part 3. Use repo receipts, not the web. Own this brief, `local/research/N8D7M12P3/`, local scratch `~/dev/ssx3-work/N8D7M12P3/` and remote WSL root `/home/brad/n8d7m12p3/` only; prior N8D7M1/N8D7H roots, Mac app fork `~/dev/ssx3-work/N8D7M12P2/PS2Recomp` at `a608ed1`, paraLLEl source, codegen, Turnip/HAL are **read-only inputs**. This pane has per-worker permissions for the local scratch path and `ssh bytesize` only; test a tiny local sentinel and a read-only SSH preflight, stop on denial. No fork/source edit, second build, stream copy, Odin/iOS action, app launch, lease, push, board/global edit, upstream contact or generated game code in git. Time box 45 min; one `assembleRelease`; remote root ≤10 GiB, local scratch ≤500 MiB, text receipts ≤512 KiB; check mini disk budget before/after. Bytesize: one heavy job at a time, stop if another active; use the N8D7M1 memory governor.

Goal: build an **arm64 APK** from the gated `a608ed1` source, preserving N8D7M1's G43/Turnip/HAL and external codegen. Preflight remote root absent, ≥20 GiB free and no heavy job. Prepare new WSL root by copying N8D7M1 source/parallel-gs/jniLibs with `.cxx`, `build`, `.gradle` and `ps2xRuntime/src/runner/` excluded (same `prepare.sh` shape, new root), then overlay **exactly these seven** Mac fork files with tar over `ssh bytesize`: `ps2xRuntime/CMakeLists.txt`, `include/runtime/gs/gs_cpu_backend.h`, `include/runtime/gs/gs_replay_core.h`, `src/lib/gs/gs_cpu_backend.cpp`, `src/lib/gs/gs_replay_core.cpp`, `src/main.cpp` (all under ps2xRuntime), and `ps2xTest/src/ps2_gs_replay_tests.cpp`. Those are the complete `git diff --name-only d1ba1d4 a608ed1` list; do not overlay any other source. Two matching SHA reads on each overlay file before/after transfer; remote source gate should show only those seven differences from N8D7M1, with paraLLEl/jniLibs unchanged and no runner files.

Adapt N8D7M1 `build.sh` to the new root, preserving Java/SDK/Gradle locations, external `/home/brad/n8b1/codegen-ssx3`, `-Pps2xGsShadowParallel=ON`, new root's parallel-gs/jniLibs, five diagnostic defaults OFF, `--max-workers=4`, and governor. One `assembleRelease`, no repair loop. On failure hand back exact log tail and B/OTHER; do not retry. Postbuild source gate must still pass. APK gate: exactly arm64 runner, `libvulkan_freedreno.so` and HAL shim; Turnip/HAL SHAs equal N8D7M1 pins (`717812c3…1ac29d`, `1b49d27c…fc387`), runner SHA/Build ID new, `PS2X_GS_REPLAY_ONDEVICE`, `PS2X_GS_REPLAY_CAPTURE`, `PS2XGSC1`, `GB4_REPLAY_SUMMARY`, selected/oracle strings present, no unexpected ABI/native member. CMake cache must show parallel ON, tests OFF, exact codegen and source root, diagnostics OFF. Pull APK to private Mac scratch (outside git), two matching SHA reads on WSL and Mac. Source/default-OFF path is statically gated; **no runtime OFF-path or Turnip/HMI behavior is proved without a later Odin run**.

Deliver `REPORT.md`, bounded prepare/build/source/APK gate scripts and JSON/text receipts, exact commands/SHAs, costs and gaps. Acceptance checker computes source/overlay pins, one build SUCCESS, APK members/SHAs/cache, no build retry, budget and no device action. A = source and package gates pass; B = build/package mismatch with exact smallest failing row; OTHER = pin/permission/resource failure. Commit `[N8D7M12] Part 3` with `Orchestrated-By: opencode`, no push; stage only named brief/evidence paths explicitly. Hand back table, do not conclude graphics cause or speed.
