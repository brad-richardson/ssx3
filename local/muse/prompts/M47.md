# M47 — Shape-711 pair wipe: singleton + far private-column listing (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M28/REPORT.md` first (all of it —
M28 gap 3 = this brief via M29 gap 6, taken on orchestrator
judgment: 711's c342 + c343 fully missing with no assigned
dest (no-cand both — 11 singleton private Y-cols exist on
movers, tabled only as a count)), plus `local/research/M34/
REPORT.md` (711's census row: tail 95, J 0.9314, NO unnamed
moves — the wipe is purely named-column). Fully offline — SSD
dumps read-only, no harness code, no replays, no lease. No
device work.

## Facts you start from

- 711's wipe (reproduce exactly or table the mismatch and
  stop): tail 95, J 0.9314, moved cells [342,343], c342
  0/0 + c343 0/0 (full wipe both), dest no-cand both
  (M28 displacement: no candidate destination).
- The 11 singleton private Y-cols on movers (M28 count —
  NAME them: which shapes + columns hold exactly 1
  private row? per-column row + value listing).
- The question: singleton + far private-column listing per
  unassigned move — for each of 711's 2 wiped columns,
  the singleton columns (exact-1 privates anywhere) + far
  private columns (multi-private columns at distance?)
  with rows + values, so a later brief can judge
  assignment. Answer by table (list, don't assign).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (711 row to reproduce — match exactly or table the
  mismatch and stop). Work dir: new `/Volumes/Extreme
  SSD/m47/`; evidence `local/research/M47/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m46/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m47/` + `local/research/M47/`.
- Evidence dir `local/research/M47/` (STANDALONE). Commit with
  `git add -f`, prefix `[M47]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M46 precedent).

## Task 1 — 711 wipe reproduction (do the 2 columns reproduce?)

711's row recomputed from the dumps (must match M28/M34
exactly — tail, J, moved cells, 0/0 standings, no unnamed
moves — or table the mismatch and stop):

1. Wipe table: c342 + c343 s0-rows vs 711-rows (empty) vs
   miss rows (full s0 row lists — the wiped rows).
2. Wiped-value table: per-site |δ_s0|, s711 status, signed
   gaps, gap signs for all wiped rows (M36-style).
3. No-cand table: M28's dest candidacy rows for c342/c343
   reproduced (why no-cand? distance? occupancy? — table
   the rule's rows, don't re-judge).

## Task 2 — singleton + far private-column listing (the new data)

1. Singleton table: all 11 singleton private Y-cols (shape,
   column, the 1 private row, |δ|, status, gaps — must
   count 11 or table the mismatch).
2. Far-column table: multi-private Y-columns on movers
   with per-column priv counts + rows + value medians
   (the far-candidate pool, listed not ranked).
3. Per-move listing: c342's + c343's wiped-row count vs the
   singleton/far pools (which pools COULD cover the wiped
   rows by count? numbers only — no assignment).

## Task 3 — controls + determinism (M18–M46 precedent)

1. Positive control: synthetic truth with KNOWN wipe +
   singleton/far pools (N wiped rows, N singleton cols, N
   far cols, known values) — recover the wipe + listing
   tables exactly.
2. Determinism: re-run Task 1 on s0+711, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M46).

## Report

`local/research/M47/REPORT.md`: DESIGN.md (bars first) +
wipe tables + listing tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a wipe/pool map
discriminates (budget-noted, pre-registered split required
per M23–M46 precedent). Commit evidence (`[M47]`, no push),
stop.
