# T37 — Resolve the steering tap's immediate heading response: dense sampling + rider-pixel tracking (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T36/REPORT.md` first (all of it: R1
reproduced the chain to LIVE gameplay, ONE 338.6 ms D-pad Left tap @T+500.82
on a live race (00:01:18 6TH/6 30%), +40 s tail to 00:02:29 6TH/6 59% with NO
measurable HUD discontinuity at the 1/3/8/15/25/40 s sampling — position
constant, progress/clock monotonic, speed/score excursions distributed;
isolation limit: single run, no same-seed control, tap contribution below
sampling resolution; G1 proposes (a) denser sampling + tracking, (b) longer
hold, (c) no-nudge control — this brief takes option (a)). This brief
executes T36's G1(a). Reuse T36's scripts (`t36-auto.sh`, `t36-cropdiff.py`,
`t36-analyze.sh`, `t36-vcount.sh` — copy, do not modify the committed
originals).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T36 verified (re-verify sha/size/rev/
  status/pad-binding INCLUDING `Left = Keyboard/Left` + NVM state before
  running; table the receipts).
- GATE NOTE (T36 G1 — gate on the HUD, not the frame): the nudge→gameplay
  whole-frame hop is large (~14.27) because the race never stops moving.
  Live-race criterion = race clock advancing + position/progress HUD read
  off VIEWED snaps. The LIVE-LIKE proxy (departed + non-panel + motion ×2)
  still gates the nudge press in-script.
- The task: reproduce the T36 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → LIVE-LIKE proxy gate → ≤3 attempts) to LIVE gameplay, then
  apply ONE 300 ms-class D-pad Left tap (same input class as T36 — do NOT
  change duration/hold) and capture a DENSE post-nudge series: 1 s cadence
  × 10 s (10 snaps) + rider-pixel tracking (a fixed, tabled method: e.g.
  rider-centroid or a track-edge reference column per snap — table the
  method + its pixel noise floor on the PRE-nudge pair BEFORE claiming any
  delta). Table per-snap: HUD (clock/position/progress/speed/score), rider
  pixel metric, and whole-frame hop.
- If the dense series STILL shows no separable tap response: table the exact
  observed series (HUD + tracking + noise floor) and STOP with a recipe —
  one input variant per attempt, never blind multi-presses. ONE bounded
  variant allowed only if the tap provably never acted on live gameplay
  (pre-nudge ≠ racing).
- Success bars (tabled, not verdicts): live race reproduced (HUD advancing,
  rider moving); tap acted on it (pre-tap = racing with HUD read); dense
  10-snap series + tracking table (or exact non-separability finding with
  noise floor); full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T36 G2: single-shot
  scripts, runs ≤6 min (table any overrun with full dmesg coverage),
  preserve emulogs first, dmesg + btime + init age, UNFILTERED dmesg +
  event log around flaps, 534 ms holds NOT bisected, `;` chains only INSIDE
  one `wsl` call, one `wsl` call per ssh; post-run dmesg read IMMEDIATELY
  after the run ssh returns). COUNT AcceptAsync lines in the committed
  dmesg files with exact counts + uptime-stamp positions relative to the
  run window. Runs in flap windows must be effects-verified or re-run
  (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T37/` (STANDALONE). Commit with `git add -f`,
  prefix `[T37]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + ONE tap + dense 10 s series (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T36 §T36-0 shape): build/input receipts (INCLUDING the
   Left binding), NVM state, T36 reference snaps present, free space both
   ends. Table the tracking method + its noise floor measurement plan
   BEFORE the run.
2. Per attempt table: race reproduction (chain scores + live-race gate:
   HUD advancing), tap row (control/duration/T+/pre-post HUD), dense
   10-snap series (HUD + rider metric + hop per snap) or exact
   non-separability finding.
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T37/REPORT.md`: pre-run receipts, attempt tables, tap log,
dense series + tracking (or non-separability finding + recipe), WSL table,
exact commands, gaps. Commit evidence (`[T37]`), stop.
