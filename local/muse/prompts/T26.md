# T26 — Name the 0x501420 writer: guest-memory write watch + pc attribution (one 240 s leased boot)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/A0/REPORT.md` §(ii-d) first (the
20 B block at `0x501420`: only cross-iteration state, writer
open as gap G2 — this brief names it, per Part 4 decision 2),
plus A0 §(ii) (the 20 records' flag halfwords `+0x10/+0x1C/
+0x1E` on the s1 list) and P26's watch recipe (the
guest-memory write watch that already exists — "watch 16/16";
find it in the fork/user notes and reuse it verbatim) and
`local/research/T22/REPORT.md` (the `pc=` field now boot-
proven by T24 — your attribution channel). The writer decides
the fix (SIF/RPC → P1ae; CD/file → CD callback; neither → E3)
— your tables route it.

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`,
  branch `ssx3` (table HEAD at start; record don't fight).
  ZERO fork changes this brief (watch + channel exist; use
  them — if the watch needs a flag/env you don't have, table
  it exactly and stop rather than modifying the fork).
- The run: ONE boot, 240 s cap (in-phase window suffices —
  the writer must fire during the 72,176-lookup buildup;
  still complete if shorter: table the window). Watch
  `0x501420..0x501434` for writes WITH pc attribution
  (T24's `pc=` semantics: post-SYSCALL dispatch pc — table
  that limit) + the 20 records' flag halfwords on the s1
  list. Boot env: T24's dual-gate recipe (verify EVERY hook
  in the linked binary before booting — E2a G1 lesson).
- Reference streams (read-only): none needed (writer is a
  runtime-side question). Tools: `tools/trace_align.py`
  (selftest must re-green; `--format-check` on the pc-on
  stream).
- The question: WHICH pc(s) write `0x501420..0x501434`
  during the window (counts + first-fire times + enclosing
  functions via OUT join) and which flag halfwords flip
  when (per-record flip census)? Answer by table. Then the
  routing row: SIF/RPC-delivered? CD/file completion?
  neither? (tabled with the code path each way).

## Gates and rules

- Lease `T26`: the boot ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/t26-waits.log`. Max 1 boot ≤240 s foreground
  (Part 4 boot budget: 600 s cap respected with margin).
  Builds any time (`-j4`) — but no rebuild expected. No `adb`.
- ZERO fork changes (no source edits, no commits, no push).
  `export COPYFILE_DISABLE=1` on every step touching the SSD.
- Evidence dir `local/research/T26/` (STANDALONE). Commit with
  `git add -f`, prefix `[T26]`, trailer `Orchestrated-By: Muse
  Code`. Do not push (the orchestrator pushes at poll when
  the tree is clean).
- Time box: 4 h. Tables, no verdicts — but the routing row
  names the fix lane (P1ae / CD callback / E3).
- Hygiene: write the report in chunks with a tail receipt
  (truncated tail fails the gate); boot script gets BOTH a
  wall cap (240 s) and a progress cap (pick: stub-block or
  event-count — table which bound, T24 precedent).

## Task 1 — writer-watch boot (or cap)

1. Pre-claim checks (T13 §T13-0 verbatim + watch + pc=):
   lease absent, `pgrep -x ps2EntryRunner` exit 1, binary
   sha (T24's `7f155f4c…` unless the tree moved), watch
   availability receipt, pc= linkage IN the binary (E2a G1
   lesson), ISO + ELF sizes, free space, aligner selftest
   green, waits-log tails, BOTH caps recorded.
2. Claim → foreground boot (watch armed pre-boot; liveness
   monitor only) → at whichever cap binds: SIGTERM →
   release AT ONCE → trace copy (lease-free) → analysis.
3. Epoch table: window covered (blocks, VBLANKs/invocations,
   394ED0 lookup count — did the buildup run? match/no).

## Task 2 — writer tables (who writes? when? what flips?)

1. Writer table: pcs writing `0x501420..0x501434` (pc +
   enclosing function + write count + first-fire +
   bytes touched — all writers, ranked).
2. Flag table: per-record halfword flips (`+0x10/+0x1C/+0x1E`
   × 20 records: flip counts + first-flip order — do flags
   complete in lookup order?).
3. Routing table: SIF/RPC-delivered? (writer in SIF/RPC
   path?) / CD/file completion? (writer in CD/file path?)
   / neither? — each with the code path + the named fix
   brief it routes to.

## Report

`local/research/T26/REPORT.md`: T13-0..T13-7 SHAPE (lease
record, epoch table, writer/flag/routing tables, exact
commands, gaps). Commit evidence (`[T26]`), remove the lease
if held, stop.
