# G21 — Adreno filing draft + standalone repro package (NO submission, NO workaround, NO device runs)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below). **Tables
+ hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G20/REPORT.md` (ssx3 mirror) first (all
of it: crashing pipeline NAMED — `7463dfd379df2855` / `sampler_feedback`,
device shader hash matching the host bank hash exactly, SPIR-V sha
`b0a09fa0…` (5049 words); filing package COMPLETE except the filing-aid
minimized repro (gap 2); the ONE next action = file-or-workaround decision,
which needs the USER — this brief does everything short of that decision).
This brief assembles the filing draft + a standalone repro. No device, no
`adb`, no renderer changes, no new dumps.

## Facts you start from

- DECISION OWNERSHIP (read carefully): filing upstream needs the user's
  identity/account and the workaround changes rendering behavior — BOTH are
  the user's call, surfaced as a BLOCKER by the orchestrator. Your job is
  to make that decision frictionless: a complete filing DRAFT (text,
  ready to paste) + a minimized standalone repro (Fossilize or bare
  `vkCreateComputePipelines` on the named SPIR-V + flags — whichever is
  feasible in the time box; table feasibility FIRST).
- Inputs (re-verify, do not move): G20 §3d filing facts (device/driver/
  build-ids/repro/fault shape); SPIR-V blob + sha (SSD `ps2x-g20/`,
  read-only); pipeline flags (specs 0, robustness 0, subgroup 0,
  `VK_PIPELINE_CREATE_DESCRIPTOR_BUFFER_BIT_EXT`, no heap); tombstones
  G18/G19/G20 (3/3 O4 — determinism receipts).
- The task, in strict order: (1) re-verify every §3d fact against its
  receipt (hashes, offsets, build-ids — a filing with a wrong hash burns
  credibility; table the re-verification); (2) disassemble the named
  SPIR-V (host tools only) and table what construct families it uses
  (G20 gap 3 — a first pass, not a full delta-minimization); (3) assess
  standalone-repro feasibility (Fossilize capture vs hand-written minimal
  app vs full-dump repro — pick ONE, table why); build it if feasible in
  the box (host-side build, SSD only, `-j2`); (4) write the filing DRAFT
  (title, environment, repro steps, expected vs actual, crash signature,
  attachments list — text file in evidence, NOT submitted anywhere).
- Success bars (tabled, not verdicts): §3d facts re-verified (or
  discrepancies tabled); SPIR-V disassembly surveyed (construct families,
  no over-claim on the trigger); repro feasibility assessed + built if
  feasible (or the exact obstacle tabled); filing draft complete; the ONE
  next action (what the user must decide/approve, and what the workaround
  brief would contain if they choose that arm).

## Gates and rules

- NO device work of any kind (no `adb`, no Odin, no tombstones, no lldb —
  all device evidence already exists). NO submission anywhere (no accounts,
  no trackers, no email). NO renderer/behavior changes. NO workaround
  implementation (the workaround arm stays the user's decision).
- Byte caps: declare up front for any repro build (SSD only) + retrieval;
  all prior `ps2x-g*` + build dirs read-only (0 growth); internal `/`
  delta ≤1 GB, no clones/builds on internal; `export COPYFILE_DISABLE=1`
  on SSD steps. No P-lane lease (no recomp boots/builds, no fork writes).
  No bytesize/WSL. Host builds `-j2`.
- Clone discipline: ZERO clone edits. No third-party code copied into any
  project tree.
- Evidence: ps2xGS commit `[G21]` + trailer `Orchestrated-By: Muse Code`
  (push allowed — G-lane owns ps2xGS), ssx3 mirror `local/research/G21/`
  (`git add -f`, prefix `[G21]`, same trailer; do NOT push ssx3 — the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts beyond the hypothesis.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — fact re-verification + SPIR-V survey (no build yet)

1. Re-verify every G20 §3d fact against its primary receipt (hashes,
   offsets, build-ids, counts). Table each as CONFIRMED or DISCREPANT
   (with both values — never silently "fix" the package).
2. Disassemble the SPIR-V host-side; survey construct families (table
   what's present — storage images, atomics, barriers, derivatives, etc.).
   Name what a delta-minimization would try first (do not perform it —
   out of scope unless the repro build leaves time).

## Task 2 — repro feasibility + filing draft

1. Assess the three repro shapes (Fossilize / minimal app / full-dump);
   pick ONE with a tabled reason; build it if feasible in the box (host
   build, SSD, `-j2`; table binary path/size/sha — UNTESTED on device is
   tabled honestly, never claimed working).
2. Write the filing draft (complete text, ready to paste; attachments =
   existing receipts + repro if built). Hypothesis verdict + the ONE next
   action (user decision text + workaround-brief sketch).

## Report

`local/research/G20/REPORT.md` is prior work; yours is
`local/research/G21/REPORT.md` (ssx3 mirror; same content in the ps2xGS
commit): contract, caps, re-verification table, SPIR-V survey, repro
record, filing draft, verdict, exact commands, gaps. Commit evidence
(`[G21]` both trees), stop.
