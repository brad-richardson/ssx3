# N8D Part 4 — replay one Odin GS stream on Mac CPU and paraLLEl

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §6. Start the device run and Mac replay only after the orchestrator says I27B's Simulator run/build are finished, to keep the Mac timing and device gate isolated. Read full N8D2/N8D3 and GB4/GB6C reports first. Write scope: `local/research/N8D4/` and `~/dev/ssx3-work/N8D4/`. Read current fork sources, N8D3 APK and existing build dependencies; no fork/source edit, APK rebuild or push. Never commit the GS capture, APK, ELF, ISO or other game data.

N8D3 captured one Odin race frame at tick 2050, FBP 112: raw mapped Vulkan bytes, backend packed rows and pre-`UpdateTexture` frontend image matched **all 917,504 RGBA bytes**, yet all show black bands and missing scene. This leaves GPU image generation versus Vulkan transfer/readback open. The fork's default-OFF `PS2X_GS_CAPTURE` records packet/priv/transfer/vblank data with a 6 GiB cap, flushes and closes at `PS2X_GS_CAPTURE_STOP_TICK`; `PS2GSReplay` consumes that stream and can render CPU and opt-in paraLLEl PPM frames. Confirm the current exact code path and GB4/GB6C replay commands before acting. Previous Mac captures reached tick 3000 in 2.75 GiB; Odin has 25 GiB free, but treat size/time as a budget, not a guarantee.

**Predictions on the same GS stream:** Mac CPU and Mac paraLLEl both show a recognizable world while Odin raw is fragmented → device-specific GPU/transfer path; CPU recognizable but Mac paraLLEl fragmented → cross-host paraLLEl behavior or input interpretation; both Mac replays fragmented → GS stream/input or common renderer behavior. A missing/incomplete marker, different FBP/PMODE, or scene timing gap leaves the split unresolved. Compare viewed regions and metadata, not free-running image hashes. A Mac replay cannot by itself distinguish Turnip GPU rasterization from its image transfer; state that limit.

Reuse N8D3 APK SHA `3970011d360b86a3f93f170df11e5964e2b467f01e7351c2cd1610b615b885be` and its Turnip/shim members. Adapt its launcher for **one** Odin install/launch, same empty card, parallel/Turnip, dev movie bypass, I26-FAST and tick-2050 raw/frontend image capture. Add only `PS2X_GS_CAPTURE=<app-files>/n8d4.gs` and `PS2X_GS_CAPTURE_STOP_TICK=2050`. Confirm by source that this capture is compiled into the APK and that the target marker closes it. Before launch: lease free, keyguard `showing=false`, battery ≥20% charging/full; install, double-read installed APK/ELF/ISO. Run ≤300 s, stop after the marker and frame or first failure, always force-stop, check PID absent. While holding the Odin lease, double-read capture SHA/size on device and pull once to Mac; release lease after file transfer. Stop if capture exceeds 4 GiB, device space falls below 10 GiB, or the file is incomplete. No second launch.

On the Mac, pin current fork `ssx3` and canonical codegen; use a fresh build or a verified matching existing binary with `PS2X_GS_SHADOW_PARALLEL=ON`. At most one Mac build, one CPU replay and one paraLLEl replay of this **same** capture. Set `PS2X_GS_REPLAY_CAPTURE`, `PS2X_GS_REPLAY_PPM_TICKS=2050`, a fresh `PS2X_GS_REPLAY_PPM_DIR`, and `PS2X_GS_REPLAY_BACKEND=parallel` only for GPU; use exact GB4/GB6C flags. Check test suite and replay parse/marker/packet counts, output FBP/PMODE and frame hashes. Capture at most one CPU and one Mac GPU PPM/PNG plus the N8D4 Odin raw/frontend images; view every frame. No clean speed claim from capture/replay.

Budgets: ≤12 GiB new Mac scratch, ≤4 GiB capture on Odin/Mac, ≤16 MiB text logs, ≤12 MiB images; `disk_budget.sh` before/after large writes. Acceptance script checks one device run, pins/SHA pairs, capture magic and last marker 2050, replay counts/outputs, image decodes, cleanup and caps. Deliver `local/research/N8D4/REPORT.md`, exact commands, pins, bounded logs, capture SHA/size (capture remains outside git), replay table with visual regions/metadata and gaps. Commit only named text receipts and small images `[N8D4]` + `Orchestrated-By: Codex`; no push. Hand back the table, not a cause verdict.
