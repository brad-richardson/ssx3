# E54F Part 2 — shared EE COP0 Count clock

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md` and `docs/orchestration.md` §4. Read
`local/research/E54F1/REPORT.md` in full and the E54 queue in
`docs/todo.md`. Hand back tables and receipts; the orchestrator decides
the gate. **One mechanism:** MFC0/MTC0 Count use a shared EE clock plus
write epoch. Do not alter Compare writes, Cause/Status, timer interrupt
policy, signed branches, or GS code.

Pin fork remote `ssx3` at `293fd81` (GB6 opt-in GPU fold; verify). Make
new worktree `~/dev/ssx3-work/E54F2/PS2Recomp`, branch `e54-count`, from
that tip. Canonical `~/dev/ssx3-work/codegen-ssx3` is still the E54D
regen (`register_functions.cpp` SHA-256
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`);
do not change it. All build/regen/run output under E54F2. Allowed fork
edits: `ps2xRecomp/src/lib/cop0_translator.cpp`, the narrow EE scheduler
and PS2Runtime interface/header/implementation needed for a shared
Count clock, plus focused `ps2xTest` tests/registration. No other E/G
source edits, no fork or ssx3 push, no generated code in Git.

Correct behavior from pinned PCSX2 `9056c0834` (E54F1 source citations):
Count advances one u32 tick per charged EE cycle; MTC0 Count stores
low32 and resets the cycle anchor; a same-cycle MFC0 adds one (including
destination `$zero`); u32 wraps. The fork's `m_eeCycle` is **synthetic
and coarse** (32 per back-edge, 8 per dispatch, idle fast-forward), so
match the PCSX2 arithmetic against this clock without claiming exact
hardware timing or dynamic game reach. The clock belongs to the EE
scheduler, not individual guest-thread contexts. Keep any context
`cop0_count` mirror coherent enough for existing debug/snapshot reads.

## Gates

1. Verify E54F1 source map and design the smallest executor-only
   scheduler interface reached through `PS2Runtime`. Focused value tests
   must exercise actual helper calls, not only source strings: reads at
   cycles 100/140 return `0x64/0x8c`; write `0xfffffff0` at cycle 200,
   read at 240 returns `0x18`; two reads at the same cycle differ by 1;
   MFC0 `$zero` still advances Count; switch guest contexts and read the
   same shared clock; idle-style `accountCycles` advance and u32 wrap.
   Check emitted MFC0/MTC0 Count calls, while other COP0 cases and
   Compare/Cause lines stay byte-identical. State any untestable case.
2. One candidate implementation and one codegen regeneration into `E54F2/codegen`
   with this worktree's recompiler (retarget a copied E54D TOML).
   Count changed generated sites and verify all four guest PCs/five
   copies; no dynamic-reach inference. No generated-source commit.
3. Mac Release/Ninja build and full suite with taps OFF, then taps ON,
   using E54F2 codegen; run `ps2x_tests` from fork root. Wait for other
   clang/ninja, use `nice`. One mini-lease I26-FAST diagnostic CPU-backend
   boot (`PS2X_SKIP_MOVIE=1` dev-only), wall cap 500 s, target race
   tick ≥2050, bounded logs, three frame targets only (`830,1810,2050`),
   view SC and two race frames. This is functional regression only,
   not clean speed or proof Count sites ran. Claim/release one mini slot;
   kill only own PID. Check ISO/ELF, runner and generated register file
   with two matching SHA reads before use.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty. If all gates pass, commit only fork source/tests `[E54F2]` on
   local branch with `Orchestrated-By: Codex`; **no push**.

Budgets: one regeneration, two configure/build attempts per tap mode
(one clear repair), one boot ≤600 s absolute, ≤9 GiB new E54F2 bytes,
global 200 GB cap; logs ≤32 MiB and frames ≤25 MiB. Stop on a failed
gate after one clear repair; no tuning loop or Compare guess. Report
any inferred rate/interrupt limitation plainly.

Deliver `local/research/E54F2/REPORT.md`: pinned source/oracle, test
table, generated-site diff, suites, boot/frames and SHA pairs, budgets,
runner-dir check, exact commands and gaps. Commit only the explicit
report `[E54F2]` with the same trailer; no push. The orchestrator gates
and promotes codegen if successful.
