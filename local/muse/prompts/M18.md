# M18 — Remainder character: filtering-scale tests on the synth residual

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/M17/REPORT.md`
first (all of it: rigid falsified at integer, half-pel, and block
scales; the 22815-B remainder with 93.6% of residual Y px at |d|=1),
plus the M16 carrier table (where the remainder lives per draw). This
brief is M17 gap 5: characterize WHAT the non-translational remainder
is. Fully offline — no harness code, no replays, no lease. No device
work.

## Facts you start from

Translation is exhausted as an explanation (M17: global half-pel 0,
block16 negative). What remains is 22815 B of edge-concentrated,
mostly-±1 differences between blend-synth and exact-mid truth on one
frame (13418 B on M15's). Candidate characters: filtering (blend
weights, bilinear taps, edge phase), sub-pixel coverage/AA decisions,
or genuinely non-rigid content. Starting input: the committed dumps +
synths on the SSD under `/Volumes/Extreme SSD/m15/`, `/Volumes/Extreme
SSD/m16/`, `/Volumes/Extreme SSD/m17/` (read-only — copy what you
extend). P-lane briefs run concurrently; you share nothing with them
(no lease needed at all).

## Gates and rules

- OFFLINE ONLY: no replay runs, no harness builds, no lease, no
  `adb`. Installed tools only (numpy/PIL/etc. — verify, never
  download).
- Disk: internal disk is tight — work under
  `/Volumes/Extreme SSD/m18/` (new dir). Never touch
  `/tmp/p1-link`, `ps2x-i*`, `ps2x-p1*`,
  `/Volumes/Extreme SSD/m14/` … `/Volumes/Extreme SSD/m17/`
  (read from them, never write into them), or other agents' dirs.
- Evidence dir `local/research/M18/` (commit with `git add -f`,
  prefix `[M18]`, trailer `Orchestrated-By: Muse Code`).
  Screenshots under 5 MB total (downscale or describe). NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Design (DESIGN.md first like M16/M17).** The test suite:
   blend-weight sweeps (does a non-0.5 v0/full weighting reduce
   R?), sub-pixel edge-phase analysis (residual vs edge phase /
   orientation / gradient magnitude), and — only if the first two
   leave a structured remainder — one non-translational warp
   family (affine? perspective? — your call, recorded). State
   falsification bars per test before running.
2. **Run offline.** Over the M16 dumps (+ M15 frame as the second
   sample): weight-sweep curve, edge-phase tables, warp-family
   fit if triggered. Record wall time, exit, input shas,
   determinism (re-run receipt).
3. **Characterize.** What the remainder IS, in bytes + structure
   (filtering residual? coverage decisions? content motion?),
   per-band and per-carrier where the tests discriminate; the
   updated deliverable table: explained vs standing remainder
   with byte counts. Screenshot(s) if they discriminate
   (weight-curve plots, phase maps, or absence reasoned).
4. **Report.** `local/research/M18/REPORT.md`: design + bars,
   run receipts, characterization tables, gap rows with the exact
   next brief each needs (negative mechanism? Odin handoff
   format? top-HUD isolation?), exact commands, receipt paths,
   "What I could not do". No verdicts. Commit
   `local/research/M18/`, stop.
