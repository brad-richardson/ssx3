# E31 — Title screen → menu → stock race on the E29 bypass build (scripted pad input)

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`). **Tables + receipts; recommend, the orchestrator
decides.** Read first, all of it: `local/research/E29/REPORT.md` (the
bypass reaches the rendered title screen, "Press START button"; build
dir, boot driver, lease protocol, the CWD-sensitive suite, the ExFAT
AppleDouble fix) and `AGENTS.md`. Pad-stim precedent:
`local/research/E2a/REPORT.md` §arming (`PS2X_PAD_STIM_AFTER` /
`PS2X_PAD_STIM_WALLMIN`, fires once).

## Goal

Drive the stock game from the title screen to a **stock race that starts
and advances**, on the Mac host build, with `PS2X_SKIP_MOVIE=1`. This is
the milestone's first half; every step you get further is progress,
and the first place it stops is the deliverable if it stops.

## You own

The PS2Recomp fork worktree at `/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`
(E30 runs in parallel but is read-only on the fork), the P-lane lease
(`/tmp/ssx3-p-lane-lease`), and the E29 build dir
`/Volumes/Extreme SSD/ps2recomp-spike/e29-movie-bypass-build` (rebuild
in place is fine, incremental). Work on local branch
`e29-movie-bypass`; mainline `ssx3` stays at `3adc0478`, never touched.

## Mission 1 — is START live? (no code change, 1 boot)

One boot of the existing E29 binary with the flag on and START injected
through the existing pad stim after the title screen appears (pick
AFTER/WALLMIN from e29b's timeline). Observable: the frame after the
press differs from the title screen and shows the next front-end screen
(read the PNG), plus `[padread]` lines. If nothing reacts, table
whether the guest is polling the pad at all (stub histogram for
`scePadRead`/`scePadGetState`), then stop and recommend.

## Mission 2 — scripted pad sequence (one small change, if needed)

If the one-shot stim can't navigate menus, add an env-gated,
default-off pad script (e.g. `PS2X_PAD_SCRIPT="t_ms:button:hold_ms,…"`,
dev-only label, in `Pad.cpp` next to the existing stim) on the bypass
branch, plus a unit test. Suite: 458 + new tests green with both flags
off, run from the fork worktree root. Then navigate: title → main menu →
the quickest path to a stock race (e.g. Single Event / first peak, first
course, default rider). Use frame PNGs after each press to choose the
next input; keep a timeline table of (time, input, screen reached).

## Mission 3 — race start

Bars, in order: (1) front-end menu reached and responding; (2) race
loading begins; (3) race HUD visible; (4) the race timer/position
advances across ≥ 3 captured frames. Hold a direction/tuck input so the
rider moves. Record wall time to each bar and the guest-speed read if
the runtime reports one (it's a CPU host build; speed is a note, not a
bar).

## Budgets and rules

- Boots: up to 8, one at a time under the lease; each ≤ 300 s wall.
  Anything longer needs orchestrator OK. Progress caps on every boot
  script.
- Builds: incremental in the E29 build dir, as many as needed for the
  one pad-script change; retain AppleDouble sidecars by rename as E29
  did (or set `COPYFILE_DISABLE=1` and purge `._*` from `_deps` before
  building).
- Frames and big logs stay on the SSD under `ps2recomp-spike/P1/run/`;
  evidence in `local/research/E31/` is text only (pin PNGs by path + SHA
  and list the 5–10 key frames in the report). Mac internal has ~2.7 GB
  free.
- If a new blocker appears (a crash, park, missing HLE stub), stop at
  the first one and table it exactly: thread states, pc/ra, last stub
  calls, last frame. Don't try to fix runtime behavior beyond the pad
  script; the next brief will.
- Commit the pad-script change on `e29-movie-bypass` (local, no push).
  Evidence commit `[E31]` with `Orchestrated-By: Muse Code`, `git add -f`,
  no push; check `git log -1` first. Time box 6 h.

## Deliverable

Timeline table (input → screen), the furthest bar reached with frame
SHAs, the pad-script diff + test result, and the next blocker if any.

## Mini resume (orchestrator, 2026-09-22 evening) — paperwork only

The laptop is retired; you are on the Mac mini (`~/dev/ssx3`, read
`local/AGENTS.local.md`). E31's code is done and pushed after the scrub:
`fork/e29-movie-bypass` @ `ee39b9f` = your `96893da` rewritten. **Do not
touch the fork or any worktree** (E32 owns it from now on) and run no boots.
Resume from `local/research/E31/CHECKPOINT.md` "Exact next step": finish
REPORT.md (e31k/l sections, final bar verdicts, e31l timeline + commands,
note the scrubbed SHA mapping `96893da` → `ee39b9f`), write
`frame-pins.json` + `boot-results.json` (frames are on the SSD under
`ps2recomp-spike/P1/run/`; two SHA reads for the pins), then the `[E31]`
commit. Include the exact e31l `PS2X_PAD_SCRIPT` string in the report:
PF1 and N4 reuse it on the Odin. Time box 45 min.
