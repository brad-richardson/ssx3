# N8D7M11 — minimal Odin GS-stream replay harness design (OpenCode Go, read-only)

You are the N-lane design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M6 and N8D7M10 `REPORT.md`/`ORCH-GATE.md`, N8D7M1 packaging receipt, and `docs/todo.md` N8D7M11. Pin N8D7L fork `a8cfefa`, N8D7F paraLLEl `3a66c19`, N8D7M6 capture SHA `f6a78f71…a593` (1,100,696,462 B), N8D7M1 APK `e077bef8…758a1`. Cite file/line for every source claim; use LSP when available and direct reads otherwise. Own only this brief and `local/research/N8D7M11/`. No source edit, build, replay, stream copy, device/lease action, push, board/global config edit or upstream contact. Time box 25 min, text <512 KiB.

Goal: design the smallest Android path that replays the **same closed stream inside the Odin app process** using its bundled Turnip backend, without launching the live EE game thread. N8D7M10 found the parser only in desktop `ps2x_tests` and the APK builds tests OFF. Compare two options: (A) a dev-only NativeActivity replay mode, (B) a headless arm64 executable. Choose one based on exact source/build/loader facts, with a table of files/signatures/entrypoint changes; say which parser code can be factored without importing MiniTest, and how existing `GSParallelBackend` and tick2050 selected capture would be invoked. Preserve recorded path byte, priv/transfer/marker order, original Present descriptor, and full 4 MiB/448-tile census. Default off; normal game boot unaffected.

Design data staging with *actual app-readable path/permission evidence* from N8D7M6 launch/capture scripts and Android manifest: the 1.10 GB stream must stay outside git, have two matching SHA reads before use, and fit a checked device free-space budget. State APK/Turnip/HAL pins, one Odin lease, battery ≥20% charging, keyguard clear, install every run, ≤600 s boot, force-stop after each run, bounded logs and device cleanup. Flag any Android scoped-storage or linker assumption unproved; do not invent an `adb push` path. For a prospective diagnostic comparison require Mac replay baseline broad, same-source decoder, Odin input byte hash and 11-field descriptor, same-binary ON/OFF control *within each platform* and a viewed tick2050 Odin frame. Broad ≥250/448 offline makes live scheduling a lead; sparse ≤100/448 makes device graphics a lead; 101–249 or any control mismatch is OTHER. Neither proves the faulty packet, copy timing or 120 Hz speed.

Outcome A = an implementable bounded harness design with grounded entrypoint, source split, app-readable input and Turnip loader path; B = one or more of those remain unproved, with the smallest read-only validation that can close them; OTHER = pin/citation mismatch. No implementation or Odin run from this part. Deliver `REPORT.md` with option matrix, exact unexecuted patch/command plan, controls/stop table, costs and gaps. `check.py` verifies pins and cited rows, rejects device commands executed or an ungrounded path, and states source strings cannot prove runtime. Commit `[N8D7M11]` with `Orchestrated-By: opencode`, no push; **stage only this brief and `local/research/N8D7M11/` by explicit paths, never another lane or pre-staged docs**. Hand back the table; orchestrator gates.
