# G5 — Port the spikes upstream: CLUT/RMW/Present as real patches, proven

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables, no verdicts**. Read first:
`docs/reports/G3.md` (§3 spike vehicle, `[G3-CLUT]`/`[G3-RMW]` hunks)
and `docs/reports/G4.md` (§2 `[G4-PRESENT]` hunk). Both reports end
with "an upstream patch would re-apply the hunks" — this brief IS that
patch, proven without ever editing the pinned submodule. G1 still waits
on first frame.

## Gates and rules

- Host only, no `adb`, no device, no emulator, no lease. Builds `-j4`
  any time in `/tmp/ps2xgs-build`. No new captures (reuse the 102);
  anything over 5 MB goes under `/Volumes/Extreme SSD/ps2xgs/`, never
  into the repo.
- NEVER edit anything inside `upstream/` (it stays pinned). Patches
  live as files under `upstream-patches/` + a proof target that
  applies them to a scratch copy at build time.
- The `cpu` backend's default behavior stays byte-identical (all 102
  identity rows must still pass unchanged). `strict` keeps its 14
  diffs; `spike` keeps its behavior.
- Commit in this repo as you go (prefix `[G5]`, trailer
  `Orchestrated-By: Muse Code`); push `origin main` at the end. Never
  commit captures. Do not touch `/Users/bradrichardson/dev/ssx3`
  except to write `local/research/G5/REPORT.md` there at the end
  (`git add -f`, `[G5]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box 6 hours.

## Steps

1. **Patches.** Emit 3 patch files under `upstream-patches/`,
   each applicable with `patch -p1` to the pinned upstream rev and
   each self-contained: `clut-cache.patch` ([G3-CLUT]), `rmw-lookup.patch`
   ([G3-RMW]), `present-scratch.patch` ([G4-PRESENT]). Receipts: the
   files, `patch --dry-run` clean on a scratch copy of the pinned
   file, hunk counts.
2. **Proof target.** A `patched-cpu` factory backend built from the
   scratch-applied sources (build-time apply, submodule untouched).
   Receipts: identity replay exact on all 102 vs `cpu` references
   (full table), `median_ms` deltas on the §2b capture set (G3/G4
   method: interleaved, 5 runs, min-max), `submit_ms_total` control.
3. **CTest + report.** Extend CTest (patched-cpu identity over the
   102; suite stays green). `docs/reports/G5.md` in the repo and a copy
   at `/Users/bradrichardson/dev/ssx3/local/research/G5/REPORT.md`:
   patch inventory, proof-target identity table + timing deltas, CTest
   output, exact commands, receipt paths, "What I could not do". No
   verdicts. Commit both, push ps2xGS `origin main`, stop.
