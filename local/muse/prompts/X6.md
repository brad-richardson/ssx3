# X6 — sparse Qwen mechanical LWU site index

You are a local opencode/Qwen **sparse** worker in `~/dev/ssx3`.
Follow `~/dev/AGENTS.md` worker rules. Read only canonical generated
code under `~/dev/ssx3-work/codegen-ssx3`; write only
`local/research/X6/index.py`, `lwu-sites.csv`, and `REPORT.md`. No fork
edit, build, boot, device, network, or push. Hand back the table, **no
semantic conclusion**. This is a bounded extraction for the next E54
`LWU` repair, and an experiment in script-checked sparse-model work.

Source pin: E56 canonical register file SHA-256
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`.
Check it twice. The generated `.cpp` files contain guest instruction
comments shaped `// 0x3e3968: 0x9c830000  lwu  $v1, 0x0($a0)`;
duplicated generated bodies may repeat the same guest address. The
frontier review estimated 92 guest LWU sites; that is a hypothesis to
check, not a number to force.

Write `index.py` first. It must stream all `sub_*.cpp` files in sorted
path order, match only guest instruction comment lines whose mnemonic
is exactly `lwu`, and emit one CSV row per **unique guest address**:
`guest_pc,opcode,operands,generated_copies,source_files`. Addresses and
opcodes should be lowercase `0x` hex, operands copied verbatim with
CSV quoting, and source files as sorted relative basenames joined by
`|`. If duplicate copies disagree on opcode or operands, exit nonzero
and list the conflicts in `REPORT.md`; do not resolve by guessing.
Print raw-hit, unique-address and duplicate-copy counts. Script must
be deterministic on a second run, with byte-identical CSV and SHA-256.

Acceptance: `python3 local/research/X6/index.py` creates the CSV;
`wc -l` equals unique addresses + header; a second run gives the same
CSV SHA; no non-`lwu` comments appear in the rows. Orchestrator will
independently rerun and spot-check addresses against source. Keep
`REPORT.md` ≤30 lines with revision/pin, exact commands, counts, CSV SHA,
conflict status and gaps. Treat `92` as checked or refuted, never infer
runtime reach from the static index.

Time box 20 minutes, ≤25k model context, ≤1 MiB new files. Commit only
these three explicit paths as `[X6]` with trailer
`Orchestrated-By: opencode`. `local/` is gitignored: `git add -f` only
the named files. No push.
