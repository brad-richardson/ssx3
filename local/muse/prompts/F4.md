# F4 — fold VR1 (VU1 stage A) + NP1 fixes + TC1 onto fork `ssx3`; Odin speed pair; iPhone + Odin builds (muse)

Gates: `local/research/{VR1,NP1,TC1}/REPORT.md`. Brad approved device installs (iPhone install only).

## Part 1 — fold + Mac check (stop after; the orchestrator gates and pushes)
1. Worktree `~/dev/ssx3-work/F4/PS2Recomp`, branch `f4-fold` from fork `ssx3` **`ec2dbf1`**. `git cherry-pick -x`:
   - VR1: every commit in `0ed07c4..1f51e48` on `vr1-vu1-recomp` (oldest first; list them in the report), then `6c2de6f` (Android Gradle flag). Not `8cea160` (g5).
   - NP1: `e5654f3 7caf516 125c9e5` (VR1 notes a trivial textual overlap with NP1 in `ps2_vu1.h`: resolve by keeping both changes; if the resolution isn't obvious, stop and hand back the hunks).
   - TC1: `e29e4975` (vf0 read-only in the emitter), plus one new commit of yours: VU0 R = 1.0-bits in the `R5900Context` constructor, mirroring main (`ps2_runtime.cpp:878`), with a unit test.
   Conflict you can't resolve mechanically → stop, save hunks to `local/research/F4/conflict.txt`, hand back.
2. **Codegen:** TC1 changes the emitter, so regenerate into `~/dev/ssx3-work/F4/codegen` (SB1/F2 recipe, tracked `games/ssx3/ssx3.toml` with paths repointed). Report `diff -rq` vs the canonical `codegen-ssx3` (expect only vf0-store removals; list the changed file count and a sample hunk). **Do NOT promote it to canonical** (other lanes are mid-gate on the current codegen); build with `PS2X_GAME_CODEGEN_DIR=~/dev/ssx3-work/F4/codegen`.
3. VU1 generated sources: `~/dev/ssx3-work/vu1gen-ssx3` (7 files; game-derived, never in git), passed via VR1's CMake option (`PS2X_VU1_RECOMP_DIR`; see `local/research/VR1/REPORT.md` Design/Generation).
4. Suite from the worktree root; runner-dir check empty. Mac Release builds (clean + det-hash), paraLLEl `19d93b2`.
5. Boots (one slot each, I26-FAST, empty mc0, `PS2X_SKIP_MOVIE=1`, paraLLEl + `PGS_HIER_BINNING=force`, deterministic): **B1** sound on to t2400, frames ~1090/1800/2100 viewed (rider solid, brightness), cid0 = 531, coverage 0/0/4, `PS2X_VU1_RECOMP_STATS=1` → 100 % generated. **B2** sound off + `PS2X_UNPACED=1`: det-hash identical to B1. **B3** clean speed `--exclusive`, `PS2X_UNPACED=1` vs F3 B3 (race 0.249×): expect ~1.4×.
6. **Android compile check** on bytesize (F3 recipe + `-Pps2xVu1RecompDir=<private copy of vu1gen-ssx3 on bytesize>` + the F4 codegen copied there; two SHA reads of `register_functions.cpp` and file counts both sides): `assembleRelease` succeeds; keep the APK.
7. `local/research/F4/REPORT.md` Part 1; commit `[F4] Part 1 …`. **No push.** Stop.

## Part 2 — Odin (after the push)
Two clean race runs (F3 launcher, `PS2X_UNPACED=1`, runbook pair method: thermal ≤ 1 + 180 s before each) vs F3's 0.139×; then install as Brad's play build + `deploy-odin.sh <serial from local/odin-serial>`, no launch. Also 30 s `simpleperf --call-graph fp` race profile (NP1 driver option) → the per-stage table (NP1 Part 1 model) so we see what's left after VU1 stage A. Append `## Part 2`.

## Part 3 — iPhone (after the push; alongside Part 2)
I33/F3 iOS recipe with the F4 codegen + the VU1 generated sources compiled in (`PS2X_VU1_RECOMP_DIR`). iPad fresh-card launch (frames viewed; diagnostic pace vs F3's iPad run), then iPhone install + `deploy-ios.sh iphone`, **never launch**. Append `## Part 3`.

Rules: text only in git; never push; never `git add -f` in the fork; generated code (game codegen and VU1 sources) never in git anywhere; scratch `~/dev/ssx3-work/F4/` ≤ 20 GB, delete build dirs when done; check/kill runners by PID. First failure: stop, save the error, hand back.
