# X9 — dense Qwen with fixed source excerpt and report skeleton

You are a local opencode/Qwen **dense** worker in `~/dev/ssx3`.
Follow `~/dev/AGENTS.md` worker rules. This is a time/context tuning
trial, not an E54 verdict. Read only this brief and
`local/research/X9/source-excerpts.txt` (3.7 KiB, pinned fork
`1aaed05`). Edit only the existing `local/research/X9/REPORT.md`.
No full source-file reads, `rg`, LSP, web, source edit, build, boot,
device, or push. The short excerpt already contains the call from
`emitConditionalBranch()` to `conditionalBranchExpression()`.

Within 2 minutes, replace **all** `FILL` cells/lines in the report.
For each BLTZ, BGEZ, BLEZ, BGTZ family, copy the current emitted
predicate and source line from the excerpt; list variants sharing its
case. Compute current (signed low 32) and expected (signed low 64)
truth values for A=`0x0000000180000000`,
B=`0xffffffff00000000`, and zero. State the emitter connection using
the excerpt's exact line. Label expected s64 as the review hypothesis;
the excerpt does not prove actual PS2 hardware semantics. If any
line is missing, write `gap` and stop rather than searching more.

Use only these commands before editing:
`cat local/research/X9/source-excerpts.txt` and
`cat local/research/X9/REPORT.md`. Do not read another file or run
`git` until the table is filled. Then check no `FILL` remains, commit
only the report as `[X9]` with trailer `Orchestrated-By: opencode`
and explicit path; `local/` is already tracked by the orchestrator.
No push. Hand back the table, no verdict.

Budget: 8 minutes, ≤20k model context, ≤1 MiB report. The
orchestrator will verify every row and compare time to first report
with X7/X8. If the context cap is reached first, stop and say so.
