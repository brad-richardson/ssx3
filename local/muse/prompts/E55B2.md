# E55B2 — cycle-only EE scheduled-event choice

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4.
Read `local/research/E55B1/REPORT.md` fully. Hand back tables and
receipts; the orchestrator decides the gate. The paths below are your
write scope. “First failure” means a failed brief step, not a typo in
an exploratory command. **One mechanism:** exact
`PS2X_DETERMINISTIC=1` makes EE scheduled-event and idle timer selection
use guest cycles rather than host deadline order. The default mode must
keep current behavior. Do not change RTC, pad, card, GS, Count/Compare,
interrupt causes, or the ExternalWake interface.

Pin fork `ssx3` at `4f932166c773e04522ffa007228670894f8b4f44`
(E55A2 pushed; verify remote). Create a new worktree
`~/dev/ssx3-work/E55B2/PS2Recomp`, branch `e55-cycle-events`. Reuse
canonical `~/dev/ssx3-work/codegen-ssx3` (9,457 files; register SHA
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`);
no regeneration. Allowed fork edits: `ps2xRuntime/include/runtime/ee_scheduler.h`,
`ps2xRuntime/src/lib/Kernel/EeScheduler.cpp`, the smallest runtime
env-wiring change if required, and focused `ps2xTest` source/registration.
Builds, bounded runs and text receipts go under `E55B2`; report only at
`local/research/E55B2/REPORT.md`. No fork or ssx3 push.

## Gate

1. Set scheduler mode before `reset`, by parsing the exact flag once
   per runtime/scheduler instance so tests can make separate instances.
   Keep unset, empty, `0`, and other values on the old path. In
   deterministic mode, a cycle-due batch includes **all** cycle-due
   scheduled records, regardless of host deadline, sorted by guest
   cycle, event type, ID, insertion sequence. For idle, choose the
   lesser guest-cycle target between scheduled event and memory timer;
   at equality preserve scheduled-event-before-timer dispatch. Host
   time may pace an already selected target, but may not change its
   guest cycle, membership or order. A pending ExternalWake/stop still
   interrupts the wait; document that ExternalWake has no guest-cycle
   timestamp and is outside this deterministic guarantee. Preserve
   legacy behavior byte-for-byte in the default branch.
2. Add focused tests that exercise the real scheduler selection path,
   with injected/fake host deadlines instead of sleeps: (a) same-cycle
   alarms with reversed host deadlines, (b) different-cycle deadlines
   with reversed host deadlines, (c) timer at cycle 80 versus event at
   cycle 100 with reversed host deadlines, (d) equal-cycle timer/event,
   and (e) default mode's old host-selected outcome on at least one
   contrast. Include exact-flag parsing. A minimal test seam is allowed
   if needed; no production-only test switch or test that just repeats
   a helper's comparator. Table any case that cannot be tested without
   widening scope before using a live boot.
3. Mac Release/Ninja build and **full suite** taps OFF then ON from the
   fork root. Baseline is 581/581 and 676/676. Wait for competing
   clang/ninja, use `nice`, and verify codegen SHA. If tests pass, one
   mini-lease diagnostic CPU boot with `PS2X_DETERMINISTIC=1`, dev-only
   `PS2X_SKIP_MOVIE=1`, I26-FAST vsync pad route, target race tick ≥2050,
   wall cap 500 s, frame targets exactly `830,1810,2050`, bounded logs.
   View all frames and report HUD/progress. Two matching SHA reads of
   ISO, ELF, runner and canonical register file before use. The boot is
   functional regression, **not** a full determinism or speed gate.
4. Runner-directory guard:
   `git diff --exit-code 14b1e5cb HEAD -- ps2xRuntime/src/runner`.
   If all gates pass, commit only allowed source/tests `[E55B2]` on the
   local branch, trailer `Orchestrated-By: Codex`; no push.

Budget: two configure/build attempts per tap mode (one named repair),
one boot ≤600 s absolute, ≤5 GiB E55B2 bytes, global 200 GB cap;
logs ≤32 MiB, frames ≤25 MiB. Stop after one failed gate and one clear
repair, no tuning loop. Claim/release mini lease and kill only own PID.
Do not run the live boot if source tests or default behavior fail.

Deliver `local/research/E55B2/REPORT.md` with pinned rev, exact source
mechanism/default contrast, synthetic test table, suites, boot/frame
SHA pairs, budgets, runner check, exact commands and gaps. Commit only
that report `[E55B2]` with the same trailer; no push.
