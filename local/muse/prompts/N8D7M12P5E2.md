# N8D7M12 Part 5E2 — one same-settings Odin OFF repeat

You are the N-lane OpenCode Go run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5E1/{REPORT.md,ORCH-GATE.md}`, Part 5D2 REPORT/gate and this brief. Own `local/research/N8D7M12P5E2/` plus private scratch created by the gated launcher `~/dev/ssx3-work/N8D7M12P5E1/`. No script/APK/stream/source/fork edit, second launch, Mac replay, push, board/global edit or upstream contact. Time box 30 min plus active ≤600 s; text <512 KiB, device output ≤64 MiB. Write REPORT/checker early, hand back the evidence table, no graphics root-cause verdict.

Goal: test whether the Odin GPU hashes repeat with **identical OFF settings**. P5D2 OFF1 passed one run but differed from ON at tick850, before the removed capture flags act; its frame is sparse. OFF2 uses the same APK `caa11102…f512`, stream `f6a78f71…a593`, parallel/Turnip backend, no three capture flags, step50/PPM2050. The only launcher diff is unique output/lease labels. Reviewed OFF2 script SHA is `ad22755d07cd4ce87cbd6823144181389e0b50d2a2f081f3cf879f54b80af4ca`.

Preflight: verify that script SHA, 33/33 self-check, compile, no existing OFF2 result/driver receipt, OFF1 hash/PPM pins intact, serial `622c49b1`, device lease free/app stopped, battery charging ≥20%, free ≥10 GiB and keyguard `showing=false`. Locked device is a blocker, never worked around. No simultaneous host heavy job. The launcher double-hashes APK/stream and device copies, installs even if present, leases, launches once, caps wall/progress/log/output, sends BACK once, force-stops, confirms PID absent, restores prior env with 2+2 SHA and releases lease in `finally`. Stop on first preflight failure; no retry.

One released command from repo root: `python3 -u local/research/N8D7M12P5E1/launch.py --released-sha ad22755d07cd4ce87cbd6823144181389e0b50d2a2f081f3cf879f54b80af4ca`.

Acceptance: full parallel replay markers2050, exact 41 ordered ticks 50..2050, frame2050, PPM/hash 2+2 SHA, one install/launch, cleanup intact. Compare OFF2 `parallel.hashes` and PPM with OFF1 `~/dev/ssx3-work/N8D7M12P5D1/` by exact tick and priv/VRAM/present equality counts, first difference and PPM SHA. If any GPU row differs, same-settings variance is observed; if all rows and PPM match, output reproduced on this pair. An input/preflight/cleanup failure voids the comparison. Give absolute OFF2 PPM path/SHA for orchestrator viewing. No speed number from diagnostic elapsed time.

Deliver `REPORT.md`, a narrow device-free receipt checker and bounded text receipts with exact pins, comparison table, first failure and cleanup. Commit `[N8D7M12] Part 5E2` with `Orchestrated-By: opencode` and **explicit paths only**, no push. Hand back the table.
