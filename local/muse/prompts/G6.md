# G6 — Release-build numbers + upstream PR prep (no submit)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables, no verdicts**. Read first:
`docs/reports/G4.md` (§2b timing tables + method, §6 -O0 caveats) and
`docs/reports/G5.md` (§1 patch inventory). Every timing in G0–G5 is a
`-O0` build; this brief re-measures at release optimization and preps
the upstream submission without submitting it.

## Gates and rules

- Host only, no `adb`, no device, no emulator, no lease. Builds `-j4`
  any time in `/tmp/ps2xgs-build-o2` (fresh dir; leave
  `/tmp/ps2xgs-build` untouched). No new captures (reuse the 102);
  anything over 5 MB goes under `/Volumes/Extreme SSD/ps2xgs/`, never
  into the repo.
- Do not edit anything inside `upstream/`. Do not change any default
  build flags in `CMakeLists.txt` (the -O2 build is a separate
  configure line, not a repo change). No network writes anywhere:
  no `gh`, no PR submission, no pushing outside `origin main` of this
  repo.
- Commit in this repo as you go (prefix `[G6]`, trailer
  `Orchestrated-By: Muse Code`); push `origin main` at the end. Never
  commit captures or build dirs. Do not touch
  `/Users/bradrichardson/dev/ssx3` except to write
  `local/research/G6/REPORT.md` there at the end (`git add -f`, `[G6]`
  prefix, same trailer; NEVER `git push` in ssx3).
- Time box 4 hours.

## Steps

1. **Release numbers.** Configure with `-DCMAKE_BUILD_TYPE=Release`
   (record the exact flags CMake uses), build, run the full CTest
   suite (must stay green — record any failure verbatim, do not fix
   beyond a build-system path issue, and say which). Re-run the G4 §2b
   capture set on all four backends (`cpu`, `strict`, `spike`,
   `patched-cpu`): `median_ms` + `submit_ms_total`, G3/G4 method
   (interleaved, 5 runs, min-max). Receipts: -O0 vs -O2 table per
   backend per capture + the CTest log. If -O2 collapses the G4 delta
   (bulk idioms now equal), that IS the finding — table it.
2. **Upstream PR prep (no submit).** Write `upstream-patches/PR.md`:
   per-patch summary (what/why/measured effect at -O0 AND -O2),
   exact base rev, the dry-run + proof-target receipts by reference
   (G5 §1–§2), test plan (the CTest gate names), and the known caveats
   (G5 §6). Verify every number cited against the G5 report + Step 1
   reruns (no copied-but-stale figures). Nothing leaves this repo.
3. **Report.** `docs/reports/G6.md` in the repo and a copy at
   `/Users/bradrichardson/dev/ssx3/local/research/G6/REPORT.md`:
   -O0 vs -O2 tables, CTest output, PR-prep inventory, exact commands,
   receipt paths, "What I could not do". No verdicts. Commit both,
   push ps2xGS `origin main`, stop.
