# N8D6C — one Odin scanout-stage run

You are a Codex Sol **medium** worker. Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. Work only in `local/research/N8D6C/`, Mac scratch `~/dev/ssx3-work/N8D6C/`, and lease-held Odin app data. **One install, one launch.** No source/build/fork/main push, extra run, speed claim, iOS action or upstream contact. No device action until the orchestrator reviews your launcher, predeclared report and script SHA and sends a release message. A device preflight blocker is reported immediately.

Why: N8D5G (`local/research/N8D5G/REPORT.md`) measured a sparse final GPU image on Odin, 34/896 active with sampled/raw exact equality and a viewed same-run PNG. N8D6A's Mac replay (`local/research/N8D6A/orch-replay-excerpt.txt`) measured circuit1 and pre-deinterlace merged each 300/448 active at 512×224, final 567/896 at 512×448, controls 128. N8D6B (`local/research/N8D6B/REPORT.md`) packaged the stage probe without an Odin run. The run locates loss within one Odin frame; the Mac and Odin routes are not byte-identical GS streams.

Pinned APK: `~/dev/ssx3-work/N8D6B/app-release.apk`, SHA `6839a0a48ed1b55affb6e6bd952a269ba3736200255bb6628270bb11a874a611` (two reads); runner `4e6c056dc48683ab725e16792748b83a122421d865f523d6d37c4bbb27fa0efd`, Build ID `8d19e78d5e5eb7cb547be96b9ebb1a72b31a99f0`; Turnip `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`; HAL shim `1b49d27c839f25363237d8276c91a401602845ee8eef67de50d6540f4cdfc387`. Installed ELF pin `1b49d05ca2793922180851b9e1ce9ae2291d61a7863565ac4e71f12e967af7bc`, ISO `3c2f8eb182c9c6208a6e8172a41e61c98f420abe3f42c845f6829aeb9761ebf5`. Use N8D5G `launch.py` as the route/lease/install/env/log/force-stop/frame-pull pattern. Copy it to N8D6C and change only run ID/paths/APK pins, plus the bounded stage parser and classification needed here. Record exact code changes. Write `REPORT.md` with pins, thresholds, commands and gaps **before** launch. Show launcher SHA and diff to orchestrator, then wait for release.

Predeclare categories at tick2050/FBP112/PMODE `ff21`, frontend 512×448, stage dimensions circuit1/merged 512×224 (448 tiles) and final 512×448 (896 tiles), every stage control128, final old control128/PASS, final sampled/raw vectors exact 896/896, same-run tick2050 PNG+metadata, no probe/pipeline error. With all those gates met: **A** circuit1 and merged active≤100/448 and final sampled/raw active≤100/896; **B** circuit1 active≥250/448, merged≤100/448, final≤100/896; **C** circuit1 and merged active≥250/448, final≤100/896. Everything else **OTHER**. Counts use the existing 16×16/≥32-pixel method. Do not conclude an Odin cause or compare byte-for-byte to Mac.

Before each APK launch check online serial `622c49b1`, `/data/local/tmp/mg/LEASE` free, keyguard `showing=false`, charging/full battery≥20%, ≥1 GiB free. Claim lease; install even if present, double-read installed APK/ELF/ISO pins, require empty `mc0`, unique empty frame dir. Recheck unlock/battery immediately before launch. Use parallel/Turnip, dev movie bypass, installed ISO, I26-FAST vsync pad route, `PS2X_N8D5_TILE_CAPTURE=1`, tick2050 one-frame dump and progress-only vsync log. Same-PID logcat cap 16 MiB; send BACK once after 6 s for USB dialog. Stop on first complete alignment + circuit1/merged/final stage summaries + control + sampled/raw vectors + tick2050 PNG/metadata; first fatal/process exit; tick2100 without complete receipt; 180 s below tick1700; or 300 s wall. Force-stop after the run, verify PID gone and release lease even on error. Pull only tick2050 PNG/txt, two device/local SHA reads; compress closed full log in scratch. Mac scratch <500 MiB; committed text/image <8 MiB; global mini ssx3 <200 GB. No second run without a new brief.

Hand back APK/runner/Turnip pins, env/script SHA, install/launch counts, PID, tick/FBP/PMODE/dimensions, each stage's tiles/occupied/active/control, old final sampled/raw equality, PNG/metadata hashes, viewed frame path, first failure and cleanup. Commit bounded receipts plus PNG as `[N8D6C]` with `Orchestrated-By: Codex`, explicit paths, no push. The orchestrator views the frame and makes the verdict.
