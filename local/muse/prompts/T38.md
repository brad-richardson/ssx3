# T38 — Longer-hold steering variant (1 s D-pad Left) on the live race: HUD effect (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T37/REPORT.md` first (all of it: R1
reproduced the chain to LIVE gameplay, ONE 338.2 ms D-pad Left tap @T+505.09
on a live race (pre-pair 00:01:26 2ND/6 40% / 00:01:31 3RD/6 42%), dense
10-snap post-tap series to 00:02:11 3RD/6 60% with NO separable tap response
— position 2ND↔3RD swaps occur with and without the tap, RDC validity-
flooded 6/12 snaps, SCPS rails ±40 nearly every step including the no-input
pre-pair gap; race clock ~1.4× wall under turbo; G1 proposes (a) longer
hold, (b) no-nudge control — this brief takes option (a)). This brief
executes T37's G1(a). Reuse T37's scripts (`t37-auto.sh`, `t37-cropdiff.py`,
`t37-track.py`, `t37-analyze.sh`, `t37-vcount.sh` — copy, do not modify the
committed originals).

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T37 verified (re-verify sha/size/rev/
  status/pad-binding INCLUDING `Left = Keyboard/Left` + NVM state before
  running; table the receipts).
- GATE NOTE (T37 G1 — gate on the HUD, not the frame): gameplay hops are
  large (~8.48–17.86 dense) because the race never stops moving. Live-race
  criterion = race clock advancing + position/progress HUD read off VIEWED
  snaps. The LIVE-LIKE proxy (departed + non-panel + motion ×2) still gates
  the press in-script.
- The task: reproduce the T37 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → LIVE-LIKE proxy gate → ≤3 attempts) to LIVE gameplay, then
  apply ONE 1 s D-pad Left HOLD (single `keydown`, `sleep 1`, `keyup` —
  the ONLY input-class change vs T36/T37; do NOT stack taps) with a PRE-hold
  pair (no input between, noise floor) + a bounded post-hold series (T37's
  dense shape: 1 s cadence × 10 s, or the T36 sparse shape if the hold's
  effect is gross — table the choice BEFORE the run). Table per-snap HUD +
  tracking (T37's RDC/SCPS with its validity channels + the T37 refinements
  if cheap: validity-gated RDC, wider SCPS window) + whole-frame hops.
- If the hold STILL shows no separable response: table the exact observed
  series + noise floors and STOP with a recipe — one input variant per
  attempt, never blind multi-presses. ONE bounded variant allowed only if
  the hold provably never acted on live gameplay (pre-hold ≠ racing).
- Success bars (tabled, not verdicts): live race reproduced (HUD advancing,
  rider moving); hold acted on it (pre-hold = racing with HUD read);
  post-hold HUD + tracking series (or exact non-separability finding with
  noise floors); full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T37 G2: single-shot
  scripts, runs ≤6 min (table any overrun with full dmesg coverage),
  preserve emulogs first, dmesg + btime + init age, UNFILTERED dmesg +
  event log around flaps, 534 ms MENU holds NOT bisected (the 1 s steering
  hold is a brief-authorized new input class, not a bisection), `;` chains
  only INSIDE one `wsl` call, one `wsl` call per ssh; post-run dmesg read
  IMMEDIATELY after the run ssh returns). COUNT AcceptAsync lines in the
  committed dmesg files with exact counts + uptime-stamp positions relative
  to the run window. Runs in flap windows must be effects-verified or
  re-run (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T38/` (STANDALONE). Commit with `git add -f`,
  prefix `[T38]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + ONE 1 s hold + bounded series (≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T37 §T37-0 shape): build/input receipts (INCLUDING the
   Left binding), NVM state, T37 reference snaps present, free space both
   ends. Table the series shape + tracking refinements BEFORE the run.
2. Per attempt table: race reproduction (chain scores + live-race gate:
   HUD advancing), hold row (control/duration/T+/pre-post HUD), post-hold
   series (HUD + tracking + hops per snap) or exact non-separability
   finding.
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T38/REPORT.md`: pre-run receipts, attempt tables, hold log,
post-hold series + tracking (or non-separability finding + recipe), WSL
table, exact commands, gaps. Commit evidence (`[T38]`), stop.
