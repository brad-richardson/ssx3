# E18 — MPEG type-1 completion fix: generic non-stream delivery in MPEG.cpp (first behavior fix; ownership-audited; 1 guarded boot)

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/I20/REPORT.md` first (all of
it: agreement A1–A13 with ZERO disagreements, P1–P3 receipted, fix-ready
spec S1–S10 + handoff H1–H6 — this brief CONSUMES that spec). Then read
`local/research/E15/NEXT-BRIEF.md` §4 + `local/research/E16/NEXT-BRIEF.md`
(the parked MPEG design constraints — they BIND this brief) and
`local/research/E17/REPORT.md` (absorb landed as fork `e63f1616`, 452/452,
1/1 probe spent with real footers; MPEG still rc1 fail-before). This brief
implements the fix ONLY (the first E-track behavior fix since K1). No map/
CSV changes, no regen expected (pure handwritten-runtime fix — if the fix
somehow needs regenerated artifacts, table it and STOP, do not regen).

## Facts you start from

- BASE: fork `e63f1616` (E17 MAP absorb, PUSHED — verify `fork/ssx3`
  before building). `/tmp/p1-link` + `/tmp/e17-map-link` exist (E16/E17
  builds — PROTECT them + DerivedData; never reclaim). Checkpoint
  re-verify first (fork SHA, 9,457-name audit, suite 452/452, E16
  closure cases, E15 MPEG cases rc1 fail-before).
- The fix (I20 S1–S5, E15-bound — file/line-pinned, no guest-PC special
  case): new `matchingNonStreamCallbacks` selector beside M:1141-1158
  (`!callback.stream && callback.type == requestedType`, type from the
  OBSERVED cbData word0 — A3 — only actually-observed types serviced);
  new `dispatchGuestNonStreamCallback` beside M:1591-1630 (a0=mpeg
  a1=cbdata a2=userdata, word0-only cbData, v0 DISCARDED, cbData freed
  in `onComplete`); trigger call on the GetPicture-ENTRY path (A4/P3 —
  NOT Demux-entry, NOT vsync; the diff names the exact call site);
  collect-then-dispatch OUTSIDE `g_mpeg_stub_mutex` (S4 — the callback
  re-enters via AddBs); `sceMpegDelete` invalidates pending deliveries
  (S5); NO `EeScheduler` sema change (S6 — census-closed).
- THE GATE (I20 A5/G2, E15 NB §4): whether `queueInvocation`/
  `RpcCallback` preserves caller-thread ownership is UNPROVEN — AUDIT
  it first (table the evidence both ways); deliver synchronously in
  the GetPicture caller if the audit fails. Matching arguments alone
  is insufficient. No invented layouts, no EOF-from-v0, no assumed
  `0x20` cbData layout, type-1-only trigger.
- Regressions (must ALL pass): suite 452/452 + leaf24/consumer3/
  predicate14/query4/DROP + E16 closure cases stay green + E15's
  actual-wrapper `no-input`/`input` cases FLIP rc1→rc0 (the fix's own
  fail-before gate) + new R1–R6 tests per S8 (delivery, AddBs
  re-entry deadlock-free, GetPicture resume, valid-no-input NOT
  spuriously completed, teardown-during-pending safe, stream behavior
  retained). Stream-callback behavior byte-for-byte retained.
- ONE guarded boot (fresh T13 pre-claims, shared P-lane lease,
  wall/progress/byte caps, REPORT_ALL=1, E15/E7/E4; lease occupied →
  table and stop). Expect: main past `0x3b1028` (or a NAMED successor),
  `0x4029d0`/`0x3b0b10` dispatches >0, input/completion events >0.
- STOP RULE (S7/S10/E13 §5): the FIRST demonstrated missing link AFTER
  delivery names the next brief — if delivery lands but decode fails
  (stub/FFmpeg), or EOF/lifetime gaps appear, table the EXACT edge and
  STOP. No stacking a second fix, no routing around a guest-side gate.

## Gates and rules

- Fork owner: you. The fix is a BEHAVIOR commit (MPEG.cpp + MPEG.h ONLY
  if the trigger needs a cross-TU entry + regression tests; NOTHING
  else — no CSV, no generated sources, no main, no scheduler). Push to
  the `fork` remote yourself with the `ls-remote` agreement receipt
  (E17 precedent).
- Boots: ≤1 guarded boot under the shared P-lane lease. Fixture boots:
  non-title only, no cap games. No boot without the lease; never wait
  on another lane's lease.
- `export COPYFILE_DISABLE=1` on SSD steps; allocated-byte caps declared
  + tracked (ExFAT). No bytesize/WSL. Host builds `-j2` unless provably
  otherwise (table it). Declare + verify the budget BEFORE editing.
- Evidence dir `local/research/E18/` (STANDALONE). Commit with `git add -f`,
  prefix `[E18]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (the orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — audit + fix + regressions (no title boot)

1. Checkpoint re-verify (fork/9,457/suite/E16-closure/E15-fail-before).
   Ownership audit (A5) FIRST — table the verdict; it gates sync vs
   queued dispatch. Budget declared + verified-fitting before edits.
2. Implement S1–S5; add R1–R6 tests. FULL regression (suite + all
   fixtures + E16 closure green + E15 MPEG cases FLIPPED rc1→rc0 +
   stream retained). Any red: table + stop (no boot on red).

## Task 2 — one guarded boot + fork commit

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Boot with E15/E7/E4;
   table delivery/completion joins (dispatch counts, GetPicture resume
   or its named successor, graphics alignment). First missing link
   after delivery → table + STOP (no second fix).
2. Fork commit (MPEG.cpp/h + tests only) + push + `ls-remote` agreement.
   Handoff: per-file diff summary + the I-lane re-probe trigger (S9:
   rebuild + reinstall + 90 s probe — their brief, not yours).

## Report

`local/research/E18/REPORT.md`: contract, audit verdict, fix + test
tables, boot joins, handoff, exact commands, gaps. Commit evidence
(`[E18]`), stop.
