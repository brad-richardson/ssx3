# P10 — Settle PollSema -1 vs KE_SEMA_ZERO (-419)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P1z/REPORT.md` §P1z-3 (Wait/Poll
essence table) and §P1z-7 item 4 first (the observed divergence: stock
kernel PollSema returns plain `-1` when count is 0; the fork returns
`KE_SEMA_ZERO` = `-419`), plus Part 23 §P23-2c (F6's PollSema drain
role in the handshake — the one in-tree consumer whose behavior could
change). This brief settles it kernel-first per the review's §7.5 rule
(hardware semantics from disassembly; the rule, not a judgment call, picks
the winner — record the game's-use check below as the blast-radius
receipt). P1ab/P9 run concurrently in other panes — you share only the
fork remote.

## Facts you start from

Two candidate truths: (a) kernel returns `-1` (P1z's static read of
`0x80004de4`→`0x80004dcc`, unre-derived); (b) the fork returns `-419`
with a `KE_SEMA_ZERO` code path + test expectations + the P1w exclusion
rule that keeps poll-misses out of the `[drop]` census. Suite baseline
is 427/427/0. Current ladder state (P25): boot parks on `0x52BE04`.

## Gates and rules

- Lease `P10`: boots ONLY while holding `/tmp/ssx3-host-lease` — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/p10-waits.log`. Builds any time (`-j4`). Max 2 boots,
  ≤90 s foreground each. No `adb`.
- Fork: the poll files ONLY (`Sync.cpp`/`EeScheduler.cpp` poll path +
  `ps2_runtime_kernel_tests.cpp` — record the list). `git add` those
  NAMED files only — never `-A`, never stage or touch anything else.
  Verify the diff before committing. One commit per change with the
  `Orchestrated-By: Muse Code` trailer. `git pull --rebase` before
  pushing; `git push` ONLY inside the fork clone to the `fork` remote.
  Stop on any foreign rebase conflict. Never commit generated sources
  or `._*`.
- Evidence dir `local/research/P10/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief owns it). Commit with `git add -f`,
  prefix `[P10]`, trailer `Orchestrated-By: Muse Code`. NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Re-derive + blast radius (lease-free).** Re-derive P1z's two
   kernel words from the in-repo BIOS copy (pristine check first);
   grep every fork + test consumer of poll-miss returns (F6's drain?
   other guests paths? test assertions?) and table what changes value
   under each candidate truth.
2. **Implement the §7.5 winner.** Kernel-first: match the kernel
   unless the blast-radius table shows a game-visible regression —
   then keep `-419` with the evidence table as the justification (the
   rule allows "settled" to mean "justified deviation", recorded).
   BEFORE/AFTER receipts (same tests, both trees). Suite must stay
   all-green (name any failure face).
3. **Max 2 boots.** Receipts: boot still parks on `0x52BE04` (33/0
   creates, balanced handshake — F6's drain behavior unchanged or
   changed-as-predicted with the exact line), census unchanged. One
   ladder table vs P25-boot1.
4. **Report.** `local/research/P10/REPORT.md`: re-derivation,
   blast-radius table, winner + diff, BEFORE/AFTER, ladder table,
   exact commands, receipt paths, "What I could not do". Commit,
   remove the lease if held, stop.
