# N8D7M12 Part 5C — same-APK replay control design (read-only)

You are the N-lane OpenCode Go design worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M12 Part 1 and Parts 5A/5B REPORTs/gates, N8D7M11 §6, and the actual shared replay core/main/selected-census source at private fork `a608ed1`. Read the pinned Mac and Odin `parallel.hashes` and bounded PID logs; cite source/receipts, not web. Own only this brief and `local/research/N8D7M12P5C/`. No source/script edit, build, replay, device/lease action, stream/APK/env change, push, board/global edit or upstream contact. Time box 25 min, text <512 KiB.

Question: what is the smallest **same-APK, same-stream** Odin control that tests whether selected/oracle/tile instrumentation perturbed the sparse ON replay? Design one OFF run using the already-gated APK `caa11102…f512`, stream `f6a78f71…a593`, parallel backend/Turnip, same 2050 PPM and 41 sampled hash rows. Keep `PS2X_GS_REPLAY_ONDEVICE=1`; disable only the selected/oracle/tile capture flags, leaving replay parsing/backend/step/PPM settings fixed. Confirm from source exactly which flags affect the `GB4_FRAME`/`GB4_REPLAY` path, and whether an OFF run can still produce a 512×448 PPM plus output hashes without the numeric census. Predeclare what would count as an instrumentation effect vs persistent device/backend divergence, using exact PPM/hash fields and broad/sparse visual observation. The ON selected 14/448 and Mac 300/448 numbers cannot be measured in OFF mode if its census is disabled; do not invent them. Note the ON comparison has priv equal41/41, VRAM equal3/41, present equal0/41, first divergence tick50, and the viewed mostly-black Odin PPM.

Also specify the Mac **same-binary** control with the exact Part 1 desktop replay binary: same stream/backend/flags ON then OFF, comparing 41-row hash/PPM outputs, or prove from existing receipts that one side was already run. Budget future actions explicitly: one Mac OFF replay and one Odin OFF install/launch at most, each separate gates, Odin battery/lock/lease/install/force-stop/env restore and ≤600 s active wall, no simultaneous host heavy job. Identify any source limitation that makes the control nondiscriminating, and stop if so. No control run is released from this design alone.

Deliver `REPORT.md` with source/receipt citation table, ON/OFF variable matrix, predicted outcomes, exact unexecuted command/script-diff plan and acceptance/stop rows, plus `check.py` that verifies pinned inputs and existing hash-row comparison but states it cannot prove OFF behavior. A = discriminating bounded control plan; B = flag/output path prevents comparison; OTHER = pin mismatch. Commit `[N8D7M12] Part 5C` with `Orchestrated-By: opencode`, explicit named paths, no push. Hand back the table, not a graphics root-cause verdict.
