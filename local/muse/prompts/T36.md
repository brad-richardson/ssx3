# T36 — First steering nudge in the live Snow Jam race: HUD effect (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T35/REPORT.md` first (all of it: R1
reached the pre-race panel via ENTER Cross on My Rules @T+326.78, then ONE
537.9 ms Cross (X Continue) @T+432.05 → countdown `2` at +0.4 s → live
gameplay from +4 s → race at 84%, 4TH/6, 00:02:52, 4330 pts by +130 s with
ZERO further input (race clock ~1.3× wall under turbo); G1 proposes the
first steering input OR a finish capture — this brief takes the STEERING
option). This brief executes T35's G1 (steering half). Reuse T35's scripts
(`t35-auto.sh`, `t35-cropdiff.py`, `t35-analyze.sh`, `t35-vcount.sh` — copy,
do not modify the committed originals). REAL filenames only (T28 G9 lesson):
T35's arrival snaps are `t35r1-x-post{1,3,8,15,25,40}.jpg` +
`t35r1-x-stab{1…6}.jpg`.

## Facts you start from

- Bytesize WSL2 over ssh/scp ONLY (no lease of any kind). Reuse the
  bit-identical T4 build + inputs T35 verified (re-verify sha/size/rev/
  status/pad-binding — INCLUDING the analog-stick/D-pad bindings you will
  use — + NVM state before running; table the receipts).
- GATE NOTE (T35 G1 — gate on the HUD, not the frame): live gameplay is
  never static (all 66 X pairs 7.97–20.23). The pre-press criterion is a
  LIVE-RACE gate: race clock advancing + position/progress HUD present
  (read off viewed snaps, not whole-frame scores). The panel→countdown hop
  (~14.18) still fires DEPARTED normally for the X transition itself.
- The task: reproduce the T35 chain (fresh boot → Cross → detector-TITLE →
  Start → menu-like gate → Cross on Single Event → SC-LIKE gate → Cross on
  Zoe → ZC-LIKE gate → Cross on Continue → SP-LIKE gate → Cross on Peak 1
  → SM-LIKE gate → Cross on Race → SE-LIKE gate → Cross on Snow Jam →
  MR-LIKE gate → Cross on Continue → PP-LIKE settled-panel gate → Cross on
  X Continue → ≤3 attempts) to LIVE gameplay (race clock running, rider
  moving), then apply ONE bounded steering nudge (single D-pad tap OR one
  short analog deflection — pick ONE, table the binding + duration first)
  and table EVERYTHING after: pre-nudge HUD (clock/position/progress/speed/
  score, read off viewed snaps), the nudge (control, direction, duration,
  T+ time), and the post-nudge HUD delta over a bounded tail (did heading/
  position/speed/score change? snaps + HUD reads per hop).
- If the nudge shows NO measurable HUD effect (deadzone, wrong control,
  needs longer hold): table the exact observed (non-)behavior (snaps + HUD
  reads + waits) and STOP with a recipe — one input variant per attempt,
  never blind multi-presses. ONE bounded variant allowed only if the nudge
  provably never acted on live gameplay (pre-nudge ≠ racing).
- Success bars (tabled, not verdicts): live race reproduced (HUD advancing,
  rider moving); nudge acted on it (pre-nudge = racing with HUD read);
  post-nudge HUD delta table (or exact non-effect characterization);
  full input log + trace sha.

## Gates and rules

- NO lease (bytesize needs none). WSL precautions stand (T35 G2: single-shot
  scripts, runs ≤6 min (T35 R1 ran 575 s — keep added scoring tight or table
  the overrun with full dmesg coverage), preserve emulogs first, dmesg +
  btime + init age, UNFILTERED dmesg + event log around flaps, 534 ms holds
  NOT bisected, `;` chains only INSIDE one `wsl` call, one `wsl` call per
  ssh; post-run dmesg read IMMEDIATELY after the run ssh returns). COUNT
  AcceptAsync lines in the committed dmesg files with exact counts +
  uptime-stamp positions relative to the run window (T35 shape). Runs in
  flap windows must be effects-verified or re-run (T27 §4 rule).
- `export COPYFILE_DISABLE=1` on every SSD step. Full-size traces stay on
  the SSD by path+sha (table bytes + sha; commit head/tail slices only).
- Evidence dir `local/research/T36/` (STANDALONE). Commit with `git add -f`,
  prefix `[T36]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — live race + ONE steering nudge (bounded: ≤3 attempts, 1200 s wall cap)

1. Pre-run checks (T35 §T35-0 shape): build/input receipts (INCLUDING
   steering bindings), NVM state, T35 reference snaps present, free space
   both ends.
2. Per attempt table: race reproduction (chain scores + live-race gate:
   HUD advancing), nudge row (control/direction/duration/T+/pre-post HUD),
   post-nudge HUD delta series (HUD reads per hop) or exact non-effect
   behavior.
3. Trace bytes + sha per run; WSL event table with exact AcceptAsync counts
   + uptime positions; NVM state at end.

## Report

`local/research/T36/REPORT.md`: pre-run receipts, attempt tables, nudge
log, HUD delta (or non-effect characterization + recipe), WSL table, exact
commands, gaps. Commit evidence (`[T36]`), stop.
