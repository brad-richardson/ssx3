# N8D7J — selected-input raw VRAM discriminator (DeepSeek Flash Go)

You are an OpenCode Go N-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, N8D7I's `REPORT.md` and `ORCH-GATE.md`, N8D7G gate, N8D7F report/source patch, and the N8D7J item in `docs/todo.md`. Work read-only in fork/G43/Android sources. Write only `local/research/N8D7J/{REPORT.md,source-map.tsv}` and this brief; no source edits, build, boot, device, web claims, upstream contact or push. Cite the repo and use LSP definition/references to confirm how code connects rather than guessing. Keep context under 35k.

Question: N8D7I's same-Odin-frame selected input, circuit1 and GPU-stage vectors were equal and sparse at 35/448 active tiles, while a **different GS stream** on Mac had 300/448. What one same-Odin-run observation would distinguish (A) sparse bytes already in the selected 4 MiB raw VRAM input from (B) loss while converting those bytes into the selected 512×224 image? Do not compare Mac/Odin streams as byte-identical. Trace the actual data path from the fork's selected capture request through G43 buffer copy/readback, CPU `vram_readback`/`swizzle_PS2`, tile census and circuit1/stage taps. Confirm ordering relative to the selected display and promotion; flag any code path where the proposed observation cannot share the tick2050 frame.

Return an 8–12-row source map with columns `stage,file_line,object_bytes,producer_consumer,clock_or_order,observable,limit`. Each row needs an exact source line or explicit `unknown`; independently identify the earliest host-visible raw VRAM bytes, any GPU/CPU copies and their completion barrier, swizzle/PSM/FBW/phase/stride handling, and what N8D7I's "input" count actually measures. Give a preregistered A/B/OTHER table: raw-byte/correct-address evidence needed, byte cap (<6 MiB capture), one-run stop conditions, and a null control. A raw byte occupancy tally alone is not a decoded-pixel census: explain a safe exact pixel test using the existing synthetic fixture and list its shared-code limitation. If the current code cannot expose an independent raw-byte observation under these caps, stop with the exact missing hook and no proposed run.

Budget 12 minutes, source reads only, <=128 KiB receipts. Pin fork/G43 commit or dirty patch state and named source lines. Hand back the table and a recommended **next observable**, not a GS cause verdict. Commit only REPORT, TSV and brief `[N8D7J]` with `Orchestrated-By: opencode`, no push.
