# E23 — Demand-cadence reference from the complete-feed path (R3-shape); name the trigger + bound by comparison

You are the ssx3 E-lane worker (Claude Code) in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** E22 closed the observation gap (ONE parse call +
66.896 s silence in the title window), eliminated the AddBs-wake fix
(C1) on evidence, and sharpened X to a mechanism (C2a/C4 reachable)
plus a missing rule (trigger point + loop bound). E22's NEXT-BRIEF
states the rule "needs a reference this lane cannot produce" — this
brief produces the closest in-lane reference: the demand cadence where
bytes SUFFICE.

Execute this brief as specified. The design below is settled by cited
evidence — if a step's rationale seems unclear, re-read the cited
receipt rather than re-litigating the approach. Table genuine blocks
with their receipts; do not stall on doubt.

## Read first (all of each)

1. `local/research/E22/REPORT.md` + `local/research/E22/NEXT-BRIEF.md`
   (the sharpened X, the two unknowns, eliminated candidates — BINDING;
   do not re-propose C1 or any closed candidate).
2. `local/research/E21/REPORT.md` + `local/research/E18/NEXT-BRIEF.md`
   (Q1/Q2/Q3 + ABI constraints — BINDING).
3. E22's dir is READ-ONLY reference — your dir is
   `local/research/E23/`. Reuse the proven dylib by copy + re-sha
   (`e4d88fdc…b38e6be`, 52,472 B — never rebuild; E21 spellings carry).

## Mission

1. Checkpoint re-verify (fork triple-agree `3adc0478`, 9,457-name audit,
   suite 458/458 UNLOADED + observer-LOADED, E16 closure, E15 rc0s, E18
   R1–R6). Any red → table + stop.
2. Mine the COMPLETE-feed demand cadence (no boot): on the R3-shape
   authored path (240 B → parsed240/packets3/frames2, frames SERVED),
   table per `GetPicture`: how many times the type-1 producer fires
   (callbacks/AddBs/completions), in what order on which threads/ticks,
   and what ends the loop (frame availability? queue drain? explicit
   end?). Use fixture re-runs (observer-loaded, E21 receipt discipline)
   + committed E18 R1–R6 evidence. This is the reference the title
   stall lacks.
3. Compare against the title stall (e21a/e22a joins: 1 callback, 1
   AddBs, 0 completions, park): derive trigger-point + loop-bound
   candidates FOR THE COMPLETE PATH FIRST (where the loop demonstrably
   terminates), then table which — if any — transfer to the incomplete
   title feed without invention. Anything requiring a guess about
   firmware behavior is tabled as ASSUMPTION, not adopted.
4. Name the ONE title observation (if any) that would confirm or kill
   the transfer: the exact boot shape, what it measures, and the
   decision it enables. Execute it (ONE guarded boot: fresh T13
   pre-claims, P-lane lease — occupied → table and stop — wall/progress/
   byte caps, runner exec'd DIRECTLY, assertion-fail on `stdbuf`) ONLY
   if it measures rather than assumes; otherwise table the design and
   STOP with no boot (a no-boot close is a valid outcome — do not burn
   the boot to look busy).
5. Fix gate (strict, unchanged): implement ONLY if the diagnosis isolates
   ONE edge with a minimal ABI-preserving change + its own fail-before +
   full regression. Policy/decoder/EOF invention excluded; no fabricated
   frames; "Guest never sends more without X" → NAME X and STOP.

## Rules

- ONE boot max (step 4 only, if justified); ONE lease claim (atomic,
  clean release, post-release `pgrep` check).
- No CSV/regen (regen need = stop). No fork behavior commits except
  through the fix gate (fail-before + full regression + push, never
  force-push).
- Byte caps declared up front + tracked (SSD lane ≤16 GB in NEW `e23-*`
  paths; internal ≤3 GB delta; 0 growth elsewhere); `COPYFILE_DISABLE=1`
  on SSD steps; large artifacts off the internal volume.
- Evidence dir `local/research/E23/` (STANDALONE). Commit with `git add
  -f`, prefix `[E23]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 8 h. Report in chunks + tail
  receipt (truncated tail fails the gate). Fill in the ACTUAL time used.
