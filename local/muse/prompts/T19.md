# T19 — G1 second input: confirm English at User Prefs, capture the next epoch (bytesize, no lease)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T17/REPORT.md` first (all of it —
T17 gap G1 = this brief, the exact proposal: boot fresh to the
language park, single 534 ms K hold → User Prefs, then a second
single 534 ms K hold to confirm English; screenshot-verify the
next screen), plus T17's `t17c-post*.jpg` (the User Prefs park)
and T17 gaps G2–G5 (WSL flap precautions: single-shot scripts,
runs ≤6 min, preserve emulogs first, re-check dmesg; wevtutil-TZ
+4 h; hold threshold 18 ms–534 ms unmapped — reuse 534 ms, do
NOT bisect on this run; caption blink — perceptual/region
compare, never whole-file sha). The bytesize track continues
while useful.

## Facts you start from

- The box: `bytesize` over Tailscale — `ssh bytesize` (key
  auth). Devel PCSX2 v2.9.75 at `/home/brad/pcsx2-t4/pcsx2/
  build/bin/pcsx2-qt` (reuse, do NOT rebuild — table binary
  sha + size at start, must reproduce `6719f5d6…` /
  130929856 B or table the deviation). Inputs staged.
  Display: Xvfb `:99` + `QT_QPA_PLATFORM=xcb` (T17 recipe).
  Keys: xdotool focus+XTEST to the PCSX2-Qt WID (T17 path 1;
  `t17-press.sh` `--window` fallback if focus fails).
- The run: fresh boot → language park (verify: snap sha
  `b6ca1aa9…` + ExecPS2 @142369/142465) → single 534 ms K
  hold → User Prefs (verify: region-match vs `t17c-post15.
  jpg`, NOT whole-sha — caption blinks) → single 534 ms K
  hold (confirm English) → capture what follows: screenshot
  series (press-relative +5/+15/+30/+60/+120/+240 like T17c)
  + continuous same-4-channel trace. Stop at the first new
  stable park OR 1200 s wall, whichever first (still a
  complete result: table epochs + inputs + trace).
- Single inputs only: one hold per screen, never blind
  multi-presses. If the 2nd hold does nothing (like the
  18 ms tap taught us timing matters), table exactly that
  (snaps + trace markers) — do NOT improvise further
  presses beyond ONE retry with a longer hold (≤1000 ms,
  tabled).
- NO lease of any kind (all boots on bytesize). No `adb`.
  No fork changes. No copyrighted downloads (inputs exist).

## Gates and rules

- Work happens on bytesize; evidence comes home. Record
  every remote path, command, version, and sha needed to
  reproduce the input run + capture from scratch.
- Evidence dir `local/research/T19/` (STANDALONE). Commit with
  `git add -f`, prefix `[T19]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 6 h. Tables + receipts, no verdicts. If input
  automation regresses (no key path works this session),
  table exactly what was tried, commit what exists, stop.

## Task 1 — park chain (do both parks reproduce?)

1. Park-1 table: fresh boot reproduces the language park?
   (snap sha + ExecPS2 lines + vblank count — match/no
   vs T17's numbers).
2. Press-1 table: single 534 ms K hold reproduces User
   Prefs? (region-match vs t17c snaps + trace markers —
   match/no).
3. Press-2 table: the confirm-English hold (method, timing,
   single-press proof — one down/up pair, exit 0s).

## Task 2 — next-epoch capture (what follows confirm?)

1. Epoch table: screens reached after press-2 (guest events,
   log times, screenshot per screen — or the press does
   nothing: table that exactly + the ≤1000 ms retry row).
2. Trace table: channels captured (same-4 format check vs T4
   samples), lines/bytes, paths + shas both ends (traces
   stay on bytesize if >1 GB, with head/tail samples
   committed).
3. Park table: first new stable park (screen + awaiting-what
   + vblank count — or NONE to 1200 s cap: bound tabled).

## Report

`local/research/T19/REPORT.md`: T4-0..T4-4 SHAPE (park
chain, presses, epochs, trace inventory, exact commands
both ends, gaps — incl. next-input proposal if deeper
epochs remain). Commit evidence (`[T19]`, no push), stop.
