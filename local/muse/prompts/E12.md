# E12 — Restore the first branch-changing card predicate 0x2c5300 (single entry): fail-before, regen, truth table, one guarded boot

You are the frontier worker in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E11/REPORT.md` and
`local/research/E11/NEXT-BRIEF.md` first (all of both: `0x2c5140` restored
— 321 actual call/return pairs, GetInfo/Sync complete, UI pending clear,
packet→GS→D→Present join complete with a new "No memory card (PS2)
inserted" message; outcome (ii) — first remaining gap is the missing
`0x2c5300` entry (raw line 5119: source `0x242150`→`0x2c5300`, a0=MC,
vtable `0x486f78`, a1=0, stale v0, policy 1; guest predicate
`status[port] == -10002`, observed status 0 requires v0=0; caller tests
stale nonzero at `0x242158` implying branch to `0x2421a8` + UI+0x344 clear
— code-derived, needs direct observation; `0x2c5358` stays OUT — 113
zero-status calls all require nonzero, matching branch sense). This brief
renews the bounded regeneration exception for the `0x2c5300` entry ONLY,
per E11's NEXT-BRIEF (which is now AUTHORIZED — execute it, don't re-propose
it). Reuse E11's scripts (copy to `e12_*`, do not modify the committed
originals).

## Facts you start from

- COORDINATION: the I-lane converged on the SAME gap — I15's new wall is
  `0x242150 → 0x2c5300`, and a concurrent I16 brief closes it device-side
  (porting YOUR row if you land first, else writing its own local row).
  Neither lane blocks on the other; the fork row is the canonical one.
  Your row: `sub_002C5300,0x2c5300,0x2c5320,0x20` after owner
  `sub_002C52D8,0x2c52d8,0x2c5320,0x48`, before `sub_002C5320` — preserve
  the owner, keep `0x2c5358` OUT.
- Baseline: fork `ffdf58c` (pushed); active checkpoint `/tmp/p1-link/runtime`
  (PROTECT it + DerivedData — never reclaim); registry
  `68217e9a…`; runner `c39e59a7…`; suite 452/452; query four-case test
  passes; DROP handler is guest code. Re-verify checkpoint identity first
  (E11 §2 shape) — do NOT repeat the `0x2c5140` generation.
- The task, in strict order: (1) checkpoint re-verify + fail-before
  (relink fixture against baseline runner objects: `hasFunction(0x2c5300)=
  false`, expected-present test fails); (2) FRESH admission sample +
  persist BEFORE any CSV mutation (≥1 GiB logical + 2 GiB floor + 256 MiB
  guard; do NOT assume E11's final free space persists — reclaim ONLY
  retired trees with per-tree ownership proof if short, E11 §3 shape);
  recheck immediately before spawn; (3) insert the row, regenerate via the
  APFS path, install changed bytes only; (4) actual-binding predicate truth
  table (ports 0 AND 1: selected `-10002→1`, `0/1/-10001/-7→0`, cross-port
  indexing proof) + preservation checks + regression (E11's busy-query
  four cases, DROP receipt, full suite); (5) ONE guarded verification boot
  (fresh T13 pre-claims, lease, REPORT_ALL=1, E7/E4, wall/progress/byte
  caps) with the NEXT-BRIEF probes (dynamic MC/UI guards, `0x2c5300`
  call/return capture, UI+0x344 store PCs, query/request join, bounded
  `0x2c5358` retention, display join).
- Success bars (tabled, not verdicts): checkpoint identity re-verified;
  fail-before recorded; admission passes on fresh samples (ordering
  repair honored); entry generated with exact-PC binding; truth table
  executes (all ports/cases); regression green; one boot shows the caller
  taking the correct path (or the first remaining wall — tabled, not
  fixed).

## Gates and rules

- Renewed bounded exception: `0x2c5300` entry ONLY (one map row + its
  generated entry). `0x2c5358` or any second gap → table, do not close.
  Standing no-regen resumes at close. Fork discipline: named-files commits
  only, generated sources NEVER staged, fork-remote push only.
- Ordering repair (E10 lesson): admission check + persist BEFORE csv edit;
  recheck immediately before generator spawn. Never lower a declared guard
  after a rejection.
- Reclamation safety (E11 shape): per-tree ownership proof BEFORE removal
  (lane-board check + no live file handles); oldest retired first; stop
  once admission passes + 512 MiB headroom; never touch `/tmp/p1-link`,
  DerivedData, or any live lane path. `export COPYFILE_DISABLE=1` on SSD
  steps; allocated-byte caps declared + tracked (ExFAT).
- Boots: ≤1 guarded boot under the shared P-lane lease (fresh T13 pre-claim
  checks, wall/progress/byte caps, REPORT_ALL=1, E7/E4 taps). No boot without
  the lease; never wait on another lane's lease — table and stop.
- Evidence dir `local/research/E12/` (STANDALONE). Commit with `git add -f`,
  prefix `[E12]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 8 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — checkpoint re-verify + fail-before + fresh admission (no regen)

1. Checkpoint identity: registry/runner SHAs, name audit vs
   `after/generated.json`, suite re-run, busy-query four-case re-run.
   Fail-before: relink fixture vs baseline objects, `hasFunction(0x2c5300)=
   false`, expected-present fails. Table every check.
2. Fresh admission sample + persist (BEFORE csv edit). If short: reclaim
   retired trees oldest-first with ownership proofs until admission + 512
   MiB headroom. If STILL short: STOP with outcome (iii) + the exact next
   target — no csv mutation, no regen.

## Task 2 — row insert + regen + install + truth table (no boot)

1. Insert the row (expect the E11-predicted CSV sha — table it). Recheck
   admission immediately before spawning; generate; install changed bytes
   only (AppleDouble purge, proven-created only).
2. Relink fixture against NEW runner objects; run the predicate truth table
   (both ports, all cases + indexing proof) + preservation checks +
   regression (busy-query four, DROP, suite). Table every receipt.

## Task 3 — one guarded verification boot

1. Fresh T13 pre-claims, lease, caps, REPORT_ALL=1. Capture the NEXT-BRIEF
   probes (dynamic MC/UI, `0x2c5300` calls/returns, UI+0x344 stores,
   query/request join, `0x2c5358` retention, display join) or the first
   remaining wall. Table the joins; no promotion without an actual
   request/completion dependency.

## Report

`local/research/E12/REPORT.md`: contract, checkpoint re-verify + fail-before,
admission record, regen + truth-table + regression tables, boot joins (or
wall), exact commands, gaps. Commit evidence (`[E12]`), stop.
