# T60 — PCSX2: who sets mode 6 in the render-state template before the rider's list append

You are muse in a herdr panel (same rules as T59). **Tables + receipts; the
orchestrator decides.** Read first: `local/research/T59/REPORT.md`.

## Why (orchestrator codegen read)

`sub_00376938` @`0x379784–0x379820` appends one item to a render list:
- `a3 = *(s2+0x18F0)` is the list header (count word). The list sits at
  `0x8095f0`; items start at +0x80, stride 0x80, and are written through
  `|0x30000000`.
- `t0 = *(s2+0xE84)` is the current 20-byte render-state **template**. The
  item copies template words 0–4, then `+0x14 = s4`, `+0x18 = t1` (a packet
  from `func_38F668`), `+0x1C = a0`.

So item w0 (`0x1b0` = mode 6 here; `0x30` = mode 0 in the recomp) is
template w0 at append time. We need the code that puts mode 6 into the
template. E44 Part 4 runs the identical trace on the recomp; use the same
line formats.

## Change (on T59's tree)

1. At every EE execution of pc `0x3797ec`: `app vsync count=<*a3> t0=0x<>
   tw0..tw4 s4 t1 ra=<$ra of sub_00376938's caller>`, first 300.
2. A write watch (with T59's fixed fold) on the template words at `t0`. Arm
   it at the first `app`, and re-arm if `t0` changes. Log only writes that
   change tw0 or tw1: `tpl vsync addr old new pc ra a0..a3 v0 v1 s0..s7`,
   first 300.

## Run

One build, G13 preservation, then **one capture: `t58-pre-sc-state` → K →
SC settled** (T59's cap script).

## Deliverable

≤1 build (+1 on compile failure), ≤2 captures, 2 h, cap 2 GB.
`local/research/T60/REPORT.md`:
- the `app` table: first 40 rows, with counts;
- the `tpl` change sequence before the first count=0 appends, with the
  setter pc/ra for each mode value;
- preservation.

No verdict. `[T60]` commit (`git add -f local/research/T60
local/muse/prompts/T60.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
