# M10 — Discriminating instruments: hit/draw order, full matrices, render targets

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M9/REPORT.md` (all of it: the narrowed residue — 21 Tex2 +
800 Pos draws consume perturbed snapshots with 0 pixels differing — and
"What I could not do" items 1, 3–6, which specify the instruments this
brief builds). Base header `local/research/M9/m9_replay_context.h` (sha in
the M9 report — record `shasum`, not memory; copy it to
`local/research/M10/m10_replay_context.h`, keep every earlier mechanism).
Evidence dir `local/research/M10/` (commit with `git add -f`, prefix
`[M10]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m10/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M10/` as a research header derived from M9's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M10\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M10/waits.log`. A P1q agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M9/build_replay_player.py` to
  `local/research/M10/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M9 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m10-det`, `m10-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M9-header copy, run `m10-det`
   once with the M9 slot-0 delta env, confirm M9's end state reproduces
   (74 hits/replay, 0/200 differ, guest/event clean). Control for later.
2. **Order + values.** Build the stream-position stamps (covering loads
   vs consuming draws in one walk) and the 12-word matrix capture at
   the first hit. Run on the slot-0 delta. Receipts: hit/draw order
   table (does the perturbation land before its consuming draws?),
   full-matrix values (is the delta word's +0.1 live at draw time with
   sane siblings?), and the standing of the "loads trail draws"
   variant — confirmed or excluded with the table.
3. **Render-target + occlusion.** Build per-draw render-target epochs
   (which draws rasterize into the compared XFB range vs the 2–6 EFB
   copies) and occlusion queries for the consuming draws. Receipts:
   per-class table (consuming draws in-range vs out-of-range vs
   occluded/culled) and the standing of the visibility variant.
4. **Verdict-free synthesis.** Combine Steps 2–3 into the mechanism
   table: each M9 residue row (order, values, visibility, texcoord
   stage) confirmed/excluded/open with its receipt. If the mechanism
   is fully determined, state the single-sentence determination as a
   tabled finding (not a verdict) + the exact experiment that would
   flip it.
5. **Report.** `local/research/M10/REPORT.md`: per-step tables
   (order, matrices, render targets, mechanism synthesis), wall rows,
   continuation, header diffs per step (`diff -u` against the M9 header),
   exact commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M10/` (header, driver copy, analyzer
   changes, REPORT, waits.log). Stop.
