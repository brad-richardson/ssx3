# E3 — Bounded read-only SPR/DMA/host-write audit (no boots; boots parked behind K1)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read first: `local/research/T26/REPORT.md` (watch semantics:
host memcpy/DMA/SIF blits BYPASS the watch — T26-0 receipt row 6, P26-1h —
so T26's 0 guest writes cannot exclude SIF/CD or establish immutable flags)
+ `local/research/A0/REPORT.md` §(ii-d) (the 20 B block at `0x501420`) +
fork `ps2xRuntime/src/ps2_memory.cpp:1595-1598` (SPR copied via raw memcpy —
the shape of what the watch cannot see). Frontier steering this poll: E3
BOOTS are parked behind K1; this brief is the bounded read-only audit, which
may proceed now.

## Facts you start from

- The question: which HOST-SIDE write paths can touch `0x501420..0x501437`
  or the s1 flag ranges (`0x70000000` ramp / `0x70001C00` steady) WITHOUT
  firing the guest watch? Candidates: SPR copies (raw memcpy class),
  DMA/SIF blits, scratchpad clears, any host memset/memcpy onto those ranges.
- For EACH path table: code site (file+line) + trigger conditions + whether
  it can fire during the park window (static/call-graph evidence only) +
  what observable would distinguish it.
- Then the exclusion table: what T26's "0 guest writes" CAN exclude (guest
  stores during the window) vs CANNOT (every host path above, each named).
- NO boots. NO lease. NO emulator runs. NO fork changes. Static source reads
  + committed streams only. If a question needs a boot, table it as an OPEN
  row for the parked follow-up — do not run it.

## Gates and rules

- `export COPYFILE_DISABLE=1` on every SSD step (reads only; no writes).
- Evidence dir `local/research/E3/` (STANDALONE). Commit with `git add -f`,
  prefix `[E3]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Time box: 4 h. Tables, no verdicts.
- Hygiene: write the report in chunks with a tail receipt (truncated tail
  fails the gate).

## Task 1 — host-write path audit

1. Enumerate every host-side write path onto `0x501420..0x501437` and the s1
   flag ranges: SPR copies, DMA/SIF delivery, scratchpad clears, host
   memset/memcpy. Table site + trigger + park-window reachability each.
2. Exclusion table: what T26's zero CAN vs CANNOT exclude, path by path.

## Task 2 — parked follow-up recipe (define, do not run)

1. Per frontier steering: IF the same park remains after K1, the follow-up
   records ONE or TWO guest frames — sequence/thread/frame, s1, flag values
   immediately before reads, intervening SPR transfers/writes — NOT another
   long census. Table the EXACT capture recipe (hooks, triggers, caps, binds).
2. Table what that capture would settle that this audit cannot.

## Report

`local/research/E3/REPORT.md`: path table, exclusion table, follow-up recipe,
exact commands (reads + greps), gaps. Commit evidence (`[E3]`), stop.
