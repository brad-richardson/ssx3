# E39 — Which VIF1 MPG uploads the recomp drops (and fix the drop): the code PCSX2 has at VU1 slots 2/8 never arrives

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini).
**Tables + receipts; recommend, the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/E38/REPORT.md`,
`local/research/T49/REPORT.md` §T49-5. You own `~/dev/PS2Recomp` (`ssx3` @
`7f022ed`) and the P-lane lease. FF pushes to fork `ssx3` allowed after the
runner-dir check.

## Facts (orchestrator)

- The recomp executes exactly what its uploads carry: our first `MPG addr=0`
  payload (`vu1-entry-e38a2.txt` L1055) has `81d26b7c` at slot 2 = the `LQI`
  we run at 0x10. PCSX2 runs `B 0x258` (`lo=40000048`) at 0x10 and `B 0x7d8`
  (`400000f2`) at 0x40. None of our 16 MPG heads in the 0x12B8 packet carries
  those words, so in PCSX2 slots 2/8 come from **another MPG we don't apply**.
- `ps2_vif1_interpreter.cpp` MPG (~593–625), three silent drop paths vs PCSX2
  (`vifCode_MPG`: `addr = (code << 3) & 0x3fff` for VU1, per-instruction wrap,
  payload continues across transfers):
  (a) `destAddr = imm*8` with `destAddr < PS2_VU1_CODE_SIZE` required: any
      `imm >= 2048` is dropped (PCSX2 wraps);
  (b) `pos + copyBytes <= sizeBytes` required: a payload that runs past the
      end of this `processVIF1Data` buffer is dropped entirely (no carry-over),
      and the rest of the stream is misparsed;
  (c) `destAddr + bytes > size` clips instead of wrapping.
  The E37/E38 trace also prints `imm & 0x1FF`, so our logs hide (a).

## Steps

1. Default-off `PS2X_VIF_MPG_LOG=<file>` (+`_FROM`/`_TO` vsync, cap 20k
   lines): per MPG: vsync, full `imm`, `num`, dest byte range, **outcome**
   (`copied` / `drop_addr` (a) / `drop_partial` (b) / `clipped` (c)), bytes
   available, payload FNV, and the lower/upper words it writes (or would
   write) to slots 2 and 8 when covered. Also fix the trace's `imm & 0x1FF`
   display to the full imm.
2. **Boot A** (E33 vsync route, cap 300 s), MPG log over vsyncs 0–1400
   (menus through Select Character settled). Table: outcomes by count; every
   non-`copied` MPG with its vsync/imm/num; any MPG (copied or not) that
   carries `40000048` for slot 2 or `400000f2` for slot 8.
3. **One fix** if a drop path fires: match PCSX2 (mask dest `& 0x3FFF`,
   per-instruction wrap; carry a partial MPG/UNPACK payload across
   `processVIF1Data` calls with interpreter state, if (b) fires). Unit tests
   per path. Suite green, flags unset.
4. **Boot A'** (same route + `PS2X_GFX_STATS` 1200–1500 +
   `PS2X_VU1_ENTRY_TRACE_PCS=0x40,0x10 _VSYNC=1300`): first pair at 0x10 /
   0x40 (expect `B`), `vu_exhausted`, XGKICKs, Select Character frame by eye +
   SHA (**Zoe?**). If Zoe renders: **Boot B** into the race (cap 800 s,
   orchestrator OK), race frame by eye + SHA (3D world?).
5. Flags-unset boot matches E32 boot (a).

If no drop path fires in step 2, stop after the table (no fix).

## Budgets, deliverable

Builds as needed, ≤4 boots (+1 retry), 4 h, internal cap 5 GB beyond
existing dirs. `local/research/E39/REPORT.md` with the tables, fix diff
summary, tests, suite, boots, frames (paths + SHAs, what you see). `[E39]`
commit (`git add -f local/research/E39 local/muse/prompts/E39.md`, trailer
`Orchestrated-By: Muse Code`, `git log -1` first); ff-push the fork after
the runner-dir check.
