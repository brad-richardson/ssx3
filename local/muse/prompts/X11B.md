# X11B — same dense Qwen scheduler table, lower thinking budget

You are a local opencode/Qwen **dense** worker in `~/dev/ssx3`, launched
with `thinking_budget=512`. Follow `~/dev/AGENTS.md` worker rules. This
repeats X11 with the same pinned input and acceptance, changing only the
model's thinking budget. It is a model/tool trial, not an E55 verdict.
Write only `local/research/X11B/REPORT.md`; no source edit, build, boot,
device, web, push, directory search or other source read. Cite the repo
excerpt, not the web. The named report path is inside your write scope.

Read exactly this brief, `local/research/X11/source-excerpts.txt` (fork
`4f93216`, 7,345 bytes), and the X11B report skeleton. Do not read the
X11 report or E55B1 report. Fill the same four rows: due membership,
within-batch order, idle timer/event selection, and the guest-cycle
timestamp question for posted events. Give exact excerpt line citations,
distinguish host batch gating from inside-batch sorting, and state gaps.
Do **one** LSP `findReferences` probe for `processDueDeadlines` at its
definition in
`~/dev/ssx3-work/E55A2/PS2Recomp/ps2xRuntime/src/lib/Kernel/EeScheduler.cpp`.
Record usable locations or the exact tool gap; do not infer a call from
an empty result. The excerpt alone supports the four semantic rows.

Write the table before the LSP call, within 3 minutes. Run
`python3 local/tooling/orch/check_x11.py local/research/X11B/REPORT.md`
and correct at most once. Commit only the report with explicit path,
subject `[X11B]`, trailer `Orchestrated-By: opencode`; no push. Hand
back the table and short context/tool note, no E55 verdict.

Budget: 8 minutes, ≤24k model context, ≤1 MiB report, one LSP probe.
At cap, stop and state the gap. Fix a typo in your own exploratory
command once; that is not a failed brief step.
