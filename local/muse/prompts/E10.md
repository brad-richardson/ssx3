# E10 — Complete the DROP preflight on APFS scratch, restore the 0x2c5140 query entry (frontier execution)

You are the frontier agent (Codex Astra) in a herdr panel in
`/Users/bradrichardson/dev/ssx3`, continuing as E-lane lead. **Tables +
hypothesis + next-action recommendation, no verdicts.** Read first:
`local/research/E9/REPORT.md` (all of it: outcome (iii) — DROP applied +
pushed (`be0c9ee`), DROP-only regen hit the allocated-output cap on ExFAT
(1 MiB/file ×2 with sidecars), stopped before binding emission; active
generated code/binaries unchanged; 0 boots) + `local/research/E9/
NEXT-BRIEF.md` (the exact recipe this brief executes: APFS scratch repair,
DROP-only output, installation, DROP behavior, query repair, regression,
verification boot). Baseline: fork `be0c9ee`.

## Facts you start from

- REGEN IS AUTHORIZED FOR THIS BRIEF (renewed bounded authorization): E9's
  exception closed with its report; this brief re-opens it ONLY for the
  NEXT-BRIEF scope below. Standing no-regen resumes the moment this brief
  closes.
- The failure was storage placement, not translation: ExFAT's 1 MiB minimum
  allocation ×2 (sources + AppleDouble sidecars) blew the 12 GiB output cap
  at 61 s. The repair: generate the UNCHANGED DROP inputs into an APFS
  scratch directory (`/tmp/ssx3-e9-retry-codegen`, owned, fresh budget:
  ≥1 GiB logical reservation + ≥2 GiB internal free-space floor, recorded
  with polling headroom; stop if unmet — never reuse the undersized ExFAT
  cap). Internal APFS use is authorized for this scratch ONLY (bounded,
  cleaned after; standing large-artifact rule otherwise holds).
- Phase 1 — complete the preflight: normal generator exit + complete
  registry/header/body manifests; `0x426230` binds the emitted guest
  handler (never the TODO stub — header presence alone insufficient);
  installation = compare all emitted names vs the active mirror,
  disposition leftovers, copy only changed generated bytes without
  metadata, before/after manifests, never stage generated runner sources.
  DROP behavior: rebuild `-j4`, suite green, relink the binding test
  against the actual new runner objects (empty-buffer handler returns to
  supplied RA, unchanged SP, preserved s0; query still absent at this
  checkpoint). No boot yet.
- Phase 2 — query repair + regression: revalidate ELF `0x2c5140..0x2c5168`;
  add sanctioned CSV row `sub_002C5140,0x2c5140,0x2c5168,0x28` in address
  order (preserve the existing owner row); regenerate/rebuild through the
  same path; no handwritten query or owner-only pointer insertion. Actual-
  binding regression: E9's expected-present baseline fails with
  hasFunction=false; after repair require actual generated query entry,
  v0=0 for (0,0), v0=1 separately for (1,0) and (0,1) (optional (1,1)),
  supplied RA, unchanged SP, all relevant callee-saved 128-bit registers,
  whole-RAM preservation, zero GetInfo calls. Recheck DROP handler + full
  suite.
- Phase 3 — ONE verification boot: E9's ≤600 s + lease/progress/byte rules;
  `PS2X_DIAG_REPORT_ALL=1`, E7/E4 taps kept; dynamic a0 capture (require
  [a0]=`0x486f78`); constructor `0x2c3fc4` + UI-pointer `0x23d5c8` guards;
  observe query return at `0x2c44b0`, state-3 path
  `0x2c48c0→0x2c4980→0x2c50e0→0x40a498`, UI pending at +0x338; preserve the
  packet→GS→D→field-Present join.
- Outcomes (tabled, not verdicts): (i) entry restored + guest advances past
  the wait → join-proof table + ONE next action; (ii) a REMAINING wall
  appears first (sibling `0x2c5358` or other) → table it, do NOT claim menu,
  stop with the exact next recipe; (iii) entry/preflight failure → exact
  failure + repair recipe, stop (no stacking). Only an actual guest
  request/completion dependency can promote memory-card storage, SIF/CD or
  input work.
- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch `ssx3`.
  Plain commits, pushed to the FORK remote only, never upstream. Generated
  runner sources follow the owning lane's staging rules (table them).

## Gates and rules

- Boots ONLY while holding `/tmp/ssx3-p-lane-lease` (SHARED P-lane) — poll
  every 5 min + log waits to `$W/P1/run/e10-waits.log` if taken. Max 2 boots
  ≤600 s each; wall + progress + BYTE caps recorded per boot, abort past
  any cap. Builds `-j4` any time. No `adb`.
- `export COPYFILE_DISABLE=1` on every SSD step. APFS scratch cleaned after
  (before/after `df` + manifest of retained copies). Retain: manifests +
  receipts that prove each phase (compress closed raws; single canonical
  copies).
- Evidence dir `local/research/E10/` (STANDALONE). Commit with `git add -f`,
  prefix `[E10]`, trailer `Orchestrated-By: Muse Code`. Do not push ssx3
  (orchestrator pushes at poll when the tree is clean). Fork commits pushed
  to fork remote (table SHAs).
- Experiment contract up front (hypothesis/observable/alternatives/stop +
  which outcome each selects). Time box: 6 h from this handover.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — APFS preflight completion (no boot)

1. Fresh budget + owned scratch dir + derived used-TOML (only `general.
   output` changed); source-identity re-checks (canonical CSV, ELF,
   generator). DROP-only generation to completion; manifests; `0x426230`
   actual-binding check (emitted guest handler, never TODO).
2. Installation (changed bytes only, no metadata, leftover disposition) +
   rebuild + suite + relinked binding test (RA/SP/s0 receipt; query still
   absent). Decision table: green (→ Task 2) vs exact failure + recipe
   (→ stop).

## Task 2 — query repair + verification boot (max 2 boots)

1. CSV row + regenerate/rebuild + actual-binding regression (truth table
   both nonzero cases + preservation checks) + DROP/suite rechecks.
   Pre-claim checks + suite re-green before any boot.
2. Claim → foreground verification boot → at first binding cap: SIGTERM →
   release AT ONCE → analysis (lease-free). Dynamic a0 + guards + copy join.
3. Finding table (exactly one of i–iii) with the receipts that force it +
   the prescribed next action. If (iii): the measurement repair, nothing else.

## Report

`local/research/E10/REPORT.md`: contract, preflight record, query repair +
truth table, verification boot, finding, exact commands, gaps. Commit fork
first if touched (pushed fork remote), then evidence (`[E10]`), remove the
lease if held, stop.
