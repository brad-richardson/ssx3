# E54A Part 1 — Audit GS CSR vsync semantics on the current fork

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. **Read only** the fork and PCSX2 reference; write only
`local/research/E54A/REPORT.md`, then commit that report. The
orchestrator decides the repair brief.

Goal: update the review's stale GS CSR finding before E54 changes the
runtime. Read `docs/research/review-2026-09-23-frontier-2.md` §A2 and
§C, plus `local/research/GB3/REPORT.md` for queue history. Source pin:
fork `ssx3` at `bc1c70f` (read
`~/dev/ssx3-work/W1F/PS2Recomp` without editing it). The current
`ps2_memory.cpp` already applies write-one-to-clear to SIGNAL/FINISH
and preserves FIFO EMPTY, so the old claim "CSR isn't W1C" is too broad.
The open question is CSR.VSINT/FIELD and guest vsync waits. Local PCSX2
bitfield reference: `~/dev/ssx3-work/GB3/pcsx2-ref/GSRegs.h`; source
behavior may be read from the bytesize PCSX2 clone if available, but do
not edit or run it. Cite exact file:line or guest address for every claim.

Correct behavior model: a guest write of CSR bit 3 acknowledges VSINT;
the next vblank sets VSINT; FIELD comes from GS timing rather than a
guest write. Alternatives: the fork already raises VSINT elsewhere, or
these writes target a different CSR abstraction. Discriminator: trace
the exact host state assignment/call path from VBlankStart to
`PS2Memory::read64(0x12001000)`, and the guest store/poll loop at
`0x37c0dc–0x37c0e8` and `0x39608c–0x396098`. Use
`local/tooling/ee/ee-at`, `ee-func`, `ee-xref`, and `ee-label` for guest
sites; name nothing `ee-label` calls unknown.

Deliver tables:
1. CSR bits 0/1/3 and FIELD: current read/write/reset/vblank code,
   unit tests, and PCSX2 behavior, each with source:line and a gap cell.
2. Guest write/read sites and whether each loop is actually reached in
   existing bounded boot receipts (cite log line, else "reach unknown").
3. A *single* minimal candidate fix and one unit-test plus one boot
   observable that would discriminate it from the alternatives. Hand
   back the table, do not implement or declare a verdict.

No build, boot, device use, fork commit/push, or network search. Time box
45 minutes, ≤150 report lines. If PCSX2 behavior cannot be read
authoritatively, state the gap; don't infer it from bit names. Include
revision, exact commands, and unresolved links. Commit `[E54A]` report
with `Orchestrated-By: Codex`; no ssx3 `main` push.
