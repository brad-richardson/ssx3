# E44 — Who writes scratchpad item 0x70000000 (w0 = 0x30 here, 0x1b0 in PCSX2)? Recomp side

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini).
**Tables + receipts; recommend, the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/E43/REPORT.md`,
`local/research/T55/REPORT.md`. You own `~/dev/PS2Recomp` (`ssx3` tip) and the
P-lane lease; ff pushes allowed after the runner-dir check (trailer
`Orchestrated-By: Muse Code`).

## Facts

- `sub_00362DE8` walks 112 scratchpad items (`0x70000000…`, stride 0x80)
  twice per vsync and hashes each into draw records. In PCSX2 items
  `0x70000000` and `0x70000500` have w0 = `0x1b0` (mode 6 = bits 6–9); in the
  recomp item `0x70000000` has w0 = `0x30` (same low bits, mode 0). That
  one field is the whole divergence downstream (no mode-6 records → no
  set-A microcode → stuck VU1 programs → missing 3D).
- Candidate code (orchestrator read, unconfirmed as the writer):
  `sub_003629B8` @`0x362b68–0x362c34` masks/ORs mode fields (`0x100`, `0x14`,
  `0x180`, `0x1`) into the record at `*(a2+0xE84)+4` and snapshots it with
  `ldl/ldr/sdl/sdr` pairs. (Orchestrator checked the generated LDL/SDL merges
  against MIPS little-endian semantics: correct.)

## Change (default off, unit-tested)

A write watch on scratchpad words `0x70000000..0x7000000c` and
`0x70000500..0x7000050c` (all store paths: macros, fast path, SPR/toSPR
DMA, host copies into the scratchpad backing): `spw vsync addr value via
pc ra fn a0..a3 v0 v1 t0..t9 s0..s7` (DMA: `via=spr-dma src=0x<EE addr>`),
first 64 per word, window 1270–1280.

## Run, then read

Boot A (E33 route, 300 s). Table: writers of w0 (and w1–w3) per item; if a
DMA copy, the EE source address, then a second watch (Boot B) on that source
word to find its EE writer. Read the final writer in the codegen: where the
mode bits come from (which condition would set 6), and log that condition's
inputs if they're memory words.

## Deliverable

Builds as needed, 2 boots, 3 h, cap 2 GB. `local/research/E44/REPORT.md`. No
verdict. `[E44]` commit (`git add -f local/research/E44
local/muse/prompts/E44.md`, `git log -1` first); ff-push the fork.
