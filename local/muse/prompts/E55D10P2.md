# E55D10 Part 2 — one Square-at-Main-Menu boot (OpenCode Go)

You are the E-lane run worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/muse/prompts/E55D10.md`, and full `local/research/E55D10/{REPORT.md,ORCH-GATE-P1.md,e55d10_boot.py,check.py}` plus E55D9 Part 2 gate. The orchestrator released **one** Mac boot with only script SHA `03536b0c5b84a95d676ca2e972446827b7be11cfc3153a1a67f8eac9c4447e3f`. Verify it first; mismatch stops. This pane has a per-worker `OPENCODE_CONFIG_CONTENT` exception only for `~/dev/ssx3-work/E55D10/**`; verify with a tiny new sentinel and remove it. Global config unchanged. Own only `local/research/E55D10/` and private `~/dev/ssx3-work/E55D10/`. No fork/source edit, build, Odin/iOS action, seeded-card change, push, board/global config edit, upstream contact or second boot.

Run exactly `python3 local/research/E55D10/e55d10_boot.py --label S1` **once**. Its precheck must pass: E55D3 fork `bab6eb3`, runner-dir guard empty, two matching SHA reads of runner/ISO/ELF/codegen, fresh empty cards. The script claims one mini slot, boots from its own cwd, enforces 500 s wall/120 s no-progress/16 MiB log/2 GiB frames, stops or kills only its recorded PID and releases the slot. Do not bypass any failed precheck, cap, permission or lease; report OTHER. Do not edit the approved script. Keep raw stdout/boot log in private scratch, gzip closed log. No speed claim.

Evidence: retain result JSON, SHA/route/stop/lease receipts, full-resolution PNG nearest the pre-Square F0 tick760 and post-Square F1 tick920, plus any unexpected screen and adjacent `.txt` metadata. Snapshot filename tick is approximate; record actual metadata tick and SHA. Curated PNG total ≤2 MiB, text ≤512 KiB. View each yourself, report exact visible title and menu entries, selected item, and footer; the orchestrator makes the visual verdict. Check private `[padscript]` press/release rows: one Start and one Square, no others. Verify card roots unchanged or report any change. Leave bulk frames/log in private scratch.

Predeclared A = post-Square readable Options screen with back path; B = readable menu persists or a different readable screen opens (state exact text); OTHER = ambiguous/missing frame, route/pin/lease/cap mismatch. Even A does not prove Save or GetDir/Read reachability. Add `check-p2.py` or a bounded receipt check for pins, frame SHAs, pad rows and caps; it cannot prove visual labels. Update REPORT with exact commands, one-run counts, gaps and next action. **Stage and commit only named `local/research/E55D10/` files and this brief by explicit path; never include previously staged docs or another lane's gate.** Check `git status` and `git log -1` before committing `[E55D10] Part 2` with `Orchestrated-By: opencode`, no push. Hand back frames/table; orchestrator gates.
