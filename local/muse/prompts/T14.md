# T14 — Dev cell-(b): wide-header incremental on the dev config (closes the adoption table)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T12/REPORT.md` first (all of it —
T12 gap 1 is this brief: cell (b) wide-header incremental was
measured on Release (658 s clean) but never on dev; this is the
last missing adoption input), plus `local/research/T6/REPORT.md`
§T6-2/§T6-4 (the (b) method to repeat verbatim: which header,
mtime-only touch with content check, clean vs contended
handling). This is a MEASUREMENT brief: one cell, no fork
changes, no boots. Peer lanes run concurrently — build
etiquette is mandatory (see rules).

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`, branch
  `ssx3` (table HEAD at start; record don't fight — T12's
  `7eed783` expected, later is fine).
- T12's dev tree SHOULD still exist at `/tmp/t12-dev-link/`
  (Release: `/tmp/t12-link/`). Reuse it if intact (verify:
  `PS2X_DEV_NO_THINLTO:BOOL=ON` in its `CMakeCache.txt` +
  `ninja -t commands` shows no `-flto=thin` on the runner
  link); else reconfigure fresh per T12's flags (record
  which).
- T6 Release (b): 658 s clean (PCH 1.2 s + 338 compiles ~250 s
  parallel + runner link 396.7 s = 60.3%). Dev prediction
  (table, don't believe): compiles same-ish, link ~1 s →
  total ≈ compile wall only.

## Gates and rules

- NO lease (no boots at all — builds + measurements only).
  `adb` not used. ZERO fork changes (no source edits, no
  commits, no push).
- Build etiquette (T6 §T6-6 / T12 §T12-7 method): `-j4` MAX;
  `pgrep -f` + self-excluding `ps` pre-check before EVERY
  build; if a peer builds (watch for T13's `sleep`-poller
  self-match — pair every pgrep with a ps read), wait + log
  to `$W/P1/run/t14-waits.log` (copy to evidence). If the
  first (b) run contends mid-way, keep it as the contended
  sample + rerun clean (T6 precedent) — table both.
- Evidence dir `local/research/T14/` (STANDALONE). Commit with
  `git add -f`, prefix `[T14]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts.

## Task 1 — dev cell-(b) (the missing measurement)

1. Header: `ps2xRuntime/include/ps2_log.h` (T6's (b) header —
   same file, mtime-only touch, content verified unchanged
   before/after by sha).
2. Wall + breakdown: total wall, PCH time, compile count +
   parallel wall, runner link edge time (from `.ninja_log`),
   tests link time. Same cells as T6's (b) table for a
   side-by-side row.
3. Commit the raw build log + the `.ninja_log` excerpt lines
   the breakdown cites (line refs, like T6).

## Task 2 — adoption-inputs refresh (the full table, no verdict)

1. Side-by-side: Release (T6/T12 numbers, cited not re-run)
   vs dev (T12 (a)/(c) + this run's (b)) for full build,
   cells (a)/(b)/(c), suite, D verdict, binary sizes.
2. The (b) ratio row (Release 658 s vs dev measured) +
   which step owns dev-(b) (compiles? PCH? tests link?).
3. Gap rows (anything unmeasured that an adoption decision
   would still want — name it, don't run it).

## Report

`local/research/T14/REPORT.md`: method + cell-(b) table +
breakdown + adoption-inputs refresh + gap rows. Commit
evidence (`[T14]`, no push), stop.
