# E59 — The race sky/sun vanished after E53; terrain textures look flat

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md` (two-slot lease, kill by PID, the I26-FAST route);
- `docs/orchestration.md` §3–4;
- `local/research/E53/REPORT.md` (Parts 1–2: every change in batch 1, the
  race frames, the counts, TEX1 K);
- `local/research/E51/REPORT.md` (GIF-dump tooling, PCSX2 comparison);
- `local/research/T65/REPORT.md` (the PCSX2 race reference).

Write scope: `local/research/E59/`, `~/dev/ssx3-work/E59/`, a fork worktree
`~/dev/ssx3-work/E59/PS2Recomp` on local branch `e59-sky` from fork `ssx3`
`b9647f5`, and the lease files. No push.

## Facts

- E50 (`eac6cba`) race frames show the sky, the sun and the lens flare
  (`local/research/E50/frames/e50f-race-tick7622.png`), but shard terrain.
- E53 (batch 1, now on `b9647f5`) draws whole terrain, but **the sky and
  the sun are gone**. There's no foliage, and the textures look flat
  (`local/research/E53/frames/e53c-race-tick8274.png`; N7 on the Odin
  shows the same).
- E53 changed several things at once:
  - (a) VSQI/VLQI/VILWR/VISWR, VCALLMSR, CTC2 CMSAR1;
  - (b) the R register;
  - (c) DIV.S/VDIV/VSQRT/VRSQRT special values and VFTOI;
  - (d) MAX/MIN/C.cond;
  - (e) the EE FP mode RTZ+FZ (runtime switch `PS2X_EE_FPMODE=ieee|ps2`);
  - (f) no FMA contraction;
  - (g) `log` unbound;
  - (h) the VU1 EFU decode/latency tables (ESIN/EATAN/EEXP/ERSQRT moved).

  Sky/sun/flare code plausibly uses the EFU trig ops (h) or the VU0 paths
  that now actually run (a).

## Work

1. **Cheapest discriminator first (1 boot):** the fold runner with
   `PS2X_EE_FPMODE=ieee`, I26-FAST into the race, frames at race ticks
   ~1800/1950/2100. Is the sky back?
2. If not, **bisect by revert** on `e59-sky`: build variants that revert
   one group each (h first, then a, then c+d, then g), and boot each to the
   same race ticks. That's ≤4 variant builds, reusing one build dir with
   incremental rebuilds, and codegen regenerated only when a group changes
   generated text. The goal is the group whose revert brings the sky back.
3. For that group, name the exact instruction/site. Compare the relevant
   sky program's VU1 inputs/outputs or GIF packets with PCSX2 (the T65 race
   dump has the programs; E51's GIF-dump diff method). Say whether our old
   or our new behaviour matches PCSX2. **It's possible E53 is right and it
   exposed a second bug:** say which.
4. **Flat textures (table only):** in one race frame, GIF-diff the terrain
   programs `0x2270`/`0x22c8` against PCSX2's race dump: TEX0/TEX1/CLAMP/
   ALPHA per prim, the IMAGE upload count per vsync (E51: 1 vs ~73), and
   the MIPTBP. Hand back the first field that differs systematically.

## Budgets and stop rules

≤6 builds, ≤6 boots (one slot each, escalated, wait for a free slot), 6 h,
cap 8 GB. One candidate fix is allowed if step 3 names a single mechanism,
with a unit test against PCSX2 semantics and one validation boot.
Otherwise hand back the tables.

## Deliverable

`local/research/E59/REPORT.md` (tables, viewed frames, commands, runner
SHAs ×2). `[E59]` commit (`git add -f local/research/E59
local/muse/prompts/E59.md`, `git log -1` first).
