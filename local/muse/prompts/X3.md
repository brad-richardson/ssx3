# X3 — 120 Hz scoping: map SSX 3's frame loop and timestep in the generated code (read-only)

You are a local worker on a brief. Follow it exactly and read only; your
one writable folder is `local/research/X3/`. **Hand back tables. Do not
conclude or recommend.** Every row needs a citation (`file:line` in the
generated code). If you can't cite it, write "not found".

## Inputs (read-only)

- Generated code: `~/dev/ssx3-work/codegen-ssx3/sub_XXXXXXXX_0x....cpp`.
  One function per file. Each MIPS instruction appears as a comment:
  `// 0xADDR: 0xWORD  mnemonic  operands`.
- `docs/archive/ssx3-120hz-handoff-2026-09-13.md` lines 37–60 (labels and
  addresses below).
- `local/research/X2/REPORT.md` and `local/research/X2/ORCH-CORRECTIONS.md`.
  **The corrections file is authoritative.**

## Lookup table (use these labels exactly; do not rename)

| Address | Label | Source |
| --- | --- | --- |
| 0x00316F00 | cAppMan_mainLoop | handoff |
| 0x00317328 | cAppMan_checkHalt | handoff |
| 0x00317184 | published "60 FPS 2 Players" patch site (writes 0x26310002 = `addiu $s1,$s1,2`) | handoff |
| 0x00230704, 0x00230710 | published "Fix Metro Slowdown" sites (write 0 = disable frame-skip) | handoff |
| 0x00227E98 | cSSXApp_preUpdate | handoff |
| 0x00227F58 | cSSXApp_timerCallback | handoff |
| 0x0022E840 | cGameViewMan_updateAll | handoff |
| syscall 0x42 / 0x44 / 0x74 | SignalSema / WaitSema / SetSyscall | ORCH-CORRECTIONS |

Syscall numbers appear as `addiu $v1, $zero, N` before a `syscall`. Label
them only with this table.

## Tasks

Hand back one table per task.

1. **cAppMan_mainLoop structure** (`sub_00316F00`). List, in address order,
   every `jal` target, every loop back-edge (a branch to a lower address),
   and every load of a `state+0x20`-style limit. Columns: address,
   instruction, target/label, cite.
2. **The counted step loop at 0x317184.** Identify:
   - the counter register;
   - its initial value (address);
   - the compare that ends the loop (address, both operands, and where the
     limit is loaded from);
   - what is called once per iteration.
   Cite every item.
3. **What the two Metro sites do.** For 0x00230704 and 0x00230710, give:
   - the containing function (file);
   - the original instruction words;
   - the 10 instructions before and after each;
   - which registers are stored where.
4. **Timestep candidates.** In cSSXApp_preUpdate and
   cSSXApp_timerCallback, list every float constant built with
   `lui`/`ori`/`mtc1`, every load from a global address, and every call.
   Give the hex value, the float value if it's a float, and the cite.
   Mark any value equal to 1/60, 1/30, 60 or 30.

## Output

`local/research/X3/REPORT.md` with the four tables, plus a "not found"
list. No other files. Commit only if the brief runner tells you to.
