# N8D5 Part 6 — restore proven PNG writer in the tile-probe APK

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §§3/4/6. **Build/package only.** No Odin install/launch, bytesize game/emulator boot, fork/main push, or shared source edit. Work in `local/research/N8D5F/`, Mac scratch `~/dev/ssx3-work/N8D5F/`, and fresh WSL scratch `/home/brad/n8d5f/` on `ssh bytesize`. Check bytesize has no other heavy job before the one build.

Facts: N8D5E's one Odin run (commit `8feec46`) reached tick 2050/FBP112/PMODE `ff21`/512×448, control 128/128, and both sampled/raw tile vectors matched **896/896**, with 15,669 occupied pixels and 79 active tiles. Its formal category is OTHER because `upload-0.png` was never written: same-PID raylib logged `Failed to open file` and `Failed to export image`, while `upload-0.txt` was written. `local/research/N8D5E/orch-vector-audit.txt` and `orch-png-diff.txt` pin the mechanism. N8D5D used N8B1 `ps2_runtime.cpp` SHA `68aa9c2ac559033b726b81948867a2ed1be581e30825291ef7b88241fd7d9f87` with `ExportImage(img,path)`. N8D3/N8D4's successful PNG runs used N8D3 `ps2_runtime.cpp` SHA `358d726bf39e319b906e7e15407e1bf361cd2cdbdd8e79d4a96b25a9588c3f96`: a **single diff hunk** replaces those two calls with `ExportImageToMemory` plus `std::ofstream`. Treat that as a pinned, narrow candidate, not a proven cause until the next device run.

Make a fresh N8D5F private WSL snapshot from `/home/brad/n8d5d/`: `PS2Recomp` excluding `.cxx`, `build`, `.gradle`, `ps2xRuntime/src/runner/`; private `parallel-gs` excluding build caches; private `jniLibs`. External codegen remains `/home/brad/n8b1/codegen-ssx3`, SHA `8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`. Copy **only** N8D3's pinned `ps2_runtime.cpp` into the N8D5F snapshot after two SHA reads, and verify the N8D5L backend SHA `4e3efc51d8d104ab5d3c537076e85c6cfcd8344c8e6c147de02f5a5015c8f1e5` and shader header SHA `19b9ba5fc747d8fcb70d712b86bd5738c64424d4b5ed58219f71bac9` stayed pinned. Against N8B1, require exactly two fork source differences (backend, PNG writer) and the named shader header addition; `jniLibs` unchanged. Require the writer's diff to be exactly the one hunk in `orch-png-diff.txt`. Write `REPORT.md` with source, build and package gates **before** building.

Use N8D5D `build.sh` as the command pattern: one arm64 `assembleRelease`, shadow paraLLEl ON, diagnostics/runtime/aggressive/IOP RPC/debug UI OFF, private G43/jniLibs, external codegen, JDK17 and N8B1 memory governor. WSL scratch <10 GiB, Mac scratch <500 MiB, text receipts <8 MiB, global mini ssx3 <200 GB. One named compile-repair retry only after explicit failure; no tuning loop. No app run.

Gate the package before handoff: two APK SHA reads on WSL and Mac after transfer to `~/dev/ssx3-work/N8D5F/app-release.apk`; exactly arm64 runner, Turnip and HAL shim, no x86; each member SHA read twice. Require Turnip `717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d` and stripped shim `1b49d27c839f25363237d8276c91a401602845ee8eef67de50d6540f4cdfc387`. Record runner SHA, Build ID, and presence of `PS2X_N8D5_TILE_CAPTURE`, `control=`, `sampled_summary`, `raw_summary`, and the PNG writer's `PNG write failed path=` runtime string in the packaged runner; verify the exact source hunk separately. Record arm64 cache flags/source paths. Check Mac N8D5L fork runner-dir diff against `14b1e5cb` empty. Commit bounded scripts, source/package gates and `REPORT.md` under `local/research/N8D5F/` as `[N8D5F]` with trailer `Orchestrated-By: Codex` and explicit paths. Hand back the package table, not a device or causal verdict; no push.
