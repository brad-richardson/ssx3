# M29 — Partial-move attribution: per-byte rows for 22/708/709/710 (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M28/REPORT.md` first (all of it —
M28 gap 1 is this brief: shapes 22/708/709/710 move single named
columns by 1–2 rows at J 0.9510–0.9902 — 2 extra-only + 2
no-cand, 0 assigned), plus `local/research/M27/REPORT.md`
(Task-1 per-site table precedent: offset/plane/row/col,
cross-frame |δ|, signed gaps both frames). Fully offline — SSD
dumps read-only, no harness code, no replays, no lease. No device
work.

## Facts you start from

- Partial movers (M28): shape 22 c296 11/9 (extra-only), 708
  c296 8/8 (missing-only −1), 709 c301 10/10 (missing-only
  −1), 710 c340 6/6 (missing-only −2). All read above J 0.95
  (0.9510–0.9902); none assigned a destination (the moves are
  ±1–2 row deltas on otherwise-present columns, not swaps).
- The attribution question (M27 Task-1 method, 4 shapes): for
  each extra/missing row (the ±1–2 delta rows per shape):
  cross-frame |δ| + cell/bulk status on the other shape,
  signed gaps both shapes, band/decile/streak/carrier joins
  (M27 machinery verbatim — s0-anchored).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28.txt`
  (partial-move rows to reproduce: per-shape n/ov/standing —
  match exactly or table the mismatch and stop). Work dir:
  new `/Volumes/Extreme SSD/m29/`; evidence
  `local/research/M29/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m28/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m29/` + `local/research/M29/`.
- Evidence dir `local/research/M29/` (STANDALONE). Commit with
  `git add -f`, prefix `[M29]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M28 precedent).

## Task 1 — delta-row tables (what are the ±1–2 rows?)

Partial-move rows recomputed from the dumps (per-shape n/ov/
standing must match M28 exactly or table the mismatch and
stop):

1. Per-shape delta rows: extra rows (present here, absent s0)
   + missing rows (absent here, present s0) with M27-style
   per-site rows (offset, plane, row, col, |δ| here,
   other-shape status + |δ|, signed gaps both).
2. Near-miss check: do delta rows read bulk on the other
   shape (|δ| just below 8?) or non-cell (H1/H2-style
   shares per shape — tabled)?
3. Position join: delta rows by band / decile / column /
   carrier (M27 joins verbatim) — do partials localize
   like full swaps (band-0, streak columns)?

## Task 2 — extra-only vs missing-only (two populations?)

1. Compare the 2 extra-only (22, 734-c342-adjacent? — no:
   22 + M28's other extra — table from the census) vs 2+
   missing-only delta rows: value stats (|δ| ranges, gap
   signs), position stats (top/bottom of column?).
2. 734-c342 row (extra-only +2 rows, M28's 3rd extra):
   same delta-row table (is it the same phenomenon as 22's
   extra, or different?).
3. Standing update: do any partials resolve to assigned
   destinations under a row-tolerant rule (table the
   rule + assignments WITHOUT adopting them — the census
   rule stays exact-match)?

## Task 3 — controls + determinism (M18–M28 precedent)

1. Positive control: synthetic truth with KNOWN partial moves
   (N shapes, ±1–2 row deltas, known rows) — recover the
   delta rows + standings exactly.
2. Determinism: re-run Task 1 on s0+22+708, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M28).

## Report

`local/research/M29/REPORT.md`: DESIGN.md (bars first) +
delta-row tables + extra/missing comparison + row-tolerant
standing table + controls + explained-vs-standing update
(attribution, not explanation — table it) + gap rows.
Screenshot only if a delta-row map discriminates
(budget-noted, pre-registered split required per M23–M28
precedent). Commit evidence (`[M29]`, no push), stop.
