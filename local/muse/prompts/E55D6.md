# E55D6 — find a reachable memory-card guest-write path (OpenCode Go)

You are the E-lane worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55D3/{REPORT.md,ORCH-GATE.md}`, `local/research/E55D4/{REPORT.md,ORCH-GATE.md}`, `local/research/E55D5/{REPORT.md,ORCH-GATE.md}`, `docs/todo.md` E55D6, and the fork's `MemoryCard.cpp`. This is **read-only design**: no build, boot, device action, source edit, fork commit, push or change to the current card image. Write only `local/research/E55D6/` and this brief. Do not edit status/todo/ledger. Cite file:line or a named receipt for every key factual claim; use LSP to confirm code connections where possible, and mark unproved links unknown.

Goal: find one realistic route that reaches `sceMcGetDir` or `sceMcRead` and puts differing bytes in a guest-write record. E55D4 A/A and E55D5 pad-B reached race tick2053 with empty mc0 but **no getdir/mcread records**; do not assume that merely pre-seeding a card causes the game to read it. Pad comparison is already done. ExternalWake policy remains parked.

Survey bounded evidence: the game's mapped card APIs/call sites in generated code or disassembly, existing `local/research/` traces and menus/routes, and the runtime's host-card semantics. Find at most two candidate user flows. For each, provide (1) exact evidence that the game calls GetDir/Read, (2) required card layout/format and how to create it without destroying an existing save, (3) route/tick or an explicit `unknown`, (4) predicted ordered probe line with seq-family/address/length and byte difference, and (5) a competing explanation plus observation that distinguishes it. If reachability is not supported by evidence, say so; identify the cheapest bounded trace or instrumentation to establish it before a card-change boot. Do not invent source call relationships from name similarity.

Predeclare a future one-change comparison against the E55D4 A/A baseline: same pinned binary/ISO/route/clock/tap, exactly one card-file byte or entry difference, stop at the first differing `getdir` or `mcread` ordered guest-write record, require a same-prefix hash comparison and flush proof. Explain when the baseline must be re-run because changing card layout invalidates its starting manifest. No actual card mutation in this part. Budget <20 min source/receipt review, <512 KiB new text, no large dumps. If codegen index is unavailable, hand back the gap rather than building it.

Deliver `local/research/E55D6/REPORT.md` with source/trace evidence table, hypotheses and predicted outputs, exact read-only commands, pins, gaps and recommended next action. Commit only this brief and named text receipts as `[E55D6]`, trailer `Orchestrated-By: opencode`, no push. Hand back the table; **the orchestrator chooses the next run**.
