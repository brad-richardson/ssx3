# E29 — Bounded DEV-ONLY movie bypass to menu/race (isolated branch + build + flag; faithful path untouched)

You are the E-lane worker (spawned under claude-cli opus) in the ssx3
repo (derive the repo root from this prompt's path: two up from
`local/muse/prompts/`). **Tables + receipts, no verdicts beyond the
missions.** Read first, all of each: `local/research/E28/REPORT.md`
(guest half CLOSED: 431,840-byte queue, head parked on `0xd49b14`,
live re-ask branch `0x3b10ac`, stall entirely host-side) +
`local/research/E27/REPORT.md` Mission 2a/3 (the host latch:
`nonStreamDeliveries`, re-entry `false`, lambda-held `delivery`,
`EeWaitState` ownership) + `docs/research/
review-2026-09-22-progress-and-parallelization.md` §E-lane (Brad
APPROVED an optional, clearly labeled development-only startup-movie
bypass while faithful movie correctness proceeds separately as
E30). This brief spends the approval. It is NOT a fix and claims
nothing about the stall mechanism beyond what it demonstrates.

## Facts you start from

- ORCH DECISION (gated): E28 PASS — the guest is healthy
  (queue/head/branch all confirmed); every remaining movie question
  is host-side. E29 buys REACHABILITY (menu/race on the recomp
  runtime), not understanding. Understanding + the faithful fix is
  E30 (authored two-chunk regression + candidate-fix eval).
- The bypass MUST be: (1) env-flag-gated, default OFF, dev-only in
  name (`PS2X_SKIP_MOVIE` — exact name, no variants); (2) labeled
  DEV-ONLY at every layer (flag name, branch name, code comments,
  build dir name, report title); (3) isolated — fork branch
  `e29-movie-bypass` cut from `3adc0478` (exact name), separate
  build dir, mainline binary/pins/worktree never touched.
- The mainline fork (`ssx3` @ `3adc0478`, status exactly
  `?? ps2_log.txt`) is FROZEN this lane: all bypass commits land on
  `e29-movie-bypass` ONLY, are NEVER merged, and are NEVER pushed.
  At close the mainline worktree must be byte-unmoved (triple-agree
  + clean status re-verified).
- Standing SSD rule (link PROVEN pattern-dependent corrupt): the
  bypass BUILD lives on SSD (internal cannot hold two trees —
  floor rule), so every build output byte needs 2+ matching reads
  separated in time AND corroboration (rebuild determinism spot +
  on-disk pins); single-read SSD evidence is suspect.

## Gates and rules

- Fork: `3adc0478` triple-agree FIRST (mainline, before cutting the
  branch). Branch `e29-movie-bypass` cut from exactly that sha
  (receipt the fork-point). ALL bypass edits on the branch; ZERO
  commits on any other ref; ZERO pushes from this lane (branch
  stays local).
- P-lane lease: UP TO TWO title boots (flag-OFF equivalence +
  flag-ON bypass), each claimed/released per protocol, one at a
  time. No third boot for any reason.
- Fix gate: NOT APPLICABLE to the bypass (it is scaffolding, not a
  fix) — but the gate file is still written, recording that + the
  E30 handoff (what the bypass teaches the faithful fix).
- Byte caps: NEW SSD build dir ≤ 6 GB; NEW SSD `e29-*` paths ≤ 16
  GB total; internal volume ≤ 512 MB delta (NO /tmp build — the
  floor forbids two trees); evidence `local/research/E29/`
  text-only; `COPYFILE_DISABLE=1` on every SSD step; zero deletions.
- Time box 8 h. Evidence commit `[E29]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 1 — Mechanism + checkpoint (static, pre-build, no lease)

From E27/E28's static knowledge ONLY: name the bypass mechanism
precisely (which state is skipped / forced / fed — file + function
+ lines + the exact inserted block, drafted here, applied in
Mission 2). Pre-register: (a) the named menu/race CHECKPOINT with
its byte observables (which boot-log markers / counters / states
prove "menu reached" — no log line, no claim); (b) the flag-OFF
equivalence bar (e28a's terminal state reproduced: park counters
×1/×1 + park pc + feed sha — exact values, not "same"); (c) what
the bypass CANNOT show (write the limits before the code). Table +
stop here if no mechanism is visible — do not build blind.

## Mission 2 — Branch + build + suite (no boot)

Cut the branch and check it out IN the main fork worktree (a
separate worktree/clone would lack the gitignored generated
sources — no `git worktree add`, no second clone). Apply ONLY the
Mission-1 block (diff receipted in full; no opportunistic edits —
the branch checkout + the bypass diff are the ONLY worktree
mutations permitted). Build in the NEW SSD dir (E25's recipe,
`-S` the fork worktree). At close: check back out to the mainline
ref, re-verify triple-agree + clean status + a sample of generated
sources against E27's committed pins. Record throughout:
configure log, build log, binary size+sha (2+ reads), and the full
458 suite on the NEW binary with the flag OFF (must be 458/458 —
the faithful-equivalence gate).

## Mission 3 — Up to TWO boots + verdict table

Boot 1 (flag OFF, lease): MUST reproduce e28a's terminal state per
Mission 1(b) or the bypass is CONTAMINATED — table + STOP (no Boot
2; the contamination is the finding). Boot 2 (flag ON, lease, only
if Boot 1 green): run to the Mission-1(a) checkpoint or the wall;
record wall/rc/fate + checkpoint observables + the full watch
ledger (carry E28's 243 vector unchanged — no new watch design this
lane). Close with the E30 handoff paragraph (what the bypass run
shows the faithful fix must reproduce WITHOUT the flag).

## Success bars (tabled)

Mechanism + checkpoint + limits pre-registered; branch exact-name +
diff + fork-point; build logs + binary pins + 458/458 flag-off;
Boot 1 equivalence green; Boot 2 checkpoint verdict with receipts;
E30 handoff; mainline triple-agree + clean at close; tail receipt
with byte-exact prefix sha; zero pushes/merges/deletions.
