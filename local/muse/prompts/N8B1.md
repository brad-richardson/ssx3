# N8B Part 1 — package and build in-process Turnip APK

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4.
Read `local/research/N8A/REPORT.md` fully, plus the pinned N7/G43/GB6C
reports it cites. Hand back tables/receipts; the orchestrator decides
the gate. The paths below are your write scope. “First failure” means a
failed brief step, not a typo in an exploratory command. This part is
**source, host tests and APK build only**: no Odin shell, install, launch,
or push. No upstream contact.

Pin fork `ssx3` at `4f932166c773e04522ffa007228670894f8b4f44`
(verify remote). Create `~/dev/ssx3-work/N8B1/PS2Recomp` on local branch
`n8-turnip-apk` from that pin. Inputs: canonical external codegen
`~/dev/ssx3-work/codegen-ssx3` (9,457 files, register SHA
`8ea8ed436b78fee0156e37a972924645d8a7f4041cb90cab1a6b2ae662d688a3`),
G43 paraLLEl/Granite **working-copy content** at
`~/dev/ssx3-work/G43/parallel-gs` (record HEAD and tracked-diff SHAs),
N7 Android branch on bytesize WSL `n7-android` `81aa92d`, and Turnip v36
`~/dev/ssx3-work/G43/inputs/libvulkan_freedreno.so` SHA
`717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`.
Keep codegen and driver outside Git. Two matching SHA reads of each
pinned binary/input before copying or packaging; verify transfer SHA.

## One source candidate and build gate

1. Port only N7's necessary Android pieces onto the current fork: Gradle
   external-codegen property, `ps2x.env` initializer/parser with its
   existing unit test/registrations, and arm64-only packaging. Merge test
   registrations with current tests, do not blindly cherry-pick. Add
   Gradle options for `PS2X_GS_SHADOW_PARALLEL=ON`, a pinned local
   `PS2X_PARALLEL_GS_SOURCE_DIR`, and an **out-of-tree**
   `jniLibs/arm64-v8a` driver staging dir. Do not commit staged Turnip,
   guest codegen, APK or source tarballs.
2. Port G43's corrected `HMI`/HAL struct and offset assertions into the
   Android in-process `ps2_gs_parallel_backend.cpp` path. Exact
   `PS2X_GS_TURNIP=1` requests `dlopen("libvulkan_freedreno.so",...)`,
   `dlsym("HMI")`, HAL `open("vulkan0")`, then a **non-null** proc address
   to `Context::init_loader`. Log the first `dlerror`/HAL failure,
   `dladdr(HMI)` mapped location and HAL operation addresses. Fail
   closed on requested Turnip failure: no silent system Vulkan or CPU
   attribution. Unset/other values retain the existing default loader
   on all platforms. Cited G43 source, not an imagined ELF export, is
   the ABI source. Allowed fork edits: Android Gradle/manifest if needed,
   `ps2_android_runtime.cpp` and its header, the backend/helper, and
   directly needed CMake/test registrations and focused tests only.
3. Run host `ps2x_tests` from the fork worktree root with the env parser
   registered; name the full count and any pre-existing failures.
   Prepare a bounded, SHA-verified copy of source/codegen/driver for
   bytesize WSL. Only **one heavy bytesize job at a time**. Build at most
   two arm64 Release APKs (`assembleRelease`), second only for one named
   compile/link/packaging repair. Diagnostics/taps OFF. Record Gradle
   command, final CMake cache flags, source snapshot pin and bounded
   error tail. Stop and report the first unresolved build/link error.
4. Gate the produced APK before any device step: it contains
   `lib/arm64-v8a/libps2EntryRunner.so` and
   `lib/arm64-v8a/libvulkan_freedreno.so`, no x86_64 member; the
   uncompressed Turnip member SHA equals the pin above. Record two APK
   and runner SHA reads, ELF build ID, APK member SHA and size, and
   compile/link evidence that the in-process backend and HMI code are
   present. An APK that merely packages Turnip without the HMI loader
   fails this gate. App namespace and runtime driver identity remain
   **unproved** until a separate one-launch Part 2.
5. Before source commit, verify
   `git diff --exit-code 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty.
   Commit only allowed source/tests `[N8B1]` on the local fork branch,
   trailer `Orchestrated-By: Codex`; no fork push.

Budget: two APK builds, one host suite, ≤20 GiB N8B1 scratch on bytesize,
≤5 GiB new Mac bytes (excluding the pre-existing external codegen),
≤32 MiB bounded text logs. Check `local/tooling/disk_budget.sh` before
and after any >1 GiB Mac write; global cap 200 GB. No device action.
Stop after one failed build gate and one clear repair, no tuning loop.

Deliver `local/research/N8B1/REPORT.md` with pin/transfer table,
source-diff/host-test table, exact build commands/cache flags, APK
member/SHA/build-ID table, source-only gaps and Part-2 launch inputs.
Commit only the explicit report `[N8B1]` with the same trailer; no push.
