# E25 — Rebuild runner + suite from fork 3adc0478 + re-baseline pins (NO boot; E26 spends the designed e24a boot)

You are the ssx3 E-lane worker (Claude Code) in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** E24 closed RED: the host restart wiped all
three protected build trees (1,725/1,725 files), and the re-baselining
decision is the ORCHESTRATOR's. This brief performs the rebuild and
assembles the re-baseline evidence. It spends NO boot and claims NO
lease — E26 spends E24's designed e24a boot on the re-baselined build
after the orchestrator gates this brief.

Execute this brief as specified. The design below is settled by cited
evidence — if a step's rationale seems unclear, re-read the cited
receipt rather than re-litigating the approach. Table genuine blocks
with their receipts; do not stall on doubt.

## Read first (all of each)

1. `local/research/E24/REPORT.md` (§1 market map + §2 checkpoint + §7
   gaps — what was lost, what survives, what E25 must restore).
2. `local/research/E23/REPORT.md` (last green checkpoint: runner sha/
   size, suite sha/size, fixture shas, E18 R1–R6).
3. `local/research/E18/NEXT-BRIEF.md` (ABI constraints — BINDING)
   plus E18's build records (the EXACT recipe: flags, toolchain,
   paths — find them in E18's evidence; the rebuild must follow the
   recipe, not improvise it).
4. E24's dir is READ-ONLY reference — your dir is
   `local/research/E25/`. The dylib (`e4d88fdc…b38e6be`, 52,472 B)
   and fixture binaries (`42d9f4f3…`, `3136f3ca…`) re-sha clean —
   reuse by copy + re-sha, never rebuild what survived.

## Mission

1. Inventory the loss precisely (per-path present/missing + the E24
   §1 table carried forward): fork triple-agree `3adc0478` FIRST
   (any fork red → table + stop — do not build on a moved fork),
   then the three protected paths, 9,457-name audit, 1,725 protected
   hashes (expect: names match, protected missing).
2. Rebuild runner + suite from fork `3adc0478` with the EXACT E18
   recipe (same flags/toolchain/paths). Table every step (commands,
   exits, durations, outputs). If the recipe is ambiguous at any
   step: STOP that line, table the ambiguity + the candidate
   readings, and continue only the unambiguous remainder — do not
   guess flags.
3. Verify the rebuild WITHOUT a title boot: suite 458/458 UNLOADED +
   observer-LOADED (reused dylib, E21 receipt discipline), E16
   closure, E15 rc0s, E18 R1–R6 fixtures (behavior-compare vs the
   retained E23 receipts — same inputs must give same outputs).
   Any red → table + stop (no re-baseline on red).
4. Assemble the re-baseline evidence (the orchestrator decides; you
   table): new runner sha/size, new suite sha/size, full build
   receipts, behavior-compare tables, and an explicit OLD-vs-NEW pin
   table (every E23 checkpoint pin: old value, new value, match-or-
   delta with cause). State plainly which pins changed and why
   (rebuild nondeterminism? toolchain drift? path change?).
5. Durability (the lesson): snapshot the rebuilt trees to SSD-resident
   paths with a size+sha manifest (so the next restart costs a copy,
   not a rebuild). Table manifest + restore procedure (do NOT test
   the restore by deleting anything — table the procedure only).
6. Fix gate: N/A (no diagnosis, no code changes). No fork commits.

## Rules

- NO title boot, NO lease claim, NO `boot-attempt.json`. (Fixture and
  suite runs are not boots.) If any step needs a boot → table it for
  E26 and stop that line.
- No CSV/regen (regen need = stop). No fork commits of any kind.
- Byte caps declared up front + tracked (SSD lane ≤16 GB in NEW `e25-*`
  paths; internal ≤3 GB delta — the volume is the scarce side;
  0 growth elsewhere); `COPYFILE_DISABLE=1` on SSD steps; large
  artifacts off the internal volume.
- Evidence dir `local/research/E25/` (STANDALONE). Commit with `git add
  -f`, prefix `[E25]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 8 h. Report in chunks + tail
  receipt (recompute AFTER the last edit — E24-E2 lesson: a stale tail
  receipt fails the gate). Fill in the ACTUAL time used.

## Carry (do not re-fix)

- E21-E1 (stale harness size/sha in E21's REPORT), E23-E1 (hex rename —
  use the hex-safe proof), E23-E2 (`:2467`/`:2498` actual), E24-E1
  (blocked set omits 31 — primary confirms), E24-E2 (stale tail
  receipt — see the rule above), E24-E3 (slice lengths: FOUR 177s;
  slice28 191 raw / 176 to-last-nonzero — the JSON was right).
