# M37 — c54 carrier coincidence: mask geometry + row-list comparison vs the other 23 extras (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M36/REPORT.md` first (all of it —
M36 gap 1 is this brief, taken on orchestrator judgment: 700-c54's
10 extras read 10/10 bulk near-miss at |δ| 8 AND 10/10 inside the
701 carrier mask — the first unnamed carrier hit in the M35/M36
value tables, vs 0/24 for every other still-below extra), plus
M16's carrier table (the 701 mask definition + rank context).
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- c54 column (M36): 700-c54 holds 10 extra rows [245, 246, 247,
  252, 253, 258, 259, 278, 279, 283], all |δ_700| = 8, all bulk
  on s0 (|δ_s0| 6:2 7:8), all gaps −−, all inside the rank-3
  701 removed-Y mask. The other 23 still-below extras read 0
  in every carrier mask.
- The coincidence question: is c54's 701-membership geometric
  (the 701 mask COVERS column 54's rows — any extra there
  would read inside), or selective (the mask covers many
  columns but only c54's extras land in it)? Answer by
  geometry: mask-column overlap table + row-list comparison
  of c54's 10 rows vs the other 23 extras' rows against the
  701 mask (and the other 9 carrier masks as controls).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), carrier triplets (mask definitions),
  `/Volumes/Extreme SSD/m36/m36.txt` (c54's 10 rows + the
  other-23 row lists to reproduce — match exactly or table
  the mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m37/`; evidence `local/research/M37/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m36/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m37/` + `local/research/M37/`.
- Evidence dir `local/research/M37/` (STANDALONE). Commit with
  `git add -f`, prefix `[M37]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M36 precedent).

## Task 1 — mask geometry (does 701 cover c54?)

c54's 10 rows + the other-23 row lists recomputed from the
dumps (must match M36 exactly or table the mismatch and stop):

1. 701-mask column profile: per-Y-column mask-row counts for
   ALL tail-Y columns (named 8 + unnamed 33 + rest — where
   does the mask sit? is c54 fully covered? partially?).
2. Overlap table: c54's 10 rows × 701 mask (all inside per
   M36 — reproduce) + the other 23 extras × 701 mask (all
   outside per M36 — reproduce) + c54's 10 rows × the other
   9 carrier masks (inside anywhere else?).
3. Coverage answer as a TABLE (mask rows per column for the
   10 c54 rows' columns vs the other-23 columns — geometric
   if c54's columns are covered and others aren't;
   selective otherwise; numbers only).

## Task 2 — row-list comparison (c54 vs the other 23)

1. Row lists: c54's 10 rows vs each other extra's row (same
   rows recurring across shapes? c54 rows unique?).
2. Value contrast: c54's uniform |δ| 8 bulk −− vs the other
   23 (|δ| ranges, bulk/noncell splits, gap signs — M36
   numbers reproduced + side-by-side).
3. 701-mask tail census (cheap): how many s0 tail bytes fall
   in the 701 mask (M22: 56.6% of cell 7 interior — table
   the tail-wide count + cell breakdown: is the mask
   tail-dense generally?).

## Task 3 — controls + determinism (M18–M36 precedent)

1. Positive control: synthetic truth with KNOWN mask
   membership (N extras inside a known mask, N outside) —
   recover the membership table exactly.
2. Determinism: re-run Task 1 on s0+700, byte-identical
   receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M36).

## Report

`local/research/M37/REPORT.md`: DESIGN.md (bars first) +
geometry tables + comparison tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a mask-overlap map
discriminates (budget-noted, pre-registered split required
per M23–M36 precedent). Commit evidence (`[M37]`, no push),
stop.
