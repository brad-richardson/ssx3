# I21 — S9 device re-probe on the E18 fork: rebuild + reinstall + 90 s diagnostic probe, compare delivery/progress vs host (NO implementation)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** Read `local/research/E18/REPORT.md` +
`local/research/E18/NEXT-BRIEF.md` first (all of both, especially the S9
table: E18 delivered 5,040 bytes on the host caller path — fixtures
flipped, 458/458 — but parser output zero packets/frames and main still
waits at `0x3b1028`; the device FFmpeg-OFF stub residual is a SEPARATELY
observed dependency). Then read `local/research/I20/REPORT.md` (agreement
A1–A13 + creator census 39/39 + spec S1–S10 — your comparison baseline).
This brief RE-PROBES the device on the new fork. It does NOT implement
anything (E-lane owns the fork; no stacking).

## Facts you start from

- BASE: fork `3adc0478` (E18 BEHAVIOR, PUSHED — verify before
  building). Rebuild the DEVICE runner from it (same recipe as I18's
  install — table the recipe + pins), reinstall on the iPad (install
  policy: force-install ONLY the previous shape; report don't ask),
  then ONE 90 s diagnostic probe (`PS2X_DIAG_PERIOD_MS` as in I19 +
  `PS2X_DIAG_SEMA_CREATE=1` — creator census already closed in I20,
  keep it for cross-check).
- Compare vs host E18 (table every row): `0x3b0b10` / `0x4029d0`
  dispatches (do they fire on-device?), actual bytes accepted, main
  beyond `0x3b1028` or its named successor, sema 26/30/32 traffic
  vs I19's census, and the DEVICE decode boundary (FFmpeg-OFF stub:
  warning / no-decode / decode — observed, never inferred from the
  host's FFmpeg-ON receipt).
- I-lane DEDUPE (queued since E17): the 5 absorbed rows now live in
  the fork CSV (E17 NEXT-BRIEF dedupe lines 873/1022/2943/7012/7176)
  — dedupe the I-lane LOCAL ports like I17 did (table each removal;
  device rebuild AFTER dedupe so the probe tests the fork rows).
  If a local port differs from its fork row: table both, keep the
  local one, do NOT force-dedupe.
- Steady-state re-confirmation (I19/I20 shape): byte-compare the
  steady-state console against I20's where the fork change doesn't
  legitimately move it; table ALL drift with both receipts.
- Success bars (tabled): dedupe table (5 rows); rebuild identity
  (recipe + pins + binary identity); reinstall receipt; 90 s probe
  (exit/fate + console extracts + screenshot); host-vs-device
  comparison (dispatches/bytes/progress/sema/decode); first
  post-delivery device wall named (or delivery-absent with the
  exact missing edge).

## Gates and rules

- Device: iPad (same as I18/I19/I20) via explicit `--timeout`
  devicectl; ONE rebuild + ONE reinstall + ONE 90 s probe. No PS2
  boots → no lease; no `adb`. NEVER prompt/steer the E-lane pane
  (E19 may be ACTIVE — read-only inspection only; remote never
  moved by you).
- Worktree under a NEW SSD lane dir; caps declared up front + tracked;
  `._*` purge before staging; screenshot to internal path first, then
  `mv`. Zero fork commits (read-only audit + device build/probe).
- Evidence dir `local/research/I21/` (STANDALONE). Commit with `git add -f`,
  prefix `[I21]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 4 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — dedupe + rebuild + reinstall (no probe until §2)

1. Dedupe table (5 rows vs E17 lines; keep-on-difference). Worktree
   recon + iPad pre-state (reachability, lock, running PIDs,
   installed apps — user sessions reported, never killed).
2. Rebuild (identity tabled) + reinstall (receipt) + cleanup (only
   OUR PIDs signaled). Table every receipt.

## Task 2 — 90 s probe + host comparison

1. ONE 90 s diagnostic probe (both env gates); pull console +
   screenshot; steady-state re-confirmation; host-vs-device table
   (dispatches/bytes/progress/sema/decode); first post-delivery
   device wall (or delivery-absent edge). Cleanup.

## Report

`local/research/I21/REPORT.md`: contract, dedupe, build/install, probe,
comparison, handoff, exact commands, gaps. Commit evidence (`[I21]`),
stop.
