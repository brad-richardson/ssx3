# T20 — channel-on drain census: runtime drain-window EE stream vs T17 menu-loop stream (1 boot + align)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/T18/REPORT.md` first (all of it —
the channel (fork `f2b1852`, `PS2X_TRACE_SYSCALLS`, PCSX2 byte
shape), the ladder (channel provably non-perturbing), the
aligner (`tools/trace_align.py`, selftest 17/17), and the k=2
boot-phase divergence (BIOS DMA/SIF init vs HLE sema/OSD)),
plus `local/research/T17/REPORT.md` §T17-2 (the menu-loop
reference: sema/event/timer storm + per-vblank
`sceCdApplySCmd2` over 23,107 vblanks) and P1 REPORT Part 34
§P34-1c (the drain's guard table + per-block bands — your
in-run watchlist). This run asks the S1/S2/S3 question in data
form: what EE-side signals exist in the reference menu loop
that the drain lacks?

## Facts you start from

- Fork `$R=/Volumes/Extreme SSD/ps2recomp-spike/PS2Recomp`,
  branch `ssx3`: T18's channel commit `f2b1852` is HEAD (or
  table what moved — record don't fight; rebuild ONLY if
  the tree moved, `-j4`, table binary sha + size).
- Reference streams (read-only): `/Volumes/Extreme SSD/
  ps2x-t4/emulog-t17c.txt` (menu loop, 914,791 EE events)
  + `/Volumes/Extreme SSD/ps2x-t4/emulog-boot3.txt` (T4
  boot). Tools: `tools/trace_align.py` (selftest must
  re-green before use).
- The run: ONE channel-on boot to the T16 exit + ~30 min
  of drain window (cap 3600 s: ~1200 s in-phase + ~1800 s
  drain sample + margin; still complete if the exit shape
  deviates — table the deviation, keep the drain sample).
  Exit must reproduce the T16 invariant (block-235
  collapse, residue-13 — match/no tabled) or the sample
  is off-epoch (table + still align: label it).
- The question: drain-window EE census (post-exit channel
  events: per-name counts over the ~1800 s sample) vs
  menu-loop EE census (same window length from t17c):
  which names exist reference-side but read 0 drain-side
  (the S1/S2/S3 candidates: i-context sema mix? SIF-side
  echoes? OSD/config traffic?), and which read drain-side
  only? Answer by table (+ shingle-locate rows: do 20-ev
  drain shingles occur in the menu stream?).

## Gates and rules

- Lease `T20`: the boot ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it
  names another agent, poll every 5 min and log waits to
  `$W/P1/run/t20-waits.log`. Max 1 boot ≤3600 s foreground.
  Builds any time (`-j4`). No `adb`.
- ZERO fork changes (channel exists; use it, don't modify
  it. No source edits, no commits, no push).
- Evidence dir `local/research/T20/` (STANDALONE). Commit with
  `git add -f`, prefix `[T20]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 6 h. Tables, no verdicts.

## Task 1 — channel-on drain boot (or cap)

1. Pre-claim checks (T13 §T13-0 verbatim + channel): lease
   absent, `pgrep -x ps2EntryRunner` exit 1, binary sha
   (T18's `a2a2f660…` unless rebuilt), ISO + ELF sizes,
   free space, aligner selftest 17/17, waits-log tails.
2. Claim → foreground boot (channel env set; monitor the
   P34-1c bands: w31 294–308, d/w31 [1.9,2.2] — log-only,
   no trigger) → at cap: SIGTERM → release AT ONCE →
   trace copy (lease-free) → analysis.
3. Epoch table: T16 invariant reproduced? (block-235 row,
   N count, residue — match/no vs T16's numbers) +
   drain-sample window (iters, wall, blocks).

## Task 2 — drain-vs-menu census (what's missing on each side?)

1. Census table: per-name EE counts drain-sample vs
   equal-length menu-loop slice (t17c) — full side-by-side
   (like T18 §T18-2 census, both directions).
2. Missing table: names with >0 menu / 0 drain (ranked by
   menu count — the S1/S2/S3 candidate list) + names with
   >0 drain / 0 menu (ranked by drain count).
3. Shingle table: `--locate 20` for 3 drain shingles
   (early/mid/late sample) in the menu stream (FOUND with
   positions? or NOT FOUND — tabled).

## Report

`local/research/T20/REPORT.md`: T13-0..T13-7 SHAPE (lease
record, epoch table, census/missing/shingle tables, exact
commands, gaps). Commit evidence (`[T20]`, no push), remove
the lease if held, stop.
