# M8 — Delta propagation: why 152 seam hits move zero pixels

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M7/REPORT.md` (all of it: Step 3's 152-hits/0-bytes
finding, the slot-0 choice rationale, "What I could not do") and
`local/research/S2/REPORT.md` Part 4 item 5 (the honesty gate this
closes). Base header `local/research/M7/m7_replay_context.h` (sha in the
M7 report — record `shasum`, not memory; copy it to
`local/research/M8/m8_replay_context.h`, keep every M5/M6/M7 mechanism).
Evidence dir `local/research/M8/` (commit with `git add -f`, prefix
`[M8]`, trailer `Orchestrated-By: Muse Code`). Large outputs (player dirs,
run dirs, jsonl over 5 MB) go under `/Volumes/Extreme SSD/m8/` with a
symlink-free path recorded in the report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M8/` as a research header derived from M7's. No
  vendor change.
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M8\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M8/waits.log`. A P1n agent
  shares the lease — never force it. Do not commit run outputs.
- Copy `local/research/M7/build_replay_player.py` to
  `local/research/M8/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M7 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m8-det`, `m8-det2`,
  … (dual core + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`).
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps

1. **Baseline.** Build the unmodified M7-header copy, run `m8-det`
   once with the M7 delta env, confirm M7's end state reproduces
   (`xform_stats` hits ≈152/replay, 0/200 frames differ, guest/event
   clean). Control for every later step.
2. **Slot-usage census.** Which XF slots actually feed visible draws in
   this frame? Instrument (header-only): per-slot write counts from
   the seam + which slots the vertex shader reads per draw
   (`VertexShaderManager` slot reads). Receipts: slot table (writes,
   reads, draws-affected) — is slot 0 read by any visible draw?
3. **Second-slot delta.** Apply the same +0.1 translation to a slot the
   census proves visible (largest draws-affected). Receipts:
   `xform_stats`, per-replay xdiff/xdmax/xdmean table (expect nonzero
   here — quantify exactly where), `done`, guest/event counters,
   continuation hash. If THIS delta also moves zero pixels, that
   re-scopes the finding from "wrong slot" to "seam writes don't reach
   rendering" — table it, do not fix the seam.
4. **Projection-path reachability.** M7 recorded that `LoadXFReg` never
   fires the seam (regs `0x1020+`, projection lives there). Confirm by
   code path + a targeted run if one is orderable within the box
   (projection-word delta, same receipts as step 3, or the exact reason
   no run can reach it). Receipt: reachable/unreachable with the path.
5. **Report.** `local/research/M8/REPORT.md`: per-step tables (census,
   second-slot delta, projection reachability, wall rows, continuation),
   header diffs per step (`diff -u` against the M7 header), exact
   commands, player and run dir paths, "What I could not do". No
   verdicts. Commit `local/research/M8/` (header, driver copy, analyzer
   changes, REPORT, waits.log). Stop.
