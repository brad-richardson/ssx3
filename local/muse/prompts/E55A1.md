# E55A Part 1 — RTC seed path and deterministic clock contract

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. This is a read-only audit for the orchestrator's E55
determinism lane, not a fix. Read `local/research/X4B/REPORT.md` (partial
orchestrator audit, **not** a completed Qwen verdict) and
`docs/research/review-2026-09-23-frontier-2.md` §B. Hand back the table;
the orchestrator decides the implementation gate.

Pin fork source at `1aaed05` in
`~/dev/ssx3-work/E54D/PS2Recomp` (the top-level fork checkout is stale),
and canonical E54D codegen in `~/dev/ssx3-work/codegen-ssx3`. No fork
source edits, builds, boots, device access, or pushes. You may write only
`local/research/E55A1/REPORT.md` and ≤5 MiB bounded text receipts under
`~/dev/ssx3-work/E55A1/`. Do not read whole generated/source files into
context: use `rg`, `local/tooling/ee/ee-at`, `ee-func`, LSP edges and small
line ranges.

Known source edges from X4B to verify, not assume: `sceCdReadClock` in
`ps2xRuntime/src/lib/Kernel/Stubs/CD.cpp:696-729` uses host `std::time`
and localtime; generated `0x402520` calls the stub; guest `0x31ae80`
passes stack address and `0x31ae88-0x31ae98` folds two clock-buffer
words into an argument for `0x3177c8`; guest `0x317958` writes six words
near `0x4ff018`. These are a static data path; the seed function's
identity and dynamic reach are unproved. The correct behavior for
`PS2X_DETERMINISTIC=1` is a stable, fully specified clock reply while
default mode preserves the existing real-time reply. A constant guest
clock should yield the same **guest input bytes** across boots regardless
of host date/timezone. It alone does not prove full-frame determinism:
idle event order, pad state and memory card state are separate E55 work.

Evidence table:

1. Exact eight-byte `sceCdReadClock` reply layout and return/error
   behavior, including BCD conversion, local timezone and year format.
   Find every runtime call site/alias and tell whether this guest path
   actually invokes the HLE stub. Cite file/line and LSP connection.
2. Exact generated guest path from `0x31ae80` through `0x3177c8` and
   `0x317958` to each destination word. Record operations, addresses,
   and where a conclusion about RNG identity would need more proof.
   Distinguish a pointer/reference from the contents it points to.
3. Existing env/clock/test patterns that allow a narrow fixed-clock
   implementation. Recommend **one** deterministic UTC calendar value
   and expected eight output bytes, with a small hand-computed or scripted
   BCD/control table. Explain how to test default passthrough without
   hard-coding today's date and how to test repeated deterministic calls.
4. List any prior bounded runtime receipt proving `sceCdReadClock` is
   called on I26-FAST, or state reach unknown. No new boot.

Time box 20 minutes. Stop if a required edge cannot be confirmed; give
the evidence gap without guessing or silently widening scope. Deliver
`REPORT.md` with pins, exact commands, citations, receipt hashes and
gaps. Commit only the explicit report `[E55A1]` with trailer
`Orchestrated-By: Codex`; do not push. No edits to board/ledger/plans.
