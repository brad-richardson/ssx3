# X10B — sparse Qwen corrected pinned excerpt

You are a local opencode/Qwen **sparse** worker in `~/dev/ssx3`.
Follow `~/dev/AGENTS.md` worker rules. This is a bounded source-table
trial, not an E54 verdict. Write only `local/research/X10B/REPORT.md`.
No source edit, build, boot, device, LSP, web, push, directory Glob, or
other source read. Cite the supplied repo excerpt, not the web.

The orchestrator generated `local/research/X10B/source-excerpts.txt`
from exact symbol anchors using `git show 293fd81:<path>` and asserted
that all nine needed source tokens are present. The excerpt is 3.6 KiB.
The prior X10 trial used stale line ranges and was invalid. Do not use
the X10 report as evidence.

Read exactly three files: this brief, `source-excerpts.txt`, and the
report skeleton. Fill every `FILL` in the six rows. Keep
`dispatchIrq(false, ...)` INTC, `dispatchIrq(true, ...)` / queued DMAC
cause, and VU1 status bits distinct. Give exact file:line citations.
The timer loop covers 0..3. The excerpt contains the relevant VBlank
case labels and dispatch calls; do not infer a guest runtime hit. State
only the direct `dispatchIrq(false, ...)` causes visible in the excerpt
and whether a direct call for 5 or 7 appears. Do not claim a fork-wide
absence.

Within 3 minutes write the table, then run
`python3 local/tooling/orch/check_x10b.py`. The checker verifies pinned
input anchors and row semantics. Correct at most once if it fails.
Commit only the report (`git add -f -- local/research/X10B/REPORT.md`;
explicit path in `git commit`) with subject `[X10B]` and trailer
`Orchestrated-By: opencode`, no push. Hand back the table and tool/context
notes, no E54 verdict.

Budget: 8 minutes, ≤24k model context, ≤1 MiB report. At the cap,
stop and state the gap. Do not use directory discovery to locate the
three exact paths above.
