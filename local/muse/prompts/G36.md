# G36 — Barrier-vs-content wall: tag via raw access, no map_write, no commit (pure-refined-H2 vs H1-as-barrier, Odin)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`; working
tree: `/Users/bradrichardson/dev/ps2xGS` — VERIFY at start, re-pin if
the Mac-mini migration moved it, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts
beyond the hypothesis.** Read `local/research/G35/REPORT.md` first
(all of it: refined-H2 WINS — the tag renders EXACTLY with no
commit, 16/16 B1 tagged-B + 10/10 ladders + 10/10 scanouts ==
pre-registered tag model; H1-barrier refuted 0/10). This brief
executes G35 §4's ONE next action ONLY. No upstream contact of any
kind (standing no-upstream order — local hunks only, filing stays
local).

## Facts you start from

- The surviving split: G35 refuted H1-barrier but RETAINS
  `map_vram_write`'s flush+wait barrier — so H1-as-barrier ("the
  retained flush+wait is the necessary element") survives untested
  vs pure-refined-H2 ("the CPU stores alone suffice").
- Discriminator (G35 §4): place the SAME sparse 112-word tag via raw
  `begin_host_vram_access` + CPU copy with NEITHER `map_vram_write`
  NOR `end_vram_write` (no flush, no wait, no commit). The
  writeback receipt still checksums the tagged temp and G31-B still
  verifies the placed source. Pure-refined-H2 predicts TAG-VISIBLE
  (ladder `86ad7b887e140383`/229712 ×10, scanouts `d19e6beb…`);
  H1-as-barrier predicts CLEARED (ladder `aa2fa32572450383` ×10,
  scanouts black `99418f1b…`).
- Gated G35 pins (re-verify every one; committed REPORT scalars are
  the authority, surviving `/tmp` oracles cross-check only):
  tagged TB `c039a1d1c29cb293`/150057; cleared `aa2fa32572450383`/
  229376 + black `99418f1b…`; tag model `86ad7b887e140383`/229712
  + PPM `d19e6beb…` (112 whites); G35 binary 265,853,184 B
  `f3a33f7c…` (NOTE: fully-zeroed at gate time — 14th read
  artifact; re-pin with 2+ separated reads; table + stop ONLY on a
  validity-CHAIN mismatch, and the G35 binary is NOT in G36's
  chain); dump `154d9d85…`; clone HEAD `3a66c19…` with G35 ×3 /
  G34 ×0 / G33 ×0.
- Drive state (gated): link corruption is now PATTERN-DEPENDENT
  (same offset, different bytes by read pattern — proven same-day
  on the E-lane ISO). Standing rule: lane-critical SSD bytes need
  2+ matching reads separated in time AND independent corroboration
  (on-device shas, pinned committed bytes) wherever available;
  single-read SSD evidence is suspect. Mirror the new `ps2x-g36/`
  receipts to the share tier.
- Task 1 (no device until §2): STATIC design — table the raw-access
  shape (exact call sequence vs G35's W1: what opens the mapping,
  what is skipped, what closes it if anything), the premise rows
  (P1 writeback receipt MUST still read tagged temp — host-side;
  P2 B-at-sample-time B1/B2 split with pre-registered verdict
  meanings — WITHOUT map_write's flush+wait, do stores even reach
  the host mapping? table both + B-neither; P3 controls), the
  reformulated predictions (every scalar pre-registered), the
  predictor reuse (re-derive every scalar from the dump as a
  self-check), and the decision matrix (tag-visible vs cleared vs
  B2-no-verdict vs paradox vs premise-break vs mixed). Name the ONE
  hunk. Read G35's gap 13 (which memory the sampler reads) — this
  wall is what separates it; claim nothing beyond the hypothesis.
- Task 2 (ONE run max): the wall — ONE hunk max (G35's block
  excised + the raw-access tagged placement inserted in the same
  edit), ONE build max (NEW dir, G22 recipe, `-j2`), full identity,
  verify-then-push with NO gap (push-time + post-run + report-time
  re-shas), TWO bounded runs max (diagnostic + one retry iff O1/O3
  fires instead of first draw).
- Success bars (tabled): design table (shape + premise rows +
  predictions + matrix); the pure-refined-H2-vs-H1-as-barrier
  VERDICT (with the discriminating bytes); if a run: exit/wall/
  fate + receipts + tombstone triage (if any) + scanouts scored AND
  pulled by explicit filename list with cleanup as its own verified
  step (G31-E1 rule); the ONE next action (adoption stays queued
  per the standing rule until the orchestrator gates it; filing
  stays held).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g36/` transient
  dir ONLY, removed at end (`mg/` only). No P-lane lease, no
  bytesize/WSL. `COPYFILE_DISABLE=1` on all SSD steps.
- SSD clone: ONE hunk max in `gs/gs_interface.cpp` (G35 excised +
  G36 inserted, single edit); G22 + G26 + G28 + G29 + G30 + G31
  hunks untouched (HUNK_MATCH re-verified pre + post + pre-push +
  post-run + report); zero commits in submodule, zero in ps2xGS.
- Byte caps: NEW SSD build dir ≤ 6 GB; NEW SSD `ps2x-g36/`
  (retrieval by explicit list) ≤ 50 MB; all older SSD dirs 0 growth
  (pre + post `du -sk` snapshots must match exactly); internal
  volume ≤ 1 GB delta, no clones/builds; committed text only.
- Knobs: `--disable-sampler-feedback` SET, `PGS_SKIP_SAMPLER_FEEDBACK`
  UNSET, `PGS_SKIP_COMPILATION_TASKS=1` KEPT, sanitizer env ABSENT —
  the ONLY delta vs G35's run is the hunk-for-hunk swap.
- Stop: any validity-chain pin mismatch → table + stop (the G36
  binary and dump ARE the chain — G28–G35 binaries are
  corroboration only); no tuning loop, no second shape, no lldb
  unless a tombstone needs triage.
- Time box 6 h. Evidence `local/research/G36/` + `[G36]` commit +
  `Orchestrated-By: Muse Code` trailer, `git add -f`, NO push
  (orchestrator pushes at poll).
