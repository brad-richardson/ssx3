# N8D7M12 Part 5F4P3 — remove diagnostic cost when disabled

You are the OpenCode Go worker in `/Users/brad/dev/ssx3-work/N8D7M12P5F4/PS2Recomp`. Read `~/dev/AGENTS.md`, repo `AGENTS.md` and `local/AGENTS.local.md`, full `local/muse/prompts/N8D7M12P5F4P2.md`, and full `local/research/N8D7M12P5F4P2/{REPORT.md,ORCH-GATE.md}`. Cite local source, not web. First verify branch `n8d7m12-p5f4`, HEAD `6799681`, only untracked `local/receipts`, and no other heavy build/lease. Own only `ps2xRuntime/include/runtime/gs/gs_frontend.h`, `ps2xRuntime/src/lib/gs/gs_frontend.cpp` and untracked `local/receipts/N8D7M12P5F4P3/`. No other fork/root edit, Android build, Odin, push or upstream contact. Write REPORT early.

Gate finding: `GS::executeQueuedCommand` unconditionally calls `noteConsumedCommand`; that function locks `m_pktSeqMutex` **before** checking the default-off gate. The 41-sample Mac ON diagnostic passed, but an OFF build would still lock on every command and perturb normal timing. Correct model: OFF takes only a race-safe fast enabled check; ON retains exactly the prior digest/count and Fence semantics. A change in OFF replay output would require stopping, not attributing it to GPU state.

Make one minimal source fix: use an atomic enabled flag with a relaxed fast check before the mutex in the per-command path; preserve a guarded check after locking if toggles could race. The replay sets the flag before starting the worker, but keep the public setter race-safe. Preserve on-path hash/field encoding, sampled log grammar, default-off silence and all existing behavior. Do not refactor the queue or broaden instrumentation. Document why OFF does not acquire the new mutex per command. If this needs a wider API change, stop with a source-grounded table.

Budget: 25 min, ≤2 GiB new private bytes, ≤256 KiB receipts. At most one incremental Ninja build of `ps2x_tests`, one suite, and one same-stream Mac replay with `PS2X_GS_REPLAY_PKTSEQ` **unset**; one mini lease, ≤300 s wall, bounded log/output, own PID. Reuse P2's build and pinned 1,100,696,462-byte stream, but write private output under `~/dev/ssx3-work/N8D7M12P5F4/mac-fastoff/`. Require suite586/586/0, no `GB4_PKTSEQ` lines, exactly 41 existing replay rows, and hashes plus tick2050 PPM byte-equal to pinned prior Mac control. Two matching SHA reads for binary and stream. Diagnostic wall is not speed. First failed brief step stops validation; a typo in an exploratory command is not a brief failure. No tuning loop.

Before commit: `git diff --check`, exact changed-path list, `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty, no generated/game data. Add a **new** private source-only commit `[N8D7M12] Part 5F4P3` with `Orchestrated-By: opencode`; do not amend the already-gated `6799681`, and do not push. Save `REPORT.md`, `check.py`, `check-result.json` and bounded log excerpt in untracked `local/receipts/N8D7M12P5F4P3/`; orchestrator copies them. Hand back a table with code audit, build/suite/replay/cleanup, exact hashes, private commit and gaps. Do not declare an Android/GPU verdict.
