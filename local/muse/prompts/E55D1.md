# E55D1 — ExternalWake placement and pad/card boundary audit (Muse Contributor Go)

You are an OpenCode Go worker doing a **read-only** E55 source audit. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `local/research/E55B1/REPORT.md`, `local/research/E55C2/REPORT.md`, and the E55 subsection in `docs/todo.md`. Pin `~/dev/ssx3-work/E55C2/PS2Recomp` at expected `ddaee780288adb076ce40050d87969b20bc4bb05`; stop and report if the checkout differs. This task does not own the E/fork mutator lease. No source edit, build, boot, device, web, upstream contact or push.

Question: `PS2X_DETERMINISTIC=1` now uses guest-cycle event selection and a VBlank state hash, but ExternalWake has no known guest-cycle timestamp; pad merge and nonempty card state remain unisolated. Identify exactly where an external post enters, when it is drained relative to guest-cycle accounting and VBlank hashing, and which pad/card paths can change the hashed state or guest actions. Correct repeatability on equal inputs predicts the same dequeue boundary and state-hash prefix. Alternatives are host-arrival reordering, pad merge variance, and card contents/state; define one observable for each that distinguishes it from the others. Do not assign a guest-cycle placement policy in this part.

Deliver `local/research/E55D1/REPORT.md` and `source-map.tsv` (<80 KiB total). TSV columns: `path,producer,enqueue_or_state,drain_or_consumer,cycle_anchor,hash_relation,unknown`; 6–10 rows with exact source file and line ranges, plus a short table of three minimal discriminating experiments and null controls. Scope source reads to `EeScheduler.cpp`, its header, the pad/card implementation files that direct references actually lead to, runtime entry and hash-tap source. Confirm cross-file calls with LSP `findReferences`/`goToDefinition` if available; otherwise cite exact caller/callee lines. Use `rg` and small line-range reads; label any unverified route. A TSV shape checker may be included, but source semantics require orchestrator gate review.

Report pinned rev, searched files/commands, exact source anchors, disagreements with earlier E55B1 assumptions if any, gaps and recommended Part 2 question. Check ssx3 `git log -1`; commit only named text receipts `[E55D1]` with `Orchestrated-By: opencode`, no push. **Hand back evidence and the experiment table; do not declare determinism or choose a production policy.**
