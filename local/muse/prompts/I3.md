# I3 — Raylib decision spike: upgrade / fork-patch / SDL, with receipts (no fork writes)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. This is a
**read-only decision spike**: you evaluate I2's three raylib-wall options
with inspectable evidence and small throwaway probes. You change nothing in
the fork (READ ONLY — no commits, no pushes, no edits there) and make no
recommendation — **tables, no verdicts**; the orchestrator decides. Target
context: iphonesimulator arm64 + eventual iOS device (GLES vs Metal matters
for device, note it per option).

## Pointers

- I1 (`local/research/I1/REPORT.md`: G1/G2/G3) + I2
  (`local/research/I2/REPORT.md`: raylib-wall statement + option table).
- Pinned raylib 5.5 sources: `/Volumes/Extreme SSD/ps2x-ios-spike-attempt1/_deps/raylib-src`.
- rlImGui `Raylib_5_5` tag + imgui `v1.92.7-docking` pins constrain API compat.

## Gates and rules

- Host only, no `adb`, no device, no lease, no Simulator launch needed
  (probes are source/config-level; booting a sim adds nothing here).
- Network READS allowed (list tags, read changelogs, shallow-clone
  candidates to scratch); no pushes, no PRs, no `gh` writes.
- Disk: internal disk is tight — ALL probe clones/builds go on
  `/Volumes/Extreme SSD/` (reuse `_deps` where possible); nothing over
  500 MB on internal; never touch `/tmp/p1-link`, `/tmp/ps2xgs-build*`,
  `/tmp/ps2x-ios-spike`, or other agents' dirs. Simulator: use ONLY the
  installed iOS 27.0 SDK path for inspection; NEVER download runtimes.
- Evidence dir `local/research/I3/` (commit with `git add -f`, prefix
  `[I3]`, trailer `Orchestrated-By: Muse Code`). Do not commit probe
  outputs. NEVER run `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Option A — upgrade the pin.** Which raylib releases (5.5 → newest)
   have an iOS backend? For each candidate: platform enum accepts iOS?
   `rcore_ios`/UIKit backend present? GLES selection? rlImGui/imgui pin
   compat (API breaks between 5.5 and candidate)? Receipts: version
   table with file:line evidence per cell + the smallest viable pin
   (fewest API breaks) identified WITHOUT recommending it.
2. **Option B — fork-patch 5.5.** Size the backport: which files would
   an iOS backend need (windowing, input incl. touch, GLES, audio
   session), line-count proxies from the android/desktop backends, and
   whether miniaudio's CoreAudio path + existing `Wave`/`Sound` use
   suffice. Receipts: file table with line counts + the touch-input gap
   (G7) folded in.
3. **Option C — SDL.** Does raylib's `PLATFORM=SDL` path target the iOS
   SDK (inspect, don't build the world — a configure probe to the first
   error is enough, on the SSD)? What selects GLES? What happens to
   input/audio mapping + G7? Receipts: probe log tail + mapping table.
4. **Report.** `local/research/I3/REPORT.md`: the three option tables
   (evidence, effort proxies, open questions each leaves), exact
   commands, receipt paths, "What I could not do". No verdicts, no
   recommendation. Commit `local/research/I3/`, stop.
