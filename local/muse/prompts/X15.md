# X15 — dense Qwen branch facts from a pinned scanout excerpt

You are the **dense Qwen3.8-27B** opencode worker. Follow `~/dev/AGENTS.md` worker rules, repo `AGENTS.md`, and `local/AGENTS.local.md`. This is a local-model trial for bounded interpretive source reading, with no implementation or GS cause verdict. Read only this brief and `local/research/X15/excerpt.txt` (11,013 bytes, SHA-256 `eac602143522d2ec2fe3c6b062070ee822c32a5fecd258400a9e66a1a102c146`). The excerpt contains numbered lines from the pinned N8D5B private paraLLEl copy, N8D5L fork backend, and N8D5G metadata. Cite the repo excerpt, not the web. No search, other source read, checker read, build, boot, device, web, push or source edit. `local/research/X15/rows.tsv` and `REPORT.md` are your only write paths.

Within 60 seconds of reading the excerpt, write a TSV with header `field<TAB>value<TAB>evidence` and these eight rows **in order**: `EN1`, `EN2`, `INT`, `FFMD`, `force_progressive`, `is_interlaced`, `raw_circuit_early_return`, `deinterlace_branch`. Values are `0`, `1`, or `unknown` only. Interpret the pinned metadata `pmode=0xff21 smode2=0x1` through the cited bitfields and the backend's zero-initialized `VSyncInfo`; follow the actual branch predicates. For `raw_circuit_early_return`, use 1 only if every shown predicate permits that return; for `deinterlace_branch`, use 1 only if the shown branch executes. Evidence is one or more exact `filetag:line` anchors from the excerpt, separated by semicolons, with no prose. `unknown` is required when the excerpt does not establish the answer. Do not infer image quality or a driver fault.

Try **one** `lsp goToDefinition` at the actual fork file `/Users/brad/dev/ssx3-work/N8D5L/PS2Recomp/ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp` line 255 on `vsync`; record exact returned target or empty/error. This is a tool-behavior observation; it does not change the table. Do not read another file through LSP. Keep context under 18k and wall time under eight minutes. `REPORT.md` lists excerpt SHA, commands, elapsed time, context if visible, LSP result, table and gaps. No checker is available to you; hand back the table for independent scoring. Commit only `rows.tsv` and `REPORT.md` as `[X15]` with `Orchestrated-By: opencode`, no push.
