# I11 — Close the 0x395730 indirect-target gap, re-codegen, re-link, re-probe on iPad

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I10/REPORT.md` first (all of it: game
objects linked (9445 syms), I9 wall gone, CD wall removed by ISO+env,
guest loads 12 IRX + SIF handshake, NEW wall = indirect JALR from
`0x2322d4` to mid-function `0x395730` (inside emitted `sub_003956E8`
`0x3956e8–0x395750`, no CSV function in `0x395700–0x39573x`); gap 1 = this
brief). This brief executes I10's ONE next action. Reuse I10's worktree
shape + device toolchain + provision/install/ISO+env mechanism — table
every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I10 app is STILL INSTALLED — plain `install` overwrites
  (I10 closed I9 gap 3 twice). No fresh prompt needed for iPad installs.
  Confirm the iPad's state before installing (user sessions reported, not
  killed).
- BASE MOVE (required this brief): the E-lane has advanced `fork/ssx3` past
  I10's pin (E7 fix `4acc59f` is now the demonstrated baseline — the device
  runtime wants it eventually, and the wiring must live forward). Create a
  SEPARATE fork worktree on the SSD at `fork/ssx3` HEAD (table the hash —
  expect `4acc59f` or later; NEVER move it yourself). Port I10's `3d2e22d`
  wiring (1-file CMake, 22 insertions) onto it: cherry-pick if clean,
  else re-apply by hand + table the merged diff. Check whether E-lane owns
  the shared clone before touching it (one `worktree add` + read-only
  inspection only, I10 shape).
- The task: close the `0x395730` gap at its layer — Ghidra map/analyzer
  boundary fix (add the missing function entry) OR recompiler mid-function
  entry splitting + `entry_` wrapper — then re-run host `ps2_recomp`,
  re-link `ps2_game_objects` (`-DPS2X_GAME_CODEGEN_DIR`), rebuild the
  device runtime, install over the I10 app, and re-probe with the I10
  ISO+env recipe. The gap layer decides the fix; table the decision with
  forcing receipts (CSV/map reads, analyzer output, emitted-range census).
- Success bars (tabled, not verdicts): entry fix lands in the worktree
  (named files, local commit; fork-remote push ONLY if the E-lane is not
  mutating — else stays local, tabled); host codegen re-run exit 0 with
  the `0x395730` entry present (symbol + table-slot census); device Release
  build WITH game objects exit 0; overwrite install exit 0; argv-probe
  launch gets PAST `0x395730` (console + `.ips` + screenshot, tabled
  against I10's R3 fate); diagnosis names the next layer with evidence if
  a new wall appears.

## Gates and rules

- Subordinate to the E-lane fork owner (if live): builds `-j2`, never
  `-j4`; build trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i11/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`. Host `ps2_recomp` runs are codegen (no lease),
  bounded + tabled.
- Fork discipline: worktree only; named-files `git add`; fork commits
  pushed to the FORK remote only, never upstream (and no `ssx3` push while
  the E-lane mutates); generated sources never staged (recompiler OUTPUTS
  are build artifacts — table paths + shas, do not commit them).
- Device discipline (extends I6–I10 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I11/` (STANDALONE). Commit with `git add -f`,
  prefix `[I11]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — entry fix + host codegen + device link (no iPad yet)

1. Pin + create the worktree at `fork/ssx3` HEAD (table hash); port the
   `3d2e22d` wiring (table method + merged diff). Diagnose the gap layer
   (map/analyzer/recompiler) with forcing receipts; implement the entry
   fix (named files only).
2. Re-run host `ps2_recomp` (rebuild the tool at the new base ONLY if the
   old binary provably cannot run the new inputs — table the decision);
   table codegen exit/counts/shas + `0x395730` entry census.
3. Configure + build Release (`-j2`); table binary path/size/sha/identifier
   + table-populated proof (symbol census). Fix ONLY build breaks in the
   wiring class.

## Task 2 — overwrite install + re-probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I10 app (table overwrite behavior).
2. Ship ELF+ISO as in I10 (same mechanism unless the link changed the
   packaging — table deltas). argv-probe launch with ISO+env: console +
   `.ips` (sleep + re-`ls` before concluding none) + screenshot.
   Past-the-wall table vs I10 (`0x395730` gone? first post-entry code
   executes? new wall?).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled past the target: diagnosis table (failing layer + evidence).
   Attempt a fix ONLY if config-level — no second fork refactor in this
   brief (one gap per brief; the next gap gets the next brief).
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I11/REPORT.md`: contract, caps, entry-fix + codegen + link
record, install/probe tables, diagnosis, exact commands, gaps. Commit
evidence (`[I11]`), stop.
