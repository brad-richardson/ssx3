# N8D7M12 Part 5F4P2 — resume bounded GS delivery fingerprint

You are the OpenCode Go implementation worker. Your pane starts **inside** `/Users/brad/dev/ssx3-work/N8D7M12P5F4/PS2Recomp`; this fixes the previous repo-cwd `../*` edit denial. Read `~/dev/AGENTS.md`, the repo `AGENTS.md` and `local/AGENTS.local.md`, full `local/muse/prompts/N8D7M12P5F4.md`, full `local/research/N8D7M12P5F4/{REPORT.md,ORCH-GATE.md}`, and full `local/research/ARCH1/{REPORT.md,ORCH-GATE.md}`. Cite repo source, not web results; use LSP to confirm call relationships. First verify branch `n8d7m12-p5f4`, HEAD `a608ed1e161f60334a0cf3a80d1af3e54b692bd2`, clean source, and no unexpected active build. Write your REPORT early under this worktree at `local/receipts/N8D7M12P5F4P2/REPORT.md` (untracked and **never** add it to the fork commit). The orchestrator will copy bounded receipts into ssx3.

Goal and model: implement the dev-only default-off command fingerprint specified in the original F4 brief, at actual GS worker consumption, as one limited delivery sanity check. Under a fixed stream, owned payload copies and one FIFO worker, equal digest/count are expected; equality cannot prove GPU execution, PrivWrite contents or a GPU cause. A differing digest suggests delivery investigation; equal digest with differing output ends input-hash expansion and calls for a downstream state/readback probe. A nonreproducing pair is inconclusive. Added hashing may perturb timing. Do **not** build an Android package or use Odin in this part.

Allowed source edits only: `ps2xRuntime/include/runtime/gs/gs_frontend.h`, `ps2xRuntime/src/lib/gs/gs_frontend.cpp`, `ps2xRuntime/src/lib/gs/gs_replay_core.cpp`, and, if needed, `ps2xTest/src/ps2_gs_queue_tests.cpp`. Follow F4 REPORT §4 unless source inspection shows a concrete flaw; if broader API changes are needed, stop with a table. Use 64-bit digest plus count, explicit field encoding, hash PrivWrite kind only, exclude timing-dependent Fence from digest/count, handle quiescent drain, sample 41 replay markers, emit `GB4_PKTSEQ tick=<t> seq=<16-hex> commands=<n>` to stdout only with `PS2X_GS_REPLAY_PKTSEQ=1`. Preserve existing hashes format and default-off path. Test stable same-sequence result, order sensitivity, payload sensitivity and default-off silence without mirroring implementation code.

Budget: 40 min, ≤10 GiB private new bytes, ≤512 KiB receipt text. After source diff review: at most one Release/Ninja configure+build of `ps2x_tests` and one suite, then one pinned-stream Mac replay under one dynamic mini lease, ≤300 s wall, bounded logs/output, own PID. Use the exact flags/stream, SHA pairs and existing route from the original F4 brief and Part 1 report. Do not run if another heavy build conflicts. Require 585/585 or documented added-test count and exactly 41 `GB4_PKTSEQ` plus 41 existing replay rows. Diagnostic wall is not speed. First failed brief step stops validation; a typo in an exploratory command is not a failed brief step. If source edit permission still fails, stop immediately with its exact denial; no shell-edit workaround.

Before committing: `git diff --check`, exact changed-path list, and `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty. No generated/game data. Commit **only allowed source files** on the private branch with `[N8D7M12] Part 5F4P2` and `Orchestrated-By: opencode`; no push. In untracked `local/receipts/N8D7M12P5F4P2/` save `REPORT.md`, `check.py`, `check-result.json` and bounded log excerpts. Hand back a table with test/build/replay results, binary/stream SHA pairs, commit, gaps and cleanup. Do not declare an Android/GPU verdict; orchestrator gates and copies receipts.
