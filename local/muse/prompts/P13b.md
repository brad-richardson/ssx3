# P13b — Unmatched-selector validation + build identity manifest (read-only)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read first: `local/research/P13/REPORT.md` (§F3 + owners
table + §P13-6/11 containment greps — the 4 unmatched stub selectors) +
P1 Part 24 §P24-1f/g (179-site census, exclusion rules) + the TOML/CSV
sources + generator the selectors feed. This is the review's preventive
item: validate callback targets and actually-used selectors BEFORE the
next long run, boot-relevant first.

## Facts you start from

- The 4 unmatched stub selectors (P13 §F3): `_sceSifCmdIntrHdlr@0x426230`,
  `sceSifLoadIopHeap@0x42AD28`, `sceSifLoadFileReset@0x42B1F8`,
  `InitTLB@0x42CD58` — each mid-function inside a recompiled owner
  (181 stub selectors → 177 matched + 4 unmatched at P13 time).
- For EACH selector table: owner function + mid-function offset evidence;
  boot-relevance (fired in T26/K1 committed traces? callback target valid?
  check the K1-era traces, not just P13-era); consequence of silence
  (what breaks if it stays unmatched); validation verdict-shape is banned —
  instead: status + evidence + recommended disposition per selector.
- Identity manifest (ONE table): CSV rows ↔ TOML selectors ↔ generator
  version/behavior ↔ runtime binding state ↔ binary identity (which
  binary/HEAD each was read against). Re-derive P13's 181→177+4 counts
  against the CURRENT tree (table any drift with the reason).
- READ ONLY: `grep`/`sed`/`awk`/reads; `git` read-only (`log`, `show`).
  No fork writes/commits/pushes, no boots, no lease, no `adb`.

## Gates and rules

- `export COPYFILE_DISABLE=1` on every SSD step (reads only).
- Evidence dir `local/research/P13b/` (STANDALONE). Commit with `git add -f`,
  prefix `[P13b]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 3 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — selector validation (boot-relevant first)

1. Re-derive the 181→177+4 counts on the current tree (table drift or
   confirm).
2. Per-selector table ×4: owner + offset evidence, boot-trace firing
   (T26/K1 committed streams), callback-target validity, silence
   consequence, recommended disposition.

## Task 2 — identity manifest

1. ONE manifest table: CSV ↔ TOML ↔ generator ↔ runtime ↔ binary identity
   (with the rev/sha each column was read against).
2. Gap list: anything the manifest cannot pin (with the exact command that
   would pin it).

## Report

`local/research/P13b/REPORT.md`: contract, re-derivation table, 4 selector
tables, manifest, exact commands, gaps. Commit evidence (`[P13b]`), stop.
