# M42 — Big-|δ| unnamed sites: value/row listing beyond the headliners (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M35/REPORT.md` first (all of it —
M35 gap 3 is this brief, taken on orchestrator judgment: c332
extra (28), c318 extra (25), c351 extra (19), c314 missings
(45, 23) — the 5 largest |δ| among the 30, 4/5 non-cell on
the other frame), plus `local/research/M41/REPORT.md` (the
boundary census: s9 splits 1/14/7 + 2/2/4; the far side is
where these 5 live). Fully offline — SSD dumps read-only, no
harness code, no replays, no lease. No device work.

## Facts you start from

- The big-5 sites (M35 — reproduce exactly or table the
  mismatch and stop):
  - (305,332) off 391064, Y, |δ_9| 28, noncell on s0,
    gaps −62/−55, −− (c332 extra-only d1).
  - (241,318) off 309116, Y, |δ_9| 25, bulk on s0
    (|δ_s0| 2), gaps 93/11, ++ (c318 extra-only d3 —
    the one bulk among the 5).
  - (226,351) off 289982, Y, |δ_9| 19, noncell on s0,
    gaps −39/−39, −− (c351 extra-only d1).
  - (288,314) off 369268, Y, |δ_s0| 45, noncell on s9,
    gaps 107/84, ++ (c314 missing).
  - (289,314) off 370548, Y, |δ_s0| 23, noncell on s9,
    gaps 54/31, ++ (c314 missing).
- The question: beyond the headliners — do the big-|δ|
  sites share row geometry (edges? clusters?), gap-sign
  sides, or band/decile seats, or is each big site sui
  generis? Answer by table: the 5 sites + full top-|δ|
  listing (top 10 per side: are there near-big sites at
  16–18?) + row/band/decile seats.
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m35/m35.txt`
  (the 5 sites to reproduce — match exactly or table the
  mismatch and stop), `/Volumes/Extreme SSD/m34/m34-census.tsv`
  (FULL TSV guard). Work dir: new `/Volumes/Extreme
  SSD/m42/`; evidence `local/research/M42/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m41/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m42/` + `local/research/M42/`.
- Evidence dir `local/research/M42/` (STANDALONE). Commit with
  `git add -f`, prefix `[M42]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M41 precedent).

## Task 1 — big-5 recompute (do the 5 sites reproduce?)

The 5 sites recomputed from the dumps (must match M35
exactly — offsets, |δ| both frames, statuses, signed gaps
both frames, gap signs — or table the mismatch and stop):

1. Per-site table: all 5 with pins + the 4/5-noncell count.
2. Rank table: each site's |δ| rank among s9's 22 extras /
   8 missings (is 45 the max? is 19 the 5th?).
3. Headliner-overlap table: which of the 5 sit in
   already-headlined cells (c314/c315) vs unheadlined cells.

## Task 2 — beyond the headliners (top-|δ| listing + seats)

1. Top-10 per side: s9 extras top 10 by |δ_9| + missings
   top 8 by |δ_s0| with statuses/gaps/signs (near-big
   sites at 16–18?).
2. Row geometry: each big-5 site's row vs its cell's s0/s9
   row-lists (edge? interior? singleton?) + pairwise row
   distances (cluster?).
3. Band/decile seats: each big-5 site's band + gradient
   decile (M35 §Task 2 joins recomputed for the 5: do big
   sites share seats?).

## Task 3 — controls + determinism (M18–M41 precedent)

1. Positive control: synthetic truth with KNOWN big sites
   (N far-|δ| sites at known ranks + N near-big decoys) —
   recover the top-|δ| listing + ranks exactly.
2. Determinism: re-run Task 1 on s0+9, byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M41).

## Report

`local/research/M42/REPORT.md`: DESIGN.md (bars first) +
big-5 tables + listing/seat tables + controls +
explained-vs-standing update (attribution, not explanation —
table it) + gap rows. Screenshot only if a big-|δ| map
discriminates (budget-noted, pre-registered split required
per M23–M41 precedent). Commit evidence (`[M42]`, no push),
stop.
