# N8D Part 2 — same-run race host upload versus Odin screen

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §6. Read N8D1's full report and brief before starting. This is **one no-build race capture** using N8D1's already packaged diagnostic APK. Write scope: `local/research/N8D2/` and `~/dev/ssx3-work/N8D2/`; source/APK inputs are read-only. No fork edit/push, upstream contact, iPhone action or tuning loop.

N8C2 reached race tick 2087 with live GIF/presents, but its race screencaps were mostly black. N8D1's menu probe captured a recognizable host mountain image at tick 781, broad black blocks in another host upload at tick 810, then a readable screen at tick 861. That transition mismatch prevents a stable image-stage verdict. At race, the Mac GB6C paraLLEl image at tick 2050 shows a recognizable world/HUD (though dark composite remains). The single N8D2 question is whether a *same-run, settled-race* host upload is already bad when the Odin screen is bad.

**Predictions:** if a host upload around race tick 2050 is recognizable while the nearby screencap is fragmented, look at `UpdateTexture`/raylib/GL presentation. If both show matching gross black/fragmented regions, the defect is at or before host upload; a later probe must separate Turnip scanout, readback and frontend packing. If the visual scene differs too much by tick to compare, or the PNG writer fails, report the gap. Do not infer exact pixel equality, gralloc cause, Turnip API/driver identity or speed.

Pin N8D1 APK SHA `ece8b84ce3bce6bb85e42e21c432f6a6123ccae6e61dd91293e9da696fc63ae2` (two reads), package runner/Turnip/shim SHAs and flags from N8D1's APK receipt, stock ELF `1b49d05c…67af7bc`, ISO `3c2f8eb1…ae3f5` (take full pins from N8D1/N8C2 receipts). Copy only the N8D1 launcher into a fresh N8D2 work dir and adapt its receipt paths, lease label, and capture schedule. Its `ExportImageToMemory` writer is already in the APK: **zero builds**. Set `PS2X_FRAME_DUMP_ONCE_TICKS=1840,1950,2050`, same parallel/Turnip/movie/I26-FAST env and empty card. Make the script wait for a `[frame:dump]` race tick ≥2050, then take one `screencap -p` promptly; stop by tick 2100 or first definitive failure. Guest tick and host upload metadata must be recorded separately. Route cap 180 s, hard cap 240 s; one install and launch only.

Odin `622c49b1`: before launch verify connected, lease free, keyguard `showing=false`, battery ≥20% charging/full. Claim the lease, **always install** this APK, verify installed APK and stock ELF/ISO by two device SHA reads each, verify empty card and dump dir, then launch. Pull at most three selected PNG+txt host pairs and one screencap, all SHA paired; decode and view each. Always force-stop after the run, verify PID absent, release lease. No second launch if captures miss.

Acceptance script: from receipt JSON and files, check APK/pin SHA pairs, exactly one install/launch, ≤3 host pairs, PNG decode and dimensions, each file's device/local SHA pair, capture metadata ticks, force-stop/PID/lease and bounds. Hand back a table of tick, host image, nearby screen image, visual regions and the timing gap; the orchestrator decides the gate. Deliver `local/research/N8D2/REPORT.md`, exact commands, bounded same-PID log, env, SHA/acceptance receipts and ≤4 small images. Budget ≤1 GiB Mac scratch, ≤16 MiB logs, ≤12 MiB images, one ≤240 s Odin launch. Commit explicit report/receipts `[N8D2]` with `Orchestrated-By: Codex`; no push.
