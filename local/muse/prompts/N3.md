# N3 — First PS2-recomp boot on the Odin: env-file shim + bypass on the Android build, install, launch to the title screen

You are muse in a herdr panel in the ssx3 repo (repo root = two up from
`local/muse/prompts/`). **Tables + receipts; recommend, the orchestrator
decides.** Read first: `local/research/N2/REPORT.md` (APK builds green
with the codegen-dir port on local branch `n2-android` on bytesize;
entry chain), `local/research/N1/REPORT.md` §P1–P8 and H4/H5/H6/H7
(paths and env vars that NativeActivity can't set),
`local/research/E29/REPORT.md` (the Mac bypass reaches the title
screen; `PS2X_SKIP_MOVIE=1`), and `AGENTS.md` + `local/AGENTS.local.md`.

## Goal

Get the stock title running on the Odin 3 through the PS2 recomp runtime
for the first time, with the dev-only movie bypass, and capture what it
shows. Target: the SSX 3 title screen on the Odin display, or the precise
first blocker. It uses PS2Recomp's CPU GS backend, so speed isn't a bar;
record it anyway.

## Mission 1 — make env-driven config settable on Android (bytesize, local branch)

On `n2-android` (bytesize, local, never pushed):
1. Cherry-pick the dev-only bypass and pad script from `fork/e29-movie-bypass`
   (`ee39b9f` and its parent; fetch `fork` first). Those two commits only.
2. Add an Android-only env-file shim, compiled only `#if defined(__ANDROID__)`,
   before `main()` reads anything. It reads `<app files dir>/ps2x.env`,
   `KEY=VALUE` lines with `#` comments, and calls `setenv` for each, then
   logs every key it set to logcat under tag `ps2x`. The files dir is the
   same directory `PS2X_DEFAULT_BOOT_ELF` points into. This one mechanism
   covers H4 (`PS2X_CD_IMAGE`), H7 (diag/frame-dump vars) and the dev
   flags without touching runtime semantics. Put it in
   `ps2xRuntime/src/lib/ps2_android_runtime.cpp` (currently a stub) or
   the entry glue. Keep it small, and add a host-side unit test for the
   parser if the suite can compile it.
3. Package **arm64-v8a only** (drop x86_64 for this build) and rebuild.
   Record size, sha and the `nm` check that the shim and the bypass
   strings are present.

## Mission 2 — stage and launch on the Odin (device lease)

G41 may be using the Odin: wait for `/data/local/tmp/mg/LEASE` to be free,
claim it, and release it at the end. Battery ≥ 20 % and charging. Check the
Odin is unlocked (PIN entered since its last reboot) before the APK
launch. If it's locked, stop and say so in the pane (Brad enters the PIN).

1. `adb install` the arm64 APK (plain `install`, even if present).
2. Stage into `/storage/emulated/0/Android/data/com.ps2x.runner/files/`:
   `SLUS_207.72` (ELF `1b49d05c…`, 3,890,784 B), the ISO (`3c2f8eb1…`,
   3,005,415,424 B; same sources I24 used, see its §Task-2), and
   `ps2x.env` with `PS2X_CD_IMAGE=<files>/SSX3.iso`,
   `PS2X_SKIP_MOVIE=1`, `PS2X_FRAME_DUMP_DIR=<files>/frames`. Verify the
   pushed ISO's sha on the device.
3. Launch with `am start`, and capture logcat (`-s ps2x raylib` plus the
   crash buffer) to the SSD. Let it run up to 5 min, taking `screencap`
   every 20 s.
4. Pull the frame dumps and screencaps. Bars, in order:
   (a) the app starts and the `ps2x` tag shows the shim's keys (H5, H6);
   (b) the ELF loads and the guest starts (runtime log lines);
   (c) the movie bypass arms (`[MPEG:DEV-SKIP-MOVIE]`);
   (d) the title screen renders (frame dump or screencap).
   Record wall time to each bar, and CPU/thermal from `dumpsys cpuinfo`
   and `/sys/class/thermal` once a minute.
5. If `PS2X_PAD_SCRIPT` from E31 got to menus on the Mac, one extra
   launch with it is allowed.

## Rules

- One device agent at a time; lease protocol. Budget: 3 launches.
- Nothing pushed anywhere; `n2-android` stays local. No generated code
  or game data in any git repo.
- APK and captures on the SSD under `n2-android/` (two matching SHA
  reads for the APK before install); evidence `local/research/N3/`
  text only.
- Stop at the first blocker, table it exactly (last logcat lines,
  tombstone if any, thread states if the runtime prints them), and
  recommend. Time box 6 h. `[N3]` commit with `Orchestrated-By: Muse
  Code`, `git add -f`, no push; check `git log -1` first.

## Deliverable

Shim diff + test; APK identity; bar table with times and frames;
performance notes; the next blocker, if any.
