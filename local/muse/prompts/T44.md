# T44 — accumulate again (crash-lottery tolerance: stop at first crash-free run)

Goal: one crash-free T41-shape run scored by ROI + gradient in parallel.
T43 R1 crashed (tree at npre1 → whiteout npre2 → wall tumble), freezing its
Task 1; the distributions below carry forward unchanged.

## Carried distributions (DO NOT re-tune, append only)

- ROI no-input floor: n=3, cx {+19.7, +1.8, +11.9}, all |cx|<=20, μ=+11.1
- Gradient no-input floor: n=7, all |cx|<=20
- Gradient accumulation pool: 10 on→on pairs (|Δcx|<=35, |Δcy|<=29)
- Hold hop: n=1, crash-confounded (T43: ROI +34.3/−25.1 borderline; gradient
  −71.9/+26.4 off-rider; SCPS40 −40@−0.75, SCPS120 −120@−0.05)
- d7-class intruder: dual-gated VALID, rider-mixed signal (T42) / off-rider
  (T43) — lock-QA-gated, never auto-counted

## Recipe (T43 §T43-7 pattern)

1. Copy T43's six scripts to `t44-*` with renames ONLY (same rename audit as
   T43: every diff line contains t43-/t44-/T43/T44; `bash -n` clean; zero
   `sham` refs; exactly one `press_hold Left`; 14× `sleep 0.5`).
2. FIX the rename miss T43 found: `t43.` log paths (`xvfb-`, `boot-`) must
   become `t44.` or R1 will overwrite T43's on-box boot logs. Committed T43
   evidence is immutable; don't touch it.
3. Pre-run pins (H51 if alive, else new boot pinned the T43 way): btime,
   binary sha, tree HEAD, inputs/NVM (`da021d2a…`, mtime must stay Sep 20
   15:20), bindings grep, dialogwatch v2, PPM self-check 0.0000/0.
4. Run T44 R1 (61 snaps: npre×2, hold, dense×10, x-post×40, sp/mr).
   - Crash-free (no tree/wall contact in npre window or hold hop): score with
     all three trackers, append to the distributions, write REPORT.md — DONE.
   - Crash-confounded (T42/T43 pattern): full 3-row crash receipts (chain /
     gradient / SCPS tables + forensics), then run R2 ONCE with the same pins.
   - R2 crash-confounded too: freeze Task 1 again, report both runs, STOP
     (return the recipe to the orchestrator; do not run R3).
5. Score T42 R3 + T41 R4 with the T44 tools as controls (expect: T41 34/34,
   T42 retuned 12/12, gradient T42-calibration table, SCPS rails/hold rows;
   the stale T42 frozen/middle cells stay stale — do not "fix" them).

## Frozen

- ROI: x[220,420] y[180,360], DARK_MAX 80, gate 200–6000.
- Gradient: GRAD_MIN 60 (Sobel |Gx|+|Gy|), gate 500–3000.
- SCPS: y[380,460] x[150,540], ±40/±120, untouched.
- Trackers `t41/t42/t43-track.py` byte-identical (shas 58afa004/a951f782/
  2e532a2d first-8); T43 dir read-only reference.

## Receipts (same set as T43)

Stream B+sha + head/tail shas, poll 168 + T44_DONE + full-precision hold
walls, census EE/IOP distinct + top rows, 61 snaps + sizes, staged tool shas,
NVM post-run FULL sha (T43 filed first-16 only — close the gap), C: post-run.

## Errata in (T43-E1)

- T43 R1 npre2 gradient centroid row (343.5/189.2) does not reproduce from the
  committed bytes (re-run: 372.4/229.6, npix=5 both, INVALID both).
  Classification identical, snap excluded either way — no verdict impact.
