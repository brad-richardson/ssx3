# M6 — Host replay milestone 2, item 2: event-bus split + frame-aging + record guard, desktop only

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/M5/REPORT.md` (all of it: the step tables, the `dframe`
+1 mystery, the skipped continuation, "What I could not do") and
`local/research/S2/REPORT.md` Part 4 (§A–§E inventory and "What a
ReplayContext still has to own" items 2–4 — this brief IS those items).
Base header `local/research/M5/m5_replay_context.h` (sha256 of the
committed file — record it; M5's brief pinned a stale S2 sha, so trust
`shasum`, not memory). Evidence dir `local/research/M6/` (commit with
`git add -f`, prefix `[M6]`, trailer `Orchestrated-By: Muse Code`).
Large outputs (player dirs, run dirs, jsonl over 5 MB) go under
`/Volumes/Extreme SSD/m6/` with a symlink-free path recorded in the
report. Time box 6 hours.

## Rules

- Desktop only, no `adb`, no device. Do not touch the vendor tree,
  `native/`, `tools/`, or `local/native/runtime-build/`. All code lives
  in `local/research/M6/` as a research header derived from M5's (copy
  it to `local/research/M6/m6_replay_context.h`, keep every M5
  mechanism: PE mask, verbatim execute stream, restore, stall, pipe
  snapshot, watched-window re-hash, `done`, XFB hash + scratch
  redirect, side-effect counters).
- Builds may run any time (`-j4`); the desktop runs only while
  `/tmp/ssx3-host-lease` is absent: claim it with `printf 'M6\n'`,
  remove it after each run; if it names another agent, poll every 5
  minutes and log waits to `local/research/M6/waits.log`. Do not
  commit run outputs.
- Copy `local/research/M5/build_replay_player.py` to
  `local/research/M6/build_replay_player.py` and change only the header
  path it compiles in. Runs use `tools/gamecube_schedule_check.py`
  exactly as M5 did (`--immediate-xfb --resolution 640x528 --game
  local/game/gxbe69-stock --seconds 240`), profiles `m6-det` (dual core
  + `SSX_S2_FORCE_DETERMINISM=1`, `--cpu-thread`) and `m6-single`.
- NEVER run `git push` in this repo (the orchestrator owns origin).

## Steps (each adds one mechanism; rebuild + one `m6-det` run each; keep every earlier receipt)

1. **Baseline.** Build the unmodified M5-header copy, run `m6-det`
   once, confirm `analyze.py` prints 200 rows + `done` +
   `xfb_equal_scratch=200/200` + `live_xfb_untouched=1` (M5's end
   state reproduced). Control for every later step.
2. **Scoped `after_frame_event` suppression.** During the 200-replay
   sequence only, prevent the live listeners (S2 §C table) from
   firing — unregister, gate flag, or scoped bus, whichever the public
   API allows with no vendor change (if none does, record exactly what
   blocks it and skip to step 5 with steps 2–4 as unresolved — do not
   force it). The header's own counter still counts every trigger.
   Receipts: per-replay `dafter_live=0`, `dframe` during the sequence,
   `dpend` min/max across the sequence (the S2 §A tension: suppression
   removes the in-stream `FlushEFBCopies` points — measure whether the
   deferred queue grows), `dtex` accumulation vs M5's -7…+1 (eviction
   never fires while aging is frozen — measure it), `done` still
   stands.
3. **Frame-aging semantics + the +1 driver.** With the bus suppressed:
   `FrameCount()` frozen during the sequence (receipt: `dframe=0` per
   replay), replay-local frame counter in its place. Then chase M5's
   unresolved `dframe` +1/replay: presenter event tracing to name the
   writer (`ViSwap`/`ImmediateSwap` call sites during the sequence).
   Receipt: the driver named with file:line + trigger, or the narrowed
   unresolved with what was ruled out.
4. **Fail-closed `g_record_fifo_data` guard.** M5 asserted false; M6
   enforces: sample before every replay; if ever true, emit
   `record_flag_set` and stop the sequence (receipt: the stop event +
   partial-row count). Negative control: one run with a recording
   armed (if arming is possible with no vendor change) proving the
   guard fires; if not possible, say so and skip, do not force it.
5. **Report.** `local/research/M6/REPORT.md`: per-step tables
   (baseline vs each step: wall med/p95, `xfb_equal_scratch`,
   `live_xfb_untouched`, counter summaries, `dpend` growth,
   suppression mechanism or its blocker, +1 driver, guard behavior,
   continuation `done`), header diffs per step (`diff -u` against the
   M5 header), exact commands, player and run dir paths, "What I could
   not do". No verdicts. Commit `local/research/M6/` (header, driver
   copy, analyzer changes, REPORT, waits.log). Stop.
