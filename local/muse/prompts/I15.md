# I15 — Close the 0x3a0158 indirect-target gap, re-codegen, re-link, re-probe on iPad

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/I14/REPORT.md` first (all of it: base
stayed `be0c9ee` + cherry-picks + 1-line CSV entry closed `0x243a80`
(count 0, guest strictly past it), guest reaches a STRICTLY LATER wall:
indirect JALR `0x39cd74 → 0x3a0158` (mid-function of emitted `sub_003A0048`
`0x3a0048–0x3a0290`, head after `jr $ra` @`0x3a014c`/nop @`0x3a0154` — no ds
lines emitted at either `jr`, tail `jr $ra` @`0x3a0284`/nop `0x3a028c`,
observed bounds `0x3a0158–0x3a0290`, caller `jalr $v1` @`0x39cd74` with
vtable-loaded target, 0 register refs — the container mention is
fall-through emission, not an edge); gap 1 = this brief). This brief
executes I14's ONE next action — the same 1-line class as I11/I12/I13/I14.
Reuse I14's worktree shape + device toolchain + provision/install/ISO+env
mechanism — table every delta.

## Facts you start from

- Target: Brad's iPad, iPad Air 11-inch (M2), UDID
  `00008112-001224302184A01E` (`xcrun devicectl list devices` to
  re-confirm). The I14 app is STILL INSTALLED — plain `install` overwrites
  (closed 6× now). No fresh prompt needed for iPad installs. Confirm the
  iPad's state before installing (user sessions reported, not killed).
- BASE: create a SEPARATE fork worktree on the SSD at CURRENT `fork/ssx3`
  HEAD (table the hash — the E-lane may have advanced past `be0c9ee`;
  NEVER move it yourself). Port I14's 6 local commits (plist, CMake wiring,
  `0x395730` row, `0x14f2a8` row, `0x156750` row, `0x243a80` row —
  cherry-pick if clean, else re-apply by hand + table the merged diff).
  Check whether the E-lane owns the shared clone before touching it (one
  `worktree add` + read-only inspection only, I14 shape).
- The task: add the missing function entry for `0x3a0158` (I14's observed
  row `sub_003A0158,0x3a0158,0x3a0290,0x138` — RE-VERIFY the `jr $ra`
  @`0x3a0284`/nop bounds from the emitted slice BEFORE writing the row;
  table the verification), then re-run host `ps2_recomp`, re-link
  `ps2_game_objects` (`-DPS2X_GAME_CODEGEN_DIR`), rebuild the device
  runtime, install over the I14 app, and re-probe with the ISO+env recipe.
  One gap per brief: if a SECOND new wall appears, table it as the next
  gap — do not close it here.
- Success bars (tabled, not verdicts): entry fix lands in the worktree
  (named files, local commit; fork-remote push ONLY if the E-lane is not
  mutating — else stays local, tabled); host codegen re-run exit 0 with
  the `0x3a0158` entry present (file + table-slot census); device Release
  build WITH game objects exit 0; overwrite install exit 0; argv-probe
  launch gets PAST `0x3a0158` (console + `.ips` + screenshot, tabled
  against I14's fate); diagnosis names the next layer with evidence if a
  new wall appears.

## Gates and rules

- Subordinate to the E-lane fork owner (if live): builds `-j2`, never
  `-j4`; build trees on the SSD only (`/Volumes/Extreme SSD/ps2x-i15/`);
  allocated-byte caps declared up front + tracked (ExFAT). No PS2 boots →
  no lease. No `adb`. Host `ps2_recomp` runs are codegen (no lease),
  bounded + tabled.
- Fork discipline: worktree only; named-files `git add`; fork commits
  pushed to the FORK remote only, never upstream (and no `ssx3` push while
  the E-lane mutates); generated sources never staged (recompiler OUTPUTS
  are build artifacts — table paths + shas, do not commit them).
- Device discipline (extends I6–I14 standing rules): app fate from console
  text + `.ips` presence, never the tool exit code; screenshots to an
  internal path first, then `mv`; `devicectl` with explicit `--timeout`;
  short console timeouts; `._*` purge before configure + staging (ExFAT).
- Evidence dir `local/research/I15/` (STANDALONE). Commit with `git add -f`,
  prefix `[I15]`, trailer `Orchestrated-By: Muse Code`. Do not push (the
  orchestrator pushes at poll when the tree is clean).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — entry fix + host codegen + device link (no iPad yet)

1. Pin + create the worktree at `fork/ssx3` HEAD (table hash); port I14's
   6 commits (table method + merged diff). Re-verify the `0x3a0158` bounds
   from the emitted slice; write the CSV row (named add, local commit).
2. Re-run host `ps2_recomp` (rebuild the tool at the new base ONLY if the
   old binary provably cannot run the new inputs — table the decision);
   table codegen exit/counts/shas + `0x3a0158` entry census.
3. Configure + build Release (`-j2`); table binary path/size/sha/identifier
   + table-populated proof (symbol census). Fix ONLY build breaks in the
   wiring class.

## Task 2 — overwrite install + re-probe (iPad)

1. Confirm the iPad's state (user sessions reported, not killed). Install
   over the I14 app (table overwrite behavior).
2. Ship ELF+ISO as in I14 (same mechanism unless the link changed the
   packaging — table deltas). argv-probe launch with ISO+env: console +
   `.ips` (sleep + re-`ls` before concluding none) + screenshot.
   Past-the-wall table vs I14 (`0x3a0158` gone? first post-entry code
   executes? new wall?).

## Task 3 — diagnose (+ opportunistic config-level fix only)

1. If walled past the target: diagnosis table (failing layer + evidence).
   Attempt a fix ONLY if config-level — no second fork-code gap in this
   brief (one gap per brief; the next gap gets the next brief).
2. Either way: hypothesis verdict + the ONE next action (with the exact
   next brief it needs if the wall is app/fork-code class).

## Report

`local/research/I15/REPORT.md`: contract, caps, entry-fix + codegen + link
record, install/probe tables, diagnosis, exact commands, gaps. Commit
evidence (`[I15]`), stop.
