# N8D7M12 Part 6M3 — prune excluded cache trees in source snapshot

You are the OpenCode Go tooling worker in `~/dev/ssx3`. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P6M2/{REPORT.md,ORCH-GATE.md}`, and `local/tooling/orch/source_manifest.py`. Own only that script, `local/research/N8D7M12P6M2/check.py` (add a meaningful fixture case), and `local/research/N8D7M12P6M3/` for REPORT/check result. No board/ledger/fork/config edit, package, device, emulator, network, mini lease or push. Time box 15 min, ≤128 KiB receipts. Write REPORT early.

Gate finding: `scan_scope` skips `build`, `.git`, `.cxx`, `.gradle`, `__pycache__`, but pre/post `list_tree` walks inside them. The orchestrator demonstrated `list_tree` contains `build/volatile.o`. An excluded cache file changing during the scan can falsely invalidate a manifest, and large build trees still cost a metadata walk. Correct model: pre/post source-stability checks compare exactly the included paths; excluded names are pruned before descent and omitted as files. Included-file mutation still fails.

Make the minimal `list_tree` change: filter `dirnames` in place and skip excluded filenames using the existing exclusion rule on scope-relative POSIX paths; never follow symlinks. Keep stable ordering and all manifest entry/aggregate behavior. Extend the P6M2 fixture to prove `list_tree` omits excluded entries and a change to an excluded cache file during an included file's two-read phase does **not** fail snapshot; also prove a change to an included file still fails. Rerun all existing fixture cases. Then do one **bounded, read-only real-source sample** using four small existing source directories (not the full fork/codegen trees), with explicit `--max-files` and `--max-bytes`; snapshot + verify, record counts, aggregate and exact paths used. If a safe four-root sample is unavailable, state that gap and do not broaden the scan. No binary/game bytes in receipts.

Commit only allowed script/checker and `local/research/N8D7M12P6M3/{REPORT.md,check-result.json}` with `[N8D7M12] Part 6M3` and `Orchestrated-By: opencode`; no push. Hand back the table and gaps, not package/GPU verdict. The orchestrator decides whether full-root use is ready.
