# I26 — Check-in quality of life: fast auto-route, no pink startup, then virtual controls

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md` (**iPhone: install only, never launch/test**; test on the
  Simulator, then the iPad);
- `local/AGENTS.local.md`;
- `local/research/I25/REPORT.md` (build-install.sh, sim-run.sh, env shim,
  Settings.bundle).

## Brad's check-in feedback (09-23 evening, from his iPhone)

- "A fully pink screen at startup for about a second."
- "The auto-run has a lot of waiting time that could be cut, probably by
  90%."
- "Would be great to add virtual controls for me to use in the future."

## Work

Worktree `~/dev/ssx3-work/I26/PS2Recomp`, branch `i26-qol` off `i25-ios`
(rebase onto fork `ssx3` if it moved). Host etiquette: `nice -n 10`, and
wait while another lane's build is running (E53 builds too).

1. **Pink startup.** `ps2_runtime.cpp:535` presents
   `GenImageColor(FB_WIDTH, FB_HEIGHT, MAGENTA)` when no guest frame
   exists. Make it black by default, and keep magenta behind a dev env
   (`PS2X_FALLBACK_MAGENTA=1`). All platforms. Unit test if the code allows.
2. **Fast auto-route.** The E33 route script's timings were set when the
   runtime crawled (Select Character at ~0.4 ticks/s). Since E46/E50 the
   guest runs about 50× faster there.
   - Measure where the route waits: vsync timestamps of each screen on
     the Mac (frame dumps plus snaps) vs each scripted press.
   - Compress the script so each press lands shortly after its screen
     becomes interactive, with a safety margin: first press ≥ 1 s after
     the screen appears, confirmed by a frame-hash change.
   - Target: title → race in ≤10% of today's time. Validate twice on the
     Mac (same result both runs; the route must reach the race HUD) and
     once on the Simulator.
   - The E-lane briefs keep the old E33 string unless told otherwise. Put
     the fast route in the iOS bundle `ps2x.env` **and** as a named
     string in `local/research/I26/ROUTES.md` for later briefs.
3. Rebuild with `build-install.sh`: **install on the iPhone (install
   only)**, and check it on the Simulator.
4. **Virtual controls (design + first cut, after 1–3 pass):**
   - an on-screen overlay drawn by the host (not in the guest frame): a
     D-pad, the four face buttons, Start/Select, L1/R1/L2/R2;
   - multi-touch, hidden automatically while a game controller is
     connected, with a Settings.bundle toggle;
   - feeds the same pad state as the keyboard/gamepad union (N6/I25),
     with the script still taking precedence when set.

   Keep it simple (raylib draw + SDL touch events). Check it on the
   Simulator (mouse clicks as touches) that START/cross drive title →
   menu. Don't hand-tune the layout; give Brad a screenshot.

## Budgets

Builds as needed, ≤6 Mac boots, 6 h, cap 10 GB.

## Deliverable

`local/research/I26/REPORT.md` with the route timing table (before and
after), screenshots, and the "how to use" update for Brad. `[I26]` commit
(`git add -f local/research/I26 local/muse/prompts/I26.md`, `git log -1`
first, trailer `Orchestrated-By: Claude Code`). Fork commits stay on
`i26-qol`; no push. The pink fix is a candidate for `ssx3`.
