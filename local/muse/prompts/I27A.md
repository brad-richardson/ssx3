# I27 Part 1 — iOS HiDPI drawable, Simulator gate

You are a Codex Sol worker. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/orchestration.md` §§4/6. Start only after the N8D1 runtime source mutation is complete: one live PS2 runtime mutator. Write scope: `local/research/I27A/` and a fresh local fork worktree under `~/dev/ssx3-work/I27A/`. No Odin, iPad or iPhone action in this part; no fork push. Do not touch generated guest code, game data or other worktrees.

Pin `fork/ssx3` at `ddaee780288adb076ce40050d87969b20bc4bb05` (re-read remote before branching). W1F2's Simulator baseline is `[ios-window] window=874x402 drawable=874x402` in `~/dev/ssx3-work/W1F/run-w1f2/console.log`, and its title/Setup Character/race images are known. The pinned raylib source `~/dev/ssx3-work/E46-build/_deps/raylib-src` at `c1ab645ca298a2801097931d1079b10ff7eb9df8` maps `FLAG_WINDOW_HIGHDPI` at window creation to `SDL_WINDOW_ALLOW_HIGHDPI` (`src/platforms/rcore_desktop_sdl.c`). Current `ps2_runtime.cpp` sets only `FLAG_WINDOW_RESIZABLE` before `InitWindow`; `ps2_ios_runtime.mm::syncWindowSize` logs window and drawable sizes. Read those exact sources and W1F2 report before editing.

**Correct behavior model:** one iOS-only `SetConfigFlags(FLAG_WINDOW_RESIZABLE | FLAG_WINDOW_HIGHDPI)` change should make `SDL_GL_GetDrawableSize` exceed `SDL_GetWindowSize` on the Retina Simulator while the guest frame stays 640×448/512 and the W1 aspect/pad geometry remains correct. Alternatives: SDL's iOS backend ignores the flag (drawable remains 1×); raylib renders to a 1× intermediate FBO despite a larger drawable; higher resolution changes geometry/touch or costs enough to prevent the route. A native-size drawable may improve presentation sampling but cannot add detail absent from the guest texture. The discriminator is an actual Simulator window/drawable log plus viewed paired title, Setup Character and race frames, with W1F2 as visual baseline. No frame hash equality is expected across free-running boots.

Create a fresh `i27-hidpi` branch/worktree at the pin. Make the one iOS-only flag candidate, with at most one small log line if needed to report `GetScreenWidth/Height`, `GetRenderWidth/Height` and DPI ratio. Preserve non-iOS behavior. Build one Mac taps-OFF suite against canonical E54F2 codegen, then one iOS Simulator build/install and one bounded I26-FAST run (≤180 s, mini slot lease, own PID cleanup) using W1F2 scripts adapted to the new worktree. At most three builds total: one Mac, one Simulator, and one retry only for a named compile repair; no tuning loop. Capture at most four SHA-paired PNGs and console; view the frames. A Python acceptance script must parse the baseline and new `[ios-window]` lines and report window/drawable ratios, test count, tick/race reach and frame SHA pairs. If drawable remains 1× or the route regresses, stop and table the evidence; do not build a device app.

Budgets: ≤8 GiB new Mac scratch, check `disk_budget.sh` before/after; ≤8 MiB logs, ≤12 MiB images, one Simulator boot ≤180 s. No clean speed claim from this diagnostic run. Deliver `local/research/I27A/REPORT.md`, exact commands, pins/source diff, suite/build logs (bounded), ratio/route script and output, image SHA pairs, frame observations, lease/PID cleanup and gaps. Commit fork candidate locally `[I27A]` and report/receipts in ssx3 `[I27A]`, both trailer `Orchestrated-By: Codex`. **No fork push, iPhone install, iPad action or final verdict**; hand back the table so the orchestrator can decide Part 2.
