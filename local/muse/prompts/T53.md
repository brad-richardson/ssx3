# T53 — PCSX2: the EE store that stamps 0x434990 into the chain CALL tags (healthy side of E42)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini;
PCSX2 on bytesize via `ssh bytesize` → WSL; **foreground runs on one held
connection**). **Tables + receipts; the orchestrator decides.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/T51/REPORT.md`
(§T51-5: CALL→`0x434990` sites `0x63d430/0x63dcb0/0x70a0b0/0x70a930`; your
watch saw 0 writes), `local/research/E41/REPORT.md` (the recomp's writes
arrive via an uncached mirror and/or a low-level fast path; a raw-address
watch misses them), `local/muse/prompts/E42.md` (recomp side).

## Change (log-only, on T51's clone)

Re-run T51's ADDR-word watch with **address folding**
(`addr & 0x0FFFFFFF` compared against `0x63d434/0x63dcb4/0x70a0b4/0x70a934`
and the `0x435bd0`-bound words `0x63c654/0x63c8a4/0x63cb64/0x63cdf4`), in
every EE store path (memWrite8…128, SWL/SWR, SQ, and any
direct-to-RAM paths the interpreter uses): `tagaddrwrite vsync addr value
pc ra a0..a3 v0 v1 t0..t9 s0..s7`, first 64 per address, over Select
Character settled (statefile route is fine). EE interpreter.

## Deliverable

≤2 builds, ≤2 captures, 3 h, cap 5 GB. `local/research/T53/REPORT.md`: patch,
hooks, preservation, table (storing pc/ra/regs per address and value). No
verdict. `[T53]` commit (`git add -f local/research/T53
local/muse/prompts/T53.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first), no push.
