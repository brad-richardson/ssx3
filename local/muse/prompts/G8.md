# G8 — Post-first-draw SSX replay (re-run the G7 table on drawn content)

You are muse in a herdr panel whose working directory is
`/Users/bradrichardson/dev/ps2xGS`. Runbook: follow the steps, produce
the receipts, **hand back tables + hypothesis + next-action recommendation,
no verdicts**. Read first: `docs/reports/G7.md` (all — E3 verdict + the
ONE next action this brief executes) and the frontier findings
`/Users/bradrichardson/dev/ssx3/docs/research/review-2026-09-20-first-frame-and-gs.md`
§paraLLEl-GS (adoption gate this extends). G7's replay was clean but
zero-transfer (init + scanout only); this brief supplies the drawn
content it explicitly deferred.

## Facts you start from

- Experiment contract (required): hypothesis = the pinned replayer replays
  a post-first-draw 5-frame dump with transfer packets and emits a scanout
  measurably comparable to PCSX2's aligned screenshot; observable = exit
  code, first-vsync PPM, diff stats, wall-per-unit, heap; alternatives =
  (a) draw-aware trigger fails → table + recipe, stop; (b) replay errors
  on transfers → table, stop; (c) large diffs → table numbers + method
  limits, no verdict. Stop: one dump, one replay configuration
  (native/X1, defaults) — no tuning loop.
- Draw-aware trigger: count Transfer packets on the GS thread; start the
  5-frame dump K transfers after the first non-zero-transfer vsync (table
  K + the vsync index). Reuse the G7 PCSX2 clone + auto-dump hunks where
  they still apply (verify revs first); bytesize WSL, ssh/scp, no lease.
- Reuse the G7 clone + replayer build (verify `3a66c197` HEAD first;
  rebuild `-j2` only if the tree moved). Dump version MUST be verified
  from bytes both sides again (never the README).
- Re-run the EXACT G7 replay table (image + diffs vs aligned ref + time +
  memory) and add a G7-vs-G8 delta column (zero-transfer baseline vs
  drawn content).

## Gates and rules

- No contention with the P-lane critical path: builds `-j2`, no recomp
  boots/builds, no P-lane lease, no `adb` installs. Byte caps declared up
  front + tracked (clone/build/dump/logs); SSD only, nothing huge on `/`.
- Reuse G7 tooling (probe scripts, parser checks); do not duplicate harness
  work. No third-party code copied into any project tree; license receipts
  for anything newly touched (expected: none).
- Commit in ps2xGS as you go (prefix `[G8]`, trailer `Orchestrated-By:
  Muse Code`); push `origin main` at the end. Never commit captures or
  build dirs. Mirror `REPORT.md` + scripts to
  `/Users/bradrichardson/dev/ssx3/local/research/G8/` (`git add -f`,
  `[G8]` prefix, same trailer; NEVER `git push` in ssx3).
- Time box: 6 h. Tables + hypothesis + next-action recommendation.

## Task 1 — post-first-draw dump (bytesize, no lease)

1. Verify G7 clone revs + hunks (table). Implement the draw-aware trigger
   (Transfer-packet counter on the GS thread).
2. ONE bounded capture run (wall cap; table it): 5-frame dump + aligned
   screenshot + run-end proof. Packet census (EOF-synced, exact) MUST show
   transfers > 0 or the task stops here with a recipe.
3. Dump provenance table (file/size/sha, trigger vsync, version-from-bytes,
   header cross-checks — G7 §3a shape).

## Task 2 — replay + delta table (Mac)

1. Replay at native/X1/defaults: image, diffs vs aligned ref, time, memory
   (G7 §3b shape, same methods).
2. G7-vs-G8 delta column per measure + hypothesis verdict + the ONE next
   action the numbers justify (adoption input, not an adoption decision).

## Report

Mirrored `REPORT.md`: contract, dump table, replay table + deltas, license
receipts, exact commands, gaps. Commit both repos as above, stop.
