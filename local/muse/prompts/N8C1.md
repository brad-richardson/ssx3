# N8C Part 1 — Turnip app-namespace dependency audit

You are a Codex Sol worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md`,
`AGENTS.md`, `local/AGENTS.local.md`, and `docs/orchestration.md` §4.
Read `local/research/N8A/REPORT.md`, N8B1 and N8B2 in full. Hand back
tables and receipts; the orchestrator decides the mechanism. Named
paths are your write scope. This part is **read-only diagnosis**: no
source edit, build, APK install/launch, fork push or upstream contact.
Claim the free Odin lease before bounded read-only device inspection,
and release it on every exit. You may read Odin system libraries and copy bounded
ELFs to `~/dev/ssx3-work/N8C1/inputs/` for inspection; no binaries in
Git. Time box 20 minutes, ≤2 MiB text receipts and ≤100 MiB copied ELFs.

Pin Turnip v36 at
`~/dev/ssx3-work/G43/inputs/libvulkan_freedreno.so`, SHA-256
`717812c3c51fd2836931ec22b82d13e805d763ec425f86bafdfa92f54c1ac29d`;
two matching reads before analysis. N8B2's one actual app launch, APK
`1096a28e…b190da`, failed `dlopen` in namespace `clns-7` because
`libhardware.so` was unavailable. `/system/etc/public.libraries.txt`
on the Odin lists `libnativewindow.so` and `libsync.so` but not
`libhardware.so`. The driver `DT_NEEDED` names all three plus public
runtime libs. Odin system `libhardware.so` exists but itself needs
`libcutils.so`, `libvndksupport.so`, and `libc++.so`; that may extend the
private dependency chain. The exact driver undefined symbol from
`libhardware.so` appears to be `hw_get_module`; verify independently.

## Table and decision boundary

1. Record exact `DT_NEEDED` and strong undefined `libhardware` symbols
   of the pinned Turnip ELF. Compare every named dependency against the
   Odin public app library list and system file presence. For any
   nonpublic dependency in the closure, tabulate its own `DT_NEEDED`
   and relevant exported symbol/version. Use `readelf`/`nm` and bounded
   source or disassembly windows; no broad binary strings dump.
2. Find `hw_get_module` call sites in the driver or pinned Mesa source,
   if available, and state the conditions under which they run. If
   stripped binary/source cannot establish that, say exactly which
   call-path fact is unknown. Do not claim a shim returning an error
   would work without evidence. Distinguish a *load-time* dependency
   failure from a later HAL/Vulkan behavior failure.
3. Compare two narrow remedies with evidence, costs and a discriminating
   Part-2 probe: (a) bundle an app-local `libhardware.so` compatibility
   implementation covering only required ABI, or (b) package the Odin
   system library and its transitive dependency closure. Include a
   driver-source relink route only if its source/build inputs actually
   exist. Name which is the smallest defensible one-candidate build,
   and what error/identity/visible-frame observation would refute it.
   No new APK or Odin launch in this part.

Check that any copied system ELF SHA matches two device reads. Deliver
`local/research/N8C1/REPORT.md` with pinned inputs, exact commands,
dependency/symbol/call-path tables, a bounded Part-2 build + one-launch
gate and gaps. Commit only explicit report/text receipts `[N8C1]`,
trailer `Orchestrated-By: Codex`; no push.
