# E54D — LWU must zero extend

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. Read `local/research/E54C/REPORT.md`, the E54 queue in
`docs/todo.md`, and the exact translator/test path below. Hand back a
table; the orchestrator decides the gate. One mechanism only: `LWU`.

Pin fork remote `ssx3` at `89bec9b` (verify). Create new worktree
`~/dev/ssx3-work/E54D/PS2Recomp`, branch `e54-lwu`. All builds, generated
code, runs, and temporary files go under `~/dev/ssx3-work/E54D/`.
Read previous E56 fold receipts/scripts for configuration but do not
change or reuse its outputs. Do not change canonical
`~/dev/ssx3-work/codegen-ssx3`; the orchestrator promotes after gate.
Fork source edits: only `ps2xRecomp/src/lib/instruction_translator.cpp`
and focused `ps2xTest` tests/registration as needed. No shared fork or
other lane edits. No fork or ssx3 push.

Correct behavior: `LWU` loads a 32-bit memory word into the GPR low 64
with **zero extension** (e.g. `0x80000000 → 0x0000000080000000`;
`0xffffffff → 0x00000000ffffffff`). The high 64-bit GPR half follows the
existing low-64 write behavior. `LW` sign extends; `LBU`/`LHU` remain
unchanged; `$zero` remains zero. Current `instruction_translator.cpp`
case `OPCODE_LWU` emits `SET_GPR_U32`, whose macro at
`ps2_runtime_macros.h:1036` casts through `int32_t` and sign extends.
Hypothesis: only the `LWU` emitter is wrong. A targeted use of
`SET_GPR_U64` with a `(uint64_t)(uint32_t)` load should distinguish it
from a global setter change. The frontier review estimated 92 static
guest sites; X6 did not verify that count. Do not infer boot reach from
static presence.

## Gates

1. Verify emitter, macro and a pinned PCSX2 `LWU` oracle/source (or
   explicitly state unavailable). Add a focused translator test that
   decodes/emits `LWU` and distinguishes it from `LW`, plus a runtime
   value test for high-bit words, low-bit word, `$zero`, and preservation
   of the upper GPR half. Show before/after results. Use existing test
   harness patterns; no tests that only mirror the source string.
2. Make the one emitter fix. One local regeneration with the new
   `ps2_recomp` into `E54D/codegen`; use E56's source TOML copied and
   retargeted to **this** worktree/output. Verify at least one generated
   LWU site has the new expression and count unique guest addresses
   without claiming runtime reach. No generated-source commit.
3. One Mac Release build and suite each with taps OFF and ON, using the
   new E54D codegen. Run `ps2x_tests` from the fork root. Then one
   mini-lease I26-FAST diagnostic boot (dev-only skip movie), target
   race tick ≥2050, wall cap 500 s, own PID, bounded logs. View SC and
   two race frames. This is a regression gate, not a clean speed or
   LWU-reach claim. No fixed-tick cross-run hash comparison before E55.
4. Check `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner`
   empty; commit `[E54D]` to local fork with trailer
   `Orchestrated-By: Codex`.

Budgets: two configure/build attempts per mode (one clear correction),
one regen, one boot, ≤600 s absolute boot, ≤9 GiB new E54D bytes and
200 GB global cap. Check disk before >1 GB writes. Verify pinned
ISO/ELF, codegen register file and runner with two matching SHA reads
before use. Claim/release one mini slot; cap logs at 64 MiB. If a
suite, regen, or route fails after one clear repair, stop and report;
no tuning loop. Do not make another E54 fix in this lane.

Deliver `local/research/E54D/REPORT.md`: pins, oracle, before/after
values, source diff, generated-site count, build/suite/boot/frame table,
SHAs, runner-dir check, budgets and gaps. Commit `[E54D]` report with
the same trailer, explicit paths; no push. The orchestrator gates and
promotes any new canonical codegen.
