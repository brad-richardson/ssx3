# P6 — Mine ssxdecomp/ssx3 (PS2 SLUS_207.72 matching decomp) for ladder names + structure (read-only research)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. Runbook:
exact steps, receipts named up front, **tables, no verdicts**. Read first:
`local/research/P1/REPORT.md` Parts 8–10 (the current ladder: MMIO fix,
GS-CSR park at `0x375d10`, caller `sub_00375A08`, `sub_003912A8`,
`sub_003DCBD8`, INTC handler `0x3e4db8`) and the sweep CSV header
(`$W/P1/ssx3-functions.sweep.csv`, ~9,270 `sub_*` rows).
`W=/Volumes/Extreme SSD/ps2recomp-spike`. Evidence dir
`local/research/P6/` (commit with `git add -f`, prefix `[P6]`, trailer
`Orchestrated-By: Muse Code`). Time box 4 hours.

## Rules

- Read-only research. Do not edit the clone, the PS2Recomp fork, this
  repo's `tools/`, `native/`, `vendor` trees, or any other agent's
  evidence dir. No builds, no boots, no emulator runs, no leases, no
  `adb`. NEVER run `git push` in `/Users/bradrichardson/dev/ssx3`.
- **License: the decomp repo has NO license file (GitHub reports NONE)
  and matching-decomp source is game-derivative either way. NAMES,
  ADDRESSES, and STRUCTURAL FACTS ONLY. Never copy decompiled function
  bodies, comments, or data tables into the report, the CSV, or
  anywhere else. The committed CSV holds addr→name pairs only.**
- Every claim cites a file you opened (clone path + line numbers);
  `grep` only locates.
- Clone: `git clone --depth 1 https://github.com/ssxdecomp/ssx3.git
  /Volumes/Extreme SSD/ssxdecomp-ssx3` (record rev + size; ~36 MB
  expected). Re-verify the LICENSE absence yourself (`ls` + `gh`/file
  read) and record it — if a license exists, re-cite it per file.

## Questions (caps; stop each at cap, record partials)

1. **Symbol map + coverage (90 min cap).** From their splat config
   (`config/`) + `src/` labels: build the addr→name table for
   `SLUS_207.72` (verify their target sha1 matches ours,
   `77114dfd…`, from their README/config — record the cite). Join
   against `$W/P1/ssx3-functions.sweep.csv`: how many of our ~9,270
   rows get a name (count + %)? Emit
   `local/research/P6/ssx3-decomp-names.csv` (`addr,name,src_file`;
   names + addresses ONLY). Name, specifically, every ladder
   function: `0x375a08`, `0x3912a8`, `0x3dcbd8`, `0x3e4db8`,
   `0x3e3588`, `0x3e3968`, `0x423dd0`, `0x423de0`, `0x424020`,
   `0x40fc6c`, `0x100008` — one row each (decomp name or UNNAMED).
2. **Boot-path + park-region structure (60 min cap).** From names and
   file layout ONLY (no bodies): what do the named regions around the
   entry (`0x100008`), the constructor dispatcher (`0x40fc6c`), the
   GS-wait caller (`0x375a08`), and the VIF0 function (`0x3912a8`)
   tell us about engine structure (module/file each lives in, named
   callers/callees)? Table: addr, decomp name, file, structural note.
3. **Function-boundary truth vs our sweep (60 min cap).** Sample 40
   demod-named functions across the map: compare decomp boundaries
   (start/end from config/labels) against our sweep CSV rows
   (start/end). Table the agreement rate + every mismatch class found
   (ours split/merged/shifted?) with 3 examples each (addr ranges +
   names only). This calibrates sweep precision/recall.
4. **Their tooling worth stealing (30 min cap).** Splat config
   idioms, progress-tracking scripts (`scripts/`), naming workflow
   (`Main Workflow.txt`): anything our sweep/CSV flow should adopt?
   Table: artifact + file:line + one-line mechanism (facts only).

## Steps

1. Questions 1–4 in order with caps; record partials as unresolved.
2. **Report.** `local/research/P6/REPORT.md`: coverage table, ladder
   naming table, structure table, boundary-agreement table, tooling
   table, the CSV path + row count, clone rev + size + license
   finding, exact commands, "What I could not do" (names/addresses
   only throughout — restate the rule). Commit `local/research/P6/`
   (REPORT + CSV only). Stop.
