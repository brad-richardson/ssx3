# F1 — fold RR1 + AU9 + E57 + N11 onto fork `ssx3`, then build and install for Brad (muse)

Brad approved (09-24): after gates, fold to fork `ssx3` and install one combined build — iPhone install only (never launch), iPad test, Odin as his play build, his save + manual-play env re-applied and verified. Gates: `local/research/{RR1,AU9,E57,N11,GB8}/REPORT.md` (orchestrator sections at the end).

## Part 1 — fold + Mac check (stop after; the orchestrator gates and pushes)
1. Worktree `~/dev/ssx3-work/F1/PS2Recomp`, local branch `f1-fold` from fork `ssx3` `71c952e`. Cherry-pick **in this order** (`git cherry-pick -x`):
   - E57: `69b3256 ed35abf cbca1ad 802f5d2 0a4aa5e a4ecce5`
   - RR1: `3bc0449 f3dff5b 4f69c98`
   - AU9: `fb75ec3 8af43c1 243b759` — **never `a46fb2e`** (unvalidated emitter change)
   - N11: `9dadccd 20db28f`
   On a conflict: stop, save `git status` + the conflict hunks to `local/research/F1/conflict.txt`, hand back. Don't resolve by guessing.
2. Suite from the worktree root (record count; all pass). `git diff --stat 14b1e5cb HEAD -- ps2xRuntime/src/runner` empty.
3. Mac Release build, diagnostics off, **with paraLLEl** (`PS2X_GS_SHADOW_PARALLEL=ON`, GB8 recipe: `local/research/GB8/REPORT.md` Exact commands). One det-hash build too if cheap (GB8 did both from one dir).
4. Boots (one mini slot each, I26-FAST, empty mc0, `PS2X_SKIP_MOVIE=1`, `PS2X_GS_BACKEND=parallel`, `GRANITE_VULKAN_LIBRARY=/opt/homebrew/lib/libvulkan.1.dylib`, `PS2X_SOUND=1`, `PS2X_DETERMINISTIC=1`):
   - **B1** to t2400 with frame dumps at 1090, 1180, 1800, 2100, 2400 and SND logging enough to count cid-0 uploads (AU9 used `cid0/dmq/done` counters). Expect: Select Peak shows the Peak 1 photo; race shows the mountain backdrop, lit snow, trees; **cid0 = 531**. View every frame; table what you see.
   - **B2** clean speed (non-det build, exclusive lease `--exclusive`, ≤ 5 min): per-phase vsyncs/s ÷ 59.94 as GB8 Q1. Compare to GB8's parallel row (race 0.224×).
5. Deliverable Part 1: `local/research/F1/REPORT.md` (commit list old→new SHAs, suite, runner-dir, runner SHAs ×2, frame table, cid0, speed table, gaps); commit `[F1] Part 1 …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`). **No push.** Stop and hand back.

## Part 2 — Android (after the orchestrator pushes `f1-fold` to fork `ssx3`)
APK on bytesize from the pushed tip + paraLLEl-GS `963cb57` (TL1 recipe `local/research/TL1/build.sh`; `ps aux` first, one heavy job at a time). Two SHA reads. Odin (lease, keyguard `showing=false`, AC + ≥ 20 %, thermal ≤ 2 before each launch, force-stop after each): **2 clean speed runs** with `local/research/N11/launch.py` (full own env, `PS2X_SOUND=1`, `mc0-test` empty, stop tick 4500) → per-phase table vs N11 S1 (race 0.117×). Then make it Brad's play build: install, `bash local/research/I31/deploy-odin.sh` (save + env `9fb46f85…`), verify SHAs, no launch. Append `## Part 2` to the report; commit `[F1] Part 2 …`.

## Part 3 — iOS (after the push; may run alongside Part 2)
Device build from the pushed tip with the I32 recipe (`local/research/I32/build-install.sh`; bundled env unchanged: sound on, `PS2X_MC_ROOT=${DOCUMENTS}/mc0`). Simulator run to the race first (view a Select Peak and a race frame). iPad: install, re-apply `local/research/I31/deploy-ios.sh ipad`, one test launch with a fresh-card `-e PS2X_MC_ROOT` (**read the live container path from the console first**; I32 used a stale UUID), view frames, terminate. iPhone: install, `deploy-ios.sh iphone`, **never launch**. Append `## Part 3`; commit `[F1] Part 3 …`.

## Rules
Text only in git; scratch `~/dev/ssx3-work/F1/` ≤ 20 GB; check/kill runners by PID; no upstream contact; never `git add -f` in the fork; never push (the orchestrator pushes). First failure: stop, save the error, hand back. Budget: Part 1 1.5 h, Parts 2/3 1 h each.
