# V1 — Durable verified storage: audit, pinned restore manifest, smoke check, mini-cutover list (NO lane-output mutation)

You are muse in a herdr panel in the ssx3 repo (derive the repo root
from this prompt's path: two up from `local/muse/prompts/`).
**Tables + receipts, no verdicts beyond the missions.** Read first:
`local/research/E25/REPORT.md` §Mission-5 + `RESTORE.md` (the
existing snapshot recipe — E25's tar was NEVER restore-tested),
`docs/research/review-2026-09-22-progress-and-parallelization.md`
§operational-changes-1 (durable storage before cutover), and the
gate history of link corruption (todo.md G33 AMENDMENT + E26 + G35
entries: 16 recurrences, now pattern-dependent). New lane "V"
(verified storage). You audit, pin, and smoke-test. You do not
rebuild lanes, do not move lane outputs, and do not touch any
worker's active files.

## Facts you start from

- What exists: E25's snapshot tar (1,762,803,200 B `9ded8065…`) +
  7,342-file manifest + `RESTORE.md` on the SSD; share-tier mirrors
  (`/Volumes/share/ssx3/`: `e25-restore/`, `ps2x-g33..g37/`,
  `ps2x-i24/logs/`, `ps2x-t4/`); live trees (`/tmp/e18-mpeg-link`,
  SSD build dirs, fork worktrees). What is missing: a SINGLE
  pinned manifest saying what restores what in which order, any
  tested restore, and a mini-cutover list.
- The SSD link is PROVEN pattern-dependent corrupt (same offset,
  different bytes by read pattern — E26 gate). Every byte this lane
  quotes needs 2+ matching reads separated in time AND
  corroboration against pinned committed bytes. Paradox note: the
  audit itself runs on the suspect path — resolve it by pinning
  everything twice + against git history, and by saying exactly
  which pins could NOT be corroborated.
- The Mac mini may arrive mid-lane. Parameterize every path
  (this-host vs mini); if the migration happens under you, table +
  stop and report what was verified where — do not chase a moving
  target.

## Gates and rules

- READ-ONLY on all lane outputs (build dirs, `ps2x-*`, worktrees,
  `/tmp/e18-mpeg-link`, share mirrors). Scratch restores go to NEW
  `v1-*` paths ONLY (SSD ≤ 4 GB, internal `/tmp/v1-*` ≤ 1 GB),
  RETAINED + receipted (no deletions ever); keep them small by
  restoring small things.
- No builds, no boots, no lease, no devices, no adb/devicectl, no
  network writes except share-tier READS (verify, never overwrite,
  the mirrors). No fork operations beyond `rev-parse`/`status`/
  `ls-remote` (read-only).
- Byte caps: evidence `local/research/V1/` text-only (manifests are
  JSON — sizes + shas, not bytes); `COPYFILE_DISABLE=1` on every
  SSD step; zero deletions.
- Time box 6 h. Evidence commit `[V1]` + `Orchestrated-By: Muse
  Code` trailer, `git add -f`, NO push (orchestrator pushes at poll).

## Mission 1 — Audit (what exists, what pins it, what is sole-copy)

Table every critical path: E25 tar + manifest + `RESTORE.md`;
`e25-restore/` share binaries; each `ps2x-g*/` + share mirror pair
(file counts + total bytes + mirror agreement re-verified by YOU);
G-build dirs (binary present? sha vs committed?); `ps2x-i24/logs`
+ mirror; `ps2x-t4`; fork worktrees (HEADs + clean/dirty); live
`/tmp/e18-mpeg-link` (present? runner sha?). Per row: size, sha or
"too big to hash this lane — sampled instead" (with the sampling
receipt), corroboration source, and SOLE-COPY? (yes/no + where the
second copy lives). Flag anything whose ONLY copy is on the corrupt
path.

## Mission 2 — Pinned restore manifest + smoke check

Write `restore-manifest.json`: ordered restore steps (1. repo clone
+ `git lfs`? no — plain clone at pinned sha; 2. share restores; 3.
SSD restores; 4. rebuild-from-fork fallbacks with measured walls),
each with paths + expected shas/sizes + verify commands. Then SMOKE
ONE small restore end-to-end (e.g. `ps2x_tests` 5.7 MB from
`e25-restore/` into `v1-smoke/`, or one `ps2x-g*/` dir from share):
copy → re-sha twice → `cmp` against live → record. Do NOT test the
full-tree restore (that needs deleting the live tree — forbidden);
state the untested remainder explicitly.

## Mission 3 — Mini-cutover list

Table the cutover: what moves to mini-internal (active inputs +
build cache — the review's condition), what stays on SSD-as-backup,
what re-verifies post-move (pins + one smoke), what waits for
active leases (P-lane lease, Odin slots, running workers — migration
cutover WAITS, it does not preempt). Include the "first commands on
the mini" checklist (mounts, paths, repo pull, pin re-verify).

## Success bars (tabled)

Full audit table with sole-copy flags; `restore-manifest.json` with
ordered steps + pins + verify commands; ONE smoke restore with
receipts; mini-cutover list; untested remainder stated; zero lane
mutations; tail receipt with byte-exact prefix sha.
