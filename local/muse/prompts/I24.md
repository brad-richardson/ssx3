# I24 — V3-vector probe: retained title 5,040 B on host-7.1.1 AND device (P4 close)

You are the ssx3 I-lane worker in `/Users/bradrichardson/dev/ssx3`.
**Tables, no verdicts.** I23's H2 verification request is now
ANSWERABLE: E24 closed Objective 3 (title movie dims 512×448 MPEG-2
MP@ML 4:2:0 29.97 + full stream/colour rows) and the full 5,040 B
title payload is retained (`observed/parser-input.bin`,
SHA256 `cde8a830…`, FNV `0xd2a9588f0e0fd358`, in E22/E23/E24 — verify
all three copies identical before use). The V3 precondition (I23 P4)
is satisfied. This brief runs the V3 vector on both sides and closes
P4 on structural identity.

## Read first (all of each)

1. `local/research/I23/REPORT.md` (shape-A integration, branch
   `i23-ffmpeg-ios`, H2 request text, P1–P10 table — P4 is the
   target; P2b/P9b/P5/P7 stay NOT OBSERVED unless V3 lights them).
2. `local/research/E24/REPORT.md` §Objective 3 + §picture finding
   (dims provenance, 35-code / 28-slice census, tail 5024+15, NO
   terminator — the vector spec; carry E24-E3: FOUR 177s, slice28
   191 raw / 176 to-last-nonzero).
3. `local/research/I22/REPORT.md` §sufficiency (host probe recipe).

## Mission

1. Vector lock: sha/FNV/size-verify the three retained copies
   (E22/E23/E24 `observed/parser-input.bin`) + first4 `000001b3` +
   dims re-parse (512×448 from the sequence header). Any mismatch →
   table + stop (do not probe a contested vector).
2. Host V3 anchor: build a V3 probe on the EXISTING host-7.1.1 prefix
   (`/Volumes/Extreme SSD/ps2x-i23/host-7.1.1/`, I22 recipe — no
   rebuild of the prefix itself) adapting `i22-decode.c` to the
   5,040 B input; table counts (parsed/packets/frames), RGBA sha
   (if frames), and the exact stall-or-serve shape. This is the
   reference the device must match structurally.
3. Device V3 probe: reuse the I23 final build if byte-identical
   (`edb3eadc…`, 122,458,696 B — re-sha; rebuild on the SAME branch
   `i23-ffmpeg-ios` only if the re-sha misses), install, run V3 via
   the C3 `PS2X_MPEG_VECTOR_PATH` diagnostic; table the vector
   digest + feed-trace + retrieved RGBA (if any).
4. P4 verdict: table host-vs-device STRUCTURAL identity row by row
   (counts, digests, stall/serve shape, bytes if served). PASS = the
   two sides agree exactly (whatever the shape — the title feed is
   5040/parsed/0-frames on both by the E-lane record; a device-only
   deviation re-opens Cause-B). Also re-check P2b/P9b/P5/P7 cheaply
   if V3's output lights them; otherwise they stay NOT OBSERVED.

## Rules

- Fork: stay on branch `i23-ffmpeg-ios`; if code changes are needed,
  cut branch `i24-v3-vector` (push branch, no force-push). NEVER
  touch `fork/ssx3` — E-lane owns it.
- Device: Brad's iPad via devicectl with explicit `--timeout`;
  rebuild/reinstall/probe AUTHORIZED (I23 precedent); user sessions
  reported, never killed.
- No PS2 boots → no lease; no `adb`.
- `export COPYFILE_DISABLE=1` on SSD steps; byte caps declared up front
  + tracked (reuse I23 prefixes + build tree where byte-identical;
  new scratch ≤2 GB in NEW `ps2x-i24/` paths; 0 growth elsewhere).
- Evidence dir `local/research/I24/` (STANDALONE). Commit with `git add
  -f`, prefix `[I24]`, trailer `Orchestrated-By: Muse Code`. Do not push
  (orchestrator pushes at poll).
- Experiment contract up front. Time box: 6 h. Tables, no verdicts.
- Hygiene: report in chunks + tail receipt (recomputed AFTER the last
  edit). Fill in the ACTUAL time used.
