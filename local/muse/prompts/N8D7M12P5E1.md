# N8D7M12 Part 5E1 — prepare one same-settings Odin repeat (no device)

You are the N-lane OpenCode Go script worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full `local/research/N8D7M12P5D1/{REPORT.md,ORCH-GATE.md,launch.py,check.py}` and Part 5D2 REPORT/gate. Cite local source/receipts, not web. Own only this brief and `local/research/N8D7M12P5E1/`. No adb/device command, install, launch, lease, APK/stream/source/fork edit, build, push, board/global edit or upstream contact. Time box 25 min; text <512 KiB. Write REPORT/checker early.

Goal: prepare one reviewed-SHA-held **OFF vs OFF** repeat to measure run-to-run GPU variance on Odin. Part 5D2's OFF replay passed all run controls but differed from same-APK ON at tick850, before the three capture flags act. Its 41 priv hashes matched ON, while VRAM matched 19/41 and present 25/41; final frame remained sparse. The ON/OFF causal comparison is void. A second OFF run with the exact same APK/stream/env/backend can distinguish reproducible OFF output from same-settings variance; it still cannot by itself identify a renderer root cause.

Copy the final P5D1 `launch.py` to `local/research/N8D7M12P5E1/launch.py`. Change only the private scratch dir to `~/dev/ssx3-work/N8D7M12P5E1`, unique remote frame/hash basenames and lease/result labels. Keep the OFF replay env **byte-for-byte identical except unique output paths** (the three tick-2050 flags absent); same APK `caa11102…f512`, stream `f6a78f71…a593`, runner/Turnip/HAL pins, replay mode, parallel backend, step50, PPM2050, exact 41 ticks, one-install/one-launch guard, local/device double SHAs, battery/free/keyguard checks, lease, BACK, force-stop/PID absence, env 2+2 restore, caps and `finally` cleanup. Preserve P5D1 and ON receipts untouched. No device action in this preparation part.

Static checker must diff old/new launchers, allow only the listed path/label changes, prove the env semantic keys are identical, exact tick gate and cleanup retained, and run the no-device functional tick tests. `py_compile`, `--self-check`, SHA the new launcher. Predeclare later run comparison: exact 41-row tick identity; OFF2 vs OFF1 (`~/dev/ssx3-work/N8D7M12P5D1/parallel.hashes`) priv/VRAM/present equality and first difference, PPM SHA/visual. If OFF2 differs before2050, same-settings GPU variance is observed. If equal across 41 rows and PPM, the earlier ON/OFF difference needs a separately controlled ON repeat or env-state audit. Any input/preflight/cleanup failure voids the comparison. No diagnostic elapsed time is speed.

Deliver `REPORT.md`, `launch.py`, `check.py`, bounded static check result, exact pins/SHAs/unexecuted command, acceptance table and gaps. A = exact same-settings script ready for separate orchestrator release; B = semantic diff unprovable; OTHER = preparation failure. Commit `[N8D7M12] Part 5E1` with `Orchestrated-By: opencode`, **explicit paths only**, no push. Hand back the table.
