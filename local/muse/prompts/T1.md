# T1 — Park snapshot (PS2X_DIAG_PARK=1) + SIF/RPC tally + ladder_diff

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/P1/REPORT.md` Part 28 or the latest
Part first (whatever a diagnosis brief rebuilds by hand from 19k-line
logs — that manual rebuild is what this brief automates), plus Part 24
§P24-1f/g (the `[drop]` census conventions to reuse). This is a TOOLING
brief: every future diagnosis brief's first hour, automated. Includes
the frontier's T2 (SIF/RPC census table) as the snapshot's SIF/RPC
tally — T2 is NOT a separate brief. Peer lanes run concurrently — you
share only the fork remote.

## Facts you start from

P1ab/P1ac/P1ad each hand-extract the same rows: per-thread state,
semaphore waiter/signaller pcs, hot pcs, drop census, SIF/RPC lines,
GS counters. "Ladder-identical" proofs (P9, P11) are manual table
diffs. The snapshot format defined here becomes the input to the IOP
decision (§13) and every future park brief.

## Gates and rules

- Lease `T1`: the proof boot ONLY while holding
  `/tmp/ssx3-p-lane-lease` (the SHARED P-lane lease) — if it names
  another agent, poll every 5 min and log waits to
  `$W/P1/run/t1-waits.log`. Builds any time (`-j4`). Max 1 proof
  boot, ≤90 s foreground. No `adb`.
- Fork: the snapshot files ONLY (runtime diag emitter +
  `tools/ladder_diff.py` — fork `tools/` next to the emitter unless
  the tree says otherwise; record the call). `git add` those NAMED
  files only — never `-A`, never stage or touch anything else.
  Verify the diff before committing. One commit per change with the
  `Orchestrated-By: Muse Code` trailer. `git pull --rebase` before
  pushing; `git push` ONLY inside the fork clone to the `fork`
  remote. Stop on any foreign rebase conflict. Never commit
  generated sources or `._*`.
- Evidence dir `local/research/T1/` (STANDALONE — do NOT append to
  `P1/REPORT.md`; a peer brief may own it). Commit with `git add
  -f`, prefix `[T1]`, trailer `Orchestrated-By: Muse Code`. NEVER
  run `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Emitter.** `PS2X_DIAG_PARK=1`: on SIGTERM/timeout the runtime
   writes ONE JSON + ONE human table: per-thread state (pc, ra
   chain, wait object), semaphore table with waiter/signaller pc
   histograms, hot-pc histogram, `[drop]` census, SIF/RPC tally
   (every `sceSifLoadModule`/`BindRpc`/`CallRpc`/`SendCmd`: sid,
   fno, payload size, thread, claimed/unclaimed by host), GS
   counters. Follow the existing diag conventions
   (`ps2_log.h`-style); unit test where the harness allows.
2. **Diff tool.** `tools/ladder_diff.py`: takes two snapshots,
   prints the rung table with EXACT/DELTA per row (the P9/P11
   ladder shape, machine-made). Include a `--tolerance` story for
   throughput rows (documented contended — never exact).
3. **Proof (1 boot).** Lease-gated boot on the CURRENT park with
   the flag on: snapshot written (JSON parses, table complete —
   cross-check 3 rows by hand against the raw log); then
   `ladder_diff` of two same-tree boots (expect zero
   non-throughput deltas). Suite must stay all-green (state
   totals, name any face).
4. **Report.** `local/research/T1/REPORT.md`: format doc (fields +
   example), diff-tool usage, proof receipts, exact commands,
   receipt paths, "What I could not do". Commit, remove the lease
   if held, stop.
