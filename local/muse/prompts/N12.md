# N12 — Odin per-stage re-profile on the F5 play build (muse, 1.5 h)

## Goal
N11 measured the Odin race frame at 145 ms, 81 % of it VU1 (hazard bookkeeping 70.1 + execute 48.2 ms), before VU1 static recompile (VR1) and commit-at-issue (VB1). F5 folds both. Brad (09-25) wants the **VU1-on-its-own-thread** decision (parked) sized with real numbers: how many ms of the race frame VU1 still takes on GameThread, and what the rest of GameThread is. Also redo the menu profile. **Measurement only**: no fork edits, no new builds. Hand back the tables; don't conclude.

## Facts
- Build: the APK F5 Part 2 installs as Brad's play build (`local/research/F5/REPORT.md` `## Part 2`: APK SHA, chosen knobs 1× or 4×+hi-res, pipelined). Profileable since F1 (`<profileable android:shell="true"/>`). Unstripped `.so` for symbols: F5's bytesize build tree (read `local/research/F5/REPORT.md` for its path; if it's gone, stop and hand back — no rebuild).
- Driver and method: `local/research/N11/` (`launch.py`, `phases.py`, `buckets.py`, `build.sh`; Part 2 `## Symbolization`, `## Per-stage ms per guest frame`). Copy into `local/research/N12/` and adapt. Keep N11's conversion model (race: GameThread-bound, k = clean race wall/frame ÷ GameThread share) and state it.
- Odin adb: serial in `local/odin-serial` (Wi-Fi, paired); fallbacks `192.168.1.53:5555`, USB `622c49b1`. Record which transport each run used.
- **New symbol shapes for `buckets.py`** (add before the old VU1 rows; first match wins):
  - `VU1 generated pairs`: `VU1RecompImage<` (the `f<pc>` pair functions and `next`)
  - `VU1 issue/hazard`: `VU1Interpreter::(issuePair|commitReadyPipelines|calculatePairReadyCycle|markPairWrites|recompChainReady)`
  - `VU1 interpreter/other`: remaining `VU1Interpreter::` / `VU1` (interpreter fallback, `run`, `progressXgkick`)
  - `VU0`: `execUpper`/`execLower` reached from `executeVU0Microprogram` (use the children view; if not separable, say so)
  - keep `__bzero`/`__memset` as its own row (VB1 saw 4.9 % from `m_xgkick = {}` zeroing)
  List every symbol→stage mapping in the appendix, as N11 did.

## Runs (≤ 3 launches, each ≤ 600 s, stop tick 4500; Brad's play env keys otherwise)
| Run | Settings | Purpose |
| --- | --- | --- |
| S1 | `PS2X_SOUND=1`, no profiler | clean per-phase rates (anchor for k) |
| S2 | same + `simpleperf record -g --app com.ps2x.runner --duration 30` from race tick ~2400 | race profile |
| S3 | same + 30 s over the menus (ticks ~636–1099) | menu profile |

## Deliverable tables
1. S1 per-phase vsyncs/s and ×59.94, next to N11 S1 and F5 Part 2.
2. S2 race stage table (share, ms/frame), next to N11's S2 column. **The key rows:** VU1 total ms (generated + issue/hazard + interpreter), GameThread-minus-VU1 ms, GsWorker ms/frame.
3. S2 top-25 GameThread symbols, and the top-10 generated pair functions by image hash + pc (hot loops).
4. S3 menu stage table next to N11's S3 (Turnip driver CPU, libc/allocator, mutex churn).
5. Thread rows (CPU-ms per guest frame) for GameThread, GsWorker, main, AAudio; GPU busy % and thermal per phase.

## Rules
Odin lease `/data/local/tmp/mg/LEASE`; keyguard `showing=false` before each launch (else stop and ask the orchestrator); `dumpsys battery` AC powered + level ≥ 20 %; cool to thermal status ≤ 1, then a fixed 180 s wait before each launch (record the waits); **force-stop after every run**. Never touch the Odin screen or keyguard. Brad's save: never touch `files/mc0/`; run with `PS2X_MC_ROOT=<FILES>/mc0-test` (empty); save his `ps2x.env` (SHA) first and restore it byte-exact after every run (SHA check). Symbolization on bytesize (one heavy job at a time, `ps aux` first). Text only in git (no perf.data, PNGs, binaries). Scratch `~/dev/ssx3-work/N12/` ≤ 5 GB. First failure: stop, save the error, hand back.

## Deliverable
`local/research/N12/REPORT.md` (tables above, exact commands, APK SHA, transports, gaps) + adapted scripts + `reports/`; commit `[N12] …` (explicit paths, `git add -f`, trailer `Orchestrated-By: Muse Code`), no push.
