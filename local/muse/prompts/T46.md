# T46 — Healthy R3 re-drive on the next audio-healthy VM (CORRECTED baselines; variant quarantined)

You are the ssx3 T-lane worker. T45 ran the orchestrator-authorized
null-sink variant (hold tree-crash, quarantined distributions, healthy
series untouched) and stopped after ONE variant run per the brief.
This brief re-drives the HEALTHY series. Read
`local/research/T45/REPORT.md` first (all of it: variant setup,
R2-variant forensics, quarantine record, gaps). T45's dir is READ-ONLY
reference. Tables, no verdicts.

## Baselines (T44-E1 corrected series — the ONLY baseline; append-only)

Healthy (append here on a crash-free run):
- ROI no-input floor n=3: T42 −0.7/−22.2; T43 d5d6 +2.8/−12.7, d9d10
  −4.5/−7.0
- Gradient no-input floor n=7: T42 +1.8/+4.8, +0.0/+10.6, +2.7/−3.9,
  −2.1/−4.2; T43 d4d5 −7.3/−4.5, d5d6 +1.6/−12.8, d9d10 −4.7/−7.7
- Hold-step ROI n=1 crash-confounded: T42 +11.2/−39.5; T43 0
- Hold-step gradient n=1 crash-confounded: T42 −4.4/−24.1; T43 0
Variant-quarantined (NEVER append healthy runs here, never merge):
- ROI no-input n=1 (d9d10 +9.4/+45.7); gradient no-input n=8
  (T45 §T45-4, 2 recovery-adjacent); hold n=0; pre-pair n=0.

## Recipe (T44 §T44-4 as executed by T45, healthy path)

1. Audio gate FIRST: demand-boot and check `/mnt/wslg/PulseServer` +
   `runtime-dir/pulse/` (immediate + delayed @uptime ≥30). HEALTHY
   required — this brief runs the healthy series ONLY. If DOWN after
   ≤2 demand-boots + one ≤10-min stopped wait: STALL honestly (dmesg +
   socket receipts, recipe returned). Do NOT run the variant again
   (one variant run was the authorization; a second needs a new
   orchestrator call).
2. On a healthy VM: re-pin the T43 way + X11 tmpfs + PPM 0.0000/0 +
   dmesg/eventlog pre. Reuse T44's staged scripts (re-verify shas;
   copy to `t46-*` ONLY if you must edit — renames-only rule carries).
   Fetch the on-box boot logs from the PREVIOUS run first (the T44
   gap discipline — never lose a run's logs to overwrite).
3. Launch R3 (background → sleep 35 → v2a → census). On live reach:
   full T44-shape run (61 snaps, ONE ≈1 s Left hold, same slot) +
   post-hoc scoring (all three trackers + lock-QA) + REPORT.
   Crash-free → append to the HEALTHY distributions, DONE.
   Crash-confounded → full 3-row crash receipts; ONE more run (R4)
   authorized iff R3 crashes; R4 crash → freeze + STOP.
4. Commit `[T46]` evidence + push when done (verify-then-push). Stream
   to the SHARE tier (`/Volumes/share/ssx3/ps2x-t4/`) if the SSD is
   still absent (T45 precedent); SSD preferred if mounted.

## Frozen

- ROI: x[220,420] y[180,360], DARK_MAX 80, gate 200–6000.
- Gradient: GRAD_MIN 60 (Sobel |Gx|+|Gy|), gate 500–3000.
- SCPS: y[380,460] x[150,540], ±40/±120, untouched.
- Trackers `t41/t42/t43/t44-track.py` byte-identical (shas 58afa004/
  a951f782/2e532a2d/cb56c827 first-8).

## Receipts (same set as T45, plus the T45-N1 close-outs)

Stream B+sha + head/tail shas, poll + T46_DONE + full-precision hold
walls, census EE/IOP + top rows, 61 snaps + sizes, staged tool shas,
NVM post-run FULL sha IN A COMMITTED FILE (not report-text-only),
C: post-run, dmesg pre/post. (T45-N1: commit pactl/env primaries for
any deviation — report-text-only setup receipts fail the next gate.)
