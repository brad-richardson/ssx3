# M30 — Span-windowed census rule: proposal + 764-shape re-run (offline, no harness code)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `local/research/M29/REPORT.md` first (all of it —
M29 gap 1 is this brief: 9 c321 r266 (+234 rows) + 22 c296
r400/r401 (+366/+367 rows) read in-column but hundreds of rows
from their streak spans — the exact-match census column
definition counts them as column members, not ±1–2 moves).
Fully offline — SSD dumps read-only, no harness code, no replays,
no lease. No device work.

## Facts you start from

- Far-flung extras (M29): 3 of 5 extra delta-rows sit 234–367
  rows outside their column's s0 span (r23–34); only 734-c342's
  r26/r33 (±1 row) read span-adjacent. The census rule question:
  should column membership require rows within ±K of the s0
  span (far sites tabled separately as off-span tail)?
- The proposal to test (FIXED before running — record K
  candidates in DESIGN.md, e.g. K ∈ {2, 5, 10}, no additions
  mid-run): rows within ±K of the s0 span rows count toward
  the column; rows beyond ±K are off-span tail (counted
  separately, never silently dropped).
- Inputs (read-only): `/Volumes/Extreme SSD/m16/` (764 per-shape
  triplets + `loo.txt`), `/Volumes/Extreme SSD/m28/m28.txt` +
  `m28-census.tsv` (exact-match census to reproduce: 10
  movers / 15 cells / standings — match exactly or table the
  mismatch and stop). Work dir: new
  `/Volumes/Extreme SSD/m30/`; evidence `local/research/M30/`.

## Gates and rules

- NO lease of any kind (offline brief — no boots, no harness
  runs). No fork changes at all. No `adb`.
- Read-only on `m15/`–`m29/` and every other agent's dirs. All
  outputs to `/Volumes/Extreme SSD/m30/` + `local/research/M30/`.
- Evidence dir `local/research/M30/` (STANDALONE). Commit with
  `git add -f`, prefix `[M30]`, trailer `Orchestrated-By: Muse
  Code`. NEVER run `git push` in ssx3.
- Time box: 4 h. Tables, no verdicts. Record falsification bars
  in DESIGN.md BEFORE running (M18–M29 precedent).

## Task 1 — rule proposal (fixed K list, exact-hit comparison)

Exact-match census recomputed from the dumps (10 movers / 15
cells / standings must match M28 exactly or table the mismatch
and stop):

1. For each DESIGN.md-listed K: re-run the presence census
   with the ±K span window (movers / moved cells / standings
   per K — full tables, not just counts).
2. Off-span tail tables per K: how many tail bytes fall
   outside all windows (per shape: count + rows — the far
   mass the window excludes)?
3. Delta table exact-vs-windowed per K (which movers
   survive? which standings change? M29's 3 far extras —
   reclassified as off-span under every K?).

## Task 2 — window behavior (does ±K stabilize the census?)

1. Standing stability: extra-only / no-cand / assigned counts
   per K (does the window REDUCE unassigned cells, or just
   move bytes to off-span?).
2. Below-0.95 join per K (M28's 2×2 recomputed — does the
   mover/below overlap change?).
3. K-sensitivity: adjacent-K diff tables (which cells flip
   between K values — the rule's stability margin, tabled).

## Task 3 — controls + determinism (M18–M29 precedent)

1. Positive control: synthetic truth with KNOWN far extras
   (N shapes, extras at known row distances) — recover
   windowed standings + off-span sets exactly per K.
2. Determinism: re-run Task 1 exact-match on s0+9+22,
   byte-identical receipt.
3. Input shas + Model-0 recompute (R_0/fnv must match M17–M29).

## Report

`local/research/M30/REPORT.md`: DESIGN.md (bars + fixed K
list first) + windowed census tables + off-span tables +
stability tables + controls + rule recommendation as a TABLE
(cost/benefit per K with the numbers — no verdict) + gap rows.
Screenshot only if a windowed-census map discriminates
(budget-noted, pre-registered split required per M23–M29
precedent). Commit evidence (`[M30]`, no push), stop.
