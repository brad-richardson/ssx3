# X13B — sparse Qwen control on the same scanout excerpt

You are the **sparse Qwen3.6-35B-A3B** opencode worker. This is the matched control for dense X13. Follow `~/dev/AGENTS.md`, repo `AGENTS.md`, and `local/AGENTS.local.md`. Cite the repo, not the web. Write `local/research/X13B/rows.tsv` early. Do not edit code, build, boot, touch a device, or decide the N8D4 cause/fix.

Pin `/Users/brad/dev/ssx3-work/N8B1/PS2Recomp` at `17e90ded3689685ad359b76a9168c80a1f752e2d`; stop if different. Read only this brief and `local/research/X13/excerpt.txt` (4.3 KiB, numbered source lines 236–310 from `ps2xRuntime/src/lib/gs/ps2_gs_parallel_backend.cpp`). Do not search other files. Fill UTF-8 TSV columns `stage`, `line`, `source` with the **entire exact line, outer whitespace stripped**, in this order: `scanout` 253, `barrier` 275, `copy` 284, `submit` 288, `wait` 289, `map` 291, `pack` 303. No paraphrases or extra rows. Try `lsp goToDefinition` once on `m_iface->vsync(vsync)` at line 253 of the pinned source; report the returned target or empty/error without inferring anything from an empty result.

Run `python3 local/tooling/orch/check_x13.py local/research/X13B/rows.tsv`; acceptance is exact 7/7. Time cap 5 minutes, context target <15k. `REPORT.md` gives the pin, commands, checker output, elapsed time, context size if visible, LSP result and gaps. Commit only `rows.tsv` and `REPORT.md` as `[X13B]` with `Orchestrated-By: opencode`; no push. Hand back the table, not a conclusion.
