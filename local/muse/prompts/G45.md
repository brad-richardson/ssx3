# G45 — Fix-only `ssx3` branches for the paraLLEl-GS and Granite forks (local commits; the orchestrator pushes)

You are muse in a herdr panel in the ssx3 repo (`~/dev/ssx3`, Mac mini).
**Tables + receipts; the orchestrator decides and pushes.** Read first:
`AGENTS.md`, `local/AGENTS.local.md`, `local/research/G43/REPORT.md` §1–3
(working copy, build recipes, equality proof), `local/research/G39/REPORT.md`
(G26/G28 regression shapes), and the REPORTs that introduced each hunk
(G24–G31, G40–G43 under `local/research/`).

## Why

The G lane runs paraLLEl-GS `3a66c19` + Granite `16e7395f` with patches
carried as diffs; the forks only hold `wip/ssx3-snapshot` (paraLLEl
`faf6400`, Granite `aaeee97`), which mixes fixes with diagnostics. Brad
approved (09-22) clean `ssx3` branches on `brad-richardson/parallel-gs`
and `brad-richardson/Granite` holding **only**: the G26 fix, the G28
Granite writer guard, the env-gated Turnip HAL loader (G42 + G43 layout
fix), and whatever build shims those need. Diagnostics (G24/G29/G31/G40
walls, G41 canary, ladders, census/LOGI probes) stay on `wip`.

## Steps

1. Work in fresh clones under `~/dev/ssx3-work/G45/` (clone
   `~/dev/parallel-gs`, then its Granite submodule from the local object
   store; no network). **Don't touch** `~/dev/ssx3-work/G43/parallel-gs`
   (G44 builds from it) or `~/dev/parallel-gs`.
2. **Hunk table** (both repos): every hunk in `wip/ssx3-snapshot` + the G43
   hunk vs the upstream pins → file, lines, introducing brief, class
   `FIX` / `LOADER` / `BUILD-SHIM` / `DIAGNOSTIC`, evidence (report line).
   If any hunk's class is unclear, **stop and hand back the table** (no
   commits).
3. On local branch `ssx3` from the upstream pin in each repo: one commit per
   logical change (G26; G28; Turnip HAL loader incl. the reserved-block
   layout + static_asserts, default off; build shims if needed), clear
   messages naming the env var and the ssx3 report. The paraLLEl `ssx3`
   commit's Granite gitlink points at Granite's new `ssx3` head.
   Never commit binaries, dumps or generated files.
4. **Proof it's the same code minus diagnostics:** Mac build from the new
   branches (internal build dir), run the G43 Mac F0 shape on the G13 dump:
   the 10 scanout SHAs must equal G43's Mac run. Android build must compile
   (NDK r30, `/opt/homebrew/share/android-ndk`). No Odin run.
5. Report the exact push commands for the orchestrator, with the fork URLs
   (`https://github.com/brad-richardson/parallel-gs.git`,
   `https://github.com/brad-richardson/Granite.git`). Note: the Granite
   submodule's `origin` is **upstream Themaister**: never push anything
   yourself, and never to upstream.

## Budgets

2 Mac builds, 1 Android build, time box 3 h, internal byte cap 12 GB
(`local/tooling/disk_budget.sh` at start and end).

## Deliverable

`local/research/G45/REPORT.md`: hunk table, commit lists (both repos, with
SHAs), F0 scanout comparison (10/10), Android build receipt, push
commands. `[G45]` commit with `git add -f local/research/G45
local/muse/prompts/G45.md`, trailer `Orchestrated-By: Muse Code`,
`git log -1` first, no push of anything.
