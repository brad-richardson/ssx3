# E54F Part 1 — COP0 Count source and clock contract

You are a Codex worker in `~/dev/ssx3`. Follow `~/dev/AGENTS.md` and
`AGENTS.md`. This is a read-only source audit for the orchestrator's E54
gate, not permission to implement a fix. Read `local/research/E54D/REPORT.md`,
`local/research/E54E/REPORT.md`, and `docs/todo.md` E section. Hand back
the evidence table; the orchestrator decides the Part 2 mechanism.

Pin fork `ssx3` at `1aaed05` and read the **E54D worktree**
`~/dev/ssx3-work/E54D/PS2Recomp` (its source is pinned there; the top-level
fork checkout may be stale). Do not edit that worktree or canonical codegen.
You may write only `local/research/E54F1/REPORT.md` in ssx3, plus bounded
text excerpts under `~/dev/ssx3-work/E54F1/` (≤5 MiB). No build, boot,
device access, fork change, generated code, or push.

Known anchors to verify: `ps2xRecomp/src/lib/cop0_translator.cpp:46-48,96-98`
reads/writes `ctx->cop0_count`; `ps2xRuntime/include/ps2_runtime.h:113`
holds that field; `ps2xRuntime/src/lib/Kernel/EeScheduler.cpp:382,886-890,2872-2878`
owns `m_eeCycle` and can idle fast-forward it. The frontier review §A2
lists four static MFC0 Count sites 0x3f4414, 0x3f4e08, 0x3f4fa4,
0x3f4fc4, with **dynamic reach unknown**. E54E's candidate signed-branch
change failed a visual boot, so base every proposed fix on E54D `1aaed05`.

Correct-behavior question: at an EE `MFC0 Count`, what value should be
returned relative to EE cycles, and how should `MTC0 Count` affect later
reads and Compare/interrupt behavior? Alternatives: direct low32 EE cycle,
EE cycle at a fixed divisor, or an offset/epoch after MTC0. A static
source search alone cannot settle the rate or dynamic reach. Use pinned
PCSX2 `9056c0834` **primary source** as oracle: get only needed files
from that commit if no local copy exists, record URLs and SHA256, and
cite exact file/line ranges. Check EE hardware behavior in the same
source; if rate/interrupt semantics remain ambiguous, say so explicitly.

Evidence table (bounded reads, use `rg`/LSP and small `sed` ranges, not
whole files):

1. Our MFC0/MTC0/Compare paths, initialization, and **all** mutations
   of `cop0_count`; generated occurrences and enclosing guest functions
   for the four sites using `local/tooling/ee/ee-at`/`ee-func` or existing
   labels. Separate static presence from dynamic reach.
2. Scheduler cycles: where charged, when visible to generated code,
   idle fast-forward, and safe access from `ps2_runtime`/context. Pin
   file/line and call connections with LSP or direct callers.
3. PCSX2 Count rate, write behavior, Compare interaction and 32-bit
   wrap. Give a small table for two sample cycles, a Count write, and a
   later cycle, each alternative's prediction and the oracle's rule.
4. Recommend **one** Part 2 candidate interface and focused value
   tests, with explicit unresolved questions. No verdict that game code
   reaches a site without a dynamic receipt.

Stop if the pinned oracle cannot be located or contradicts the proposed
model; report the gap, do not guess. Time box 20 minutes. Deliver the
complete `REPORT.md` with exact commands, pins, source citations, bounded
receipt hashes and gaps. Commit only the report as `[E54F1]` with trailer
`Orchestrated-By: Codex` and explicit path; do not push.
