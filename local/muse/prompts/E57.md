# E57 — VU1 interpreter speed, bit-exact (Opus, 3 h)

Worker: Claude Code (Opus), approved by Brad 2026-09-24. You choose the experiments inside the rules below. Read `~/dev/AGENTS.md` (exploratory freedom overrides "follow the brief exactly"), repo `AGENTS.md`, `local/AGENTS.local.md`, `docs/facts.md`, `docs/orchestration.md` §3, then `local/research/{N5,E45,E53,E55C2}/REPORT.md`.

## Goal
The Odin race runs at **0.113×** (N10, sound off). The VU1 interpreter is the largest single cost: **50 % self time in the Odin race** (N5), and the pipeline/hazard bookkeeping alone is ~30 % (`commitReadyPipelines` 17.9 %, `calculatePairReadyCycle` 9.4 %, `markPairWrites` 2.3 %; N5 table). Land the biggest **bit-exact** VU1 speed-up you can, with a Mac measurement, so it can go to the Odin next.

## Facts
- Code: fork `~/dev/PS2Recomp` `ssx3` `f949ff0`: `ps2xRuntime/src/lib/vu/ps2_vu1_{core,upper,lower}.cpp`, `ps2_vu1_detail.h`, `include/runtime/ps2_vu1.h`; tests `ps2xTest/src/ps2_vu1_tests.cpp`. `VuWide=double` exact FMAC is folded (E45); quad stays behind `PS2X_VU_WIDE_QUAD`.
- SSX 3 uses 7 VU1 microprograms (7,305 instructions, E53); 9 race vsyncs hit the 33-program cap (todo "Race rendering gaps").
- Correctness oracle: `PS2X_DETERMINISTIC=1` + `PS2X_ENABLE_DET_HASH_TAP` (build flag) + `PS2X_DET_HASH_EVERY=N` gives per-VBlank XXH64 of RDRAM + VU1 execute count (E55C2). Two identical-settings runs repeat exactly. The GS stream capture/replay on `ssx3` gives a packet digest (TL1/`[gs-path]`).
- Route: I26-FAST (`local/research/I26/ROUTES.md`, race HUD ~t1714); boot script template `local/research/E55D16/e55d16_boot.py` (use an **empty** mc0, not Brad's seed).

## Correct-behavior model and the observable
A speed change is a **pure refactor** only if guest-visible results and cycle accounting are unchanged. Acceptance (computed by a script you write, `local/research/E57/check.py`):
1. Full suite from the worktree root: all pass (baseline count first).
2. Baseline vs candidate, same settings, deterministic, I26-FAST to ≥ t2400 (well into the race): **every** `[det-hash:v1]` line identical, and the GS packet digest identical over the same window.
3. Any hash difference = not bit-exact: stop that candidate, record the first differing tick, don't tune toward the hash.

Candidate directions (your call, labelled as hypotheses): cache static per-PC pair dependency/ready data (programs are fixed; key on microprogram memory contents); cheaper pipeline commit (event list instead of scanning); skip hazard work for pairs with no in-flight writers; decode caching. Static recompilation of the 7 programs is out of scope for this pass (write up what it would take).

## Measurement
- Speed numbers only from diagnostics-off Release builds (AGENTS.md "Speed numbers"): guest vsyncs per wall second ÷ 59.94 in the race window, baseline vs candidate, same binary config, **exclusive** mini lease (`p_lane_lease.py claim E57speed --exclusive` takes all 4 slots; wait politely if busy, don't hold it > 15 min). Also report VU1 share from a `sample`/xctrace profile (diagnostic, labelled).
- A microbenchmark (replay captured VU1 program + input data through the interpreter in a test) is welcome and doesn't need the lease.

## Rules
- Worktree `~/dev/ssx3-work/E57/PS2Recomp` on local branch `e57-vu1` from `f949ff0`; scratch `~/dev/ssx3-work/E57/` ≤ 20 GB. Never push; never `git add -f` in the fork; runner-dir check empty on every commit. No Odin, no iPhone, no bytesize needed.
- Diagnostic boots: one mini slot each (`p_lane_lease.py`), check/kill your runner by PID. Builds `nice`d.
- Text only in git (no PNG/binaries). No upstream contact. Time box 3 h from your first command; write up in the last 15 min; `NOTEBOOK.md` committed every ~30 min.

## Deliverables (commit `[E57] …`, explicit paths, trailer `Orchestrated-By: Claude Code`, no push)
`local/research/E57/NOTEBOOK.md` (append-only), `check.py`, `REPORT.md`: candidate(s) with fork commit SHAs + `--stat`, suite counts, hash/digest equality table (first differing tick if any), Mac race speed baseline vs candidate with runner SHAs (two reads), VU1 profile share before/after, what's left (static recomp estimate), gaps. Recommend whether it's ready for an Odin speed pair; the orchestrator decides.
