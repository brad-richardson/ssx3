# G32 — sample_quad[0] VRAM-sample unit probe: controlled patterns through sample_circuit[0] (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G31/REPORT.md` first (all of it:
promotion off structurally + on-device (`nprom=0/hack=0/p1null=1` ×16),
DISPFB1 region stale load at all 16 sample times, circuit emits uniform
cleared pattern via the always-taken `sample_quad[0]` VRAM path: (b1)
CONFIRMED, (b2) refuted structurally). This brief executes G31 §4's ONE
next action ONLY. No upstream contact of any kind (standing no-upstream
order — local hunks only, filing stays local).

## Facts you start from

- The defect is localized to "the circuit's VRAM sampler emits cleared
  pattern while its addressed source holds stale bytes", with all
  sampler inputs pinned (FBP=112/FBW=8/PSM=1/DBX=DBY=0, DW=2560/DH=447/
  MAGH=4/MAGV=0, super_samples=1, `vram_size-1` spec; G31 §3g).
- Discriminator (G31 §4): controlled VRAM patterns (e.g. address-echo
  words) sampled through `sample_circuit[0]` with known push constants —
  maps WHERE the Adreno sampler actually reads (addressing vs decode vs
  bindless-descriptor fault).
- The G31 binary is INTACT (`c91719a0…`, re-sha at start per hygiene —
  still no 10th recurrence at the G31 gate). The G28 binary is INTACT
  (`450471e2…`). Clone holds G22/G26/G28/G29/G30/G31 hunks uncommitted
  (HUNK_MATCH re-verify at start; G29-E1 truncated FNV basis carries
  for comparability).
- Task 1 (no device until §2): STATIC design — table the pattern set
  (address-echo words? gradients? per-page tags?), the injection site
  (VRAM preload vs in-run write — with the mechanism each choice
  assumes), the known push constants, and the decision matrix
  (addressing-shift vs decode-fault vs descriptor-fault signatures +
  what mixed outcomes mean). Name the ONE hunk.
- Task 2 (ONE run max): the unit probe — ONE hunk max (diagnostic-dump
  ONLY + the pattern injection it requires — table the injection as
  part of the hunk, no separate change), ONE build max (NEW dir, G22
  recipe, `-j2`), full identity, verify-then-push with NO gap
  (push-time + post-run + report-time re-shas — destruction is
  progressive), TWO bounded runs max (diagnostic + one retry iff O1/O3
  fires instead of first draw).
- Success bars (tabled): design table (patterns + injection + constants
  + matrix); the addressing-vs-decode-vs-descriptor VERDICT (with the
  discriminating bytes); if a run: exit/wall/fate + receipts + tombstone
  triage (if any) + scanouts scored AND pulled by explicit filename
  list (G31-E1 lesson — never a remote glob; cleanup as its own
  verified step); the ONE next action (fix shape vs next wall —
  adoption stays queued until brightness is understood at the sample
  level).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g32/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max; ONE build max (new dir, G22 recipe, `-j2`); TWO bounded
  device runs max; no tuning loop, no second shape, no new dumps; no
  lldb unless the tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g32/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G32/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G32]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).
  Fill in the ACTUAL time used (no `~X h` placeholders).
