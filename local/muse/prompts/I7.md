# I7 — Wire UIKit entry: SDL video init to first window (or next wall)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3`. **Tables,
no verdicts.** Read `/Users/bradrichardson/dev/ssx3/local/research/I6/REPORT.md`
first (all of it: the `bdae295` identifier fix, install exit 0, the argv
probe's `WARNING: SDL: Failed to initialize SDL` + SEGV in `rlLoadTexture`,
the 6 gap rows, the standing measurement rules). This brief is I6 gap 1's
exact next brief: wire UIKit entry so `InitWindow` proceeds past SDL video
init toward `loadELF`.

## Facts you start from

With plain `main` and no `SDL2main`/`UIApplicationMain`, SDL video init
fails on the sim and `InitWindow` SEGVs with no GL context behind it (I6
Step 3 + crash stack). `simctl launch` forwards argv and `argv[1]` is
honored (`Using argv boot path`); no-arg launch clean-fatals on the missing
ELF path (I6 gaps 3/6 carried, not this brief). P-lane (P1w/P1y) is
concurrently active in the SAME fork clone — the named-files discipline
below is load-bearing. Standing rules from I6 gaps 3–4: stdout via
`--console` with a shell redirect (simctl-owned writes fail on the
external volume); exit behavior from console text + `.ips` presence, never
the simctl exit code; screenshots to an internal path first, then `mv`.

## Gates and rules

- No PS2 boots in this brief → no host lease needed. Builds may run any
  time (`-jobs 4`).
- Fork: touch ONLY iOS entry files (the `PS2X_IS_IOS` CMake blocks and
  any `main.cpp`/entry iOS branch your approach needs — record the
  list). `git add` those NAMED files only — never `-A`, never stage or
  touch P-lane files. Verify the diff before committing. One commit per
  change with the `Orchestrated-By: Muse Code` trailer. `git pull
  --rebase` before pushing; `git push` ONLY inside the fork clone to
  the `fork` remote. If the rebase shows any conflict outside your
  files, stop and report — do not resolve P-lane's side.
- Simulator: use ONLY the installed iOS 27.0 runtime + ONE existing
  device (iPhone 17 `4DE6C37C-77EF-45AE-827E-26286E68D022` unless it is
  unavailable — record substitution). NEVER download runtimes or
  platforms. Boot it yourself, shut it down at the end.
- Disk: internal disk is tight — build + logs under
  `/Volumes/Extreme SSD/ps2x-i7/` (new dir); small logs (<5 MB total)
  may live in the evidence dir. Never touch `/tmp/p1-link`,
  `/tmp/ps2xgs-build*`, `/tmp/ps2x-ios-spike`, `ps2x-i3`, `ps2x-i4`,
  `ps2x-i6` (read from them, never write into them), or other agents'
  dirs.
- Evidence dir `local/research/I7/` (commit with `git add -f`, prefix
  `[I7]`, trailer `Orchestrated-By: Muse Code`). Do not commit
  screenshots over 5 MB total (downscale or describe). NEVER run
  `git push` in ssx3.
- Time box 4 hours.

## Steps

1. **Wire UIKit entry** (implementer's call: link `SDL2main` / call
   `UIApplicationMain` / SDL iOS bootstrap — record the choice and why
   the others were not chosen). Show the diff is entry-only; commit
   (named files), pull --rebase, push fork remote only.
2. **Rebuild.** Fresh Release-iphonesimulator build in the `ps2x-i7`
   scratch dir (same I6 configure shape). Record: configure exit,
   build result, `.app` bytes/arch/symbols (baseline: `_main`,
   `_SDL_Init`, `_InitWindow`, ~292 SDL symbols + whatever entry
   symbols your approach adds — `nm` receipt), and `Info.plist` still
   carries the identifier.
3. **Re-measure.** Boot, `simctl install` (exit + bundle id), then the
   I6 launch shapes: no-arg `--console` (expect the same clean fatal —
   record it) + argv probe `--console` (does SDL video init pass now?
   does `InitWindow` reach `loadELF`?). Record console texts complete,
   crash report if any, one screenshot if a window appears (internal
   path first). If `loadELF` runs: record EXACTLY what it does with the
   probe path (gap-6 strategy is still a LATER brief — observe, don't
   design).
4. **Report.** `local/research/I7/REPORT.md`: approach choice + fork
   diff receipt, build/install/launch receipts, console excerpts,
   screenshot (or its absence reasoned), entry/lifecycle map update,
   new gap rows with the exact next brief each needs, exact commands,
   receipt paths, "What I could not do". No verdicts. Commit
   `local/research/I7/`, shut the sim down, stop.
