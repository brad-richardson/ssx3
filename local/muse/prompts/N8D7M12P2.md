# N8D7M12 Part 2 — dev-only Android replay entrypoint (OpenCode Go, source only)

You are the N-lane implementation worker. Read `~/dev/AGENTS.md`, repo `AGENTS.md`, `local/AGENTS.local.md`, full N8D7M11 design/gate, N8D7M12 Part 1 report/gate and the pinned fork source `ps2xRuntime/src/main.cpp`, `gs_replay_core.h`, Android env shim, CMake and manifest. Use LSP when available and direct reads otherwise; cite the repo, not the web. The orchestrator created isolated `~/dev/ssx3-work/N8D7M12P2/PS2Recomp` branch `n8d7m12-app` at `24801bc`. Own only that worktree, this brief and `local/research/N8D7M12P2/`. This pane has an edit exception only for `~/dev/ssx3-work/N8D7M12P2/**`; test/remove a tiny sentinel before edits, stop on denial. No Android/Mac build, replay, APK transfer, stream copy, Odin/iOS action, lease, push, board/global edit, upstream contact or generated guest code. Time box 25 min, text <512 KiB.

Goal: add the **single** default-off Android NativeActivity replay branch described in N8D7M11. Only `PS2X_GS_REPLAY_ONDEVICE=1` may enter it; unset, empty, `0` or other values must continue the existing game boot path. Place it after Android `redirectStdioToLogcat()`/terminate setup but before `getExecutablePath`, `PS2Runtime` construction, `initialize`, `loadELF` and `run`, so no live EE/game thread starts in replay mode. Require nonempty `PS2X_GS_REPLAY_CAPTURE`, `PS2X_GS_REPLAY_BACKEND=parallel` and `PS2X_GS_TURNIP=1`; reject missing/mismatched flags explicitly instead of silently running CPU/system Vulkan. Call the Part 1 `ps2x_gs_replay_run()` core and map its result flags to one clear success/failure line and exit code. Preserve current `main()` process-exit/log-flush pattern; inspect its `std::_Exit` and logcat thread before choosing return/exit behavior. Key and capture path come from the already-proven `ps2x.env` app-files shim; do not add manifest/Gradle permissions, a new binary or test harness.

Acceptance is **source only**: checker asserts pinned branch, single Android-only branch before all game-boot calls, strict key and backend/Turnip/capture checks, core call, result gate, default-off fallthrough and runner-dir diff empty; compare `main.cpp` to base `24801bc` and show only the bounded hunk. A = source-grounded branch with all checks; B = one source condition missing (state smallest fix); OTHER = pin/permission mismatch. Source strings cannot prove Android compile, loader or runtime behavior. No build/device verdict. Deliver REPORT, `check.py`/result JSON, exact source diff receipt and gaps. Commit fork `[N8D7M12] Part 2` on local branch with `Orchestrated-By: opencode`, no push. Commit ssx3 `[N8D7M12] Part 2 source` with only named evidence and this brief, same trailer, no push; stage by explicit paths. Hand back the table, do not conclude Android behavior.
