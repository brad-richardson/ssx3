# I25 — iOS to parity with Android, installed on Brad's iPhone (quick check-in build)

You are a worker in a herdr pane in the ssx3 repo (`~/dev/ssx3`, Mac mini).
Follow `~/dev/AGENTS.md`. **Tables + receipts; recommend, the orchestrator
decides.** Read first:
- `AGENTS.md` (Devices: always `install`, even if the app is present;
  iPhone installs are fine);
- `local/AGENTS.local.md`;
- `local/research/I23/REPORT.md` (the iOS build, FFmpeg prefix, staging,
  signing and install recipe);
- `local/research/N6/REPORT.md` and `local/research/N4/REPORT.md` (what
  Android has).

## Why

Brad is around his iPhone far more than the Odin. He wants a current
build on it for quick check-ins, to flag overall quality issues early
(like the interlace stutter). It has to match what Android and the Mac
show today: the menus, the Select Character rider, the race.

## Facts

- The fork builds for iOS (`PS2X_IS_IOS`: raylib on SDL2 + GLES2; FFmpeg
  7.1.x iOS prefix; recipe in `ps2xRuntime/cmake/iOS-FFmpeg-7.1.1.md`).
  The last device build was I23 on the laptop, iPad only, at old fork revs,
  with build assets on the SSD (`/Volumes/Extreme SSD/ps2x-i23/`: SDL2 and
  FFmpeg prefixes).
- **Build on the internal disk.** NDK/lld-style outputs on the ExFAT SSD
  can read back as zeros. Copy any prefix you need to
  `~/dev/ssx3-work/I25/` and check SHAs.
- Signing on the mini: "Apple Development: Brad Richardson (E4R78PLLKY)" and
  provisioning profiles in `~/Library/MobileDevice/Provisioning Profiles`.
  Use I23's wildcard profile/entitlements approach. Target: "Brad's iPhone"
  (UDID `00008140-0002505001F3001C`, iOS 27); `devicectl` with explicit
  `--timeout`.
- Current runtime: fork `ssx3` `b48b502` (E49); codegen
  `~/dev/ssx3-work/codegen-ssx3-e49` (19 `extra_function_starts`). Work in a
  worktree on a new local branch `i25-ios` off `ssx3`. E50 has its own
  worktree; don't touch `~/dev/PS2Recomp` itself.

## Parity list (Android → iOS)

1. The env shim equivalent: a `ps2x.env` (or plist) read at startup from
   the app bundle or Documents, setting:
   - `PS2X_SKIP_MOVIE=1` (dev default);
   - the CD image path;
   - `PS2X_DEINTERLACE=weave`;
   - optional `PS2X_PAD_SCRIPT` + `PS2X_PAD_SCRIPT_CLOCK=vsync`.

   Brad must be able to launch it without a Mac attached.
2. **A no-controller check-in mode:** a bundled vsync pad script that
   auto-routes title → Select Character → Happiness race (E33/E46 route
   strings). Brad just opens the app and watches. Make it the default for
   this build, and say how to switch it off.
3. Controller input: N6's union of keyboard and gamepad on Android.
   Confirm that SDL's game-controller path works on iOS (MFi, Xbox or PS
   controllers paired over Bluetooth), and that the script still takes
   precedence when set. A touch overlay is out of scope.
4. Fit the 512×448 output to the phone screen in landscape, aspect
   preserved, with no stretching.
5. Skip PNG frame dumping by default (a diagnostic cost).

## Validation (on the iPhone)

- Install and launch: title, auto-route to Select Character (rider
  visible), then the race.
- Get screenshots: try `xcrun devicectl` capabilities first, then in-app
  dumps copied out of the app container. Report the approximate guest
  vsync rate, labelled diagnostic.
- Keyguard/locked-phone behaviour: note it; don't fight it.

## Budgets and stop rules

Builds as needed, 5 h, cap 15 GB (the app bundles the 3 GB ISO: stage it
once). If signing or provisioning blocks you (for example an expired
profile, or a device that isn't registered), stop and report it as a Brad
blocker with the exact message.

## Deliverable

`local/research/I25/REPORT.md` with:
- the diff summary;
- a build recipe on the mini (so an Opus or Qwen worker can rebuild it
  later from one script, `local/research/I25/build-install.sh`);
- the parity table;
- the screenshots;
- **a 5-line "how to use it" for Brad.**

`[I25]` commit (`git add -f local/research/I25 local/muse/prompts/I25.md`,
`git log -1` first, trailer `Orchestrated-By: Claude Code`). Fork commits
stay on local branch `i25-ios`; no push.
