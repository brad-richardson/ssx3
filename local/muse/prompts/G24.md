# G24 — Plumbing: deliver `debug_mode` unconditionally in the dump replayer (ONE hunk), rebuild, re-run the flag run + pixel-diff vs G13 (Odin)

You are muse in a herdr panel in `/Users/bradrichardson/dev/ssx3` (working
tree: `/Users/bradrichardson/dev/ps2xGS`, SSD lane dirs as below).
**Tables + hypothesis + next-action recommendation, no verdicts beyond the
hypothesis.** Read `local/research/G23/REPORT.md` first (all of it: the
upstream flag is a RenderDoc-gated NO-OP — the ONLY `set_debug_mode` call
(`tools/gs_dump_replayer.cpp:101`) sits inside `if (use_rdoc)` (`:98-103`,
HEAD-identical upstream shape) while sibling replayers call it
unconditionally; run 2 receipted `use_rdoc=false`, so the flag never left
the local struct and O4 fired textbook). This brief executes G23 §4's ONE
next action ONLY: plumb delivery, rebuild, re-test. No upstream contact of
any kind (standing no-upstream order — local hunk only, filing stays a
local doc; fork-and-fix standing order covers app-side hunks like this).

## Facts you start from

- The hunk (ONE, local-only, SSD clone `parallel-gs-g7` @`3a66c19…`):
  move `iface.set_debug_mode(debug_mode)` out of the `if (use_rdoc)` gate
  so it runs unconditionally (sibling shape: `gs_repro_replayer.cpp:165`,
  `gs_stream_replayer.cpp:185/235`). At most a one-line LOGI delivery
  receipt may ride in the SAME hunk (table the choice — the receipt
  proves delivery independently of the behavioral discriminators). No
  other source edits; hunk stays UNCOMMITTED in the SSD clone (G18/G22
  precedent) with its diff text committed beside the report.
- Rebuild in a NEW SSD build dir (G22 recipe: NDK r30, arm64-v8a,
  `-j2`, `parallel-gs-replayer` target). Re-sha discipline: start,
  pre-push, report-time (standing hygiene — zero-fill recurred before).
  G14/G18/G20/G22 build dirs are read-only (0 growth).
- The run: the SAME bounded flag run as G23 run 2 (dump/iterations,
  flag SET, `PGS_SKIP_SAMPLER_FEEDBACK` UNSET,
  `PGS_SKIP_COMPILATION_TASKS=1` KEPT — table the knob matrix explicitly
  if you deviate, with rationale). Discriminators: delivery receipt (if
  the LOGI rides along) + O4-absent legs (G22 §3b 4-leg shape) + bright
  pixels (`g14-diff.py` vs G13, zero new tooling). O5 may re-fire
  post-`Done!` (table it either way — same signature or drift).
- Success bars (tabled): hunk minimal + behavior-preserving except
  delivery (table the no-new-hazard reasoning); build exit 0 + binary
  identity (size/sha/BuildID); ONE run (exit/wall/fate + receipts +
  tombstone triage if any); scanouts scored (exact/le2/le32 + means —
  bright vs black vs N/A decides the adoption shape).

## Gates and rules

- Device: Odin3 `622c49b1` ONLY; `/data/local/tmp/g24/` transient dir
  ONLY, removed at end (`mg/` only). No P-lane lease, no bytesize/WSL.
- ONE hunk max (the move + at most one LOGI line); TWO bounded device
  runs max (flag run + one retry iff O1/O3 fires instead of first draw);
  no tuning loop, no second shape, no new dumps; no lldb unless the
  tombstone cannot triage.
- `export COPYFILE_DISABLE=1` on all SSD steps; byte caps declared +
  tracked (new build dir ≤6 GB; new `ps2x-g24/` retrieval dir ≤50 MB;
  0 growth elsewhere).
- Evidence dir `local/research/G24/` (STANDALONE, text only — no
  binaries). Commit with `git add -f`, prefix `[G24]`, trailer
  `Orchestrated-By: Muse Code`. Do not push (orchestrator pushes at poll).
- Experiment contract up front (hypothesis/observable/alternatives/stop).
  Time box: 6 h. Tables + hypothesis + next action, no other verdicts.
- Hygiene: report in chunks + tail receipt (truncated tail fails the gate).

## Task 1 — hunk + rebuild (no device until §2)

1. Re-verify pins (clone HEAD/tree, G22 HUNK_MATCH, dump sha, dirs
   0-growth, device pre-check). Write the ONE hunk; table the
   no-new-hazard reasoning + LOGI choice. Configure + build in the NEW
   dir (exit 0, warnings tabled); binary identity (size/sha/BuildID,
   ELF magic); `strings` presence check. Table every step.

## Task 2 — ONE flag run + score + adoption input

1. Push + run per the knob matrix; pull logcat + tombstone (if any) +
   scanouts; remove the device dir. `g14-diff.py` vs G13 + pixel census.
   O4-absent/-present legs + O5 table. Name the ONE next action the
   numbers justify (adoption shape vs next wall).

## Report

`local/research/G24/REPORT.md`: contract, hunk + build, run table,
score, hypothesis verdict, next action, exact commands, gaps. Commit
evidence (`[G24]` + hunk diff text), stop.
