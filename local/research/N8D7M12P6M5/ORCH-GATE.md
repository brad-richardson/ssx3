# N8D7M12 Part 6M5 orchestrator gate — staged source A (2026-09-24)

Read full REPORT, committed 24-row checker and commit `b9efb6de` (`git show --check` clean). Independently reran checker: 24/24 A, including a fresh collector verify match. Independently reran `rsync -ani --checksum` for renderer and codegen with the recorded exclusions; both produced no lines. Read the private manifest: 26,211 entries, 612,576,408 file bytes, aggregate `6877de873b197bf915d068d1b03331e6488c529e6bdac64273513f338080316a`; no symlinks, no excluded/cache/receipt entries, sole fork runner file is the pinned upstream stub. The staged JNI scope contains exactly the 7,112-byte HAL member and 14,188,488-byte Turnip member with the reported SHAs. Disk budget after staging 157.5/200 GB; stage 645 MiB.

**A for clean next-package source-byte staging and manifest.** This is a new baseline, not a recovery of every old APK input. The dirty renderer patch stack is included byte-for-byte but not correctness-reviewed here; HAL is staged from the old APK member, while its historical source transform remains unknown. The package worker must transfer/verify these exact staged roots and capture build flags, actual compiled file list, native SHA/Build ID and APK SHA. No build, device, image, GPU cause or speed verdict.
