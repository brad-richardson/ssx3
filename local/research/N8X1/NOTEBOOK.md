# N8X1 notebook (append-only)

Clock start: 2026-09-24 18:26:28 EDT (first command). Time box ends 20:26:28, write-up by 20:41.
Worker: Claude Code (Opus 5.5), brief `local/muse/prompts/N8X1.md` + orchestrator lead `local/research/N8D7M13/ORCH-GATE-B.md` (first Odin VRAM departure tick44).

| time | change (env/code/binary SHA) | result (rows, first VRAM split vs Mac/prev, non-black frac @2050) | frame | takeaway |
| --- | --- | --- | --- | --- |
| 18:30 | Mac: synthetic stream `s44split.gs` (sha `6d16eab5…a119`, 10,115 B) = original records through tick43, tick-43 GIF packets split at EOP, a marker (tick=44) after each piece; STEP=1, host build `N8D7M12P5F4/build/ps2xTest/ps2x_tests` @4fa0df1 | 55 rows; final row `97b13124` = Mac ground truth tick44. Piece hashes: path3 copy → `ced3975a`, black strip clear → `ab21b091`, logo sprite (path2 off 9353) → `97b13124` | n/a | Synthetic mid-tick markers reproduce ground truth; bisect method valid |
| 18:31 | Odin: same `s44split.gs` pushed to `<FILES>/n8x1-s44split.gs` (2 SHA reads match), APK `da9a41a8…` (installed SHA re-read), env replay-only STEP=1 | 55 rows; ticks 1..43 + pieces 1–6 equal Mac (`ced3975a`, `ab21b091`); **first diff at the logo sprite piece: Odin `e11bfd7c` vs Mac `97b13124`** (= M13 Odin tick44 hash) | n/a | The first Odin VRAM departure is one path-2 packet: PRIM 0x5e sprite (TME, IIP, ABE, STQ floats), TEX1 0x61 (bilinear), TEX0 `0x2005400555b04e00`, RGBAQ alpha 0x15, ALPHA 0x44. The 16-strip path-3 FB copy (bilinear, half-texel) and the black clear match exactly. Note: the app auto-relaunched after `_Exit` (5 replays in 12 s, all identical) — runner fixed to force-stop on `replay ok` |
| 18:34 | Variant streams (`tools/mkvariant.py`): prefix to the tick-43 logo sprite, optional injected A+D packet, logo packet, marker. Mac vs Odin final VRAM | base DIFF (97b13124/e11bfd7c); ZMSK=1 DIFF; FBMSK rgb-only DIFF; alpha-only DIFF; ABE off DIFF; **TEX1_1=0 (nearest) EQUAL `160743b5`** | n/a | The departure is in the **bilinear** texture path of the STQ sprite (8H/CLUT texture), in both colour and alpha; not Z, not blending |
| 18:36 | (note) first three row times corrected from file mtimes before first commit; they had been typed from memory | | | |
| 18:39 | Full stream rewritten with every A+D TEX1_1/TEX1_2 forced to nearest (`tools/nearestify.py`; 48,358 TEX1 writes, 46,723 patched; `nearest-full.gs` sha `cca1d32f…bb51`, same size). STEP=50, PPM 2050, Mac + Odin | **VRAM equal 50..650 (13 rows), first split tick700**, diff 700..1450, **equal again 1500..1600**, diff 1650..2050. Odin frame nonblack 0.089 (Mac 0.995), 2.4% px equal | `runs/{mac,odin}-nearest-full/frames/vq-002050.ppm` | Bilinear precision explains the tick44 split, but a **second fault** starts in 651..700 and still blacks out the Odin frame; the re-convergence at 1500 shows VRAM gets fully rewritten (level load) and the second fault re-fires after it |
| 18:41 | `n701.gs` = nearest stream truncated after marker 701 (29.3 MB), STEP=1, Mac + Odin | first diffs tick665–666 (transient, equal again 667–673), **persistent from tick674** | n/a | Two events inside the nearest run |
| 18:44 | `n665split.gs` / `n674split.gs` (nearest prefix, tick-664 / tick-673 packets split at EOP with a marker = drain + VRAM readback after each piece; 126 / 334 pieces) | 665: pieces 0–107 EQ, **first diff piece 108** (path-1 PRIM 0x5c tri-strip, TME+ABE, STQ floats, Q=1) even with per-piece syncs. 674: **all 334 pieces and the final row EQ** — Odin final `4e882d1d` = Mac (Mac split-final = Mac unsplit row674 `4e882d1d`; Odin unsplit row674 `cc69ac5e`) | n/a | tick665 = deterministic per-primitive difference (likely STQ interpolation/snap precision; transient). **tick674 = a missing sync inside the batch: extra drains/readbacks between packets make the Odin exact** |
| 18:45 | Nearest stream + `TU_DEBUG=flushall` and `TU_DEBUG=syncdraw` (logcat confirms Turnip parsed `TU_DEBUG=0x201` for startup,flushall) | identical row pattern to plain nearest (split 700, re-converge 1500–1600); frame nonblack 0.066 / 0.060 | `runs/odin-nearest-{flushall,syncdraw}/frames/vq-002050.ppm` | Turnip cache-flush/sync levers do not touch the second fault |
| 18:51 | `tools/hazard_bisect.py`: tick-673 pieces (344), marker after a subset only, Odin vs Mac `4e882d1d` (18 Odin runs) | none ✗, all ✓; prefix needs markers through piece 257; suffix needs a marker from piece 66 on; single marker at 257 ✗ | `hazard-674.log` | **Not one writer→reader hazard**: the fix needs many batch cuts. Pieces 66/257 are ordinary glyph sprites (PRIM 0x56) with no texture change between them → points at batch **size**, not a RAW pair |
| 18:55 | Source read (no run): paraLLEl-GS `gs/gs_renderer.cpp:1937-1948` `get_target_hierarchical_binning()` returns 1 under `#ifdef __APPLE__` ("Broken Metal drivers can't deal with the hierarchical binning"); otherwise hierarchical when `num_primitives >= 256`. `set_hierarchical_binning_subgroup_config` (2212-2236) falls back to a free 4..128 subgroup range + `wg = vk11.subgroupSize * hier²` when an exact 32-wide subgroup is unsupported | n/a | n/a | **Hypothesis H2: the Mac ground truth never runs the hierarchical binner; the Odin runs it for every ≥256-primitive render pass and it bins differently there.** Markers cut passes below 256 → flat binner → Odin exact. Test = env knob forcing flat binning on the Odin |
| 18:58 | Patch `patch/n8x1-hier.diff` (72 lines) on the staged paraLLEl-GS `gs/gs_renderer.cpp` (`85c29cb0…` → `0613bbbf…`): dev-only `PS2X_PGS_HIER=off|on|wave64` (off = flat binning always; on = allow hierarchical on Apple; wave64 = hierarchical requires 64-wide subgroups) + one-time LOGI of the knob and of the 4..128 fallback. Mac `-fsyntax-only` rc=0. APK build 1/4 started on bytesize `~/n8x1/root` (fresh copy of `n8d7m12p6m6`); Mac host build `~/dev/ssx3-work/N8X1/macbuild` started | pending | n/a | Build discriminates H2 on the Odin (off) and on the Mac (on) |
| 18:57 | Mac host build `macbuild/ps2xTest/ps2x_tests` (fork 4fa0df1 + patched paraLLEl-GS copy `~/dev/ssx3-work/N8X1/pgs`), nearest stream STEP=50: default and `PS2X_PGS_HIER=on` | patched default rows **byte-identical** to old Mac rows; HIER=on rows all 41 = Mac default; frame 0.995 nonblack. Log: fallback `range 4..128, wg=32 (hier=1)` on the Mac | `runs/mac-nearest-hier-on/frames/vq-002050.ppm` | Knob build doesn't perturb the Mac. Allowing hierarchical binning on Apple changes nothing (gap: no log proves hier>1 actually engaged on the Mac) |
| 18:59 | Old APK, nearest stream again (run 2), STEP=50 | Odin run1 = run2 through tick800, split from 850; frame 0.047 nonblack, 88% px equal to run 1 | `runs/odin-nearest-full-r2/frames/vq-002050.ppm` | Run-to-run variance from 850 persists without bilinear: third effect, or the same binning fault racing |
| 19:04 | APK build 1/4 OK (`BUILD SUCCESSFUL in 9m 43s`): APK `19fed514a192468529dcb1ecf7dc3e86125d2cd90f3645ccbc927dfd6a068569` (WSL + Mac reads), runner `8ef277c9…6ef2` (contains the knob strings), Turnip member `717812c3…` unchanged. `adb install -r`, installed SHA read = `19fed514…` | — | — | — |
| 19:05 | New APK, nearest stream STEP=50: `PS2X_PGS_HIER=off` vs default | **off: 34/41 rows = Mac (first diff 950), frame nonblack 0.995, 97.4% px = Mac**. default: old pattern (split 700, 1500–1600 re-converge), frame 0.062 nonblack | `runs/odin-a1-nearest-{off,default}/frames/vq-002050.ppm` | **H2 confirmed on the device: hierarchical binning on the Odin causes the black frame** |
| 19:06 | New APK: original stream + HIER=off; nearest + `wave64`; nearest + off (repeat) | orig+off: frame nonblack 0.998, 78.8% px equal, 91.0% within ±2 per channel (remaining = bilinear precision), rows ≠ from tick50 as expected. **wave64 = off exactly** (same rows, same frame). off r1 = r2 **byte-identical**. Logcat default-mode line: `hierarchical binning subgroup fallback: range 4..128, wg=512 (hier=2)` | `compare-2050.png` (Mac / old Odin / Odin off / Odin wave64 nearest / Mac nearest), `runs/odin-a1-orig-off/frames/vq-002050.ppm` | The fault is the **free-range subgroup fallback** in the hierarchical binner, not hierarchical binning as such: a fixed 64-wide subgroup fixes it and also removes the run-to-run variance. Viewed: Odin off frame = full race scene like the Mac |
| 19:08 | APK build 2/4 (incremental, `BUILD SUCCESSFUL in 16s`, native task re-ran): APK `132344204d25880e508a0cbdb3b029414a0beed59c2b91024ce2a4dbfb55efe3` (2 reads), runner `33017577…a428`; patch `n8x1-hier-knobs-build2.diff`: default = candidate fix (fixed 64, then 128, before the free range) + modes `wave128`, `fallback`. Mac rebuild with same patch: nearest rows + frame **byte-identical** to old Mac; orig stream 41/41 VRAM+priv = M13 Mac (PPM differs from the M13 frame, but the old Mac binary with the same env gives the identical PPM → env/present path, not the patch) | — | — | Fix leaves the Mac untouched |
| 19:09 | Live boot 1 (orchestrator steering), knob APK `19fed514…`, N8D2 env (parallel, Turnip, `PS2X_SKIP_MOVIE=1`, I26-FAST vsync pad, empty `mc0`) + `PS2X_PGS_HIER=wave64` | menu screencap at guest tick ~1000; race screencap on the tick-2050 host dump. **Screen: Select Peak menu and the race (2ND/2, 00:00:05, terrain, rider) render correctly.** Rate line invalid: my 2-value `PS2X_FRAME_DUMP_ONCE_TICKS` isn't parsed (needs 3), so 1,551 per-present dumps ran | `runs/live-wave64/screen-{menu,race}.png` (`714a59da…`, `2630f3c9…`), `live-wave64-screens.png` | First correct live Odin race screen |
| 19:13 | Live boot 2, same APK/env, `PS2X_PGS_HIER=off`, dumps fixed (`1000,2050,999999`) | menu screencap at tick ~1012 (t=40 s, menu mid-transition: right panel empty), race at tick ~2028 (t=129 s) with host dump tick 2050 `fnv1a=4483c15c fbp=112/112 fallback=0`. `[vsync-rate]` early 0.74x (tick 224), race segment **0.12x (7.39/s at tick 2028)** — diagnostic build, no speed claim | `runs/live-off/screen-{menu,race}.png` (`dcdae82f…`, `dfbfc2da…`), `live-off-screens.png` | Race renders correctly with flat binning too |
| 19:15 | Build 2 APK, nearest stream replays: default (no env), `wave128`, `fallback` | **default = build-1 wave64 rows byte-identical**, frame 97.4% = Mac. **wave128: broken like upstream** (split 700, 0.063 nonblack). fallback: broken (0.071), logs `range 4..128, wg=512 (hier=2)` | `runs/odin-a2-nearest-*/` | The fault needs 128-wide waves, not a varying size: required wave128 fails too. Candidate fix validated as the default path |
| 19:16 | Cleanup: force-stop (no PID), knob APK `19fed514…` reinstalled (installed SHA read), `ps2x.env` restored `176eff84…` (2 device reads = backup), stream `f6a78f71…` intact, lease `LEASE_FREE N8X1 explore done`. My `n8x1-*` files stay under `<FILES>` (largest `n8x1-nearest-full.gs` 1.1 GB) | — | — | — |
