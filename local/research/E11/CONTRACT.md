# E11 experiment contract

Start 2026-09-21 07:44:49 UTC; deadline 15:44:49 UTC. Latest main pulled
(already up to date). E11 brief and E10 REPORT/NEXT-BRIEF read in full.
The renewed exception authorizes only the 0x2c5140 query entry. DROP generation
will not be repeated. Standing no-regen resumes at closure.

| Hypothesis / observable | Alternative / stop |
|---|---|
| Active DROP checkpoint is exactly E10's completed output. | Compare all 9,449 names/hashes, registry and runner SHA; rerun suite and actual-binding absent test. Any mismatch is tabled before reclamation/regen. |
| Retired /tmp/*-link trees can supply the declared APFS reservation. | Oldest first; live lane-board and no live file-handle proof per tree; before/after df per removal. Protect /tmp/p1-link, DerivedData and any live owner. Stop removing at admission + 512 MiB headroom. If unavailable, outcome (iii), exact next target, no CSV mutation. |
| One sanctioned CSV row restores exact query entry. | Persist fresh admission before edit; recheck immediately before spawn. Normal generator completion, changed-byte installation, actual generated binding, all four truth-table cases and preservation checks; DROP and suite rechecks. Failure → (iii), no second gap fix. |
| One guarded boot joins the query result to intended guest progress. | Dynamic a0/vtable, constructor/UI pointer, query return, state-3 request path, UI pending, packet→GS→D→field Present. First remaining wall → (ii); demonstrated advance → (i). Missing/truncated observation is tabled with its precise repair; no second boot. |

| Resource / ownership | Upfront bound |
|---|---|
| Fresh admission | 1 GiB logical output reservation + 2 GiB internal free floor + 256 MiB guard = 3,489,660,928 B; reclamation target adds 512 MiB = 4,026,531,840 B. Persist both df readings and disk_usage. Never lower guards. |
| Scratch | Fresh owned /tmp/ssx3-e11-query-codegen; only query regeneration, cleaned after retained manifests and installed-copy verification. |
| Generation | E10 bounds: 1 GiB logical, 1.25 GiB allocated, 128 MiB output headroom; 1200 s wall with 15 s reserve; 16 MiB log with 1 MiB reserve; 0.25 s polling; free floor + guard enforced. |
| SSD installation | Changed bytes only; disposition leftovers first. 24 GiB allocated-growth cap with 256 MiB guard; 2 GiB free floor. Remove only metadata companions proven created by E11's writes. Generated sources never staged. |
| Builds | Existing active /tmp/p1-link/runtime; -j4; no deletion of this tree or DerivedData. Isolated test output on SSD. |
| Boot | At most one, planned wall 90 s (maximum 600), SIGTERM at wall minus 15 s or first progress/byte cap; 1,000,000 syscall lines; shared P-lane lease, never wait if occupied; immediate release before analysis. |
| Capture bytes | Logical/allocated aggregate 1.5 GiB; boot log 256 MiB, syscall trace 96 MiB, function trace 1 GiB; bounded per-directory caps inherited from E10 and recorded before claim. 8 MiB allocated headroom per file group and aggregate; SSD free floor 2 GiB. |
| Retention | Closed logs compressed, one canonical copy, all forcing receipts in standalone E11 evidence. COPYFILE_DISABLE=1 on SSD operations. |
| Commits | Named handwritten/config files only to fork, pushed only to fork remote; no generated staging. Evidence [E11] with Orchestrated-By: Muse Code; no ssx3 push. |

No adb, second gap repair, handwritten query, new HLE, or storage/SIF/CD/input
promotion without an actual guest request/completion dependency.

Tail receipt: contract recorded before checkpoint execution, reclamation, CSV
mutation, regeneration, build or boot.
