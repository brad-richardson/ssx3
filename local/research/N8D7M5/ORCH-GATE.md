# N8D7M5 orchestrator gate — PASS executed Mac packet provenance

Worker commits `21b3e53f` and wording correction `f630af5b` in ssx3, plus private oracle-fork commit `a8cfefa` (`Orchestrated-By: opencode`), contain a default-OFF test-harness word tap. I read the corrected full report/source/checker, independently parsed all 1,980 change rows for first/last writes and page coverage, reran the checker, checked the fork runner-dir guard and viewed the tick2050 Mac race frame. The fork commit and correction pass `git show --check`; the first ssx3 commit's raw `fork-n8d7m5.patch` receipt has trailing blank context lines, with no source-code whitespace error.

| Gate check | Result |
| --- | --- |
| Execution witness | Both replays explicitly use direct CPU mode, with no GS worker. In that mode `processGIFPacket` executes synchronously; the test-only tap brackets each packet and checks `submitCount == replay_index+1`. It would not be valid without this mode check. No per-packet drain was added. |
| Build/control | One isolated Release build (322 targets); flag-OFF suite 585/585. One OFF and one ON CPU replay of the same SHA-pinned N8D4 stream through marker2050, each under slot 1; lease files absent afterward. Packet/priv/transfer/marker counts match N8D4. OFF tick2050 hash matches its CPU baseline; ON/OFF full hash files and tick2050 PPM bytes match. No device action or speed claim. |
| Provenance | Six FBP112 storage addresses on four pages each changed after an executed packet; 1,980 bounded trace rows (1,706 draw, 274 transfer), no truncation/unknown op. First transitions are zero→nonzero. Latest writes are identified through tick2049, with six post-marker words. Independent parse agrees with the worker table and checker. |
| Backend limit | Only `0xE0000` and `0xF0000` CPU final words equal the N8D7L Mac paraLLEl control values; four others differ. The CPU trace proves packet/write provenance on its replay, **not** the expected Odin GPU word values. A device value oracle must use the Mac paraLLEl snapshot of the newly captured **same stream**. |

Verdict: **PASS for Mac CPU executed-word provenance and an unchanged frame.** The Odin sparse-snapshot cause remains open. Next use the existing N8D7M1 APK in one bounded, combined same-run GS-stream + selected-VRAM capture, then replay that exact new stream through Mac paraLLEl and compare selected descriptors, full tile vectors and selected words. Outcomes should distinguish stream/content, selection, or device-specific selected-VRAM divergence; they must not claim missing GPU writes versus copy ordering without an additional same-packet timing witness.
