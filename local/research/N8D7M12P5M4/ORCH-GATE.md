# N8D7M12 Part 5M4 orchestrator gate — PASS, Mac OFF flag-unperturbed control (2026-09-24)

Read the full REPORT and worker commit `2cbd97ad` (`git show --check` clean). Reran independent checker: acceptance PASS, 17/17. Independently read the private `result.json` (exit 0, 41 rows), full-suite totals in `run.log` (585/585/0), and SHA-256/byte-compared OFF versus pinned Mac ON `parallel.hashes` and tick2050 PPM: both exact matches (`94b433df…10c290`, `9490484c…14fce3e`). Viewed the OFF 512×448 frame myself: snow/race scene and HUD, with the already known dark lower field. The worker's one released command used driver SHA `1056304d…d6738`; pinned binary/stream/ON inputs unchanged. Independently found both mini lease files absent and child PID 92827 gone. The diagnostic 3.05 s wall is not a speed measurement.

**PASS for this Mac same-binary/stream ON/OFF control: the three tick2050 capture flags do not change any of its 41 sampled hash rows or the final PPM.** This is one stream on one Mac binary. It does not explain the mostly black Odin frame or the same-settings Odin GPU variance first seen at tick850, and it cannot validate an Odin ON/OFF flag effect. Next use a bounded worker-consumption measurement on two same-settings Odin replays, with the Part 5F3 gaps stated; do not claim a GPU root cause from this Mac control.
