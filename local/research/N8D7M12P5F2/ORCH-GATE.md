# N8D7M12 Part 5F2 orchestrator gate — A for packaged-source provenance and scoped audit (2026-09-24)

Read the full REPORT and commit `7c5811c3` (`git show --check` clean). Reran checker: 29/29 A. Independently SHA-256 checked all three `N8D7F/parallel-gs` working-tree files against the N8D7H packaged-source pins: `gs_renderer.cpp` `85c29cb0…3de77e`, `gs_interface.hpp` `3a1751b4…05954d9d`, `gs_renderer.hpp` `fae3261a…a93a401a`. Read the pinned `flush_submit(0)` and `GSRenderer::vsync` bodies: the scanout submits work without a renderer-level CPU wait. The interface implementation and Granite/driver behavior are not pinned or established here.

**A accepted for locating exact APK-tied renderer source bytes and for the bounded source map; no GPU root cause.** The files match the package pins despite being dirty relative to their own Git HEAD. That corrects the earlier Part 5F1 provenance gap, but does not show that readback is faulty. Next use one bounded observable to distinguish replay ordering from GPU state/readback behavior. No build, replay, boot, device action or speed result.

**2026-09-24 ARCH1 correction:** This REPORT calls the N8D7F backend `c6135b3c…d4fb41f` APK-tied. That is incorrect for the later N8D7M12P3/OFF-pair APK: its source gate double-pins `84a13a80…a4645`. The latter adds the independent oracle while preserving the relevant `Present` wait-idle and `SnapshotVram` delegation. The three renderer files remain matched to P3's package gate. See `local/research/ARCH1/ORCH-GATE.md`; cite the actual P3 backend in future APK claims.
