# E55D13 orchestrator gate — B, requested card path is unobserved (2026-09-24)

Read the full worker REPORT and commit `de495dc8` (`Orchestrated-By: opencode`). `git show --check` passed and the commit contains only `REPORT.md` and `check.py`. Reran `check.py`: all source and receipt pin checks pass. Independently read `sceMcGetDir` and the E55D3 `noteGetDir` signature: the stub reads `rawPath`, normalizes it, derives a host directory and pattern, then copies 64-byte entries only for matches; the current probe records status and copied bytes but no query path. E55D12's fifth GetDir at tick1740 returned empty without copied bytes. The earlier four empty calls are separate.

**B accepted for design only.** The `BASLUS-20772` string is a possible folder name, not evidence of the tick1740 pattern or a valid save payload. The `/SAVEDATA/*` example belongs to a stub self-test. No seed is grounded yet. The checker proves selected source/receipt pins, not absence of every possible save elsewhere or guest behavior. The report's statement about empty manifests applies to the cited E55D4/E55D12 test cards.

Release a separate default-off path-tap implementation and static/unit gate, then a bounded empty-card run using E55D12's exact script. Preserve the existing `getdir` record schema and per-family order; a new sibling record may shift global `seq`, so compare its own stable fields and guest bytes/results, not old global sequence numbers. A seeded run remains unreleased until the requested query and the input bytes/provenance are pinned. No build, boot, seed, device action, or speed result occurred in E55D13.
