| Handoff | Required boundary |
|---|---|
| Base | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176` unchanged and remote-agreed at open and close. E22 made zero fork commits and zero pushes. |
| Completed | Checkpoint 9457/1725/458 unloaded + 458 observer-loaded/E15/E16/prior/R1–R6; ONE `stdbuf`-free observed title boot (`e22a`, wall-bound, rc0); e21a's per-call parser-observation gap CLOSED; nine candidate second-demand edges enumerated over a closed surface, seven closed; fix gate stopped with X sharpened. |
| Argv warning DISCHARGED | E21's `stdbuf` warning is now a settled result, not a caution: same dylib, same env, same harness, only the argv changed, and the parser directory went 0 files → closed receipt. Any future observed boot must keep exec'ing the runner directly; `e22_capture.py` carries an assertion that fails the run if `stdbuf` ever reappears in the boot argv. |
| Instrument | `parser/e21-parser-observer.dylib` (SHA256 `e4d88fdc…b38e6be`) REUSED by copy + re-sha, never rebuilt. It owns its interface, so `PS2X_E21_PARSER_DIR`, `PS2X_E21_PROOF` and the `# E21 PARSER CLOSURE` footer stay E21-spelled in any later lane. Rebuilding it re-opens E19's forwarding question and needs its own isolation proof. |
| New measurement landed | Inside the 75.46 s title window the host parser is called **exactly once**: 5,040 B offered / 5,040 consumed / `packetSize=0`, then **66.896 s of total silence**; `sendCalls=0`, `receiveCalls=0`, `frames=0`, `errors=0`; `backendParse/Send/Receive` 1:1; 4/4 valid bindings; real `_Exit rc=0` footer. |
| New byte evidence | The complete real 5,040 B payload is retained for the first time (`observed/parser-input.bin`, SHA256 `cde8a830…2a1c875a`), FNV64 `0xd2a9588f0e0fd358` equal to E18's full-span hash and first64 byte-identical. E19's "only 64 E18 bytes are known" limit is lifted; a standalone replay of the REAL first feed is now possible without authoring. |
| Candidate ELIMINATED (do not re-propose) | Broadening the AddBs wake (`MPEG.cpp:2053`) **cannot work**. AddBs runs at `seq=6397/6398` on thread 1, the park at `seq=6401` on the same thread at the same tick — AddBs completes *before* its own caller parks — and `EeScheduler::completeExternalWait` matches only threads already `Waiting`, with no latch and no pending-completion queue. Dynamically confirmed: 0 `mpeg-complete` events in 6,401. |
| Candidates also closed | Multi-callback loop (1 registration); timer/vsync re-poll (`waitExternal` `[[noreturn]]`, no timeout; `setAlarm` unused by MPEG; 0 alarm calls in 54,944 BIOS lines); guest retry (thread 1 `scheduled=0` across 12 consecutive 5 s samples); EOF/`streamEnded` (`ended=0`, and firing it fabricates EOF); demux/flush (never dispatched); delete/reset (aborts, not demand). |
| Named missing signal X (sharpened) | The edge from **"parser holds accepted bytes but emitted no packet"** back to the registered type-1 producer, **plus its termination rule**. Mechanism half is reachable at exactly two sites — C2a (`MPEG.cpp:1757–1760` + `:2484`, release the delivery then re-request) and C4 (`:2496–2513`, re-request before parking). The rule half is missing. |
| The two unknowns that block a fix | (1) **Trigger point** — re-ask inside `onComplete` or before the park; C1 is eliminated, so it is one of those two. (2) **Loop bound** — how many rounds and stopped by what (a cap? a packet? source exhaustion? a failure?). Neither is observable from E18/E21/E22. |
| Exact next measurement edge | A reference for the firmware's real demand cadence — how many times the type-1 producer fires per `GetPicture` before a frame, and what ends the loop. Nothing in this lane's evidence can supply it; it needs an external reference (hardware/reference-implementation trace) or a bounded experiment that measures rather than assumes the cadence. Until then the fix gate stays shut. |
| Decode-threshold scope limit | Q1's `GOP offset + 5,461` was measured on `authored-black.m2v` (5,457-byte first picture + 4 lookahead). The REAL stream's GOP offset is 0 (`000001b3` at byte 0), but its first-picture length is unmeasured because the rest was never delivered. **Do not import 5,461 as the real-stream deficit** — it is false precision. |
| No target fix authority inferred | E22 eliminated candidates; it did not isolate one edge. A future fix still needs exactly one demonstrated edge plus its own fail-before and full regression. No decoder/EOF/policy invention, no fabricated frames, no second fix. |
| ABI carry | Caller-owned synchronous delivery, word0-only cbData, v0 discarded, valid-no-input waits, no guest invocation under MPEG mutex, delete/reset cancellation, stream behavior unchanged. |
| Protected state | `/tmp/p1-link`, `/tmp/e17-map-link`, `/tmp/e18-mpeg-link`, DerivedData; all 1,725 hashes equal at close. No reclaim authorized. E22's `P1/e22-*` fixtures/tooltmp/boot artifacts retained; E21's retained too. |
| Probe count | E22 spent 1/1 title boots and 1/1 lease claims (wall-bound, rc0, clean release, post-release `pgrep` rc1). A later brief requires its own fresh admission, T13, lease and caps. |
| Errata carried | E21-E1 (stale harness size/sha in E21's REPORT; `parser-build.json` + on-disk are authoritative) carried forward unchanged. E22 introduces no errata. |
| Source receipts | `REPORT.md`, `X-HUNT.md`, `x-hunt-evidence.json`, `e22a-observed.json`, `e22a-payload-identity.json`, `e22a-mpeg.json`, `q3-complete.json`, `e22a-result.json`, `e22a-config.json`, `observer-regression.json`, `rename-proof.json`, `tooling-diff.md`, `fix-gate.json`, `final-audit.json`, `observed/`. |

| E22 NEXT-BRIEF TAIL COMPLETE | Observation gap closed and the argv warning discharged; the obvious fix eliminated on evidence; X sharpened to a mechanism plus a missing rule, and the rule needs a reference this lane cannot produce. |
|---|---|
