| E22 | Receipt |
|---|---|
| Stop point | All gates executed: checkpoint re-verified unloaded + observer-loaded, ONE `stdbuf`-free observed title boot landed the per-call parser receipt e21a lacked, nine candidate second-demand edges enumerated over a closed surface, seven closed. Fix gate: STOP with X named and sharpened (policy invention still required, still excluded). |
| Checkpoint | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176` triple-agreed; 9,457 generated names/hashes; 1,725 protected builds; baseline 458/458 UNLOADED; 458/458 observer-LOADED with the REUSED dylib; E15 rc0s; E16 closure 6; prior 24/3/14/4/DROP; E18 R1–R6 in suite text. |
| Questions reached | e21a's observation gap CLOSED (the argv hypothesis held); X-signal hunt tabled (C1–C9); decode-threshold half tabled with its scope limit. |
| Mutations / launches | Zero fork source edits, zero fork commits, zero pushes. One guarded title boot (wall-bound, rc0), one lease claim/release. No target fix, no regeneration, no observer rebuild, no probe code changed to chase X. |
| Next dependency | X = the edge from "parser holds accepted bytes but emitted no packet" back to the registered type-1 producer, **plus its termination rule**. The mechanism is reachable (C2a/C4); the rule is unobserved. Two named unknowns block it: the re-ask trigger point and the loop bound. |

| Experiment contract | Bound / observable |
|---|---|
| Time box | Start `2026-09-21T23:05:22Z`; deadline `2026-09-22T07:05:22Z`; eight hours. Final audit `2026-09-21T23:15:14Z`. **Actual time used: 12 min 25 s** (open → final audit), well inside the box. |
| Required reads | E21 REPORT 154 lines + NEXT-BRIEF 16 lines, E18 NEXT-BRIEF 41 lines, E19 NEXT-BRIEF 17 lines, E21 prompt 68 lines, E22 prompt — all read in full including tails, before any copy, build or boot. |
| Hypothesis | The e21a parser-observation gap is caused solely by `stdbuf` in the boot argv; exec'ing the runner directly preserves `DYLD_INSERT_LIBRARIES` and yields a closed per-call parser receipt inside the title window. |
| Result | **Held.** Identical harness, identical dylib, identical env — only the argv changed — and the parser directory went from 0 files (e21a) to `parser-events.txt` + `parser-input.bin` with a real `# E21 PARSER CLOSURE` footer. |
| Alternatives | (a) argv was the only cause; (b) a second cause (re-exec, posix_spawn, SIP/hardened-runtime, DYLD stripping); (c) observation lands but the window is unchanged. (a) and (c) both realised; (b) excluded by the closed receipt. |
| Observable | Per-call parser timing inside the title window joined to the guest feed. |
| Strict order | Checkpoint → loaded regression → probe gates → ONE boot → mine/join → X hunt → fix gate. Opened in order; no red gate tripped; no boot on red. |
| Fix gate | Exactly one demonstrated edge + minimal ABI-preserving change + own fail-before + full regression. Closed: STOP, X named. `fix-gate.json`. |
| Preserved constraints | Caller-owned synchronous dispatch; word0-only cbData; v0 discarded; valid-no-input waits; dispatch outside the MPEG mutex; delete/reset cancellation. No CSV, main, scheduler or stream-callback edits. |
| Contract source | `CONTRACT.md`, written before the first copy, build or boot. |

| Storage / execution budget | Admission and accounting |
|---|---|
| Internal reservation | 3 GiB (evidence 0.5, observer/scratch 0.25, reserve 2.25); floor 2 GiB + 0.5 GiB guard. |
| SSD reservation | 16 GiB in NEW `e22-*` paths (tooltmp 8, fixtures 2, probe 1.5, reserve 4.5); same floor/guard. |
| Initial admission | `2026-09-21T23:08:05Z`: internal 6,619,082,752 B / SSD 180,370,800,640 B free. Both reservations fit. Internal free was the scarce side all run; large artifacts stayed on the SSD. |
| Fresh admissions | Every bounded tool saved its start sample and re-checked the reservation plus floor; all entered fitting. |
| Final audit sample | Internal owned 4,222,976 B; SSD owned 420,478,976 B across 22 `e22-*` paths; fork growth 0. Internal free 6,615,175,168 B; SSD free 179,918,864,384 B. `final-audit.json`. |
| Accounting | `st_blocks × 512`, incl. ExFAT directory/AppleDouble allocation, E22-owned paths and positive fork source/Git growth; `COPYFILE_DISABLE=1` on every SSD step. No reclaim or deletion. |
| Protected | `/tmp/p1-link`, `/tmp/e17-map-link`, `/tmp/e18-mpeg-link` and DerivedData reserved; all 1,725 protected hashes re-verified equal at close. No protected build command run. |
| Boot cap utilisation | Bound hit: **wall only**. Highest byte caps: `e4_dir` 67.6% logical, `function_log` 11.8%, `e7_dir` 12.8%; aggregate 9.3% logical / 13.5% allocated. Trace 54,924 lines of 1,000,000. |
| Lease | Atomic `/tmp/ssx3-p-lane-lease`, never waited; 1 claim (`23:08:51Z`) / 1 clean release (`23:10:06Z`); post-release `pgrep` rc1, lease absent. `e22-waits.log`. |

| Checkpoint re-verification | Receipt |
|---|---|
| Fork identity | HEAD, `refs/remotes/fork/ssx3` and `git ls-remote fork refs/heads/ssx3` agree at `3adc0478b6d2260acdd28a249466f2eef9a20176` at open and at close. Preexisting AppleDouble Git-index warning retained (stderr only; `status --short` is exactly `?? ps2_log.txt`). |
| Generated names / bytes | 9,457 `.cpp`/`.h` files; every SHA256 matches E18's manifest at open and at close. `before-generated.json`. |
| Protected build hashes | 1,725 objects/archives/PCH/runners/suites across the three retained builds; all re-hashed equal at close. |
| Runner | 163,529,696 B; SHA256 `e462e4482fbf5b3fe0902e793f9d677e51c56b220022bca619305f1f69967e22`. |
| Suite | 5,695,128 B; SHA256 `2152e5ad53f19741788aa3051e26184a9c5e831857114ce74407f199de5f04c0`. |
| Prior actual fixture | Reused SSD `P1/e18-fixtures/after/binding-test`, 165,183,488 B; SHA256 `42d9f4f373265f5f40ea29920db6149e9043f5afbd65d2d576ea9477d6587326`. |
| Full suite / E18 R1–R6 | rc0, 458 passed / 0 failed UNLOADED; R1–R6 present in suite text. `checkpoint-suite.txt/json`. |
| Prior bindings | leaf 24 / consumer 3 / predicate 14 / query 4 / DROP, all rc0. |
| E15 no-input / input | Both rc0 with `saved128=1`, `waitReason=6`, `callbackCalls=1`, `resumes=0`; delivered 0 / 16 bytes. |
| E16 closure | Six rc0 (`quiescent`, `idempotent`, `window-complete`, `disabled`, `unopened`, `fallback-error`). |
| Absorbed entries | Five presence checks rc0 (`0x14f2a8`, `0x156750`, `0x243a80`, `0x395730`, `0x3a0158`); ownership controls rc0. |

| Instrument reuse (no rebuild) | Receipt |
|---|---|
| Reused dylib | E21's `e21-parser-observer.dylib` copied to `local/research/E22/parser/`; re-sha **hits** `e4d88fdc6740449893d5a96c112fae6ce3f19135bad8a309d140d45bbf38e6be` (52,472 B). Not rebuilt, not renamed. |
| Reused harness | `e21-isolation-test`, 35,232 B, SHA256 `f71f4a85f280d25c029778bcbed227dc18196d1c51be9be17b1becb925eda727` — the E21-E1 errata values, carried not re-fixed. |
| Interface carried verbatim | The binary owns its env names and footer text, so `PS2X_E21_PARSER_DIR`, `PS2X_E21_PROOF` and `# E21 PARSER CLOSURE` are E21-spelled throughout E22 by necessity, not oversight. Protected from the rename. |
| Inherited proof | E21's isolation forwarding (byte-identical feed, `_Exit` rc3 both ways, 4/4 valid bindings) is consumed by binary identity, re-asserted in `e22_observer_regression.py` and `e22_prepare_probe.py`. |
| E19/E20 failed artifacts | Never loaded for any suite, fixture, or title run. |

| Tooling provenance | Receipt |
|---|---|
| Pure rename (9 files) | `e22_common/io/bounded/closed_events/parser_receipt/validate/observer_regression/checkpoint/baseline`. Normalized diff (E21/E22 tokens unified to EXX/exx) is **byte-empty for all nine**. `rename-proof.json`. |
| Receipt verifier | `e22_parser_receipt.py` is the brief's required functional-identity copy: byte-empty normalized diff, same closure/seq/byte/counter/1:1-backend/4-binding assertions, applied to every observer run in E22. |
| Intentionally changed (5 files) | `e22_capture.py` (drop `stdbuf`; instrument manifest read from E21; record `stdbuf_free`/`dyld_insert`/`argv0_is_runner`), `e22_prepare_probe.py` + `e22_observer_regression.py` (inherited-receipt gates + instrument re-sha), `e22_mine.py` (per-call timeline extraction), `e22_close.py` (E22 deliverables). Full itemized diffs in `tooling-diff.md`. |
| `stdbuf` in E22 | Present only as the forbidding assertion and the receipt field. Grep-verified across all thirteen scripts. |

| The one boot (e22a) — observation gap closed | Receipt |
|---|---|
| Pre-claims + lease | Fresh T13 battery green (process/binary/ELF/aligner/suite/hash/space, `e22a-preflight.json`); suite preclaim 458/458; atomic claim `E22A`, clean release; post-release `pgrep` rc1. `boot-attempt.json` (sole boot). |
| Boot argv | `['/tmp/e18-mpeg-link/runtime/ps2xRuntime/ps2EntryRunner', '/Volumes/Extreme SSD/ps2recomp-spike/P1/cd/SLUS_207.72']` — runner exec'd DIRECTLY, `stdbuf_free=true`, `argv0_is_runner=true`, `DYLD_INSERT_LIBRARIES` = the reused dylib. `e22a-config.json`. |
| Bound / rc / window | `bound=wall`, rc 0, 75.46 s; 54,924 trace lines; span-complete at 7.61 s. `e22a-result.json`, `e22a-liveness.txt`. |
| **Parser observation** | **Present.** `RUN/e22a-parser/` holds `parser-events.txt` (2,196 B) + `parser-input.bin` (5,040 B), receipt-verified, with a real `# E21 PARSER CLOSURE … reason=_Exit rc=0` footer. e21a's gap is closed by the argv change alone. |
| E7 closure | 6,401 events (same count as E18 and e21a); `# E15 CLOSURE tick=4507 calls=10 returned=9 unwound=1 pending=0 selections=1 invocations=1`; `# E7 SHUTDOWN` byte-exact, all truncation flags 0, `missing_pair_ids` empty. |
| Demand verdict (reproduces e21a) | ONE registration (`handle=1`), ONE type-1 callback (`0x3b0b10`, `cbWordReadable=1`), ONE AddBs (requested 5,040 / returned 5,040), ZERO completions, ONE park (thread 1, `pc=ra=0x3b1028`, `sp=0x1fffd60`, reason 6). |
| Host feed counters | `[MPEG:feedES] #0 size=5040 first4=000001b3 decoderFailed=0 waitSeq=1`; `[MPEG:feed] inSize=5040 parsed=5040 packets=0 newFrames=0 totalFrames=0`; `[MPEG:GetPicture] waiting … ended=0 failed=0 sawInput=1`. Byte-identical shape to E18. |

| Per-call parser timing inside the title window (E22's target measurement) | Receipt |
|---|---|
| Parse calls in 75.46 s | **1.** `parse-enter call=1` at observer `ns=8,509,239,000`; `parse-return` at `ns=8,509,248,000`. Duration **9 µs**. |
| That call | `size=5040`, `payloadOffset=0`, `kept=5040`; returned `used=5040`, `packetSize=0`, `consumed=5040`, `packets=0`. |
| Downstream | `sendCalls=0`, `sendEof=0`, `receiveCalls=0`, `frames=0`, `errors=0` — `avcodec_send_packet` was never reached because no packet existed to send. |
| Silence after it | **66.896 s** to the real `_Exit` closure at `ns=75,405,554,000`. Zero further parser activity of any kind. |
| Forwarding health | `backendParse=1 / backendSend=0 / backendReceive=0` — exact 1:1 against `parseCalls/sendCalls/receiveCalls`; `bindingChecks=4` all `valid=1` by direct import with `nextIsReplacement=1`; `recursionGuards=0`; `pending=0`; `textTruncated=payloadTruncated=ioErrors=0`. |
| Threads | Observer opened on tid 1788649; the parse ran on tid 1788771. |
| Payload identity | 5,040 B, SHA256 `cde8a830265592be927d28ec78c26a27567ce046a7ac161698759e742a1c875a`, FNV64 `0xd2a9588f0e0fd358` — **equals E18's full-span FNV64**, and the first 64 bytes are byte-identical to E18's retained `first64`. First four bytes `000001b3` (sequence header). |
| New evidence | The complete real 5,040 B payload is retained for the first time; E19's brief recorded that only 64 E18 bytes were known. `observed/parser-input.bin`, `e22a-payload-identity.json`. |

| X-signal hunt — nine candidates over a closed surface | Result |
|---|---|
| Surface closure | Repository-wide grep for `getMpegPicture` / `kMpegPictureWaitType` / `MpegNonStreamDelivery` returns **zero matches outside `MPEG.cpp`**. The enumeration is closed, not a sample. |
| C1 AddBs-wake broadening (`MPEG.cpp:2053`) | **Proven inert — E22's main elimination.** AddBs is `seq=6397/6398`, the park is `seq=6401`, same tick 249, **same thread 1**: AddBs runs inside the callback, before its own caller parks. `EeScheduler::completeExternalWait` scans only threads already `Waiting` — no latch, no pending-completion queue — so a wake raised there matches 0 threads and is lost. Confirmed dynamically: **0 `mpeg-complete` events** in 6,401. |
| C2 flip `onComplete` flag (`:1760`) | Inert. `nonStreamDeliveries` is `weak_ptr` (`:564`) but the `onComplete` lambda captures the `shared_ptr` (`:1758`), so `pending.lock()` (`:2465`) still succeeds and the `!delivery` gate (`:2467`) stays closed. |
| C2a release-then-re-request (`:1757–1760` + `:2484`) | **Live candidate.** Mechanism works, but needs a termination rule. |
| C3 multi-callback loop (`:1740`) | Absent. Exactly one registration (`handle=1`, E15 closure `registrations=1`); the loop iterates once by data. |
| C4 pre-park re-request (`:2496–2513`) | **Live candidate.** Precondition holds (`sawInput=1` at the park), so the site is reachable; same live-delivery gate and same missing rule as C2a. |
| C5 timer / vsync re-poll | Proven absent. `waitExternal` is `[[noreturn]]` with **no timeout parameter** (`ee_scheduler.h:337`); `setAlarm` (`:311`) is never called from MPEG.cpp; `currentVSyncTick` (`:2530`, `:2678`) only timestamps frames. Dynamically: 0 alarm calls in 54,944 BIOS lines. |
| C6 guest-side retry | Proven absent. Thread 1 parked at `0x3b1028` `waitReason=6` in diag samples 2–14 with `scheduled=0` every sample (≈60 s); all 10 `mpeg-call` events are tick 249, thread 1; threads 2–6 never touch MPEG. |
| C7 EOF / `streamEnded` (`:1999`) | Absent and excluded — `ended=0`, flag never set, source not exhausted; firing it fabricates EOF, barred by the ABI carry. |
| C8 demux / flush (`:2285/2291/2378/2384`, `:2023`) | Absent. The target census holds 10 targets, none a demux or flush stub. |
| C9 delete / reset (`:1231`, `:2239`) | Absent, and not a demand edge — it aborts the wait rather than supplying input. |
| Runtime liveness control | Thread 4 scheduled ~300×/5 s in all 14 samples while thread 1 sat at `scheduled=0` — the 60 s of MPEG silence is a live runtime that never asks, not a stalled host. |
| Full table | `X-HUNT.md`; joins in `x-hunt-evidence.json`. |

| Decode-threshold half (scope-limited) | Receipt |
|---|---|
| Parser state | 5,040 B consumed and retained, `packets=0`, `errors=0`, `decoderFailed=0` — Q1's pre-threshold state, now confirmed on the **real** stream rather than an authored one. |
| Real-stream GOP offset | 0 — the payload opens with the `000001b3` sequence header, so all 5,040 B are stream from byte 0. |
| Deficit | **Not computable from this run.** Q1's `GOP offset + 5,461` was measured on `authored-black.m2v` (5,457-byte first picture + 4 lookahead). The real stream's first-picture length is unmeasured because the rest was never delivered. Importing 5,461 here would be false precision. |
| What it does establish | The feed is genuinely incomplete, not malformed: every byte accepted, no error raised, simply no full picture yet. |

| Fix gate | Receipt |
|---|---|
| Demonstrated edges | (1) The single feed is incomplete and the parser holds it cleanly (E22 per-call receipt). (2) C1, the most obvious candidate fix, cannot reach the waiter (ordering + no-latch semantics). (3) No runtime re-request, no guest retry, no timer, no EOF, no demux/flush path fires (C3, C5–C9). |
| Isolated to ONE edge? | **No.** Two candidates remain (C2a, C4) and both require the same two unobserved rules: the re-ask trigger point and the loop bound / stop condition. |
| Gate decision | **STOP with X named.** Choosing either remaining candidate invents a demand policy the firmware's true shape is unknown for — explicitly excluded. Per brief: "Guest never sends more without X" → NAME X and STOP. |
| Mutations | Zero fork source edits, zero fork commits, zero pushes. Diagnosis-only. `fix-gate.json`. |

| Exact command record | Invocation / artifact |
|---|---|
| Environment | `export COPYFILE_DISABLE=1 PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=…/E22`; bounded wrapper sets task-specific TMPDIR and `CMAKE_BUILD_PARALLEL_LEVEL=2`. |
| Rename + instrument copy | Lane rename with protected tokens → `rename-proof.json`, `tooling-diff.md`; `cp -p` of the E21 dylib + harness, re-sha'd. |
| Checkpoint | `python3 local/research/E22/e22_checkpoint.py`; then `… e22_bounded.py checkpoint-regression 1200 -- … e22_baseline.py`; rc0. |
| Loaded regression | `… e22_bounded.py observer-regression 1200 -- … e22_observer_regression.py`; rc0, 458/458 loaded, 10/10 receipts. |
| Probe gates | `python3 local/research/E22/e22_prepare_probe.py` → `entry-preflight.json`, `e22a-build.json`. |
| The boot | `python3 local/research/E22/e22_capture.py a --report-all` (sole boot, own caps/lease logic). |
| Mining / joins | `python3 local/research/E22/e22_mine.py`; payload identity and X-hunt joins. |
| Final close | `python3 local/research/E22/e22_close.py`; 9,457 + 1,725 hashes rechecked; fork HEAD/ref/remote agree unchanged; lease absent; 1 boot / 1 claim / 0 fork commits. |
| Never executed | Any fork edit, commit, or push; any game regeneration; a second title boot; an observer rebuild; E19's or E20's dylib in any run; `stdbuf` in the boot argv. |

| Evidence hygiene / handoff | Receipt |
|---|---|
| Standalone data | Checkpoint + loaded logs and receipts, rename/tooling proofs, boot config/preflight/liveness/result, the retained parser events + full payload, gzipped E7 trace, X-hunt table and joins, payload identity, fix-gate record, final audit. |
| Retained copies | `observed/parser-events.txt` (SHA256 `0e2e2b1d…01ee037f`), `observed/parser-input.bin` (`cde8a830…2a1c875a`), `observed/e7-events.txt.gz` (`3777653e…04375195`). |
| `.suite-*` scratch | Remains unstaged (checkpoint, observer, preclaim); loaded-suite observer receipts preserved canonically under `observer-receipts/`. |
| Tail qualification | Every bounded tool, suite, fixture, receipt and boot artifact ends with its real `TAIL COMPLETE` / runtime closure footer; counts re-verified, never appended. No source-tail gap remains — e21a's absent parser log is the gap this brief closed. |
| Errata carried | E21-E1: the E21 REPORT's harness size/sha (35,072 B `f6059aa0…`) is stale; the on-disk binary and `parser-build.json` (35,232 B `f71f4a85…`) are authoritative. Carried, not re-fixed. No verdict impact. |
| Publication | `[E22]` evidence commit with `Orchestrated-By: Muse Code`, `git add -f`. Scope: E22 evidence only; no fork files, no generated sources. **Not pushed** — the orchestrator pushes at poll. |

| Tail receipt | Value |
|---|---|
| Complete prefix | Lines 1–134; 18778 B; SHA256 `9590e85f44c06a68f21c6de4fe59f592b0bcc796b392f61704edf23de9274cbc` |
| Source-tail gap | **None.** Every E22 capture (suite, fixtures, boot E7, boot parser) carries its real runtime closure footer. e21a's absent per-call parser log — the one gap E21 handed over — is closed by this brief's `stdbuf`-free boot, not papered over. |
| E22 REPORT TAIL COMPLETE | Observation gap closed by the argv change; per-call parser timing landed inside the title window; nine candidate demand edges enumerated, seven closed, C1 eliminated on evidence; fix gate stopped with X sharpened; 1 boot, 0 fork edits/commits/pushes. |
