# Proposed next brief — repair generation allocation, then resume E9's entry work

E9 selected (iii) and stopped. The canonical and P1 used configs already exclude
`_sceSifCmdIntrHdlr@0x00426230` at fork be0c9eeaa337d9685e9536fa4f66c5ce5029aad8.
Active generated code and binaries still match E8/fork 4acc59f. No query map
entry was added and no boot ran. Standing no-regen has resumed; this recipe
requires a renewed bounded regeneration brief before execution.

The ONE next action is to complete the interrupted preflight using APFS scratch
output. Keep the input/translation hypothesis unchanged. This is a storage
measurement/placement repair, not an emulator fix.

| Step | Exact recipe / acceptance |
|---|---|
| Fresh budget | Recheck both `df` results. Reserve ≥1 GiB for generated logical output on internal APFS and retain ≥2 GiB internal free space. Record logical and allocated limits, wall and log limits, with polling headroom. Stop if the reservation cannot be met. Do not reuse the undersized ExFAT cap. |
| Scratch destination | Make a new owned `/tmp/ssx3-e9-retry-codegen` directory. Derive used TOML from canonical post-DROP config by changing only `general.output` to that directory. Preserve hashes and require both canonical + P1 configs to remain selector-free. |
| Inputs/tool | Canonical CSV SHA b9aae74782fde6ba54671cfe07a9887c0af8ba1aad9d5befc1095e8cfef146ca; ELF 1b49d05ca2793922180851b9e1ce9ae2291d61a7863565ac4e71f12e967af7bc; generator 511791795c16649e69706725c412408acc8d35fda68c4d83a3229eae6bda0763, subject to fresh source-identity checks. Do not select stale P1 CSV. |
| DROP-only output | Require normal generator exit and complete registry/header/body manifests. 0x426230 must bind the emitted guest handler, never ps2_stubs::sceSifCmdIntrHdlr/TODO. Header presence alone is insufficient. |
| Installation | Compare all emitted names against the active mirror; disposition any leftover before installing. Copy only changed generated bytes, without metadata; track allocated storage on the destination and preserve before/after manifests. Never stage generated runner sources. |
| DROP behavior | Rebuild `-j4`, suite green, relink e9_binding_test.cpp against the actual new runner objects. Empty-buffer handler must return to supplied RA with unchanged SP and preserved s0. Query should still be absent at this checkpoint. No boot yet. |
| Query repair | Revalidate ELF 0x2c5140..0x2c5168. Add the sanctioned CSV row `sub_002C5140,0x2c5140,0x2c5168,0x28` in address order. Preserve existing owner row. Regenerate/rebuild through the same path. No handwritten query or owner-only pointer insertion. |
| Actual-binding regression | E9's expected-present baseline fails with hasFunction=false. After repair require actual generated query entry, v0=0 for (0,0), v0=1 separately for (1,0) and (0,1); optional (1,1). Require supplied RA, unchanged SP, all relevant callee-saved 128-bit registers, whole-RAM preservation and zero GetInfo calls. Recheck DROP handler and full suite. |
| Verification boot | One guarded boot, original E9 ≤600 s and lease/progress/byte rules. REPORT_ALL=1; retain E7/E4 taps. Capture query a0 dynamically and require [a0]=0x486f78. Constructor store 0x2c3fc4 establishes MC; UI pointer store 0x23d5c8 establishes UI+0x434. Observe query return at 0x2c44b0, state-3 request path through 0x2c48c0→0x2c4980→0x2c50e0→0x40a498, and UI pending at +0x338. Preserve packet→GS→D→field-Present join. |
| Stop | Entry/preflight failure: exact recipe and stop. First remaining wall, including reached sibling 0x2c5358: table and stop. No stacked fixes or storage/SIF/CD/input promotion without actual request/completion evidence. |

Candidate MC/UI addresses remain E8 candidates only: MC=0xb851a0,
MC+4=0xb851a4, MC+0xc=0xb851ac, MC+0x40=0xb851e0;
UI=0xb84a50, UI+0x434=0xb84e84, UI+0x338=0xb84d88.
GetInfo globals: 0x4a3938/0x4a393c/0x4a3940/0x4a3944. Same-run constructor,
UI-pointer and query-a0 guards must establish these before joining observations.

Tail receipt: one interrupted preflight to repair; original query-entry work
remains pending; no new emulator behavior change proposed; renewed regeneration
authorization required after E9 closes.
