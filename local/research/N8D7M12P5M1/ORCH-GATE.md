# N8D7M12 Part 5M1 orchestrator gate — A for Mac OFF driver preparation (2026-09-24)

Read the full REPORT and DeepSeek Flash worker commit `d12f316a` (`git show --check` passes). Reran `check.py` (32/32 A); the driver SHA-256 is `bec9a4c626092b5a2e66a8f1597f90235c1335631aa2f2cba432851de6ecd9f1`. Independently checked its pinned binary, stream and Mac ON artifact SHAs; its declared OFF env differs from ON only by removing the three tick2050 capture flags and changing private output paths. A pre-gate correction clears all ambient `PS2X_*` variables before adding the exact OFF env; a functional checker injects stray replay/live variables and proves none leak. The driver holds a dynamic mini slot in `finally`, uses its own PID only, refuses output reuse, caps wall at 300 s, log at 16 MiB and output at 64 MiB, and requires the reviewed SHA to run. No replay, build, device or lease action occurred in this preparation part.

**A accepted for script preparation only.** Release that exact SHA for one separate Mac OFF replay with the pinned Part 1 binary/stream, then compare all 41 rows and the PPM against Mac ON. A pre-2050 difference voids the flag comparison; all-equal supports Mac flag-insensitivity; a tick2050-only difference suggests a Mac instrumentation effect. The Mac source/binary provenance gap remains: the exact dirty renderer bytes at Part 1 build time were not pinned, so this is a same-binary runtime comparison, not a source-attribution result. No Odin graphics cause or speed is established here.
