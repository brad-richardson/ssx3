# N8D7M12 Part 5M2 orchestrator gate — acceptance FAIL, replay artifacts equal (2026-09-24)

Read the full REPORT and commit `fa9c51c1` (`git show --check` clean). Reran receipt checker: 15/15 A for accurate receipts, with **acceptance FAIL**. Independently SHA-256 compared the private Mac OFF and pinned Mac ON `parallel.hashes` (both `94b433df…10c290`) and PPM (both `9490484c…14fce3e`); the checker confirms 41/41 ordered rows equal. The OFF frame was viewed during the run and matches the fuller Mac snow/race scene. The replay emitted its tick2050 summary and passed its own test.

The binary exited 1 because the CodeGenerator suite test could not open `ps2xRecomp/include/ps2recomp/instructions.h` relative to the driver's private cwd; suite 584/585. Independently read that three-candidate relative lookup at `ps2xTest/src/code_generator_tests.cpp:1141-1150`, checked the pinned header SHA `b8de8745…970cd`, and confirmed both Mac mini lease slots free. This is a harness working-directory defect, but the predeclared exit-0 gate still fails. **Do not treat the artifact equality as an accepted Mac ON/OFF causal control.** No retry occurred under this brief; a separately prepared fixture correction may be released once reviewed. No speed or GPU root-cause verdict.
