# N8D6C orchestrator gate — one Odin run and same-run recovery

**Formal worker outcome: OTHER.** The released launcher did one install and one launch, then reached its tick-2100 incomplete-receipt stop. Its vector parser rejected a logcat continuation that began with a comma, so it did not pull the frame. The app was force-stopped, PID `17446` was absent, and the lease was released. The worker made no second run or device action after the stop. Worker report and receipt commit: `eeabeb0`.

The orchestrator independently recovered evidence from **that same run**, without launching the app again. The saved same-PID log has both tile vectors split into two logcat segments; joining the comma-led continuation yields 896 little-endian u32 values in each, 5,894 occupied pixels, 39 active tiles, packed SHA-256 `3f06a58ad6805c695a01c305fe06b52cc0d92198a6fb699aeee0c4ea09681ba5` for both, and 896/896 exact equality. This matches the worker's postrun `vector-audit.json` and the logged summaries. The released parser's `split(',')` treated the continuation's first comma as an empty field, explaining its null vectors. No source or launcher edit was made after release.

After confirming the app PID absent and the device lease free, the orchestrator briefly claimed the Odin lease for readback only. The existing `upload-0.png` and `.txt` in the run's unique frame directory were each hashed twice on device, pulled once, and hashed twice locally; all four reads matched per file. The 26,512-byte 512×448 PNG SHA-256 is `9845de61c7658beecbcf225ee38c3f5dc493f3a186e47b37830b68b63ceaa2a8`; metadata SHA-256 is `23cc34ae3be2b16e32021030581f044112dc3c49e8fd69eb9ec5f7a919ceb75c`. Metadata says seq0/tick2050/512×448/display and source FBP112/fallback0/SMODE2 `0x1`/PMODE `0xff21`/FNV `5da9eba2`, matching the same-PID dump line. The lease was released again and the app PID remained absent. Exact hashes and cleanup: `orch-pull-receipt.txt`.
The receipt renders an empty `pidof` result as `<empty>` to avoid trailing whitespace; the scratch command output had a blank value.

The orchestrator viewed `orch-upload-0.png`: mostly black race image with isolated HUD and snow fragments, consistent with the prior Odin appearance. The same-run stage data are circuit1 **21/448** active and pre-deinterlace merged **21/448** at 512×224, and final sampled/raw **39/896** at 512×448. All three stage controls and the old final control are 128; no probe/pipeline error appears in the captured log. These recovered measurements satisfy the **predeclared A numerical and frame conditions**. Because the released launcher failed its complete-receipt stop condition and reached tick 2136 before cleanup, the formal run stays **OTHER**. The recovered evidence supports the narrower within-run observation: the image was already sparse at circuit1; the merge and deinterlace were not the first stage where sparsity appeared. It does not distinguish the preceding VRAM contents, circuit sampler, or driver behavior. Mac and Odin routes are not byte-identical GS streams, and there is no speed claim.

The worker report's fixed-input table still says “not found” for packaged HAL and installed ELF/ISO although its run receipt records their matching double reads; that table was stale at commit time. The orchestrator checked those pins from `result.json`, the package gate and the full worker report. No further device run was made.

`git show --check eeabeb0` reports whitespace on blank context lines inside the committed `launcher.diff`; those leading spaces are part of the exact unified-diff artifact. The reviewed launcher itself compiled and its SHA remained unchanged. The gate retains the diff verbatim.
