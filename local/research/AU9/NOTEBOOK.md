# AU9 notebook (append-only)

Worker: Claude Code (Opus 5.5), exploratory brief `local/muse/prompts/AU9.md`.

## 2026-09-24 21:49:20 EDT — clock start
3 h box → experiments end 00:49 EDT, write-up done by ~01:05 EDT.

| time (EDT) | experiment | result |
| --- | --- | --- |
| 21:50 | Read brief, facts, AU2/AU3/AU8/T47; bytesize idle (just rebooted) | Plan: characterize in PCSX2 first (uploads, tag-3, SPU2 regs), in parallel look at our side |
| 21:52 | E1: PCSX2 AU9 hooks (`pcsx2/au9_patch.py`, env `PCSX2_AU9_DIR`): full tag buffer + EE status per tick, every other EE SetDma (hdr + ≤128 KB payload), every SPU2write, plain SPU2 DMA writes, SPU RAM dump every 20 s; all stamped with the 48 kHz output-sample count | 3 builds (1: missing extern; 2: duplicate symbol, Mixer.cpp is multi-ISA). `pcsx2-qt` sha256 `a0e4a5fea9fa41fed7775415075c0afe1dd6ab1e5bf8a23a733c1a1a74196c94`; hunk `/home/brad/au9/au9-hunk.diff`, pristine copies `/home/brad/au9/pre/` |
| 21:53 | First capture launch | Died with the ssh session (WSL); second try rc 20: TL1's APK build started on bytesize. Queued behind it (`au9_wait_cap.sh`; idle Gradle daemon not counted) |
| 21:54–22:00 | E2: PCSX2 race capture `au9r` (AU8 route: menus → Happiness race, +21,000 records after SC) | rc 0, 38,786 records, race screenshot OK. sha256: tagbuf `5895c5e8…b00c`, spureg `13f39b92…f820`, sifdma `dfa3b94a…ad78`, spudma `03cb04e9…32c0e5`, tap `f0511dde…114b`, tag1 `19dc8646…625d` |
| 21:55 | E3 (static): SNDDRV `SNDIOP_updatevoices` 0x7d1c | Per voice v<48: entry at payload+0x20+v·8. Addr word changed → if low 23 bits 0: key off; else read ENVX, if ENVX≠0 key off and retry next tick, else SSA=addr, LSAX=0x5000, ADSR1=0x000F, ADSR2=0x0005, VOLL/VOLR from s8 (v≥0: v·129, else (v<<7)&0x7f80), key on. Pitch u16 → PITCH when changed; vol changes → `spusetvol` (direct if |Δ|/2<2, else a hardware volume sweep). Then NAX read back into the status block. |
| 21:56 | E4: our baseline race boot (AU8 runner `a81f8343…`, slot 2, I26-FAST, SND log+dumps) | `cid0=0 dmq=0`, setdma == tagbufs through 5,828 ticks: **our EE never uploads anything to SPU RAM**. (Slot 1 held by a stale `au8-race pid=66267` lease; process is dead; left alone.) |
| 22:00 | E5: PCSX2 SIF DMA scan (`sifdma_up.py`) | 596 × 4 KB SND payload DMAs (ra `0x3c4438` → IOP `0x105c00`) + 531 cid-0 packets `{type 1, id, 0x105c00, SPU dst, 0x1000 size}`: **boot bank** vsync 1553–1560 (83 chunks, SPU 0x5040+), **race bank** vsync 24209–24317 (448 chunks = T47's 1.75 MB) |
| 22:02 | E6: tag-3 decode (`tagbuf.py`) of PCSX2 | Payload = buf[0x18:0x18+0x2A8]; +0 wet mask 0, +8 dry mask 0xFFFFFFFFFFFF (all 48 voices dry), +0x13 = 0x32; voice word = keyon serial<<23 \| SPU byte addr. 239 keyons / 234 keyoffs; stereo SFX = voice pairs panned (vol 40,0 / 0,40); pitch 3763 (44.1 kHz) / 4130. SPU2 reg log: 19,930 writes (PITCH 5,123, VOL 3,868, SSA 506, ADSR 1,047, KON 147 pairs); init sets VMIXL/R = all voices on both cores |
| 22:05 | E7: lldb-launched boot (own lease, runner launched under lldb, never attached), hit counts on the bank-load chain | `sub_003B6098` (BNKl loader) 6 hits, chunk uploader `sub_003C42D8` 0 hits. The six banks we load all have SPU size (+0xC) = 0 and EE size (+0x10) = 0 |
| 22:08 | E8: ISO scan of BNKl headers + ISO9660/BIGF directories (`isofiles.py`) | Only 7 banks carry SPU data, all in `/DATA/AUDIO/AUDIO.BIG`: SSX3Menu (0x527b0 = PCSX2's 83 boot chunks), FE Menu, Crowd, land, Mtn, zboardSPU, zBxsfx; Crowd+land+Mtn+zboardSPU+zBxsfx = 109+32+77+59+171 = **448 chunks = PCSX2's race upload**. The list comes from `/DATA/CONFIG/BANKS.INF` (`[FE] MAIN = "SSX3Menu.bnk"`, `[WORLD] …`) |
| 22:10 | E9: boot with movies (`PS2X_SKIP_MOVIE=0`) | Still cid0 = 0 at 3,008 ticks: not the movie bypass |
| 22:12 | E10: our cd.log vs ISO directory | We read MUSIC/PLAYLIST/MIX/SPEECH.INF (vsync 67–68) and CROWD.INF (race) but **never BANKS.INF**; lldb: FE loader `sub_00285FB0` 3 hits, WORLD loader `sub_002862A8` 4 hits (both run) |
| 22:16 | E11: lldb name log on `func_3DE4D0`/`func_3E3208` | `data/config/banks.inf` gets a lookup but never the follow-up open every found file gets → lookup fails. `func_3E4508` = uppercase path, hash h·33+c (seed 0x1505), **bsearch** over a qsort-ed table with comparator `0x3E3968` (`lwu, lwu, dsubu, bltz, bgtz`) |
| 22:20 | E12: dump the table from our runtime (lldb python breakpoint) + offline simulation | 163 entries, **rotated** (ascending from 0x4ab5c634, wraps at index 111). bsearch with the 32-bit wrapped difference misses exactly 6 files: **BANKS.INF**, SLUSOVF.BIG, GRNT_ZOE.BNK, GRNT_ARI.BNK, NETCNF.IRX, EZMIDI.IRX |
| 22:22 | E13: generated code for `sub_003E3968` | `dsubu` is 64-bit, but `bltz`/`bgtz` emit `GPR_S32(ctx, 3) < 0`/`> 0`. **Recompiler bug:** `control_flow_emitter.cpp` BLEZ/BGTZ/BLTZ/BGEZ(+L/AL) test the low 32 bits; the R5900 tests bit 63 (BEQ/BNE already compare 64 bits) |
| 22:30 | Fix on fork worktree `au9-spu` (from `ssx3` `f949ff0`): emitter → `GPR_S64` + codegen test; runtime override `ssx3-file-key-compare` replacing 0x3E3968 with the guest semantics (the external codegen tree predates the emitter fix) | Build OK |
| 22:37 | E14: leased boot with the override (slot 4, I26-FAST, 4,000 records, 385 s wall on a busy mini) | **cid0 = 531, dmq = 531, done = 531**: exactly PCSX2's 83 + 448 chunks; first packet `spu=0x5040 size=4096` like PCSX2 |
| 22:38 | Orchestrator note: E54E made the same emitter change globally and its boot went black. Scope: keep the runtime override; emitter change = separate, labelled, not validated | Emitter change kept as its own fork commit `a46fb2e`, marked NOT validated; boots use the old codegen |
| 22:40 | E15: our tag-3 after the fix (sampled tag-buffer dumps of the E14 boot) | Menu keyons identical to PCSX2's (`0x822470`/`0x102ddc0` pitch 3763, `0x1814d50`/`0x201b8e0` pitch 4130, same vols); race 20–27 active voices |
| 22:45 | SPU voice layer in the fork (`8af43c1`): `ps2_snd_spu.h` (ADPCM/ADSR/Gaussian from PCSX2, attributed; SNDDRV `updatevoices` port incl. ENVX retry and status word/NAX slots; SNDDRV 3→4 upsampler), SND HLE mixes music + core0 + core1 voices at 48 kHz, cid-0 payloads → SPU RAM, `PS2X_SND_VOICES=0`, `PS2X_SND_MIX_RAW` | Suite **602/602** (6 new tests); runner sha256 `receipts/runner-sha.txt` ×2; runner-dir diff vs `14b1e5cb` empty |
| 22:47 | E16: PCSX2 race tap levels (`tap-race.bin`, tap from sample 19.4 M) | out = in0 + dry0 + dry1 exactly (residual 2 LSB RMS/s, except 2 clipped seconds): the mix model needs no extra gains (MVOL 0x3fff, wet silent) |
| 22:49 | E17: upload bytes: our 531 upload dumps (E14) vs PCSX2 SPU RAM dumps after each phase (`spuram_cmp`-style check) | boot bank 82/83 chunks, race banks 443/448 chunks byte-identical; the 1 + 5 mismatching chunks = the last partial chunk of each of the 1 + 5 banks (hypothesis: staging-buffer leftovers past the bank end) |
| 22:50 | E18: offline replay (`spu_replay.cpp`): PCSX2's own tag-3 records through our driver + SPU model, SPU RAM = PCSX2 dump 021 (after race banks), vs PCSX2's tapped dry0+dry1, same capture, one NCC lag | 28.5 s: **RMS per second equal within 0.1–0.3 %** (e.g. 2298/2298, 2585/2591), NCC 0.967 overall (L 0.966, R 0.962), **0.997–0.999** in the loud SFX seconds, 0.84–0.88 in quiet ones. `receipts/e16-replay-vs-pcsx2.txt` |
| 22:53 | Live validation boot queued: E57 holds all four mini slots (speed boots) | waiting on the lease loop |
| 22:54–23:03 | E19: live validation boot `run-v1` (runner `8af43c1` build, slot 3, I26-FAST, voices on, `PS2X_SND_MIX_RAW`, frame dumps) | rc 0, wall bound 560 s (busy mini), 5,922 ticks = 64 s guest; cid0/dmq/done 531. Race HUD at 00:00:36, tick 3908 (frame not black). **Menu SFX vs PCSX2, aligned on the first voice onset: NCC 0.9997 over 2 s**, RMS 3862/3672 vs 3913/3721 (s0), 1813/1562 vs 1821/1567 (s1). Race voice layer: ours 1728/1714 RMS, 98 % of frames non-zero; PCSX2 1260/1202, 66 % (different scene: our rider sits at 1 MPH) — `receipts/e19-ours-vs-pcsx2.txt` |
| 23:05 | E20: music path in that boot vs its own tag-1 records (planar, block 1 = L, SNDDRV 3→4 integer model) | **100.000 % exact, both channels, 6,002 ticks** — `receipts/e20-music-exact.txt` |
| 23:06 | Clips (48 kHz AAC 128k): `AU9-A-ours-before-music-only`, `AU9-B-ours-after-with-sfx` (same 59 s of run-v1: menus → race), `AU9-AB-15s-race-before-then-after`, `AU9-C-pcsx2-race-output`, `AU9-D-pcsx2-race-voices-only`, `AU9-E-pcsx2-menu-output` | 0.18–1.01 MB each; sha256 `receipts/clips-sha.txt` |
| 23:08 | Fork `243b759`: missing `<cstdlib>` in `ps2_snd_spu.h` (found by the offline replay build); rebuild | Suite 602/602; runner sha256 `e829827b…6a61` ×2 (run-v1 used the build before this include-only change: `receipts/runner-sha.txt`); runner-dir diff empty |
| 23:10 | E21: race voice sets | Ours settles at 4 steady voices (`0xc6e20` ×3, `0xafc80`); PCSX2's last race tick uses the same samples. The always-on layer is the game's loop sounds while our rider is stopped, not a stuck voice |
| 23:07 | E22 (orchestrator datum): paired deterministic boots, frame dumps at ticks 2400/3000/3600, AU8 runner (no override) vs AU9 runner | running |
