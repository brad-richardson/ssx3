# GB7C4 orchestrator gate — PASS actual glyph-row sample, category B

Worker commits `52dfba68` and corrective `e978ebe0` (`Orchestrated-By: opencode`) contain a 73-row bounded trace and report. Private GB4 fork commit `a01e679` adds a default-OFF CPU replay probe in three files; fork tree is clean and runner-dir diff vs `14b1e5cb` is empty. The first worker stopped at the OpenCode edit boundary before any fork change; the resumed pane used a scoped worktree edit exception. I read the full corrected report and source, independently parsed the four source/destination trace pairs, checked `git show --check`, and viewed the tick600/601 title and tick700 menu frames.

| Gate check | Orchestrator result |
| --- | --- |
| Validation and scope | One incremental build; flag-OFF suite 556/556; one ON direct-CPU replay through marker700 and one OFF full-stream control, each 556/556. ON trace 73 data rows/25,944 reported bytes, cap not hit. Both mini P-lane slots free; no device/GPU run or speed claim |
| Frame control | ON/OFF present hashes match at ticks600 `a6948f2`, 601 `582dd887`, 700 `97b4641e`; all three ON/OFF PPM SHA pairs match exactly and agree with GB7C2 baseline. Title text is visible at both 600 and 601; menu at 700 has existing sprite artifacts |
| Actual glyph-row samples | For candidates A–D, independently checked each packet47176 C1 changed source word and GS swizzled address against packet47240 carrier tap0 word/address: 4/4 exact. Measured glyph-row UV maps dst+(1,1), with fx=fy=0 at the four target pixels. CLAMP, TEST, FRAME/FBMSK, ABE and vertex state were captured. All four destination old/new words are equal, with RGB already matching the sampled source; state supports an accepted same-value write, while a changed write was not observed |
| Perturbation prediction | At measured A target only, a correctly timed source word `0xDEAD0001` at address `000bae74` predicts destination `(342,377)` `00353341→00AD0001`. The corrective report withdraws the unsupported claim that every other pixel stays unchanged and treats a mismatch as ambiguous until poke timing/application is proved |

Verdict: **PASS for the actual glyph-row carrier sample; predeclared category B** (sampled source and equal destination on the baseline replay). This establishes the CPU path's measured address/word relationship, not causal transport or a paraLLEl-GS damage cause. Because glyph RGB already exists in the displayed FBP112 destination before packet47240, the next G step is to identify the earlier packet that first writes that displayed glyph pixel and compare its CPU/GPU behavior. The bounded one-pixel carrier poke remains a possible follow-up after that producer is located.
