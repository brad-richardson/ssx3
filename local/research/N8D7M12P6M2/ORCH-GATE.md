# N8D7M12 Part 6M2 orchestrator gate — fixture A, real-root use held (2026-09-24)

Read full REPORT, complete `source_manifest.py`, fixture checker and commit `a8e42230` (`git show --check` clean). Reran the fixture checker: 13/13 A. It verifies two-read files, symlink targets, mutation/add/remove detection, runner rejection, caps and deterministic entries on small roots. No real source root, build, package, device, lease or speed result.

**B for next-package readiness:** `scan_scope` skips excluded names, but its pre/post `list_tree` uses `os.walk(root)` without pruning them. I independently demonstrated `list_tree` includes `build/volatile.o` even though `build` is excluded from the manifest. A live build/cache change can therefore make a valid snapshot fail, and a large build tree still incurs a full metadata walk. Fix `list_tree` to prune the same exclusion names in `dirnames` and filenames, then add a fixture where an excluded cache file changes between pre/post scans while an included file is read. Keep exact entry and aggregate behavior otherwise. Do not run this collector on the large real roots until that follow-up is gated. The script remains a source-byte snapshot, not compilation or old-APK proof.
