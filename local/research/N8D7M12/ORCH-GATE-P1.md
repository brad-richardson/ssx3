# N8D7M12 Part 1 orchestrator gate — A, desktop parity (2026-09-24)

Read the complete REPORT and checker, ssx3 worker commit `a65bc907` and fork source commit `24801bc`. Reran `check.py` (40/40 PASS), inspected the shared core/result API, desktop wrapper and CMake guard, and checked fork `git show --check`, clean worktree and empty runner-dir diff. The ssx3 `git show --check` flags six trailing spaces only in the committed *text patch receipt* at literal `+ ` diff lines; its SHA exactly matches `git show --format= --binary` from the fork and the fork source commit itself passes `--check`. This is a receipt-format exception, not source whitespace.

The isolated Mac Release build passed 585/585 tests. One replay of the pinned 1,100,696,462-byte stream used matching SHA reads and produced the same parser counts, marker2050 descriptor, input/circuit/stage/oracle 448-tile vectors (300 active), named present `d19b96fe`, hash file and PPM bytes as the N8D7M6 Mac baseline. The N8D7M5 word-watch code is guarded in the replay TU for test builds; a flag-free preprocess check found no active word-watch reader or emission, though inert CPU-backend counter definitions remain. These receipts meet **A for desktop parity of the shared parser**.

No Android branch, APK or Odin run occurred. Android compile/link/loader behavior remains unproved, as does the graphics cause. Next: add the dev-only, default-off NativeActivity branch in the isolated N worktree, build/package the Android app, and verify OFF-path behavior before staging the same stream for a separately gated Odin run. No diagnostic wall time is a speed number.
