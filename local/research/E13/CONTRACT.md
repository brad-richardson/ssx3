# E13 experiment contract

Start 2026-09-21 10:39:04 UTC; eight-hour deadline18:39:04 UTC. Latest pull
already current. E13 brief and E12 REPORT/NEXT-BRIEF read in full. The renewed
exception authorizes only the exact `0x2c5358` entry; prior entries are guards.

| Hypothesis / observable | Alternative / stop |
|---|---|
| E12 checkpoint remains identical | All9,451 generated names/hashes, registry and binaries, suite452, predicate5300 fourteen/query4/DROP rerun before mutation. Mismatch → table |
| Exact leaf entry is absent | Fresh fixture linked to baseline runner objects; absent mode rc0 and expected-present rc1 before CSV edit |
| Fresh admission allows one row | Persist after fail-before and before CSV edit; recheck immediately before spawn. Expected CSV SHA `0c6085744ab1a2afd60170a18ac261db02472441fa1d343ffd6756026e92d9f4`; table independent match/mismatch |
| Actual leaf returns exact0/1 | Both ports, all12 specified statuses and cross-port contradictions; RA/SP/callee-save128/RAM/no API calls; actual leaf result in low-bit consumer must contribute0 for status0, stale even value contributes8 |
| One boot advances the flag/state path | Fresh dynamic MC/UI guards; leaf/wrapper/caller returns; UI+0x43c old/new stores; state handler original arguments; prior query/predicate/copy/source/D/Present joins |
| Outcome selection | (i) result and state path advance; (ii) next wall appears, table without another fix; (iii) admission/generation/binding/observation fails, repair recipe only |

| Resource / ownership | Declared bound |
|---|---|
| Generation admission | 1 GiB logical reservation +2 GiB free floor +256 MiB guard =3,489,660,928 B. Reclamation stop target plus512 MiB =4,026,531,840 B |
| Initial read-only sample | Internal4,546,560,000 B; no admission inferred until fresh ordered samples |
| Reclamation | Only retired trees, oldest-first, individual board and no-file-handle proofs; recheck before removal. Protect `/tmp/p1-link` and DerivedData; no waiting on another lane's lease |
| APFS scratch | Fresh owned `/tmp/ssx3-e13-leaf-codegen`, bounded and cleaned after installation hash check, before build |
| Generation | 1 GiB logical/1.25 GiB allocated;128 MiB output reserve;2 GiB free floor+256 MiB guard;1,200 s wall with15 s reserve;16 MiB log with1 MiB reserve;0.25 s polling |
| Installation | Changed bytes only, no metadata, proven-created sidecars only;24 GiB allocation-growth cap with256 MiB reserve and2 GiB SSD floor; generated sources never staged |
| Build/link | `-j4`; SSD-owned outputs/TMPDIRs,8 GiB temp allocation cap with256 MiB reserve;2 GiB SSD free floor;separate1 GiB internal free floor;1,200 s link/1,800 s build with15 s reserve;16 MiB logs with1 MiB reserve |
| Single boot | At most one, planned90 s wall with15 s termination reserve (≤600 s);1,000,000 syscall lines;atomic shared lease, release immediately after exit; never wait if occupied |
| Boot bytes | Aggregate1.5 GiB logical/allocated;boot256 MiB,syscalls96 MiB,function1 GiB;E4 12/64 MiB,park32/128 MiB,frames32/64 MiB,E7 8/32 MiB logical/allocated;8 MiB allocation reserve,SSD floor2 GiB |
| Observation sink | Existing E7 tick≤603 limits:4 MiB boot,1 MiB boundary,512 KiB packets; read-only additions, original a0/a1 retained |
| Retention / commits | Canonical closed captures and complete tails; named fork files only,pushed to fork remote;standalone `[E13]` evidence with `Orchestrated-By: Muse Code`;no ssx3 push |

I17's row bytes are an independent check only; derive this row from the ELF
and emitted owner slice first. Neither lane blocks. No adb, second repair,
new HLE, raster/Present change, or storage/input/SIF/CD promotion without a
named guest request/completion dependency. Standing no-regen resumes at close.

**E13 CONTRACT TAIL COMPLETE — recorded before checkpoint execution,
fixture relink, reclamation, CSV mutation, generation, build or boot.**
