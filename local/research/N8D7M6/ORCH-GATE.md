# N8D7M6 orchestrator gate — PASS/A (2026-09-24)

Read the complete REPORT, both scripts, prepared receipt, device result, comparison and tile excerpt. Reran `accept.py --capture`: the 1,100,696,462-byte PS2XGSC1 file has SHA `f6a78f71fa1a5f5bb21442ff2c6757a743cd42c5d2d74a108977d8416da4a593`, 862,958 packets, 11,499 priv, 25,445 transfers, 2,050 markers, and EOF marker tick2050. Independently parsed all four 448-word vectors from the Odin and Mac logs: Odin input/circuit/stage/oracle each 3,990 occupied, 23 active, SHA `2106649d…c9137`; Mac each 64,399 occupied, 300 active, SHA `1d847d50…bd988b`. The 11 selected-image fields match. Checked the Mac replay summary counts equal the captured stream and the final suite is 585/585. `git show --check effbf5ec` passed; its commit contains text/scripts and one PNG, no raw stream or game data. Pinned fork runner-dir diff is empty.

Viewed the SHA-checked tick2050 Odin PNG (`1fec7d39…59114c4`): mostly black, sparse HUD/terrain. Viewed Mac paraLLEl PPM (`9490484c…ce3e`): slope, HUD and rider scene present. The visual difference is consistent with the full-vector census. Odin app PID is absent and lease is `LEASE_FREE N8D7M6 done`.

**Verdict A:** on the *same* captured stream and selected-image descriptor, the Mac paraLLEl selected VRAM is broad and the Odin selected VRAM is sparse. The divergence is device-specific at or before the selected snapshot. This does not yet distinguish a missing/incorrect GPU write from copy/readback timing or another earlier device-specific step. Eight control words are supporting observations, not a frame-wide proof. No speed claim.

Budget deviation accepted and recorded: the worker ran two Mac replays because the first was launched from the repo cwd and an unrelated VU0 test that reads `instructions.h` by relative path failed (584/585), although GS replay completed. The second used the fork cwd, exited 0 and passed 585/585. The first log was overwritten, so only the second is a preserved full replay receipt; this weakens audit of the first attempt but not the clean second result. One Odin install/launch only, and no further replay.

Next: a bounded design that samples one same-stream checkpoint *after GPU work is complete but before selected-VRAM copy/readback*, then compares the full selected data at the copy boundary on Mac and Odin. Require an execution/completion witness; queued submit counts or a later broad frame alone cannot separate missing write from ordering. Have the worker hand back a source-grounded table before authorizing another device build/run.
