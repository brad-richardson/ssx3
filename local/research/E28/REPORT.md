| E28 | Receipt |
|---|---|
| Stop point | **Both missions executed; nothing blocked, and the boot was not stopped.** Mission 0 took path **(a) VERIFIED IN PLACE** and the capture preflight gate passed **5/5 with zero re-pinning**. Mission 1 **derived the kind-1 queue slot's guest address statically** — `0xd486ec` — so the boot was armed rather than stopped. Mission 2 spent the one `e28a` boot: rc 0, bound `wall` at 75.419 s, 243 watch entries, lease released 8.93 ms after process end. **E27's four guest-half predictions all CONFIRM**, with one value-half reported `unobservable-as-designed` for the reason the brief names. Fix gate: **STOP**, honored as written. |
| Checkpoint | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176` triple-agreed at open **and** close; `status --short` exactly `?? ps2_log.txt`. Runner `e462e448…67e22` / suite `2152e5ad…f04c0` re-hashed **six times** across the lane (two restore passes × two reads, the probe gate, the driver's own preflight). Observer dylib `e4d88fdc…b38e6be` reused by `cp -p` + re-sha, never rebuilt. The 458-test suite ran **fresh inside the boot driver's own preflight**, `Failed: 0`, before the atomic claim. |
| Questions reached | **Mission 1 CLOSED, and it did not need a boot to close.** The slot address falls out of an arithmetic identity in the STRM constructor's own layout code, run backwards through a number E26 already measured. **Mission 2 CLOSED.** P1 **CONFIRM** · P2 **CONFIRM (change-half); at-park value unobservable-as-designed** · P3 **CONFIRM** · P4 **CONFIRM**. And the boot returned one number nobody had: the queue the guest is sitting on is **431,840 bytes** deep, not one chunk. |
| Mutations / launches | Zero fork source edits, zero fork commits, zero pushes, zero regeneration, zero builds, zero relinks, zero observer rebuilds, zero deletions. **One title boot (1/1), one lease claim, one lease release.** |
| Next dependency | None blocking. E28 hands over a confirmed guest half and names, in one paragraph, the single host-side observable E29 needs. |

| Experiment contract | Bound / observable |
|---|---|
| Time box | 8 h. Opened `2026-09-22T15:02:05Z` (fork gate), closed the same session. **Actual time used: well inside the box.** |
| Required reads | E27 `REPORT.md` (199 lines) + `mission-3-reask.json`; E26 `REPORT.md` §Mission‑2(a) + `chunk-map.json` — all read in full before the first gate. |
| Hypotheses | **E27's, carried verbatim; E28 pre-registered nothing new.** P1 head/byte-count · P2 `0xd49b18` at the park · P3 zero post-feed stores · P4 `[MPEG:GetPicture]` ×1 and `[MPEG:feedES]` ×1. Written into `CONTRACT.md` before the instrument copy, and into `pre-registration.json` — with the exact pass/fail bytes and the address carrying each row — before the lease was claimed. |
| Result | **P1 realised. P2 realised on its observable half. P3 realised. P4 realised.** Zero refutations. |
| Observable | 6,804 watch emissions across **243** armed addresses over **100** distinct addresses; a 46,907-line boot log; a 127,121,563 B function log; the parser's own closure receipt; the park snapshot. |
| Strict order | Fork gate (**first**) → hex-safe tooling carry + proof → `CONTRACT.md` → instrument reuse → open admission → **Mission 0** (two restore passes 107.5 s apart, restore gate, e26a capture re-sha) → **Mission 1** (static derivation) → watch-set change + receipts → boot fidelity → probe gate → **ONE boot** → **Mission 2** verdicts → fix gate → close. No gate tripped. |
| Fix gate | **STOP.** `fix-gate.json`. |
| Preserved constraints (E18 ABI, BINDING) | **Preserved by construction** — E28 edited no source and built nothing — and by identity: the binary it booted is byte-for-byte the one every E15–E27 receipt was taken on. |
| Contract source | `CONTRACT.md`, written before the instrument copy, the open admission and the boot. Internal reservation 3 GiB, floor 2 GiB + 0.5 GiB guard, unchanged. **No amendment declared.** |

## Mission 0 — verify-or-restore + preflight, no boot

| Mission 0 | Receipt |
|---|---|
| **Restore path taken** | **(a) VERIFIED IN PLACE.** No rebuild, no share-tier restore. Every pin standing at its exact path. `restore-pass-1.json`, `restore-pass-2.json`. |
| Two matching reads, separated in time | Pass 1 `15:03:42Z`, pass 2 `15:05:29Z`, **107.5 s apart**; each pass reads every pin **twice back to back**. Four reads per pin, **1 distinct value each**, all equal to the pin. `restore-gate.json`, **green**. |
| Independent corroboration | Every pin's SHA also equals the one **E25** measured on its own run. Both halves of the standing SSD rule satisfied — repetition in time *and* independent corroboration. |
| Runner / suite | `163,529,696 B` / `e462e4482fbf5b3fe0902e793f9d677e51c56b220022bca619305f1f69967e22` · `5,695,128 B` / `2152e5ad53f19741788aa3051e26184a9c5e831857114ce74407f199de5f04c0`. |
| **The five-pin gate, as written** | **5/5 PASS, 0 re-pinned**: runner, suite, ELF (cd) and ELF (P1) both `1b49d05c…7af7bc` at 3,890,784 B, observer dylib `e4d88fdc…b38e6be` at 52,472 B. Run **twice**; the two passes' gate rows are identical. `readiness-pass-1.json`, `readiness-pass-2.json`, both **9/9 green**. |
| ISO | Size only — 3,005,415,424 B — exactly as E24's designed preflight checks it. |
| **The e26a captures, re-sha'd before mining** | `ps2_log-e26a-1.txt` `d849775b…cf1024ec` (127,074,085 B) and `boot-e26a-1.log` `c9ddf91a…78e33e5` (13,355,448 B) — **both equal to E26's committed pins**, read at `15:04:08Z` and again at `15:19:52Z`, **15 min apart**. `MPEG.cpp` `f83ed02e…` **equal to its git object**. `pins-pass-1.json`, `pins-pass-2.json`, `pins-gate.json`. |
| **A corroboration E27 could not have** | E27 stated, correctly, that the 16 generated guest sources are `.gitignore`d and that "repetition in time is the only check available" for them. That is no longer true: **E27's own pin values are now committed in this repo** (`E27/pins-pass-{1,2}.json` are git-tracked). E28 compares every generated source it touches against those committed values as well as against its own two reads. **19/19 rows equal across E28 pass 1, E27 pass 1 and E27 pass 2.** The upgrade is named because it changes what a later reader may rely on. |
| SSD link | Mounted and stable throughout; re-checked at the fork gate, both restore passes, both pin passes, the boot and the close. It never disappeared, so the brief's table-and-stop rule was never triggered. |
| Posture at open | Lease absent, `pgrep -x ps2EntryRunner` rc 1, no `boot-attempt.json`, no `*e28*` path in the run directory. |

| Tooling carry | Receipt |
|---|---|
| Hex-safe rename (18 files, two source lanes) | From **E26**: `fork_gate · io · closed_events · parser_receipt · mine · validate · bounded · instrument · tail · readiness · restore · restore_gate · boot_fidelity · open · prepare_probe · capture · close`. From **E27**: `pins`. Normalized diff **byte-empty for all 18**; every hex run byte-equal; every protected token count preserved. `rename-proof-hexsafe.json`. |
| Bootstrap | `e26_rename.py` hardcodes `e26` as its **destination** token and so cannot rename itself. `e28_bootstrap_rename.py` applies E26's own algorithm — its regex, its protected list, its masking order — with the destination parameterised, and then proves the result by **FIXPOINT**: the tool it produced, asked to rename `e26_rename.py`, returns its own bytes. `rename-bootstrap.json`. |
| **A bug the rename proof is blind to, found and fixed** | E25 named its readiness receipt **`e26-readiness.json`** — it wrote the readiness for the lane that followed it. That is a **real path**, not a lane token, and the mechanical `e26→e28` carry silently rewrote `e28_restore_gate.py`'s reference to `E25/e28-readiness.json`, **which does not exist**: the Mission 0 restore gate would have died on `FileNotFoundError` at the gate. The normalized-diff proof cannot see it, because it maps both lane tokens to one placeholder and the file is *supposed* to change there. This is errata **E23-E1's class**, one directory over. The token is now PROTECTED (CHANGE 1) and `e28_carry_audit.py` **resolves every cross-lane path literal in every carried tool** — 8 references, 0 dangling — so the class is caught, not just the instance. `carry-audit.json`. |
| Intentional changes (4, all recorded, all idempotent) | **CHANGE 1** the protected token above. **CHANGE 2** `e28_boot_fidelity.py`: the watch set is the one difference the brief permits, so "SHA-equal to E24's" is replaced by a **stronger** statement — E24's 230-entry vector must be carried verbatim, unreordered, and appear as an **exact prefix**. **CHANGE 3** `e28_close.py` computes this lane's release latency instead of carrying E26's literal `8`. **CHANGE 4** `e28_close.py` refits the cost model from E26's **committed** point list, so E26's measured third point is not silently thrown away. `tooling-changes.json`, `tooling-diff.md`. |
| Written fresh, not claimed as a carry | `e28_fixgate.py` (E26's reads E26's own mission receipts, which E28 never produces), `e28_common.py`, `e28_bootstrap_rename.py`, `e28_carry_audit.py`, `e28_slot.py`, `e28_watchset.py`, `e28_prereg.py`, `e28_verdict.py`. Absent from the rename proof **by design**, and listed in `tooling-changes.json` under `written_fresh`. |
| **The capture driver itself** | **UNCHANGED.** `e28_capture.py` is byte-identical to E24's under the hex-safe rename — 18,158 B → 18,158 B — because the watch set lives in `watch-set.json`, which the driver loads. The one permitted behaviour change needed **zero** driver edits. |

## Mission 1 — the slot address, derived statically

E27 named the kind-1 slot as `ctx0->[0x24] + (kind-1)*16` and never needed to know where that is. E28 cannot arm a watch without it, and the brief forbids blind watches. It is **not** a guess: the STRM constructor computes the slot array base and then computes the **data region base** *from it*, in the same eight instructions — and E26 measured that data base in bytes.

| The chain | Receipt |
|---|---|
| **The constructor** | **`sub_003E06D8`**, guest `0x3e06d8`. `$s0 = $a3` is the allocation; `$s1/$s2/$s3 = $a0/$a1/$a2` are `n1`, `n2`, `nKinds`; `[ctx0+0] = 'STRM'` at `0x3e0768`. |
| **The layout, instruction by instruction** | `0x3e0770 $a2 = ctx0 + 0x180` · `0x3e0774 $v1 = n1 * 0x124` · `0x3e0794 $t3 = $v1 + $a2` · `0x3e07a4 $a0 = n2 * 0xC` · **`0x3e07b8 $v1 = $a0 + $t3` — the slot array base** · `0x3e077c $v0 = nKinds * 16` · `0x3e07c0 $v0 = $v1 + $v0` · `0x3e07c8 $v0 &= ~0x3F` · `0x3e07d0 $v0 += 0x40` — **the data region base**. Then `0x3e0808 sw $v1, 0x24($s0)` and `0x3e07e8 sw $v0, 0x54($s0)`. |
| **A second function computes the same constant** | `sub_003E06B0` exists only to return the header size: `n1*0x124 + (n2*0xC + 0x180) + nKinds*16`. For (2, 3, 2) that is **`0x40c`** — equal to `slot_offset + nKinds*16`, derived twice by two functions that share no code. |
| **Which call site ran** | `sub_003ADEC8` has **three** `jal func_3E06D8` sites. Sites A (`0x3ae008`) and B (`0x3ae024`) are **straight-line consecutive** — no branch between them — so reaching A forces B and gives **two** calls. The e26a function log has **3** constructor entries: **2** under `sub_003B7450` and **1** under `sub_003ADEC8`. One call therefore means the site at **`0x3ae1b0`**. |
| **And the log is not the only witness** | Sites A/B pass `nKinds = 1` (`0x3ae004`). E26 **measured a kind-2 tag** — `0x02000028` stored at `0xd49af0`, boot-log line 22351 — and the walker publishes into `ctx0->[0x24] + kind*16 - 16`, so a kind-2 chunk is impossible with one kind. **Only the `0x3ae1b0` site (`nKinds = 2`, `0x3ae1a4`) can produce the bytes E26 already had.** The call site is fixed by measurement as well as by the log. |
| **The arguments** | `0x3ae1a8 $a0 = 2` (n1) · `0x3ae1ac $a1 = 3` (n2) · `0x3ae1a4 $a2 = 2` (nKinds) · `$a3` = the allocator's return. |
| **The alignment, not assumed** | `0x3ae188` passes alignment **0** to the allocator. `sub_00253AD0` is `if (0x3F < align) keep else align = 0x40` (`0x253ad0`–`0x253ae8`), and its delay slot at `0x3ae194` clobbers `$a3`, **not** `$a2`. **The allocation is 64-byte aligned by the allocator's own floor.** |
| **The identity, run backwards** | `slot_offset = 0x180 + 2*0x124 + 3*0xC = 0x3ec`; `header = 0x40c`. `((ctx0 + 0x40c) & ~0x3F) + 0x40 = 0xd48740` ⟹ `ctx0 ∈ [0xd482f4, 0xd48334)`. Exactly **one** multiple of 64 lies in that half-open 64-byte interval. |
| **The answer** | **`ctx0 = 0xd48300`** · **kind-1 slot = `0xd486ec`** — `+0 owner`, `+4 kind`, **`+8 bytes = 0xd486f4`**, **`+0xC head = 0xd486f8`**. Kind-2 slot at `0xd486fc`. Forward check: `((0xd48300 + 0x40c) & ~0x3F) + 0x40 = 0xd48740` ✔. |
| **Every quoted instruction is checked, not recalled** | **59 receipts** across 9 generated sources, each grepped out of the file; the tool refuses to continue if one is absent. **0 missing.** Two reads per source, and 4 of the 9 compared against E27's **committed** pins. Run twice, 15 min apart: **identical, `0xd486ec` both times.** `mission-1-slot.json`, `mission-1-slot-pass-{1,2}.json`. |
| **Confirming consumers** | The walker `0x3dffa4-0x3dffb0` computes `ctx0->[0x24] + kind*16 - 16`; the accessor `sub_003E0BF8` `0x3e0c3c-0x3e0c44` computes the same; the resolver `sub_003DFBD0` reads `ctx0` back out of **`slot->[0]`** and checks the `'STRM'` magic; `sub_003AEAD0` `0x3aeb14` fetches the slot as `source->[0x30]`, which is the constructor's own return value stored at `0x3ae1c4`. Four independent consumers, one layout. |

| The watch set — the ONE permitted change, with before/after | Receipt |
|---|---|
| Before | E26's `watch-set.json`, `a37a5eb0…945204`, 6 tiers, 197 tiered + 4 singles + 29 carried = **230**. |
| After | `797fdee8…`, 7 tiers, 210 + 4 + 29 = **243**. |
| The appended tier | **`kind-1 queue slot (+ chunk 0 header)`**, `0xd486e0`–`0xd48748`, stride 8, **13 entries, complete byte cover**. |
| Why a window and not four addresses | The tier covers **every position the slot array could occupy if the 64-byte alignment step were wrong** — the whole `ctx0` window at 4-byte granularity. An emission outside `0xd486ec`–`0xd486fb` would have **refuted** the derivation and named the true address. The boot therefore **measures** the address instead of trusting it. |
| Why it reaches down to `0xd48740` | E27 named `0xd48744` as outside every E24 tier, so chunk 0's own tag store "was never observable". The store that **strips** that tag (`sub_003E12E0` `0x3e1330`) is the companion of the head advance P1 is about. One extra entry closes a named blind spot. |
| Proved, not asserted | Carried tiers identical element for element · only `tiers`/`new_entries`/`total_entries` differ · **E26's vector is an exact prefix of E28's** · no duplicates · new tier disjoint from everything carried · all four slot fields covered. **6/6.** `watch-set-change.json`. |
| Cost | 13 entries ≈ 0.10 s of span-complete on E26's refit, against a 75 s bound. |

| Boot fidelity — proved, not asserted | Receipt |
|---|---|
| **14/14 green** | Driver byte-identical to E24's under the rename · watch set differs **only** by the declared tier · every tier's count honest · 230 + 13 = 243 · CAPS and ALLOCATED_CAPS identical · E24's address list an exact prefix · display/offset tiers identical · ELF pin identical · argv identical · lease path identical · full vector 243, no duplicates · lane wait log E28-spelled · evidence directory E28's. `boot-fidelity.json`. |
| The one declared label difference | `e24a` → `e28a`, by the same mechanical rename that carried E23's driver into E24 and E24's into E26. Six environment values are label-derived output **destinations**; every other key and value is identical. Declared in `CONTRACT.md` before the run. |

## Mission 2 — the one boot, and the verdict table

| Mission 2 — the boot | Receipt |
|---|---|
| Authorization | `e28_prepare_probe.py` green; `entry-preflight.json` + `e28a-build.json` written. The driver's own preflight then ran for real: presence gate on six paths, five-pin hash gate, `pgrep` before **and** after, fresh T13 pre-claims, the aligner selftest (`ALL PASS`), and the **full 458-test suite fresh, rc 0, `Failed: 0`**. |
| Lease | Atomic claim `15:20:29.007237Z` (`LEASE.open('x')`); boot **4.05 ms** later. Released `15:21:44.434785Z`, **8.93 ms** after process end. Post-release `pgrep` rc 1, lease absent. `e28-waits.log` carries the CLAIM and RELEASE lines. |
| Execution | PID 72140. **rc 0**, bound **`wall`**, elapsed **75.419 s**, **no SIGKILL**. `watch_entries=243`. `span-complete` **8.835 s**. |
| Fate | The title reached the same terminal state as e23a and e26a: MPEG `GetPicture` fed one chunk and parked at boot-log line 22876, and the run then continued for **24,031 further lines** without the feed advancing. |
| Byte caps | boot log 13,272,194 B (cap 256 MiB) · trace 3,444,639 (96 MiB) · function log 127,109,447 (1024 MiB) · **aggregate 153,540,919 logical / 216,006,656 allocated** against 1536 MiB. Every bound satisfied; the only bound that fired was `wall`, by design. |
| Capture pins, two reads separated in time | `boot-e28a-1.log` **`21d702c5…3058d1`** 13,272,194 B · `ps2_log-e28a-1.txt` **`bb987c8f…e375dc`** 127,121,563 B · `syscalls-e28a-on.txt` **`a3f4b88b…3cec43`** 3,444,639 B · `parser-input.bin` **`cde8a830…2a1c875a`** 5,040 B. `capture-pins.json`, `observed/e28a-capture-sha256.txt`. |
| **The feed reproduces exactly, a fourth time** | The parser received **5,040 B, SHA256 `cde8a830…2a1c875a`, FNV64 `0xd2a9588f0e0fd358`, first4 `000001b3`** — byte-identical to E22's, E23's, E24's and E26's. `parseCalls=1 offered=5040 consumed=5040 packets=0 frames=0 errors=0 pending=0`. |

| **The kind-1 queue, as the guest built and used it** | Line | Receipt |
|---|---:|---|
| classifier table row 3 | 22325–22327 | `0xd486e8 = 0x1`, `0xd486e0 = 0x0`, `0xd486e4 = 0x0` at `pc=0x3e08dc/e4/ec` — the last `{mask, match, kind}` row, ending exactly where the slot array begins |
| **slot 0 constructed** | **22328–22330** | `0xd486f4 = 0x0` (`pc=0x3e090c`, bytes) · **`0xd486ec = 0xd48300` (`pc=0x3e0914`, owner)** · `0xd486f0 = 0x1` (`pc=0x3e091c`, kind) |
| slot 1 constructed | 22331–22333 | `0xd48704 = 0x0` · `0xd486fc = 0xd48300` · `0xd48700 = 0x2` |
| the CD read | 22362 | `sceCdRead lbn=0x13ba33 sectors=16 buf=0xd48740`, payload `4d504368ac130000` |
| chunk 0 tagged | 22373 | `0xd48744 = 0x10013ac` at `pc=0x3dff8c` — **E27's blind spot, now observed** |
| chunk 0 published | 22375–22376 | `0xd486f4 = 0x13ac` (`pc=0x3dffc0`) · **`0xd486f8 = 0xd48740`** (`pc=0x3dffc4`) — the head takes chunk 0 |
| chunk 1 tagged and published (kind 2) | 22378–22381 | `0xd49af0 = 0x2000028` · `0xd48704 = 0x28` · `0xd48708 = 0xd49aec` |
| **chunk 2 tagged** | **22383** | **`0xd49b18 = 0x100381c`** at `pc=0x3dff8c` — the only store to that address in the whole run |
| 27 more publishes | 22385…22793 | `0xd486f4` climbs to `0x6aa8c` = **436,876 B** queued. **28 walker publishes in all** (`pc=0x3dffc0` ×28), and exactly **one** head store (`pc=0x3dffc4` ×1) — after the first entry `slot->[8] != len`, so the `bne` at `0x3dffbc` skips the head store, precisely as the source says |
| **the dequeue** | **22816–22820** | `0xd48744 = 0x13ac` (`pc=0x3e1330`, tag stripped) · `0xd486f4 = 0x696e0` (`pc=0x3e134c`, 436,876 − 5,036 = **431,840**) · **`0xd486f8 = 0xd49b14`** (`pc=0x3e13c8`, **the head advance E27 predicted**) |
| chunk 0 marked dead | 22864 | `0xd48740 = 0xfffffffe` at `pc=0x3e1450` |
| last emission of the run | **22872** | `0x548800 = 0x548880` at `pc=0x3204c4 ra=0x319ffc` — E23's head advance, now named as a **free**, reproduced a third time. Two lines before the feed |
| the feed | 22874 | `[MPEG:feedES] #0 size=5040 first4=000001b3 decoderFailed=0 waitSeq=1` |
| **the park** | **22876** | `[MPEG:GetPicture] waiting for frames, mpeg=0x587b30 ended=0 failed=0 sawInput=1` |

| **The P1–P4 verdict table** | Verdict | Receipt |
|---|---|---|
| **P1** — the kind-1 queue head reads `0xd49b14` with a byte count ≥ 14,364 from the feed onward and **never** changes | **CONFIRM** | Head `0xd486f8`: **2 stores in the whole run**, last value **`0xd49b14`** (line 22820, `pc=0x3e13c8 ra=0x3e135c thread=1`). Bytes `0xd486f4`: **30 stores**, last value **`0x696e0` = 431,840** ≥ 14,364 (line 22818, `pc=0x3e134c`). **0 stores to either after the feed; 0 after the park.** |
| **P2** — `0xd49b18` still reads `0x0100381c` at the park | **CONFIRM (change-half); at-park VALUE unobservable-as-designed** | **Exactly one** store to `0xd49b18` in the entire run — `0x100381c`, line 22383, `pc=0x3dff8c ra=0x3dff90` — and **0 after the feed**. The at-park **read** is **not obtainable with the existing instruments**: `diagWatchEmit` is store-triggered, so an address nothing writes emits nothing and its value is never read. E28 reports the change-half and does **not** infer the rest. The residual is host CD DMA, which E26 named as writing this buffer without emitting — **bounded here to zero by count: 551 `sceCdRead` calls in the run, 0 after the feed.** |
| **P3** — ZERO guest stores to `0xd49b14`/`0xd49b18` after the feed | **CONFIRM** | **1** store to either address in the whole run (the P2 row above); **0 after the feed, 0 after the park.** `0xd49b14` is never written at all. Both addresses sit in E24's past-end extension tier, carried into E28 unchanged. |
| **P4** — `[MPEG:GetPicture]` ×1 and `[MPEG:feedES]` ×1 | **CONFIRM** | `[MPEG:GetPicture]` **×1** (line 22876, the park itself) · `[MPEG:feedES]` **×1** (line 22874) · `[MPEG:feed]` ×1 · `[MPEG:GetPicture:FRAME]` **×0**. Every `[MPEG:*]` counter caps at 32 in `MPEG.cpp`, so 1 is a true count and not a cap. |

| Controls, as pre-registered | Receipt |
|---|---|
| **The derived address, confirmed by the guest itself** | `0xd486ec` received `0xd48300` at `pc=0x3e0914` — **the value Mission 1 derived, written by the machine into the field Mission 1 said would hold it.** `0xd486f0` received `0x1`: kind 1. |
| Every emission in the derived window, placed | `0xd486e0/e4/e8` ×2 each (classifier row 3) · **`0xd486ec` ×1, `0xd486f0` ×1, `0xd486f4` ×30, `0xd486f8` ×2 — the kind-1 slot** · `0xd486fc/0xd48700/0xd48704/0xd48708` — the kind-2 slot, same four fields, same four `pc`s · `0xd48740` ×1, `0xd48744` ×2 — chunk 0's header. **Nothing landed anywhere the derivation did not predict.** |
| `nKinds = 2`, confirmed dynamically | The kind-2 slot is constructed and used exactly 16 bytes above the kind-1 slot, and its head takes the `SCHl` chunk at `0xd49aec`. The static argument that only the `0x3ae1b0` site could produce E26's kind-2 tag is now a measurement. |
| **The whole vector** | **243** armed entries · **6,804** parsed emissions of 6,810 `[diag:watch]` lines, over **100** distinct addresses · **0 post-park** · **0 post-feed** · last emission line 22872 against a park at 22876, with **24,031 log lines and ~66 s of run still to come.** E26's zero reproduces on a vector 13 entries larger. |
| Torn lines | **6** of 6,810 `[diag:watch]` lines interleaved with a concurrent `[frame:*]` / `INFO: FILEIO` write on the same fd. **0 post-park**, and all 6 land on `0x10005000`, `0x1000a000`, `0x12000070` — the **carried non-producer** tier. **None in the new slot tier.** Counted and located, not dropped. |
| Park snapshot | Reproduces e26a's: thread 1 `Waiting wait=Mpeg:0 pc=0x3b1028 ra=0x3b1028` — E27's `jal func_402A10` return address — and semaphore 36 with one waiter at `ra=0x3c19f0`. |

| The cost model, with its fourth point | Receipt |
|---|---|
| The four points | e22a 29 → 7.61 s · e23a 37 → 8.30 s · e26a 230 → 9.524 s · **e28a 243 → 8.835 s**. |
| Four-point least squares | `k = 0.00596 s/entry`, `base = 7.764 s`. E24's two-point fit predicted **24.9 s** at 243 entries; measured **8.835 s** — over-predicted by **2.82×**. |
| **The number worth naming** | e26a ran **230** entries in 9.524 s; e28a ran **243** entries in **8.835 s**. **Thirteen more watches ran 0.69 s faster.** Run-to-run variation dominates the watch-count term outright, which is the same conclusion E26 reached from a different pair and is now much harder to argue with. A full 630-entry cover would cost about 11.5 s on this fit. |
| Caveat | Four points is still a small fit, and the driver keeps recording span-complete on every path. |

## What E29 needs — one paragraph

E27 asked for a boot that would settle the guest half and log the host half; E28 was given only the guest half, and the guest half is now closed. The guest is not waiting for anything it cannot do: its kind-1 queue holds **431,840 bytes** with the head parked on **`0xd49b14`**, the chunk whose payload at `0xd49b1c` is the start code the parser needs; `0xd49b18` still carries the tag `0x0100381c` and no guest store touches either address again; and the re-ask branch at `0x3b10ac` needs nothing but the `jal func_402A10` at `0x3b1020` to return. So **every remaining question is host-side, and exactly one observable decides it**: instrument `MPEG.cpp` to log each entry to `getMpegPicture` with its `requestInput` flag **and the result of `pending.lock()`**, plus every `completeExternalWait(kMpegPictureWaitType, …)` with its caller. E27's prediction stands unrevised — `getMpegPicture` entered exactly **twice** (`requestInput=true`, then `false` from the `onComplete` at `MPEG.cpp:1760`), `pending.lock()` **non-null on the second**, and `completeExternalWait` for the picture wait called **zero** times after the feed — and e28a changes only what it would *mean*: with the guest proved healthy, a non-null `pending.lock()` at the second entry is no longer one half of a mutual wait but the **whole** of the stall, and the `shared_ptr` moved into the parked thread's own `EeWaitState` at `EeScheduler.cpp:2120` is the only thing left holding it. That is one log line and one boot; E28 names it and designs it nowhere. E29 should also know two operational facts this lane paid for: the capture driver needs **no** edit to change what is watched (`watch-set.json` is data, and a 13-entry addition cost 0.10 s of a 75 s budget), and the mechanical lane rename **will** rewrite a real path that happens to contain the previous lane's token — run `e28_carry_audit.py` before the first gate.

## Gates, hygiene, budget

| Fork gate | Receipt |
|---|---|
| Open | `2026-09-22T15:02:05Z`. `HEAD` = `refs/remotes/fork/ssx3` = `ls-remote fork refs/heads/ssx3` = **`3adc0478b6d2260acdd28a249466f2eef9a20176`**, 7/7 green, `status --short` exactly `?? ps2_log.txt`. `fork-gate-open.json`. |
| Close | Re-run at close, same seven checks, same sha. `fork-gate-close.json`, and the triple-agree is re-checked a third time inside `final-audit.json`. |
| Pre-existing stderr | The four `non-monotonic index …._pack-….idx` lines are the AppleDouble artefact E23–E27 all recorded; carried verbatim, stderr only, `rc 0`. |
| Fork mutations | **Zero.** Read-only throughout: `git rev-parse`, `ls-remote`, `status`, `show`, plus `cat`/`grep` on sources. No edit, no commit, no push, no `git add` inside the fork. Fork allocation growth measured against a 1,549-path baseline: **0**. |

| Fix gate | Receipt |
|---|---|
| Decision | **STOP**, as the brief set it before the lane began. `fix-gate.json`. |
| Demonstrated edges | **Four**, and they are not one edge: the slot address derived and then confirmed by the guest's own store; the head parked on `0xd49b14` and never moving; the 431,840-byte queue depth; and E27's whole prediction set reproducing on a fresh boot with a larger vector. |
| Why no fix | A fix needs exactly one edge plus its own fail-before and full regression; E28 has none of those and was not asked to build them. And a fix would be premature regardless: **every edge E28 demonstrates is on the guest side, and the guest side is not where the stall lives.** |
| Candidates still KILLED | C2a and C4 (E23); C1, C3, C5–C9 (E22). E28 re-confirms the reference they were killed against **by identity** — the runner it booted *is* the binary E23 and E26 booted — and re-proposes none. |
| **X, after E28** | X is no longer "why does the guest stop one chunk short of feeding". **It does not stop one chunk short.** It holds 431,840 bytes in a queue whose head is parked on exactly the chunk it needs, and a live backward branch that would ask for it the moment the host call returns. X is now entirely host-side. |

| Budget | Receipt |
|---|---|
| Internal | Reservation **3 GiB** declared before the first action; **declared, not consumed** — E28 runs no build step. Owned **1,767,870,464 B** (the E18 build tree E25 rebuilt bit-identically), free **4,403,818,496**. Open admission: free 4,658,450,432 against 4,138,201,088 required. |
| SSD | Reservation 16 GiB in NEW `e28-*` paths; owned **223,346,688 B**, free **135,966,752,768**. `COPYFILE_DISABLE=1` on every step. |
| Evidence | **Text only.** The one binary is the **reused** E21 observer dylib (52,472 B), carried by `cp -p` + re-sha exactly as E22–E26 carried it, because the driver loads it by path. The 5,040 B feed vector is **not duplicated**: it is byte-identical to E24's retained copy and is referenced by SHA. |
| Deletions / reclaim | **Zero**, as required. |
| Boots / leases | **1/1 title boots, 1/1 lease claims, 1/1 releases.** No second boot, no probe-claim, no `stdbuf` in any argv. |
| Final audit | **14/14 green.** `final-audit.json`. |

| Evidence hygiene | Receipt |
|---|---|
| Standalone data | Contract; fork gate open and close; rename bootstrap and hex-safe proof; carry audit; tooling changes with full diffs; open admission and fork-allocation baseline; two restore passes and the restore gate; two readiness passes; two pin passes and the pin gate; **Mission 1 with two passes**; the watch-set before/after; the 14-check boot-fidelity proof; the probe-gate manifests; the boot config, preflight, preclaim suite, liveness and result; the **pre-registration**; the **P1–P4 verdicts**; the capture pins; the cost-model refit; fix gate; final audit. |
| Retained text (`observed/`) | `e28a-boot-log-strm-construction.txt` (the slot array being built), `e28a-boot-log-first-publish.txt` (the tag and the first publish), `e28a-boot-log-dequeue-feed-park.txt` (the head advance, the feed and the park), `e28a-slot-emissions.txt` (**all 75 emissions in the new tier**), `e28a-parser-events.txt` (with its real `# E21 PARSER CLOSURE` footer), `e28a-park-snapshot.txt`, `e28-waits.log`, `e28a-capture-sha256.txt`. |
| Errata carried | **E21-E1, E23-E1, E23-E2, E24-E1, E24-E2, E24-E3 carried forward unchanged and not re-fixed.** E24-E2 is honoured operationally: the tail receipt below is recomputed **after** the last edit and its row lies outside the hashed prefix. |
| **Errata added** | **E28-E1.** The mechanical lane rename can rewrite a **real path** whose spelling contains the source lane's token, and `rename-proof-hexsafe.json` is structurally blind to it — the normalized diff maps both tokens to one placeholder, so the corruption looks exactly like the rename working. Instance: `E25/e26-readiness.json` → `E25/e28-readiness.json`, which would have failed the Mission 0 restore gate. Mitigation, now standing: protect the token **and** run a cross-lane path-resolution audit (`e28_carry_audit.py`) before the first gate. Related to, and not covered by, E23-E1. |
| Publication | `[E28]` evidence commit with `Orchestrated-By: Muse Code`, `git add -f`. Scope: E28 evidence only; no fork files, no generated sources. **Not pushed** — the orchestrator pushes at poll. |

| Tail receipt | Value |
|---|---|
| Complete prefix | Lines 1–167; 31920 B; SHA256 `7ffb5aacbe9611756ba884663887b202b506f37a86f0d5ca641ae83d389e8e62` (the prefix is every line ABOVE the `| Tail receipt | Value |` header, so this row lives after the boundary and cannot invalidate itself — errata E24-E2) |
| Source-tail gap | **None, and four named limits, stated where they bite.** (1) `diagWatchEmit` fires on **guest stores only** — which is why P2's at-park **value** is reported `unobservable-as-designed` rather than inferred, and why its host-DMA residual is bounded by a `sceCdRead` count (551 total, 0 after the feed) instead of by a read. (2) The function log's `depth()` is `thread_local` and the EE runs all guest threads on one host thread, so the parent chain used to identify the constructor call site is one interleaved stack; it is corroborated by a straight-line-code argument and by E26's measured kind-2 tag, neither of which depends on the log. (3) The 16+ generated guest sources are `.gitignore`d and have no git object — but, unlike E27, E28 can compare them against **E27's committed pin values**, which are git-tracked in this repo; that is repetition in time *plus* a content-addressed record. (4) Mission 1's uniqueness argument needs the allocator's 64-byte floor; it is read out of `sub_00253AD0` rather than assumed, and the boot then measured the answer anyway by covering the whole window. |
| E28 REPORT TAIL COMPLETE | Mission 0 verified the instrument in place, the five-pin gate passed 5/5 with zero re-pinning, and both e26a captures re-hashed equal to E26's committed pins before a byte of them was mined. **Mission 1 closed without a boot:** the STRM constructor `sub_003E06D8` computes the kind-1 slot array base and then computes the data region base *from it*, so running that identity backwards through the number E26 measured (`sceCdRead buf=0xd48740`) over the allocator's 64-byte floor gives **`ctx0 = 0xd48300` uniquely** and **the kind-1 slot at `0xd486ec`** — 59 checked instruction receipts, two passes 15 min apart, same answer. The single `e28a` boot then **watched the guest write the derived value into the derived field**: `0xd486ec = 0xd48300` at `pc=0x3e0914`. **P1 CONFIRM** — the head takes `0xd48740` at the first publish and **`0xd49b14`** at the dequeue, 2 stores in the whole run, 0 after the feed; the byte count climbs to 436,876 over 28 publishes and settles at **431,840** after the dequeue, **30× E27's predicted floor**. **P2 CONFIRM on its change-half**, with the at-park value reported unobservable-as-designed and its host-DMA residual bounded to zero by 0 CD reads after the feed. **P3 CONFIRM** — one store to `0xd49b18` in the entire run and none to `0xd49b14` ever. **P4 CONFIRM** — `[MPEG:GetPicture]` ×1, `[MPEG:feedES]` ×1, both true counts against a cap of 32. Across **243** armed entries: 6,804 emissions, **0 post-park and 0 post-feed**, the last one two lines before the feed, with 24,031 log lines still to run — E26's zero, reproduced on a larger vector. The feed vector is byte-identical for the **fourth** independent time (`cde8a830…2a1c875a`, `packets=0`). E27's guest half is confirmed in every row that a store-triggered instrument can reach, and the one row it cannot reach is named rather than guessed. The capture driver was **not edited** — the watch set is data — and the one permitted change is proved to be E24's 230-entry vector carried verbatim as an exact prefix plus 13 appended entries. 1 boot, 1 lease claim, 0 fork edits/commits/pushes, 0 deletions, 0 amendments, **1 new erratum (E28-E1)**, fix gate **STOP**. |
