# ARCH1 orchestrator gate — accepted steering and backend provenance correction (2026-09-24)

Read the full independent Astra memo and commit `b886a3b0` (`git show --check` clean). Independently checked the decisive package receipt: `local/research/N8D7M12P3/source-gate.json` double-pins the actual OFF-pair APK backend to SHA-256 `84a13a80686be8d4ed17750a9399d298ea6b998b98f48d3720b6b5bc839a4645`. The earlier N8D7H source `c6135b3c…d4fb41f` is a predecessor, not that APK's backend. Hashes of the P2 and N8D7F fork files match those respective pins. Their diff adds the oracle; the actual packaged backend still calls `wait_idle` before Present mapping and still delegates SnapshotVram to the interface. The three renderer file pins used in F2/F3 do match P3's package gate. No build, replay, device action or speed result occurred in this review.

**Accept the steering:** prioritize a visible, advancing, input-responsive live Odin race before deep speed work. Permit one bounded worker-consumption fingerprint pair as a delivery check; equal digests cover only hashed fields and cannot localize a GPU fault. Stop adding delivery hashes after an equal-digest/different-output pair. Before the next Android package, pin a complete source/input manifest and explicitly mark any historical gaps. The permission-stopped F4 implementation stays unapplied; restart it from the private worktree cwd if retained. This gate corrects F2/F3 backend attribution and does not declare a graphics cause.
