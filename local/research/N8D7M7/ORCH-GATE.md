# N8D7M7 orchestrator gate — PARTIAL source design (2026-09-24)

Read the complete corrected REPORT and checker, including worker commits `65dffdd3`, `49e2d7af` and `67865212`. Reran `check.py`: 23 source-token citations PASS, zero errors. Checked the selected-copy/barrier, later circuit sample, flush/submit and backend `wait_idle`/map source sites directly. `git show --check` passes all three commits. No edit, build, replay, device action, frame or speed measurement occurred.

The source map is useful: the existing 4 MiB selected VRAM copy is recorded before the circuit shader samples the same GPU buffer, and the host maps both after GPU idle. An additional copy in the same command before submit could compare the two points without adding a new flush or wait, subject to a Mac ON/OFF nonperturbation check. A sparse selected copy with a broad circuit sample would implicate the copy/barrier/readback path; both sparse show only that the original submitted Present saw sparse content.

The initial four-way design was unsound and was corrected before this gate. Its proposed later sample called an extra `flush`/submit/wait, thereby executing work absent from the original Present; a broad later image cannot uniquely prove early copy versus delayed work. `post_tick==2050` is an EE clock observation, not a GPU write due-date. A byte-identical broad Mac staging buffer passed through an identical decoder/descriptor cannot yield a sparse census, so that proposed decode category is contradictory without separate decoder evidence. The corrected checker rejects these categories, but it verifies table consistency and source tokens, not runtime execution or nonperturbation.

**Verdict PARTIAL.** No sound one-run Odin W/O/C/D gate yet. Next priority is a nonperturbing execution witness that relates the relevant tick2050 GPU writes to the original selected-copy command. A Mac-only same-command second-copy experiment is bounded and may test copy consistency, but cannot by itself resolve write timing. Do not cut another Odin run from this design alone.
