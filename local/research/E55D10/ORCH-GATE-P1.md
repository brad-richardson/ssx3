# E55D10 Part 1 orchestrator gate — Square script approved, no screen verdict (2026-09-24)

Read the full REPORT and the E55D10/E55D9 boot-script diff; checked worker commit `55f91f88`. Reran `e55d10_boot.py --self-check` (25/25) and `check.py` (35/35), and SHA-read the prepared script as `03536b0c5b84a95d676ca2e972446827b7be11cfc3153a1a67f8eac9c4447e3f`. It preserves E55D9's output-reuse check before frame-directory creation, exact pinned-input precheck, one mini lease, recorded-PID cleanup and bounded post-target frame proof. This part made no boot, frame, card, device or speed measurement.

The route is START at guest tick636, a settled Main Menu frame around tick760, **one Square pulse** at tick820, and a post-pulse frame around tick920. No Down/Cross, seeded card change or other game input. The `square` script token is present in the pinned pad parser. Approved for one E55D10 Part 2 Mac boot using only this script SHA and a per-pane scratch-write exception limited to `~/dev/ssx3-work/E55D10/`. The orchestrator must view pre/post frames before classifying A (Options screen opened), B (readable unchanged/different screen), or OTHER (ambiguous/missing). Even A would prove a displayed Options route only, not Save or GetDir/Read reachability.

Git provenance note: `55f91f88` included the orchestrator's already-staged N8D7M10 gate and board/ledger files because the shared Git index was committed by the worker. Those files were independently gated and are recorded in this following `[orch]` commit. Future worker commits must add only their named paths. No history was rewritten or ungated work pushed.
