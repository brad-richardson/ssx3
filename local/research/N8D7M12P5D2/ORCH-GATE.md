# N8D7M12 Part 5D2 orchestrator gate — run PASS, ON/OFF comparison VOID (2026-09-24)

Read the full REPORT and worker commit `34a9c6c5` (`git show --check` passes); reran its receipt checker (14/14 A). The exact reviewed launcher SHA `223fd15e…03170` made one install and one launch with the same APK and stream pins as ON. Result records queue/parallel 862958 packets, 11499 priv, 25445 transfers, 2050 markers, 41 exact ordered replay ticks and `replay ok`. The private PPM and hashes have matching two-device/two-local SHA reads. Independently compared all 41 ON/OFF rows: priv 41/41 equal, VRAM 19/41 equal, present 25/41 equal, first VRAM/present difference tick850. Independently viewed the OFF tick2050 PPM (`5e0caca3…adbea4`): still mostly black with sparse fragments. The ON PPM SHA `39b70d67…0d41` differs. Read-only adb recheck found app PID absent, lease `LEASE_FREE N8D7M12P5D1 done`, and two restored env SHAs `176eff84…cef32d` matching the pre-run pin. Diagnostic elapsed 14.277 s is not speed.

**Run PASS; causal comparison VOID.** The three removed capture flags only act at tick2050 in the reviewed source. A tick850 difference therefore cannot be attributed to those flags. Same-stream guest priv state is stable, while GPU-side VRAM/present output varies across these runs. The viewed OFF frame is sparse too, but this pair cannot isolate whether the flags affect the final frame. Next characterize same-env run-to-run variance with a separate bounded repeat before any ON/OFF causal claim; the Mac same-binary OFF control remains separate.
