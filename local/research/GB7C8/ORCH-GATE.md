# GB7C8 orchestrator gate — B, bounded GPU comparison design (2026-09-24)

Read the full corrected REPORT and worker commits `016f4086` and `9103185f`; `git show --check` passed. Reran `check.py`: 40 PASS, 0 fail, RESULT B. Independently checked the dirty G43 `check_flush_stats` pressure path and the packet5470/marker ordering already verified at the GB7C7P2 gate. This was source/capture analysis only: no new build, replay, device action, frame, or speed number. Prior GB7C7P2 title frames were viewed at that gate.

The source paths inspected expose no nonperturbing per-packet GPU texture or destination readback: a read at packet5470 would force a flush, submission and/or wait. Internal pressure, overflow and hazard cuts can segment the render pass mid-stream; they do not give a packet-tagged readback. Thus **B is accepted as a design limit**, not proof that no possible GPU witness exists. Packet-entry byte equality is an input control only. No title GPU cause, whole-glyph result, or earlier texture producer is established.

The closest available post-packet marker is tick260, with 121 intervening packets and no interleaved non-packet records in the worker's scan; tick300/301 have 5,001/5,123 intervening packets. The existing replay harness can sample marker260. Even a marker260 mismatch cannot be assigned to packet5470 alone. A future matched CPU/paraLLEl run should first pin and rebuild the dirty G43 source, retain a same-binary ON/OFF control, check packet-input equality, and compare the selected texture/destination addresses and a surrounding crop at marker260. Source hashes identify the inspected dirty files, but the observed GB4 binary's provenance is unproved.
