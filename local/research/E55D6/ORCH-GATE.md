# E55D6 orchestrator gate — PARTIAL, card reachability design

Worker commits `5f1401a9` and corrective `db2a3455` (`Orchestrated-By: opencode`) contain only the brief and read-only report. No build, boot, card mutation, device action, fork edit or push. I read the whole corrected report, checked `git show --check`, independently reproduced the three `sceMcRead` and five `sceMcGetDir` game call-site rows from `P8/census-pad-sound.tsv` with `ee-xref`, and checked the relevant `MemoryCard.cpp` branches at fork `eac6cba`. The fork runner-dir guard remains empty.

The useful result is a mapped game-side card API cluster and two plausible trigger flows. **Neither flow is reachable by a proven menu route yet.** E55D4/D5's complete capped traces had zero GetDir/Read records, so their empty-card I26-FAST route does not exercise this tap family. The exact guest query pattern, max entries, table address, seeded save layout and trigger tick remain unknown.

The first report incorrectly said every empty-card GetDir call would write `.`/`..` and that a seeded `BASLUS-20772` entry would follow. I checked the code: `appendSpecial` requires `wildcardMatch`, `entryCount` is capped by `maxEntries`, and a guest write requires nonzero count and mapped destination. The corrective commit explicitly fixes those claims and treats the I26 empty-card note as a route setup rule, not proof of a seeded-card prompt. Because E55D3 taps all GetDir/Read exits, zero complete probe records still supports zero calls; a future call may yield only a status record.

Verdict: **PARTIAL design.** The callee map and baseline invalidation rule are useful; a one-byte card comparison is not ready. Next: map a real menu-to-save-manager ingress or capture one bounded empty-card save-menu detour to prove a GetDir/Read call. If the candidate route is silent, investigate GetInfo/HLE gates before seeding a card. A seeded layout needs its own A/A baseline because it changes the E55D4 starting manifest and because host timestamps can enter directory bytes. No speed or determinism claim follows.
