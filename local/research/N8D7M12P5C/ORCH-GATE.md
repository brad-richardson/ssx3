# N8D7M12 Part 5C orchestrator gate — A for control design (2026-09-24)

Read the full worker REPORT and commits `a4253320` + `5cf99f70`; both pass `git show --check`. Independently checked the backend source: selected and tile capture are gated on `vsyncTick == 2050`, and oracle is inside the selected block. Reran `check.py`: 16/16 A, including exact APK/stream/Mac binary pins, ON comparison and source checks. Corrected an invalid mini lease CLI in the initial unexecuted Mac plan through the worker: `claim <label>` returns a dynamic slot; the corrected plan captures and releases that slot.

**A accepted for a design only.** An OFF run with the same APK and stream, dropping exactly `PS2X_N8D7F_SELECTED_CAPTURE`, `PS2X_N8D7L_ORACLE` and `PS2X_N8D5_TILE_CAPTURE`, retains 41 replay hashes and the tick-2050 PPM. If OFF differs from ON before tick 2050, the comparison is void because those flags have no pre-2050 effect in the reviewed source. An equal OFF result would show the sparse Odin frame persists without capture instrumentation; a tick-2050-only difference would implicate that instrumentation. Neither outcome establishes the underlying graphics cause. OFF loses the selected tile census by design. The Mac same-binary OFF replay and Odin OFF launcher/run each need a separate reviewed gate; no replay, build, install or device action happened in Part 5C. The reported 13.336 s Odin ON diagnostic elapsed time remains a control receipt, not speed.
