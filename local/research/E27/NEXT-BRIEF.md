| Handoff | Required boundary |
|---|---|
| Base | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176`, triple-agreed at open **and** close, `status --short` exactly `?? ps2_log.txt`. E27 made **zero** fork source edits, commits, pushes, builds, boots and lease claims. |
| What E27 is | A **static autopsy** of the e26a capture plus the fork sources. No boot was spent, none was needed, and the one boot E26 spent turns out to have carried everything required. |
| **The walker — do not re-derive** | **`sub_003DFED0`**, `ps2xRuntime/src/runner/sub_003DFED0_0x3dfed0.cpp`, guest `0x3dfed0 - 0x3e00d4`. The tag is file line **257**, `0x3dff8c sw $v0, 0x4($s2)`, in the **delay slot** of `jal func_3E5700` at `0x3dff88` — which is the whole explanation of E26's `ra = pc + 4`. Value = `len \| (kind << 24)`; `kind` comes from **`sub_003DFE88`**, a `{mask, match, kind}` table lookup returning `-2` on no match. |
| **What the top byte means — do not re-ask** | An **occupancy bit carrying the kind**. Set by the walker on publish (`0x3dff8c`); **stripped by the consumer on dequeue** (`sub_003E12E0`, `0x3e132c` / `0x3e1330`). The walker reads it back at `0x3e0074`: a chunk that is still tagged is still queued, so the walker stubs it out and stops. It is the producer/consumer handshake of a single-buffer ring, stored in the chunk headers. |
| **What the walker does after tagging — do not re-ask** | Takes the stream lock, **publishes** into the per-kind slot at `ctx0->[0x24] + (kind-1)*16` (`+8` bytes, `+0xC` head), **advances the cursor** `ctx0->[0x54] += len`, unlocks, loops. **It never feeds.** Its only caller is `sub_003E0170` at `0x3e01ec` — the **read-completion** path — so the walker is driven by **data arrival**, never by consumption. |
| **The feed path, end to end — do not re-derive** | `sub_003DFED0` publish → `sub_003E12E0` dequeue (strips the tag, advances the head) → `sub_003AEAD0` descriptor `{+0 header, +4 header+8, +8 total}` → one-slot pushback at `0x5487c0` → `sub_003B06B0` pop → `sub_003B0B40` memcpy + zero-pad → `sceMpegAddBs`. Every step has a `[diag:watch]` receipt in e26a; see the REPORT row. |
| **The 5,040 is now derived, not just measured** | `(5036 + 7) & ~0xF = 5040` at `0x3b0b90`/`0x3b0b98`; `5036 - 8 = 5028` at `0x3b0b94`; the zero-fill loop at `0x3b0bb8-0x3b0bd0` accounts for the 12 pad bytes. E24's and E26's byte measurements and the source agree exactly. |
| **`sceMpegGetPicture` has exactly ONE call site in the image** | `sub_003B0FB8` at `0x3b1020`, return `0x3b1028` — byte-exactly thread 1's park `pc`/`ra`. `sceMpegAddBs` likewise has exactly one, `sub_003B0B40` at `0x3b0c2c`. Enumerated over all 9,454 generated sources. |
| **THE CORRECTION TO CARRY: the guest call site is NOT one-shot** | It sits inside **`sub_003B1050`** (the second guest function in the same generated file), a **counter-bounded re-ask loop**: `0x3b108c` guard → `0x3b109c` GetPicture → `0x3b10a4`/`0x3b10a8` counter test → **`0x3b10ac bnez $v1 → 0x3b1070`**. Its trigger is **the `jal` at `0x3b1020` returning, and nothing else.** |
| **THE CORRECTION TO CARRY: the chunk is already the queue head** | `sub_003E12E0` advanced the kind-1 head to **`0xd49b14`** when it dequeued chunk 0 (`0x3e1360` → `0x3e1384` walk → `0x3e13c8 sw $a2, 0xC($t1)`). The start code E26 located at `0xd49b1c` is the **payload of the chunk the head already points at**. The guest is **one `func_3E12E0` call** away — not one chunk short of finding it, one call short of asking for it. |
| **THE CORRECTION TO CARRY: the mutual-wait reading loses a leg** | Legs 1 and 2 hold and are now named in code. The leg that breaks is the unstated fourth one, "and so each side is waiting for the other". The **host** holds the token: `g_mpeg_stub_state.nonStreamDeliveries[(mpegAddr << 32) \| 1]` is a `weak_ptr` whose `shared_ptr` is captured by the `waitExternal` resume lambda (`MPEG.cpp:2514`) and **moved into the parked thread's own `EeWaitState`** (`EeScheduler.cpp:2120`). The suppressing token is owned by the wait it suppresses. **Not a mutual wait — one party holding the key to its own door.** |
| **A guest path nobody had named** | `sub_003B0B40` `0x3b0b7c → 0x3b0bdc`: if the pop yields nothing, the guest feeds 16 bytes of `0xB7010000` — little-endian `00 00 01 B7`, the MPEG-2 **`sequence_end_code`**. The guest owns its own stream terminator. Both of its exits from this state are live code; neither can run. |
| **Do not re-ask (settled, with counts)** | Does thread 1 do **anything** after the feed? **No** — 151,020 further function-log lines, **16** distinct symbols, 4,195 identical timer iterations, not one MPEG/stream/chunk/CD symbol. Does the walker/classifier/feeder/pop/GetPicture site run again? **No**, each with an exact count. Does the refill kick `sub_003DCFC0` fire? **Zero** enters in the whole 3.6 M-line log. |
| **E26's sema-36 link, downgraded** | E26 tabled that `sub_003C1980` (the `WaitSema(36)` owner) sits on the MPEG delivery path "immediately after `AddBs`" and declined to upgrade it. E27 measured it: `sub_003C1980` is entered **4,195 times after the feed**, once per timer iteration. Its position next to `AddBs` in the park trace is the **first** iteration of that loop. **The shared-function observation is weaker than E26 left it.** Do not build on it. |
| **The one thing a boot could still add** | The **host** side, and it needs no new guest instrumentation. Arm the kind-1 queue slot (`ctx0->[0x24]+0`, count at `+8`, head at `+0xC`) and `0xd49b14`/`0xd49b18`; log from `MPEG.cpp` every `getMpegPicture` entry with its `requestInput` flag and the `pending.lock()` result, and every `completeExternalWait(kMpegPictureWaitType, …)` with its caller. **E27's falsifiable prediction:** head = `0xd49b14`, count ≥ 14,364, unchanged from the feed onward; `0xd49b18` still `0x0100381c` at the park; `getMpegPicture` entered exactly twice (`true` then `false`) with `pending.lock()` non-null on the second; `completeExternalWait` for the picture wait called **zero** times after the feed. One boot settles all four. |
| **Fix authority** | **None inferred.** Fix gate **STOP**, as briefed. Four edges, not one, and the load-bearing one is a host lifetime question adjacent to the preserved E18 ABI. E27 ran no boot, so it has no fail-before, no regression and no suite run; a fix needs all three plus exactly one edge. |
| Candidates still KILLED | C2a, C4 (E23); C1, C3, C5–C9 (E22). E27 re-proposes none and re-opens none. |
| **Standing SSD rule, how E27 satisfied it** | Every quoted file hashed **twice, 455 s apart**, in separate invocations: 19/19 equal. `MPEG.cpp` additionally equal to its **git object** (content-addressed, a different read path). Both e26a logs equal to **E26's committed pins**. **Named gap:** `ps2xRuntime/src/runner` is `.gitignore`d (`.gitignore:21`), so the 16 generated sources have **no** content-addressed copy anywhere — repetition in time is the only check available for them, and every Mission 1 / 2(a) disassembly rests on that alone. |
| **One receipt discrepancy for the orchestrator** | E26's byte-cap table gives the boot log as 13,315,314 B and the function log as 127,058,902 B; the pinned files measure **13,355,448** and **127,074,085**, with **SHAs equal to E26's pins**. E26's cap accounting evidently sampled sizes at a different moment from the pinning. Nothing in either lane's result depends on it; recorded so it is not later mistaken for link corruption. |
| Errata carried | E21-E1, E23-E1, E23-E2, E24-E1, E24-E2, E24-E3 unchanged. **E27 adds none.** |
| Probe count | E27 spent **0/0** title boots and **0/0** lease claims. A later brief needs its own fresh admission, T13, lease and caps. |
| Source receipts | `REPORT.md`, `fork-gate.json`, `fork-gate-close.json`, `mission-1-walker.json`, `mission-2-feedonce.json`, `mission-3-reask.json`, `logmine.json`, `logmine2.json`, `tail-after-feed.json`, `pins-pass-1.json`, `pins-pass-2.json`, `fix-gate.json`, `final-audit.json`, `tail-receipt.json`. |

| E27 NEXT-BRIEF TAIL COMPLETE | The walker is `sub_003DFED0` and its tag is the delay-slot store at `0x3dff8c`; the top byte is an occupancy bit carrying the chunk kind, set on publish and **stripped on dequeue**, so it is the ring's producer/consumer handshake. After tagging, the walker publishes into a per-kind queue and advances its cursor — it never feeds, and its only caller is the **read-completion** path, so consumption can never re-enter it. `sceMpegGetPicture` has exactly one call site in all 9,454 sources, and that call site sits inside a **counter-bounded re-ask loop** whose only trigger is the call returning. The chunk the guest needs is **already the head of the kind-1 queue at `0xd49b14`**, whose payload at `0xd49b1c` is the start code E26 found. After the single feed, thread 1 executes **nothing**: 151,020 log lines, 16 symbols, one timer cycle. The one-shot is **host-side** — the `nonStreamDeliveries` `weak_ptr` whose `shared_ptr` is owned by the parked thread's own wait continuation — so E26's mutual-wait reading loses its fourth, unstated leg. 0 boots, 0 lease claims, 0 fork edits/commits/pushes, 0 deletions, 0 errata added, fix gate **STOP**. |
|---|---|
