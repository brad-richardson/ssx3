# N8D7M12 Part 5A orchestrator gate — A for replay script (2026-09-24)

Read the full REPORT, launcher and checker, worker commits `92fe35f9` and wall-drain correction `f8330b3f` (`Orchestrated-By: opencode`). Both commits pass `git show --check`. Reran `check.py --self-check` (24/24 A) and `py_compile`; the final launcher SHA-256 is `287140bf370acfb105f1b64347a635822300699150e517e6b912340ae031334e`. Independently checked the shared core's `vq-002050.ppm` filename and summary/frame log formats, the early Android replay branch, the ported N8D7M6 numeric census parser, and the launch/cleanup control flow. The initial draft checked process liveness before a complete receipt, used line presence for the census, lacked verified env restoration, and could extend its log drain beyond the wall deadline; the worker corrected those before this gate.

**A accepted for script preparation only.** Release the exact SHA above for one Odin diagnostic replay. The launcher requires a matching SHA argument and refuses a second run when its private result/driver receipt exists. It rechecks the APK/stream and device state, installs, holds the lease, caps the active replay at 600 s with a 180 s progress limit, force-stops, restores the previous env with matching hashes, and releases the lease. The wall cap governs active replay and post-exit drain; bounded collection/cleanup follows. No device call or runtime result occurred in this part. The replay result stays provisional until the orchestrator reads the full receipts and views the frame; this single ON run does not establish default-off behavior, a graphics root cause or speed.
