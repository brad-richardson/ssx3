# N8D7M12 Part 6M6 orchestrator gate — stopped at OpenCode SSH policy (2026-09-24)

Read the complete REPORT, denial receipt, four-row result, preflight checker output and commit `fc99c365` (`git show --check` clean). OpenCode denied the first `ssh bytesize` call with `permission=bash`, pattern `ssh *`, action `deny`, despite the scoped pane environment request. The worker obeyed its stop rule: no transfer, WSL root, build, APK or device action. Mac P6M5 checker24/24, aggregate and disk budget were rechecked. The result's C is an access stop, not a source or build failure.

**No package verdict.** Reroute the same pinned build brief to a worker kind with authorized SSH access; retain the P6M5 stage and manifest. Do not weaken global OpenCode permissions or infer anything about compiled inputs from this stop.
