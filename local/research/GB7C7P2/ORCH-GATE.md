# GB7C7P2 orchestrator gate — PASS/A for one CPU sampled pixel (2026-09-24)

Read the final REPORT, full tap row, checker and source diff; reran `check.py` (`RESULT A`, 31 PASS checks). Checked worker commits `f3ebb601` and private GB4 fork `f54adff` with `git show --check`; runner-dir diff versus upstream `14b1e5cb` is empty. The fork and ssx3 trees are clean and both mini P-lane slots are free. The committed 7,162-byte tap receipt SHA matches private `tap3.tsv` (`ce442a28…e4ec3`). No device action or speed claim.

The direct CPU replay at tick259/path3/packet5470/batch10 samples CT32 tap0 `(343,378)` at swizzled `0x000bae74`; the **actual read word** is `63353341` and `SampleTexture` returns it (fx=fy=0). The same sprite changes raw FBP112 storage at `(342,377)`, `0x0019ae38`, from `dc302f3b` to `dc353341`. CT24 logical reads mask the preserved alpha byte, so the trace also shows `00302f3b→00353341`; source and the GB7C5 raw watch independently corroborate the raw pair. This accepts category A for that single executed CPU sample and write. It does not identify the earlier upload/draw that placed the texture word, establish a whole glyph, or explain paraLLEl damage.

Independently parsed the capture order: marker259 at offset 6673264 occurs before packet5470 at offset 6673427. Thus the viewed tick259 title PPM precedes this write; its different pixel is not evidence of a later overwrite. Viewed tick259 and tick301 title frames. Final ON and OFF have equal common frame hashes (7 rows) and byte-equal PPMs at ticks259/300/301, but **they used adjacent builds** (final ON build-5, OFF build-4); a same-binary nonperturbation pair remains unverified. The checker label `on_binaries_equal` describes equality of ON hash rows from build-2/build-5, not binary identity.

Budget deviation disclosed and accepted for this bounded result: five incremental build invocations (two compile failures repaired, three successes), three ON replays and one OFF replay; the second repair cycle followed the orchestrator's raw-storage correction, and the third ON restored missing tap columns. All scratch logs/traces retained, final suite and replays 556/556, trace 18 rows/7,368 B uncapped, scratch 6.3 MiB. No further run is needed for this one-pixel fact.

Next: design a matched CPU/paraLLEl comparison at this exact texture address and destination on one stream, with an execution/ordering witness before any GPU cause claim. Keep the earlier texture-word producer and whole-glyph questions separate. Prioritize the Odin selected-VRAM execution-witness design for the stock-race milestone.
