# GB7C7 orchestrator gate — OTHER, permission stop (2026-09-24)

Read the full REPORT and `check.py`, and reran it: capture/path pins PASS, `trace_present` FAIL, `RESULT OTHER`. `git show --check 18841b9b` passed. The worker's first attempted edit in the private GB4 fork was denied by OpenCode's `../*` edit rule. It stopped without source changes, build, replay, device use or lease. The fork remains clean at `8966b0be`; no sampled texture word was observed.

The read-only source table is a useful implementation design, not a graphics result. Resume the same bounded tap with a per-pane permission exception restricted to the GB4 fork and GB7C7 scratch. Verify the exception before editing. Then use the existing A/B/OTHER and ON/OFF controls; gate the actual trace separately. No sampled-word or glyph conclusion from this stop.
