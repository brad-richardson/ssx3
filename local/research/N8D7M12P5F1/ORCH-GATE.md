# N8D7M12 Part 5F1 orchestrator gate — A for scoped hash/sync source map (2026-09-24)

Read the full REPORT and worker commits `28718aef` + correction `11c6c191` (`git show --check` passes); reran checker (31/31 A). Independently checked the replay core: each marker drains the GS queue; sampled VRAM hash reads a 4 MiB snapshot before `presentForDiagnostics`; the present hash reads the returned frame, and the output file copies those rows. The parallel backend's public `Flush`/`Sync` methods are no-ops, while its `Present` path submits a readback and waits idle; `SnapshotVram` calls renderer `flush` and `map_vram_read` without its own backend wait. The correction narrows two overclaims: equal sampled `priv` hashes prove only equal sampled CPU GS-register mirrors, not packet ordering; the VRAM image's writer coverage is unproved. Verified fork `a608ed1` clean and parallel-gs tree `3a66c19` dirty as disclosed.

**A accepted for the scoped source map, not a root-cause or binary-level sync verdict.** The three capture flags only affect tick2050 in the reviewed fork, so they cannot explain tick850 differences. The same-settings Odin repeat now confirms GPU-side output variance with first difference tick850; this audit identifies readback/synchronization and state-initialization paths to test, but the dirty parallel-gs source is not the pinned APK renderer binary. Before a source-level fix, tie exact packaged renderer bytes to source or use a bounded observable that separates replay ordering from GPU readback/state behavior. No run or speed result occurred in this audit.
