# N8D7M2 orchestrator gate — PASS, same-run selected-input oracle

Worker commit `9804ebb8` (`Orchestrated-By: opencode`) used the reviewed launcher SHA `6b5976ceca948f32ce45c7c2fb4619d72b7d946180a4cd1849eff562c09677ef`. I read the whole report, launcher/diff, result, vector excerpt and log, independently reran the 45/45 parser self-check and syntax check, and viewed the committed 512×448 PNG (`1a6a89e9…741c54e3`). It is mostly black with sparse pale fragments and HUD elements; no rider identification follows from this frame. `git show --check` flags only blank context lines in the raw `launcher.diff` receipt.

| Gate check | Orchestrator result |
| --- | --- |
| Scope and device safety | Exactly one install and one launch of pinned N8D7M1 APK `e077bef8…758a1`; packaged runner/Turnip/HAL and installed APK/ELF/ISO matched two SHA reads. Keyguard false, battery 100%/full, storage >1 GiB before install and launch. Empty mc0. App PID 24461 force-stopped and absent; Odin lease released. No build, source change, second run or speed claim |
| Same frame | tick2050, FBP112, PMODE ff21, status2; selected and oracle metadata agree on all 11 shared fields. Circuit1, merged and final controls 128; old-final control 128 PASS. Tick2050 frontend PNG and metadata had two matching device and two local SHA reads |
| Independent vector audit | Reparsed each full same-PID logcat vector independently: oracle, G43 input, circuit and GPU stage are each 448 words, 2,058 occupied and 15 active tiles, packed SHA `bd33829e…19d5cd`, **identical 448/448**. Final sampled/raw are each 896 words, 4,355 occupied and 29 active, packed SHA `887f9998…e5f73`, identical 896/896. Logged equality counts match recomputation |
| Controls | Eight expected addresses in order; **0/8 values** match the Mac N8D7L literal set. This is a separate cross-run observation; Mac and Odin GS streams are not proved identical, so it is not a category failure or device fault verdict |
| Bounds | First complete receipt/frame stop at tick2053, 116.06 s diagnostic wall; 16 MiB log cap not hit; no probe/pipeline errors. Diagnostic wall is not speed |

Verdict: **PASS, predeclared category A** for this one Odin frame. The independent fork-table decoder and G43 decoder agree on the selected 448-tile input, which is already sparse on this run. This rules out a disagreement between those decoders as the explanation for this frame's sparse G43 input. It does **not** prove why the selected raw-VRAM snapshot is sparse: an earlier GS writer, selection/layout, or stream difference remains possible. The prior N8D7I boot had 35 active selected tiles; that is a different free-running boot, not a controlled same-stream comparison. Next: design a bounded same-run raw-VRAM/selection discriminator using packet/source evidence before another device run.
