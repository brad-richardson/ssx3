# N8D7M12 Part 5F4P2 orchestrator gate — host diagnostic PASS, off-path timing blocker (2026-09-24)

Read full REPORT, exact four-file private fork diff and `6799681` (`git show --check` clean). Copied the bounded worker receipts here and reran checker escalated in its private worktree: 36/36 PASS. Independently read suite tail 586/586/0, parsed exactly 41 ordered `GB4_PKTSEQ` and 41 `GB4_REPLAY` ticks 50…2050 (command counts 251→1,737,496 strictly increasing), compared Mac `parallel.hashes` and tick2050 PPM byte-for-byte to the prior pinned ON control, and verified their SHA-256s `94b433df…10c290` and `9490484c…14fce3e`. Fork runner diff against `14b1e5cb` is empty; source commit is private and unpushed. Lease released, no runner remains. No Android package, Odin action, causal GPU result or speed number.

**A for host implementation/validation; hold Android packaging.** The worker did implement a default-off env gate, but `GS::executeQueuedCommand` calls `noteConsumedCommand` unconditionally and that function takes `m_pktSeqMutex` before checking the gate. Thus the nominally off diagnostic still locks once per consumed command, a material timing perturbation in the hot path. Fix with a race-safe fast enabled guard before acquiring the mutex, then run one bounded host suite and default-off replay control before using it for an Odin A/B. Do not reinterpret the prior output equality as a GPU cause. Digest equality later covers only hashed fields and excludes opaque PrivWrite contents and GPU execution.
