# E14 experiment contract — absorb the five named I-lane entries

Start 2026-09-21 11:51:36 UTC; eight-hour deadline 19:51:36 UTC.
Latest pull is current. Scope is the five rows in E13's independent I17
cross-check: 14f2a8, 156750, 243a80, 395730 and 3a0158. MPEG is queued for
E15 and is not part of this experiment. Standing no-regen resumes at close.

| Hypothesis / observable | Alternative / stop |
|---|---|
| E13 checkpoint is unchanged | Match every generated name/hash, runner/registry, prior actual-binding fixture and suite before mutation. A mismatch is tabled before proceeding |
| Five exact bindings are absent | Relink against the actual baseline objects; one absent check and one failing expected-present check for each named PC |
| Row bytes and bounds agree | Check I11–I15 emitted receipts, baseline owner slices and raw ELF. Exclude any disagreement with its reason; fewer than three verified rows stops the absorb |
| Admission permits a bounded generation | Fresh sample persisted after fail-before and before CSV edit; immediate sample before generator spawn. Never lower a guard |
| One map change restores the verified entries | Compute expected CSV hash before editing; sorted inserts only, owners and previous entries unchanged. One fork commit, no generated staging |
| Prior behavior remains intact | New actual-binding presence plus leaf 24, consumer 3, predicate 14, query 4, DROP and suite. Failure stops before boot |
| One boot confirms joins | Dynamic MC/UI, query/predicate/leaf, S/copy and field-processed Present; table first wall or any further advance without repairing it |
| Outcome shape | (i) absorb and required checks complete; (ii) absorb complete with remaining guest wall; (iii) verification/admission/observation failure with exact repair recipe |

| Resource / ownership | Declared bound |
|---|---|
| Generation admission | 1 GiB reservation + 2 GiB free floor + 256 MiB guard = 3,489,660,928 B. Reclamation stop target adds 512 MiB = 4,026,531,840 B |
| Reclamation | Retired trees only, oldest first, per-tree lane/ownership/no-handle proofs and recheck; protect `/tmp/p1-link`, DerivedData and live lanes |
| APFS scratch | Owned `/tmp/ssx3-e14-absorb-codegen`, cleaned after installation hashes; no unrelated internal artifacts |
| Generation | 1 GiB logical, 1.25 GiB allocated; 128 MiB output guard; 2 GiB free floor plus 256 MiB guard; 1,200 s wall with 15 s reserve; 16 MiB log with 1 MiB reserve; 0.25 s polling |
| Installation | Changed bytes only, no metadata; proven-created sidecars only; 24 GiB allocated-growth cap with 256 MiB reserve and 2 GiB SSD floor |
| Build / fixture link | `-j4`; SSD-owned outputs and TMPDIR; 8 GiB temporary allocation with 256 MiB guard; 2 GiB SSD and 1 GiB internal floor; 1,800 s build / 1,200 s link with 15 s reserve; 16 MiB log with 1 MiB reserve |
| Single boot | At most one, planned 90 s with 15 s termination reserve, below 600 s; 1,000,000 syscall lines; atomic shared lease. If occupied, table and stop without waiting |
| Capture bytes | Aggregate 1.5 GiB logical/allocated; boot 256 MiB, syscalls 96 MiB, functions 1 GiB; E4 12/64 MiB, park 32/128 MiB, frames 32/64 MiB, E7 8/32 MiB logical/allocated; 8 MiB allocation reserve and 2 GiB SSD floor |
| Observation limits | Existing E7 sink and E4 taps; preserve raw gaps and source quiescence. No frame-progression claim without a packet/source/display/Present content join |
| Retention / commits | Compressed canonical captures, full tails, standalone E14 evidence. Named fork files only, pushed to fork remote; `[E14]` evidence with `Orchestrated-By: Muse Code`, no main push |

No adb, MPEG change, extra map row, new HLE or runtime repair. Release the
lease immediately after the capture process exits and analyze lease-free.

**E14 CONTRACT TAIL COMPLETE — recorded before checkpoint tests, fixture
relink, admission, CSV mutation, generation, build or boot.**
