# E10 experiment contract

Start 2026-09-21 06:48:40 UTC; deadline 12:48:40 UTC. Fork baseline
be0c9eeaa337d9685e9536fa4f66c5ce5029aad8. E9 REPORT and NEXT-BRIEF read in full.
The E10 brief renews regeneration only for DROP completion and the 0x2c5140
entry repair. Standing no-regen resumes at E10 closure.

| Phase / hypothesis | Observable | Alternative / stop |
|---|---|---|
| DROP-only generation on APFS avoids ExFAT allocation amplification and preserves guest handler 0x426230. | Normal generator exit; full registry/header/body manifests; changed-bytes-only installation with leftover disposition; -j4 rebuild; full suite; actual generated handler returns to supplied RA with unchanged SP and s0. Query remains absent. | Preflight failure → (iii), exact failure and repair recipe, stop before query mutation. |
| One sanctioned map row restores exact entry 0x2c5140. | Fail-before actual binding; after regeneration, query truth table (0,0)→0, (1,0)→1, (0,1)→1, (1,1)→1; supplied RA, SP, 128-bit callee-saves, whole RAM preserved; zero GetInfo calls; DROP and suite rechecks. | Entry failure → (iii), stop; no guest-body replacement or additional gap fix. |
| Restored query allows the first intended guest request. | One guarded boot: dynamic a0/vtable, constructor and UI-pointer guards, query v0 at 0x2c44b0, state-3/GetInfo path, UI pending, singleton/S fields and packet→GS→D→field-Present join. | (i) demonstrated advance; (ii) first remaining wall, exact next recipe; (iii) failed repair/observation, exact repair recipe only. |

| Resource / ownership | Upfront bounds |
|---|---|
| APFS scratch | Owned fresh /tmp/ssx3-e9-retry-codegen, reused only across these two generations, removed after retained manifests/copies are verified. |
| APFS admission | At least 1 GiB logical reservation + 2 GiB free-space floor + 256 MiB guard; fresh df and disk_usage recorded. Stop if unmet. |
| Each generation | 1 GiB logical output; 1.25 GiB allocated output; 1200 s wall; 16 MiB log. Stop at 128 MiB output headroom, 2.25 GiB remaining internal space, 15 s wall reserve, or 1 MiB log headroom; poll 0.25 s. |
| Mirror installation | Only generated byte changes; no metadata copies; no generated sources staged. Budget 24 GiB allocated destination growth with 256 MiB guard; ≥2 GiB SSD free floor. Pin names and disposition leftovers before mutation. |
| Builds | Existing /tmp/p1-link/runtime; -j4. Preserve exact commands/logs and fresh binary hashes; internal scratch exception does not authorize unrelated large artifacts. |
| Boots | One planned, max two, ≤600 s each; exact wall/progress/logical+allocated-byte caps before each claim. Shared /tmp/ssx3-p-lane-lease, never parallel; SIGTERM at first binding cap and immediate release before analysis. |
| Lease waits | If occupied, log to P1/run/e10-waits.log and retry no sooner than 300 s. |
| Capture budget | Initial aggregate ceiling 1.5 GiB; function trace 1 GiB, boot log 256 MiB; ≤1,000,000 syscall lines. Per-directory allocated caps and headroom fixed before boot. |
| Retention | Closed raws compressed once; manifests and proving receipts retained in standalone E10 evidence. COPYFILE_DISABLE=1 on every SSD step. |

No adb, stacked fixes, new HLE, raster/Present rework, or storage/SIF/CD/input
promotion without an actual guest request/completion dependency. Fork commits
go only to the fork remote; evidence is committed with [E10] and
Orchestrated-By: Muse Code, never pushed from ssx3.

Tail receipt: contract recorded before E10 regeneration, installation or boot.
