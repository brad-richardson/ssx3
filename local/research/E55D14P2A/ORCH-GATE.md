# E55D14 Part 2A orchestrator gate — A for one path-run script (2026-09-24)

Read the full REPORT, script/checker and worker commit `079b57a5` (`Orchestrated-By: opencode`); `git show --check` passes. Reran `e55d14_boot.py --self-check` (38/38) and `check.py` (57/57), then independently diffed the script against E55D12's proved route. Input pulses, frame targets, empty-card input, PID/lease cleanup, probe/log/frame caps and no-reuse guard are unchanged. The changes are the new output lane, fork/runner pins, a precheck that allows only the private `.work/` build tree as untracked, and frame-proof grace reduced to 100 s so the 500 s active cap plus grace is at most 600 s. Script SHA-256 is `fa9444a9f80c89dfa2b1e8911d8870ddca87397db083e318f678ea4dc9285694`. The fork remains at `80777cb` with only `.work/` untracked; the new runner matches `d8fa114d…ef04`. ISO/ELF/codegen are rehashed twice by the script before any boot; they were carried from E55D12 rather than freshly read in this preparation part.

**A accepted for script preparation only.** Release that exact SHA for one Mac diagnostic boot in a new E55D14P2 S1 lane after a final disk/lease preflight. Require viewed pre/post Load game frames, nine pad pulses, five status GetDir records and five `getdirpath` siblings paired by per-family ordinal/tick/port/slot/max. The sibling advances shared global sequence, which is not a comparison key. No boot, requested path, save bytes, determinism or speed result exists yet. A seeded run remains unreleased.
