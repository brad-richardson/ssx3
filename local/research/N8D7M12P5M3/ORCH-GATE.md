# N8D7M12 Part 5M3 orchestrator gate — A for corrected Mac OFF driver preparation (2026-09-24)

Read the full REPORT and worker commit `fdb5270c` (`git show --check` clean). Reran `check.py --self-check`: 39/39 A. Independently hashed `mac_off.py` to `1056304d3f2d559936f56ae189c419813581bb46f7cf35ae26d0dc7ad12d6738`, checked the pinned Part 1 fork HEAD `24801bc…5e8` and header SHA `b8de8745…970cd`, and read the fixture code. Its only intended runtime change from Part 5M1 is a private-cwd `ps2xRecomp` symlink to that pinned tree, verified before the lease and binary run. The checker compares the normalized script bodies and OFF env, and tests the relative header candidate without running the binary. The reviewed driver still requires its exact SHA to run, uses one dynamic mini lease, own-PID stop, 300 s wall and 16/64 MiB caps.

**A accepted for preparation, not a replay or graphics verdict.** Release one corrected Mac OFF run under a separate brief. Its acceptance must independently require binary exit 0, suite 585/585, replay markers2050, exact 41 ordered rows and PPM; matching artifacts alone do not override a failed suite. No speed number from diagnostic wall.
