| Handoff | Required boundary |
|---|---|
| Base | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176` unchanged and remote-agreed at open and close. E23 made zero fork source edits, zero fork commits and zero pushes. |
| Completed | Checkpoint 9457/1725/458 unloaded + 458 observer-LOADED/E15/E16/prior/R1–R6; the complete-feed demand cadence mined on the ACTUAL title wrappers at four sweep points; trigger point and loop bound named by comparison; ONE guarded title boot (`e23a`, wall-bound, rc 0) spent on the observation that decides the transfer; fix gate stopped with X moved upstream. |
| **The reference, and what it says** | On the path where the bytes SUFFICE the type-1 producer fires **exactly once per `GetPicture`** — the same as the stall. Measured at 60 B (STALLED), 120 B (STALLED), 180 B (SERVED), 240 B (SERVED): `producerFirings=1`, `AddBsCalls=1`, `selections=1`, `invocations=1` in all four. **There is no multi-round cadence anywhere in this runtime to calibrate a re-ask against.** Do not go looking for one in-lane again. |
| Candidates now KILLED (do not re-propose) | **C2a** (`MPEG.cpp:1757-1760` + `:2484`) and **C4** (`:2496-2513`). Both require bytes the guest does not have: `e23a` recorded 29 guest writes to the producer/source windows, **all before the park and none in the 66 s after it**, while a guest thread stayed scheduled ~300x per 5 s and DMA/GIF stayed frozen. A re-ask would fetch nothing. Implementing either adds a demand policy that provably cannot produce a picture. |
| Candidates already closed by E22 | C1 (AddBs-wake broadening), C3, C5, C6, C7, C8, C9. Still closed; do not re-propose. |
| **C1's elimination strengthened** | On the SERVED branch `sceMpegAddBs` DOES change the frame count, so `completeExternalWait` **is** raised — and matches **zero** waiters (`mpeg-complete matched=0`), because the caller is still inside its own callback. The wake edge is inert **even when raised on the path that works**. The `onComplete` retry at `:2484` is the only mechanism that has ever delivered a picture in this runtime. |
| Structural facts worth carrying | Three loops, none a demand rule: callback dispatch bounded by `nextCallback < delivery->callbacks.size()` (registration-list exhaustion, constant 1 everywhere); the host parser feed bounded by offered-buffer exhaustion **inside one AddBs** (the 240 B feed's four parse calls are one offering being drained, not four demands); the picture wait bounded by `decodedFrames.empty()`. **No round counter exists.** |
| C2a vs C4, settled | C4's site is reached **only** on branches that stall; no run that served a picture ever visited it. C2a's site runs on both. If a later lane ever revisits the demand edge, C2a is the site with precedent — but see KILLED above. |
| **Named missing signal X (moved upstream)** | The missing bytes are **not obtainable at the demand edge**. X is now: **why does the guest hand over exactly 5,040 B and then leave all six threads waiting — thread 1 `waitReason=6` at `pc=0x3b1028`, threads 2-6 `waitReason=2` (semaphores 26/30/32/36) at `pc=0x423de8` — with `dma=4542 gif=210` frozen across every post-park sample?** The next dependency is the producer's own source and whatever the other five threads are waiting on, not the host's demand policy. |
| Exact next measurement edge | Two cheap, well-posed follow-ups, in order: (1) **close E23's named residual** — add `0x548880`-`0x5488ff` (the successor descriptor node the head advanced to at boot-log line 10,168) and the `0xd48748+` data region to `PS2X_DIAG_WATCH`, and attribute thread 4's ~300 wakes per 5 s, which currently touch nothing watched; (2) **follow the semaphore wait** — threads 2-6 are all blocked on `waitId` 26/30/32/36 at `pc=0x423de8`; identify who is meant to signal them and why nothing does. If (2) resolves, the MPEG stall is a symptom, not the defect. |
| Instrument | `parser/e21-parser-observer.dylib` (SHA256 `e4d88fdc…b38e6be`, 52,472 B) REUSED by copy + re-sha, never rebuilt. It owns its interface, so `PS2X_E21_PARSER_DIR`, `PS2X_E21_PROOF` and `# E21 PARSER CLOSURE` stay E21-spelled in any later lane. Rebuilding it re-opens E19's forwarding question and needs its own isolation proof. |
| New reusable asset | `P1/e23-fixtures/complete/binding-test` (165,202,160 B, SHA256 `3136f3ca…fd4279a2`), linked from the current unmodified runner object set. Mode `complete <bytes>` drives the R3 authored payload through the actual title wrappers and reports the cadence; it reproduces every carried fixture receipt unchanged (prior, E15 no-input/input, all six closure modes). Its sources are in `local/research/E23/`. |
| **Relink cost warning** | That link cost **405 s and ~2.93 GB of internal volume that was not returned** — not swap, not a snapshot, and not a findable file. The internal volume was already at 98% before E23 opened. A lane that needs another relink must admit against that cost *before* linking, or link on the SSD lane. E23 declared amendment A1 (internal reservation 3 GiB → 512 MiB, **floor and guard untouched**) rather than bypass its own admission gate; see `AMENDMENT-A1.md`. |
| **Rename hazard (errata E23-E1)** | A mechanical `e22`→`e23` rename rewrote the substring `e22` **inside a hex digest** (`ELF_SHA`), and the normalized-diff proof was blind to it because it maps both tokens to the same placeholder. It was caught by the capture script's own preflight file-hash gate **before the atomic lease claim, so no boot was spent**. Any later lane doing this rename must use the hex-safe proof (`rename-proof-hexsafe.json`) and the `hex-audit.json` check, which mask long hex runs before normalizing. |
| Decode-threshold scope limit | Unchanged from E22: Q1's `GOP offset + 5,461` was measured on `authored-black.m2v`. **Do not import it as the real-stream deficit.** On the R3 authored stream the serve threshold is 180 B (frames = packets − 1, packets = pictures fully terminated by a following start code); that number is the authored stream's, not the title's. |
| No target fix authority inferred | E23 killed candidates; it did not isolate one edge. A future fix still needs exactly one demonstrated edge plus its own fail-before and full regression. No decoder/EOF/policy invention, no fabricated frames, no second fix. |
| ABI carry | Caller-owned synchronous delivery, word0-only cbData, v0 discarded, valid-no-input waits, no guest invocation under the MPEG mutex, delete/reset cancellation, stream behavior unchanged. |
| Protected state | `/tmp/p1-link`, `/tmp/e17-map-link`, `/tmp/e18-mpeg-link`, DerivedData; all 1,725 hashes equal at close. **No reclaim authorized and none performed.** E23's `P1/e23-*` fixtures/tooltmp/boot artifacts retained; E21's and E22's retained too. |
| Probe count | E23 spent 1/1 title boots and 1/1 lease claims (wall-bound, rc 0, clean release, post-release `pgrep` rc 1). A later brief requires its own fresh admission, T13, lease and caps. |
| Errata carried | E21-E1 (stale harness size/sha in E21's REPORT) carried forward unchanged. E23 adds E23-E1, repaired and audited in-lane. |
| Source receipts | `REPORT.md`, `CADENCE.md`, `BOOT-DESIGN.md`, `AMENDMENT-A1.md`, `cadence.json`, `suite-parse-episodes.json`, `e23a-boot.json`, `e23a-watch-liveness.json`, `e23a-postpark-liveness.json`, `e23a-result.json`, `e23a-config.json`, `observer-regression.json`, `hex-audit.json`, `rename-proof-hexsafe.json`, `tooling-diff.md`, `fix-gate.json`, `final-audit.json`, `observed/`. |

| E23 NEXT-BRIEF TAIL COMPLETE | The complete-feed path fires the producer once, exactly like the stall, so no in-lane reference can calibrate a re-ask; the wake edge is inert even when raised; and the guest never refills its source while parked, which kills C2a and C4 outright and moves X upstream to the producer's source and the five semaphore waits. |
|---|---|
