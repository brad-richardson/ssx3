# E55D11 Part 1 orchestrator gate — script approved, no boot (2026-09-24)

Read the full REPORT, script, checker and worker commit `9724d596`; `git show --check` passed. Reran the script self-check (31/31) and checker (42/42), inspected route constants, fresh-output guard before directory creation, the frame-proof stop condition, probe environment and PID/lease cleanup. Independently read the E55D3 `PS2X_PAD_CARD_PROBE` GetDir/mcRead formats and call sites. Script SHA is `f5434273f8384932502f47fc98f80ce5f5c313e3077c41ee8167c520535c476b`. It plans Start tick636, Square820, four separate Down pulses at 1000/1070/1140/1210, a pre-Cross view near1290, one Cross1360 and final frame at/after1460, using fresh empty cards and a ≤500 s diagnostic Mac boot.

**Approved for one Part 2 Mac boot with exactly this script SHA**, a per-pane scratch-write exception confined to `~/dev/ssx3-work/E55D11/`, and no source or seeded-card changes. The worker must preserve pre-Cross and post-Cross full frames and the bounded probe log; a missing pre-Cross frame makes the route ambiguous. Empty-card GetDir calls with `ok=0` prove API reach but not guest-written table bytes; count them separately from copied-table/read evidence. Screen/API outcomes are unobserved until the run, and no speed claim follows from this diagnostic boot.
