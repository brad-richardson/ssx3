# N8D7M12 Part 6M4 orchestrator gate — exact runner stub A (2026-09-24)

Read full REPORT, 23-row check result, source/checker diff and commit `27734506`; `git show --check` clean. The new shared `check_runner_entries` runs after both snapshot and verify scans. It accepts only the pinned 438-byte regular upstream stub and rejects extra paths, changed bytes or a changed kind; Git status alone cannot hide untracked files. The seven new fixture rows pass alongside the previous 16. I independently reran verify on the real private fork probe: 336 fork files, 371 total entries, no add/missing/change, aggregate `70a31d1df4f4fecc467b4a09093a192242fc9b467cccf957780a05eb498331e4`; the sole runtime runner entry has the exact pinned SHA. The fork runner-dir diff to upstream is empty.

**A for the runner guard and bounded real-fork probe.** The next full package preflight may use this collector with explicit file/byte/time caps, but must snapshot the **exact staged build roots**. The real-fork probe includes untracked `local/receipts` that are irrelevant to compilation; use a clean fork source export or remove those from staging before treating its manifest as package provenance. Renderer/Granite, full codegen and JNI roots remain unscanned. No APK, Odin image, GPU cause or speed verdict.
