# AU8 notebook (append-only)

Start: 2026-09-24 21:06:24 EDT (first command). Time box ends 23:06 EDT; write-up by 23:21.

| time | experiment | result |
| --- | --- | --- |
| 21:10 | E1: AU4 Part 2 video audio (same run as `pcsx2-tag1.bin`) vs tag-1 resampled 36→48 k, NCC search (`spk_vs_tag.py`, 0.5 s and 2 s windows over the whole video) | **No alignment**: best NCC 0.10–0.34 anywhere. Spectrogram (`~/dev/ssx3-work/AU8/e1-spec.png`) shows the video's audio full of dropouts (`-turbo` capture, host-clock video) and much less energy above 2 kHz than tag 1. Unusable for a same-run subtraction; need a guest-time SPU2 tap. |
| 21:20 | E2: PCSX2 build with env-gated SPU2 tap (`PCSX2_AU8_DIR`: per 48 kHz sample, core0/1 ADMA input, dry/wet voice mix per core, core0 out, final out) + tag-1 path redirect | First build failed (`Console` undeclared in Mixer.cpp); added include, rebuilt OK. `pcsx2-qt` sha256 `712c132a…01a0` ×2. Hunk `/home/brad/au8/au8-hunk.diff`; pristine copies `/home/brad/au8/pre/`. |
| 21:35 | E3: read SNDDRV `SNDIOP_mix` → `SNDIOP_ee36_iop24_spu48` (AU2 `snddrv-disasm.txt` 0x6c20–0x6c64, 0x5c88–0x6058) | `$fp` = tag-1 payload (`$20 + [$19]` after the record header); per output channel the loop reads 3 EE s16 from `$fp` and advances `$fp += 0x300` per channel; `$21 = payload+0x2fe` = last sample of ch0 is saved as the next tick's interpolation history. **Hypothesis H-planar: tag-1 is planar per tick (384 s16 ch0, then 384 s16 ch1), not 384 interleaved frames.** 3→4 linear interpolation to 512 samples/ch (0x400 bytes) at 48 kHz, plus the IOP slice. |
| 21:38 | E4: `planar.py` on PCSX2 AU4 tag-1 and our AU7 tag-1 (existing WAVs): |Δ| at in-tick frame 191→192 and at the 384-frame record seam, relative to mean |Δ| | Interleaved reading: mid-tick jump **6.63×** (PCSX2) / **7.30×** (ours), seam 2.65× / 1.91×. Planar reading: **1.00 / 1.00** and 1.03 / 0.97; mean |Δ| drops 865→502 (PCSX2). Side RMS 432→1,545 (stereo image of AU6's hard-panned L-only/R-only voices restored). **H-planar supported on both captures.** |
| 21:45 | E5: PCSX2 run au8a (bytesize, T48 route to Select Character, 179 s wall, `-turbo`): tag-1 records (22,261, serials gap-free) + SPU2 tap; last 70 s of tap (`tapcmp.py`) vs tag-1 read planar / interleaved, SNDDRV-style 3→4 linear interpolation | Tap stream RMS: **in0 4761/4552, in1 0, dry0/dry1/wet0/wet1 all 0**, ext0 = out = in0. Interleaved reading vs in0: NCC **0.154** (no match). Planar reading: coarse NCC **0.9997**, mid 0.9993 but **side −0.9987** → channels swapped: first 384-sample block feeds the SPU2 **right** input. Captures sha256: tag `ef283884…aae2`, tap `3eaa7fe5…46eb` (×2 on bytesize), tail `cbf69f7d…bcf2` (matched on mini). |
| 21:50 | E6/E7: planar + swap; interpolation phase φ ∈ {0, 0.25, 0.5} of the 0.75-step grid | φ=0.5 (SNDDRV starts from the saved history sample): **NCC 1.00000, residual/in 0.0002, max |residual| 2 LSB, per-5 s residual RMS 1 LSB over all 70 s**; gain in0→out 0.9998, (out−in)/out 0.0004. **PCSX2's final output = tag-1 planar (block0→R, block1→L) 3→4 linear-resampled, nothing else.** IOP mixer slice silent, no SPU voices, no reverb at Select Character. The "missing IOP mix" lead is refuted; the defect is our interleaved reading of a planar payload. |
| 21:58 | E8: our AU7 runtime tag-1 (`~/dev/ssx3-work/AU7/run/tag1.bin`) vs PCSX2 AU4 tag-1, both re-read planar+swap (`planar.py`), `AU7/midside.py` at AU7's record-aligned lag 107.552 s (= 10,083 records) | Planar: L/R/Mid/Side NCC **1.0000**, diff/ref 0.000, **Side RMS 922** (interleaved misread: 382, +7.7 dB); L 5043 ≠ R 4960 (a real stereo mix; misread L≈R 5002/5002). Our EE mix is already right: fixing the reading is all that's needed. (`ab.py` against the au8 tap: windows mix fade-in and loop repeats; not used for the verdict.) |
| 22:00 | Clips (48 kHz AAC 128k, 59 s, same 5–64 s span of AU7's capture): A current interleaved, B planar fix, C PCSX2 SPU2 final output (au8 tap, different position in the same menu music), A→B 15 s each | `~/dev/ssx3-work/AU8/AU8-{A-ours-current-interleaved,B-ours-planar-fix,C-pcsx2-spu2-output,AB-15s-current-then-fix}.m4a`, 0.56–1.10 MB each. |
