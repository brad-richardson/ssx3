# E55D9 Part 2 orchestrator gate — B, Main Menu route observed (2026-09-24)

Read the full corrected REPORT, run result, bounded stdout/receipts and worker commits `9982a2cf`/`5dd59a9d`. Independently viewed all five curated full-resolution PNGs and SHA-checked each against the receipt. Checked the private boot log's `[padscript]` lines: one Start and four Down presses executed at the planned windows, with all releases and no extra press. The worker corrected its initial false statement that the log lacked press markers. The initial raw stdout commit has inert trailing whitespace; the corrected repo copy trims it and clearly labels the exact raw file kept in private scratch. Final correction commit passes `git show --check`.

The visible Main Menu list is **Single Event, Conquer The Mountain, Multi Play, Previews, Online**. The orange highlight advances one row per Down press through those five items. Every frame also shows a bottom-right **Square: Options** footer prompt. This is an observed button affordance, not an opened Options screen: this run pressed no Square, and no Save/profile item appears in the selected menu list. Thus the predeclared down-only result is **B**. It does not prove that Options/Save is unreachable and says nothing about GetDir/Read execution.

One diagnostic Mac boot used the approved script SHA `c72e5893…10c39f4`; `result.json` records two matching input SHA reads, fork/runner guard, fresh empty cards (unchanged at end), `bound=target`, final frame proof tag1193, 47.729 s wall, 567282-byte log and 12314814-byte frame directory. These are run-control metrics, **not speed**. The recorded runner PID 19213 is gone and both mini P-lane slots are free. Snapshot filename ticks are approximate; five selected images have adjacent metadata and visible one-step changes. No build or device action occurred.

Next: a separate bounded Main Menu boot with one Square press after a settled menu frame, followed by a frame of the resulting screen. Only if that visibly opens Options should a further Save/card detour be designed. Do not repeat a full empty-card I26 race boot to infer save reachability.
