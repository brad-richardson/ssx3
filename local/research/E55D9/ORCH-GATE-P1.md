# E55D9 Part 1 orchestrator gate — run script approved, no menu verdict (2026-09-24)

Read the full corrected REPORT and boot/checker scripts; checked commits `e4801547` and `5b678ea9` with `git show --check`. Independently reran `e55d9_boot.py --self-check` (21/21) and `check.py` (33/33), and SHA-read the final script as `c72e58937e69ded0f63b2ffc780d98a4f9a94341cdf05d2dd5d34863310c39f4`. Confirmed the pinned E55D3 runner/input SHAs and runner-dir guard in the worker's table. No boot, frame, card action, device action or speed measurement occurred; A/B/OTHER menu result is pending.

The initial script created `frames/snap` before checking for existing output and would always refuse a fresh run. The corrected `prepare_lane` checks first; its self-check exercises fresh and reused directories. The initial stop at hash tick1170 might miss the last frame; the corrected script requires a persisted PNG snapshot tagged at or after 1170 or reports `frame_unproven` within the 120 s grace. These checks establish script behavior only. The snapshot tag is the most recent hash tick at copy time, not the exact PNG tick; the orchestrator must view actual frames and verify the button transition after the run.

Approved for **one** E55D9 Part 2 Mac boot using only that script SHA, after a per-pane OpenCode write exception limited to `~/dev/ssx3-work/E55D9/` is in place. The route has START then four separated Down pulses, no Cross or seeded card change, one mini P-lane slot, pinned two-read precheck, recorded-PID cleanup and 500 s wall/120 s progress caps. Any pin, lease, frame proof, or visual ambiguity is OTHER. No menu or save-path claim is approved from Part 1.
