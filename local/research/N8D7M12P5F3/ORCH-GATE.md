# N8D7M12 Part 5F3 orchestrator gate — A for scoped source map and narrowed worker-consumption design (2026-09-24)

Read the full REPORT and commits `c531e5d3` + correction `0ef4b00e` (`git show --check` clean). Reran checker: 156/156 A. Independently read `GS::executeQueuedCommand`, the replay core's marker/drain path, and `GsWorker::threadMain`: the worker pops the FIFO head, handles it, then signals a Fence RPC. This makes a digest updated at **worker consumption** and snapshotted at the Fence a useful sampled receipt. The original report's parser-side digest was non-discriminating for same-stream queue order; the correction replaced it before this gate. The three packaged renderer source SHA pins remain verified from Part 5F2.

**A accepted for the source inventory and a bounded next observable; no GPU mechanism verdict.** Interpret an equal digest as equality of the **hashed command kinds and fields at sampled fences**, not proof of every command's content or GPU execution order. `PrivWrite` carries an opaque callable, so its content is absent; a 32-bit digest can collide. Also, source reads outside the pinned renderer/backend remain informative rather than APK-binary proof, and command handlers may submit GPU work before the replay's explicit readback. A future implementation should state these limits in its receipt and run a same-settings pair before attributing the tick850 split. No build, replay, boot, device action or speed result in this audit.

**2026-09-24 ARCH1 correction:** The REPORT's `c6135b3c…d4fb41f` backend is from N8D7H, not the OFF-pair APK. P3 double-pins `84a13a80…a4645`; its relevant wait/snapshot behavior persists. The worker-consumption design is a limited delivery check under fixed-stream FIFO, not a direct GPU readback discriminator. See `local/research/ARCH1/ORCH-GATE.md`.
