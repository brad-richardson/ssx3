# N8D7M12 Part 5B orchestrator gate — A, offline Odin replay is sparse (2026-09-24)

Read the full worker REPORT, `result.json`, PID log, compressed log, checker and private frame, plus worker commit `41d279e7` (`Orchestrated-By: opencode`). `git show --check` passes and the commit contains text only. Reran `check.py --self-check`: 12/12 A. Independently rehashed the private 688,143-byte PPM `39b70d67…50d41` and 2,686-byte hash file `0e89a493…c82a`; both match the worker's two device/two local reads. I converted the PPM to a temporary PNG without changing pixels and **viewed** it: a mostly black 512×448 race frame with sparse snow/rider fragments, versus the much fuller Mac PPM from the same stream, also viewed. The PNG conversion and original PPM remain outside git. The Odin lease is free and app PID absent after force-stop; the previous `ps2x.env` was restored with matching hashes. One install and one launch only; elapsed13.336s is run control, not a speed number.

The exact 1,100,696,462-byte stream and APK were rehashed; installed APK matched. Replay reached tick2050 with the same packet/priv/transfer/marker counts as the Mac (862958/11499/25445/2050), parallel init_ok=1, no unsupported VRAM I/O or null scanout, a matching 11-field selected descriptor, and numeric control `128/128 PASS`. On Odin the selected input is **14/448 active tiles** (sampled final 24/896); the Mac replay of these bytes is **300/448** (sampled final 567/896). The Odin/Mac sampled `priv` hashes match at all 41 ticks; VRAM hashes match at 3/41 and present hashes at 0/41, with first VRAM/present divergence at tick50. This confirms a sparse **offline** device result, so live EE scheduling is not required for the observed loss under this stream. It does not identify a shader, barrier, copy, driver or renderer root cause.

**A accepted for one on-device replay and viewed frame; graphics cause remains open.** The ON run does not prove default-off behavior or same-binary nonperturbation; 4 MiB on-device SHAs were unavailable, though the 448-tile vectors were parsed and locally hashed. Next: design a bounded same-APK selected/oracle OFF control and a marker-level device/host comparison to separate instrumentation from backend/device execution. No speed or determinism conclusion.

Process note: this worker commit landed while the orchestrator was staging the E55D14 gate; the ensuing main push included `41d279e7` before this gate was written. The worker commit held text only and passed its content checks; this gate records the sequencing exception. Future pushes will recheck the full `origin/main..HEAD` range immediately before push.
