| Handoff | Required boundary |
|---|---|
| Base | Fork `3adc0478b6d2260acdd28a249466f2eef9a20176` unchanged and remote-agreed at open and close. E24 made zero fork source edits, zero fork commits, zero pushes, zero builds and **zero title boots**. |
| **BLOCKER — read this first** | The host restart between E23's close and E24's open wiped `/tmp`, taking **all three protected build trees: 1,725 of 1,725 files, 1,578,996,782 B**, including `ps2EntryRunner` (163,529,696 B, `e462e448…67e22`) and `ps2x_tests` (5,695,128 B, `2152e5ad…f04c0`). No local snapshot exists; a size-exact search of the whole SSD finds no copy; DerivedData holds only CMake probe dirs. **No P-lane boot and no 458-test suite run is possible until a build is restored.** `env-audit.json`. |
| **Nothing irreplaceable was lost** | The fork is unchanged, all 9,457 generated sources and all 21 E18 behavior/input source hashes are equal, and the ELF, the ISO and the game data are intact. The lost trees are **derived output**. What was lost is build time, not information. |
| **The decision the orchestrator owns** | A rebuilt runner will **not** match `e462e448…67e22`. Every E15–E23 receipt is pinned to that hash. Either (a) rebuild and **re-baseline** the protected manifest against the new binary, re-running the carried regression to prove behavioural equality, or (b) treat the pinned-hash chain as closed and open a new one. E24 did not choose; choosing is not a lane's call. Whichever is chosen, note E23's measurement that a single thin-LTO **relink** cost 405 s and **2.93 GB of internal volume that was not returned**, and admit against it first. |
| Completed | Checkpoint with every gate marked VERIFIED or BLOCKED; the unloaded and observer-LOADED fixture regressions green on the surviving binaries; the complete-feed cadence reference re-established independently; **objective 3 CLOSED**; **objective 1b CLOSED**; objective 2 closed on its static half and its wait/signal ledger; the extended-watch boot designed, driven, gated and pre-registered. |
| **Objective 1a — STILL OPEN, and it is the only one that needs the boot** | Does the successor descriptor node at `0x548880` move post-park? E23 named this residual; E24 could not close it because the boot was barred. Do **not** treat E23's KILLED verdict as exhaustive until this runs — E23 itself called it "strong but not exhaustive". |
| **Objective 3 — CLOSED. Relay to the I-lane (I23 H2)** | Title movie dims are **512 × 448**. MPEG-2, `profile_and_level_indication=72` (**Main Profile @ Main Level**), `chroma_format` **4:2:0**, `progressive_sequence=1`, square samples (1:1), **29.97 fps** (frame_rate_code 4 = 30000/1001), bit rate **3,836,400 bps**, `vbv_buffer_size_value=112`, no quantiser matrices loaded, colour primaries 5 / transfer 5 / matrix 4, `display_horizontal_size=512` `display_vertical_size=448`. Derived from `sequence_header_code` at offset 0 + `sequence_extension` id 1 at offset 12 of the already-retained payload. Nothing extra was burned. `dims.json`. |
| **V3 vector, both halves delivered** | The standalone replayable 5,040 B is at `local/research/E24/observed/parser-input.bin` (SHA256 `cde8a830…2a1c875a`, FNV64 `0xd2a9588f0e0fd358`, first4 `000001b3`), byte-identical to E22's and E23's copies and to the e23a run-dir copy. `observed-vector.json`. |
| **WARNING the I-lane must carry into any V3 replay** | The vector is **one structurally complete picture with no terminating start code**. A device-side replay will reproduce the host's exact terminal state — bytes consumed, `packets=0` — unless the harness appends a start code or feeds a following chunk. That is a property of the captured bytes, not of any decoder. |
| **New: what the 5,040 B IS** | 35 start codes: sequence header, 3 extensions, user data, GOP header, **1 picture start code**, **28 slice start codes at vertical positions 1..28 contiguous** — exactly `448/16 = 28` macroblock rows. Slices 1..27 are 176 B (two are 177 B); **slice 28 is 176 B, the modal length — not short**. Last non-zero byte at offset 5,024, then **15 zero-stuffing bytes**, and **no `00 00 01` prefix anywhere after the final slice header**. `picture-completeness.json`, `payload-tail.json`. |
| **X, sharpened — carry this** | E22 recorded the deficit as "not computable from this run" and barred importing Q1's `GOP + 5,461`. E24 computes it from the retained bytes, importing nothing: **the missing input is a TERMINATING START CODE, not thousands of bytes of picture data.** X becomes: *why does the guest stop one start code short of terminating a picture it has already fully delivered?* |
| Named limit on that finding | Slice **header** structure and length are measured. Macroblock-level completeness of slice 28 is **not** provable from start-code structure alone and is not claimed. A decoder-side check on the retained vector would settle it and needs no boot. |
| **Objective 1b — CLOSED** | Thread 4 (entry `0x31ac08`, priority 99) parks on semaphore **31**. Its `scheduled` count at the park is **4,467** and semaphore 31's wait tally is **4,467** — equal. Its ~300 wakes per 5 s **are** its own `WaitSema(31)` round trips, driven by **4,466 `iSignalSema`** calls (interrupt context). They touch **nothing** in any watched producer/source window, which is why E23 could not attribute them. What thread 4 touches *between* wake and re-wait is still outside every watched window — the extended watch set would name it. |
| **Errata E24-E1 (correct this in any carried text)** | E23's REPORT and NEXT-BRIEF both give the blocked set as `26/30/32/36` — four ids for five threads. The recorded set is **26/30/31/32/36**. Thread 4 waits on **31**, omitted from both documents. The E24 brief inherited the error. |
| **Objective 2 — the ledger** | thread 2 → sema 26: 737 waits / 736 signals · thread 3 → sema 30: 5 / 4 · thread 4 → sema 31: 4,467 / 4,466 · thread 5 → sema 32: 616 / 615 · thread 6 → sema 36: **1 / 0**. **Every deficit is exactly 1.** Threads 2–5 are workers parked on their next item on semaphores signalled 4 to 4,466 times — **idle, not deadlocked**. |
| **Objective 2 — the exception, enumeration CLOSED** | Semaphore 36 has **never been signalled**. Its id is stored once (`0x3c1e08 sw $v0, 0x1AA4($s0)`, the `CreateSema ret=36` return) and never copied, so every use is findable: across **all 9,457 generated sources** the slot appears in exactly **4** instructions — the store, `0x3c19ec lw $a0` → **WaitSema** (returns to `0x3c19f0`, **exactly** thread 6's recorded park `ra`), `0x3c15c0 lw $a0` → **iSignalSema**, `0x3c2378 lw $a0` → **DeleteSema**. **Exactly one signaller exists in the whole image, and its function has ZERO entries in the 127 MB e23a function log.** The only code that can ever wake thread 6 never executed. `signaller-closure.json`. |
| **Is the MPEG stall a symptom?** | **Not answered, and deliberately not guessed.** The evidence shows **two independent blocked chains** — thread 1 on the host MPEG wait, thread 6 on a semaphore whose sole signaller never runs — and nothing links them in either direction. Resolving it needs `PS2X_DIAG_SEMA=1`, which the boot carries. |
| What still needs the boot, exactly | (1) objective 1a; (2) the **waker `ra` per signal** — the always-on park tally records only the syscall **stub** pc (`0x423dc8` / `0x423dd8`) and cannot name a caller; (3) whether `sub_003C1298`/`0x3c15c0` stays unreached over a longer window. |
| **The boot is pre-built — do not redesign it** | `BOOT-DESIGN.md` + `watch-set.json` + the patched `e24_capture.py`. Watch set **230 entries** vs e23a's 37: the descriptor node array `0x548800`-`0x5489ff` at 8-byte stride (64 — **completely covers E23's named residual**), data-region head (32) and tail (16), body `0xd48848`-`0xd49a73` sampled at stride 128 (37), the **past-end extension** `0xd49af4`-`0xd49b73` (16), staging buffer (32), plus 33 carried. Caps **identical** to e22a/e23a so exactly one variable changes. |
| Why tiered, and the residual it leaves | `diagWatchEmit` (`ps2_runtime.cpp:1309`) runs from **every** guest store and scans the whole vector, so watch count multiplies per-store cost. Fitted on e22a (29 → 7.61 s) and e23a (37 → 8.30 s): `k = 0.0863 s/entry`. 230 entries predicts ~24.9 s span-complete and **~50 s** of post-park window; 630 (full body cover) predicts ~64 s and leaves ~11 s. **Residual: an isolated write shorter than 121 B inside the sampled body span.** A bulk refill cannot hide there; a stray word can. The driver now records span-complete on **every** path so the next lane can refit with a third point. |
| Second instrument, already compiled in | `PS2X_DIAG_SEMA=1` + `PS2X_DIAG_SEMA_S0=1` (`EeScheduler.cpp:156,168`) — one line per signal and wait with id, count transition, waiters, waker thread, waker **pc and `ra`**, inInt, iSafe, invocation kind/depth/cbFunc, target waiter, result. Cached static env checks, nothing printed when unset. **No rebuild and no fork edit.** ~20k lines, ~4 MB against a 256 MiB cap. |
| New gate the driver now has | **Presence before lease.** e23a's driver went straight to `.stat()` and would have raised `FileNotFoundError` deep inside a loop; an absent tree is exactly what stopped E24. The driver now checks every required binary's presence first and names each missing path before the atomic claim. Both E24 gates were **run for real and refused**, with no lease created and no `boot-attempt.json`. |
| Instrument | `parser/e21-parser-observer.dylib` (SHA256 `e4d88fdc…b38e6be`, 52,472 B) REUSED by copy + re-sha, never rebuilt. It owns its interface, so `PS2X_E21_PARSER_DIR`, `PS2X_E21_PROOF` and `# E21 PARSER CLOSURE` stay E21-spelled in any later lane. |
| **New protected-token hazard** | The E23 cadence fixture **binary** survived at `P1/e23-fixtures/complete/binding-test` and owns the footer `# E23 COMPLETE FEED FIXTURE TAIL COMPLETE`. Any `e23`→`eNN` rename must hold **both** out, or the lane points at a non-existent path or asserts on a footer the binary never prints. Same class as the E21 dylib's interface. |
| Rename hazard (errata E23-E1) carried | E24's `e24_rename.py` masks every run of ≥16 hex characters **before** renaming and asserts hex-run equality afterwards — the check E23's normalized-diff proof lacked by construction. `ELF_SHA` came through byte-identical. Reuse this tool rather than re-deriving one. |
| Surviving assets worth knowing about | Both fixture binaries sha-equal on the SSD; the full e23a artifact set including **`ps2_log-e23a-1.txt`, a 127 MB function log** that records every guest function entry for the whole boot. E23 never mined it; it is what settled objective 2's static half. **Mine it before spending a boot on a question it can already answer.** |
| Candidates still KILLED | C2a, C4 (E23), and C1, C3, C5–C9 (E22). E24 re-confirmed the reference they were killed against — `producerFirings=1` at 60/120/180/240 B, both branches, independently on the surviving cadence fixture. Do not re-propose. |
| No target fix authority inferred | E24 isolated no edge, and could not have acted on one: with the suite and runner gone there is no fail-before and no regression. A future fix still needs exactly one demonstrated edge plus its own fail-before and full regression. |
| ABI carry | Caller-owned synchronous delivery, word0-only cbData, v0 discarded, valid-no-input waits, no guest invocation under the MPEG mutex, delete/reset cancellation, stream behavior unchanged. |
| Protected state | **No reclaim authorized and none performed** — in particular nothing was deleted in response to the loss. E24's `P1/e24-*` fixture/tooltmp paths retained (350,224,384 B across 4 paths); E21's, E22's and E23's retained. |
| Probe count | E24 spent **0/1** title boots and **0/1** lease claims. A later brief requires its own fresh admission, T13, lease and caps. |
| Errata carried | E21-E1, E23-E1, E23-E2 unchanged. E24 adds **E24-E1** (the omitted semaphore 31). |
| Source receipts | `REPORT.md`, `BOOT-DESIGN.md`, `CONTRACT.md`, `env-audit.json`, `checkpoint.json`, `dims.json`, `picture-completeness.json`, `payload-tail.json`, `observed-vector.json`, `sema-mine.json`, `signaller-closure.json`, `watch-set.json`, `cadence.json`, `observer-regression.json`, `probe-authorization.json`, `capture-gate-proof.json`, `probe-preclaim-stop.json`, `rename-proof-hexsafe.json`, `tooling-diff.md`, `fix-gate.json`, `final-audit.json`, `observed/`. |

| E24 NEXT-BRIEF TAIL COMPLETE | The host restart wiped all three protected build trees, so the checkpoint went red and the boot was not spent — the brief's own rule. Everything else landed: the title movie dims are **512 × 448** MPEG-2 MP@ML 4:2:0 29.97 fps; the 5,040 B is **one structurally complete picture missing only its terminating start code**, which sharpens X from "missing bytes" to "one missing start code"; thread 4's wakes are sema-31 round trips; the blocked set is 26/30/**31**/32/36 with every deficit exactly 1; and semaphore 36's **sole** signaller in the entire image **never executes**. Objective 1a alone still needs the boot, and that boot is designed, driven, gated and pre-registered. |
|---|---|
