# E55D12 Part 2 orchestrator gate — A, Load game reaches card API (2026-09-24)

Read the full REPORT, corrected receipts and worker commits `c558992d`+`63a34f91`; `git show --check` passed. Reran `check-p2.py` (44/44 PASS), checked the private nine press/nine release pad rows and all five GetDir records, and independently viewed both curated 512×448 frames. The pre-C2 frame shows **Load game** highlighted in the Save/Load submenu; the post-C2 frame shows a **Load game** screen headed “MEMORY CARD slot1” with six `<EMPTY>` rows. Curated PNG/metadata SHAs match receipts. The recorded runner PID53708 is gone, both mini slots are free, and fresh empty card roots remained unchanged. One diagnostic Mac boot stopped at target with frame proof tag1808; wall68.084s, log867385B, frames13644633B and probe511890B are run-control figures, **not speed**.

The first four `getdir ok=0 reason=empty` calls were early boot checks at ticks118/122/126/223. A distinct fifth GetDir at tick1740, 40 ticks after the Load game Cross at1700, used table address `0x00ba5b20` and returned `ok=0 reason=empty`. This meets predeclared **A for a readable Load game result plus post-choice card-API reach**. No table bytes were copied, no mcRead occurred and no save file was loaded; the exact requested pattern and whether a seeded card produces guest-visible bytes remain open.

Next: source-grounded design of one minimal valid seeded-card input for this GetDir path, with a null same-scene control and a changed-card A/A gate before any further boot. Do not infer determinism or saved-game compatibility from the empty-card screen.
