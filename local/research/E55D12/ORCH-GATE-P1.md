# E55D12 Part 1 orchestrator gate — script approved, no boot (2026-09-24)

Read the full corrected REPORT, script/checker and worker commits `d9c41ef6`+`edc951b3`; both pass `git show --check`. Reran script self-check (33/33) and checker (46/46), verified script SHA `9efe732d358a20605fcf9fd88725095dcefd1e15e9e69f5b4a95bc359d6ee8cd`, and inspected the nine-input route, output-reuse guard, frame-proof target and PID/lease cleanup. The route reuses E55D11's proved Start/Square/four-Down/Cross spine, presses one Down at tick1540 after the observed Save/Load submenu settled, captures Load game highlight near1620, presses one Cross at1700 and seeks a result frame at/after1800. Pins, empty cards and the card probe match the prior bounded run. The corrected predeclared A counts any post-choice GetDir/mcRead call as API reach, with `ok=0` and bytes-bearing results distinguished.

**Approved for one Part 2 diagnostic Mac boot with this exact script SHA** and a per-pane write exception confined to `~/dev/ssx3-work/E55D12/`. Require pre-choice Load game highlight and post-choice full frames, exact pad/probe rows, fresh unchanged card roots unless the game itself writes, and both mini slots free afterward. Screen, card-call and byte outcomes remain unobserved until that run. No diagnostic wall time is a speed result.
