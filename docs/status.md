# Status board (compact, authoritative — orchestrator updates each poll)

Milestone: stock SSX 3 gameplay through our PS2 static recomp runtime on Odin.
Rules: `AGENTS.md`. Open work: `docs/todo.md`. Numbers: `docs/numbers-ledger.md`.
Updated 2026-09-24 12:06 EDT on the **Mac mini** (host of record). Resume notes: `docs/orchestration.md` §10.

| Lane | State | Pinned rev | Next action | Blocker / lease |
| --- | --- | --- | --- | --- |
| E (PS2 runtime) | Fold E58 + GB4 + E56 pushed. Race terrain, rider and HUD draw; sky/sun loss, flat textures and dark GS region remain. **E54B/C/D/F2 PASS; E54E FAIL:** shared Count clock; dynamic reach unproved. **E55A2/B2/C2 PASS:** fixed RTC, guest-cycle scheduled-event choice and VBlank XXH64 tap. **E55D4 A/A PASS:** two pinned boots equal through tick2053 (2053 hashes/3996 pad writes). **E55D5 pad-B PASS:** one Cross→Square entry first changes ordered pad write seq3909/vsync2010; VBlank hash first changes tick2011. **E55D6/D7 PARTIAL:** card API call sites and upstream static chain mapped, but no menu-to-card trigger or route; GetDir writes and seeded-card prompt remain conditional. Clean Mac speed remains E58 baseline: menus 0.54×, race 0.13× | fork `ssx3` @ `04f3ace` pushed (I27 iOS-only tip); E55D3 private `bab6eb3` on `ddaee78`; E55D4 `df99252f`, E55D5 `e80163eb`, E55D6 `5f1401a9` + `db2a3455`, E55D7 `586f41af`; canonical `codegen-ssx3` = E54F2 regen | E55D8 discover a real save/login menu route from existing evidence; no repeat I26 empty-card boot. Then detour probe and changed-card baseline only if reachable. Park ExternalWake policy pending a real poster; then sky, E54E diagnosis, INTC 5/7 and E57 speed | — |
| G (GS composite) | **GB6C fold PASS and pushed:** six opt-in GPU commits; matched CPU/GPU replays 578/578 and live paraLLEl race tick2218. Backend remains opt-in; damaged glyphs and dark composite persist. **GB7C2 CPU trace PASS for offscreen composition, displayed transfer OTHER:** tick600 p47176 has 166 changed T4 pixel traces and 9 localized ROI diffs. First following carrier p47240 has 17 stable ROI links but sampled only background pixels old==new; target crop unchanged. **GB7C3 design PARTIAL:** four source pixels traced, projected carrier destinations inside rectangles, but glyph-row sampling, GS storage address and mask/blend state unknown. No displayed glyph producer or paraLLEl cause proved | GB4 private fork `7bd8349`, workers `869f2659`/`57a7de9b`; current fork `04f3ace`, canonical E54F2 | GB7C4 capture an actual glyph-row carrier UV/address and destination old/new before designing a one-pixel perturbation | — |
| N (Android) | **N7** software GS draws the race world; clean speed menus 0.39–0.46×, SC 0.27×, race 0.076× (one run; rider stall check open). **N8D4 same-stream Mac/Odin divergence**, **N8D5G sparse Odin final GPU image**. **N8D7I Odin category A:** tick2050 input/circuit/stage 35 active each, final 63 active. **N8D7L Mac oracle PASS:** fork-table decoder independently matches G43 input/circuit/stage across 448 tiles, 300 active. **N8D7M1 package PASS; N8D7M2 Odin category A PASS:** one install/launch, independent oracle and G43 selected input/circuit/stage identical 448/448, each 15 active; final sampled/raw 29 active, viewed mostly black PNG. Eight control addresses match; values 0/8 versus a different Mac stream. Selected raw snapshot sparse under both decoders; upstream producer/selection unknown, no GS cause or speed claim | N8D7M1 APK `e077bef8…758a1`, runner `f3de999a…c1bf`, Turnip `717812c3…1ac29d`; N8D7L `b5200bf2`, package `1da356b9`, Odin `9804ebb8`, private oracle fork `d1ba1d4`; current fork `04f3ace` pushed | N8D7M3 design a bounded same-run raw-VRAM/selection discriminator from packet/source evidence before another Odin run | Odin lease free; app stopped |
| T (PCSX2 reference) | **T49 PASS**: PCSX2 entry trace showed different VU1 code at 0x10/0x40 and 38 MPG uploads | bytesize WSL (G7 clone + T48/T49 patches) | Keep for post-fix comparisons | — |
| Perf / GS bridge | **PF1 PASS**: Odin 0.36–0.43× in title/menus, GameThread saturated; no Odin race yet (script desync) | N3 APK `69a79e29…` | Mac clean baseline after E32; profileable APK for symbols (N) | — |
| V (storage) | Mini is up. SSD tier-1 cleanup done (free 27 → 369 GB); tier 2 (parked I-lane dirs) now unblocked by the fold | — | Brad decides the GameCube-reserve and personal folders | — |
| I (iOS) | **I27B Simulator viewport PASS, I27A+B folded, I27C iPhone install PASS:** window 874×402, drawable and raylib render 2622×1206; four viewed title/menu/race frames fill intended width and race reached tick 2363. Host suite 585/585. I27C built the same folded source with verified `-O3 -DNDEBUG`, signed and installed it on the paired iPhone only. No iPhone launch or rendering claim. Guest font detail remains coarse; dark race geometry persists. iPad validation awaits unlock | fork `ssx3` `04f3ace` pushed; iPhone signed binary `30bdafdc…2d01b68` installed, no launch | iPad install/run only after unlock; continue GS rendering diagnosis on Odin. Later guest-font detail work if requested | iPad locked |
| A (audio) | **AU6:** matched menu EE PCM loudness equals PCSX2 within +0.014 dB over 59 s. AU5's low capture used a different pad route; six active voices and levels match. Waveform residual ~9.3%, race and host-output loudness remain open | local `au6-snd` `045dd6a` (from AU5) | Brad listens to AU6 capture; then compare exact perceived scene through host output and EE PCM | Brad's listening result |
| W (widescreen) | **W1/W1F2 folded**: native anamorphic 16:9 is the default; 3D proportions right, 2D widens ~33% (Brad accepted). Mac race and Simulator title→race pass; I27C signed iPhone build installed (no launch). | fork `ssx3` `04f3ace` (pushed, includes I27 viewport fix) | iPad validation after unlock; later 2D correction only if requested | iPad locked |
| X (model trial) | **X16 Space Bunny Free:** valid table/anchors, four concrete bit values, four conservative unknowns; LSP empty. **X15B local dense Qwen:** invalid interpretive derivation/budget overrun. Brad enabled both Go Privacy options. **X18 DeepSeek Flash Go:** saved vectors parsed but hidden boundary case failed; X18B follow-up passed strict checker. **N8D7E Muse Contributor Go:** useful nine-row source map/design, but gate corrected compact-page copy, stage-patch attribution and byte arithmetic; pane 2m24/82.7k context versus <35k. No GS verdict | X16 `627dc06`; X18 `a72172d`, X18B `c71d6c7`, N8D7E `40e457d`; audits in `local/research/` | OpenCode Go is standard tier for suitable bounded worker briefs; local Qwen is budget. Orchestrator gates source and numbers; use Sol medium when a task needs it and quota allows | — |
| GameCube | Reserve (`docs/reserve.md`) | — | none | — |

Gate reads 2026-09-22/23 (orchestrator): **G44 PASS** (shadow works; both GS backends draw the same menu incl. the stray sprites, PCSX2 differs → upstream cause). **X1S** A/B arm scored: model beats brief for interpretive reads. **T49 PASS, E37 PASS → root cause**: VU1 code at 0x10/0x40 differs from PCSX2 (`B` there, stale code here) because REF/REFS/REFE tags drop their TTE-embedded VIF codes, losing the game's `MPG` microcode uploads. **E36 PASS** (one wild-bounds loop; provenance next). **T48 PASS** (healthy programs ≤2,090 cycles at SC; its startPCs are ours ÷ 8). **E33 PASS**. Budget truncation shows (498/583 programs/vsync on Select Character) but resuming them issues no extra work: the programs are stuck in loops; next E36 names the loop. **N4 PASS**. Profileable APK; Odin Select Character ~0.2 guest vsyncs/s with VU1 at 94.8% (quad-precision soft-float FMAC); guest code <0.4%. **G45 PASS** (fork `ssx3` branches pushed). **G43 PASS**. Turnip initializes and renders the G13 scene to PSNR 70–78 dB of the Mac; the proprietary driver's output stays blank and its losses vary per run: driver fault, bundle Turnip. **E32 PASS**. Fold pushed ff to `e57b5f8`, 463/463, both boots green, side branches verified content-contained (remote delete awaits Brad). **PF1 PASS**. Odin baseline 0.36× title / 0.43× My Rules, GameThread saturated; launch 6 reached My Rules but no race (wall-clock script desync); next: vsync-keyed pad script. **V2 PASS** (SSD inventory; tier-1 delete awaits Brad). **E31 PASS**. All four bars met on Happiness Rival (e31l): menus respond, load completes, HUD live, timer advances; caveat: 3D renders near-black. Snow Jam stalls at 99%. **G42 PASS (table + stop)**. The Turnip HAL opened, but our `G42HalDevice` omitted `hw_device_t.reserved[12]` and read `GetInstanceProcAddr` at offset 40 instead of 136; the driver question stays open (G43). C1 landed on the system control this time (G41: lost). **T47 PASS**. Reference frames are renderer-independent; by eye the recomp lacks 3D (no rider on Select Character) and misplaces sprite atlas cells. **The E31 race frame is almost black apart from the HUD**: the race runs (timer advances) but the 3D world (terrain textures, sky, rider) doesn't render. **G41 PASS**. Masked PSM1 draws land byte-exact on the Odin, so B's loss depends on pass context; the Mission-2 skip was correct; the build overage (4 Mac builds) is accepted as declared. **N3 PASS**. First native Odin boot of the PS2 recomp reaches the animating title screen (orchestrator viewed on-device screencap `scap-14`); bar (c) accepted on behavioral proxy because the marker is compiled out of release builds. **N2 PASS**. The Android full-title link is green with the codegen-dir port (APK `21a9230c…`, 9,441 functions, 0 undefined); gate exception accepted, because the stub premise was wrong at the old pin. **G40 PASS**. F2 execution-loss: composite draws are accepted with byte-exact input but never write B on Adreno (Mac writes `840cd308…`). The mission-2 skip was correct; the budget overage (3 Mac builds/runs for two hunk-bug repairs) is accepted as declared. **E30 PASS (design)**. The zero-packet stall is a correct parser waiting on chunk 2, which the host latch never requests; the fix + 6 new tests are ready as diffs (harness-proven, both compile under E29's flags). **E29 PASS**. `PS2X_SKIP_MOVIE=1` reaches the rendered SSX 3 title screen ("Press START button"; frame `2ab49bac…` checked by eye). Flag off reproduces e28a 20/20; suite 458/458 with the flag off, 453/458 on (the 5 MPEG tests the bypass suspends). Three movies skipped, `offered=15056 consumed=15056 packets=0`. C3 missed as written (wrong proxy). A2 amendment accepted: the SSD byte caps are logical bytes, because ExFAT allocates 1 MiB per file. **G39 PASS**. Both fixes hold on
current HEAD, 48/48 no-perturbation classes equal. Adoption decision:
carried diffs now, in-clone commit after the storage cutover. **V1 PASS**.
7610/7610 files stable across two passes, restore manifest written, smoke
restore passed. **N1 PASS**. The Android scaffold exists; the logcat pipe is
in code but never built; the entry provider comes from raylib and is
unverified; `PS2X_CD_IMAGE` can't be set on device (H4); the codegen-dir
mechanism isn't on `3adc0478` (H1).
