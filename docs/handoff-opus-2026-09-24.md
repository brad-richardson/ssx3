# Opus orchestrator handoff — 2026-09-24, ~17:45 EDT

Brad requested this handoff as Codex quota runs low. **Do not issue new tasks until the handoff; let the one active worker finish.** Read in order: `~/.config/agents/AGENTS.md` orchestrator sections, repo `AGENTS.md`, all of `docs/orchestration.md`, `docs/status.md`, the N8D7M12 tail of `docs/todo.md`, then this file and `docs/numbers-ledger.md`. User preferences: plain progress updates; delegate longer work; OpenCode Go is standard tier to preserve frontier quota; local Qwen is budget; stop issuing new tasks when Codex weekly quota reaches 5%. User approved stretched 2D widescreen. No upstream contact. Do not close/re-prompt old Claude pane `w2:p1`.

## Right now

- Main pushed through `bbd59da5 [orch] Gate private source transfer and wrapper stop` at handoff preparation; check `git log --oneline -15` for a newer worker commit before committing. Tree was clean then.
- Only worker: Herdr workspace `w2`, pane **`w2:p76`**, tab “N: Android build host”, name `n8d7m12p6m6r`, Muse Spark Contributor via `--kind muse`. Read its tail with `herdr agent read n8d7m12p6m6r --lines 50`. Our orchestrator pane is `w2:p26`. Finished panes were closed. No other live lane. A watcher was running (`WS=w2 MAX=2700 local/tooling/orch/watch.sh`); restart in the new session if needed.
- The worker's first turn committed `743f9449` (source transfer parity, then stopped because the tracked fork lacks Gradle wrapper). I read/gated/pushed it in `bbd59da5`; `local/research/N8D7M12P6M6R/ORCH-GATE.md`. I then steered the **same worker** to double-pin and invoke the prior WSL Gradle wrapper as an external build tool from the new project cwd, leaving the four staged source roots unchanged. Wrapper script SHA `a3648413b47ef77af21d5ebc36c687c7d103aaef3e17f33de7d4f080a6f300a3`; JAR `498495120a03b9a6ab5d155f5de3c8f0d986a449153702fb80fc80e134484f17`; properties `3d91f0932da99885c41e9dc4e85c9f9a2d3bfef4f0ad87473014dc0827a94884`, equal to staged project. `local/research/N8D7M12P6M6R/wrapper-pins.txt` has double reads.
- **The one `assembleRelease` has now succeeded** in 5m34s, 48/48 tasks (`local/research/N8D7M12P6M6R/build2.txt` and private WSL `/home/brad/n8d7m12p6m6/assembleRelease.log`). Worker is auditing compiled inputs, then post-build manifest, APK/native members/Build ID and private Mac APK transfer. **No final package gate yet.** Do not install/launch the new APK until that gate passes.

## Exact input chain and next gate

- P6M5 `b9efb6de`, orchestrator gate `8218f558`: ignored Mac stage `local/research/N8D7M12P6M5/stage/{PS2Recomp,parallel-gs,codegen-ssx3,jniLibs}`; private `source-manifest.json`; 26,211 entries/612,576,408 file bytes; aggregate **`6877de873b197bf915d068d1b03331e6488c529e6bdac64273513f338080316a`**. Fork private HEAD `4fa0df1`, exact upstream runner stub; dirty N8D7F renderer/Granite staged byte-for-byte; codegen external; Turnip source pin `717812c3…54c1ac29d`; HAL staged from old APK member `1b49d27c…cdfc387` (historical HAL source transform remains unknown). WSL fresh root `/home/brad/n8d7m12p6m6` pre-build collector verify matched aggregate with no add/missing/change.
- When worker commits: read its **entire** follow-up REPORT and all bounded receipts; audit exact build argv, external-wrapper pins/cwd, one success, pre/post source manifest equality, compiled file-list evidence, six diagnostics OFF, `PS2X_GS_SHADOW_PARALLEL=ON`, arm64 only, native member double SHAs, runner Build ID and APK double SHA on WSL and Mac. Treat missing compiled-list proof as a real provenance gap, even if APK builds. Read/check final checker, compare ZIP native members independently, confirm no game/binary bytes committed, disk cap and WSL heavy-job cleanup. Gate in `docs/todo.md`, `docs/status.md`, `docs/numbers-ledger.md`; `[orch]` commit with `Orchestrated-By: <orchestrator>` and **explicit commit paths**, push main, close `w2:p76`, restart watcher.
- If package gate passes, next controlled discriminator is one same-settings **Odin replay pair with worker-consumption `GB4_PKTSEQ` enabled**, same new APK/stream/env; compare 41 ordered digest/count rows to each other and the pinned Mac ON control (`local/research/N8D7M12P5F4P2/REPORT.md`). Existing OFF1/OFF2 same-APK runs had guest priv41/41 equal but GPU VRAM16/41 and present25/41 equal; first sampled GPU split tick850. If command digests match but frames differ, stop adding input hashes and inspect downstream renderer state/readback. PrivWrite content is opaque, and hash equality is diagnostic, not proof of identical GPU execution. Follow Odin lease, unlocked/charging battery check, install, and force-stop after **every** run; no iPhone launch.

## Other current facts / user question

- The project-local Android 35 ARM64 emulator is installed under ignored `local/emulator/` (startup instructions there). It booted and installed the old APK, but bundled Turnip could not create a Vulkan device on its virtual GPU. Emulator is stopped; receipt `local/research/NAVD1/REPORT.md`, gate/status pushed `87c76d66`. It helps Android install/startup checks, not an Odin image verdict. A second physical Snapdragon/Adreno phone would be useful only as a later comparison if it can load the same bundled Turnip and expose ADB/logs; a generic emulator or cloud phone is lower value. Turnip is an Adreno Vulkan driver; the project choice is Turnip versus Qualcomm's stock driver. G43's reference scene rendered with Turnip while the stock driver stayed blank, but the current stock-race problem persists under Turnip, so its cause is unproved.
- No active Brad decision blocks package work. Brad will listen to AU6 capture; iPad remains locked for iOS validation. W1 stretched 2D was approved and shipped. Other lanes/parked priorities are in `docs/status.md`/`docs/todo.md`.
- OpenCode Go package worker `fc99c365` stopped before SSH due its global `ssh *` deny despite scoped pane env; gate `15027e8e`. Do not modify global OpenCode config for this. Muse Spark through `--kind muse` has SSH access and is doing the current build. No Codex worker should be launched near quota.
