# Current numbers ledger

One table for every quoted metric. Documents cite rows here instead of
restating values. Status: **live** (trust it) | **contested**
(conflicting evidence, do not quote) | **stale** (superseded config,
re-run before quoting) | **void** (measured broken, never quote).

Rules that keep this table honest (perf-review §1+§7):

- Verdict-grade runs get an exclusive host AND device: no sibling
  builds, runs, or device shells during the window.
- Under any cap, report host CPU-ms per guest frame (or busy fraction)
  as the primary number; speed is pass/fail only (it saturates at 1.00
  and hides wins as idle).
- Uncapped means `EmulationSpeed = 0` (true unlimited), never 10.
- Every race number is screenshot-verified as racing (timer/HUD/slope);
  menu pace is never reported as race pace.
- Every receipt records module sha, backend (true, post-movie-override),
  cap, determinism mode, vertex loader, and validation-layer state.

## Live

| Metric | Value | Date | Device / config | How measured |
| --- | --- | --- | --- | --- |
| Odin race pace, OGL | 0.83× mean / 0.63× low (~50/~38 fps) | 09-17 | Odin 3, m3-menu verified racing (gate→0:07/49 MPH), dual-core detGPU, idle ON, null audio, speed 10 fixed | 4–5 sample window, `pace-hunt/pace-menu-egl-receipts/` |
| Odin race pace, Vulkan (true) | 0.73× mean / 0.64× low (~44/~38 fps) | 09-17 | Same, backend-patched DTM | Same, `pace-menu-vk-receipts/` |
| Odin menu pace | ~1.3–1.4× OGL, ~1.5× VK | 09-17 | Same runs, menu windows | Same receipts |
| Desktop menu ceiling | 3.89× OGL / 4.0× Metal / 6.6× idle-skip | 09-17 | Mac, m3-menu, post-throttle-fix | pace-hunt Phase 1 |
| Throttle proof | speed9: 0.15× pre (= predicted 0.1626×) → 3.66× post; speed10 menus 3.89×; speed1/2 intact; throttle sleep 56% → 0% | 09-17 | Desktop + Odin | `1f1bc2d` |
| FIFO fix | Control panics sample 140 (tombstoned GatherPipe stack); fix runs clean to completion, zero tombstones; desktop baseline panics ~129 s, fix exits 0 at 200 s; movie A/B 2234-prefix identical | 09-17 | Odin + desktop | `309f639`, `panic-fix/` |
| Pinned PAUSE tail, NOT race (emu→7, vid→6) | 1.974× (+25%) / 14.93 ms/frame (−19.5%) vs 1.578×/18.52 unpinned; samples 120–270 are the scripted post-race pause menu | 09-17 | Odin 3 (2+6, prime cpu6–7 @4.32GHz), m3-menu EGL, speed 0 | `23c5b86`, `affinity/` (pin2/both agree; flag replicates manual within 0.6%) |
| Pinned RACE window (gate→last racing shot) | HUD pace base 0.938 → pin2 1.000 / both 1.071 / fd-pin 1.00 (±20%: 5 s CPU ticks over 14–16 s windows); lows 0.63–0.70 → 0.94+ are the LOAD phase, not racing | 09-17 | Same runs | `affinity-summary.json` results.race_hud_pace; "remainder is GPU-driver-bound" there is an inference from the desktop OGL det-GPU profile, contradicted by M7 on the Odin (emu thread ~98% busy, video 15% of samples) — treat as unproven |
| Pinned load dip | 0.70 → 1.15 (+64%); lows 0.63–0.70 → 0.94+ | 09-17 | Same | Same; FD alone +37%, FD+pin stacks ≈1.41× |
| Priority / quarantine | renice −10: nil; emu→cpu0 control: −41% tail | 09-17 | Same | Same (contaminated pin1 kept as negative control) |
| Capped/uncapped reconciliation | RESOLVED: capped saturates 1.0 while sub-capability dips run identically in both regimes — content + cap saturation, no DVFS paradox | 09-17 | Odin, m3-menu capped arm + busy fraction | Affinity hunt |
| Snow scorer calibration | clean ≤0.05 (iPad) / ≤0.08 (Odin menu); corrupt ≥0.17 transition / ≥0.38 full; threshold 0.12 | 09-17 | Archived shots | `tools/gamecube_snow_check.py` |
| Onscreen trial | 25/25 presented + correlated | 09-17 | Odin onscreen | `8df3c42` |
| FP-unavailable fault rate, desktop | ~1,640/s in-race (41,022 in a 25 s race window); ~610–690/s whole-run average (menus dilute) | 09-17 | Mac, C backend module, `hle_vectors=1` (syscall vector already HLE'd) | `native_exc` counters in `android-spike/codegen/` ab2 receipts and RESULTS.md, read by M2; per-fault host cost NOT yet measured (M2b); Odin rate NOT yet measured (D1b dispatch-samples arm) |
| Upstream native-ABI module has no runtime (M1c) | Native-ABI flavor LINKED (961 MB dylib, 0 undefined) but the upstream runner rejects it: it only accepts `ModernGekkoModuleDesc` via `staticrecomp_get_module`; no consumer of the native module ABI exists anywhere in the upstream checkout | 09-18 | Mac, upstream trees on the SSD | `local/research/M1/REPORT.md` Part 3; using that ABI means writing the runtime side (S1-class), not a link fix |
| Upstream runner limits (M1c) | No movie playback, no working frame dump on Metal (`DumpFramesAsImages` honoured but inert), no `--config`; boot-only evidence is counters | 09-18 | Same | Same; M1d rebuilds this repo's patched runner on upstream to get movies + screenshots |
| Repin port (S1) | `local/research/S1/moderngekko-platform.upstream.patch`: this repo's outer platform patch rebased onto upstream ModernGekko, +254/−19 over 4 files (9 conflicts resolved; upstream factored SHA-256 into `sha256.hpp`), applies to pristine upstream with no fuzz and builds with zero fixes; movie playback, assets cache and HLE hooks verified live | 09-18 | Mac, `m1d-src`/`m1d-build` on the SSD | `local/research/S1/REPORT.md` §1–2; the CPU ABI gap below is the other half of a repin |
| CPU ABI gap | Repo pin: `cpu_abi=3`, `sizeof(CPUState)=3528`; upstream: `cpu_abi=4`, 3536. The pinned C module is rejected by the upstream runner before any code runs; upstream-generated modules (C and LLVM) pass | 09-18 | Same | Same §3; a same-runtime pinned-vs-upstream codegen A/B is impossible without regenerating on the new ABI |
| Upstream C module on the patched-upstream runtime, desktop | 10.23 ms CPU per guest frame capped 1.0 (speed 1.0095), 9.96 uncapped (1.64× headroom), race verified (HUD 0:37, 1st/6, 75 MPH); two legs agree within 2.7% | 09-18 | Mac Air, `det-record-1.dtm`, single core, Metal, NO 10-min idles (legs back to back), screenshot cadence 12 | Same §4; provisional absolute numbers; no pinned-C leg on the same runtime (ABI gap) |
| Upstream LLVM module under our automation stack | NEVER RACES: 0.184–0.205× over 200 s, 0.119× mean over 900 s degrading to 0.0094× at the end (reaches briefing, never rides); `native_exc` 3,372,844 tracks `hle_vectors` 3,318,322 almost 1:1 (the C module takes the syscall vector once, native_exc 117 K); `SSX3_HOST_HLE=0` zeroes both counters and leaves speed at 0.184 | 09-18 | Same runtime, modules (a) LLVM recompcore flavor 270 MB | Same §4–5; the guest-exception / MSR[FP] seam M1c flagged; LLVM route PARKED (see todo) |
| Upstream LLVM module boots (M1b) | recompcore-flavor LLVM module (14,419 objects, 270 MB dylib, 54 min generation at -j8) linked via the module template plus a one-function shim and ran 155 s on Metal under the upstream runner: native cycles 4.72 G, fallback 0, native_exc 0, exit 0 | 09-18 | Mac, `/Volumes/Extreme SSD/upstream-review/build-m1` (Release -O3), LLVM 23 + compat patch | `local/research/M1/REPORT.md` Part 2; NOT visually verified (no screen capture from any agent context), no fps number, host under load ~40 during the run; ABI stats: this flavor is 1,616 native / 12,803 compat functions, so it carries little of the native-ABI benefit |
| Upstream LLVM module, native-ABI flavor | `--runtime moderngekko` flavor: 14,367 native / 52 compat functions, 63,583 direct call edges; generated (66 min) but NO in-tree link path (template parses recompcore headers only) | 09-18 | Same | Same; linking it is S1/M1c work |
| LSE / no-outline-atomics runtime (M4) | INCONCLUSIVE: emu ms/frame base 11.76–12.30 (×3) vs LSE 10.97–12.77 (×2), direction flips between repeats; video 3.62–3.75 vs 3.46–3.77; nm shows 0 `__aarch64_*` outline atomics. Window caveat: M4's analysis window (samples 43–55) is the scripted PAUSE MENU (anchored shots), the race is ~15 HUD s earlier; flags landed for hygiene (5cc328b), not as a measured win | 09-18 | Odin 3 stock, pinned, uncapped, m3-menu, `aff-tpl` | `android-spike/M4/REPORT.md` §3; LSE arms have zero emulator screenshots (dead shot path in binaries built from the pristine `m4-src` tree) |
| Shader cache persistence (M4) | Nothing is ever written: no `Cache/` in any user dir; the only "cache" log lines are the asset-list metadata cache. No warm/cold A/B possible; race-start dip is content load | 09-18 | Same | Same §2 |
| Per-callback update/render split on Odin | STILL OPEN: FIFO-fixed trial relink (220488ca) survives the race but the trial skips at engage (`invalid_immediate_copy_setup`, status 1→4 at wall 45.01) under the pace template; needs the trial's own template (immediate XFB) or a simpleperf race profile (D6) | 09-18 | Same | Same §4 |
| FP-unavailable HLE (S3), landed 1aa479c | Every in-race FP fault emulated on the host (`hle_fp` 412k–445k, `native_exc` → 1, rejects 0); desktop strict determinism gate PASSED (fingerprint match, 6,305-callback common riding prefix); Odin emu ms/frame Δ: −0.12/−0.09/−1.66/−0.11 uncapped pairs, +0.20 capped, against a 0.47–1.51 ms/f run-to-run spread — **inside the noise floor**. D5's 5.7% was the handler's inherent FPU context-copy work, which the HLE still performs, not trap overhead | 09-18 | Odin 3 stock, D1 launch shape, m4-src-derived binaries (no screenshots; metric-pattern window); desktop det-record-1 capped 1.0 | `local/research/S3/REPORT.md` §5–6; compile-time default OFF, env `SSX_HLE_FP_UNAVAILABLE=1`; M2 fix A landed alongside (f58470d) |
| Odin in-race exception attribution (D5) | 100% vector 0x800 (FP-unavailable): 36,121/s over the HUD-timed race window uncapped, mean 1,408 ns raise→rfi-return = **5.68% of race emu-thread CPU**; capped 1.0: 8,693/s, 2,140 ns, 3.78%; menus 1,385/s. Two faulting PCs (0x8026e1a0 `lfd`, 0x80264b48 `stfd f31` prologue) carry 59% with identical counts (a thread-pair ping-pong); nearly all faulting words are FP loads/stores | 09-18 | Odin 3 stock, pinned, D1 launch shape, instrumented m4-src build (temporary, `D5/instrument.patch`) | `android-spike/D5/REPORT.md`; fault count per guest second is host-timing dependent (capped full run 185k vs uncapped race alone ~397k); race verified by metric pattern vs D1b (m4-src builds record no screenshots: `!headless` gate at `dolphin_runtime.cpp:488`) |
| Stock-track corpus census (M3), desktop | 13 of 17 courses ride; render CPU median (ms) / update (ms): Crow's Nest 23.26/8.07, Launch Time 23.23/7.96, R&B 22.76/8.46, Gravitude 21.54/8.38, Kick Doubt 21.33/8.02, Much-2-Much 20.97/8.54, Style Mile 17.87/7.20, Ruthless Ridge 14.80/7.62, Schizophrenia 9.37/3.57, Metro-City 8.41/5.23, The Junction 7.12/3.25, Intimidator 6.69/3.14, **Snow Jam 6.30/3.08 (the lightest course in the corpus)**. Heaviest that rides: Crow's Nest, 3.7× Snow Jam's render and 2.6× its update ON THE DESKTOP SINGLE-CORE PLAYER, where the render callback includes the whole Metal backend (draw-count bound: draws p95 1027 vs 638). On the Odin (dual-core) the emu-thread cost is nearly track-independent — see "Odin corpus budget (M3b)". Part 2 added The Throne 8.05/2.86, Ruthless 7.59/2.87 (draws p95 6518), Happiness 6.30/2.66, Perpendiculous 5.55/2.77: 17/17 measured | 09-18 | Mac, census player (probe + per-present draw hook), 640x528 immediate XFB, single core, Metal validation OFF, 180 s legs, local stock game, load 2.8–8.0 at leg start (M1d build overlapped) | `local/research/M3/REPORT.md` step 2 + Part 2 B, `census-table.json`; the desktop ranking is a draw-count ranking, not an Odin emu-thread ranking |
| Corpus movies (M3) | `local/research/M3/movies/m3-snow-jam-3min.dtm` (sha 563b3b46…, 18,022 frames, strict compare 1325/1325 identical, gate passed); `m3-crows-nest-3min.dtm` (sha d80a1050…, 10,942 frames, results screen reached; strict dispatch compare diverges across plays with equal rider-state transitions — dual-core interleave, trajectory equal). Both bake dual-core, immediate XFB, FastDisc, backend Metal | 09-18 | Mac, production runner 24c2e26c, recorded with `--cpu-thread` | Same, step 4; Odin controls (step 5) not run yet (device busy, pace binary not on device) |
| Odin race-window emu-thread profile, stock (D6) | guest module 74.6/74.9%; dispatcher + core 13.2/13.0% (Run 3.6, HookExternalWrite 2.4, chassis_dispatch 1.7, CoreTiming::Advance ~3% of Run's children); FIFO/GatherPipe 4.5/4.4%; libc+kernel 2.4%; atomics 2.1% (`__aarch64_swp4_acq_rel` 1.17); JIT/Poison 0.3%; explicit exception frames 0.02% (the FP storm's cost sits inside guest handler chunks + Run). Top chunks: func_802197A0 8.1/8.4%, func_8022D7A0 6.3/6.4%, func_801097A0 3.1% — the first two are the codegen-spike chunks; func_802197A0's callees are the FP helper set (`ppc_fcmp` 4.9%, `dolrecomp_f32_from_bits_slow` 3.0% of its subtree; `f32_from_bits_slow` 1.28% flat) | 09-18 | Odin 3 stock, pace binary 84de2c22, simpleperf cpu-clock 4 kHz on the emu TID, gate→pause bracket (≤2–4 s pause sliver), two arms agree within 0.3 points | `android-spike/D6/REPORT.md` symbol/grouping tables and caller chains; kernel symbols unresolved (kptr_restrict) |
| Host replay capacity, Odin EGL (D2) — PARTIAL, unverified | 30 replay rows before the process died (no `done`/re-hash): wall per replay median 0.52–0.59 ms (min 0.49, max 0.69), thread CPU median 0.51–0.58 ms, frames 391–469 KB with 2.9–3.6k memory updates; stable, no degradation before death. Death signature deterministic across two builds (~100 s host, torn stdio line); candidate cause: GPFifo gather-pipe accumulator not saved/restored around the bulk section (D1 tombstones show SIGTRAP in GatherPipe on live resume) | 09-18 | Odin 3 stock, EGL, `d2single` template (Android forces dual-core: GameINI CPUThread ignored), m3-snow-jam-3min, pinned | `android-spike/D2/REPORT.md` "Odin EGL outcome"; NOT a capacity table until D2b verifies `done`; Vulkan unattempted |
| Host replay capacity, Odin EGL (D2b) — 27 replays, unverified | With the gather-pipe drain/snapshot and per-row flush: wall per replay median 2.03 ms (p95 2.3–2.4, min 1.96), thread CPU median 2.0 ms, frames 467–479 KB with 3.7–3.9k memory updates, two arms agree; the process dies mid-loop at replay 27 with SIGSEGV in `LoadIndexedXF ← RunFifo<false>` (identical stack, two frames): the replayed stream references CP/XF indexed-array state that drifts across re-executions. `done` never verified; Vulkan not run | 09-18 | Odin 3 stock, EGL, `d2single`, pinned, trial-egl12 6b883e00, launch temps 38 °C | `android-spike/D2/REPORT.md` Part 2; supersedes the 0.5 ms partial (which lacked the pipe restore); under the S2 gate (≤6 ms) if per-replay restore fixes the death — S2's first milestone |
| Host replay capacity, desktop (D2) | 200 back-to-back replays of one captured race frame (268,836 GP bytes, 2,455 memory updates) at the idle seam: wall per replay median 10.07 ms (p95 16.9, min 3.25, max 25.4) with `Flush()` + `WaitForGPUIdle()` after each (CPU and GPU serialized, no pipelining); thread CPU per replay median 3.26 ms (p95 4.74); watched window unchanged, guest rides on cleanly, zero GPU command errors | 09-18 | Mac Air, Metal, 640x528, single core, immediate XFB suppressed during the sequence, research header `local/research/D2/d2_replay_capacity.h` (env-gated, not shipped) | `android-spike/D2/REPORT.md` desktop table; the Odin EGL/Vulkan half decides S2 (plan gate: ≤6 ms per replay on the video thread) |
| Odin corpus budget (M3b), stock, pinned | 3-min movies, uncapped: Snow Jam pace 1.44/1.46× full race, emu 10.9/10.8 ms per guest frame, video 3.2 ms; Crow's Nest pace 1.39/1.36×, emu 12.0/12.2 ms, video 3.5/3.6 ms — the heaviest track costs only ~11% more emu-thread CPU per frame than the lightest on the device (dual-core: the desktop's 3.7× was the Metal backend on the same thread). Capped 1.0: Snow Jam holds 1.00× with cpu7 parked at 3.28 GHz the whole run; **Crow's Nest opens at 0.82–0.84× for its first 51 HUD seconds (below real time) then locks 1.00×**, cpu7 mean 3.83 / min 3.28 GHz — under the cap the governor settles at the 3.28 GHz floor and the heavy opening cannot keep 60 fps | 09-18 | Odin 3 stock, pace binary 84de2c22, `aff-tpl`/`m6h`, pinned, HUD-anchored windows (157 shots per arm), `m3-snow-jam-3min.dtm` / `m3-crows-nest-3min.dtm` | `local/research/M3/REPORT.md` Part 2 A; snow-cap partial (movie exhausted at HUD 3:33; the 330 s timeout is short for a 4:11 race at 1.0×); crows a/b/cap reproduce identical guest states at identical HUD times |
| Odin race pace, stock vs underclock (D1b) | HUD pace 1.273×/1.273× stock (base-stock-a/b) vs 1.071×/1.071× underclock (base-a/b); emu busy 0.92–0.93 vs 0.88–0.89; video busy 0.39 vs 0.26. +41% clock bought +19% pace | 09-17 | Odin 3 stock (cpu7 4.32 GHz), m3-menu EGL dual-core det-GPU, pinned emu→7 video→6, uncapped, perf mode 1 | `android-spike/D1/REPORT.md` Part 2 table; pace is HUD-seconds/wall over 11–14 s windows (1 s quantization ≈ ±9%); the table's "emu ms/f" column is inflated by 5 s tick bracketing — quote busy fractions, not it |
| Odin determinism tax (race) | det-none 1.364×/1.364× vs det-GPU 1.273×/1.273× (+7%); boot→gate 19 s vs 35 s (menus much faster); trajectory identical | 09-17 | Same, `user-aff-detnone` template | Same; every movie-driven Odin race number carries this ~7% harness tax |
| Odin Null video backend | 1.25×/1.273× = base; video busy 0.39 = base; screenshots still render | 09-17 | Same, `--graphics Null` | Same; the video thread's cost is CPU-side FIFO decode, not the GPU driver — retires the "GPU-driver-bound" inference for good |
| Odin capped 1.0 budget, stock | emu busy 0.58, video 0.27 at 60 Hz → ≈9.7 ms / 4.5 ms CPU per guest frame (cpu7 mean 3.79 GHz: DVFS idles down under the cap; ≈8.6 ms emu at a held 4.3 GHz) | 09-17 | Same, `user-m6h` (EmulationSpeed 1.0) | Same; 120 Hz sim on the emu thread needs ≤8.33 ms at 100% busy — F route is ~1.05–1.2× short before codegen wins |
| Odin in-race native exception rate | native_exc 35,646/s in-race (base-stock-a − menu-pre over 11 s); menus/boot ≈1,550/s; hook_fb 38,861/s tracks it 1:1; dispatch samples 0.000% at 0x800/0x500/0x900 | 09-17 | Same | Same, "Derived numbers"; `m_native_exceptions` counts every dispatch where the module raised a guest exception — vector NOT attributed on the Odin (D5); 20× the desktop's 1,640/s |
| Odin shader cache | No `Cache/` dir after a 330 s graceful stop; zero shader/cache log lines | 09-17 | Same, `cache-late` | Same; race-start dip at stock is small (min 1.185–1.23× vs 1.273× steady) |
| Odin stock thermals | cpu-1-1-1 reads exactly 104.7 °C on 7 arms (reading clamp; 46.5 idle); cpu7 never below 3.28 GHz, zero throttle ticks; capped1x peaks 94.6 | 09-17 | Same | Same; Sport fan not needed for correctness of these numbers |
| Odin per-callback update/render split, stock (D6) | update CPU 3.19 ms med / 3.69 p95 (wall 3.20 / 3.73, n=707); render CPU 7.93 med / 11.00 p95 (wall 8.63 / 12.51, max 20.7, n=1010 incl. 303 blended extras) — 11.1 ms CPU per 60 Hz frame; render alone is 95% of a 120 Hz budget | 09-18 | Odin 3 stock, FIFO-fixed trial binary 220488ca, `m6h` + `m3-menu-imm.dtm`, trial at=112 s for 12 s, capped 1.0, guest-clean (all updates position_changed) | `android-spike/D6/REPORT.md` probe table; trial engages only with the imm movie + m6h template (the `aff-tpl`/m3-menu shape skips with `invalid_immediate_copy_setup`); no screenshots from that binary (dead shot path), race anchored by the D1 capped1x mapping + engagement rows |
| Android repin A/B (S1b, run by S1c): upstream C backend on the repinned Android stack vs the pinned stack, each on its own runtime | NO WIN for the repin: repinned emu ms/frame ≥ pinned on every pair — uncapped b +17.7% D1-med / +20.5% HUD-total (identical 0:00→0:10 spans), uncapped a common-span +10.5%, the one clean boosted capped pair a wash (+6.2% D1-med vs −4% totals, inside the ±5% estimator noise), Crow's Nest +3.1% / +7.1% but confounded by divergent race states (3RD vs 2ND at HUD 0:01:30). Pinned executes MORE native instructions for the same movie (848M vs 734M uncapped, 1111M vs 1046M capped, 2271M vs 2197M cn) with ~6× more hook fallbacks (388k vs 64k) and is still faster; video ms/frame matches within ~5% on every pair (same OGL path) | 09-18 | Odin 3, D1 launch shape, screenshot-anchored race windows, D1 sampler math; the six early arms ran with cpu6/7 parked at 1.02 GHz (battery-collapsed at 2–3%, discharging: SUSPECT, relative A/B only); boosted arms launched ≥20% charging, cpu7 3.28–4.32 GHz, win tmax 98–104 °C | `local/research/S1b/REPORT.md` A/B table + observations, `S1b/s1b-analysis.json`; the throttled pinned-cap is superseded by pinned-cap2; repin PARKED (todo) |
| PS2 throughput gate: stock SSX 3 (SLUS-20772) uncapped in NetherSX2 on the Odin, Snow Jam race | **4.2–4.4× real time in the race** (424% / 254 fps riding, 441% / 265 fps airborne; menu 812%). Per-frame thread times from the OSD: EE 2.94 / 2.48 ms (74.8 / 65.6% of its thread), GS 2.39 / 1.45 ms (60.7 / 38.4%), VU 2.36 / 1.55 ms (60.0 / 41.1%), GPU 1.12 / 0.68 ms (28.4 / 18.0%); whole-emulator frame time avg 3.94 / 3.78 ms (min 1.80 / 0.79, max 11.53 / 8.28). **Low point = a six-rider pack start (second race, 00:00:01): 158% / 94.6 fps, EE 6.46 ms (61.1%), GS 4.51 ms, VU 4.03 ms, GPU 1.34 ms, frame avg 10.57 ms, max 13.63 ms** — the EE thread is at 78% of a 120 Hz budget there; the user also saw texture streaming lag behind at 4× (uncapped is not a fair ride; a 2× capped rerun is the fair form). **2.5× capped rerun (NominalScalar 2.5, limiter on): open riding HOLDS the cap at 253% / 151.6 fps in every riding frame (frame avg 6.59–6.60 ms, max 8.69–8.71 ms; EE 4.5–4.8 ms = 68–73% of the 6.67 ms period, GS 2.9–3.0, VU 2.3–2.5, GPU 1.36–1.37 ms) with cpu7 parked at 3.28 GHz the whole run (same DVFS floor as our capped GameCube runs); the six-rider start-gate panel dips to 202% (EE 4.79 ms, frame avg 8.26, max 10.61); the intro flyover holds 251%.** Note EE% tracks the period, not a fixed cost, so the uncapped throughput and the capped sustain are the reliable reads, not the EE ms at a cap. Against a 120 Hz budget of 8.33 ms the EE thread alone has ~2.8–3.4× headroom under a JIT, with GS and VU on their own threads | 09-18 | NetherSX2 v2.2n-4248 (AetherSX2 package), Vulkan renderer 1× native (512x448 NTSC interlaced field), MTVU on, EE cycle rate 100% / skip 0, frame limiter + vsync off via the per-game ini, widescreen patch on; Odin on battery (73%, unplugged by the user), performance_mode 1, cpu7 3.28–4.32 GHz samples; two OSD frames 10 s apart, user riding by hand | `local/research/ps2-gate/shots/*.png` (OSD frames), `08FFF00D.ini` (settings used) and `.orig`; JIT emulator numbers, NOT a static-recomp measurement — a recomp is normally faster on EE/VU but PS2Recomp's GS is a CPU software backend today |
| Host replay capacity, Odin EGL (S2, fixed) — 200/200 replays, `done` verified, two arms | **Wall per replay median 0.665 / 0.866 ms** (p95 0.729 / 0.963, min 0.638 / 0.826, max 0.874 / 1.077), thread CPU median 0.660 / 0.860 ms, 200-replay sequence 135 / 175 ms, no tombstone; phases (a) mem-restore 0.164, CP 0.001, preprocess 0.035, run 0.460, sync 0.000 ms; both arms det=1 dual=1 mask_bp=2 unknown=0, walk 100%; a no-op `XFReplay::g_transform` hook adds nothing measurable | 09-18 | Odin 3 stock, EGL, `d2single`, emu=80/video=40, present suppressed, `Flush`+`WaitForGPUIdle` per replay, trial-egl14/15 header 31ffc39d, launch temps 50/47 °C, sampler max cpu 104 °C, cpu7 ≥3.28 GHz; no screenshots (m4-src trial), so neither arm is HUD-verified as racing | `local/research/S2/REPORT.md` Part 3b; supersedes the D2b 2.03 ms / death-at-27 row: that death was the deterministic dual-core path popping indexed-XF bytes from the 2 MiB FIFO aux buffer that only the paired preprocess pass fills (2 MiB / 79 KiB = 26.5 replays), not CP/XF drift, and D2b's 26 replays rendered stale aux bytes as matrices. **Capacity gate (≤6 ms) PASSED ~7×.** Vulkan not runnable from the EGL trial (no `libvideovulkan.a`); needs a relink against `core-vk-build` |
| Clock floor under the 60 Hz cap, Crow's Nest (D8): base control vs spin pacer vs ADPF hint | NO LEVER: HUD pace first-60 s 0.946 / 0.946 / 0.961 (full race 0.978 / 0.978 / 0.989), opening segment 0→51 s 0.895 / 0.895 / 0.911, metric mean 1.000 all arms; emu ms/frame 13.06 / 13.46 / 13.15 at busy 0.74–0.77; cpu7 mean 3.39 / 3.73 / 3.43 GHz with min 3.28 GHz (the policy floor) in every arm; run max cpu 86.8 / 96.1 / 91.9 °C — spin only adds heat, ADPF session creation proven in-log changes nothing. M3b's 0.839 opening (quoted) was not reproduced: today's control opens at ~0.90×; conditions differ (cool 42 °C launch, battery 86–98%) | 09-18 | Odin 3 stock, `m6h` capped 1.0, `m3-crows-nest-3min.dtm`, D1 launch shape, affinity emu=80/video=40, 157–160 HUD screenshots per arm (nearest-content anchors ±1–2 s → pace ±0.03–0.05), base over Wi-Fi then USB; d8-src = m4-src + f58470d + 1aa479c hunks, HLE off | `android-spike/D8/REPORT.md` §3–4, `spin.patch`, `adpf.patch`, `shots.patch` (screenshot gate removal, device-verified); arm (a) High-performance skipped (user flag never set). Side finding: `dumpsys battery` status reads 3 while charge-limited (`battery_charging_enforce_level=90`), so "status 2/5" is not a reliable charging test above ~90% |
| Host replay capacity, Odin EGL, third arm (S2b) + Vulkan attempt | s2-egl-c: 200/200 `done`, wall median 0.806 ms (p95 0.874, min 0.781, max 1.013), CPU median 0.802, 200-sequence 164 ms, frame 423 KB / 3,389 updates — three EGL arms now 0.67 / 0.87 / 0.81 ms. Race proof (no screenshots possible): capture at +100.0 s wall on every arm with emu busy 0.93–0.94 in the prior 10 s (menu would be far lower) and race-sized frames. **Vulkan: unmeasurable with the available libs** — `core-vk-build` (2026-09-17 08:51) predates `TryHleFpUnavailable`, so the current TU fails to link; the m4-src TU links but the binary SIGSEGVs in `StaticRecompCore::Run()+1032` ~20 s into boot on both arms | 09-18 | Odin 3 stock, EGL `d2single`, emu=80/video=40, USB adb, battery 93–97% (status 3 while charge-limited, accepted ≥80% cabled), launch temps ≤51 °C | `local/research/S2b/REPORT.md` §1–4, 9; `S2b/trial-vk1` (sha dd5fb34b…) is the crashing Vulkan binary; a Vulkan number needs a fresh `core-vk-build` from the current vendor tree (M4 recipe + `-DHAS_VULKAN`) before a relink |
| PS2Recomp re-spike on the Mac, arm64 (P1) | Reproduced the 09-12 spike exactly: VU1 census 7 microprograms / 7,305 instructions / 85 distinct ops / 0 unimplemented encodings; EE recompile 8143 discovered, 8017 recompiled, 126 stubs, 0 skipped, 0 decode failures, 0 unhandled, 3,594 unresolved JR/JALR warnings. NEW: runtime builds on macOS (raylib/Cocoa), `ps2xTest` 425/425 pass, port runner links (8,144 generated units, unity build), game boots: ELF loads (entry 0x100008), execution starts, `SetupHeap`, then spins forever in `sub_0042C1F0` — the SDK kernel-patch scanner polling syscall 0x83 `FindAddress` (46.4 M balanced calls, pc ∈ {0x42c278, 0x42c28c}, dma=vif=gif=0, no crash) because no kernel image exists to scan. GS sources: gs_frontend 57 KB, gs_cpu_backend 72 KB, gs_memory 15 KB, gif_arbiter 2 KB; only two TODO markers (CLUT cache, RMW lookup) | 09-18 | Apple M4, macOS 27, clang 21, cmake 4.4.3, ninja; ran-j/PS2Recomp `14b1e5cb` (head since 2026-08-18); ISO from the share (`ps2/SSX 3 (USA).iso`, sha1 667c9b2b…), ELF sha1 77114dfd…; no C++ or TOML changes; boot with the ELF alone (no disc tree staged) | `local/research/P1/REPORT.md` Part 2 (§P2-1…P2-13), receipts `/Volumes/Extreme SSD/ps2recomp-spike/P1/`; stall diagnosed by the orchestrator from `output/sub_0042C1F0_0x42c1f0.cpp` + `Kernel/Syscalls/System.cpp:810`; fix = TOML `ret0@0x0042c1f0` (P1b) |
| ps2xGS harness + census on synthetic streams (G0) | Repo `github.com/brad-richardson/ps2xGS` (GPL-3): standalone build of upstream's CPU GS backend, `.gscap` v1 capture format + recording backend, `gsreplay` with a backend factory and per-present diff table, `gsgen` = 78 synthetic captures (433.7 MB, 64×64 presents) across clears, transfers, prims, z-tests, textures, blends, alpha/dest-alpha tests, fog, presentation; `gscensus`; **identity replay CPU→CPU byte-exact on all 78** (max 0, VRAM exact, ~21–23 ms per 64×64 present on the CPU backend); CTest 6/6. Reference caveat: the CPU backend never reads TEX1 and ignores DIMX/DTHE, COLCLAMP, SCANMSK, aa1, fix and the ZTE bit (ZTST applied regardless) | 09-18 | Mac (M4), clang 21, upstream `14b1e5cb`, build on APFS `/tmp/ps2xgs-build`, captures on the SSD `/Volumes/Extreme SSD/ps2xgs/` | `local/research/G0/REPORT.md` (identity table §4, census §5, upstream candidates §9); no game captures yet (Gate C = P1b) |
| PS2Recomp boot ladder after the scanner stub (P1b) | Eight boots, no crashes. Disc tree staged (163 files, 3.0 GB, ELF sha1 matches); `ret0@0x0042c1f0` stubs the scanner; six IOP modules load through the HLE (SIO2MAN, PADMAN, LIBSD, SNDDRV, MCMAN, MCSERV); `sceCdRead` LBN 0x10 served once `PS2X_CD_IMAGE` names the ISO; every remaining missing-target was a global constructor the analyzer had merged into a neighbour: the table at 0x43ce38 has 39 entries (count word, reverse-called via jalr at 0x40fc6c), all 39 verified prologues, split via a function-map CSV (8,017 → 8,244 rows; map-mode recompile 8243/8067/176 stubs/0/0/0). Boot 8 (all fixes): **zero missing targets, no LBN error, then silent for 10 min: no syscall line, no VIF MPG/MSCAL, no GIF kick, no frame, no idle dump (a thread stays runnable), runner ~10% CPU** | 09-18 | Mac (M4), fork `brad-richardson/PS2Recomp` branch `ssx3` at 04905db (4 commits: gitignore, `PS2X_CD_IMAGE` hook, idle-thread dump diagnostic, CD callback HLE — the last unverified: no receipt that it fired); build on APFS `/tmp/p1-link`, receipts on the SSD | `local/research/P1/REPORT.md` Part 3 (§P3-3 ladders, §P3-4 constructor table, §P3-5 patches, §P3-8 could-not-do); next: P1c names the wait with env-gated diagnostics (periodic thread dump, syscall + HLE-stub histograms) |
| PS2Recomp steady-state diagnosis (P1c) | Env-gated diagnostics (`PS2X_DIAG_PERIOD_MS`: periodic thread dump with schedule counts, syscall and call histograms, CD entry logs; 3 commits on the fork) named boot 8's silence: one thread parked in `WaitSema` on semaphore 6, and the writer is the guest's own alarm callback at 0x3e3588, which `SetAlarm` had rejected because the address had no function-table slot (an unsplit prologue, same class as the constructors). Split → boot 2: WaitSema 32 = SignalSema 12 + iSignalSema 20, CD thread created (entry 0x3e3be0, priority 12), `sceCdInitEeCB`, 20 `sceCdRead` (PVD + directory sectors), one more unsplit callee (0x3e3968, a frameless comparator, 1,135 calls once split). Boot 3: **no missing targets, but both threads Dormant within 5 s through `makeDormant` on pc 0, no exit syscall; the CD thread was never scheduled** (its entry 0x3e3be0 sits inside `sub_003E3B00`, and `Thread.cpp:269` refuses entries without a slot). `sceCdCallback` never called by the guest (Fix D idle). Still no VIF/GIF/frame | 09-18 | Mac (M4), fork `ssx3` at dbf0080, map CSV 8,246 rows (2 splits, untracked), 3 boots × 3 min | `local/research/P1/REPORT.md` Part 4 (§P4-2 loop tables + quoted bodies, §P4-3 fixes/ladders, §P4-6); next P1d: code-pointer sweep to split every prologue reachable only through data (constructors, alarm/thread/RPC handlers, comparators) in one pass, strict-return diagnostics for the return-to-0, CD payload check |
| PS2Recomp code-pointer sweep (P1d) | One-pass sweep of the ELF for code addresses held in data words (7,995 distinct) and `lui`/`addiu` pairs (269): 462 already mapped, 2,639 rejected as internal branch targets, **1,024 new function splits** (map 8,246 → 9,270 rows; recompile 9,092 functions, 177 stubs, 0 decode failures, 0 unhandled; all 39 constructors and the 3 known misses found by the sweep as a self-check). Boot: no missing targets; **the CD thread now runs** (one scheduling, then WaitSema on semaphore 26); main thread still Dormant with pc 0 after 22 schedulings. Strict-return build shows one Return-to-0 at startup (bss-clear loop reached from crt0 with ra 0, execution continued) and nothing later, consistent with per-target de-dup hiding the real one. CD raw reads deliver correct bytes (LBN 0x10 = `01 CD001`). Still no VIF/GIF/frame | 09-18 | Mac (M4), fork `ssx3` at f0b5040 (payload log), sweep CSV untracked on the SSD, strict build `/tmp/p1-link-strict`; 50 min of a 5 h box | `local/research/P1/REPORT.md` Part 5 (§P5-2 sweep script + counts, §P5-3 strict receipt + body, §P5-4 payload); next P1e: Dormant-site receipt with dispatch history, un-deduplicated Return reports, name the returner and semaphore 26's owner |
| Onscreen APK parity + display proof attempt (D4 Part 2) | Parity (movie `m3-menu.dtm`, uncapped, pinned emu→cpu7/video→cpu6, screen on): HUD race pace **1.134×** onscreen vs 1.273× headless (D1), emu busy 0.86 vs 0.93, video busy 0.27 vs 0.39, cpu7 4.29 GHz mean, cpu max 103.5 °C, our layer on a **HWC DEVICE overlay**, panel 120 Hz; the run dies at ~74 s in the trial binary's known GatherPipe panic. Display arms (calibrated `at=120`, capped seed, smoothing trial 4 s): 324–327 frames, 91–97 extras, ~81 swaps/s in-window, swap-return spacing med 12.0 / p95 ~17 / p99 17.5–19.7 ms, **distinct display presents unmeasurable: `eglGetFrameTimestampsANDROID` returns nothing on this Adreno path (0 of 7,622 + 978 rows across 6 runs)**; explicit 120 Hz request lands (`OT_FRAMERATE rc=0`, SF column `120.00 ExactOrMultiple`) | 09-18 | Odin 3 unlocked after the 17:27 reboot, USB, battery 97–100 charging; APKs 42c52be2 / 19b4a9c9 (dlopen fix + rebuild, new debug key); `at=128` no longer fires (warm-install timing), `at=120` calibrated | `/Volumes/Extreme SSD/android-spike/D4/REPORT.md` (Parity, Display arms, Timestamps, Could-not-do); nine failed bootstrap attempts kept as receipts; no repo commit; next D4c: count presents with `dumpsys SurfaceFlinger --timestats` per layer (verified working on the device) |
| PS2Recomp return-to-0 named (P1e) | Dormant-site receipt with dispatch history + un-deduplicated Return reports (1 commit): the main thread dies at `0x3dcc7c`, the epilogue of `sub_003DCBD8`, when `ld $ra,0($sp)` (sp 0x1ffff60) reads 0 and `jr $ra` goes to 0. The function was re-entered mid-body at 0x3dcc64 through the runtime's dispatch after the thread switch at StartThread (the C frames are unwound on a switch; guest returns then go through the entry switch), so the ra slot in guest memory is the only carrier and it holds 0. The prologue's delay-slot `sd $ra` is generated correctly (orchestrator check), so the 0 is written by someone else or ra was 0 on entry. Also: the runtime's periodic handler at 0x3e4e8c always returns with sp 0x1ffff20 (a stored handler sp), and invocation stacks come from `reserveAsyncCallbackStack(0x4000)`. Semaphore 26 = the 26th CreateSema (in `sub_003E4040`), signalled from two main-thread sites; the 20 ephemeral semaphores are the SetAlarm/WaitSema handshake | 09-18 | Mac (M4), fork `ssx3` at b15aff0, strict build, 23 min of a 4 h box | `local/research/P1/REPORT.md` Part 6 (§P6-2 returner table + 64-hop chain + body, §P6-3 semaphores, §P6-4 artefact record); next P1f: memory watchpoint on 0x1ffff60 + stack map (thread stacks, invocation stacks, handler sp) to name the writer, then the fix |
| PS2Recomp INTC-stack overwrite fixed (P1f) | Watchpoint on the true ra slot `0x1ffff00`: 32,682 writes in boot 1b, all but 153 from the INTC cause-10 handler prologue (`0x3e4db8`, `sq` s0/s1 storing zeros over thread 1's parked frame from `sp=0x1fffed0`). Fix (`6046260`): `dispatchIrq` + Alarm queue with `sp=0` (reserved top assigned at dequeue); `reserveAsyncCallbackStack` moved from the RAM top to `[0x80000,0x100000)` (below the ELF `PT_LOAD`, the heap base, and every guest thread/CD stack). Boot 2: 165 watch lines, 0 from the handler, prologue save intact, thread 1 `Running` at `0x391330` (`sub_003912A8`), threads 3+4 new; no VIF/GIF/frame/crash | 09-18 | Mac (M4), fork `ssx3` at `6046260`, ~2 h of a 4 h box | `local/research/P1/REPORT.md` Part 7 (orchestrator-verified: counts, tops, fix files, fork push); next P1g: diagnose the `0x391330` park |
| PS2Recomp park at 0x391330 is an MMIO fold (P1g) | Thread 1's post-fix spin is a recompiler bug: the MMIO detector (`elf_analyzer.cpp:427-447`) reads only the LUI high half, folding LUI+ORI accesses to the page base — all 7 `sub_003912A8` MMIO pcs map to `0x10000000`, so the VIF0 DMA kick never runs and the STR poll reads the stuck `0x104` forever. 249/273 TOML `[mmio]` entries fold the same way (7 proven). No boot needed; closed log + ELF + sources fully determine it | 09-18 | Mac (M4), fork `ssx3` at `6046260`, well under a 2 h box | `local/research/P1/REPORT.md` Part 8 (orchestrator-verified: ELF words, TOML, recomp fold, detector, both memory gates, branch arithmetic); next P1h: fix the detector, regenerate, re-boot |
| PCSX2 EE reference is a negative result (P2) | PCSX2 (`1275b25a`) boots the real BIOS (`0xbfc00000`) and HLEs ~14 EE syscalls (no `AddIntcHandler`/`SetAlarm`/CD/RPC threading): interrupt delivery never writes `sp`, and handler/alarm/CD/RPC stacks are all guest-BIOS behavior PCSX2 source never states. P1f's reserved-stack direction stands uncontradicted. Tension (no action): `EENULL`/`EELOAD` (`0x81FC0`/`0x82000`) sit inside P1f's `[0x80000,0x100000)` on real hardware — but no BIOS runs here and the SSX3 ELF has a single `PT_LOAD` at `0x100000` | 09-18 | Mac, PCSX2 clone `/Volumes/Extreme SSD/pcsx2-ref` (source only, never built) | `local/research/P2/REPORT.md` (orchestrator-verified: rev, enum, EELOAD lines) |
| PS2Recomp fork/downstream survey (P3) | 133 forks / 50 PRs enumerated; 16 mechanisms tabled (M1–M14 borrowable, fork LICENSE files near-uniformly GPL-3). Most relevant downstream: bt3-recomp (`4b8a766`, GPL-3, playable, GPU path at ~30 fps). Architectural alternative: N64ModernRuntime message-not-nesting (never invoke game code on event threads; GPL-3 family). Unusable: PSXRecomp (PolyForm Noncommercial), SaturnRecomp (unlicensed). Gap: per-fork diff bodies not preserved (clones persist for a P4 follow-up) | 09-18 | Mac, clones under `/Volumes/Extreme SSD/{fork-survey,q2-recomp-scan,q3-siblings,dobiestation-q4}` | `local/research/P3/REPORT.md` §1–§8 (orchestrator-verified: bt3, N64MR, thetharin + sm2 licenses) |
| Host replay fidelity + fences on desktop (M5) | 200/200 + `done` every run (`det=1 dual=1`, +`det=0` ref). XFB: 1 copy, `0x004dc660` + 573440 B, `xfb_equal=200/200` all runs. Counters: `dpend` 0/0, `pediff`/`vidiff` 0/0 (det=1; `pediff` 0/2 det=0), `dtex` −7…+1 (removals, no accumulation), no `record_flag_set`; `dframe` exactly +1/replay (driver UNRESOLVED — survives the scratch redirect, admits no wall-clock reading). Scratch XFB: `xfb_equal_scratch=200/200`, `live_xfb_untouched=1`, 6 non-XFB copies untouched. Continuation skipped per brief (seam 8121 vs 8142); triple-identical resume hash tabulated without verdict | 09-18 | Mac, Metal, 640x528, `gxbe69-stock`, 8 runs × 240 s | `local/research/M5/REPORT.md` (orchestrator-verified by independent probe parse); next M6: bus split + frame aging + record guard |
| PS2Recomp MMIO fold fixed, ladder advances (P1h) | Analyzer fix `f2149e7` (same-register ORI/ADDIU low-half fold, +28 lines): 245/273 `[mmio]` entries corrected (273→273, 0 added/removed; 4 remaining `0x10000000` proven genuine by hand decode; independent python model 0 mismatches). Regen recompiles clean (9092/177/0/0/0, 57 runner files, all MMIO-constant lines). Boot 1: thread 1 past `0x391344` to `0x375d10` (all 35 blocks, sub_003912A8 frame popped); no new syscalls, no VIF/GIF/frame/crash. New park: GS CSR `(CSR&0xC000)!=0x4000` spin at `0x375d10` | 09-18 | Mac (M4), fork `ssx3` at `f2149e7`, ~35 min of a 4 h box | `local/research/P1/REPORT.md` Part 9 (orchestrator-verified: fork push, TOML, recomp, boot log, park words); next P1i: diagnose the CSR park |
| Fork/downstream borrowable table (P4) | 20 snippets (S1–S20) with full bodies + license re-cites. Highlights: bt3 dynamic MMIO dispatch (TOML-as-boolean — working end state tolerates 160 folded entries); bt3 sync-CD + guest tick pump; M1 empty-transfer STR clear + DMAC-drain-from-pump + VIF IRQ→INTC wiring; M2 nesting guard; M7 DI-gated preemption; M12 syscall-override `return true` (rest of M12 contradicts #214, excluded); Q4 delivery point (`queueInvocation` `:1370-1376`, Sites A/B). Gaps: 7 revs absent (depth-1 clones) | 09-18 | Mac, existing SSD clones, read-only | `local/research/P4/REPORT.md` (orchestrator-verified: bt3 MMIO + CD, M1 dispatch + STR); feeds P1i/P1j |
| PS2Recomp GS-CSR park has no producer (P1i) | Exhaustive `csr` grep: runtime writers touch bits {0, 1, 13} + init-0 + guest merge only — NOTHING ever sets bit 14 or clears bit 15, so the `(CSR&0xC000)==0x4000` exit state is unreachable. Consumer is dynamic `READ64` (`0x375d10` not in `[mmio]`, address computed). Vsync worker touches bit 13 only; present/timer paths CSR-free. Bits 15:14 honestly unidentified-from-tree (no-guess rule). 10 min, no boot | 09-18 | Mac (M4), fork `ssx3` at `f2149e7` | `local/research/P1/REPORT.md` Part 10 (orchestrator-verified: TOML absence, generated line, `0xC000` absence); next P1j: identify bits from public sources, minimal producer, re-boot |
| 360/N64/PSP/Xbox recomp tooling survey (P5) | 32 borrowables (B1–B32) with bodies. HLE tooling: Xenon weak-link override + addr→name map; psprecomp work-list census + deliberate-return miss log + zero ring + compile-free trace + self-test; xboxrecomp ordinal-switch + A/B/C coverage audit + trace budget. Function detection: N64 CreateStatic + LUI/LW/JR register-state jump-table discovery; Xenon BL-sweep + gap-fill + offline switch TOML (hard errors beat `// ERROR` fall-through). GPU: UnleashedRecomp is API-level HLE (no capture stream — validates the ps2xGS harness bet); batching split + hash tables + backend try-list borrowable; xboxrecomp NV2A census/executor maps onto gscensus/loop rows. Testing: annotated-asm + golden-binary execution tests (the LUI+ORI-catching shape); shape assertions for emitters. xboxrecomp is MIT but holds NO SSX3-Xbox specifics (Burnout 3/Halo titles) | 09-18 | Mac, existing SSD clones + new xboxrecomp `6f55eaa`, read-only | `local/research/P5/REPORT.md` (orchestrator-verified: xbox MIT + absence substance, 2 mechanism quotes) |
| PS2Recomp CSR FIFO wait cleared (P1j) | Bits 15:14 = GS FIFO status (`01` empty), reset value, from 4 cited sources (Dobiestation `FIFO_status`, PCSX2 `CSR_FifoState`, gsKit layout, Play! `CSR_FIFO_*`). Fix `8fad69e` (+43/-7): init EMPTY + force read-only on guest CSR writes (game writes `00` via FINISH/VSINT stores at 4 sites), cheat row documented (no occupancy tracking). Boot 1: thread 1 to `0x3e5980` dispatch loop (31/35); FIRST `[cd:callback]` queued+start; thread 5 (WaitSema 30); syscalls `0x15`/`0x17` new; 1 new missing target (`0x3760d0→0x395cf0`); still no VIF/GIF/frame/crash. Tests 424/425, 1 failure proven pre-existing by stash A/B | 09-18 | Mac (M4), fork `ssx3` at `8fad69e`, ~2 h of a 4 h box | `local/research/P1/REPORT.md` Part 11 (orchestrator-verified: fork push, hunks, both FIFO cites, boot receipts); next P1k: dispatch-loop park + missing target |
| ssxdecomp/ssx3 mining (P6) | Same sha1 (`77114dfd…`), 749 symbols + 52 extra labels → 801-row CSV (names/addresses only — repo unlicensed). Coverage 618/9,270 = 6.67%; all 11 ladder addrs UNNAMED. Structure: entry calls `main` (`0x31af80`); GS-wait caller from `cSSXApp` constructor (`jal` verified). Sweep validated: 100% recall on JAL-called named fns, 0 splits (merges only); dup `0x42c1f0` rows found (known issue, deferred). Tooling: sheet→symbols pipeline, objdiff units, fail-fast naming | 09-18 | Mac, clone `/Volumes/Extreme SSD/ssxdecomp-ssx3` @ `9cd4626`, read-only | `local/research/P6/REPORT.md` + `ssx3-decomp-names.csv` (orchestrator-verified: rev, license absence, sha1, counts, caller) |
| PS2Recomp SYNCTASK park diagnosed (P1k, PASS) | Park fn named via P6: `SYNCTASK_run` 16-slot queue (base `0x51ED98`); slots 0–1 fire ~600×/5 s — fills WATCH-traced (`0x31ad20` from `systemInit:0x31af58`, `0x3e33b0` from `sub_003E3020:0x3e3048`, `0x3e4000` del'd pre-fire); driver `sub_003DD1D8` waits on `*(entry+8)` flag (writer unknown); outer caller narrowed to 8 wrappers (sd-watch missed, next receipt `w4 0x1ffe000`); `0x395cf0` = unsplit frameless leaf + 3 unsplit siblings (`0x395c70` already split — found at re-verification); CD callback signals sema 26 but T2 never wakes (no finish line exists); `0x15`/`0x17` one-shot disables. VERIFIED post-recovery 09-19 (orchestrator greps): 33,038 lines, census 15/1/1, sema 26/29/30, watch 32077 (widths 32050/25/2), stub top-3 = p1j, ELF 11/11, fork `8fad69e`+M | 09-18 | Mac (M4), fork `ssx3` @ `8fad69e`, 90 s boot + closed-log analysis | `local/research/P1/REPORT.md` Part 12 |
| Display presents counted via SurfaceFlinger timestats (D4 Part 3) | Per-layer `totalFrames` + present-to-present histogram sampled 1/s (analyzer validated on a static launcher: ~0.7 presents/s). Smoothing trial, capped seed, 4 s window: default APK 81.1 swaps/s → **90.9 presents/s, spacing med 16.5 / p95 16.5 / p99 24.5 ms**; 120 Hz-request APK 82.5 swaps/s → 74.7 presents/s, same spacing (`requestedFrameRate 120 ExactOrMultiple` visible in SF, no effect on cadence); **uncapped, no trial: 101 swaps/s → 83 presents/s whole-run, spacing med 8.5 / p95 24.5 / p99 33.5 ms** (single-vsync cadence reachable). Dropped frames 0 everywhere; our layer on a HWC DEVICE overlay; panel 120 Hz throughout | 09-18 | Odin 3 USB, unlocked, battery 100 charging, thermal gate 50 °C clear; APKs 42c52be2 / 19b4a9c9, module 1c6c89cf; trial binary = the old smoothing trial (not the host-replay route); arm c's emu thread ran unpinned | `/Volumes/Extreme SSD/android-spike/D4/REPORT.md` Part 3 (`part3/sfstats.sh`, `sfstats_analyze.py`); read: the display path can present at 8.3 ms spacing, the old trial's ~81 swaps/s is the limiter, so the 120 proof must be re-run on the host-replay build |
| Odin clock caps | The 3.072 GHz prime / 2.75 GHz mid `scaling_max_freq` caps seen in D1 part 1 were a user-installed underclock tool running by default; removed 09-17 ~21:35, stock caps now read cpu6–7 4.32 GHz, cpu0–5 3.53 GHz, GPU max 1.1 GHz. `performance_mode` 0/1 never changed the caps. **Every Odin number dated 09-17 before ~21:35 (pace hunt, affinity, D1 base-a/base-b) was measured underclocked at ≤3.07 GHz on the emulation core.** D1b re-ran base at stock: see "Odin race pace, stock vs underclock" | 09-17 | Odin 3 | adb sysfs read-back; `android-spike/D1/REPORT.md` Part 1 for the underclocked arm (emu core 3.072 GHz every race sample, cpu zones 74–92 °C) |

## Contested

None currently. (Resolved 09-17: M5-vs-0.83× — see Live "Capped/uncapped
reconciliation".)

## Stale (re-run before quoting)

| Metric | Why stale |
| --- | --- |
| Desktop cost table ("needs 1.3–1.9×") | Predates the FP wave by a day; taken with Metal validation on |
| M5 profile ("CPU core now the frontier") | Module predates Wave B (no fast-FP, no conversion split) |
| "Endian helpers 12.1 → 0.00" | Reads "inlined, now unattributable" — cost moved into `func_*` bodies |
| Host replay milestone 2 item 2 (M6) | Scoped bus: `dafter_live` 0/0, `dpend` 0/0, `dtex` 0/+1, `done` stands, 200/200 scratch-equal; +1 driver NAMED (`Presenter::ImmediateSwap` `Present.cpp:235` via `VideoConfig` after_frame listener, flag re-armed mid-Trigger — code path + elimination, no direct flip observation); guard fires pre-replay-0 (`replay=0 armed=1`, 0 rows, `done` stands, bus restored); continuation hash quintuple-identical `0cbfe49a0d4ee325` (seams differ, comparison skipped per rule) | 09-19 | Mac, Metal, 640x528, `gxbe69-stock`, 5 × 240 s `m6-det` runs (negctrl post-hold on AC) | `local/research/M6/REPORT.md` (orchestrator-verified: header shas, guard event verbatim, analyzer rows reproduced, 5/5 hashes) |
| PS2Recomp missing target split, park holds (P1l, PASS) | 4 CSV splits (9270→9274, recompile 9092→9096, 0/0); boot 9,137 lines, 0 missing-target; thread 1 `0x3e5980` ×14 + `0x3e5440` ×3; outer-caller missed on both `sw`/`sd` addrs (addrs audited correct — mechanism question now); first `[gs:kick]` ×66 = aggressive-logging visibility, not guest change; ~4× rate vs p1k open; tests 424/425 same pre-existing failure | 09-19 | Mac (M4), fork `ssx3` @ `8fad69e` (untouched), fresh `/tmp/p1-link` `0b38f7b6`, 90 s boot | `local/research/P1/REPORT.md` Part 13 (orchestrator-verified: counts, census, ELF spot, regs, binary, fork state) |
| ps2xGS harness sensitivity (G2, PASS) | 20 new captures (98 total, +105 MB); `strict` backend honors 7 ignored fields; cross table 14/98 flipped, all field-mapped, 84 exact; CTest 9/9 (3 new gates); census clean; strict semantics plausible-but-unverified (test instrument, not reference) | 09-19 | Mac, `/tmp/ps2xgs-build`, captures on SSD | `ps2xGS/docs/reports/G2.md` + ssx3 copy (identical; orchestrator-verified: factory, 98 files, CTest re-run, push) |
| PS2Recomp watch-miss mechanism (P1m, PASS) | 17-path audit: driver stores on fires-paths (sd-theory excluded by source); trace 72.3M lines: driver 3940/3939 (~44/s), 0 stub hits = checkpointed depth-0; 7/8 candidates never entered → frame-elsewhere; live caller unknown; Step 2 skipped, probe designed | 09-19 | Mac (M4), closed logs + sources, 0 boots, fork untouched | `local/research/P1/REPORT.md` Part 14 (orchestrator-verified: trace counts, candidate zeros, source lines, fork state) |
| ps2xGS present pins + CLUT/RMW spikes (G3, PASS) | 4 captures pin `fbp==0` fallback (black→ctx0, nonblack→0, multi→first, empty→0); `spike` fork: CLUT-cache + RMW spikes identity-exact on 102/102; CTest 11/11; Submit savings observed (CLUT ~15–20% paletted, RMW ~40% fbmsk); limits disclosed (CLUT memo scope, RMW CT32-only) | 09-19 | Mac, `/tmp/ps2xgs-build`, 719 MB captures on SSD | `ps2xGS/docs/reports/G3.md` + ssx3 copy (identical; orchestrator-verified: md5s, old-98 clean, CTest re-run, push) |
| Host replay milestone 2 item 3, partial (M7, PASS + open Q) | M6 gaps closed: det=0 matches (except `pediff` 0/+2), replay_disabled clean, flip DIRECTLY observed (`trig_imx` 1/1); no-op parity holds; delta applies 152×/replay yet 0/200 frames differ, no drift, guest/event clean — WHY is open (M8) | 09-19 | Mac, Metal, 640x528, 6 × 240 s runs | `local/research/M7/REPORT.md` (orchestrator-verified: shas, analyzer rows, xform_stats verbatim, 6/6 hashes, flip counters) |

## Void (measured at the phantom 1.16 wall — never quote)

fast-FP "0.00 prize" + STOP decision · "every CPU opt ~0%" ·
determinism-off 0.0% · pinning 0.0% · EGL==Vulkan to 4 decimals ·
EFB-2× "full speed" · desktop 1.17 bind + validation-on/off-identical ·
psq wall-clock null · "1.16 is STRUCTURAL". All re-measured or queued
for re-measurement off the wall (perf-review §1, `todo.md` Now).
